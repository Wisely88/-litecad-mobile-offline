/**
 * Resolves PDF export options with package defaults.
 */
export function resolveAcApPdfExportOptions(options) {
    if (options === void 0) { options = {}; }
    return {
        modelSpaceFit: options.modelSpaceFit === 'display' ? 'display' : 'extents',
        exportLayouts: options.exportLayouts !== false,
        textMode: options.textMode === 'vector' ? 'vector' : 'text'
    };
}
//# sourceMappingURL=AcApPdfExportOptions.js.map