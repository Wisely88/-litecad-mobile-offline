var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
import { AcApDocManager, AcApI18n, AcApSettingManager, resolveExportDownloadName } from '@mlightcad/cad-simple-viewer';
import { exportDatabaseToPdf } from '@mlightcad/pdf-renderer';
import { resolveAcApPdfExportOptions } from './AcApPdfExportOptions';
import { resolveViewerTextFont } from './AcApPdfFontResolver';
import { createViewerPdfGlyphProvider } from './AcApPdfGlyphProvider';
/**
 * Converts the current CAD drawing to a vector PDF and downloads it.
 *
 * Framing matches HTML export: `'extents'` fits drawable geometry; `'display'`
 * uses the live camera box. Paper-space pages ignore model-space fit and use
 * their own drawable extents.
 *
 * When {@link AcApPdfExportOptions.exportLayouts} is true, every layout becomes
 * one PDF page. A busy indicator is shown for the duration.
 */
var AcApPdfConvertor = /** @class */ (function () {
    function AcApPdfConvertor() {
    }
    /**
     * Renders the current drawing to PDF and triggers a browser download.
     *
     * @param context - Active document context
     * @param options - Export framing and layout options
     */
    AcApPdfConvertor.prototype.convert = function (context_1) {
        return __awaiter(this, arguments, void 0, function (context, options) {
            var resolved;
            var _this = this;
            if (options === void 0) { options = {}; }
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        resolved = resolveAcApPdfExportOptions(options);
                        return [4 /*yield*/, AcApDocManager.instance.withBusyIndicator(function () { return __awaiter(_this, void 0, void 0, function () {
                                var db, pdfOptions, inModelSpace, bytes, downloadName;
                                return __generator(this, function (_a) {
                                    switch (_a.label) {
                                        case 0:
                                            db = context.doc.database;
                                            pdfOptions = {
                                                title: context.doc.fileName || context.doc.docTitle,
                                                ltscale: db.ltscale,
                                                celtscale: db.celtscale,
                                                showLineWeight: !!db.lwdisplay,
                                                fontMapping: AcApSettingManager.instance.fontMapping,
                                                background: 'none',
                                                fit: resolved.modelSpaceFit === 'display' ? 'current' : 'extents',
                                                layouts: resolved.exportLayouts ? 'all' : 'current',
                                                glyphProvider: createViewerPdfGlyphProvider(),
                                                textMode: resolved.textMode
                                            };
                                            if (resolved.textMode === 'text') {
                                                pdfOptions.textFontResolver = resolveViewerTextFont;
                                            }
                                            if (resolved.modelSpaceFit === 'display') {
                                                inModelSpace = db.currentSpaceId === db.tables.blockTable.modelSpace.objectId;
                                                if (inModelSpace || !resolved.exportLayouts) {
                                                    pdfOptions.fitBox = this.captureFitBox(context.view);
                                                }
                                            }
                                            return [4 /*yield*/, exportDatabaseToPdf(db, pdfOptions)];
                                        case 1:
                                            bytes = _a.sent();
                                            downloadName = resolveExportDownloadName(context.doc.fileName || context.doc.docTitle, 'pdf');
                                            this.downloadBytes(bytes, downloadName);
                                            return [2 /*return*/];
                                    }
                                });
                            }); }, AcApI18n.t('main.message.exportingPdf'))];
                    case 1:
                        _a.sent();
                        return [2 /*return*/];
                }
            });
        });
    };
    /**
     * Captures the visible world box from the active view’s screen corners.
     */
    AcApPdfConvertor.prototype.captureFitBox = function (view) {
        var topLeft = view.screenToWorld({ x: 0, y: 0 });
        var bottomRight = view.screenToWorld({
            x: view.width,
            y: view.height
        });
        return {
            min: {
                x: Math.min(topLeft.x, bottomRight.x),
                y: Math.min(topLeft.y, bottomRight.y)
            },
            max: {
                x: Math.max(topLeft.x, bottomRight.x),
                y: Math.max(topLeft.y, bottomRight.y)
            }
        };
    };
    AcApPdfConvertor.prototype.downloadBytes = function (bytes, downloadName) {
        var blob = new Blob([bytes], { type: 'application/pdf' });
        var url = URL.createObjectURL(blob);
        var link = document.createElement('a');
        link.href = url;
        link.download = downloadName || 'drawing.pdf';
        link.rel = 'noopener';
        document.body.appendChild(link);
        link.click();
        document.body.removeChild(link);
        window.setTimeout(function () { return URL.revokeObjectURL(url); }, 60000);
    };
    return AcApPdfConvertor;
}());
export { AcApPdfConvertor };
//# sourceMappingURL=AcApPdfConvertor.js.map