const s = "PdfPlugin", o = ["-cpdf", "cpdf", "ipdf"];
function P(r, e = {}) {
  const t = e.disableExport === !0, i = t ? ["ipdf"] : [...o];
  r.registerLazyPlugin({
    name: s,
    triggers: [...i],
    loader: async () => {
      const { createPdfPlugin: n } = await import("@mlightcad/cad-pdf-plugin");
      return n({ disableExport: t });
    }
  });
}
export {
  s as PDF_PLUGIN_NAME,
  o as PDF_PLUGIN_TRIGGERS,
  P as registerLazyPdfPlugin
};
