var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
import { acgiForegroundColorForBackground } from '@mlightcad/data-model';
import * as THREE from 'three';
import { LineMaterial } from 'three/examples/jsm/lines/LineMaterial.js';
import { LineSegments2 } from 'three/examples/jsm/lines/LineSegments2.js';
import { LineSegmentsGeometry } from 'three/examples/jsm/lines/LineSegmentsGeometry.js';
import { batchOriginOffsetDistance, canMergeIntoBatchOrigin } from '../draw/AcTrBatchDrawPolicy';
import { AcTrPointSymbolCreator } from '../geometry/AcTrPointSymbolCreator';
import { followsLayerStyle, getMaterialMetadata, setMaterialMetadata } from '../style/AcTrMaterialMetadata';
import { AcTrBufferGeometryUtil, AcTrMaterialUtil, AcTrMTextColorUtil, AcTrSubEntityTraitsUtil } from '../util';
import { HIGHLIGHT_HOVER_COLOR, HIGHLIGHT_SELECT_COLOR } from '../util/AcTrMaterialUtil';
import { copyHighlightObjectFlags, getHighlightUserData, getObjectUserData, getSceneDrawableUserData, markHighlightOverlayGroup, patchDrawableMaterialFromCache, syncStyleMaterialIdFromMaterials } from '../util/AcTrObjectUserData';
import { isThreeLineSegments, isThreeLineSegments2, isThreeMesh, isThreePoints } from '../util/AcTrThreeObjectGuards';
import { isObjectHierarchyVisible } from '../util/AcTrVisibility';
import { AcTrBatchedLine } from './AcTrBatchedLine';
import { AcTrBatchedLine2 } from './AcTrBatchedLine2';
import { AcTrBatchedMesh } from './AcTrBatchedMesh';
import { AcTrBatchedPoint } from './AcTrBatchedPoint';
import { installBatchHighlightRenderer } from './highlight';
/**
 * Aggregates and manages all batched render objects belonging to one CAD
 * layer/layout group.
 *
 * Entities are distributed into per-material batch containers (line/mesh/point
 * and wide-line variants). Multiple containers may share one material when their
 * world-space origins are too far apart for safe float32 rebasing. Unsupported
 * render paths are stored in a dedicated unbatched group.
 */
var AcTrBatchedGroup = /** @class */ (function (_super) {
    __extends(AcTrBatchedGroup, _super);
    /**
     * Creates an empty batched group with highlight and unbatched child containers.
     */
    function AcTrBatchedGroup() {
        var _this = _super.call(this) || this;
        /** Entity ids whose unbatched drawables are currently selection-highlighted. */
        _this._unbatchedSelectedIds = new Set();
        /** Entity ids whose unbatched drawables are currently hover-highlighted. */
        _this._unbatchedHoveredIds = new Set();
        /** Compare-display role overrides keyed by entity object id. */
        _this._compareRoles = new Map();
        /** Whether compare display mode is active for this group. */
        _this._compareEnabled = false;
        /** Unchanged-entity color while compare display is enabled. */
        _this._compareBaseColor = 0x9ca3af;
        /** Deleted-entity color while compare display is enabled. */
        _this._compareDeletedColor = 0xe11d48;
        /** Added-entity color while compare display is enabled. */
        _this._compareAddedColor = 0x22c55e;
        /** Modified-entity color while compare display is enabled. */
        _this._compareModifiedColor = 0xf59e0b;
        _this._pointBatches = new Map();
        _this._pointSymbolBatches = new Map();
        _this._lineBatches = new Map();
        _this._lineWithIndexBatches = new Map();
        _this._line2Batches = new Map();
        _this._meshBatches = new Map();
        _this._meshWithIndexBatches = new Map();
        _this._entitiesMap = new Map();
        _this._originBatchById = new Map();
        _this._unbatchedEntities = new Map();
        _this._unbatchedObjects = new THREE.Group();
        _this._selectedObjects = markHighlightOverlayGroup(new THREE.Group());
        _this._hoverObjects = markHighlightOverlayGroup(new THREE.Group());
        _this._compareOverlay = markHighlightOverlayGroup(new THREE.Group());
        _this._compareOverlay.name = 'CompareOverlay';
        _this._compareOverlay.renderOrder = 10000;
        _this.add(_this._unbatchedObjects);
        _this.add(_this._selectedObjects);
        _this.add(_this._hoverObjects);
        _this.add(_this._compareOverlay);
        return _this;
    }
    Object.defineProperty(AcTrBatchedGroup.prototype, "entityCount", {
        /**
         * The number of entities stored in this batched group
         */
        get: function () {
            return this._entitiesMap.size;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrBatchedGroup.prototype, "stats", {
        /**
         * The statistics data of this batched group
         */
        get: function () {
            var unbatched = this.getUnbatchedStats();
            var stats = {
                summary: {
                    entityCount: this._entitiesMap.size,
                    totalGeometrySize: 0,
                    totalMappingSize: 0
                },
                mesh: {
                    indexed: {
                        count: this.countBatchContainers(this._meshWithIndexBatches),
                        geometrySize: this.getBatchedGeometrySize(this._meshWithIndexBatches),
                        mappingSize: this.getBatchedGeometryMappingSize(this._meshWithIndexBatches)
                    },
                    nonIndexed: {
                        count: this.countBatchContainers(this._meshBatches),
                        geometrySize: this.getBatchedGeometrySize(this._meshBatches),
                        mappingSize: this.getBatchedGeometryMappingSize(this._meshBatches)
                    }
                },
                line: {
                    indexed: {
                        count: this.countBatchContainers(this._lineWithIndexBatches),
                        geometrySize: this.getBatchedGeometrySize(this._lineWithIndexBatches),
                        mappingSize: this.getBatchedGeometryMappingSize(this._lineWithIndexBatches)
                    },
                    nonIndexed: {
                        count: this.countBatchContainers(this._lineBatches) +
                            this.countBatchContainers(this._line2Batches),
                        geometrySize: this.getBatchedGeometrySize(this._lineBatches) +
                            this.getBatchedGeometrySize(this._line2Batches),
                        mappingSize: this.getBatchedGeometryMappingSize(this._lineBatches) +
                            this.getBatchedGeometryMappingSize(this._line2Batches)
                    }
                },
                point: {
                    indexed: {
                        count: this.countBatchContainers(this._pointSymbolBatches),
                        geometrySize: this.getBatchedGeometrySize(this._pointSymbolBatches),
                        mappingSize: this.getBatchedGeometryMappingSize(this._pointSymbolBatches)
                    },
                    nonIndexed: {
                        count: this.countBatchContainers(this._pointBatches),
                        geometrySize: this.getBatchedGeometrySize(this._pointBatches),
                        mappingSize: this.getBatchedGeometryMappingSize(this._pointBatches)
                    }
                },
                unbatched: unbatched
            };
            stats.summary.totalGeometrySize =
                stats.line.indexed.geometrySize +
                    stats.line.nonIndexed.geometrySize +
                    stats.mesh.indexed.geometrySize +
                    stats.mesh.nonIndexed.geometrySize +
                    stats.point.indexed.geometrySize +
                    stats.point.nonIndexed.geometrySize +
                    stats.unbatched.geometrySize;
            stats.summary.totalMappingSize =
                stats.line.indexed.mappingSize +
                    stats.line.nonIndexed.mappingSize +
                    stats.mesh.indexed.mappingSize +
                    stats.mesh.nonIndexed.mappingSize +
                    stats.point.indexed.mappingSize +
                    stats.point.nonIndexed.mappingSize;
            return stats;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Rebuilds point-symbol batches for a new point display mode and size.
     *
     * @param displayMode - `PDMODE` value.
     * @param displaySize - `PDSIZE` value used to scale unit symbol templates.
     */
    AcTrBatchedGroup.prototype.rerenderPoints = function (displayMode, displaySize) {
        if (displaySize === void 0) { displaySize = 0; }
        var creator = AcTrPointSymbolCreator.instance;
        var pointSymbol = creator.create(displayMode, { x: 0, y: 0, z: 0 }, displaySize);
        if (pointSymbol.line) {
            this._pointSymbolBatches.forEach(function (batches) {
                batches.forEach(function (item) {
                    item.resetGeometry(displayMode, displaySize);
                });
            });
        }
        var isShowPoint = pointSymbol.point != null;
        this._pointBatches.forEach(function (batches) {
            batches.forEach(function (item) {
                item.visible = isShowPoint;
            });
        });
    };
    /**
     * Collects world-space AABBs for point and point-symbol batch slots.
     *
     * Used after {@link rerenderPoints} so layouts can refresh spatial-index
     * boxes when `PDMODE` / `PDSIZE` change marker size.
     *
     * @param out - Map of object id to world AABB; values are unioned when the
     *   same id appears in multiple slots.
     * @returns The same map instance passed in {@link out}.
     */
    AcTrBatchedGroup.prototype.collectPointObjectWorldBoxes = function (out) {
        if (out === void 0) { out = new Map(); }
        var scratch = new THREE.Box3();
        var absorbBatch = function (batch) {
            batch.updateMatrixWorld(true);
            for (var i = 0; i < batch.geometryCount; i++) {
                var info = void 0;
                try {
                    info = batch.getGeometryAt(i);
                }
                catch (_a) {
                    continue;
                }
                if (!info.objectId || info.vertexCount <= 0) {
                    continue;
                }
                if (!batch.getBoundingBoxAt(i, scratch)) {
                    continue;
                }
                scratch.applyMatrix4(batch.matrixWorld);
                var existing = out.get(info.objectId);
                if (existing) {
                    existing.union(scratch);
                }
                else {
                    out.set(info.objectId, scratch.clone());
                }
            }
        };
        this._pointSymbolBatches.forEach(function (batches) {
            batches.forEach(absorbBatch);
        });
        this._pointBatches.forEach(function (batches) {
            batches.forEach(absorbBatch);
        });
        return out;
    };
    /**
     * Computes axis-aligned bounds from packed batch vertex data and visible
     * unbatched drawables. Prefer this over entity-level bounding boxes when
     * framing the view — it reflects what is actually rendered in GPU buffers.
     */
    AcTrBatchedGroup.prototype.computeBoundingBox = function (target, options) {
        var _this = this;
        if (target === void 0) { target = new THREE.Box3(); }
        target.makeEmpty();
        var scratch = new THREE.Box3();
        var unionBatchMap = function (map) {
            map.forEach(function (batches) {
                batches.forEach(function (batch) {
                    batch.unionActiveVisibleBoundingBoxInto(target, options);
                });
            });
        };
        unionBatchMap(this._lineBatches);
        unionBatchMap(this._lineWithIndexBatches);
        unionBatchMap(this._line2Batches);
        unionBatchMap(this._meshBatches);
        unionBatchMap(this._meshWithIndexBatches);
        unionBatchMap(this._pointBatches);
        unionBatchMap(this._pointSymbolBatches);
        this._unbatchedEntities.forEach(function (objects, objectId) {
            var e_1, _a;
            var _b;
            if ((_b = options === null || options === void 0 ? void 0 : options.excludeObjectIds) === null || _b === void 0 ? void 0 : _b.has(objectId)) {
                return;
            }
            if ((options === null || options === void 0 ? void 0 : options.includeObjectIds) &&
                !options.includeObjectIds.has(objectId)) {
                return;
            }
            try {
                for (var objects_1 = __values(objects), objects_1_1 = objects_1.next(); !objects_1_1.done; objects_1_1 = objects_1.next()) {
                    var child = objects_1_1.value;
                    if (child.visible === false)
                        continue;
                    _this.unionUnbatchedObjectBounds(child, target, scratch);
                }
            }
            catch (e_1_1) { e_1 = { error: e_1_1 }; }
            finally {
                try {
                    if (objects_1_1 && !objects_1_1.done && (_a = objects_1.return)) _a.call(objects_1);
                }
                finally { if (e_1) throw e_1.error; }
            }
        });
        return target;
    };
    /**
     * Clears all batched/unbatched data and disposes owned resources.
     */
    AcTrBatchedGroup.prototype.clear = function () {
        var _this = this;
        this.groups.forEach(function (group) {
            group.forEach(function (batches) {
                batches.forEach(function (batch) {
                    batch.dispose();
                    batch.removeFromParent();
                });
            });
            group.clear();
        });
        this.clearHighlightGroup(this._selectedObjects);
        this.clearHighlightGroup(this._hoverObjects);
        this.clearHighlightGroup(this._compareOverlay);
        this._originBatchById.clear();
        this._unbatchedObjects.children.forEach(function (object) {
            _this.disposeObject(object);
        });
        this._unbatchedObjects.clear();
        this._unbatchedEntities.clear();
        this._unbatchedSelectedIds.clear();
        this._unbatchedHoveredIds.clear();
        this._entitiesMap.clear();
        return this;
    };
    /**
     * Update material of batch objects
     * @param oldId - Id of the old material associated with batch objects
     * @param material - The new material associated with the batch object
     */
    AcTrBatchedGroup.prototype.updateMaterial = function (oldId, material) {
        var e_2, _a, e_3, _b;
        try {
            for (var _c = __values(this.groups), _d = _c.next(); !_d.done; _d = _c.next()) {
                var group = _d.value;
                var batches = group.get(oldId);
                if (!batches) {
                    continue;
                }
                try {
                    for (var batches_1 = (e_3 = void 0, __values(batches)), batches_1_1 = batches_1.next(); !batches_1_1.done; batches_1_1 = batches_1.next()) {
                        var batch = batches_1_1.value;
                        // Each batch keeps a private material clone so highlight-mask uniforms
                        // never leak across containers that share the same style cache entry.
                        batch.material = this.createOwnedBatchMaterial(material);
                    }
                }
                catch (e_3_1) { e_3 = { error: e_3_1 }; }
                finally {
                    try {
                        if (batches_1_1 && !batches_1_1.done && (_b = batches_1.return)) _b.call(batches_1);
                    }
                    finally { if (e_3) throw e_3.error; }
                }
                if (material.id !== oldId) {
                    group.delete(oldId);
                    var existing = group.get(material.id);
                    if (existing) {
                        existing.push.apply(existing, __spreadArray([], __read(batches), false));
                    }
                    else {
                        group.set(material.id, batches);
                    }
                }
            }
        }
        catch (e_2_1) { e_2 = { error: e_2_1 }; }
        finally {
            try {
                if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
            }
            finally { if (e_2) throw e_2.error; }
        }
        this._unbatchedObjects.traverse(function (object) {
            if (!('material' in object))
                return;
            var drawableUserData = getSceneDrawableUserData(object);
            if (drawableUserData.styleMaterialId === oldId) {
                object.material = material;
                drawableUserData.styleMaterialId = material.id;
            }
        });
    };
    /**
     * Refreshes batched and unbatched drawables after a layer-table style change.
     *
     * Runs one unbatched-object traversal for text refresh, cache id patching, and
     * layer-bound remapping instead of scanning the full batched geometry tree.
     */
    AcTrBatchedGroup.prototype.syncAppearanceFromRecord = function (layerName, layerTraits, materials, needsIdPatch, getLayerBoundMaterial, styleManager) {
        var _this = this;
        this.rebindBatchedMaterialsForLayer(layerName, layerTraits, getLayerBoundMaterial, styleManager);
        this._unbatchedObjects.traverse(function (object) {
            var refresh = object.refreshTextMaterials;
            if (typeof refresh === 'function') {
                refresh.call(object, layerTraits);
            }
            if (!('material' in object)) {
                return;
            }
            var drawable = object;
            if (needsIdPatch) {
                patchDrawableMaterialFromCache(drawable, materials);
            }
            _this.rebindUnbatchedDrawableMaterial(drawable, layerName, layerTraits, getLayerBoundMaterial, styleManager);
        });
        if (styleManager) {
            this.rematerializeLayerTextDrawables(layerName, styleManager, layerTraits);
        }
    };
    /**
     * Rebinds layer-bound batch materials after a layer-table style change.
     *
     * Batched MTEXT/TEXT glyphs and cloned unbatched drawables may not share the
     * same material instance as the style-manager cache entry updated by
     * {@link AcTrStyleManager.updateLayerMaterial}.
     */
    AcTrBatchedGroup.prototype.rebindMaterialsForLayer = function (layerName, layerTraits, getLayerBoundMaterial, styleManager) {
        var _this = this;
        this.rebindBatchedMaterialsForLayer(layerName, layerTraits, getLayerBoundMaterial, styleManager);
        this._unbatchedObjects.traverse(function (object) {
            if (!('material' in object)) {
                return;
            }
            _this.rebindUnbatchedDrawableMaterial(object, layerName, layerTraits, getLayerBoundMaterial, styleManager);
        });
    };
    AcTrBatchedGroup.prototype.rebindBatchedMaterialsForLayer = function (layerName, layerTraits, getLayerBoundMaterial, styleManager) {
        var e_4, _a, e_5, _b, e_6, _c;
        var _d;
        var reboundByOldId = new Map();
        try {
            for (var _e = __values(this.groups), _f = _e.next(); !_f.done; _f = _e.next()) {
                var group = _f.value;
                try {
                    for (var _g = (e_5 = void 0, __values(__spreadArray([], __read(group.keys()), false))), _h = _g.next(); !_h.done; _h = _g.next()) {
                        var materialId = _h.value;
                        var batches = group.get(materialId);
                        var material = (_d = batches === null || batches === void 0 ? void 0 : batches[0]) === null || _d === void 0 ? void 0 : _d.material;
                        if (!material || reboundByOldId.has(materialId)) {
                            continue;
                        }
                        if (!followsLayerStyle(material, layerName, layerName)) {
                            continue;
                        }
                        var rebound = this.resolveLayerBoundMaterial(material, layerName, layerTraits, getLayerBoundMaterial, styleManager, layerName);
                        if (!rebound) {
                            continue;
                        }
                        if (rebound === material) {
                            try {
                                // Each batch owns a material clone; refresh color on every clone.
                                for (var _j = (e_6 = void 0, __values(batches)), _k = _j.next(); !_k.done; _k = _j.next()) {
                                    var batch = _k.value;
                                    this.refreshLayerBoundMaterialColor(batch.material, layerTraits, styleManager);
                                }
                            }
                            catch (e_6_1) { e_6 = { error: e_6_1 }; }
                            finally {
                                try {
                                    if (_k && !_k.done && (_c = _j.return)) _c.call(_j);
                                }
                                finally { if (e_6) throw e_6.error; }
                            }
                            continue;
                        }
                        reboundByOldId.set(materialId, rebound);
                        this.updateMaterial(materialId, rebound);
                    }
                }
                catch (e_5_1) { e_5 = { error: e_5_1 }; }
                finally {
                    try {
                        if (_h && !_h.done && (_b = _g.return)) _b.call(_g);
                    }
                    finally { if (e_5) throw e_5.error; }
                }
            }
        }
        catch (e_4_1) { e_4 = { error: e_4_1 }; }
        finally {
            try {
                if (_f && !_f.done && (_a = _e.return)) _a.call(_e);
            }
            finally { if (e_4) throw e_4.error; }
        }
    };
    AcTrBatchedGroup.prototype.rebindUnbatchedDrawableMaterial = function (drawable, layerName, layerTraits, getLayerBoundMaterial, styleManager) {
        var _this = this;
        var objectLayerName = drawable.userData.layerName;
        var effectiveObjectLayerName = objectLayerName !== null && objectLayerName !== void 0 ? objectLayerName : layerName;
        var drawableUserData = getSceneDrawableUserData(drawable);
        var rebindSingle = function (material) {
            if (!followsLayerStyle(material, layerName, effectiveObjectLayerName)) {
                return material;
            }
            var rebound = _this.resolveLayerBoundMaterial(material, layerName, layerTraits, getLayerBoundMaterial, styleManager, effectiveObjectLayerName);
            if (!rebound) {
                return material;
            }
            if (rebound === material) {
                _this.refreshLayerBoundMaterialColor(material, layerTraits, styleManager);
                return material;
            }
            return rebound;
        };
        var currentMaterial = drawable.material;
        if (Array.isArray(currentMaterial)) {
            drawable.material = currentMaterial.map(rebindSingle);
            syncStyleMaterialIdFromMaterials(drawableUserData, drawable.material);
            return;
        }
        var rebound = rebindSingle(currentMaterial);
        if (rebound === currentMaterial) {
            return;
        }
        drawable.material = rebound;
        syncStyleMaterialIdFromMaterials(drawableUserData, rebound);
    };
    /**
     * Rebinds MTEXT/TEXT glyph materials under unbatched placement roots.
     *
     * Entity wrappers are not kept in the scene tree after batching, so layer
     * colour updates must rematerialize the cloned glyph subtrees directly.
     */
    AcTrBatchedGroup.prototype.rematerializeLayerTextDrawables = function (layerName, styleManager, layerTraits) {
        var e_7, _a, e_8, _b, e_9, _c;
        var _d;
        try {
            for (var _e = __values(this._unbatchedObjects.children), _f = _e.next(); !_f.done; _f = _e.next()) {
                var root = _f.value;
                var storedTraits = getSceneDrawableUserData(root).textEntityTraits;
                if (storedTraits == null || storedTraits.layer !== layerName) {
                    continue;
                }
                AcTrMTextColorUtil.rematerializeTextHierarchy(root, AcTrMTextColorUtil.cloneEntityTraits(storedTraits), styleManager);
            }
        }
        catch (e_7_1) { e_7 = { error: e_7_1 }; }
        finally {
            try {
                if (_f && !_f.done && (_a = _e.return)) _a.call(_e);
            }
            finally { if (e_7) throw e_7.error; }
        }
        if (!(layerTraits === null || layerTraits === void 0 ? void 0 : layerTraits.color)) {
            return;
        }
        try {
            for (var _g = __values(this.groups), _h = _g.next(); !_h.done; _h = _g.next()) {
                var group = _h.value;
                try {
                    for (var _j = (e_9 = void 0, __values(__spreadArray([], __read(group.keys()), false))), _k = _j.next(); !_k.done; _k = _j.next()) {
                        var materialId = _k.value;
                        var batches = group.get(materialId);
                        var material = (_d = batches === null || batches === void 0 ? void 0 : batches[0]) === null || _d === void 0 ? void 0 : _d.material;
                        if (!material || !followsLayerStyle(material, layerName, layerName)) {
                            continue;
                        }
                        this.refreshLayerBoundMaterialColor(material, layerTraits, styleManager);
                    }
                }
                catch (e_9_1) { e_9 = { error: e_9_1 }; }
                finally {
                    try {
                        if (_k && !_k.done && (_c = _j.return)) _c.call(_j);
                    }
                    finally { if (e_9) throw e_9.error; }
                }
            }
        }
        catch (e_8_1) { e_8 = { error: e_8_1 }; }
        finally {
            try {
                if (_h && !_h.done && (_b = _g.return)) _b.call(_g);
            }
            finally { if (e_8) throw e_8.error; }
        }
    };
    AcTrBatchedGroup.prototype.resolveLayerBoundMaterial = function (material, layerName, layerTraits, getLayerBoundMaterial, styleManager, objectLayerName) {
        var rebound = getLayerBoundMaterial(material, layerName, layerTraits);
        if (rebound) {
            return rebound;
        }
        if (!styleManager) {
            return undefined;
        }
        return this.resolveLayerBoundReplacement(material, layerName, styleManager, objectLayerName !== null && objectLayerName !== void 0 ? objectLayerName : layerName);
    };
    AcTrBatchedGroup.prototype.resolveLayerBoundReplacement = function (material, layerName, styleManager, objectLayerName) {
        var _a;
        if (!followsLayerStyle(material, layerName, objectLayerName)) {
            return undefined;
        }
        var metadata = getMaterialMetadata(material);
        var traits = AcTrSubEntityTraitsUtil.createDefaultTraits();
        traits.layer = layerName;
        traits.color.setByLayer();
        traits.drawOrder = (_a = metadata.drawOrder) !== null && _a !== void 0 ? _a : 0;
        if (metadata.drawOrder === 0 && metadata.isForeground !== true) {
            return styleManager.getMTextFillMaterial(traits);
        }
        return styleManager.getLineMaterial(traits, true);
    };
    AcTrBatchedGroup.prototype.refreshLayerBoundMaterialColor = function (material, layerTraits, styleManager) {
        var _a, _b;
        // Only rewrite colour when the material is actually colour-bound to the
        // layer. `followsLayerStyle` is also true for ByLayer lineweight / linetype
        // alone; applying the layer colour in those cases clobbers absolute entity
        // colours (e.g. true-color solid hatches on an ACI-7 layer become white).
        var metadata = getMaterialMetadata(material);
        if (metadata.isByLayerColor !== true) {
            return;
        }
        // An ACI-7 (foreground) layer colour is theme-dependent: applying its raw
        // RGB bakes white onto a light canvas (#464). Resolve it against the
        // current background, and keep the clone's isForeground metadata in sync
        // so later foreground-only repaints reach it too.
        var isForegroundColor = ((_a = layerTraits.color) === null || _a === void 0 ? void 0 : _a.isForeground) === true;
        setMaterialMetadata(material, { isForeground: isForegroundColor });
        if (isForegroundColor && !styleManager) {
            // Cannot resolve a theme-dependent colour without the background.
            return;
        }
        var rgb = isForegroundColor && styleManager
            ? acgiForegroundColorForBackground(styleManager.currentBackgroundColor)
            : (_b = layerTraits.color) === null || _b === void 0 ? void 0 : _b.RGB;
        if (typeof rgb !== 'number') {
            return;
        }
        var currentRgb = this.getMaterialDisplayRgb(material);
        if (currentRgb === rgb) {
            return;
        }
        AcTrMaterialUtil.setMaterialColor(material, new THREE.Color(rgb));
    };
    AcTrBatchedGroup.prototype.getMaterialDisplayRgb = function (material) {
        var _a, _b;
        if (material instanceof THREE.MeshBasicMaterial ||
            material instanceof THREE.LineBasicMaterial) {
            return material.color.getHex();
        }
        var shaderMaterial = material;
        var uniformColor = (_b = (_a = shaderMaterial.uniforms) === null || _a === void 0 ? void 0 : _a.u_color) === null || _b === void 0 ? void 0 : _b.value;
        if (uniformColor instanceof THREE.Color) {
            return uniformColor.getHex();
        }
        return undefined;
    };
    /**
     * Return true if this group contains the entity with the specified object id. Otherwise, return false.
     * @param objectId Input the object id of one entity
     * @returns Return true if this group contains the entity with the specified object id. Otherwise,
     * return false.
     */
    AcTrBatchedGroup.prototype.hasEntity = function (objectId) {
        return (this._entitiesMap.has(objectId) || this._unbatchedEntities.has(objectId));
    };
    /**
     * Updates visibility for one entity without removing it from batch containers.
     *
     * @param objectId - Entity object id.
     * @param visible - Desired visibility state.
     * @returns `true` when the entity exists in this group.
     */
    AcTrBatchedGroup.prototype.setEntityVisible = function (objectId, visible) {
        var _this = this;
        var entityInfo = this._entitiesMap.get(objectId);
        var unbatchedObjects = this._unbatchedEntities.get(objectId);
        if (!entityInfo && !unbatchedObjects) {
            return false;
        }
        entityInfo === null || entityInfo === void 0 ? void 0 : entityInfo.forEach(function (item) {
            var batchedObject = _this.getOriginBatch(item.batchedObjectId);
            batchedObject === null || batchedObject === void 0 ? void 0 : batchedObject.setVisibleAt(item.batchId, visible);
        });
        unbatchedObjects === null || unbatchedObjects === void 0 ? void 0 : unbatchedObjects.forEach(function (object) {
            object.visible = visible;
        });
        if (!visible) {
            this.unselect(objectId);
            this.unhover(objectId);
        }
        return true;
    };
    /**
     * Returns the current scene visibility for one entity, or `undefined` when
     * the entity is not present in this group.
     */
    AcTrBatchedGroup.prototype.getEntityVisible = function (objectId) {
        var _this = this;
        var entityInfo = this._entitiesMap.get(objectId);
        var unbatchedObjects = this._unbatchedEntities.get(objectId);
        if (!entityInfo && !unbatchedObjects) {
            return undefined;
        }
        var visible;
        if (entityInfo && entityInfo.length > 0) {
            visible = entityInfo.every(function (item) { return _this.getBatchItemVisible(item); });
        }
        if (unbatchedObjects && unbatchedObjects.length > 0) {
            var unbatchedVisible = unbatchedObjects.some(function (object) { return object.visible; });
            visible =
                visible === undefined ? unbatchedVisible : visible && unbatchedVisible;
        }
        return visible;
    };
    /**
     * Adds one converted entity into batch/unbatched containers.
     */
    AcTrBatchedGroup.prototype.addEntity = function (entity) {
        var _this = this;
        var _a;
        // Skip invisible entities on initial insert so invisible DWG/DXF content does
        // not allocate batch memory. Runtime visibility toggles use setEntityVisible.
        if (entity.visible === false) {
            return;
        }
        var objectId = String((_a = entity.objectId) !== null && _a !== void 0 ? _a : '');
        var entityVisible = entity.visible;
        // One logical entity (same objectId) can be appended in multiple passes
        // (e.g. INSERT decomposition by source layer and inherited layer-0 bucket).
        // Keep accumulating geometry mappings instead of overwriting previous ones.
        var entityInfo = this._entitiesMap.get(objectId);
        if (!entityInfo) {
            entityInfo = [];
            this._entitiesMap.set(objectId, entityInfo);
        }
        var existingUnbatched = this._unbatchedEntities.get(objectId);
        var unbatchedObjects = existingUnbatched !== null && existingUnbatched !== void 0 ? existingUnbatched : [];
        var hasUnbatched = false;
        var styleManager = entity.styleManager;
        entity.updateMatrixWorld(true);
        var visitDrawable = function (object) {
            var e_10, _a;
            // traverse() visits descendants even when an intermediate AcTrEntity is invisible.
            if (!isObjectHierarchyVisible(object)) {
                return;
            }
            var drawableUserData = getSceneDrawableUserData(object);
            var bboxIntersectionCheck = !!drawableUserData.bboxIntersectionCheck;
            if (drawableUserData.noBatch) {
                var cloned = _this.cloneUnbatchedObject(object);
                cloned.visible = entityVisible && object.visible;
                getSceneDrawableUserData(cloned).bboxIntersectionCheck =
                    bboxIntersectionCheck;
                _this._unbatchedObjects.add(cloned);
                unbatchedObjects.push(cloned);
                hasUnbatched = true;
                return;
            }
            // Duck-type across duplicate three.js copies (mtext-renderer vs app).
            // `instanceof` silently drops glyph LineSegments/Meshes — see
            // AcTrThreeObjectGuards.
            if (isThreeLineSegments2(object)) {
                var item = _this.addLine2(object, {
                    objectId: objectId,
                    bboxIntersectionCheck: bboxIntersectionCheck
                });
                if (item) {
                    entityInfo.push(item);
                    _this.applyBatchSlotVisibility(item, entityVisible && object.visible);
                }
                return;
            }
            if (isThreeLineSegments(object)) {
                var item = _this.addLine(object, {
                    position: drawableUserData.position,
                    objectId: objectId,
                    bboxIntersectionCheck: bboxIntersectionCheck
                });
                if (item) {
                    entityInfo.push(item);
                    _this.applyBatchSlotVisibility(item, entityVisible && object.visible);
                }
            }
            else if (isThreeMesh(object)) {
                var item = _this.addMesh(object, {
                    objectId: objectId,
                    bboxIntersectionCheck: bboxIntersectionCheck
                }, styleManager);
                if (item) {
                    entityInfo.push(item);
                    _this.applyBatchSlotVisibility(item, entityVisible && object.visible);
                }
            }
            else if (isThreePoints(object)) {
                var item = _this.addPoint(object, {
                    objectId: objectId,
                    bboxIntersectionCheck: bboxIntersectionCheck
                });
                if (item) {
                    entityInfo.push(item);
                    _this.applyBatchSlotVisibility(item, entityVisible && object.visible);
                }
            }
            try {
                for (var _b = __values(object.children), _c = _b.next(); !_c.done; _c = _b.next()) {
                    var child = _c.value;
                    visitDrawable(child);
                }
            }
            catch (e_10_1) { e_10 = { error: e_10_1 }; }
            finally {
                try {
                    if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
                }
                finally { if (e_10) throw e_10.error; }
            }
        };
        visitDrawable(entity);
        if (hasUnbatched) {
            this._unbatchedEntities.set(objectId, unbatchedObjects);
        }
        this.syncCompareRoleForEntity(objectId);
    };
    /**
     * If compare display is on, tints a newly packed entity with its role
     * (or the unchanged base color).
     *
     * @param objectId - Entity just added to this group.
     */
    AcTrBatchedGroup.prototype.syncCompareRoleForEntity = function (objectId) {
        var _a;
        if (!this._compareEnabled)
            return;
        var role = (_a = this._compareRoles.get(objectId)) !== null && _a !== void 0 ? _a : null;
        this.applyCompareRoleToEntity(objectId, role);
        this.refreshUnbatchedCompareMaterial(objectId);
        // Growth replaces packed attributes; overlay wrappers keep the old ones
        // until rebound. Draw ranges stay valid across capacity increases.
        this.rebindCompareOverlaySharedBuffers();
        this.syncCompareOverlayForEntity(objectId);
    };
    /**
     * Appends pre-built indexed/non-indexed line geometry directly into a line
     * batch without creating a temporary {@link THREE.LineSegments} / `AcTrLine`.
     *
     * Geometry must already be in local coordinates relative to `worldOffset`
     * (bbox-center rebase). This method mutates the geometry during rebase into
     * the batch origin; the caller must dispose it afterward.
     *
     * @returns `false` when `visible` is false or append fails.
     */
    AcTrBatchedGroup.prototype.appendLineGeometry = function (geometry, material, worldOffset, options) {
        var _this = this;
        if (options.visible === false) {
            return false;
        }
        var hasIndex = geometry.getIndex() !== null;
        var batches = hasIndex ? this._lineWithIndexBatches : this._lineBatches;
        var batchedLine = this.resolveOriginBatch(batches, material.id, worldOffset, function () {
            return new AcTrBatchedLine(AcTrBatchedGroup.INITIAL_LINE_VERTEX_CAPACITY, AcTrBatchedGroup.INITIAL_LINE_INDEX_CAPACITY, _this.createOwnedBatchMaterial(material));
        });
        if (geometry.hasAttribute('lineDistance')) {
            AcTrBufferGeometryUtil.recomputeLineDistanceForLineSegments(geometry);
        }
        var geometryId = batchedLine.addGeometry(geometry, -1, -1, worldOffset);
        batchedLine.setGeometryInfo(geometryId, { objectId: options.objectId });
        var item = {
            batchedObjectId: batchedLine.id,
            batchId: geometryId
        };
        // Visible was already gated above; remaining cases are visible.
        this.registerDirectAppend(options.objectId, item, true);
        return true;
    };
    /**
     * Appends pre-built fat-line geometry directly into a `LineSegments2` batch
     * without creating a temporary {@link LineSegments2} / `AcTrLine`.
     *
     * Geometry must already be in local coordinates relative to `worldOffset`.
     * The caller must dispose the geometry afterward.
     *
     * @returns `false` when `visible` is false or append fails.
     */
    AcTrBatchedGroup.prototype.appendLine2Geometry = function (geometry, material, worldOffset, options) {
        var _this = this;
        if (options.visible === false) {
            return false;
        }
        var batchedLine = this.resolveOriginBatch(this._line2Batches, material.id, worldOffset, function () {
            return new AcTrBatchedLine2(AcTrBatchedGroup.INITIAL_LINE_VERTEX_CAPACITY, _this.createOwnedBatchMaterial(material));
        });
        var geometryId = batchedLine.addGeometry(geometry, -1, worldOffset);
        batchedLine.setGeometryInfo(geometryId, { objectId: options.objectId });
        var item = {
            batchedObjectId: batchedLine.id,
            batchId: geometryId
        };
        // Visible was already gated above; remaining cases are visible.
        this.registerDirectAppend(options.objectId, item, true);
        return true;
    };
    /**
     * Appends pre-built point geometry directly into a point batch without
     * creating a temporary {@link THREE.Points} / {@link AcTrPoint}.
     *
     * Geometry must already be in local coordinates relative to `worldOffset`.
     * The caller must dispose the geometry afterward.
     */
    AcTrBatchedGroup.prototype.appendPointGeometry = function (geometry, material, worldOffset, options) {
        var _this = this;
        if (options.visible === false) {
            return false;
        }
        var batchedPoint = this.resolveOriginBatch(this._pointBatches, material.id, worldOffset, function () {
            return new AcTrBatchedPoint(AcTrBatchedGroup.INITIAL_POINT_VERTEX_CAPACITY, _this.createOwnedBatchMaterial(material));
        });
        var geometryId = batchedPoint.addGeometry(geometry, -1, worldOffset);
        batchedPoint.setGeometryInfo(geometryId, {
            objectId: options.objectId,
            bboxIntersectionCheck: true,
            position: options.position
        });
        var item = {
            batchedObjectId: batchedPoint.id,
            batchId: geometryId
        };
        this.registerDirectAppend(options.objectId, item, true);
        return true;
    };
    /**
     * Appends pre-built mesh geometry directly into a mesh batch without
     * creating a temporary {@link THREE.Mesh} / {@link AcTrPolygon}.
     *
     * Geometry must already be in local coordinates relative to `worldOffset`.
     * The caller must dispose the geometry afterward.
     */
    AcTrBatchedGroup.prototype.appendMeshGeometry = function (geometry, material, worldOffset, options) {
        var _this = this;
        if (options.visible === false) {
            return false;
        }
        var hasIndex = geometry.getIndex() !== null;
        var batches = hasIndex ? this._meshWithIndexBatches : this._meshBatches;
        var batchedMesh = this.resolveOriginBatch(batches, material.id, worldOffset, function () {
            var _a;
            var metadata = getMaterialMetadata(material);
            var drawOrder = (_a = metadata.drawOrder) !== null && _a !== void 0 ? _a : 0;
            var batch = new AcTrBatchedMesh(AcTrBatchedGroup.INITIAL_MESH_VERTEX_CAPACITY, AcTrBatchedGroup.INITIAL_MESH_INDEX_CAPACITY, _this.createOwnedBatchMaterial(material));
            batch.renderOrder = drawOrder;
            return batch;
        });
        var geometryId = batchedMesh.addGeometry(geometry, -1, -1, worldOffset);
        batchedMesh.setGeometryInfo(geometryId, { objectId: options.objectId });
        var item = {
            batchedObjectId: batchedMesh.id,
            batchId: geometryId
        };
        this.registerDirectAppend(options.objectId, item, true);
        return true;
    };
    /**
     * Records a direct-append slot in `_entitiesMap` and applies visibility.
     */
    AcTrBatchedGroup.prototype.registerDirectAppend = function (objectId, item, visible) {
        objectId = String(objectId);
        var entityInfo = this._entitiesMap.get(objectId);
        if (!entityInfo) {
            entityInfo = [];
            this._entitiesMap.set(objectId, entityInfo);
        }
        entityInfo.push(item);
        this.applyBatchSlotVisibility(item, visible);
        this.syncCompareRoleForEntity(objectId);
    };
    /**
     * Removes one entity from batch/unbatched containers.
     */
    AcTrBatchedGroup.prototype.removeEntity = function (objectId) {
        var _this = this;
        var result = false;
        var compactedBatches = false;
        var entityInfo = this._entitiesMap.get(objectId);
        if (entityInfo) {
            var batchedObjects = new Map();
            for (var index = 0, len = entityInfo.length; index < len; index++) {
                var item = entityInfo[index];
                var batchedObject = this.getOriginBatch(item.batchedObjectId);
                if (batchedObject) {
                    batchedObject.deleteGeometry(item.batchId);
                    batchedObjects.set(item.batchedObjectId, batchedObject);
                    result = true;
                }
            }
            batchedObjects.forEach(function (batchedObject) { return batchedObject.optimize(); });
            compactedBatches = batchedObjects.size > 0;
            this.unselect(objectId);
            this.unhover(objectId);
            this._entitiesMap.delete(objectId);
        }
        var unbatchedObjects = this._unbatchedEntities.get(objectId);
        if (unbatchedObjects) {
            this.unselect(objectId);
            this.unhover(objectId);
            unbatchedObjects.forEach(function (object) {
                _this.disposeObject(object);
                _this._unbatchedObjects.remove(object);
            });
            this._unbatchedEntities.delete(objectId);
            result = true;
        }
        if (this._compareEnabled) {
            // Compact moves remaining slot ranges, so shared overlay draw ranges
            // must be rebuilt. Unbatched-only deletes only drop this entity's copy.
            if (compactedBatches) {
                this.refreshCompareOverlay();
            }
            else {
                this.removeCompareOverlayForEntity(objectId);
            }
        }
        return result;
    };
    /**
     * Return true if the object with the specified object id is intersected with the ray by using raycast.
     * @param objectId  Input object id of object to check for intersection with the ray.
     * @param raycaster Input raycaster to check intersection
     */
    AcTrBatchedGroup.prototype.isIntersectWith = function (objectId, raycaster) {
        var result = false;
        var entityInfo = this._entitiesMap.get(objectId);
        if (entityInfo) {
            var intersects = [];
            for (var index = 0, len = entityInfo.length; index < len; index++) {
                var item = entityInfo[index];
                var batchedObject = this.getOriginBatch(item.batchedObjectId);
                if (batchedObject) {
                    batchedObject.intersectWith(item.batchId, raycaster, intersects);
                    if (intersects.length > 0)
                        return true;
                }
            }
        }
        var unbatchedObjects = this._unbatchedEntities.get(objectId);
        if (unbatchedObjects) {
            for (var i = 0; i < unbatchedObjects.length; i++) {
                if (this.isUnbatchedDrawableIntersecting(unbatchedObjects[i], raycaster)) {
                    return true;
                }
            }
        }
        return result;
    };
    /**
     * Adds hover highlight for one entity id.
     *
     * @param objectId - Database object id to hover.
     */
    AcTrBatchedGroup.prototype.hover = function (objectId) {
        this.setEntityHighlight([objectId], 'hover', true);
    };
    /**
     * Removes hover highlight for one entity id.
     *
     * @param objectId - Database object id to unhover.
     */
    AcTrBatchedGroup.prototype.unhover = function (objectId) {
        this.setEntityHighlight([objectId], 'hover', false);
    };
    /**
     * Adds selection highlight for one entity id.
     *
     * @param objectId - Database object id to select.
     */
    AcTrBatchedGroup.prototype.select = function (objectId) {
        this.setEntityHighlight([objectId], 'select', true);
    };
    /**
     * Removes selection highlight for one entity id.
     *
     * @param objectId - Database object id to unselect.
     */
    AcTrBatchedGroup.prototype.unselect = function (objectId) {
        this.setEntityHighlight([objectId], 'select', false);
    };
    /**
     * Adds hover highlight for many entity ids with one mask flush pass.
     *
     * @param objectIds - Database object ids to hover.
     */
    AcTrBatchedGroup.prototype.hoverMany = function (objectIds) {
        this.setEntityHighlight(objectIds, 'hover', true);
    };
    /**
     * Removes hover highlight for many entity ids with one mask flush pass.
     *
     * @param objectIds - Database object ids to unhover.
     */
    AcTrBatchedGroup.prototype.unhoverMany = function (objectIds) {
        this.setEntityHighlight(objectIds, 'hover', false);
    };
    /**
     * Adds selection highlight for many entity ids with one mask flush pass.
     *
     * @param objectIds - Database object ids to select.
     */
    AcTrBatchedGroup.prototype.selectMany = function (objectIds) {
        this.setEntityHighlight(objectIds, 'select', true);
    };
    /**
     * Removes selection highlight for many entity ids with one mask flush pass.
     *
     * @param objectIds - Database object ids to unselect.
     */
    AcTrBatchedGroup.prototype.unselectMany = function (objectIds) {
        this.setEntityHighlight(objectIds, 'select', false);
    };
    /**
     * Enables or updates compare-display coloring for this group.
     *
     * When enabled, all entities render in `baseColor` unless they have a
     * role override (deleted / added / modified). Selection and hover still win.
     *
     * @param options.enabled - Whether compare coloring is active.
     * @param options.baseColor - Color for unchanged entities.
     * @param options.colors - Optional role color overrides.
     * @param options.overrides - Full per-entity role set; replaces any previous
     *   roles. Omit to keep the current set (color-only updates).
     */
    AcTrBatchedGroup.prototype.setCompareDisplay = function (options) {
        var e_11, _a;
        var _b, _c, _d;
        this._compareEnabled = options.enabled;
        if (options.baseColor != null) {
            this._compareBaseColor = options.baseColor;
        }
        if (((_b = options.colors) === null || _b === void 0 ? void 0 : _b.deleted) != null) {
            this._compareDeletedColor = options.colors.deleted;
        }
        if (((_c = options.colors) === null || _c === void 0 ? void 0 : _c.added) != null) {
            this._compareAddedColor = options.colors.added;
        }
        if (((_d = options.colors) === null || _d === void 0 ? void 0 : _d.modified) != null) {
            this._compareModifiedColor = options.colors.modified;
        }
        // A new override list replaces the previous one. Merging would leave
        // deleted roles on a pane that should only show added/modified hits
        // (DWG handles collide across files).
        if (options.overrides) {
            this._compareRoles.clear();
            try {
                for (var _e = __values(options.overrides), _f = _e.next(); !_f.done; _f = _e.next()) {
                    var entry = _f.value;
                    if (entry.role != null) {
                        this._compareRoles.set(String(entry.objectId), entry.role);
                    }
                }
            }
            catch (e_11_1) { e_11 = { error: e_11_1 }; }
            finally {
                try {
                    if (_f && !_f.done && (_a = _e.return)) _a.call(_e);
                }
                finally { if (e_11) throw e_11.error; }
            }
        }
        if (!options.enabled) {
            this._compareRoles.clear();
        }
        this.applyCompareDisplayToBatches();
        this.refreshCompareOverlay();
        this.refreshAllUnbatchedCompareMaterials();
    };
    /** Clears all compare role overrides and disables compare display. */
    AcTrBatchedGroup.prototype.clearCompareDisplay = function () {
        this.setCompareDisplay({ enabled: false, overrides: [] });
    };
    /**
     * Applies or clears the compare role for one entity id.
     *
     * @param objectId - Entity object id.
     * @param role - Compare role, or `null` to clear.
     */
    AcTrBatchedGroup.prototype.setEntityCompareRole = function (objectId, role) {
        var id = String(objectId);
        if (role == null) {
            this._compareRoles.delete(id);
        }
        else {
            this._compareRoles.set(id, role);
        }
        this.applyCompareRoleToEntity(id, role);
        this.refreshUnbatchedCompareMaterial(id);
        this.syncCompareOverlayForEntity(id);
    };
    /** Pushes compare colors and role masks onto every origin batch in this group. */
    AcTrBatchedGroup.prototype.applyCompareDisplayToBatches = function () {
        var e_12, _a;
        var _this = this;
        var colorOptions = {
            enabled: this._compareEnabled,
            baseColor: this._compareBaseColor,
            deletedColor: this._compareDeletedColor,
            addedColor: this._compareAddedColor,
            modifiedColor: this._compareModifiedColor
        };
        try {
            for (var _b = __values(this.groups), _c = _b.next(); !_c.done; _c = _b.next()) {
                var map = _c.value;
                map.forEach(function (batches) {
                    batches.forEach(function (batch) {
                        batch.setCompareDisplayColors(colorOptions);
                        batch.applyPackedCompareRoles(_this._compareRoles);
                    });
                });
            }
        }
        catch (e_12_1) { e_12 = { error: e_12_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_12) throw e_12.error; }
        }
    };
    /**
     * Writes `role` into every packed slot owned by `objectId`.
     *
     * @param objectId - Entity object id.
     * @param role - Compare role, or `null` to clear.
     */
    AcTrBatchedGroup.prototype.applyCompareRoleToEntity = function (objectId, role) {
        var _this = this;
        var entityInfo = this._entitiesMap.get(objectId);
        var dirtyBatches = new Set();
        entityInfo === null || entityInfo === void 0 ? void 0 : entityInfo.forEach(function (item) {
            var batchedObject = _this.getOriginBatch(item.batchedObjectId);
            if (batchedObject === null || batchedObject === void 0 ? void 0 : batchedObject.setCompareRoleAt(item.batchId, role)) {
                dirtyBatches.add(batchedObject);
            }
        });
        dirtyBatches.forEach(function (batch) { return batch.flushHighlightMask(); });
    };
    /** Reapplies compare tints to every unbatched drawable. */
    AcTrBatchedGroup.prototype.refreshAllUnbatchedCompareMaterials = function () {
        var _this = this;
        this._unbatchedEntities.forEach(function (_objects, objectId) {
            _this.refreshUnbatchedCompareMaterial(objectId);
        });
    };
    /**
     * Reapplies compare tint (or restores highlight) for one unbatched entity.
     *
     * @param objectId - Entity object id.
     */
    AcTrBatchedGroup.prototype.refreshUnbatchedCompareMaterial = function (objectId) {
        var e_13, _a, e_14, _b;
        var unbatchedObjects = this._unbatchedEntities.get(objectId);
        if (!unbatchedObjects) {
            return;
        }
        // Selection/hover still take precedence
        if (this._unbatchedSelectedIds.has(objectId) ||
            this._unbatchedHoveredIds.has(objectId)) {
            this.refreshUnbatchedHighlight(unbatchedObjects[0], objectId);
            try {
                // refreshUnbatchedHighlight only handles one root; apply to all
                for (var unbatchedObjects_1 = __values(unbatchedObjects), unbatchedObjects_1_1 = unbatchedObjects_1.next(); !unbatchedObjects_1_1.done; unbatchedObjects_1_1 = unbatchedObjects_1.next()) {
                    var object = unbatchedObjects_1_1.value;
                    this.refreshUnbatchedHighlight(object, objectId);
                }
            }
            catch (e_13_1) { e_13 = { error: e_13_1 }; }
            finally {
                try {
                    if (unbatchedObjects_1_1 && !unbatchedObjects_1_1.done && (_a = unbatchedObjects_1.return)) _a.call(unbatchedObjects_1);
                }
                finally { if (e_13) throw e_13.error; }
            }
            return;
        }
        try {
            for (var unbatchedObjects_2 = __values(unbatchedObjects), unbatchedObjects_2_1 = unbatchedObjects_2.next(); !unbatchedObjects_2_1.done; unbatchedObjects_2_1 = unbatchedObjects_2.next()) {
                var object = unbatchedObjects_2_1.value;
                if (!this._compareEnabled) {
                    this.unhighlightUnbatchedObject(object);
                    continue;
                }
                var role = this._compareRoles.get(objectId);
                var color = this.resolveCompareColor(role);
                this.applyUnbatchedCompareMaterial(object, color);
            }
        }
        catch (e_14_1) { e_14 = { error: e_14_1 }; }
        finally {
            try {
                if (unbatchedObjects_2_1 && !unbatchedObjects_2_1.done && (_b = unbatchedObjects_2.return)) _b.call(unbatchedObjects_2);
            }
            finally { if (e_14) throw e_14.error; }
        }
    };
    /**
     * Resolves the compare color for `role`, falling back to the unchanged base.
     *
     * @param role - Compare role, or `undefined` for unchanged.
     */
    AcTrBatchedGroup.prototype.resolveCompareColor = function (role) {
        if (role === 'deleted')
            return this._compareDeletedColor;
        if (role === 'added')
            return this._compareAddedColor;
        if (role === 'modified')
            return this._compareModifiedColor;
        return this._compareBaseColor;
    };
    /**
     * Tints an unbatched drawable (and its children) with a shared compare material.
     *
     * @param object - Root drawable.
     * @param colorHex - 24-bit RGB color.
     */
    AcTrBatchedGroup.prototype.applyUnbatchedCompareMaterial = function (object, colorHex) {
        var e_15, _a;
        var _b;
        if ('material' in object) {
            var material = object.material;
            var objectData = getObjectUserData(object);
            if (objectData.originalMaterial == null) {
                objectData.originalMaterial = material;
            }
            var source = (_b = objectData.originalMaterial) !== null && _b !== void 0 ? _b : material;
            object.material = this.getSharedCompareMaterial(source, colorHex);
            return;
        }
        try {
            for (var _c = __values(object.children), _d = _c.next(); !_d.done; _d = _c.next()) {
                var child = _d.value;
                this.applyUnbatchedCompareMaterial(child, colorHex);
            }
        }
        catch (e_15_1) { e_15 = { error: e_15_1 }; }
        finally {
            try {
                if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
            }
            finally { if (e_15) throw e_15.error; }
        }
    };
    /**
     * Returns a cached clone of `material` tinted to `colorHex`.
     *
     * @param material - Source material or material array.
     * @param colorHex - 24-bit RGB color used as the cache key.
     */
    AcTrBatchedGroup.prototype.getSharedCompareMaterial = function (material, colorHex) {
        var _this = this;
        if (Array.isArray(material)) {
            return material.map(function (entry) {
                return _this.getSharedCompareMaterial(entry, colorHex);
            });
        }
        var kind = "compare-".concat(colorHex);
        var cache = AcTrBatchedGroup.sharedHighlightMaterialsBySource.get(material);
        if (!cache) {
            cache = new Map();
            AcTrBatchedGroup.sharedHighlightMaterialsBySource.set(material, cache);
            material.addEventListener('dispose', AcTrBatchedGroup.onSourceMaterialDisposed);
        }
        var cached = cache.get(kind);
        if (cached) {
            return cached;
        }
        var compareMaterial = AcTrMaterialUtil.cloneMaterial(material);
        if (!Array.isArray(compareMaterial)) {
            AcTrMaterialUtil.setMaterialColor(compareMaterial, new THREE.Color(colorHex));
            cache.set(kind, compareMaterial);
            return compareMaterial;
        }
        return compareMaterial;
    };
    /**
     * Applies or clears batched/unbatched highlight state for entity ids.
     *
     * @param objectIds - Database object ids whose highlight state should change.
     * @param kind - Whether to update selection or hover state.
     * @param enabled - `true` to highlight the entities, `false` to clear them.
     */
    AcTrBatchedGroup.prototype.setEntityHighlight = function (objectIds, kind, enabled) {
        var e_16, _a, e_17, _b;
        var _this = this;
        if (objectIds.length === 0) {
            return;
        }
        var dirtyBatches = new Set();
        try {
            for (var objectIds_1 = __values(objectIds), objectIds_1_1 = objectIds_1.next(); !objectIds_1_1.done; objectIds_1_1 = objectIds_1.next()) {
                var objectId = objectIds_1_1.value;
                var entityInfo = this._entitiesMap.get(objectId);
                entityInfo === null || entityInfo === void 0 ? void 0 : entityInfo.forEach(function (item) {
                    var batchedObject = _this.getOriginBatch(item.batchedObjectId);
                    if (batchedObject &&
                        batchedObject.setHighlightAt(item.batchId, kind, enabled)) {
                        dirtyBatches.add(batchedObject);
                    }
                });
                var unbatchedObjects = this._unbatchedEntities.get(objectId);
                if (unbatchedObjects) {
                    if (kind === 'select') {
                        if (enabled) {
                            this._unbatchedSelectedIds.add(objectId);
                        }
                        else {
                            this._unbatchedSelectedIds.delete(objectId);
                        }
                    }
                    else if (enabled) {
                        this._unbatchedHoveredIds.add(objectId);
                    }
                    else {
                        this._unbatchedHoveredIds.delete(objectId);
                    }
                    try {
                        for (var unbatchedObjects_3 = (e_17 = void 0, __values(unbatchedObjects)), unbatchedObjects_3_1 = unbatchedObjects_3.next(); !unbatchedObjects_3_1.done; unbatchedObjects_3_1 = unbatchedObjects_3.next()) {
                            var object = unbatchedObjects_3_1.value;
                            this.refreshUnbatchedHighlight(object, objectId);
                        }
                    }
                    catch (e_17_1) { e_17 = { error: e_17_1 }; }
                    finally {
                        try {
                            if (unbatchedObjects_3_1 && !unbatchedObjects_3_1.done && (_b = unbatchedObjects_3.return)) _b.call(unbatchedObjects_3);
                        }
                        finally { if (e_17) throw e_17.error; }
                    }
                    // Restore compare tint when highlight is cleared
                    if (!enabled) {
                        this.refreshUnbatchedCompareMaterial(objectId);
                    }
                }
            }
        }
        catch (e_16_1) { e_16 = { error: e_16_1 }; }
        finally {
            try {
                if (objectIds_1_1 && !objectIds_1_1.done && (_a = objectIds_1.return)) _a.call(objectIds_1);
            }
            finally { if (e_16) throw e_16.error; }
        }
        dirtyBatches.forEach(function (batch) { return batch.flushHighlightMask(); });
    };
    /**
     * Applies the effective unbatched highlight material for one entity id.
     *
     * Selection takes precedence over hover, matching batched slot-mask behavior.
     *
     * @param object - Root of an unbatched drawable subtree.
     * @param objectId - Database object id owning the drawable subtree.
     */
    AcTrBatchedGroup.prototype.refreshUnbatchedHighlight = function (object, objectId) {
        if (this._unbatchedSelectedIds.has(objectId)) {
            this.applyUnbatchedHighlightMaterial(object, 'select');
            return;
        }
        if (this._unbatchedHoveredIds.has(objectId)) {
            this.applyUnbatchedHighlightMaterial(object, 'hover');
            return;
        }
        this.unhighlightUnbatchedObject(object);
    };
    /**
     * Recursively swaps unbatched drawables to shared highlight materials.
     *
     * @param object - Root of an unbatched drawable subtree.
     * @param kind - Whether to apply selection or hover tinting.
     */
    AcTrBatchedGroup.prototype.applyUnbatchedHighlightMaterial = function (object, kind) {
        var e_18, _a;
        if ('material' in object) {
            var material = object.material;
            var objectData = getObjectUserData(object);
            if (objectData.originalMaterial == null) {
                objectData.originalMaterial = material;
            }
            object.material = this.getSharedHighlightMaterial(material, kind);
            return;
        }
        try {
            for (var _b = __values(object.children), _c = _b.next(); !_c.done; _c = _b.next()) {
                var child = _c.value;
                this.applyUnbatchedHighlightMaterial(child, kind);
            }
        }
        catch (e_18_1) { e_18 = { error: e_18_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_18) throw e_18.error; }
        }
    };
    /**
     * Restores original materials on one unbatched drawable subtree.
     *
     * @param object - Root of an unbatched drawable subtree.
     */
    AcTrBatchedGroup.prototype.unhighlightUnbatchedObject = function (object) {
        var e_19, _a;
        if ('material' in object) {
            var objectData = getObjectUserData(object);
            if (objectData.originalMaterial != null) {
                object.material = objectData.originalMaterial;
                delete objectData.originalMaterial;
            }
            return;
        }
        try {
            for (var _b = __values(object.children), _c = _b.next(); !_c.done; _c = _b.next()) {
                var child = _c.value;
                this.unhighlightUnbatchedObject(child);
            }
        }
        catch (e_19_1) { e_19 = { error: e_19_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_19) throw e_19.error; }
        }
    };
    /**
     * Returns one cached highlight material per source material and highlight kind.
     *
     * @param material - Source material or material array from an unbatched drawable.
     * @param kind - Whether to tint for selection or hover.
     * @returns Highlight material clone suitable for in-place tinting.
     */
    AcTrBatchedGroup.prototype.getSharedHighlightMaterial = function (material, kind) {
        var _this = this;
        if (Array.isArray(material)) {
            return material.map(function (entry) { return _this.getSharedHighlightMaterial(entry, kind); });
        }
        var cache = AcTrBatchedGroup.sharedHighlightMaterialsBySource.get(material);
        if (!cache) {
            cache = new Map();
            AcTrBatchedGroup.sharedHighlightMaterialsBySource.set(material, cache);
            material.addEventListener('dispose', AcTrBatchedGroup.onSourceMaterialDisposed);
        }
        var cached = cache.get(kind);
        if (cached) {
            return cached;
        }
        var highlightMaterial = AcTrMaterialUtil.cloneMaterial(material);
        if (!Array.isArray(highlightMaterial)) {
            AcTrMaterialUtil.setMaterialColor(highlightMaterial, kind === 'hover' ? HIGHLIGHT_HOVER_COLOR : HIGHLIGHT_SELECT_COLOR);
            cache.set(kind, highlightMaterial);
            return highlightMaterial;
        }
        return highlightMaterial;
    };
    /**
     * Disposes cached highlight clones when a source unbatched material is disposed.
     *
     * @param source - Source material whose highlight clones should be released.
     */
    AcTrBatchedGroup.releaseSharedHighlightMaterials = function (source) {
        var cache = AcTrBatchedGroup.sharedHighlightMaterialsBySource.get(source);
        if (!cache) {
            return;
        }
        cache.forEach(function (highlightMaterial) { return highlightMaterial.dispose(); });
        AcTrBatchedGroup.sharedHighlightMaterialsBySource.delete(source);
    };
    /**
     * Builds a standalone preview subset for the specified entity ids.
     *
     * Extracts batched slots via {@link AcTrBatchedLine.getObjectAt} and clones
     * unbatched drawables without re-running entity conversion.
     *
     * @param entityIds - Database object ids to include in the preview subset.
     * @param options - Optional preview style and slot limits.
     * @returns A group containing preview drawables, or `null` when nothing matched.
     */
    AcTrBatchedGroup.prototype.createPreviewSubset = function (entityIds, options) {
        var e_20, _a;
        var _b, _c, _d;
        if (entityIds.length === 0) {
            return null;
        }
        var maxSlots = (_b = options === null || options === void 0 ? void 0 : options.maxSlots) !== null && _b !== void 0 ? _b : 10000;
        var missingEntity = (_c = options === null || options === void 0 ? void 0 : options.missingEntity) !== null && _c !== void 0 ? _c : 'fail';
        var container = new THREE.Group();
        container.name = 'PreviewSubset';
        container.userData.previewSubsetGroup = true;
        var slotCount = 0;
        try {
            for (var entityIds_1 = __values(entityIds), entityIds_1_1 = entityIds_1.next(); !entityIds_1_1.done; entityIds_1_1 = entityIds_1.next()) {
                var objectId = entityIds_1_1.value;
                if (slotCount >= maxSlots) {
                    disposePreviewSubset(container);
                    return null;
                }
                var added = this.appendEntityOverlayDrawables(objectId, container, {
                    maxSlots: maxSlots - slotCount,
                    mode: 'preview',
                    previewStyle: (_d = options === null || options === void 0 ? void 0 : options.style) !== null && _d !== void 0 ? _d : 'normal'
                });
                if (added === 0) {
                    if (missingEntity === 'skip') {
                        continue;
                    }
                    disposePreviewSubset(container);
                    return null;
                }
                slotCount += added;
            }
        }
        catch (e_20_1) { e_20 = { error: e_20_1 }; }
        finally {
            try {
                if (entityIds_1_1 && !entityIds_1_1.done && (_a = entityIds_1.return)) _a.call(entityIds_1);
            }
            finally { if (e_20) throw e_20.error; }
        }
        return slotCount > 0 ? container : null;
    };
    Object.defineProperty(AcTrBatchedGroup.prototype, "groups", {
        /**
         * Returns all batch maps managed by this group.
         */
        get: function () {
            return [
                this._lineBatches,
                this._lineWithIndexBatches,
                this._line2Batches,
                this._meshBatches,
                this._meshWithIndexBatches,
                this._pointBatches,
                this._pointSymbolBatches
            ];
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Removes batched slot-mask or unbatched material highlight for one entity id.
     *
     * @param objectId - Database object id whose highlight should be cleared.
     * @param containerGroup - Legacy overlay group that maps to select or hover state.
     */
    AcTrBatchedGroup.prototype.unhighlight = function (objectId, containerGroup) {
        if (containerGroup === this._selectedObjects) {
            this.unselect(objectId);
            return;
        }
        if (containerGroup === this._hoverObjects) {
            this.unhover(objectId);
        }
    };
    /**
     * Appends batched or unbatched drawables for one entity into an overlay group.
     *
     * @param objectId - Database object id whose drawables should be extracted
     * @param containerGroup - Overlay group receiving cloned or extracted drawables
     * @param options - Slot limit, overlay mode, and optional preview style
     * @returns Number of drawables appended
     */
    AcTrBatchedGroup.prototype.appendEntityOverlayDrawables = function (objectId, containerGroup, options) {
        var _this = this;
        var added = 0;
        var applyOverlayStyle = function (object) {
            var _a, _b;
            if (options.mode === 'highlight') {
                _this.applyHighlightMaterial(object);
                return;
            }
            if (options.mode === 'compare') {
                _this.applyCompareOverlayMaterial(object, (_a = options.compareColor) !== null && _a !== void 0 ? _a : 0x22c55e);
                return;
            }
            _this.applyPreviewMaterial(object, (_b = options.previewStyle) !== null && _b !== void 0 ? _b : 'normal');
        };
        var entityInfo = this._entitiesMap.get(objectId);
        if (entityInfo && added < options.maxSlots) {
            var limit = Math.min(entityInfo.length, options.maxSlots);
            for (var index = 0; index < limit; index++) {
                var item = entityInfo[index];
                var batchedObject = this.getOriginBatch(item.batchedObjectId);
                if (!batchedObject || !this.hasBatchObjectAt(batchedObject)) {
                    continue;
                }
                var object = batchedObject.getObjectAt(item.batchId);
                this.copyHighlightMetadata(batchedObject, object);
                applyOverlayStyle(object);
                var overlayUserData = getHighlightUserData(object);
                overlayUserData.objectId = objectId;
                overlayUserData.batchedObjectId = item.batchedObjectId;
                overlayUserData.disposeGeometryOnRemove =
                    batchedObject instanceof AcTrBatchedLine2;
                overlayUserData.previewDrawable = options.mode === 'preview';
                object.renderOrder = containerGroup.renderOrder;
                containerGroup.add(object);
                added++;
            }
        }
        var unbatchedObjects = this._unbatchedEntities.get(objectId);
        if (unbatchedObjects && added < options.maxSlots) {
            var limit = Math.min(unbatchedObjects.length, options.maxSlots - added);
            for (var index = 0; index < limit; index++) {
                var obj = unbatchedObjects[index];
                var overlayObj = obj.clone();
                this.copyHighlightMetadata(obj, overlayObj);
                applyOverlayStyle(overlayObj);
                var overlayUserData = getHighlightUserData(overlayObj);
                overlayUserData.objectId = objectId;
                overlayUserData.previewDrawable = options.mode === 'preview';
                overlayObj.renderOrder = containerGroup.renderOrder;
                containerGroup.add(overlayObj);
                added++;
            }
        }
        return added;
    };
    /**
     * Returns true when a batch container exposes per-slot object extraction.
     *
     * @param batchedObject - Batch container candidate for drawable extraction
     */
    AcTrBatchedGroup.prototype.hasBatchObjectAt = function (batchedObject) {
        return typeof batchedObject.getObjectAt === 'function';
    };
    /**
     * Clones materials for preview drawables without applying highlight tint.
     *
     * @param object - Drawable node or subtree root to receive cloned materials
     * @param _style - Reserved preview line style; dashed rendering is best-effort
     */
    AcTrBatchedGroup.prototype.applyPreviewMaterial = function (object, _style) {
        var _this = this;
        if (this.hasMaterial(object)) {
            object.material = AcTrMaterialUtil.cloneMaterial(object.material);
        }
        object.children.forEach(function (child) { return _this.applyPreviewMaterial(child, _style); });
    };
    /**
     * Recursively clones and recolors materials on a highlight object subtree.
     */
    AcTrBatchedGroup.prototype.applyHighlightMaterial = function (object) {
        var _this = this;
        if (this.hasMaterial(object)) {
            var clonedMaterial = AcTrMaterialUtil.cloneMaterial(object.material);
            AcTrMaterialUtil.setMaterialColor(clonedMaterial);
            object.material = clonedMaterial;
        }
        object.children.forEach(function (child) { return _this.applyHighlightMaterial(child); });
    };
    /**
     * Assigns a fresh, unpatched material in the compare role color.
     *
     * Cloning the batch material would keep the highlight shader, which tints
     * everything to the unchanged base color when the packed slot mask misses.
     */
    AcTrBatchedGroup.prototype.applyCompareOverlayMaterial = function (object, color) {
        var replace = function (node) {
            if (node instanceof LineSegments2) {
                var src = node.material;
                var material = new LineMaterial({
                    color: color,
                    linewidth: src.linewidth,
                    worldUnits: src.worldUnits,
                    dashed: false
                });
                material.resolution.copy(src.resolution);
                material.depthTest = true;
                material.polygonOffset = true;
                material.polygonOffsetFactor = -2;
                material.polygonOffsetUnits = -2;
                node.material = material;
            }
            else if (node instanceof THREE.LineSegments ||
                node instanceof THREE.Line) {
                node.material = new THREE.LineBasicMaterial({
                    color: color,
                    depthTest: true,
                    polygonOffset: true,
                    polygonOffsetFactor: -2,
                    polygonOffsetUnits: -2
                });
            }
            else if (node instanceof THREE.Mesh) {
                node.material = new THREE.MeshBasicMaterial({
                    color: color,
                    side: THREE.DoubleSide,
                    depthTest: true,
                    polygonOffset: true,
                    polygonOffsetFactor: -2,
                    polygonOffsetUnits: -2
                });
            }
            else if (node instanceof THREE.Points) {
                var src = node.material;
                node.material = new THREE.PointsMaterial({
                    color: color,
                    size: src.size,
                    sizeAttenuation: src.sizeAttenuation,
                    depthTest: true
                });
            }
            node.children.forEach(function (child) { return replace(child); });
        };
        replace(object);
    };
    /**
     * Rebinds compare-overlay wrappers onto the live packed attribute arrays.
     *
     * {@link AcTrBatchedLine.getObjectAt} shares `attributes`/`index` with the
     * batch, not the `BufferGeometry` object. `setGeometrySize` disposes those
     * arrays and allocates new ones; overlay meshes would otherwise keep the
     * disposed buffers. Line2 overlays own cloned sub-geometry and are skipped.
     */
    AcTrBatchedGroup.prototype.rebindCompareOverlaySharedBuffers = function () {
        var e_21, _a;
        if (this._compareOverlay.children.length === 0) {
            return;
        }
        try {
            for (var _b = __values(this._compareOverlay.children), _c = _b.next(); !_c.done; _c = _b.next()) {
                var child = _c.value;
                var data = getHighlightUserData(child);
                if (data.batchedObjectId == null || data.disposeGeometryOnRemove) {
                    continue;
                }
                var batch = this.getOriginBatch(data.batchedObjectId);
                var childGeometry = this.getDrawableGeometry(child);
                if (batch == null || childGeometry == null) {
                    continue;
                }
                if (childGeometry.attributes !== batch.geometry.attributes) {
                    childGeometry.index = batch.geometry.index;
                    childGeometry.attributes = batch.geometry.attributes;
                }
            }
        }
        catch (e_21_1) { e_21 = { error: e_21_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_21) throw e_21.error; }
        }
    };
    /**
     * Rebuilds the compare overlay for every current role override.
     */
    AcTrBatchedGroup.prototype.refreshCompareOverlay = function () {
        var _this = this;
        this.clearHighlightGroup(this._compareOverlay);
        if (!this._compareEnabled) {
            return;
        }
        this._compareRoles.forEach(function (role, objectId) {
            _this.appendEntityOverlayDrawables(objectId, _this._compareOverlay, {
                maxSlots: 10000,
                mode: 'compare',
                compareColor: _this.resolveCompareColor(role)
            });
        });
    };
    /**
     * Updates the compare overlay for one entity after a late batch append.
     */
    AcTrBatchedGroup.prototype.syncCompareOverlayForEntity = function (objectId) {
        this.removeCompareOverlayForEntity(objectId);
        if (!this._compareEnabled) {
            return;
        }
        var role = this._compareRoles.get(objectId);
        if (role == null) {
            return;
        }
        this.appendEntityOverlayDrawables(objectId, this._compareOverlay, {
            maxSlots: 10000,
            mode: 'compare',
            compareColor: this.resolveCompareColor(role)
        });
    };
    /**
     * Removes compare-overlay drawables owned by `objectId`.
     */
    AcTrBatchedGroup.prototype.removeCompareOverlayForEntity = function (objectId) {
        var _this = this;
        var stale = this._compareOverlay.children.filter(function (child) { return getHighlightUserData(child).objectId === objectId; });
        stale.forEach(function (child) { return _this.disposeHighlightObject(child); });
        stale.forEach(function (child) { return child.removeFromParent(); });
    };
    /**
     * Resolves a packed batch container by its Three.js object id.
     */
    AcTrBatchedGroup.prototype.getOriginBatch = function (batchedObjectId) {
        return this._originBatchById.get(batchedObjectId);
    };
    /**
     * Copies highlight-related user-data flags from source to target object.
     */
    AcTrBatchedGroup.prototype.copyHighlightMetadata = function (source, target) {
        copyHighlightObjectFlags(source, target);
    };
    /**
     * Applies entity-level visibility to one batched geometry slot.
     *
     * Batched geometry defaults to visible; DXF group code 60 and AcDbEntity
     * visibility must be reflected per slot so invisible entities are not drawn.
     */
    AcTrBatchedGroup.prototype.applyBatchSlotVisibility = function (item, visible) {
        var batchedObject = this.getOriginBatch(item.batchedObjectId);
        batchedObject === null || batchedObject === void 0 ? void 0 : batchedObject.setVisibleAt(item.batchId, visible);
    };
    /**
     * Returns visibility state for one batched geometry slot.
     */
    AcTrBatchedGroup.prototype.getBatchItemVisible = function (item) {
        var _a;
        var batchedObject = this.getOriginBatch(item.batchedObjectId);
        return (_a = batchedObject === null || batchedObject === void 0 ? void 0 : batchedObject.getVisibleAt(item.batchId)) !== null && _a !== void 0 ? _a : false;
    };
    /**
     * Adds one `THREE.LineSegments` object into matching line batch.
     */
    AcTrBatchedGroup.prototype.addLine = function (object, userData) {
        var _this = this;
        var material = object.material;
        var batches = this.getMatchedLineBatches(object);
        var worldOffset = new THREE.Vector3().setFromMatrixPosition(object.matrixWorld);
        var batchedLine = this.resolveOriginBatch(batches, material.id, worldOffset, function () {
            return new AcTrBatchedLine(AcTrBatchedGroup.INITIAL_LINE_VERTEX_CAPACITY, AcTrBatchedGroup.INITIAL_LINE_INDEX_CAPACITY, _this.createOwnedBatchMaterial(material));
        });
        // Bake rotation/scale into geometry, but keep world translation as offset.
        // This preserves block reference transforms while avoiding large Float32 coords.
        var geometry = object.geometry.clone();
        var matrixNoTranslation = object.matrixWorld.clone();
        matrixNoTranslation.setPosition(0, 0, 0);
        if (!AcTrBufferGeometryUtil.safeApplyMatrix4(geometry, matrixNoTranslation, 2)) {
            geometry.dispose();
            return null;
        }
        if (geometry.hasAttribute('lineDistance')) {
            AcTrBufferGeometryUtil.recomputeLineDistanceForLineSegments(geometry);
        }
        var geometryId = batchedLine.addGeometry(geometry, -1, -1, worldOffset);
        batchedLine.setGeometryInfo(geometryId, userData);
        geometry.dispose();
        return {
            batchedObjectId: batchedLine.id,
            batchId: geometryId
        };
    };
    /**
     * Adds one `LineSegments2` object into wide-line batch.
     */
    AcTrBatchedGroup.prototype.addLine2 = function (object, userData) {
        var _this = this;
        var material = object.material;
        var worldOffset = new THREE.Vector3().setFromMatrixPosition(object.matrixWorld);
        var batchedLine = this.resolveOriginBatch(this._line2Batches, material.id, worldOffset, function () {
            return new AcTrBatchedLine2(AcTrBatchedGroup.INITIAL_LINE_VERTEX_CAPACITY, _this.createOwnedBatchMaterial(material));
        });
        var matrixNoTranslation = object.matrixWorld.clone();
        matrixNoTranslation.setPosition(0, 0, 0);
        var geometry = this.cloneLineSegments2Geometry(object, matrixNoTranslation);
        if (!geometry) {
            return null;
        }
        var geometryId = batchedLine.addGeometry(geometry, -1, worldOffset);
        batchedLine.setGeometryInfo(geometryId, userData);
        geometry.dispose();
        return {
            batchedObjectId: batchedLine.id,
            batchId: geometryId
        };
    };
    /**
     * Adds one `THREE.Mesh` object into matching mesh batch.
     *
     * When the mesh's world transform has a negative determinant (mirrored
     * block reference), the triangle winding is reversed and `FrontSide`
     * culling would discard the fill.  In that case we swap to a
     * `BackSide` variant of the same material — zero fillrate overhead,
     * and the mesh lands in a separate batch keyed by the variant's id.
     *
     * Lines and points are unaffected by face culling and do not need
     * this treatment.
     *
     * **Static-transform assumption:** this check runs once when the mesh
     * enters the batch.  If a future feature mutates transforms after
     * batching (live edit, animation), this check will not re-run.
     */
    AcTrBatchedGroup.prototype.addMesh = function (object, userData, styleManager) {
        var _this = this;
        var material = object.material;
        // Detect mirrored transforms: a negative determinant means the
        // transform reversed triangle winding (odd number of negative scale
        // factors).  Swap to BackSide so the culler keeps these triangles.
        // det === 0 is a singular (collapsed) matrix — nothing renders, skip.
        if (object.matrixWorld.determinant() < 0) {
            material = styleManager.getBackSideVariant(material);
            object.material = material;
        }
        var batches = this.getMatchedMeshBatches(object);
        var worldOffset = new THREE.Vector3().setFromMatrixPosition(object.matrixWorld);
        var batchedMesh = this.resolveOriginBatch(batches, material.id, worldOffset, function () {
            var _a;
            var metadata = getMaterialMetadata(material);
            var drawOrder = (_a = metadata.drawOrder) !== null && _a !== void 0 ? _a : 0;
            var batch = new AcTrBatchedMesh(AcTrBatchedGroup.INITIAL_MESH_VERTEX_CAPACITY, AcTrBatchedGroup.INITIAL_MESH_INDEX_CAPACITY, _this.createOwnedBatchMaterial(material));
            // All CAD geometry lives on the same Z plane, so depth test alone
            // cannot decide which primitive wins on a shared pixel. Use the
            // material's explicit draw-order tier, derived from
            // `AcGiSubEntityTraits.drawOrder`, so hatch fills sit below
            // linework while wide polylines and text glyph meshes stay at the
            // normal linework tier.
            batch.renderOrder = drawOrder;
            return batch;
        });
        var geometry = object.geometry.clone();
        var matrixNoTranslation = object.matrixWorld.clone();
        matrixNoTranslation.setPosition(0, 0, 0);
        if (!AcTrBufferGeometryUtil.safeApplyMatrix4(geometry, matrixNoTranslation, 3)) {
            geometry.dispose();
            return null;
        }
        var geometryId = batchedMesh.addGeometry(geometry, -1, -1, worldOffset);
        batchedMesh.setGeometryInfo(geometryId, userData);
        geometry.dispose();
        return {
            batchedObjectId: batchedMesh.id,
            batchId: geometryId
        };
    };
    /**
     * Adds one `THREE.Points` object into matching point batch.
     */
    AcTrBatchedGroup.prototype.addPoint = function (object, userData) {
        var _this = this;
        var material = object.material;
        var worldOffset = new THREE.Vector3().setFromMatrixPosition(object.matrixWorld);
        var batchedPoint = this.resolveOriginBatch(this._pointBatches, material.id, worldOffset, function () {
            var batch = new AcTrBatchedPoint(AcTrBatchedGroup.INITIAL_POINT_VERTEX_CAPACITY, _this.createOwnedBatchMaterial(material));
            batch.visible = object.visible;
            return batch;
        });
        var geometry = object.geometry.clone();
        var matrixNoTranslation = object.matrixWorld.clone();
        matrixNoTranslation.setPosition(0, 0, 0);
        if (!AcTrBufferGeometryUtil.safeApplyMatrix4(geometry, matrixNoTranslation, 2)) {
            geometry.dispose();
            return null;
        }
        var geometryId = batchedPoint.addGeometry(geometry, -1, worldOffset);
        batchedPoint.setGeometryInfo(geometryId, userData);
        geometry.dispose();
        return {
            batchedObjectId: batchedPoint.id,
            batchId: geometryId
        };
    };
    /**
     * Resolves an existing batch container for one material and world offset, or
     * creates a new container when every existing origin is too far away.
     *
     * When multiple containers are eligible, picks the one whose established
     * origin is closest to `worldOffset` so rebased vertex magnitudes stay small.
     */
    AcTrBatchedGroup.prototype.resolveOriginBatch = function (batches, materialId, worldOffset, create) {
        var e_22, _a;
        var list = batches.get(materialId);
        if (list == null) {
            list = [];
            batches.set(materialId, list);
        }
        var best;
        var bestDistance = Infinity;
        try {
            for (var list_1 = __values(list), list_1_1 = list_1.next(); !list_1_1.done; list_1_1 = list_1.next()) {
                var batch_1 = list_1_1.value;
                if (!canMergeIntoBatchOrigin(batch_1.origin, worldOffset)) {
                    continue;
                }
                var origin_1 = batch_1.origin;
                if (origin_1 == null) {
                    return batch_1;
                }
                var distance = batchOriginOffsetDistance(origin_1, worldOffset);
                if (distance < bestDistance) {
                    bestDistance = distance;
                    best = batch_1;
                }
            }
        }
        catch (e_22_1) { e_22 = { error: e_22_1 }; }
        finally {
            try {
                if (list_1_1 && !list_1_1.done && (_a = list_1.return)) _a.call(list_1);
            }
            finally { if (e_22) throw e_22.error; }
        }
        if (best) {
            this.ensureBatchHighlightRenderer(best);
            return best;
        }
        var batch = create();
        if (this._compareEnabled) {
            batch.setCompareDisplayColors({
                enabled: true,
                baseColor: this._compareBaseColor,
                deletedColor: this._compareDeletedColor,
                addedColor: this._compareAddedColor,
                modifiedColor: this._compareModifiedColor
            });
        }
        list.push(batch);
        this._originBatchById.set(batch.id, batch);
        this.add(batch);
        this.ensureBatchHighlightRenderer(batch);
        return batch;
    };
    /**
     * Applies ACI-7 / foreground colour to every owned batch material clone.
     *
     * Style-manager cache entries are updated by {@link AcTrMaterialManager.changeForeground};
     * batch containers keep private clones so they must be repainted separately.
     *
     * @param color - Resolved foreground colour for the current canvas background.
     */
    AcTrBatchedGroup.prototype.repaintForegroundMaterials = function (color) {
        var e_23, _a;
        var threeColor = new THREE.Color(color);
        var paint = function (material) {
            if (getMaterialMetadata(material).isForeground === true) {
                AcTrMaterialUtil.setMaterialColor(material, threeColor);
            }
        };
        try {
            for (var _b = __values(this.groups), _c = _b.next(); !_c.done; _c = _b.next()) {
                var group = _c.value;
                group.forEach(function (batches) {
                    batches.forEach(function (batch) {
                        var material = batch.material;
                        if (Array.isArray(material)) {
                            material.forEach(paint);
                        }
                        else if (material) {
                            paint(material);
                        }
                    });
                });
            }
        }
        catch (e_23_1) { e_23 = { error: e_23_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_23) throw e_23.error; }
        }
        this._unbatchedObjects.traverse(function (object) {
            if (!('material' in object)) {
                return;
            }
            var material = object.material;
            if (Array.isArray(material)) {
                material.forEach(paint);
            }
            else if (material) {
                paint(material);
            }
        });
    };
    /**
     * Clones a style-cached material for exclusive use by one batch container.
     *
     * Batches are still grouped by the source `material.id`, but each container
     * must own a distinct `THREE.Material` instance. Highlight mask uniforms are
     * stored on the material; sharing one instance across point-symbol and line
     * batches lets a selected slot tint the same slot index in sibling batches.
     */
    AcTrBatchedGroup.prototype.createOwnedBatchMaterial = function (source) {
        var owned = AcTrMaterialUtil.cloneMaterial(source);
        setMaterialMetadata(owned, __assign({}, getMaterialMetadata(source)));
        return owned;
    };
    /**
     * Ensures the batch rebinds its own highlight mask before every draw.
     *
     * Required when multiple batches share one style-cached material; otherwise
     * a selected sibling's mask stays bound and other batches' slot 0 tint.
     */
    AcTrBatchedGroup.prototype.ensureBatchHighlightRenderer = function (batch) {
        var state = batch._highlightState;
        if (state) {
            installBatchHighlightRenderer(batch, state);
        }
    };
    /**
     * Counts batch containers across all material keys in one batch map.
     */
    AcTrBatchedGroup.prototype.countBatchContainers = function (map) {
        var count = 0;
        map.forEach(function (batches) {
            count += batches.length;
        });
        return count;
    };
    /**
     * Estimates geometry memory size for all objects in one batch map.
     */
    AcTrBatchedGroup.prototype.getBatchedGeometrySize = function (batch) {
        var _this = this;
        var memory = 0;
        batch.forEach(function (batches) {
            batches.forEach(function (value) {
                memory += _this.getGeometrySize(value);
            });
        });
        return memory;
    };
    /**
     * Estimates mapping metadata memory size for all objects in one batch map.
     */
    AcTrBatchedGroup.prototype.getBatchedGeometryMappingSize = function (batch) {
        var memory = 0;
        batch.forEach(function (batches) {
            batches.forEach(function (item) {
                memory += item.mappingStats.size;
            });
        });
        return memory;
    };
    /**
     * Resolves matching line batch map by geometry/index mode.
     */
    AcTrBatchedGroup.prototype.getMatchedLineBatches = function (object) {
        if (getSceneDrawableUserData(object).isPoint) {
            return this._pointSymbolBatches;
        }
        else {
            var hasIndex = object.geometry.getIndex() !== null;
            var batches = this._lineBatches;
            if (hasIndex) {
                batches = this._lineWithIndexBatches;
            }
            return batches;
        }
    };
    /**
     * Resolves matching mesh batch map by geometry/index mode.
     */
    AcTrBatchedGroup.prototype.getMatchedMeshBatches = function (object) {
        var hasIndex = object.geometry.getIndex() !== null;
        var batches = this._meshBatches;
        if (hasIndex) {
            batches = this._meshWithIndexBatches;
        }
        return batches;
    };
    /**
     * Estimates geometry memory usage for one render object.
     */
    AcTrBatchedGroup.prototype.getGeometrySize = function (object) {
        var _this = this;
        var visitedBuffers = new Set();
        var memory = 0;
        // Geometry memory usage
        if (this.hasGeometry(object)) {
            var geometry_1 = object.geometry;
            Object.keys(geometry_1.attributes).forEach(function (attributeName) {
                var attribute = geometry_1.attributes[attributeName];
                var array = _this.getAttributeArray(attribute);
                if (array && !visitedBuffers.has(array.buffer)) {
                    memory += array.byteLength;
                    visitedBuffers.add(array.buffer);
                }
            });
            if (geometry_1.index) {
                var indexArray = geometry_1.index.array;
                if (!visitedBuffers.has(indexArray.buffer)) {
                    memory += indexArray.byteLength;
                    visitedBuffers.add(indexArray.buffer);
                }
            }
        }
        return memory;
    };
    /**
     * Computes summary stats for objects that were not batched.
     */
    AcTrBatchedGroup.prototype.getUnbatchedStats = function () {
        var _this = this;
        var stats = {
            count: 0,
            geometrySize: 0,
            byType: {
                line: 0,
                mesh: 0,
                point: 0,
                other: 0
            }
        };
        this._unbatchedObjects.children.forEach(function (object) {
            stats.count += 1;
            stats.geometrySize += _this.getGeometrySize(object);
            if (_this.isLineObject(object)) {
                stats.byType.line += 1;
            }
            else if (object instanceof THREE.Mesh) {
                stats.byType.mesh += 1;
            }
            else if (object instanceof THREE.Points) {
                stats.byType.point += 1;
            }
            else {
                stats.byType.other += 1;
            }
        });
        return stats;
    };
    /**
     * Clones an unbatched object into world space for group ownership.
     *
     * Leaf drawables keep local geometry buffers and receive the source world
     * transform on the clone root. This preserves precision for entities that
     * rebase vertices around a local origin (lines, wide lines) instead of baking
     * large world coordinates into float32 attributes.
     */
    AcTrBatchedGroup.prototype.cloneUnbatchedObject = function (source) {
        var _a;
        if (this.shouldCloneUnbatchedSubtree(source)) {
            return this.cloneUnbatchedSubtree(source);
        }
        var cloned = source.clone();
        this.copyWorldMatrixOntoClone(source, cloned);
        if (this.hasMaterial(source) && this.hasMaterial(cloned)) {
            cloned.material = source.material;
            var sourceDrawable = getSceneDrawableUserData(source);
            var clonedDrawable = getSceneDrawableUserData(cloned);
            clonedDrawable.styleMaterialId =
                (_a = sourceDrawable.styleMaterialId) !== null && _a !== void 0 ? _a : this.getMaterialId(source.material);
            clonedDrawable.bboxIntersectionCheck =
                sourceDrawable.bboxIntersectionCheck;
            clonedDrawable.sharesTemplateGeometry =
                sourceDrawable.sharesTemplateGeometry;
            clonedDrawable.bakedWorldMatrix = source.matrixWorld.toArray();
            if (sourceDrawable.textEntityTraits) {
                clonedDrawable.textEntityTraits = AcTrMTextColorUtil.cloneEntityTraits(sourceDrawable.textEntityTraits);
            }
        }
        this.finalizeUnbatchedLineClone(cloned);
        return cloned;
    };
    /**
     * Applies line-specific setup so unbatched wide/basic lines render reliably
     * at large world coordinates (matches batched line frustum-culling behavior).
     */
    AcTrBatchedGroup.prototype.finalizeUnbatchedLineClone = function (cloned) {
        if (!this.isLineObject(cloned)) {
            return;
        }
        cloned.frustumCulled = false;
        var geometry = this.getDrawableGeometry(cloned);
        if (!geometry) {
            return;
        }
        AcTrBufferGeometryUtil.safeComputeBoundingBox(geometry);
        AcTrBufferGeometryUtil.safeComputeBoundingSphere(geometry);
    };
    /**
     * Resolves drawable geometry from supported line/mesh/point object types.
     */
    AcTrBatchedGroup.prototype.getDrawableGeometry = function (object) {
        if (object instanceof LineSegments2) {
            return object.geometry;
        }
        if (object instanceof THREE.Mesh ||
            object instanceof THREE.LineSegments ||
            object instanceof THREE.Line ||
            object instanceof THREE.Points) {
            return object.geometry;
        }
        return undefined;
    };
    /**
     * Returns true when an unbatched source is a placement root with child drawables.
     */
    AcTrBatchedGroup.prototype.shouldCloneUnbatchedSubtree = function (source) {
        return source.children.length > 0 && !this.hasGeometry(source);
    };
    /**
     * Clones a no-batch placement root together with its render children. MTEXT
     * keeps merged glyph geometry in local space under one insertion transform.
     */
    AcTrBatchedGroup.prototype.cloneUnbatchedSubtree = function (source) {
        var _this = this;
        var cloned = source.clone(true);
        this.copyWorldMatrixOntoClone(source, cloned);
        var sourceDrawable = getSceneDrawableUserData(source);
        var clonedDrawable = getSceneDrawableUserData(cloned);
        clonedDrawable.bboxIntersectionCheck = sourceDrawable.bboxIntersectionCheck;
        clonedDrawable.bakedWorldMatrix = source.matrixWorld.toArray();
        if (sourceDrawable.textEntityTraits) {
            clonedDrawable.textEntityTraits = AcTrMTextColorUtil.cloneEntityTraits(sourceDrawable.textEntityTraits);
        }
        cloned.traverse(function (child) {
            if (!_this.hasMaterial(child)) {
                return;
            }
            var childDrawable = getSceneDrawableUserData(child);
            if (childDrawable.styleMaterialId == null) {
                childDrawable.styleMaterialId = _this.getMaterialId(child.material);
            }
        });
        return cloned;
    };
    /**
     * Places an unbatched clone in world space using the source's exact
     * {@link THREE.Object3D.matrixWorld}.
     *
     * Avoids {@link THREE.Matrix4.decompose}: mirrored INSERT scales produce
     * negative determinants that do not round-trip through TRS, which previously
     * shoved attribute text (e.g. DOOR_FIRE_TEXT) tens of millions of units away
     * and blew up zoom-to-extents / layer-fit boxes.
     */
    AcTrBatchedGroup.prototype.copyWorldMatrixOntoClone = function (source, cloned) {
        source.updateMatrixWorld(true);
        cloned.matrixAutoUpdate = false;
        cloned.matrix.copy(source.matrixWorld);
        cloned.matrixWorld.copy(source.matrixWorld);
        cloned.matrixWorldNeedsUpdate = false;
    };
    /**
     * Clones `LineSegments2` geometry and bakes non-translation transforms.
     */
    AcTrBatchedGroup.prototype.cloneLineSegments2Geometry = function (object, transform) {
        var source = object.geometry;
        var instanceStart = source.getAttribute('instanceStart');
        var instanceEnd = source.getAttribute('instanceEnd');
        var count = instanceStart.count;
        var segmentPositions = [];
        for (var i = 0; i < count; i++) {
            _v1.fromBufferAttribute(instanceStart, i).applyMatrix4(transform);
            _v2.fromBufferAttribute(instanceEnd, i).applyMatrix4(transform);
            if (!Number.isFinite(_v1.x) ||
                !Number.isFinite(_v1.y) ||
                !Number.isFinite(_v1.z) ||
                !Number.isFinite(_v2.x) ||
                !Number.isFinite(_v2.y) ||
                !Number.isFinite(_v2.z)) {
                continue;
            }
            segmentPositions.push(_v1.x, _v1.y, _v1.z, _v2.x, _v2.y, _v2.z);
        }
        if (segmentPositions.length < 6) {
            return null;
        }
        var geometry = new LineSegmentsGeometry();
        geometry.setPositions(new Float32Array(segmentPositions));
        if (source.hasAttribute('instanceColorStart')) {
            geometry.setAttribute('instanceColorStart', source.getAttribute('instanceColorStart').clone());
            geometry.setAttribute('instanceColorEnd', source.getAttribute('instanceColorEnd').clone());
        }
        AcTrBufferGeometryUtil.safeComputeBoundingBox(geometry);
        AcTrBufferGeometryUtil.safeComputeBoundingSphere(geometry);
        return geometry;
    };
    /**
     * Recursively disposes one object subtree owned by this group.
     *
     * Skips {@link THREE.BufferGeometry} borrowed from an immutable block
     * template (`sharesTemplateGeometry`); unbatched INSERT clones may alias
     * those buffers via {@link THREE.Object3D.clone}.
     */
    AcTrBatchedGroup.prototype.disposeObject = function (object) {
        var _this = this;
        object.removeFromParent();
        if (this.hasGeometry(object) &&
            !getSceneDrawableUserData(object).sharesTemplateGeometry) {
            object.geometry.dispose();
        }
        object.children.forEach(function (child) { return _this.disposeObject(child); });
    };
    /**
     * Disposes all highlight children of one highlight container.
     */
    AcTrBatchedGroup.prototype.clearHighlightGroup = function (group) {
        var _this = this;
        var objects = __spreadArray([], __read(group.children), false);
        objects.forEach(function (obj) { return _this.disposeHighlightObject(obj); });
        group.clear();
    };
    /**
     * Disposes highlight object resources (cloned material and optional geometry).
     */
    AcTrBatchedGroup.prototype.disposeHighlightObject = function (object) {
        disposePreviewObjectTree(object);
    };
    /**
     * Unions world-space bounds from one unbatched drawable or its geometry leaves.
     */
    AcTrBatchedGroup.prototype.unionUnbatchedObjectBounds = function (object, target, scratch) {
        var e_24, _a;
        var geometry = this.getDrawableGeometry(object);
        if (geometry) {
            AcTrBufferGeometryUtil.safeComputeBoundingBox(geometry);
            if (!geometry.boundingBox) {
                return;
            }
            scratch.copy(geometry.boundingBox).applyMatrix4(object.matrixWorld);
            target.union(scratch);
            return;
        }
        try {
            for (var _b = __values(object.children), _c = _b.next(); !_c.done; _c = _b.next()) {
                var child = _c.value;
                if (child.visible === false)
                    continue;
                this.unionUnbatchedObjectBounds(child, target, scratch);
            }
        }
        catch (e_24_1) { e_24 = { error: e_24_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_24) throw e_24.error; }
        }
    };
    /**
     * Ray-tests one unbatched drawable, honoring bbox-only pick metadata.
     */
    AcTrBatchedGroup.prototype.isUnbatchedDrawableIntersecting = function (object, raycaster) {
        if (getSceneDrawableUserData(object).bboxIntersectionCheck) {
            return this.isUnbatchedBboxIntersecting(object, raycaster);
        }
        return raycaster.intersectObject(object, true).length > 0;
    };
    /**
     * Tests ray intersection against the world-space bounds of one unbatched drawable.
     */
    AcTrBatchedGroup.prototype.isUnbatchedBboxIntersecting = function (object, raycaster) {
        object.updateMatrixWorld(true);
        _intersectBox.makeEmpty();
        var geometry = this.getDrawableGeometry(object);
        if (geometry) {
            AcTrBufferGeometryUtil.safeComputeBoundingBox(geometry);
            if (!geometry.boundingBox) {
                return false;
            }
            _intersectBox.copy(geometry.boundingBox).applyMatrix4(object.matrixWorld);
        }
        else {
            this.unionUnbatchedObjectBounds(object, _intersectBox, _intersectScratchBox);
            if (_intersectBox.isEmpty()) {
                return false;
            }
        }
        return raycaster.ray.intersectBox(_intersectBox, _v1) !== null;
    };
    /**
     * Type guard for objects that expose `material`.
     */
    AcTrBatchedGroup.prototype.hasMaterial = function (object) {
        return 'material' in object;
    };
    /**
     * Type guard for objects that expose `geometry`.
     */
    AcTrBatchedGroup.prototype.hasGeometry = function (object) {
        return 'geometry' in object;
    };
    /**
     * Returns typed array backing one geometry attribute.
     */
    AcTrBatchedGroup.prototype.getAttributeArray = function (attribute) {
        if ('array' in attribute && attribute.array) {
            return attribute.array;
        }
        if ('data' in attribute && attribute.data && attribute.data.array) {
            return attribute.data.array;
        }
        return null;
    };
    /**
     * Returns true when object should be counted as line-like for stats.
     */
    AcTrBatchedGroup.prototype.isLineObject = function (object) {
        if (object instanceof THREE.Line)
            return true;
        return !!object
            .isLineSegments2;
    };
    /**
     * Gets deterministic material id from single/multi-material values.
     */
    AcTrBatchedGroup.prototype.getMaterialId = function (material) {
        var _a, _b;
        if (Array.isArray(material)) {
            return (_b = (_a = material[0]) === null || _a === void 0 ? void 0 : _a.id) !== null && _b !== void 0 ? _b : -1;
        }
        return material.id;
    };
    AcTrBatchedGroup.INITIAL_LINE_VERTEX_CAPACITY = 128;
    AcTrBatchedGroup.INITIAL_LINE_INDEX_CAPACITY = 256;
    AcTrBatchedGroup.INITIAL_MESH_VERTEX_CAPACITY = 128;
    AcTrBatchedGroup.INITIAL_MESH_INDEX_CAPACITY = 256;
    AcTrBatchedGroup.INITIAL_POINT_VERTEX_CAPACITY = 16;
    /**
     * Cached highlight material clones keyed by source material and highlight kind.
     *
     * Shared across all {@link AcTrBatchedGroup} instances for unbatched drawables.
     * Entries are evicted when the source material is disposed.
     */
    AcTrBatchedGroup.sharedHighlightMaterialsBySource = new WeakMap();
    AcTrBatchedGroup.onSourceMaterialDisposed = function (event) {
        AcTrBatchedGroup.releaseSharedHighlightMaterials(event.target);
    };
    return AcTrBatchedGroup;
}(THREE.Group));
export { AcTrBatchedGroup };
/**
 * Disposes one preview subset tree created by {@link AcTrBatchedGroup.createPreviewSubset}.
 *
 * Shared batch geometry buffers are never disposed; cloned materials and
 * Line2-owned geometry follow the same rules as highlight overlays.
 *
 * @param group - Preview subset root group to dispose
 */
export function disposePreviewSubset(group) {
    disposePreviewObjectTree(group);
}
/**
 * Recursively disposes preview overlay drawables without touching shared batch buffers.
 *
 * @param object - Preview subset root or descendant node
 */
function disposePreviewObjectTree(object) {
    var e_25, _a;
    try {
        for (var _b = __values(__spreadArray([], __read(object.children), false)), _c = _b.next(); !_c.done; _c = _b.next()) {
            var child = _c.value;
            disposePreviewObjectTree(child);
        }
    }
    catch (e_25_1) { e_25 = { error: e_25_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_25) throw e_25.error; }
    }
    disposePreviewObject(object);
}
/**
 * Disposes cloned preview materials and optional Line2-owned geometry on one node.
 *
 * @param object - Preview drawable node to release
 */
function disposePreviewObject(object) {
    if (hasPreviewDrawableMaterial(object)) {
        var material = object.material;
        if (Array.isArray(material)) {
            material.forEach(function (item) { return item.dispose(); });
        }
        else {
            material.dispose();
        }
    }
    if (hasPreviewDrawableGeometry(object) &&
        getHighlightUserData(object).disposeGeometryOnRemove) {
        object.geometry.dispose();
    }
}
/**
 * Type guard for drawable nodes that own preview-cloned materials.
 *
 * @param object - Candidate drawable node
 */
function hasPreviewDrawableMaterial(object) {
    return 'material' in object && object.material != null;
}
/**
 * Type guard for drawable nodes that own disposable preview geometry.
 *
 * @param object - Candidate drawable node
 */
function hasPreviewDrawableGeometry(object) {
    return 'geometry' in object && object.geometry != null;
}
var _v1 = /*@__PURE__*/ new THREE.Vector3();
var _v2 = /*@__PURE__*/ new THREE.Vector3();
var _intersectBox = /*@__PURE__*/ new THREE.Box3();
var _intersectScratchBox = /*@__PURE__*/ new THREE.Box3();
//# sourceMappingURL=AcTrBatchedGroup.js.map