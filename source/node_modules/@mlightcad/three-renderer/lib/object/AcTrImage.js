var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
import { acdbOleBlobNeedsMetafileRasterization, acdbRasterizeOleMetafile } from '@mlightcad/data-model';
import * as THREE from 'three';
import { AcTrBufferGeometryUtil } from '../util/AcTrBufferGeometryUtil';
import { AcTrEntity } from './AcTrEntity';
/**
 * Textured image drawable for {@link AcDbRasterImage} / {@link AcDbOle2Frame}.
 *
 * Raster blobs (`image/png`, `image/bmp`, …) are textured immediately.
 * OLE metafile blobs (`image/wmf`, `image/emf`) are rasterized asynchronously
 * via {@link acdbRasterizeOleMetafile} before the texture is bound — otherwise
 * Three.js `TextureLoader` fails and the mesh stays an opaque white fill.
 *
 * Metafile loads participate in {@link asyncDraw} so export/`waitUntilIdle`
 * waits for the PNG texture before capturing the canvas.
 */
var AcTrImage = /** @class */ (function (_super) {
    __extends(AcTrImage, _super);
    function AcTrImage(blob, style, context) {
        var _this = _super.call(this, context) || this;
        _this._textureReady = false;
        var shape = new THREE.Shape(style.boundary);
        var geometry = new THREE.ShapeGeometry(shape);
        _this.generateUVs(geometry);
        // Spatial pick / box selection index entities via wcsBbox. Without this,
        // filled RasterImage / Ole2Frame meshes never enter the pick candidates
        // even though their interior is raycastable.
        var boundingBox = AcTrBufferGeometryUtil.safeComputeBoundingBox(geometry);
        if (boundingBox) {
            _this.wcsBbox = boundingBox;
        }
        // Transparent until a real texture arrives — avoids the white OLE placeholder.
        _this._material = new THREE.MeshBasicMaterial({
            side: THREE.DoubleSide,
            transparent: true,
            opacity: 0,
            depthWrite: false
        });
        _this._mesh = new THREE.Mesh(geometry, _this._material);
        _this.add(_this._mesh);
        _this.finalizeLeafDrawables();
        if (acdbOleBlobNeedsMetafileRasterization(blob)) {
            _this._texturePromise = _this.loadMetafileTexture(blob);
        }
        else {
            _this._texturePromise = _this.loadRasterTexture(blob);
        }
        return _this;
    }
    AcTrImage.prototype.resolveDrawMode = function () {
        return 'unbatch';
    };
    /**
     * Metafile textures are not drawable until rasterization finishes — keep
     * {@link asyncDraw} on the critical path for exports.
     */
    AcTrImage.prototype.hasDrawableGeometry = function () {
        return this._textureReady;
    };
    AcTrImage.prototype.asyncDraw = function () {
        return __awaiter(this, void 0, void 0, function () {
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0: return [4 /*yield*/, this._texturePromise];
                    case 1:
                        _a.sent();
                        return [2 /*return*/];
                }
            });
        });
    };
    /**
     * Generate UVs for the specified THREE.ShapeGeometry instance. THREE.ShapeGeometry does not automatically
     * generate UVs. To apply textures, we need to manually generate the UV coordinates for your shape.
     * @param geometry Input geometry to generate UVs
     */
    AcTrImage.prototype.generateUVs = function (geometry) {
        var position = geometry.attributes.position.array;
        var uv = new Float32Array((position.length / 3) * 2);
        var minX = Math.min.apply(Math, __spreadArray([], __read(position.filter(function (_, i) { return i % 3 === 0; })), false));
        var maxX = Math.max.apply(Math, __spreadArray([], __read(position.filter(function (_, i) { return i % 3 === 0; })), false));
        var minY = Math.min.apply(Math, __spreadArray([], __read(position.filter(function (_, i) { return i % 3 === 1; })), false));
        var maxY = Math.max.apply(Math, __spreadArray([], __read(position.filter(function (_, i) { return i % 3 === 1; })), false));
        var width = maxX - minX;
        var height = maxY - minY;
        for (var i = 0; i < position.length; i += 3) {
            var x = position[i];
            var y = position[i + 1];
            uv[(i / 3) * 2] = (x - minX) / width;
            uv[(i / 3) * 2 + 1] = (y - minY) / height;
        }
        geometry.setAttribute('uv', new THREE.BufferAttribute(uv, 2));
    };
    AcTrImage.prototype.loadRasterTexture = function (blob) {
        var _this = this;
        return new Promise(function (resolve) {
            var blobUrl = URL.createObjectURL(blob);
            var textureLoader = new THREE.TextureLoader();
            var texture = textureLoader.load(blobUrl, function () {
                URL.revokeObjectURL(blobUrl);
                _this.applyTexture(texture);
                resolve();
            }, undefined, function () {
                URL.revokeObjectURL(blobUrl);
                _this._textureReady = true;
                resolve();
            });
            texture.colorSpace = THREE.SRGBColorSpace;
        });
    };
    AcTrImage.prototype.loadMetafileTexture = function (blob) {
        return __awaiter(this, void 0, void 0, function () {
            var png, _a;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        _b.trys.push([0, 3, , 4]);
                        return [4 /*yield*/, acdbRasterizeOleMetafile(blob, {
                                // Cap huge Excel tables so first paint stays responsive.
                                maxWidth: 4096,
                                maxHeight: 4096,
                                dpiScale: 1
                            })];
                    case 1:
                        png = _b.sent();
                        if (!png) {
                            this._textureReady = true;
                            return [2 /*return*/];
                        }
                        return [4 /*yield*/, this.loadRasterTexture(png)];
                    case 2:
                        _b.sent();
                        return [3 /*break*/, 4];
                    case 3:
                        _a = _b.sent();
                        // Leave the transparent placeholder mesh; outline-only fallback is
                        // handled by AcDbOle2Frame when extraction itself fails.
                        this._textureReady = true;
                        return [3 /*break*/, 4];
                    case 4: return [2 /*return*/];
                }
            });
        });
    };
    AcTrImage.prototype.applyTexture = function (texture) {
        this._material.map = texture;
        this._material.opacity = 1;
        this._material.transparent = true;
        this._material.depthWrite = true;
        this._material.needsUpdate = true;
        this._textureReady = true;
    };
    return AcTrImage;
}(AcTrEntity));
export { AcTrImage };
//# sourceMappingURL=AcTrImage.js.map