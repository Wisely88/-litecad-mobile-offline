var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
import { resolveShapeGlyphKey, resolveShapeTextStyle } from '../util';
import { AcTrGlyphEntity } from './AcTrGlyphEntity';
/**
 * Display object for a CAD SHAPE entity rendered through the mtext-renderer.
 */
var AcTrShape = /** @class */ (function (_super) {
    __extends(AcTrShape, _super);
    /**
     * Creates a SHAPE display object.
     *
     * @param shape SHAPE definition and placement from the CAD database.
     * @param traits CAD sub-entity traits used to resolve color and layer behavior.
     * @param style Optional text style override; resolved against shape and context when omitted.
     * @param context Active renderer context that owns style and batching policy.
     * @param delay When `true`, skips the initial draw so callers can finish setup first.
     */
    function AcTrShape(shape, traits, style, context, _delay) {
        if (_delay === void 0) { _delay = false; }
        var _this = _super.call(this, context, traits, resolveShapeTextStyle(shape, style, context)) || this;
        _this._shape = shape;
        return _this;
        // Geometry is built by syncDraw/asyncDraw in AcTrView2d / AcTrGroup so
        // font-awaiting asyncDraw can run without blocking other entity converts.
    }
    /**
     * Builds renderer input with only the glyph key that should be resolved.
     *
     * Chooses either a non-numeric name or a shape code via
     * {@link resolveShapeGlyphKey}, then clears the unused field so mtext-renderer
     * does not incorrectly fall back between them.
     *
     * @returns SHAPE payload suitable for the mtext-renderer.
     */
    AcTrShape.prototype.toRenderableShapeData = function () {
        var source = this._shape;
        var _a = resolveShapeGlyphKey(this._shape), byName = _a.byName, byCode = _a.byCode;
        return __assign(__assign({}, source), { name: byName, shapeNumber: byCode });
    };
    /**
     * @inheritdoc
     */
    AcTrShape.prototype.getDrawPosition = function () {
        return this._shape.position;
    };
    /**
     * @inheritdoc
     */
    AcTrShape.prototype.renderSync = function (renderer) {
        return renderer.syncRenderShape(this.toRenderableShapeData(), this._style, this._colorSettings);
    };
    /**
     * @inheritdoc
     */
    AcTrShape.prototype.renderAsync = function (renderer) {
        return __awaiter(this, void 0, void 0, function () {
            return __generator(this, function (_a) {
                return [2 /*return*/, renderer.asyncRenderShape(this.toRenderableShapeData(), this._style, this._colorSettings)];
            });
        });
    };
    /**
     * @inheritdoc
     */
    AcTrShape.prototype.describeRenderFailure = function () {
        var _a, _b;
        var label = ((_a = this._shape.name) === null || _a === void 0 ? void 0 : _a.trim()) || String((_b = this._shape.shapeNumber) !== null && _b !== void 0 ? _b : '');
        return "shape '".concat(label, "'");
    };
    /**
     * Preserves SHAPE payload across INSERT template clones.
     *
     * {@link AcTrEntity.fastDeepClone} would otherwise create a plain
     * {@link AcTrEntity} shell that can no longer {@link asyncDraw} glyphs.
     */
    AcTrShape.prototype.fastDeepClone = function (shareGeometry) {
        if (shareGeometry === void 0) { shareGeometry = false; }
        var cloned = new AcTrShape(__assign(__assign({}, this._shape), { position: __assign({}, this._shape.position) }), this.traitsForClone(), __assign({}, this._style), this.renderContext);
        cloned.copyGlyphIdentity(this);
        this.copyGeometry(this, cloned, shareGeometry);
        return cloned;
    };
    return AcTrShape;
}(AcTrGlyphEntity));
export { AcTrShape };
//# sourceMappingURL=AcTrShape.js.map