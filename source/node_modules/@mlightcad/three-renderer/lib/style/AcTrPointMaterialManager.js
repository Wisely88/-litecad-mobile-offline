var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import * as THREE from 'three';
import { AcTrMaterialManager } from './AcTrMaterialManager';
/**
 * Material manager dedicated to point entities.
 *
 * Produces and caches THREE.PointsMaterial instances keyed by color and size.
 * This ensures that repeated point drawings reuse the same optimized material.
 */
var AcTrPointMaterialManager = /** @class */ (function (_super) {
    __extends(AcTrPointMaterialManager, _super);
    function AcTrPointMaterialManager() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    /**
     * Builds a stable material key from traits.
     */
    AcTrPointMaterialManager.prototype.buildKey = function (traits, options) {
        var _a;
        var size = (_a = options.size) !== null && _a !== void 0 ? _a : 1;
        var drawOrderSuffix = this.buildDrawOrderSuffix(traits);
        var colorKey = this.buildKeyColorSegment(traits);
        return "".concat(traits.layer, "_").concat(colorKey, "_").concat(size).concat(drawOrderSuffix);
    };
    /** Returns true if color is ByLayer. */
    AcTrPointMaterialManager.prototype.hasByLayerKeyTraits = function (traits) {
        return traits.color.isByLayer;
    };
    AcTrPointMaterialManager.prototype.createMaterialImpl = function (traits, options, layerColorRgb) {
        if (options === void 0) { options = {}; }
        return new THREE.PointsMaterial({
            color: this.resolveMaterialRgb(traits, layerColorRgb),
            size: options.size
        });
    };
    return AcTrPointMaterialManager;
}(AcTrMaterialManager));
export { AcTrPointMaterialManager };
//# sourceMappingURL=AcTrPointMaterialManager.js.map