import { AcGePoint3dLike } from '@mlightcad/data-model';
import * as THREE from 'three';
export interface AcTrPointSymbolGeometry {
    line?: THREE.BufferGeometry;
    point?: THREE.BufferGeometry;
}
/**
 * Internal class used to create point style geometry
 * @internal
 */
export declare class AcTrPointSymbolCreator {
    private static _instance?;
    private _symbols;
    private constructor();
    static get instance(): AcTrPointSymbolCreator;
    /**
     * Return true if showing one point using THREE.Points
     */
    isShowPoint(displayMode?: number | null): displayMode is 0 | 32 | 64 | 96 | null;
    /**
     * Resolves the world-unit scale applied to unit point-symbol templates.
     *
     * Templates are authored so the circle diameter (and overall marker size for
     * modes that include a circle) equals `1`. Positive {@link displaySize}
     * (`PDSIZE`) is an absolute size in drawing units, matching AutoCAD.
     * Non-positive sizes keep the unit template (viewport-relative `PDSIZE`
     * handling is applied by callers when they can supply a viewport height).
     *
     * @param displaySize - Point display size from `PDSIZE` / {@link AcGiPointStyle.displaySize}.
     * @returns Scale factor applied to cloned symbol geometry.
     */
    static resolveDisplayScale(displaySize: number): number;
    create(displayMode?: number | null, _point?: AcGePoint3dLike, displaySize?: number): AcTrPointSymbolGeometry;
    private initialize;
}
//# sourceMappingURL=AcTrPointSymbolCreator.d.ts.map