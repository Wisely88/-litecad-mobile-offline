var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { acTrHtmlCssColor } from './AcTrHtmlColorUtil';
import { AC_TR_HTML_SELECTED_CLASS, AcTrHtmlElement } from './AcTrHtmlElement';
/** Class on overlay endpoint grips (hidden until the parent group is selected). */
export var AC_TR_HTML_GRIP_CLASS = 'ml-html-grip';
/** Class toggled on overlay grips while a grip drag is in progress. */
export var AC_TR_HTML_GRIP_DRAGGING_CLASS = 'ml-html-grip-dragging';
/** Shared visual class used by CAD entity grips. */
export var ML_GRIP_HANDLE_CLASS = 'ml-grip-handle';
var GRIP_HANDLE_STYLE_ID = 'ml-grip-handle-style';
var HTML_GRIP_STYLE_ID = 'ml-html-grip-style';
/**
 * Injects the shared square-grip stylesheet used by CAD entity grips.
 *
 * Safe to call more than once. No-op when `document` is unavailable.
 */
export function injectMlGripHandleCss() {
    if (typeof document === 'undefined')
        return;
    if (document.getElementById(GRIP_HANDLE_STYLE_ID))
        return;
    var style = document.createElement('style');
    style.id = GRIP_HANDLE_STYLE_ID;
    style.textContent = "\n      .".concat(ML_GRIP_HANDLE_CLASS, " {\n        position: absolute;\n        width: var(--ml-ui-grip-size, 8px);\n        height: var(--ml-ui-grip-size, 8px);\n        pointer-events: auto;\n        transform: translate(-50%, -50%);\n        z-index: 5;\n        box-sizing: border-box;\n        cursor: pointer;\n      }\n\n      .").concat(ML_GRIP_HANDLE_CLASS, "-normal {\n        background: var(--ml-ui-grip-normal, #0080ff);\n      }\n\n      .").concat(ML_GRIP_HANDLE_CLASS, ":hover,\n      .").concat(ML_GRIP_HANDLE_CLASS, "-hover,\n      .").concat(ML_GRIP_HANDLE_CLASS, "-hot {\n        background: var(--ml-ui-grip-hot, #ff0000);\n      }\n    ");
    document.head.appendChild(style);
}
function injectHtmlGripCss() {
    if (typeof document === 'undefined')
        return;
    if (document.getElementById(HTML_GRIP_STYLE_ID))
        return;
    var style = document.createElement('style');
    style.id = HTML_GRIP_STYLE_ID;
    style.textContent = "\n      .".concat(AC_TR_HTML_GRIP_CLASS, " {\n        visibility: hidden;\n        pointer-events: none;\n      }\n      .").concat(AC_TR_HTML_GRIP_CLASS, ".").concat(AC_TR_HTML_SELECTED_CLASS, " {\n        visibility: visible;\n        pointer-events: auto;\n      }\n      .").concat(AC_TR_HTML_GRIP_CLASS, ".").concat(AC_TR_HTML_GRIP_DRAGGING_CLASS, " {\n        visibility: hidden !important;\n        pointer-events: none !important;\n      }\n    ");
    document.head.appendChild(style);
}
/** True when `el` is an overlay HTML endpoint grip. */
export function acTrIsHtmlGrip(el) {
    return !!(el === null || el === void 0 ? void 0 : el.classList.contains(AC_TR_HTML_GRIP_CLASS));
}
/**
 * Hide or restore every overlay endpoint grip (matches entity grip drag:
 * all handles disappear until the drag finishes).
 */
export function acTrSetHtmlGripsDragging(dragging, root) {
    if (root === void 0) { root = document; }
    root.querySelectorAll(".".concat(AC_TR_HTML_GRIP_CLASS)).forEach(function (node) {
        node.classList.toggle(AC_TR_HTML_GRIP_DRAGGING_CLASS, dragging);
    });
}
/**
 * Circular overlay endpoint, shown only when the parent HTML group is selected.
 * Visual matches {@link AcTrHtmlDot} and scales with view using WCS size.
 */
var AcTrHtmlGrip = /** @class */ (function (_super) {
    __extends(AcTrHtmlGrip, _super);
    function AcTrHtmlGrip(options) {
        var _a;
        return _super.call(this, AcTrHtmlGrip.createElement(options.color), __assign(__assign({}, options), { scaleWithView: (_a = options.scaleWithView) !== null && _a !== void 0 ? _a : true })) || this;
    }
    /** Update the marker fill color. */
    AcTrHtmlGrip.prototype.setColor = function (color) {
        this.element.style.background = acTrHtmlCssColor(color);
    };
    AcTrHtmlGrip.createElement = function (color) {
        injectHtmlGripCss();
        var el = document.createElement('div');
        el.className = "ml-html-dot ".concat(AC_TR_HTML_GRIP_CLASS);
        el.style.cssText =
            'width:12px;height:12px;border-radius:50%;' +
                "background:".concat(acTrHtmlCssColor(color), ";border:2px solid var(--ml-ui-border, #fff);box-sizing:border-box;") +
                'transform:translate(-50%,-50%);cursor:grab;';
        return el;
    };
    return AcTrHtmlGrip;
}(AcTrHtmlElement));
export { AcTrHtmlGrip };
//# sourceMappingURL=AcTrHtmlGrip.js.map