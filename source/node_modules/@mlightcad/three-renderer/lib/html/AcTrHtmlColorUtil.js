/** Converts an AcCmColor to a CSS color string, with rgb() fallback. */
export function acTrHtmlCssColor(c) {
    var _a;
    // Prefer live RGB channels so palette edits are reflected immediately.
    // `cssColor` can lag behind ACI/index mutations on some AcCmColor builds.
    if (Number.isFinite(c.red) &&
        Number.isFinite(c.green) &&
        Number.isFinite(c.blue)) {
        return "rgb(".concat(c.red, ", ").concat(c.green, ", ").concat(c.blue, ")");
    }
    return (_a = c.cssColor) !== null && _a !== void 0 ? _a : '#ffffff';
}
/** Converts an AcCmColor to a CSS rgba() string. */
export function acTrHtmlCssColorAlpha(c, alpha) {
    return "rgba(".concat(c.red, ", ").concat(c.green, ", ").concat(c.blue, ", ").concat(alpha, ")");
}
//# sourceMappingURL=AcTrHtmlColorUtil.js.map