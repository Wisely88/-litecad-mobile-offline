var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AC_TR_HTML_DEFAULT_LAYER } from './AcTrHtmlElement';
import { acTrIsHtmlGrip } from './AcTrHtmlGrip';
/**
 * Logical group of related HTML overlays.
 *
 * Unlike a leaf element, a group has no world-space CSS2D anchor of its own.
 * Adding a group to {@link AcTrHtmlTransientManager} publishes every CSS2D
 * child and (when selectable) wires click → group selection. Screen-space
 * {@link AcTrHtmlCanvasOverlay} members are owned by the group (visibility /
 * selection / dispose) and are not published into the CSS2D scene.
 */
var AcTrHtmlGroup = /** @class */ (function () {
    function AcTrHtmlGroup(options) {
        var _a;
        this._children = [];
        this._canvases = [];
        this._selected = false;
        this._visible = true;
        this._clickCleanups = [];
        this.id = options.id;
        this.layer = (_a = options.layer) !== null && _a !== void 0 ? _a : AC_TR_HTML_DEFAULT_LAYER;
        this.layoutId = options.layoutId;
        this.selectable = options.selectable === true;
        this.onSelectedChanged = options.onSelectedChanged;
        this.onVisibleChanged = options.onVisibleChanged;
        this.onDispose = options.onDispose;
    }
    Object.defineProperty(AcTrHtmlGroup.prototype, "children", {
        /** CSS2D child overlays currently owned by this group. */
        get: function () {
            return this._children;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrHtmlGroup.prototype, "canvases", {
        /** Screen-space canvas overlays currently owned by this group. */
        get: function () {
            return this._canvases;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrHtmlGroup.prototype, "selected", {
        /** Whether the group is currently drawn as selected. */
        get: function () {
            return this._selected;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrHtmlGroup.prototype, "visible", {
        /** Whether the group's children are currently visible. */
        get: function () {
            return this._visible;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Append one or more CSS2D child overlays.
     * Children keep their own ids / world positions / layers.
     */
    AcTrHtmlGroup.prototype.add = function () {
        var e_1, _a;
        var children = [];
        for (var _i = 0; _i < arguments.length; _i++) {
            children[_i] = arguments[_i];
        }
        try {
            for (var children_1 = __values(children), children_1_1 = children_1.next(); !children_1_1.done; children_1_1 = children_1.next()) {
                var child = children_1_1.value;
                this._children.push(child);
                if (this.selectable && !acTrIsHtmlGrip(child.element)) {
                    child.element.style.pointerEvents = 'auto';
                    child.element.style.cursor = 'pointer';
                }
                child.object.visible = this._visible;
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (children_1_1 && !children_1_1.done && (_a = children_1.return)) _a.call(children_1);
            }
            finally { if (e_1) throw e_1.error; }
        }
        // If already bound to a manager, rebind so new children receive clicks.
        if (this._onSelectRequest) {
            this.bindSelection(this._onSelectRequest);
        }
        return this;
    };
    /**
     * Append one or more viewport canvas overlays owned by this group.
     * Visibility / selection follow the group; dispose removes the canvases.
     */
    AcTrHtmlGroup.prototype.addCanvas = function () {
        var e_2, _a;
        var canvases = [];
        for (var _i = 0; _i < arguments.length; _i++) {
            canvases[_i] = arguments[_i];
        }
        try {
            for (var canvases_1 = __values(canvases), canvases_1_1 = canvases_1.next(); !canvases_1_1.done; canvases_1_1 = canvases_1.next()) {
                var canvas = canvases_1_1.value;
                this._canvases.push(canvas);
                canvas.setVisible(this._visible);
                if (this._selected) {
                    canvas.setSelected(true);
                }
            }
        }
        catch (e_2_1) { e_2 = { error: e_2_1 }; }
        finally {
            try {
                if (canvases_1_1 && !canvases_1_1.done && (_a = canvases_1.return)) _a.call(canvases_1);
            }
            finally { if (e_2) throw e_2.error; }
        }
        return this;
    };
    /**
     * Toggle selection visuals on every child / canvas and notify listeners.
     */
    AcTrHtmlGroup.prototype.setSelected = function (selected) {
        var e_3, _a, e_4, _b;
        var _c;
        if (this._selected === selected)
            return;
        this._selected = selected;
        try {
            for (var _d = __values(this._children), _e = _d.next(); !_e.done; _e = _d.next()) {
                var child = _e.value;
                child.setSelected(selected);
            }
        }
        catch (e_3_1) { e_3 = { error: e_3_1 }; }
        finally {
            try {
                if (_e && !_e.done && (_a = _d.return)) _a.call(_d);
            }
            finally { if (e_3) throw e_3.error; }
        }
        try {
            for (var _f = __values(this._canvases), _g = _f.next(); !_g.done; _g = _f.next()) {
                var canvas = _g.value;
                canvas.setSelected(selected);
            }
        }
        catch (e_4_1) { e_4 = { error: e_4_1 }; }
        finally {
            try {
                if (_g && !_g.done && (_b = _f.return)) _b.call(_f);
            }
            finally { if (e_4) throw e_4.error; }
        }
        (_c = this.onSelectedChanged) === null || _c === void 0 ? void 0 : _c.call(this, selected, this);
    };
    /**
     * Show or hide every child / canvas overlay and notify listeners.
     */
    AcTrHtmlGroup.prototype.setVisible = function (visible) {
        var e_5, _a, e_6, _b;
        var _c;
        if (this._visible === visible)
            return;
        this._visible = visible;
        try {
            for (var _d = __values(this._children), _e = _d.next(); !_e.done; _e = _d.next()) {
                var child = _e.value;
                child.object.visible = visible;
            }
        }
        catch (e_5_1) { e_5 = { error: e_5_1 }; }
        finally {
            try {
                if (_e && !_e.done && (_a = _d.return)) _a.call(_d);
            }
            finally { if (e_5) throw e_5.error; }
        }
        try {
            for (var _f = __values(this._canvases), _g = _f.next(); !_g.done; _g = _f.next()) {
                var canvas = _g.value;
                canvas.setVisible(visible);
            }
        }
        catch (e_6_1) { e_6 = { error: e_6_1 }; }
        finally {
            try {
                if (_g && !_g.done && (_b = _f.return)) _b.call(_f);
            }
            finally { if (e_6) throw e_6.error; }
        }
        (_c = this.onVisibleChanged) === null || _c === void 0 ? void 0 : _c.call(this, visible, this);
    };
    /**
     * Wire child click handlers. Called by {@link AcTrHtmlTransientManager}
     * after the group is added.
     */
    AcTrHtmlGroup.prototype.bindSelection = function (onSelect) {
        var e_7, _a;
        var _this = this;
        this.unbindSelection();
        this._onSelectRequest = onSelect;
        if (!this.selectable)
            return;
        var _loop_1 = function (child) {
            if (acTrIsHtmlGrip(child.element))
                return "continue";
            child.element.style.pointerEvents = 'auto';
            child.element.style.cursor = 'pointer';
            var handler = function (e) {
                var target = e.target;
                if ((target === null || target === void 0 ? void 0 : target.isContentEditable) ||
                    (target === null || target === void 0 ? void 0 : target.closest('[contenteditable="true"]'))) {
                    e.stopPropagation();
                    return;
                }
                // Do not preventDefault: that suppresses the following dblclick.
                e.stopPropagation();
                onSelect(_this);
            };
            child.element.addEventListener('click', handler);
            this_1._clickCleanups.push(function () {
                return child.element.removeEventListener('click', handler);
            });
        };
        var this_1 = this;
        try {
            for (var _b = __values(this._children), _c = _b.next(); !_c.done; _c = _b.next()) {
                var child = _c.value;
                _loop_1(child);
            }
        }
        catch (e_7_1) { e_7 = { error: e_7_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_7) throw e_7.error; }
        }
    };
    /** Remove click handlers previously installed by {@link bindSelection}. */
    AcTrHtmlGroup.prototype.unbindSelection = function () {
        var e_8, _a;
        try {
            for (var _b = __values(this._clickCleanups), _c = _b.next(); !_c.done; _c = _b.next()) {
                var cleanup = _c.value;
                cleanup();
            }
        }
        catch (e_8_1) { e_8 = { error: e_8_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_8) throw e_8.error; }
        }
        this._clickCleanups = [];
    };
    /**
     * Release selection bindings, dispose owned canvases, and invoke
     * {@link onDispose}. CSS2D child disposal is owned by the manager.
     */
    AcTrHtmlGroup.prototype.dispose = function () {
        var e_9, _a;
        var _b;
        this.unbindSelection();
        this._onSelectRequest = undefined;
        try {
            for (var _c = __values(this._canvases), _d = _c.next(); !_d.done; _d = _c.next()) {
                var canvas = _d.value;
                canvas.dispose();
            }
        }
        catch (e_9_1) { e_9 = { error: e_9_1 }; }
        finally {
            try {
                if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
            }
            finally { if (e_9) throw e_9.error; }
        }
        this._canvases.length = 0;
        try {
            (_b = this.onDispose) === null || _b === void 0 ? void 0 : _b.call(this);
        }
        catch (_e) {
            // Domain dispose must not block manager cleanup.
        }
    };
    return AcTrHtmlGroup;
}());
export { AcTrHtmlGroup };
//# sourceMappingURL=AcTrHtmlGroup.js.map