var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { acTrHtmlCssColor } from './AcTrHtmlColorUtil';
import { AcTrHtmlElement } from './AcTrHtmlElement';
/**
 * Capsule-shaped HTML label used for overlay text (e.g. measurement results).
 */
var AcTrHtmlBadge = /** @class */ (function (_super) {
    __extends(AcTrHtmlBadge, _super);
    function AcTrHtmlBadge(options) {
        var _a, _b;
        return _super.call(this, AcTrHtmlBadge.createElement(options.color, (_a = options.text) !== null && _a !== void 0 ? _a : '', options.fontSize, options.transform), __assign(__assign({}, options), { scaleWithView: (_b = options.scaleWithView) !== null && _b !== void 0 ? _b : true })) || this;
    }
    /** Update the badge label text. */
    AcTrHtmlBadge.prototype.setText = function (text) {
        this.element.textContent = text;
    };
    /** Update the badge font size (CSS px). */
    AcTrHtmlBadge.prototype.setFontSize = function (fontSize) {
        if (!(fontSize > 0))
            return;
        this.element.style.fontSize = "".concat(fontSize, "px");
    };
    /** Update the badge text color and matching capsule border. */
    AcTrHtmlBadge.prototype.setColor = function (color) {
        var css = acTrHtmlCssColor(color);
        this.element.style.color = css;
        this.element.style.borderColor = css;
    };
    AcTrHtmlBadge.createElement = function (color, text, fontSize, transform) {
        var el = document.createElement('div');
        el.className = 'ml-html-badge';
        el.textContent = text;
        var css = acTrHtmlCssColor(color);
        var size = fontSize != null && fontSize > 0 ? fontSize : 13;
        el.style.cssText =
            "background:var(--ml-ui-bg, rgba(255,255,255,0.95));color:".concat(css, ";") +
                "border:1px solid ".concat(css, ";box-sizing:border-box;") +
                "font-size:".concat(size, "px;font-family:sans-serif;font-weight:500;") +
                'padding:3px 14px;border-radius:20px;pointer-events:none;' +
                "transform:".concat(transform !== null && transform !== void 0 ? transform : 'translate(-50%,-50%)', ";white-space:nowrap;") +
                'box-shadow:var(--ml-ui-shadow, 0 1px 4px rgba(0,0,0,0.2));';
        return el;
    };
    return AcTrHtmlBadge;
}(AcTrHtmlElement));
export { AcTrHtmlBadge };
//# sourceMappingURL=AcTrHtmlBadge.js.map