var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { acTrHtmlCssColor } from './AcTrHtmlColorUtil';
import { AcTrHtmlElement } from './AcTrHtmlElement';
var STAMP_LABELS = {
    approved: 'APPROVED',
    rejected: 'REJECTED',
    revised: 'REVISED',
    'for-review': 'FOR REVIEW',
    custom: 'STAMP'
};
/**
 * Rectangular review stamp anchored in world space.
 */
var AcTrHtmlStamp = /** @class */ (function (_super) {
    __extends(AcTrHtmlStamp, _super);
    function AcTrHtmlStamp(options) {
        var _a;
        return _super.call(this, AcTrHtmlStamp.createElement(options), __assign(__assign({}, options), { scaleWithView: (_a = options.scaleWithView) !== null && _a !== void 0 ? _a : true })) || this;
    }
    /** Update the stamp caption text. */
    AcTrHtmlStamp.prototype.setText = function (text) {
        var caption = this.element.querySelector('.ml-html-stamp-text');
        if (caption)
            caption.textContent = text;
    };
    AcTrHtmlStamp.createElement = function (options) {
        var _a, _b;
        var el = document.createElement('div');
        el.className = 'ml-html-stamp';
        var accent = acTrHtmlCssColor(options.color);
        el.style.cssText =
            'display:flex;flex-direction:column;align-items:center;gap:4px;' +
                'pointer-events:none;transform:translate(-50%,-50%);' +
                'font-family:sans-serif;';
        if (options.imageUrl) {
            var img = document.createElement('img');
            img.src = options.imageUrl;
            img.alt = (_a = options.text) !== null && _a !== void 0 ? _a : options.stampId;
            img.style.cssText =
                'max-width:96px;max-height:96px;object-fit:contain;' +
                    'border:2px solid ' +
                    accent +
                    ';border-radius:4px;background:var(--ml-ui-bg, #fff);';
            el.appendChild(img);
        }
        else {
            var badge = document.createElement('div');
            badge.className = 'ml-html-stamp-badge';
            badge.style.cssText =
                "border:3px solid ".concat(accent, ";color:").concat(accent, ";") +
                    'background:rgba(255,255,255,0.92);padding:8px 14px;' +
                    'font-size:14px;font-weight:800;letter-spacing:0.06em;' +
                    'border-radius:4px;transform:rotate(-12deg);' +
                    'box-shadow:var(--ml-ui-shadow, 0 1px 4px rgba(0,0,0,0.2));' +
                    'white-space:nowrap;';
            badge.textContent =
                (_b = STAMP_LABELS[options.stampId]) !== null && _b !== void 0 ? _b : options.stampId.toUpperCase();
            el.appendChild(badge);
        }
        if (options.text) {
            var caption = document.createElement('div');
            caption.className = 'ml-html-stamp-text';
            caption.style.cssText =
                "color:".concat(accent, ";font-size:11px;font-weight:600;background:var(--ml-ui-bg, rgba(255,255,255,0.9));") +
                    'padding:1px 6px;border-radius:10px;';
            caption.textContent = options.text;
            el.appendChild(caption);
        }
        return el;
    };
    return AcTrHtmlStamp;
}(AcTrHtmlElement));
export { AcTrHtmlStamp };
//# sourceMappingURL=AcTrHtmlStamp.js.map