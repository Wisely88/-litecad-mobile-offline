var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { effectivePdfLayer } from '../pdf/AcPdfEffectiveLayer';
import { AcPdfEntity } from '../renderer/AcPdfEntity';
/**
 * Paper-space viewport content backed by the shared model-space drawables.
 *
 * The exporter walks model space once and reuses the same {@link AcPdfEntity}
 * tree for every viewport (and every paper layout): this node only supplies
 * the viewport's clip rectangle and model→paper matrix at paint time. This
 * replaces the former per-viewport deep clone, which duplicated all
 * tessellated geometry and dominated heap use on drawings with several
 * viewports.
 *
 * Shared trees are never mutated while painting: the matrix is passed through
 * {@link AcPdfPaintContext.localToDrawing} and draw ops are transformed into
 * transient copies, so one tree can be painted through any number of
 * viewports in sequence.
 */
var AcPdfViewportContent = /** @class */ (function (_super) {
    __extends(AcPdfViewportContent, _super);
    function AcPdfViewportContent(sharedRoots, modelToPaper, paperBox) {
        var _this = _super.call(this) || this;
        _this._sharedRoots = sharedRoots;
        _this._modelToPaper = modelToPaper.clone();
        _this.entityType = 'VIEWPORT_CONTENT';
        // Page framing and clipping both use the paper-space frame.
        _this.setClipBox(paperBox);
        return _this;
    }
    /**
     * Image pre-embedding walks every op, including geometry that is only
     * reachable through the shared model roots.
     */
    AcPdfViewportContent.prototype.forEachOp = function (visitor) {
        var e_1, _a;
        try {
            for (var _b = __values(this._sharedRoots), _c = _b.next(); !_c.done; _c = _b.next()) {
                var root = _c.value;
                root.forEachOp(visitor);
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_1) throw e_1.error; }
        }
    };
    AcPdfViewportContent.prototype.forEachWorldOp = function (visitor, parentMatrix) {
        var e_2, _a;
        var matrix = parentMatrix
            ? parentMatrix.clone().multiply(this._modelToPaper)
            : this._modelToPaper.clone();
        try {
            for (var _b = __values(this._sharedRoots), _c = _b.next(); !_c.done; _c = _b.next()) {
                var root = _c.value;
                root.forEachWorldOp(visitor, matrix);
            }
        }
        catch (e_2_1) { e_2 = { error: e_2_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_2) throw e_2.error; }
        }
    };
    /**
     * Paints shared model geometry clipped to the viewport's paper rectangle
     * and mapped through its model→paper transform.
     */
    AcPdfViewportContent.prototype.paint = function (writerOrCtx) {
        var e_3, _a;
        var _b;
        var ctx = 'drawOp' in writerOrCtx ? { writer: writerOrCtx } : writerOrCtx;
        if (!this.visible || this._sharedRoots.length === 0) {
            return;
        }
        var writer = ctx.writer;
        var layer = effectivePdfLayer(this.layerName, ctx.insertLayer);
        var ocgName = (_b = ctx.ocg) === null || _b === void 0 ? void 0 : _b.ensure(layer).resourceName;
        if (ocgName) {
            writer.beginOcg(ocgName);
        }
        if (this.objectId) {
            writer.beginEntity({
                handle: this.objectId,
                type: 'VIEWPORT_CONTENT',
                layer: layer
            });
        }
        var clipBox = this.clipBox;
        if (clipBox) {
            writer.save();
            writer.clipRect(clipBox);
        }
        var localToDrawing = ctx.localToDrawing
            ? ctx.localToDrawing.clone().multiply(this._modelToPaper)
            : this._modelToPaper.clone();
        try {
            for (var _c = __values(this._sharedRoots), _d = _c.next(); !_d.done; _d = _c.next()) {
                var root = _d.value;
                root.paint(__assign(__assign({}, ctx), { localToDrawing: localToDrawing }));
            }
        }
        catch (e_3_1) { e_3 = { error: e_3_1 }; }
        finally {
            try {
                if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
            }
            finally { if (e_3) throw e_3.error; }
        }
        if (clipBox) {
            writer.restore();
        }
        if (this.objectId) {
            writer.endMarked();
        }
        if (ocgName) {
            writer.endMarked();
        }
    };
    return AcPdfViewportContent;
}(AcPdfEntity));
export { AcPdfViewportContent };
//# sourceMappingURL=AcPdfViewportContent.js.map