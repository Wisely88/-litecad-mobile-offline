import { type AcDbBlockTableRecord, AcDbDatabase, AcDbEntity, AcDbViewport } from '@mlightcad/data-model';
import { AcPdfEntity } from '../renderer/AcPdfEntity';
import type { AcPdfRenderer } from '../renderer/AcPdfRenderer';
/**
 * Attaches common PDF metadata copied from the source database entity.
 */
export declare function attachPdfEntityMeta(drawable: AcPdfEntity, entity: AcDbEntity, typeName: string): void;
/**
 * Walks model space once and returns PDF drawables for viewport reuse.
 */
export declare function collectModelSpaceRoots(db: AcDbDatabase, renderer: AcPdfRenderer): AcPdfEntity[];
/**
 * Builds clipped, model→paper transformed content for one user viewport.
 *
 * `resolveModelRoots` is invoked only for viewports that actually show model
 * geometry, so layouts without usable viewports never traverse model space.
 * The returned content paints the shared roots on demand — no per-viewport
 * geometry clone is created.
 */
export declare function buildViewportModelContent(viewport: AcDbViewport, resolveModelRoots: () => AcPdfEntity[], renderer: AcPdfRenderer): {
    content: AcPdfEntity;
    border: AcPdfEntity | undefined;
} | null;
/**
 * True when `block` is not model space (paper-space layout BTR).
 */
export declare function isPaperSpaceBlock(db: AcDbDatabase, block: AcDbBlockTableRecord): boolean;
//# sourceMappingURL=AcPdfPaperSpaceExport.d.ts.map