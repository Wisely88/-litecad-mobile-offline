import { AcDbViewport, AcGeBox2d, AcGeMatrix3d, type AcGiViewport } from '@mlightcad/data-model';
/**
 * Returns `true` when the viewport is AutoCAD's internal default paper-space
 * viewport (`*Paper_Space`), which does not show model content.
 *
 * Matches {@link AcTrViewportView.isDefaultPaperSpaceViewport}: `number` alone
 * is unreliable after LibreDWG reassignment, so structural fingerprints are
 * used instead.
 */
export declare function isDefaultPaperSpaceViewport(viewport: {
    centerPoint: {
        x: number;
        y: number;
    };
    viewCenter: {
        x: number;
        y: number;
    };
    height?: number;
    viewHeight?: number;
    viewTarget?: {
        x: number;
        y: number;
    };
}): boolean;
/**
 * Builds the affine map from model-space WCS into the viewport's paper
 * rectangle (inverse of paper→model through DVIEW twist).
 *
 * `M = T(paperCenter) · S(sx,sy) · R(-twist) · T(-modelCenter)`
 */
export declare function buildModelToPaperMatrix(paper: AcGeBox2d, model: AcGeBox2d, twistRadians?: number): AcGeMatrix3d;
/**
 * Resolves paper and model view boxes from a viewport entity / GI viewport.
 * Returns `null` when either box is empty or degenerate.
 */
export declare function resolveViewportBoxes(viewport: AcDbViewport | AcGiViewport): {
    paper: AcGeBox2d;
    model: AcGeBox2d;
    twist: number;
} | null;
//# sourceMappingURL=AcPdfPaperViewport.d.ts.map