import type { AcPdfPoint } from '../renderer/AcPdfStyle';
/** One AutoCAD hatch pattern definition line. */
export interface AcPdfHatchPatternLine {
    angle: number;
    base: {
        x: number;
        y: number;
    };
    offset: {
        x: number;
        y: number;
    };
    dashLengths: number[];
}
/**
 * Clips an infinite-direction segment to even-odd hatch loops.
 */
export declare function clipSegmentToLoops(a: AcPdfPoint, b: AcPdfPoint, loops: AcPdfPoint[][]): Array<[AcPdfPoint, AcPdfPoint]>;
/**
 * Generates WCS stroke segments for a patterned hatch, matching the
 * Three.js hatch shader (offset in line-local space, `patternAngle` extra).
 */
export declare function tessellateHatchPattern(loops: AcPdfPoint[][], definitionLines: AcPdfHatchPatternLine[], patternAngle?: number): AcPdfPoint[][];
//# sourceMappingURL=AcPdfHatchTessellator.d.ts.map