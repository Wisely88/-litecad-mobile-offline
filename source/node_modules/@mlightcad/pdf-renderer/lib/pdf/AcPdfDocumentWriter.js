var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcGeBox2d, AcGeMatrix3d } from '@mlightcad/data-model';
import { PDFDocument } from 'pdf-lib';
import { AcPdfContentWriter } from './AcPdfContentWriter';
import { AcPdfOcgManager } from './AcPdfOcgManager';
import { computePageLayout, PDF_MIN_STROKE_PT } from './AcPdfPageLayout';
/**
 * Serializes accumulated {@link AcPdfEntity} nodes into a PDF document.
 */
var AcPdfDocumentWriter = /** @class */ (function () {
    function AcPdfDocumentWriter() {
    }
    /**
     * Creates a single-page document from `entities` and returns its bytes.
     * Multi-page exports use {@link writePage} against one shared document to
     * avoid serializing and re-parsing one PDF per layout.
     */
    AcPdfDocumentWriter.write = function (entities, options) {
        return __awaiter(this, void 0, void 0, function () {
            var doc;
            var _a;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0: return [4 /*yield*/, PDFDocument.create()];
                    case 1:
                        doc = _b.sent();
                        doc.setProducer('MLightCAD pdf-renderer');
                        doc.setCreator('MLightCAD');
                        if (options.title) {
                            doc.setTitle(options.title);
                        }
                        return [4 /*yield*/, AcPdfDocumentWriter.writePage(doc, entities, options)
                            // Flush deferred font embeds and repair their font programs (see
                            // AcPdfFontManager.finalize) before serialization.
                        ];
                    case 2:
                        _b.sent();
                        // Flush deferred font embeds and repair their font programs (see
                        // AcPdfFontManager.finalize) before serialization.
                        return [4 /*yield*/, ((_a = options.fonts) === null || _a === void 0 ? void 0 : _a.finalize())];
                    case 3:
                        // Flush deferred font embeds and repair their font programs (see
                        // AcPdfFontManager.finalize) before serialization.
                        _b.sent();
                        return [2 /*return*/, doc.save({ useObjectStreams: true })];
                }
            });
        });
    };
    /**
     * Paints `entities` as a new page of `doc`.
     *
     * The page owns its content stream, but the OCG manager and embedded-image
     * cache can be shared with previously written pages to keep one layer per
     * CAD layer and a single image XObject per source image.
     */
    AcPdfDocumentWriter.writePage = function (doc, entities, options) {
        return __awaiter(this, void 0, void 0, function () {
            var unionBbox, bbox, layout, page, c, background, writer, ocg, _loop_1, entities_1, entities_1_1, entity, e_1_1, drawingToPage, indexed, indexed_1, indexed_1_1, entity;
            var e_1, _a, e_2, _b;
            var _c;
            return __generator(this, function (_d) {
                switch (_d.label) {
                    case 0:
                        unionBbox = unionDrawableBoxes(entities);
                        bbox = options.fitBox
                            ? boxFromFit(options.fitBox)
                            : unionBbox;
                        layout = computePageLayout(bbox, options);
                        page = doc.addPage([layout.pageWidth, layout.pageHeight]);
                        if (typeof options.background === 'number') {
                            c = options.background;
                            background = new AcPdfContentWriter(page, doc, {
                                imageCache: options.imageCache
                            });
                            background.push("".concat((c >> 16) & 0xff, " ").concat((c >> 8) & 0xff, " ").concat(c & 0xff, " rg 0 0 ") +
                                "".concat(layout.pageWidth, " ").concat(layout.pageHeight, " re f\n"));
                            background.flushToPage(page);
                        }
                        writer = new AcPdfContentWriter(page, doc, {
                            minUserLineWidth: PDF_MIN_STROKE_PT / Math.max(Math.abs(layout.scale), 1e-12),
                            imageCache: options.imageCache,
                            fonts: options.fonts,
                            formRegistry: options.formRegistry
                        });
                        ocg = (_c = options.ocg) !== null && _c !== void 0 ? _c : new AcPdfOcgManager(doc);
                        _loop_1 = function (entity) {
                            var embeds;
                            return __generator(this, function (_e) {
                                switch (_e.label) {
                                    case 0:
                                        embeds = [];
                                        entity.forEachOp(function (op) {
                                            if (op.kind === 'image') {
                                                embeds.push(writer.embedImage(op));
                                            }
                                            else if (op.kind === 'text' &&
                                                op.hex === '' &&
                                                !(op.glyphHex && op.glyphHex.length > 0) &&
                                                options.fonts) {
                                                // Encoding must happen before painting: paint bakes transformed
                                                // op copies, and those copies carry the encoded `hex`.
                                                embeds.push(options.fonts.embedOp(doc, op));
                                            }
                                        });
                                        return [4 /*yield*/, Promise.all(embeds)];
                                    case 1:
                                        _e.sent();
                                        return [2 /*return*/];
                                }
                            });
                        };
                        _d.label = 1;
                    case 1:
                        _d.trys.push([1, 6, 7, 8]);
                        entities_1 = __values(entities), entities_1_1 = entities_1.next();
                        _d.label = 2;
                    case 2:
                        if (!!entities_1_1.done) return [3 /*break*/, 5];
                        entity = entities_1_1.value;
                        return [5 /*yield**/, _loop_1(entity)];
                    case 3:
                        _d.sent();
                        _d.label = 4;
                    case 4:
                        entities_1_1 = entities_1.next();
                        return [3 /*break*/, 2];
                    case 5: return [3 /*break*/, 8];
                    case 6:
                        e_1_1 = _d.sent();
                        e_1 = { error: e_1_1 };
                        return [3 /*break*/, 8];
                    case 7:
                        try {
                            if (entities_1_1 && !entities_1_1.done && (_a = entities_1.return)) _a.call(entities_1);
                        }
                        finally { if (e_1) throw e_1.error; }
                        return [7 /*endfinally*/];
                    case 8:
                        writer.save();
                        drawingToPage = new AcGeMatrix3d().set(layout.scale, 0, 0, layout.offsetX, 0, layout.scale, 0, layout.offsetY, 0, 0, 1, 0, 0, 0, 0, 1);
                        writer.concatMatrix(drawingToPage);
                        indexed = entities.map(function (entity, index) { return ({ entity: entity, index: index }); });
                        indexed.sort(function (a, b) {
                            var order = a.entity.drawOrder - b.entity.drawOrder;
                            return order !== 0 ? order : a.index - b.index;
                        });
                        try {
                            for (indexed_1 = __values(indexed), indexed_1_1 = indexed_1.next(); !indexed_1_1.done; indexed_1_1 = indexed_1.next()) {
                                entity = indexed_1_1.value.entity;
                                entity.paint({
                                    writer: writer,
                                    ocg: ocg,
                                    embedActualText: options.embedTextActualText !== false,
                                    formReuse: false
                                });
                            }
                        }
                        catch (e_2_1) { e_2 = { error: e_2_1 }; }
                        finally {
                            try {
                                if (indexed_1_1 && !indexed_1_1.done && (_b = indexed_1.return)) _b.call(indexed_1);
                            }
                            finally { if (e_2) throw e_2.error; }
                        }
                        writer.restore();
                        // The raw content stream only reaches the page here: the string-based
                        // writer accumulates chunks in memory, unlike the former pdf-lib operator
                        // path that serialized automatically at save time.
                        writer.flushToPage(page);
                        ocg.attachToPage(page);
                        return [2 /*return*/];
                }
            });
        });
    };
    return AcPdfDocumentWriter;
}());
export { AcPdfDocumentWriter };
function boxFromFit(fit) {
    var box = new AcGeBox2d();
    box.min.set(fit.min.x, fit.min.y);
    box.max.set(fit.max.x, fit.max.y);
    return box;
}
function splitDominantCluster(boxes, axis) {
    if (boxes.length < 8) {
        return boxes;
    }
    var keyed = boxes
        .map(function (box) { return ({
        value: axis === 'x' ? (box.min.x + box.max.x) / 2 : (box.min.y + box.max.y) / 2,
        box: box
    }); })
        .sort(function (a, b) { return a.value - b.value; });
    var span = keyed[keyed.length - 1].value - keyed[0].value;
    if (!(span > 0)) {
        return boxes;
    }
    var bestGap = 0;
    var bestIndex = 0;
    for (var i = 1; i < keyed.length; i++) {
        var gap = keyed[i].value - keyed[i - 1].value;
        if (gap > bestGap) {
            bestGap = gap;
            bestIndex = i;
        }
    }
    if (bestGap < span * 0.2) {
        return boxes;
    }
    var left = keyed.slice(0, bestIndex);
    var right = keyed.slice(bestIndex);
    var majority = left.length >= right.length ? left : right;
    // Require an outlier-scale gap relative to typical spacing inside the
    // majority side. Using the majority's full axis extent fails when that
    // side still contains a second far subcluster (origin leftover + INSERT
    // double-transform): majExtent becomes huge and peeling never starts.
    // Median nearest-neighbor gap stays small for real drawings and for
    // dense samples (gradient hatch rows), so nearby columns are kept.
    var sizes = boxes.map(function (box) {
        return Math.max(box.max.x - box.min.x, box.max.y - box.min.y, 1e-9);
    });
    sizes.sort(function (a, b) { return a - b; });
    var medianSize = sizes[Math.floor(sizes.length / 2)] || 1;
    var majNnGaps = [];
    for (var i = 1; i < majority.length; i++) {
        majNnGaps.push(majority[i].value - majority[i - 1].value);
    }
    majNnGaps.sort(function (a, b) { return a - b; });
    var typicalGap = majNnGaps.length > 0
        ? majNnGaps[Math.floor(majNnGaps.length / 2)] || medianSize
        : medianSize;
    var outlierGap = Math.max(typicalGap * 10, medianSize * 20);
    if (bestGap < outlierGap) {
        return boxes;
    }
    return majority.map(function (item) { return item.box; });
}
/**
 * Unions the primary geometry cluster. Header EXTMIN/EXTMAX and a naive
 * union of every drawable are unreliable: origin leftovers plus INSERT
 * double-transforms sit tens of millions of units away and collapse the
 * real drawing to a speck.
 */
function unionDrawableBoxes(entities) {
    var e_3, _a, e_4, _b, e_5, _c;
    var boxes = [];
    try {
        for (var entities_2 = __values(entities), entities_2_1 = entities_2.next(); !entities_2_1.done; entities_2_1 = entities_2.next()) {
            var entity = entities_2_1.value;
            if (!entity.visible) {
                continue;
            }
            var box = entity.box;
            if (box.isEmpty()) {
                continue;
            }
            if (!Number.isFinite(box.min.x) ||
                !Number.isFinite(box.min.y) ||
                !Number.isFinite(box.max.x) ||
                !Number.isFinite(box.max.y)) {
                continue;
            }
            boxes.push(box);
        }
    }
    catch (e_3_1) { e_3 = { error: e_3_1 }; }
    finally {
        try {
            if (entities_2_1 && !entities_2_1.done && (_a = entities_2.return)) _a.call(entities_2);
        }
        finally { if (e_3) throw e_3.error; }
    }
    var fallback = new AcGeBox2d();
    try {
        for (var boxes_1 = __values(boxes), boxes_1_1 = boxes_1.next(); !boxes_1_1.done; boxes_1_1 = boxes_1.next()) {
            var box = boxes_1_1.value;
            fallback.union(box);
        }
    }
    catch (e_4_1) { e_4 = { error: e_4_1 }; }
    finally {
        try {
            if (boxes_1_1 && !boxes_1_1.done && (_b = boxes_1.return)) _b.call(boxes_1);
        }
        finally { if (e_4) throw e_4.error; }
    }
    if (boxes.length < 8) {
        return fallback;
    }
    var previous = -1;
    while (boxes.length !== previous) {
        previous = boxes.length;
        boxes = splitDominantCluster(boxes, 'x');
        boxes = splitDominantCluster(boxes, 'y');
    }
    var clustered = new AcGeBox2d();
    try {
        for (var boxes_2 = __values(boxes), boxes_2_1 = boxes_2.next(); !boxes_2_1.done; boxes_2_1 = boxes_2.next()) {
            var box = boxes_2_1.value;
            clustered.union(box);
        }
    }
    catch (e_5_1) { e_5 = { error: e_5_1 }; }
    finally {
        try {
            if (boxes_2_1 && !boxes_2_1.done && (_c = boxes_2.return)) _c.call(boxes_2);
        }
        finally { if (e_5) throw e_5.error; }
    }
    return clustered.isEmpty() ? fallback : clustered;
}
//# sourceMappingURL=AcPdfDocumentWriter.js.map