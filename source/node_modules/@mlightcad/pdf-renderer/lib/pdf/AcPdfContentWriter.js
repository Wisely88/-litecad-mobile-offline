var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
import { deflate } from 'pako';
import { PDFArray, PDFName, PDFRawStream } from 'pdf-lib';
import { AcPdfMatrixUtil } from '../renderer/AcPdfMatrixUtil';
import { pdfHexText } from './AcPdfMarkedContent';
import { setPageNamedResource } from './AcPdfOcgManager';
var KAPPA = 0.5522847498307936;
export function createPdfFormRegistry() {
    return {
        entries: new Map(),
        bufferIds: new WeakMap(),
        nameSeq: 0,
        bufferSeq: 0
    };
}
/**
 * Flate-compresses stream bytes for embedding (`/Filter /FlateDecode`).
 * Returns `null` for tiny streams where the compression overhead outweighs
 * the savings, or when the platform deflate fails.
 */
function flateStreamBytes(bytes) {
    if (bytes.length < 256) {
        return null;
    }
    try {
        return deflate(bytes);
    }
    catch (_a) {
        return null;
    }
}
/**
 * Formats a number as a PDF real/integer operand.
 *
 * `String(n)` gives the shortest round-trip form; exponent notation (invalid
 * inside content streams) is expanded to plain decimal.
 */
function pdfNum(n) {
    if (Number.isInteger(n) && Math.abs(n) < 1e15) {
        return String(n);
    }
    if (!Number.isFinite(n)) {
        return '0';
    }
    var s = String(n);
    if (s.indexOf('e') < 0 && s.indexOf('E') < 0) {
        return s;
    }
    var t = n.toFixed(20);
    if (t.indexOf('e') >= 0 || t.indexOf('E') >= 0) {
        return '0';
    }
    if (t.indexOf('.') >= 0) {
        t = t.replace(/0+$/, '').replace(/\.$/, '');
    }
    return t === '' || t === '-' ? '0' : t;
}
function pdfLiteral(text) {
    return ('(' +
        text
            .replace(/\\/g, '\\\\')
            .replace(/\(/g, '\\(')
            .replace(/\)/g, '\\)')
            .replace(/\r/g, '\\r')
            .replace(/\n/g, '\\n') +
        ')');
}
/**
 * Formats a glyph-space coordinate. Float32 buffers carry binary artifacts
 * (`0.1` reads back as `0.10000000149011612`), so values are rounded to 4
 * decimals — far below visual tolerance for text geometry — keeping both the
 * content streams and the JS string churn small.
 */
function pdfCompact(n) {
    var r = Math.round(n * 1e4) / 1e4;
    return Number.isFinite(r) ? String(r) : '0';
}
/** Bounding box of a flat `[x, y, ...]` coordinate buffer. */
function flatBounds(data) {
    var minX = Infinity;
    var minY = Infinity;
    var maxX = -Infinity;
    var maxY = -Infinity;
    for (var i = 0; i + 1 < data.length; i += 2) {
        var x = data[i];
        var y = data[i + 1];
        if (x < minX) {
            minX = x;
        }
        if (y < minY) {
            minY = y;
        }
        if (x > maxX) {
            maxX = x;
        }
        if (y > maxY) {
            maxY = y;
        }
    }
    return { minX: minX, minY: minY, maxX: maxX, maxY: maxY };
}
/** Mean of the X/Y affine scale factors of `matrix`. */
function affineScale2d(matrix) {
    var el = matrix.elements;
    var sx = Math.hypot(el[0], el[1]);
    var sy = Math.hypot(el[4], el[5]);
    var scale = (sx + sy) / 2;
    return Number.isFinite(scale) && scale > 0 ? scale : 1;
}
/**
 * Serializes CAD drawables into raw PDF content-stream text.
 *
 * pdf-lib's `pushOperators` path retains one JS object graph per operand
 * (PDFNumber wrappers, arg arrays, operator objects), which multiplies large
 * drawings' content by ~20-30x in heap terms — enough to exhaust the tab on
 * drawings with millions of points. This writer emits the exact same
 * operators as compact text instead, and {@link flushToPage} registers the
 * bytes as one `PDFRawStream` appended to the page's `/Contents` array.
 */
var AcPdfContentWriter = /** @class */ (function () {
    function AcPdfContentWriter(page, doc, options) {
        if (options === void 0) { options = {}; }
        var _a, _b, _c;
        this._opacityStates = new Map();
        this._imageNames = new Map();
        this._chunks = [];
        this._segments = [];
        this._segmentLength = 0;
        this._shadingCount = 0;
        this._page = page;
        this._doc = doc;
        this._minUserLineWidth = Math.max((_a = options.minUserLineWidth) !== null && _a !== void 0 ? _a : 0, 0);
        this._images = (_b = options.imageCache) !== null && _b !== void 0 ? _b : new Map();
        this._forms = (_c = options.formRegistry) !== null && _c !== void 0 ? _c : createPdfFormRegistry();
        this._fonts = options.fonts;
    }
    AcPdfContentWriter.prototype.createNestedWriter = function () {
        return new AcPdfContentWriter(null, this._doc, {
            minUserLineWidth: this._minUserLineWidth,
            imageCache: this._images,
            formRegistry: this._forms,
            fonts: this._fonts
        });
    };
    /**
     * Paints `paint` into a cached Form XObject and returns its resource name.
     */
    AcPdfContentWriter.prototype.ensureForm = function (key, bbox, paint) {
        var existing = this.hasForm(key);
        if (existing) {
            return existing;
        }
        var formWriter = this.createNestedWriter();
        paint(formWriter);
        var bytes = formWriter.buildBytes();
        if (bytes.length === 0) {
            return null;
        }
        var ref = this.createFormXObject(bytes, bbox);
        return this.registerForm(key, ref);
    };
    AcPdfContentWriter.prototype.flushToPage = function (page) {
        var bytes = this.buildBytes();
        if (bytes.length === 0) {
            return;
        }
        var ref = this.registerRawStream(bytes);
        // normalize() turns a direct Contents ref into an array and (on first
        // call) wraps prior streams with q/Q. Appending keeps paint order: our
        // stream is self-contained (opens with `q`, installs its own `cm`).
        page.node.normalize();
        var contents = page.node.Contents();
        if (contents instanceof PDFArray) {
            contents.push(ref);
        }
        else {
            page.node.set(PDFName.of('Contents'), ref);
        }
    };
    /**
     * Registers raw stream bytes, Flate-compressing large payloads
     * (`/Filter /FlateDecode`) — content streams of big drawings compress
     * ~10x, which is most of the exported file's size.
     */
    AcPdfContentWriter.prototype.registerRawStream = function (bytes) {
        var compressed = flateStreamBytes(bytes);
        var dict = this._doc.context.obj(compressed ? { Filter: 'FlateDecode' } : {});
        return this._doc.context.register(PDFRawStream.of(dict, compressed !== null && compressed !== void 0 ? compressed : bytes));
    };
    AcPdfContentWriter.prototype.compact = function () {
        if (this._chunks.length === 0) {
            return;
        }
        var joined = this._chunks.join('');
        this._chunks.length = 0;
        var bytes = new Uint8Array(joined.length);
        for (var i = 0; i < joined.length; i++) {
            bytes[i] = joined.charCodeAt(i) & 0xff;
        }
        this._segments.push(bytes);
        this._segmentLength += bytes.length;
    };
    AcPdfContentWriter.prototype.buildBytes = function () {
        var e_1, _a;
        this.compact();
        var out = new Uint8Array(this._segmentLength);
        var offset = 0;
        try {
            for (var _b = __values(this._segments), _c = _b.next(); !_c.done; _c = _b.next()) {
                var segment = _c.value;
                out.set(segment, offset);
                offset += segment.length;
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_1) throw e_1.error; }
        }
        return out;
    };
    /** Appends a raw content-stream chunk (also used by the document writer). */
    AcPdfContentWriter.prototype.push = function (chunk) {
        this._chunks.push(chunk);
        // Bound the string-object count on huge drawings: join periodically.
        if (this._chunks.length >= 1024) {
            this.compact();
        }
    };
    AcPdfContentWriter.prototype.save = function () {
        this.push('q\n');
    };
    AcPdfContentWriter.prototype.restore = function () {
        this.push('Q\n');
    };
    /**
     * Installs a rectangular clip in the current user space (even-odd).
     * Caller must {@link save} first and {@link restore} after clipped draws.
     */
    AcPdfContentWriter.prototype.clipRect = function (box) {
        this.push("".concat(pdfNum(box.min.x), " ").concat(pdfNum(box.min.y), " m ") +
            "".concat(pdfNum(box.max.x), " ").concat(pdfNum(box.min.y), " l ") +
            "".concat(pdfNum(box.max.x), " ").concat(pdfNum(box.max.y), " l ") +
            "".concat(pdfNum(box.min.x), " ").concat(pdfNum(box.max.y), " l h W* n\n"));
    };
    AcPdfContentWriter.prototype.concatMatrix = function (matrix) {
        var m = AcPdfMatrixUtil.toPdfMatrix(matrix);
        this.push("".concat(pdfNum(m.a), " ").concat(pdfNum(m.b), " ").concat(pdfNum(m.c), " ").concat(pdfNum(m.d), " ") +
            "".concat(pdfNum(m.e), " ").concat(pdfNum(m.f), " cm\n"));
    };
    AcPdfContentWriter.prototype.beginOcg = function (resourceName) {
        this.push("/OC /".concat(resourceName, " BDC\n"));
    };
    AcPdfContentWriter.prototype.beginActualText = function (text) {
        this.push("/Span << /ActualText ".concat(pdfHexText(text), " >> BDC\n"));
    };
    AcPdfContentWriter.prototype.beginEntity = function (payload) {
        var dict = '';
        if (payload.handle) {
            dict += " /Handle ".concat(pdfLiteral(payload.handle));
        }
        if (payload.type) {
            dict += " /Type ".concat(pdfLiteral(payload.type));
        }
        if (payload.name) {
            dict += " /Name ".concat(pdfLiteral(payload.name));
        }
        if (payload.layer) {
            dict += " /Layer ".concat(pdfLiteral(payload.layer));
        }
        this.push("/Entity <<".concat(dict, " >> BDC\n"));
    };
    AcPdfContentWriter.prototype.endMarked = function () {
        this.push('EMC\n');
    };
    AcPdfContentWriter.prototype.invokeForm = function (resourceName) {
        this.push("/".concat(resourceName, " Do\n"));
    };
    /**
     * Registers (or reuses) a Form XObject under `key` and returns its resource
     * name. Names are unique document-wide through the shared registry; the
     * resource is added to this page's `/XObject` dict so cross-page reuse
     * keeps working.
     */
    AcPdfContentWriter.prototype.registerForm = function (key, ref) {
        var entry = this._forms.entries.get(key);
        if (entry) {
            this.ensurePageForm(entry.name, entry.ref);
            return entry.name;
        }
        var name = "Fm".concat(++this._forms.nameSeq);
        this._forms.entries.set(key, { name: name, ref: ref });
        this.ensurePageForm(name, ref);
        return name;
    };
    AcPdfContentWriter.prototype.hasForm = function (key) {
        var _a;
        return (_a = this._forms.entries.get(key)) === null || _a === void 0 ? void 0 : _a.name;
    };
    /** Adds a form (or image) resource to the current page if one exists. */
    AcPdfContentWriter.prototype.ensurePageForm = function (name, ref) {
        if (this._page) {
            this._page.node.setXObject(PDFName.of(name), ref);
        }
    };
    AcPdfContentWriter.prototype.embedImage = function (op) {
        return __awaiter(this, void 0, void 0, function () {
            var image, _a, _b;
            return __generator(this, function (_c) {
                switch (_c.label) {
                    case 0:
                        if (this._images.has(op) || op.bytes.byteLength === 0) {
                            return [2 /*return*/];
                        }
                        _c.label = 1;
                    case 1:
                        _c.trys.push([1, 6, , 7]);
                        if (!(op.format === 'jpg')) return [3 /*break*/, 3];
                        return [4 /*yield*/, this._doc.embedJpg(op.bytes)];
                    case 2:
                        _a = _c.sent();
                        return [3 /*break*/, 5];
                    case 3: return [4 /*yield*/, this._doc.embedPng(op.bytes)];
                    case 4:
                        _a = _c.sent();
                        _c.label = 5;
                    case 5:
                        image = _a;
                        this._images.set(op, image);
                        return [3 /*break*/, 7];
                    case 6:
                        _b = _c.sent();
                        return [3 /*break*/, 7];
                    case 7: return [2 /*return*/];
                }
            });
        });
    };
    AcPdfContentWriter.prototype.drawOp = function (op) {
        if (op.kind === 'stroke') {
            this.strokePaths([{ points: op.points, closed: op.closed === true }], op.style);
            return;
        }
        if (op.kind === 'fill') {
            this.fillLoops(op.loops, op.style);
            return;
        }
        if (op.kind === 'text') {
            this.paintText(op);
            return;
        }
        if (op.kind === 'circle') {
            this.fillCircle(op.x, op.y, op.r, op.style);
            return;
        }
        if (op.kind === 'gradient') {
            this.fillGradient(op);
            return;
        }
        if (op.kind === 'triangles') {
            this.paintTriangles(op);
            return;
        }
        if (op.kind === 'polylines') {
            this.paintFlatPolylines(op);
            return;
        }
        this.drawEmbeddedImage(op);
    };
    /**
     * Paints a run of ops from one entity, coalescing adjacent draws that share
     * the same style.
     *
     * Dense geometry (polyline meshes, patterned hatches, complex linetypes)
     * produces hundreds of stroke ops whose styles are identical. Independent
     * subpaths in a single stroked path render identically (caps apply per
     * subpath, no joins across moves). Consecutive opaque fills likewise
     * combine under even-odd.
     *
     * Compact glyph ops (`triangles`/`polylines`) are matched on the raw op
     * before `map` so their shared float32 buffers are never copied per
     * instance: triangles go through one Form XObject per unique glyph set
     * (color/opacity set outside, `cm` per instance), polylines serialize
     * inline with the matrix baked numerically.
     */
    AcPdfContentWriter.prototype.drawOps = function (ops, map, matrix) {
        var resolve = map !== null && map !== void 0 ? map : (function (op) { return op; });
        for (var i = 0; i < ops.length;) {
            var raw = ops[i];
            if (raw.kind === 'triangles') {
                this.paintTriangles(raw, matrix);
                i++;
                continue;
            }
            if (raw.kind === 'polylines') {
                this.paintFlatPolylines(raw, matrix);
                i++;
                continue;
            }
            var op = resolve(raw);
            if (op.kind === 'text') {
                this.paintText(op);
                i++;
                continue;
            }
            if (op.kind === 'stroke') {
                var paths = [
                    { points: op.points, closed: op.closed === true }
                ];
                var j = i + 1;
                while (j < ops.length) {
                    var rawNext = ops[j];
                    if (rawNext.kind === 'triangles' ||
                        rawNext.kind === 'polylines' ||
                        rawNext.kind === 'image' ||
                        rawNext.kind === 'gradient' ||
                        rawNext.kind === 'circle' ||
                        rawNext.kind === 'text') {
                        break;
                    }
                    var next = resolve(rawNext);
                    if (next.kind !== 'stroke' || !sameStrokeStyle(op.style, next.style)) {
                        break;
                    }
                    paths.push({ points: next.points, closed: next.closed === true });
                    j++;
                }
                this.strokePaths(paths, op.style);
                i = j;
                continue;
            }
            if (op.kind === 'fill' && op.style.opacity >= 0.999) {
                var loops = __spreadArray([], __read(op.loops), false);
                var j = i + 1;
                while (j < ops.length) {
                    var rawNext = ops[j];
                    if (rawNext.kind === 'triangles' ||
                        rawNext.kind === 'polylines' ||
                        rawNext.kind === 'image' ||
                        rawNext.kind === 'gradient' ||
                        rawNext.kind === 'circle' ||
                        rawNext.kind === 'text') {
                        break;
                    }
                    var next = resolve(rawNext);
                    if (next.kind !== 'fill' ||
                        next.style.opacity < 0.999 ||
                        !sameFillStyle(op.style, next.style)) {
                        break;
                    }
                    loops.push.apply(loops, __spreadArray([], __read(next.loops), false));
                    j++;
                }
                this.fillLoops(loops, op.style);
                i = j;
                continue;
            }
            this.drawOp(op);
            i++;
        }
    };
    /**
     * Paints one laid-out line of real PDF text (`BT … ET`).
     *
     * The op's glyph string was encoded through the embedded subset font by
     * {@link AcPdfFontManager.embedOp} before painting; ops whose font failed
     * to embed (empty `hex`/`glyphHex`) are skipped. Rotation, the CAD width
     * factor and the `\Q` oblique shear collapse into the text matrix:
     * glyph advances scale by `size * hScale` along the baseline and `size`
     * perpendicular to it, sheared by `tan(oblique)` after scaling. Tracking
     * runs paint as one TJ array with per-glyph displacement numbers.
     */
    AcPdfContentWriter.prototype.paintText = function (op) {
        var _a, _b;
        var glyphHex = op.glyphHex;
        var tracked = glyphHex !== undefined && glyphHex.length > 0;
        if (!op.hex && !tracked) {
            return;
        }
        var resource = (_a = this._fonts) === null || _a === void 0 ? void 0 : _a.resourceFor(op.font);
        if (!resource) {
            return;
        }
        if (this._page) {
            setPageNamedResource(this._page, 'Font', resource.name, resource.ref);
        }
        var rad = (op.angleDeg * Math.PI) / 180;
        var cos = Math.cos(rad);
        var sin = Math.sin(rad);
        // `flipX` mirrors glyphs about their local vertical axis: the text
        // matrix x-axis is negated (R(θ)·S(-size·hScale, size)). `\Q` oblique
        // shears glyph space after scaling: Tm = R(θ)·Sh(tan)·S, so the c/d
        // columns gain a `tan·cos` / `tan·sin` term.
        var flip = op.flipX === true ? -1 : 1;
        var shear = op.obliqueDeg
            ? Math.tan((op.obliqueDeg * Math.PI) / 180)
            : 0;
        var chunk = 'q\n';
        chunk += this.applyOpacity(op.style.opacity);
        chunk += "".concat(pdfNum(op.style.rgb.r), " ").concat(pdfNum(op.style.rgb.g), " ").concat(pdfNum(op.style.rgb.b), " rg\n");
        chunk += 'BT\n';
        chunk += "/".concat(resource.name, " 1 Tf\n");
        chunk +=
            "".concat(pdfNum(op.size * op.hScale * cos * flip), " ").concat(pdfNum(op.size * op.hScale * sin * flip), " ") +
                "".concat(pdfNum(op.size * (shear * cos - sin)), " ").concat(pdfNum(op.size * (shear * sin + cos)), " ") +
                "".concat(pdfNum(op.x), " ").concat(pdfNum(op.y), " Tm\n");
        if (tracked) {
            // Per-glyph tracking: each displacement number adds the advance that
            // tracking owes (see embedOp); zero adjustments stay out of the array.
            var adjust_1 = (_b = op.charAdjust) !== null && _b !== void 0 ? _b : [];
            var parts_1 = [];
            glyphHex.forEach(function (hex, index) {
                var _a;
                if (index > 0) {
                    var t = (_a = adjust_1[index - 1]) !== null && _a !== void 0 ? _a : 0;
                    if (t !== 0) {
                        parts_1.push(pdfNum(t));
                    }
                }
                parts_1.push("<".concat(hex, ">"));
            });
            chunk += "[".concat(parts_1.join(' '), "] TJ\nET\nQ\n");
            this.push(chunk);
            return;
        }
        chunk += "<".concat(op.hex, "> Tj\nET\nQ\n");
        this.push(chunk);
    };
    /**
     * Paints a flat triangle glyph set.
     *
     * The unique glyph geometry is emitted once into a Form XObject keyed by
     * buffer identity; every instance then costs one `q cm /FmN Do Q`. Fill
     * color and opacity are installed in the graphics state before `Do` — PDF
     * forms execute inside the current graphics state, so one form serves all
     * layers/colors.
     */
    AcPdfContentWriter.prototype.paintTriangles = function (op, matrix) {
        var data = op.data;
        if (data.length < 6) {
            return;
        }
        if (!this._page) {
            // Nested writer (form content): serialize inline, no color operators.
            this.push(this.bakedTrianglesChunk(data, matrix) + 'f\n');
            return;
        }
        var name = this.ensureTrianglesForm(data);
        if (!name) {
            return;
        }
        var chunk = 'q\n';
        chunk += this.applyOpacity(op.style.opacity);
        chunk += "".concat(pdfNum(op.style.rgb.r), " ").concat(pdfNum(op.style.rgb.g), " ").concat(pdfNum(op.style.rgb.b), " rg\n");
        if (matrix) {
            var m = AcPdfMatrixUtil.toPdfMatrix(matrix);
            chunk += "".concat(pdfNum(m.a), " ").concat(pdfNum(m.b), " ").concat(pdfNum(m.c), " ").concat(pdfNum(m.d), " ").concat(pdfNum(m.e), " ").concat(pdfNum(m.f), " cm\n");
        }
        chunk += "/".concat(name, " Do\nQ\n");
        this.push(chunk);
    };
    /** Creates (once) and names the Form XObject for a triangle buffer. */
    AcPdfContentWriter.prototype.ensureTrianglesForm = function (data) {
        var key = this.compactFormKey(data);
        var existing = this.hasForm(key);
        if (existing) {
            return existing;
        }
        var bbox = flatBounds(data);
        if (!Number.isFinite(bbox.minX) ||
            !Number.isFinite(bbox.minY) ||
            !Number.isFinite(bbox.maxX) ||
            !Number.isFinite(bbox.maxY)) {
            return null;
        }
        var bytes = this.trianglesFormBytes(data);
        if (bytes.length === 0) {
            return null;
        }
        var ref = this.createFormXObject(bytes, bbox);
        return this.registerForm(key, ref);
    };
    /** Serializes triangle geometry (paths + single fill) into PDF bytes. */
    AcPdfContentWriter.prototype.trianglesFormBytes = function (data) {
        var writer = this.createNestedWriter();
        writer.push(this.bakedTrianglesChunk(data, undefined) + 'f\n');
        return writer.buildBytes();
    };
    /** Closed-triangle subpaths for a flat triangle buffer. */
    AcPdfContentWriter.prototype.bakedTrianglesChunk = function (data, matrix) {
        var el = matrix === null || matrix === void 0 ? void 0 : matrix.elements;
        var chunk = '';
        for (var i = 0; i + 5 < data.length; i += 6) {
            chunk += this.flatPointChunk(data, i, el) + ' m ';
            chunk += this.flatPointChunk(data, i + 2, el) + ' l ';
            chunk += this.flatPointChunk(data, i + 4, el) + ' l h\n';
        }
        return chunk;
    };
    AcPdfContentWriter.prototype.flatPointChunk = function (data, i, el) {
        var x = data[i];
        var y = data[i + 1];
        if (el) {
            var tx = el[0] * x + el[4] * y + el[12];
            var ty = el[1] * x + el[5] * y + el[13];
            x = tx;
            y = ty;
        }
        return "".concat(pdfCompact(x), " ").concat(pdfCompact(y));
    };
    /**
     * Paints a flat polyline glyph set inline (strokes carry per-instance line
     * widths, so they are not form-shared), baking the matrix numerically.
     */
    AcPdfContentWriter.prototype.paintFlatPolylines = function (op, matrix) {
        var data = op.data;
        if (data.length < 5) {
            return;
        }
        var el = matrix === null || matrix === void 0 ? void 0 : matrix.elements;
        var scale = matrix ? affineScale2d(matrix) : 1;
        var lineWidth = Math.max(op.style.lineWidth * scale, this._minUserLineWidth);
        var chunk = 'q\n';
        chunk += this.applyOpacity(op.style.opacity);
        chunk += "".concat(pdfNum(op.style.rgb.r), " ").concat(pdfNum(op.style.rgb.g), " ").concat(pdfNum(op.style.rgb.b), " RG ").concat(pdfNum(lineWidth), " w 1 J 1 j\n");
        if (op.style.dashArray && op.style.dashArray.length > 0) {
            chunk += "[".concat(op.style.dashArray.map(function (d) { return pdfNum(d * scale); }).join(' '), "] 0 d\n");
        }
        var i = 0;
        while (i < data.length) {
            var n = data[i];
            if (!(n >= 2) || i + 1 + n * 2 > data.length) {
                break;
            }
            i++;
            for (var k = 0; k < n; k++) {
                var x = data[i];
                var y = data[i + 1];
                i += 2;
                if (el) {
                    var tx = el[0] * x + el[4] * y + el[12];
                    var ty = el[1] * x + el[5] * y + el[13];
                    x = tx;
                    y = ty;
                }
                chunk += "".concat(pdfCompact(x), " ").concat(pdfCompact(y), " ").concat(k === 0 ? 'm' : 'l', "\n");
            }
        }
        chunk += 'S\nQ\n';
        this.push(chunk);
    };
    /** Stable string key for a shared flat buffer (Form XObject cache key). */
    AcPdfContentWriter.prototype.compactFormKey = function (data) {
        var id = this._forms.bufferIds.get(data);
        if (!id) {
            id = "tri".concat(++this._forms.bufferSeq);
            this._forms.bufferIds.set(data, id);
        }
        return id;
    };
    AcPdfContentWriter.prototype.createFormXObject = function (bytes, bbox, resources) {
        var compressed = flateStreamBytes(bytes);
        var dict = this._doc.context.obj(__assign(__assign({ Type: 'XObject', Subtype: 'Form', BBox: [bbox.minX, bbox.minY, bbox.maxX, bbox.maxY], Matrix: [1, 0, 0, 1, 0, 0] }, (compressed ? { Filter: 'FlateDecode' } : {})), (resources ? { Resources: resources } : {})));
        return this._doc.context.register(PDFRawStream.of(dict, compressed !== null && compressed !== void 0 ? compressed : bytes));
    };
    AcPdfContentWriter.prototype.drawEmbeddedImage = function (op) {
        var image = this._images.get(op);
        if (!image || !(op.width > 0) || !(op.height > 0)) {
            return;
        }
        var name = this.ensureImageName(image);
        this.push("q ".concat(pdfNum(op.width), " 0 0 ").concat(pdfNum(op.height), " ") +
            "".concat(pdfNum(op.x), " ").concat(pdfNum(op.y), " cm /").concat(name, " Do Q\n"));
    };
    AcPdfContentWriter.prototype.ensureImageName = function (image) {
        var existing = this._imageNames.get(image);
        if (existing) {
            return existing;
        }
        var name = "Im".concat(this._imageNames.size + 1);
        this._imageNames.set(image, name);
        if (this._page) {
            this._page.node.setXObject(PDFName.of(name), image.ref);
        }
        return name;
    };
    AcPdfContentWriter.prototype.applyOpacity = function (opacity) {
        if (opacity >= 0.999) {
            return '';
        }
        var key = this.ensureOpacityState(opacity);
        return "/".concat(key, " gs\n");
    };
    AcPdfContentWriter.prototype.ensureOpacityState = function (opacity) {
        var rounded = Math.round(opacity * 1000) / 1000;
        var existing = this._opacityStates.get(rounded);
        if (existing) {
            return existing;
        }
        var name = "GS".concat(this._opacityStates.size + 1);
        var dict = this._doc.context.obj({
            Type: 'ExtGState',
            CA: rounded,
            ca: rounded
        });
        if (this._page) {
            this._page.node.setExtGState(PDFName.of(name), dict);
        }
        this._opacityStates.set(rounded, name);
        return name;
    };
    AcPdfContentWriter.prototype.strokePaths = function (paths, style) {
        var e_2, _a;
        var drawable = paths.filter(function (path) { return path.points.length >= 2; });
        if (drawable.length === 0) {
            return;
        }
        var chunk = 'q\n';
        chunk += this.applyOpacity(style.opacity);
        var lineWidth = Math.max(style.lineWidth, this._minUserLineWidth);
        chunk +=
            "".concat(pdfNum(style.rgb.r), " ").concat(pdfNum(style.rgb.g), " ").concat(pdfNum(style.rgb.b), " RG ") +
                "".concat(pdfNum(lineWidth), " w 1 J 1 j\n");
        if (style.dashArray && style.dashArray.length > 0) {
            chunk += "[".concat(style.dashArray.map(pdfNum).join(' '), "] 0 d\n");
        }
        try {
            for (var drawable_1 = __values(drawable), drawable_1_1 = drawable_1.next(); !drawable_1_1.done; drawable_1_1 = drawable_1.next()) {
                var path = drawable_1_1.value;
                chunk += this.polylineChunk(path.points, path.closed);
            }
        }
        catch (e_2_1) { e_2 = { error: e_2_1 }; }
        finally {
            try {
                if (drawable_1_1 && !drawable_1_1.done && (_a = drawable_1.return)) _a.call(drawable_1);
            }
            finally { if (e_2) throw e_2.error; }
        }
        chunk += 'S\nQ\n';
        this.push(chunk);
    };
    AcPdfContentWriter.prototype.fillLoops = function (loops, style) {
        var e_3, _a;
        if (loops.length === 0) {
            return;
        }
        var chunk = 'q\n';
        chunk += this.applyOpacity(style.opacity);
        chunk += "".concat(pdfNum(style.rgb.r), " ").concat(pdfNum(style.rgb.g), " ").concat(pdfNum(style.rgb.b), " rg\n");
        try {
            for (var loops_1 = __values(loops), loops_1_1 = loops_1.next(); !loops_1_1.done; loops_1_1 = loops_1.next()) {
                var loop = loops_1_1.value;
                if (loop.length < 3) {
                    continue;
                }
                chunk += this.polylineChunk(loop, true);
            }
        }
        catch (e_3_1) { e_3 = { error: e_3_1 }; }
        finally {
            try {
                if (loops_1_1 && !loops_1_1.done && (_a = loops_1.return)) _a.call(loops_1);
            }
            finally { if (e_3) throw e_3.error; }
        }
        chunk += 'f*\nQ\n';
        this.push(chunk);
    };
    AcPdfContentWriter.prototype.fillGradient = function (op) {
        var e_4, _a, e_5, _b;
        if (op.loops.length === 0 || !this._page) {
            return;
        }
        var chunk = 'q\n';
        chunk += this.applyOpacity(op.style.opacity);
        try {
            for (var _c = __values(op.loops), _d = _c.next(); !_d.done; _d = _c.next()) {
                var loop = _d.value;
                if (loop.length < 3) {
                    continue;
                }
                chunk += this.polylineChunk(loop, true);
            }
        }
        catch (e_4_1) { e_4 = { error: e_4_1 }; }
        finally {
            try {
                if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
            }
            finally { if (e_4) throw e_4.error; }
        }
        chunk += 'W* n\n';
        if (op.strips && op.strips.length > 0) {
            try {
                for (var _e = __values(op.strips), _f = _e.next(); !_f.done; _f = _e.next()) {
                    var strip = _f.value;
                    if (strip.points.length < 3) {
                        continue;
                    }
                    chunk +=
                        "".concat(pdfNum(strip.rgb.r), " ").concat(pdfNum(strip.rgb.g), " ").concat(pdfNum(strip.rgb.b), " rg\n");
                    chunk += this.polylineChunk(strip.points, true);
                    chunk += 'f*\n';
                }
            }
            catch (e_5_1) { e_5 = { error: e_5_1 }; }
            finally {
                try {
                    if (_f && !_f.done && (_b = _e.return)) _b.call(_e);
                }
                finally { if (e_5) throw e_5.error; }
            }
            this.push(chunk + 'Q\n');
            return;
        }
        var name = "Sh".concat(++this._shadingCount);
        var functionDict = this._doc.context.obj({
            FunctionType: 2,
            Domain: [0, 1],
            C0: [op.c0.r, op.c0.g, op.c0.b],
            C1: [op.c1.r, op.c1.g, op.c1.b],
            N: 1
        });
        var shading = this._doc.context.obj({
            ShadingType: op.shadingType,
            ColorSpace: 'DeviceRGB',
            Coords: op.coords,
            Function: functionDict,
            Extend: [true, true]
        });
        var shadingRef = this._doc.context.register(shading);
        setPageNamedResource(this._page, 'Shading', name, shadingRef);
        chunk += "/".concat(name, " sh\nQ\n");
        this.push(chunk);
    };
    AcPdfContentWriter.prototype.fillCircle = function (x, y, r, style) {
        if (!(r > 0)) {
            return;
        }
        var ox = r * KAPPA;
        var chunk = 'q\n';
        chunk += this.applyOpacity(style.opacity);
        chunk += "".concat(pdfNum(style.rgb.r), " ").concat(pdfNum(style.rgb.g), " ").concat(pdfNum(style.rgb.b), " rg\n");
        chunk += "".concat(pdfNum(x + r), " ").concat(pdfNum(y), " m ");
        chunk += "".concat(pdfNum(x + r), " ").concat(pdfNum(y + ox), " ").concat(pdfNum(x + ox), " ").concat(pdfNum(y + r), " ").concat(pdfNum(x), " ").concat(pdfNum(y + r), " c ");
        chunk += "".concat(pdfNum(x - ox), " ").concat(pdfNum(y + r), " ").concat(pdfNum(x - r), " ").concat(pdfNum(y + ox), " ").concat(pdfNum(x - r), " ").concat(pdfNum(y), " c ");
        chunk += "".concat(pdfNum(x - r), " ").concat(pdfNum(y - ox), " ").concat(pdfNum(x - ox), " ").concat(pdfNum(y - r), " ").concat(pdfNum(x), " ").concat(pdfNum(y - r), " c ");
        chunk += "".concat(pdfNum(x + ox), " ").concat(pdfNum(y - r), " ").concat(pdfNum(x + r), " ").concat(pdfNum(y - ox), " ").concat(pdfNum(x + r), " ").concat(pdfNum(y), " c ");
        chunk += 'h f*\nQ\n';
        this.push(chunk);
    };
    AcPdfContentWriter.prototype.polylineChunk = function (points, closed) {
        var chunk = "".concat(pdfNum(points[0].x), " ").concat(pdfNum(points[0].y), " m");
        for (var i = 1; i < points.length; i++) {
            chunk += " ".concat(pdfNum(points[i].x), " ").concat(pdfNum(points[i].y), " l");
        }
        if (closed) {
            chunk += ' h';
        }
        return chunk + '\n';
    };
    return AcPdfContentWriter;
}());
export { AcPdfContentWriter };
function sameRgb(a, b) {
    return a.r === b.r && a.g === b.g && a.b === b.b;
}
function sameDashArray(a, b) {
    if (a === b) {
        return true;
    }
    if (!a || !b || a.length !== b.length) {
        return false;
    }
    for (var i = 0; i < a.length; i++) {
        if (a[i] !== b[i]) {
            return false;
        }
    }
    return true;
}
function sameStrokeStyle(a, b) {
    return (a === b ||
        (a.opacity === b.opacity &&
            a.lineWidth === b.lineWidth &&
            sameRgb(a.rgb, b.rgb) &&
            sameDashArray(a.dashArray, b.dashArray)));
}
function sameFillStyle(a, b) {
    return a === b || (a.opacity === b.opacity && sameRgb(a.rgb, b.rgb));
}
//# sourceMappingURL=AcPdfContentWriter.js.map