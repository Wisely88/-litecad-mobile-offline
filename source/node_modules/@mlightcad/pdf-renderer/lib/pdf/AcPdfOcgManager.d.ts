import { PDFDict, PDFDocument, PDFPage, PDFRef } from 'pdf-lib';
/**
 * Builds Optional Content Groups so Acrobat's Layers panel can toggle CAD layers.
 */
export declare class AcPdfOcgManager {
    private readonly _doc;
    private readonly _ocgs;
    private readonly _resourceNames;
    private _nextResourceId;
    constructor(doc: PDFDocument);
    resourceName(layerName: string): string;
    ensure(layerName: string): {
        resourceName: string;
        ref: PDFRef;
    };
    attachToPage(page: PDFPage): void;
    layerNames(): string[];
}
export declare function setPageNamedResource(page: PDFPage, category: string, name: string, value: PDFRef | PDFDict): void;
//# sourceMappingURL=AcPdfOcgManager.d.ts.map