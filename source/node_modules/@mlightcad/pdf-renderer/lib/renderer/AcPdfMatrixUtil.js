var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
/**
 * 2D helpers for AcGe 4×4 matrices used while painting PDF content.
 */
var AcPdfMatrixUtil = /** @class */ (function () {
    function AcPdfMatrixUtil() {
    }
    /**
     * PDF `cm` operands `[a b c d e f]` for the XY affine part of a 4×4 matrix.
     */
    AcPdfMatrixUtil.toPdfMatrix = function (matrix) {
        var el = matrix.elements;
        return {
            a: el[0],
            b: el[1],
            c: el[4],
            d: el[5],
            e: el[12],
            f: el[13]
        };
    };
    AcPdfMatrixUtil.transformPoint = function (matrix, point) {
        var _a;
        var el = matrix.elements;
        var x = point.x;
        var y = point.y;
        var z = (_a = point.z) !== null && _a !== void 0 ? _a : 0;
        var w = el[3] * x + el[7] * y + el[11] * z + el[15];
        var invW = w === 0 ? 1 : 1 / w;
        return {
            x: (el[0] * x + el[4] * y + el[8] * z + el[12]) * invW,
            y: (el[1] * x + el[5] * y + el[9] * z + el[13]) * invW
        };
    };
    AcPdfMatrixUtil.transformBox = function (box, matrix) {
        var e_1, _a;
        if (box.isEmpty()) {
            return;
        }
        var min = box.min;
        var max = box.max;
        var corners = [
            { x: min.x, y: min.y, z: 0 },
            { x: max.x, y: min.y, z: 0 },
            { x: max.x, y: max.y, z: 0 },
            { x: min.x, y: max.y, z: 0 }
        ];
        var minX = Infinity;
        var minY = Infinity;
        var maxX = -Infinity;
        var maxY = -Infinity;
        try {
            for (var corners_1 = __values(corners), corners_1_1 = corners_1.next(); !corners_1_1.done; corners_1_1 = corners_1.next()) {
                var corner = corners_1_1.value;
                var p = this.transformPoint(matrix, corner);
                minX = Math.min(minX, p.x);
                minY = Math.min(minY, p.y);
                maxX = Math.max(maxX, p.x);
                maxY = Math.max(maxY, p.y);
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (corners_1_1 && !corners_1_1.done && (_a = corners_1.return)) _a.call(corners_1);
            }
            finally { if (e_1) throw e_1.error; }
        }
        box.min.set(minX, minY);
        box.max.set(maxX, maxY);
    };
    return AcPdfMatrixUtil;
}());
export { AcPdfMatrixUtil };
//# sourceMappingURL=AcPdfMatrixUtil.js.map