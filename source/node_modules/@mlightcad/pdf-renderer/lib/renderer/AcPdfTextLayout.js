var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
/**
 * MTEXT/TEXT layout engine for text-mode PDF export.
 *
 * Consumes the inline-format parse trees produced by {@link parseMText} and
 * positions them as styled runs inside wrapped lines: per-run height (`\H`),
 * width factor (`\W`), tracking (`\T`), oblique (`\Q`), color, font,
 * underline/overline/strikethrough, stacked fractions and tolerances (`\S`),
 * and paragraph justification (`\pxql|qc|qr;`). Baselines advance by the CAD
 * single-spacing distance `5/3 × height × lineSpaceFactor`; wrapping breaks
 * against the entity's column width.
 */
import { AcGiMTextFlowDirection } from '@mlightcad/data-model';
import { parseMText } from './AcPdfMTextParser';
/** Baseline-to-baseline distance of single-spaced text, in heights. */
export var LINE_SPACING_RATIO = 5 / 3;
/** Approximate ascender/descender extents of a text line, as height fractions. */
export var ASCENT_RATIO = 0.75;
export var DESCENT_RATIO = 0.25;
/** Decoration rule offsets relative to a run's baseline, in run sizes. */
export var UNDERLINE_OFFSET_RATIO = -0.05;
export var OVERLINE_OFFSET_RATIO = 1.05;
export var STRIKE_OFFSET_RATIO = 0.3;
/** Glyph scale of `\S a^b;` tolerance stacks relative to the current size. */
export var TOLERANCE_SCALE = 0.7;
/** Fallback per-character advance (in sizes) when no font metrics exist. */
var CHAR_FALLBACK_RATIO = 0.5;
/** Splits a run's text into wrap units: words for Latin, chars for CJK. */
function wrapUnits(line) {
    var e_1, _a;
    var units = [];
    var latin = '';
    var flushLatin = function () {
        if (latin !== '') {
            units.push(latin);
            latin = '';
        }
    };
    try {
        for (var line_1 = __values(line), line_1_1 = line_1.next(); !line_1_1.done; line_1_1 = line_1.next()) {
            var char = line_1_1.value;
            if (char === ' ') {
                flushLatin();
                units.push(' ');
            }
            else if (char.charCodeAt(0) > 0x2e7f) {
                // CJK and fullwidth ranges break anywhere.
                flushLatin();
                units.push(char);
            }
            else {
                latin += char;
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (line_1_1 && !line_1_1.done && (_a = line_1.return)) _a.call(line_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
    flushLatin();
    return units;
}
/** Advance of `text` for one style: metrics × width factor × tracking. */
function runAdvance(text, style, measure) {
    var raw = measure(text, style.size, style.fontName);
    if (raw === undefined) {
        return (text.length * style.size * CHAR_FALLBACK_RATIO * style.hScale * style.tracking);
    }
    return raw * style.hScale * style.tracking;
}
/** Total advance of a stack part (plain inline runs only). */
function segmentsWidth(segments, measure) {
    var e_2, _a;
    var total = 0;
    try {
        for (var segments_1 = __values(segments), segments_1_1 = segments_1.next(); !segments_1_1.done; segments_1_1 = segments_1.next()) {
            var segment = segments_1_1.value;
            if (segment.text !== undefined) {
                total += runAdvance(segment.text, segment.style, measure);
            }
        }
    }
    catch (e_2_1) { e_2 = { error: e_2_1 }; }
    finally {
        try {
            if (segments_1_1 && !segments_1_1.done && (_a = segments_1.return)) _a.call(segments_1);
        }
        finally { if (e_2) throw e_2.error; }
    }
    return total;
}
/**
 * Positions a stack part's inline runs along a shared baseline. `scale`
 * shrinks tolerance (`^`) glyph runs; widths and pen positions follow the
 * scaled size.
 */
function layoutInlineRuns(segments, measure, scale) {
    var e_3, _a;
    var runs = [];
    var pen = 0;
    try {
        for (var segments_2 = __values(segments), segments_2_1 = segments_2.next(); !segments_2_1.done; segments_2_1 = segments_2.next()) {
            var segment = segments_2_1.value;
            if (segment.text === undefined || segment.text === '') {
                continue;
            }
            var style = segment.style;
            var size = style.size * scale;
            var width = runAdvance(segment.text, __assign(__assign({}, style), { size: size }), measure);
            runs.push({
                text: segment.text,
                dx: pen,
                dy: 0,
                width: width,
                size: size,
                hScale: style.hScale,
                tracking: style.tracking,
                obliqueDeg: style.obliqueDeg,
                rgb: style.rgb,
                font: style.fontName,
                underline: style.underline || undefined,
                overline: style.overline || undefined,
                strike: style.strike || undefined
            });
            pen += width;
        }
    }
    catch (e_3_1) { e_3 = { error: e_3_1 }; }
    finally {
        try {
            if (segments_2_1 && !segments_2_1.done && (_a = segments_2.return)) _a.call(segments_2);
        }
        finally { if (e_3) throw e_3.error; }
    }
    return { runs: runs, width: pen };
}
/**
 * Lays out one `\S` stack into positioned runs at pen offset 0.
 *
 * Matches mtext-renderer semantics: tolerance stacks (`^`) paint at
 * `TOLERANCE_SCALE` without centering; fractions (`/`, `#`) paint at full
 * size, centered over the stacked width, with a horizontal rule — the stack
 * advance is the full-size maximum for every divider.
 */
function layoutStackRuns(style, stack, entityHeight, measure) {
    var e_4, _a, e_5, _b;
    var fs = style.size;
    var tolerance = stack.divider === '^';
    var scale = tolerance ? TOLERANCE_SCALE : 1;
    var num = layoutInlineRuns(stack.num, measure, scale);
    var den = layoutInlineRuns(stack.den, measure, scale);
    var advance = Math.max(segmentsWidth(stack.num, measure), segmentsWidth(stack.den, measure));
    var numDy = (tolerance ? -0.6 : -0.7) * fs + 1.3 * entityHeight;
    var denDy = (tolerance ? -1.3 : -1.6) * fs + 1.3 * entityHeight;
    var numOffset = tolerance ? 0 : (advance - num.width) / 2;
    var denOffset = tolerance ? 0 : (advance - den.width) / 2;
    var runs = [];
    try {
        for (var _c = __values(num.runs), _d = _c.next(); !_d.done; _d = _c.next()) {
            var run = _d.value;
            runs.push(__assign(__assign({}, run), { dx: run.dx + numOffset, dy: numDy }));
        }
    }
    catch (e_4_1) { e_4 = { error: e_4_1 }; }
    finally {
        try {
            if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
        }
        finally { if (e_4) throw e_4.error; }
    }
    try {
        for (var _e = __values(den.runs), _f = _e.next(); !_f.done; _f = _e.next()) {
            var run = _f.value;
            runs.push(__assign(__assign({}, run), { dx: run.dx + denOffset, dy: denDy }));
        }
    }
    catch (e_5_1) { e_5 = { error: e_5_1 }; }
    finally {
        try {
            if (_f && !_f.done && (_b = _e.return)) _b.call(_e);
        }
        finally { if (e_5) throw e_5.error; }
    }
    if (!tolerance && advance > 0) {
        runs.push({
            text: '',
            dx: 0,
            dy: 0,
            width: 0,
            size: fs,
            hScale: style.hScale,
            tracking: 1,
            obliqueDeg: 0,
            bar: { dx0: 0, dx1: advance, dy: -0.8 * fs + 0.3 * entityHeight }
        });
    }
    return runs;
}
/** Flattens a paragraph's segments into atomic wrap items. */
function paragraphItems(paragraph) {
    var e_6, _a, e_7, _b;
    var items = [];
    try {
        for (var _c = __values(paragraph.segments), _d = _c.next(); !_d.done; _d = _c.next()) {
            var segment = _d.value;
            if (segment.stack) {
                items.push({ style: segment.style, stack: segment.stack });
                continue;
            }
            if (segment.text === undefined || segment.text === '') {
                continue;
            }
            try {
                for (var _e = (e_7 = void 0, __values(wrapUnits(segment.text))), _f = _e.next(); !_f.done; _f = _e.next()) {
                    var unit = _f.value;
                    items.push({ style: segment.style, text: unit });
                }
            }
            catch (e_7_1) { e_7 = { error: e_7_1 }; }
            finally {
                try {
                    if (_f && !_f.done && (_b = _e.return)) _b.call(_e);
                }
                finally { if (e_7) throw e_7.error; }
            }
        }
    }
    catch (e_6_1) { e_6 = { error: e_6_1 }; }
    finally {
        try {
            if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
        }
        finally { if (e_6) throw e_6.error; }
    }
    return items;
}
function itemWidth(item, measure) {
    var _a;
    if (item.stack) {
        return Math.max(segmentsWidth(item.stack.num, measure), segmentsWidth(item.stack.den, measure));
    }
    return runAdvance((_a = item.text) !== null && _a !== void 0 ? _a : '', item.style, measure);
}
function trimTrailingSpaces(items) {
    while (items.length > 0 && items[items.length - 1].text === ' ') {
        items.pop();
    }
}
/**
 * Lays out MTEXT/TEXT contents as styled runs.
 *
 * Handles paragraph breaks (`\P`, `\N`, real newlines), width-driven word
 * wrap, stacked fractions/tolerances, and attachment-point anchoring with
 * per-paragraph justification overrides.
 */
export function layoutMText(params) {
    var e_8, _a, e_9, _b;
    var _c, _d, _e;
    var data = params.data, measure = params.measure;
    var height = data.height > 0 ? data.height : 1;
    var hScale = data.widthFactor && data.widthFactor > 0 ? data.widthFactor : 1;
    // The parser already expands `%%` symbol codes inline.
    var paragraphs = parseMText((_c = data.text) !== null && _c !== void 0 ? _c : '', {
        size: height,
        hScale: hScale
    });
    var step = ((_d = data.lineSpaceFactor) !== null && _d !== void 0 ? _d : 1) * LINE_SPACING_RATIO * height;
    var maxWidthLimit = data.width > 0 ? data.width : undefined;
    // Wrap each paragraph against the column width, keeping its justification.
    var wrapped = [];
    try {
        for (var paragraphs_1 = __values(paragraphs), paragraphs_1_1 = paragraphs_1.next(); !paragraphs_1_1.done; paragraphs_1_1 = paragraphs_1.next()) {
            var paragraph = paragraphs_1_1.value;
            var items = paragraphItems(paragraph);
            if (items.length === 0) {
                wrapped.push({ items: [], justify: paragraph.justify });
                continue;
            }
            if (maxWidthLimit === undefined) {
                trimTrailingSpaces(items);
                wrapped.push({ items: items, justify: paragraph.justify });
                continue;
            }
            var current = [];
            var currentWidth = 0;
            try {
                for (var items_1 = (e_9 = void 0, __values(items)), items_1_1 = items_1.next(); !items_1_1.done; items_1_1 = items_1.next()) {
                    var item = items_1_1.value;
                    var width = itemWidth(item, measure);
                    var isSpace = item.text === ' ';
                    if (current.length > 0 &&
                        !isSpace &&
                        currentWidth + width > maxWidthLimit) {
                        trimTrailingSpaces(current);
                        wrapped.push({ items: current, justify: paragraph.justify });
                        current = [];
                        currentWidth = 0;
                    }
                    if (isSpace && current.length === 0) {
                        continue;
                    }
                    current.push(item);
                    currentWidth += width;
                }
            }
            catch (e_9_1) { e_9 = { error: e_9_1 }; }
            finally {
                try {
                    if (items_1_1 && !items_1_1.done && (_b = items_1.return)) _b.call(items_1);
                }
                finally { if (e_9) throw e_9.error; }
            }
            trimTrailingSpaces(current);
            wrapped.push({ items: current, justify: paragraph.justify });
        }
    }
    catch (e_8_1) { e_8 = { error: e_8_1 }; }
    finally {
        try {
            if (paragraphs_1_1 && !paragraphs_1_1.done && (_a = paragraphs_1.return)) _a.call(paragraphs_1);
        }
        finally { if (e_8) throw e_8.error; }
    }
    // Trim trailing empty lines (paragraph joiners produce them).
    while (wrapped.length > 1 && wrapped[wrapped.length - 1].items.length === 0) {
        wrapped.pop();
    }
    // Right-to-left flow (DXF drawing direction 2) draws each line's items
    // from right to left: reverse the item order and the characters inside
    // each text item; extents stay unchanged. Top-to-bottom / bottom-to-top
    // flows fall back to left-to-right layout.
    var rtl = data.drawingDirection === AcGiMTextFlowDirection.RIGHT_TO_LEFT;
    var lines = wrapped.map(function (_a) {
        var e_10, _b, e_11, _c;
        var _d;
        var items = _a.items;
        var ordered = rtl
            ? __spreadArray([], __read(items), false).reverse()
                .map(function (item) {
                return item.text !== undefined
                    ? __assign(__assign({}, item), { text: __spreadArray([], __read(item.text), false).reverse().join('') }) : item;
            })
            : items;
        var runs = [];
        var pen = 0;
        var text = '';
        try {
            for (var ordered_1 = __values(ordered), ordered_1_1 = ordered_1.next(); !ordered_1_1.done; ordered_1_1 = ordered_1.next()) {
                var item = ordered_1_1.value;
                if (item.stack) {
                    try {
                        for (var _e = (e_11 = void 0, __values(layoutStackRuns(item.style, item.stack, height, measure))), _f = _e.next(); !_f.done; _f = _e.next()) {
                            var run = _f.value;
                            runs.push(__assign(__assign({}, run), { dx: run.dx + pen }));
                            if (run.text !== '') {
                                text += run.text;
                            }
                        }
                    }
                    catch (e_11_1) { e_11 = { error: e_11_1 }; }
                    finally {
                        try {
                            if (_f && !_f.done && (_c = _e.return)) _c.call(_e);
                        }
                        finally { if (e_11) throw e_11.error; }
                    }
                }
                else {
                    var runText = (_d = item.text) !== null && _d !== void 0 ? _d : '';
                    runs.push({
                        text: runText,
                        dx: pen,
                        dy: 0,
                        width: itemWidth(item, measure),
                        size: item.style.size,
                        hScale: item.style.hScale,
                        tracking: item.style.tracking,
                        obliqueDeg: item.style.obliqueDeg,
                        rgb: item.style.rgb,
                        font: item.style.fontName,
                        underline: item.style.underline || undefined,
                        overline: item.style.overline || undefined,
                        strike: item.style.strike || undefined
                    });
                    text += runText;
                }
                pen += itemWidth(item, measure);
            }
        }
        catch (e_10_1) { e_10 = { error: e_10_1 }; }
        finally {
            try {
                if (ordered_1_1 && !ordered_1_1.done && (_b = ordered_1.return)) _b.call(ordered_1);
            }
            finally { if (e_10) throw e_10.error; }
        }
        return { runs: runs, text: text, dx: 0, dy: 0, width: pen };
    });
    var lineWidths = lines.map(function (line) { return line.width; });
    var blockWidth = maxWidthLimit !== null && maxWidthLimit !== void 0 ? maxWidthLimit : Math.max.apply(Math, __spreadArray(__spreadArray([], __read(lineWidths), false), [0], false));
    // Attachment point (DXF group 71 / TEXT extensions 10-12): horizontal
    // 0 left / 1 center / 2 right, vertical 0 top / 1 middle / 2 bottom /
    // 3 baseline.
    var attachment = (_e = data.attachmentPoint) !== null && _e !== void 0 ? _e : 1;
    var horizontal = (attachment - 1) % 3;
    var vertical = Math.floor((attachment - 1) / 3);
    var defaultJustify = horizontal === 1 ? 'center' : horizontal === 2 ? 'right' : 'left';
    // Per-line extents from the runs' sizes and baseline offsets; empty lines
    // keep the entity-height defaults so blank paragraphs still take room.
    var ascents = lines.map(function (line) {
        var e_12, _a;
        var ascent = ASCENT_RATIO * height;
        try {
            for (var _b = __values(line.runs), _c = _b.next(); !_c.done; _c = _b.next()) {
                var run = _c.value;
                if (run.text !== '') {
                    ascent = Math.max(ascent, run.dy + ASCENT_RATIO * run.size);
                }
            }
        }
        catch (e_12_1) { e_12 = { error: e_12_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_12) throw e_12.error; }
        }
        return ascent;
    });
    var descents = lines.map(function (line) {
        var e_13, _a;
        var descent = DESCENT_RATIO * height;
        try {
            for (var _b = __values(line.runs), _c = _b.next(); !_c.done; _c = _b.next()) {
                var run = _c.value;
                if (run.text !== '') {
                    descent = Math.max(descent, -run.dy + DESCENT_RATIO * run.size);
                }
            }
        }
        catch (e_13_1) { e_13 = { error: e_13_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_13) throw e_13.error; }
        }
        return descent;
    });
    var lineCount = lines.length;
    var blockHeight = (lineCount - 1) * step + ascents[0] + descents[lineCount - 1];
    // First-line baseline per vertical attachment: the block spans
    // `[baseline0 − ascent0, baseline0 − (N−1)·step + descentLast]`.
    var baseline0;
    if (vertical === 0) {
        baseline0 = -ascents[0];
    }
    else if (vertical === 1) {
        baseline0 = blockHeight / 2 - ascents[0];
    }
    else if (vertical === 2) {
        baseline0 = (lineCount - 1) * step + descents[lineCount - 1];
    }
    else {
        // Baseline attachment (TEXT): the first line sits on the anchor.
        baseline0 = 0;
    }
    // The laid-out frame spans `[0, blockWidth]`, pinned to the anchor by the
    // horizontal attachment; each line's justification offsets it within the
    // frame (`\pxq*` overrides the attachment default).
    var frameOrigin = horizontal === 1 ? -blockWidth / 2 : horizontal === 2 ? -blockWidth : 0;
    lines.forEach(function (line, index) {
        var _a;
        var justify = (_a = wrapped[index].justify) !== null && _a !== void 0 ? _a : defaultJustify;
        var justifyOffset = justify === 'center'
            ? (blockWidth - line.width) / 2
            : justify === 'right'
                ? blockWidth - line.width
                : 0;
        line.dx = frameOrigin + justifyOffset;
        line.dy = baseline0 - index * step;
    });
    return {
        lines: lines,
        lineWidths: lineWidths,
        maxWidth: Math.max.apply(Math, __spreadArray(__spreadArray([], __read(lineWidths), false), [0], false)),
        blockHeight: blockHeight
    };
}
//# sourceMappingURL=AcPdfTextLayout.js.map