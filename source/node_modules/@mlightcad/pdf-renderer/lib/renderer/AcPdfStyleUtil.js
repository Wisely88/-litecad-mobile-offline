var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcGiContext } from '@mlightcad/data-model';
import { lineWeightToDrawingUnits } from '../AcPdfUnits';
import { isComplexLineType } from '../linetype/AcPdfLineTypeStroker';
import { rgbFromPacked } from './AcPdfStyle';
/**
 * Converts entity traits and export context into PDF stroke/fill styles.
 */
var AcPdfStyleUtil = /** @class */ (function () {
    function AcPdfStyleUtil() {
    }
    AcPdfStyleUtil.resolveRgb = function (traits, ctx, _kind) {
        return AcGiContext.fromBackgroundColor(ctx.backgroundColor).resolveSubEntityTraitsRgb(traits);
    };
    AcPdfStyleUtil.strokeStyle = function (traits, ctx) {
        var packed = this.contrastAgainstPaper(this.resolveRgb(traits, ctx, 'line'), ctx);
        var opacity = this.resolveOpacity(traits);
        var style = {
            rgb: rgbFromPacked(packed),
            opacity: opacity !== null && opacity !== void 0 ? opacity : 1,
            lineWidth: ctx.showLineWeight
                ? this.resolveStrokeWidth(traits.lineWeight, ctx.insunits)
                : 0
        };
        var dashArray = this.strokeDasharray(traits, ctx);
        if (dashArray) {
            style.dashArray = dashArray;
        }
        return style;
    };
    AcPdfStyleUtil.fillStyle = function (traits, ctx) {
        var packed = this.contrastAgainstPaper(this.resolveRgb(traits, ctx, 'fill'), ctx);
        var opacity = this.resolveOpacity(traits);
        return {
            rgb: rgbFromPacked(packed),
            opacity: opacity !== null && opacity !== void 0 ? opacity : 1
        };
    };
    AcPdfStyleUtil.pointStyle = function (traits, ctx) {
        var packed = this.contrastAgainstPaper(this.resolveRgb(traits, ctx, 'point'), ctx);
        var opacity = this.resolveOpacity(traits);
        return {
            rgb: rgbFromPacked(packed),
            opacity: opacity !== null && opacity !== void 0 ? opacity : 1
        };
    };
    /**
     * True-colour white (and ACI 7) geometry on white paper would otherwise
     * vanish. Hatches use the same contrast as strokes: AutoCAD plots ACI 7
     * as foreground, and the Three.js viewer inverts hatch-tier ACI 7 so it
     * stays visible against both light and dark canvases.
     */
    AcPdfStyleUtil.contrastAgainstPaper = function (packed, ctx) {
        var paper = ctx.backgroundColor;
        var dr = ((packed >> 16) & 0xff) - ((paper >> 16) & 0xff);
        var dg = ((packed >> 8) & 0xff) - ((paper >> 8) & 0xff);
        var db = (packed & 0xff) - (paper & 0xff);
        if (dr * dr + dg * dg + db * db < 40 * 40) {
            return ctx.foregroundColor;
        }
        return packed;
    };
    AcPdfStyleUtil.resolveStrokeWidth = function (lineWeight, insunits) {
        if (lineWeight < 0) {
            return 0;
        }
        return lineWeightToDrawingUnits(lineWeight, insunits);
    };
    /**
     * Maps {@link AcCmTransparency} to PDF opacity in `[0, 1]`.
     *
     * `AcCmTransparency.alpha` is 0–255 and only meaningful when the method is
     * `ByAlpha`. ByLayer / ByBlock leave opacity unset (fully opaque).
     *
     * Fully clear (`alpha === 0` / `isClear`) is also left unset: the Three.js
     * viewer does not apply entity transparency to materials yet, and some DWGs
     * mark visible FORMAT/dimension linework as `0x02000000` (ByAlpha clear).
     * Honoring that literally made dimensions vanish from PDF while still
     * visible on canvas.
     */
    AcPdfStyleUtil.resolveOpacity = function (traits) {
        var transparency = traits.transparency;
        if (!(transparency === null || transparency === void 0 ? void 0 : transparency.isByAlpha)) {
            return null;
        }
        var alpha = transparency.alpha;
        if (alpha == null || Number.isNaN(alpha) || alpha <= 0) {
            return null;
        }
        return Math.min(1, Math.max(0, alpha / 255));
    };
    AcPdfStyleUtil.strokeDasharray = function (traits, ctx) {
        var pattern = traits.lineType.pattern;
        if (!pattern || pattern.length === 0 || isComplexLineType(pattern)) {
            return undefined;
        }
        var scale = ctx.ltscale * ctx.celtscale * traits.lineTypeScale;
        var segments = this.patternToDashSegments(pattern, scale);
        return segments.length === 0 ? undefined : segments;
    };
    AcPdfStyleUtil.patternToDashSegments = function (pattern, scale) {
        var e_1, _a;
        var segments = [];
        try {
            for (var pattern_1 = __values(pattern), pattern_1_1 = pattern_1.next(); !pattern_1_1.done; pattern_1_1 = pattern_1.next()) {
                var element = pattern_1_1.value;
                var len = element.elementLength;
                if (len < 0 && element.elementTypeFlag !== 0) {
                    len = Math.abs(len);
                }
                len *= scale;
                if (len === 0) {
                    len = 0.5 * scale;
                }
                segments.push(Math.abs(len));
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (pattern_1_1 && !pattern_1_1.done && (_a = pattern_1.return)) _a.call(pattern_1);
            }
            finally { if (e_1) throw e_1.error; }
        }
        return segments;
    };
    return AcPdfStyleUtil;
}());
export { AcPdfStyleUtil };
//# sourceMappingURL=AcPdfStyleUtil.js.map