var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcCmColor, AcCmTransparency, acdbDrawTessellateOptions, acdbRasterizeOleMetafile, AcDbRenderingCache, AcGeMatrix3d, ACGI_DARK_THEME_FOREGROUND, ACGI_LIGHT_THEME_FOREGROUND, AcGiContext, acgiIsLightBackground, AcGiLineWeight } from '@mlightcad/data-model';
import { shadingFromGradient } from '../hatch/AcPdfGradient';
import { tessellateHatchPattern } from '../hatch/AcPdfHatchTessellator';
import { isComplexLineType, walkLineType } from '../linetype/AcPdfLineTypeStroker';
import { AcPdfDocumentWriter } from '../pdf/AcPdfDocumentWriter';
import { stripMtextCodes } from '../pdf/AcPdfMarkedContent';
import { AcPdfEntity } from './AcPdfEntity';
import { AcPdfGroup } from './AcPdfGroup';
import { AcPdfStyleUtil } from './AcPdfStyleUtil';
import { ASCENT_RATIO, DESCENT_RATIO, layoutMText, OVERLINE_OFFSET_RATIO, STRIKE_OFFSET_RATIO, UNDERLINE_OFFSET_RATIO } from './AcPdfTextLayout';
var DEFAULT_POINT_RADIUS = 0.5;
/**
 * AcGi backend that records vector drawables for PDF serialization.
 */
var AcPdfRenderer = /** @class */ (function () {
    function AcPdfRenderer() {
        this._entities = [];
        this._fontMapping = {};
        this._ltscale = 1;
        this._celtscale = 1;
        this._currentBackgroundColor = 0xffffff;
        this._foregroundColor = 0x000000;
        this._showLineWeight = false;
        this._insunits = 4;
        this._pageBackground = 'none';
        this._embedTextActualText = true;
        /** `'text'` paints MTEXT/TEXT as real PDF text through `_fonts`. */
        this._textMode = 'vector';
        this._pending = [];
        /**
         * Reuses rendered glyph primitives for identical text content + style.
         *
         * ATTRIB texts render once per INSERT instance, so drawings with thousands
         * of block references carrying the same attribute values re-tessellate the
         * same glyphs over and over; each result then dominated heap use. Cached
         * entries are shared read-only and re-positioned through a per-entity
         * translation matrix (paint bakes transformed copies, so sharing is safe).
         */
        this._mtextGlyphCache = new Map();
        this._shapeGlyphCache = new Map();
        this._mtextGlyphInflight = new Map();
        this._shapeGlyphInflight = new Map();
        this._context = AcGiContext.fromBackgroundColor(this._currentBackgroundColor);
        this._subEntityTraits = {
            color: new AcCmColor(),
            lineType: {
                type: 'ByLayer',
                name: 'Continuous',
                standardFlag: 0,
                description: 'Solid line',
                totalPatternLength: 0
            },
            lineTypeScale: 1,
            lineWeight: AcGiLineWeight.ByLayer,
            fillType: {
                solidFill: true,
                patternAngle: 0,
                definitionLines: []
            },
            transparency: new AcCmTransparency(),
            thickness: 0,
            layer: '0',
            drawOrder: 0
        };
    }
    /**
     * Clears the shared block rendering cache before PDF export.
     *
     * The cache stores drawable objects from the last renderer that populated it
     * (typically Three.js). Reusing those entries during export causes failures
     * when block references are resolved from cache.
     */
    AcPdfRenderer.prepareExport = function () {
        AcDbRenderingCache.instance.clear();
    };
    Object.defineProperty(AcPdfRenderer.prototype, "subEntityTraits", {
        get: function () {
            return this._subEntityTraits;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcPdfRenderer.prototype, "context", {
        get: function () {
            return this._context;
        },
        enumerable: false,
        configurable: true
    });
    AcPdfRenderer.prototype.setFontMapping = function (mapping) {
        this._fontMapping = mapping;
    };
    Object.defineProperty(AcPdfRenderer.prototype, "ltscale", {
        set: function (scale) {
            this._ltscale = scale;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcPdfRenderer.prototype, "celtscale", {
        set: function (scale) {
            this._celtscale = scale;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcPdfRenderer.prototype, "currentBackgroundColor", {
        get: function () {
            return this._currentBackgroundColor;
        },
        set: function (value) {
            this._currentBackgroundColor = value;
            this._context.backgroundIsDark = !acgiIsLightBackground(value);
            this._context.foregroundOnDark = ACGI_DARK_THEME_FOREGROUND;
            this._context.foregroundOnLight = ACGI_LIGHT_THEME_FOREGROUND;
        },
        enumerable: false,
        configurable: true
    });
    AcPdfRenderer.prototype.changeForeground = function (color) {
        this._foregroundColor = color;
    };
    Object.defineProperty(AcPdfRenderer.prototype, "showLineWeight", {
        get: function () {
            return this._showLineWeight;
        },
        set: function (value) {
            this._showLineWeight = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcPdfRenderer.prototype, "insunits", {
        set: function (value) {
            this._insunits = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcPdfRenderer.prototype, "textFontManager", {
        /** Bound font manager, or `undefined` while the host provided none. */
        get: function () {
            return this._fonts;
        },
        /**
         * Enables `textMode: 'text'` with the document font manager. Called by the
         * export entry point when the host provided a font resolver; text entities
         * then paint as real PDF text when an embeddable, coverage-complete font
         * is available, and fall back to vector glyphs otherwise.
         */
        set: function (fonts) {
            this._fonts = fonts;
            this._textMode = 'text';
        },
        enumerable: false,
        configurable: true
    });
    AcPdfRenderer.prototype.configureExport = function (options) {
        var _a;
        this._title = options.title;
        this._paper = options.paper;
        this._marginMm = options.marginMm;
        this._pageBackground = (_a = options.background) !== null && _a !== void 0 ? _a : 'none';
        this._glyphProvider = options.glyphProvider;
        this._fitBox = options.fitBox;
        this._embedTextActualText = options.embedTextActualText !== false;
        this._textMode = options.textMode === 'text' ? 'text' : 'vector';
        if (options.fontMapping) {
            this.setFontMapping(options.fontMapping);
        }
        if (options.ltscale != null) {
            this.ltscale = options.ltscale;
        }
        if (options.celtscale != null) {
            this.celtscale = options.celtscale;
        }
        if (options.showLineWeight != null) {
            this.showLineWeight = options.showLineWeight;
        }
    };
    Object.defineProperty(AcPdfRenderer.prototype, "styleContext", {
        get: function () {
            return {
                ltscale: this._ltscale,
                celtscale: this._celtscale,
                backgroundColor: this._currentBackgroundColor,
                foregroundColor: this._foregroundColor,
                showLineWeight: this._showLineWeight,
                insunits: this._insunits
            };
        },
        enumerable: false,
        configurable: true
    });
    AcPdfRenderer.prototype.pushEntity = function (entity) {
        var _a;
        entity.drawOrder = (_a = this._subEntityTraits.drawOrder) !== null && _a !== void 0 ? _a : 0;
        if (!entity.layerName) {
            entity.layerName = this._subEntityTraits.layer;
        }
        this._entities.push(entity);
        return entity;
    };
    AcPdfRenderer.prototype.removeEntities = function (entities) {
        var e_1, _a;
        try {
            for (var entities_1 = __values(entities), entities_1_1 = entities_1.next(); !entities_1_1.done; entities_1_1 = entities_1.next()) {
                var entity = entities_1_1.value;
                var index = this._entities.indexOf(entity);
                if (index >= 0) {
                    this._entities.splice(index, 1);
                }
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (entities_1_1 && !entities_1_1.done && (_a = entities_1.return)) _a.call(entities_1);
            }
            finally { if (e_1) throw e_1.error; }
        }
    };
    AcPdfRenderer.prototype.group = function (entities) {
        this.removeEntities(entities);
        return this.pushEntity(new AcPdfGroup(entities));
    };
    AcPdfRenderer.prototype.point = function (point, style) {
        var entity = new AcPdfEntity();
        var r = style.displaySize > 0 ? style.displaySize / 2 : DEFAULT_POINT_RADIUS;
        entity.addOp({
            kind: 'circle',
            x: point.x,
            y: point.y,
            r: r,
            style: AcPdfStyleUtil.pointStyle(this._subEntityTraits, this.styleContext)
        });
        entity.box.expandByPoint({ x: point.x - r, y: point.y - r });
        entity.box.expandByPoint({ x: point.x + r, y: point.y + r });
        return this.pushEntity(entity);
    };
    AcPdfRenderer.prototype.circularArc = function (arc) {
        return this.pushStrokedPolyline(arc.tessellate(acdbDrawTessellateOptions(this)), arc.box);
    };
    AcPdfRenderer.prototype.ellipticalArc = function (ellipseArc) {
        return this.pushStrokedPolyline(ellipseArc.tessellate(acdbDrawTessellateOptions(this)), ellipseArc.box);
    };
    AcPdfRenderer.prototype.lines = function (points) {
        var e_2, _a;
        var entity = new AcPdfEntity();
        var pts = toPdfPoints(points);
        var style = AcPdfStyleUtil.strokeStyle(this._subEntityTraits, this.styleContext);
        this.appendStrokedPoints(entity, pts, style);
        try {
            for (var pts_1 = __values(pts), pts_1_1 = pts_1.next(); !pts_1_1.done; pts_1_1 = pts_1.next()) {
                var p = pts_1_1.value;
                entity.box.expandByPoint(p);
            }
        }
        catch (e_2_1) { e_2 = { error: e_2_1 }; }
        finally {
            try {
                if (pts_1_1 && !pts_1_1.done && (_a = pts_1.return)) _a.call(pts_1);
            }
            finally { if (e_2) throw e_2.error; }
        }
        return this.pushEntity(entity);
    };
    AcPdfRenderer.prototype.lineSegments = function (array, itemSize, indices) {
        var entity = new AcPdfEntity();
        var style = AcPdfStyleUtil.strokeStyle(this._subEntityTraits, this.styleContext);
        for (var i = 0; i + 1 < indices.length; i += 2) {
            var ai = indices[i] * itemSize;
            var bi = indices[i + 1] * itemSize;
            var a = { x: array[ai], y: array[ai + 1] };
            var b = { x: array[bi], y: array[bi + 1] };
            entity.addOp({ kind: 'stroke', points: [a, b], style: style });
            entity.box.expandByPoint(a);
            entity.box.expandByPoint(b);
        }
        return this.pushEntity(entity);
    };
    AcPdfRenderer.prototype.area = function (area) {
        var e_3, _a, e_4, _b, e_5, _c;
        var _d, _e;
        var entity = new AcPdfEntity();
        var loops = area.tessellate(acdbDrawTessellateOptions(this));
        var pdfLoops = [];
        try {
            for (var loops_1 = __values(loops), loops_1_1 = loops_1.next(); !loops_1_1.done; loops_1_1 = loops_1.next()) {
                var loop = loops_1_1.value;
                if (loop.length === 0) {
                    continue;
                }
                var pts = toPdfPoints(loop);
                pdfLoops.push(pts);
                try {
                    for (var pts_2 = (e_4 = void 0, __values(pts)), pts_2_1 = pts_2.next(); !pts_2_1.done; pts_2_1 = pts_2.next()) {
                        var p = pts_2_1.value;
                        entity.box.expandByPoint(p);
                    }
                }
                catch (e_4_1) { e_4 = { error: e_4_1 }; }
                finally {
                    try {
                        if (pts_2_1 && !pts_2_1.done && (_b = pts_2.return)) _b.call(pts_2);
                    }
                    finally { if (e_4) throw e_4.error; }
                }
            }
        }
        catch (e_3_1) { e_3 = { error: e_3_1 }; }
        finally {
            try {
                if (loops_1_1 && !loops_1_1.done && (_a = loops_1.return)) _a.call(loops_1);
            }
            finally { if (e_3) throw e_3.error; }
        }
        var style = AcPdfStyleUtil.fillStyle(this._subEntityTraits, this.styleContext);
        var fillType = this._subEntityTraits.fillType;
        if (pdfLoops.length > 0 && fillType.gradient) {
            var packed = (Math.round(style.rgb.r * 255) << 16) |
                (Math.round(style.rgb.g * 255) << 8) |
                Math.round(style.rgb.b * 255);
            var shading = shadingFromGradient(fillType.gradient, pdfLoops, packed);
            entity.addOp({
                kind: 'gradient',
                loops: pdfLoops,
                shadingType: shading.shadingType,
                coords: shading.coords,
                c0: shading.c0,
                c1: shading.c1,
                strips: shading.strips,
                style: style
            });
        }
        else if (pdfLoops.length > 0 &&
            !fillType.solidFill &&
            ((_d = fillType.definitionLines) === null || _d === void 0 ? void 0 : _d.length)) {
            var segments = tessellateHatchPattern(pdfLoops, fillType.definitionLines, (_e = fillType.patternAngle) !== null && _e !== void 0 ? _e : 0);
            var stroke = AcPdfStyleUtil.strokeStyle(this._subEntityTraits, this.styleContext);
            stroke.rgb = style.rgb;
            stroke.dashArray = undefined;
            try {
                for (var segments_1 = __values(segments), segments_1_1 = segments_1.next(); !segments_1_1.done; segments_1_1 = segments_1.next()) {
                    var segment = segments_1_1.value;
                    entity.addOp({ kind: 'stroke', points: segment, style: stroke });
                }
            }
            catch (e_5_1) { e_5 = { error: e_5_1 }; }
            finally {
                try {
                    if (segments_1_1 && !segments_1_1.done && (_c = segments_1.return)) _c.call(segments_1);
                }
                finally { if (e_5) throw e_5.error; }
            }
        }
        else if (pdfLoops.length > 0) {
            entity.addOp({
                kind: 'fill',
                loops: pdfLoops,
                style: style
            });
        }
        return this.pushEntity(entity);
    };
    AcPdfRenderer.prototype.mtext = function (mtext, style, _delay) {
        var _this = this;
        var _a;
        var entity = new AcPdfEntity();
        var mapped = this.resolveStyle(style);
        var fill = AcPdfStyleUtil.fillStyle(this._subEntityTraits, this.styleContext);
        var stroke = AcPdfStyleUtil.strokeStyle(this._subEntityTraits, this.styleContext);
        // Capture synchronously: the source entity may move before promises resolve.
        var position = { x: mtext.position.x, y: mtext.position.y };
        if (this._textMode === 'text' && this._fonts) {
            var fonts = this._fonts;
            var fontName_1 = mapped.font;
            var contents = (_a = mtext.text) !== null && _a !== void 0 ? _a : '';
            if (contents.trim() !== '') {
                var pending_1 = Promise.resolve(fonts.load(fontName_1)).then(function (ok) {
                    if (ok &&
                        _this.applyTextLayout(entity, mtext, fontName_1, fill, position)) {
                        return;
                    }
                    // No embeddable program or the font misses glyphs: vector glyphs.
                    _this.applyVectorMtext(entity, mtext, mapped, fill, stroke, position);
                });
                this._pending.push(pending_1);
                return this.pushEntity(entity);
            }
        }
        this.applyVectorMtext(entity, mtext, mapped, fill, stroke, position);
        return this.pushEntity(entity);
    };
    /**
     * Lays `mtext` out as real PDF text runs (entity-local, then translated
     * to `position`). Returns `false` when a laid-out run's mapped font does
     * not cover its characters so the caller falls back to vector glyphs.
     */
    AcPdfRenderer.prototype.applyTextLayout = function (entity, mtext, fontName, fill, position) {
        var e_6, _a, e_7, _b, e_8, _c, e_9, _d;
        var _this = this;
        var _e, _f;
        var fonts = this._fonts;
        if (!fonts) {
            return false;
        }
        // Run fonts (`\F`) map through the font-mapping table like entity fonts;
        // unloaded run fonts measure through the entity font so line math stays
        // stable (the per-run covers check below still rejects the entity then).
        var measure = function (text, size, font) {
            var _a;
            if (font) {
                var width = fonts.widthOfText((_a = _this._fontMapping[font]) !== null && _a !== void 0 ? _a : font, text, size);
                if (width !== undefined) {
                    return width;
                }
            }
            return fonts.widthOfText(fontName, text, size);
        };
        var layout = layoutMText({ data: mtext, measure: measure });
        var joined = layout.lines.map(function (line) { return line.text; }).join('\n');
        if (joined.trim() === '') {
            return false;
        }
        // Every run must paint: a missing or glyph-incomplete run font falls
        // back to the entity font; if that misses too, the vector path takes over.
        var fontFor = function (raw) {
            var _a;
            var mapped = raw ? ((_a = _this._fontMapping[raw]) !== null && _a !== void 0 ? _a : raw) : fontName;
            return fonts.has(mapped) ? mapped : fontName;
        };
        try {
            for (var _g = __values(layout.lines), _h = _g.next(); !_h.done; _h = _g.next()) {
                var line = _h.value;
                try {
                    for (var _j = (e_7 = void 0, __values(line.runs)), _k = _j.next(); !_k.done; _k = _j.next()) {
                        var run = _k.value;
                        if (run.text !== '' && !fonts.covers(fontFor(run.font), run.text)) {
                            return false;
                        }
                    }
                }
                catch (e_7_1) { e_7 = { error: e_7_1 }; }
                finally {
                    try {
                        if (_k && !_k.done && (_b = _j.return)) _b.call(_j);
                    }
                    finally { if (e_7) throw e_7.error; }
                }
            }
        }
        catch (e_6_1) { e_6 = { error: e_6_1 }; }
        finally {
            try {
                if (_h && !_h.done && (_a = _g.return)) _a.call(_g);
            }
            finally { if (e_6) throw e_6.error; }
        }
        // Text plane frame. libredwg leaves MTEXT `rotation` at 0 and encodes the
        // real angle in the direction vector, so derive the baseline angle from a
        // planar direction whenever present. Up = normal × baseline flattened to
        // XY; a negative-Z extrusion (mirrored OCS) makes the projected frame
        // left-handed, rendered as mirrored glyphs through the op's `flipX`.
        var dv = mtext.directionVector;
        var rotation = dv && Math.hypot(dv.x, dv.y) > 1e-9
            ? Math.atan2(dv.y, dv.x)
            : ((_e = mtext.rotation) !== null && _e !== void 0 ? _e : 0);
        var rotationDeg = (rotation * 180) / Math.PI;
        // `normal` is typed only in newer data-model revisions (absent from the
        // published ^1.14.6 typings, which also never populate it at runtime) —
        // read it structurally so both revisions compile; when undefined the
        // default +Z normal applies.
        var n = mtext
            .normal;
        var nz = n && (n.x !== 0 || n.y !== 0 || n.z !== 0) ? n.z : 1;
        var cos = Math.cos(rotation);
        var sin = Math.sin(rotation);
        var upX = -nz * sin;
        var upY = nz * cos;
        var flipX = nz < 0;
        var angleDeg = flipX ? rotationDeg + 180 : rotationDeg;
        try {
            for (var _l = __values(layout.lines), _m = _l.next(); !_m.done; _m = _l.next()) {
                var line = _m.value;
                var _loop_1 = function (run) {
                    var e_10, _q;
                    var x = line.dx + run.dx;
                    var y = line.dy + run.dy;
                    if (run.text !== '') {
                        entity.addOp({
                            kind: 'text',
                            text: run.text,
                            hex: '',
                            font: fontFor(run.font),
                            size: run.size,
                            x: x * cos + y * upX,
                            y: x * sin + y * upY,
                            angleDeg: angleDeg,
                            hScale: run.hScale,
                            tracking: run.tracking,
                            obliqueDeg: run.obliqueDeg || undefined,
                            flipX: flipX,
                            style: run.rgb ? __assign(__assign({}, fill), { rgb: run.rgb }) : fill
                        });
                    }
                    // Stacked-fraction rules and run decorations draw as strokes in the
                    // same rotated frame, colored like the run.
                    var ruleStyle = {
                        rgb: (_f = run.rgb) !== null && _f !== void 0 ? _f : fill.rgb,
                        opacity: fill.opacity,
                        lineWidth: 0.03 * run.size
                    };
                    var addRule = function (x0, y0, x1, y1) {
                        entity.addOp({
                            kind: 'stroke',
                            points: [
                                { x: x0 * cos + y0 * upX, y: x0 * sin + y0 * upY },
                                { x: x1 * cos + y1 * upX, y: x1 * sin + y1 * upY }
                            ],
                            style: ruleStyle
                        });
                    };
                    if (run.bar) {
                        addRule(x + run.bar.dx0, y + run.bar.dy, x + run.bar.dx1, y + run.bar.dy);
                    }
                    if (run.text !== '') {
                        if (run.underline) {
                            addRule(x, y + UNDERLINE_OFFSET_RATIO * run.size, x + run.width, y + UNDERLINE_OFFSET_RATIO * run.size);
                        }
                        if (run.overline) {
                            addRule(x, y + OVERLINE_OFFSET_RATIO * run.size, x + run.width, y + OVERLINE_OFFSET_RATIO * run.size);
                        }
                        if (run.strike) {
                            addRule(x, y + STRIKE_OFFSET_RATIO * run.size, x + run.width, y + STRIKE_OFFSET_RATIO * run.size);
                        }
                        var runAscent = run.dy + ASCENT_RATIO * run.size;
                        var runDescent = run.dy - DESCENT_RATIO * run.size;
                        var corners = [
                            { x: x, y: runDescent },
                            { x: x + run.width, y: runDescent },
                            { x: x + run.width, y: runAscent },
                            { x: x, y: runAscent }
                        ];
                        if (run.bar) {
                            var by = y + run.bar.dy;
                            corners.push({ x: x + run.bar.dx0, y: by }, { x: x + run.bar.dx1, y: by });
                        }
                        try {
                            for (var corners_1 = (e_10 = void 0, __values(corners)), corners_1_1 = corners_1.next(); !corners_1_1.done; corners_1_1 = corners_1.next()) {
                                var corner = corners_1_1.value;
                                entity.box.expandByPoint({
                                    x: corner.x * cos + corner.y * upX,
                                    y: corner.x * sin + corner.y * upY
                                });
                            }
                        }
                        catch (e_10_1) { e_10 = { error: e_10_1 }; }
                        finally {
                            try {
                                if (corners_1_1 && !corners_1_1.done && (_q = corners_1.return)) _q.call(corners_1);
                            }
                            finally { if (e_10) throw e_10.error; }
                        }
                    }
                };
                try {
                    for (var _o = (e_9 = void 0, __values(line.runs)), _p = _o.next(); !_p.done; _p = _o.next()) {
                        var run = _p.value;
                        _loop_1(run);
                    }
                }
                catch (e_9_1) { e_9 = { error: e_9_1 }; }
                finally {
                    try {
                        if (_p && !_p.done && (_d = _o.return)) _d.call(_o);
                    }
                    finally { if (e_9) throw e_9.error; }
                }
            }
        }
        catch (e_8_1) { e_8 = { error: e_8_1 }; }
        finally {
            try {
                if (_m && !_m.done && (_c = _l.return)) _c.call(_l);
            }
            finally { if (e_8) throw e_8.error; }
        }
        entity.applyMatrix(new AcGeMatrix3d().makeTranslation(position.x, position.y, 0));
        return true;
    };
    /**
     * Vector-glyph fallback: tessellated text primitives through the glyph
     * provider, deduplicated by content + style (`_mtextGlyphCache`).
     */
    AcPdfRenderer.prototype.applyVectorMtext = function (entity, mtext, mapped, fill, stroke, position) {
        var _this = this;
        var _a;
        var provider = this._glyphProvider;
        if (!provider) {
            return;
        }
        var key = mtextGlyphKey(mtext, mapped);
        var cached = this._mtextGlyphCache.get(key);
        if (cached) {
            this.applyCachedGlyphs(entity, cached, position, stroke, fill);
            if (this._embedTextActualText && cached.actualText) {
                entity.actualText = cached.actualText;
            }
            return;
        }
        var contents = (_a = mtext.contents) !== null && _a !== void 0 ? _a : '';
        var inflight = this._mtextGlyphInflight.get(key);
        if (!inflight) {
            var render = Promise.resolve(provider.renderMText(mtext, mapped)).then(function (result) {
                var entry = {
                    primitives: result.primitives,
                    box: result.box,
                    actualText: _this._embedTextActualText
                        ? result.actualText || stripMtextCodes(contents)
                        : undefined
                };
                _this._mtextGlyphCache.set(key, entry);
                return entry;
            });
            this.trackGlyphInflight(this._mtextGlyphInflight, key, render);
            var pending_2 = render.then(function (entry) {
                _this.applyCachedGlyphs(entity, entry, position, stroke, fill);
                if (_this._embedTextActualText && entry.actualText) {
                    entity.actualText = entry.actualText;
                }
            });
            this._pending.push(pending_2);
            return;
        }
        // Identical glyph work already in flight: reuse its result instead of
        // rendering the same text again.
        var pending = inflight.then(function (entry) {
            _this.applyCachedGlyphs(entity, entry, position, stroke, fill);
            if (_this._embedTextActualText && entry.actualText) {
                entity.actualText = entry.actualText;
            }
        });
        this._pending.push(pending);
    };
    AcPdfRenderer.prototype.shape = function (shape, style, _delay) {
        var _this = this;
        var entity = new AcPdfEntity();
        var provider = this._glyphProvider;
        if (!provider) {
            return this.pushEntity(entity);
        }
        var mapped = style ? this.resolveStyle(style) : style;
        var fill = AcPdfStyleUtil.fillStyle(this._subEntityTraits, this.styleContext);
        var stroke = AcPdfStyleUtil.strokeStyle(this._subEntityTraits, this.styleContext);
        var position = { x: shape.position.x, y: shape.position.y };
        var key = shapeGlyphKey(shape, mapped);
        var cached = this._shapeGlyphCache.get(key);
        if (cached) {
            this.applyCachedGlyphs(entity, cached, position, stroke, fill);
            return this.pushEntity(entity);
        }
        var inflight = this._shapeGlyphInflight.get(key);
        if (!inflight) {
            var render = Promise.resolve(provider.renderShape(shape, mapped)).then(function (result) {
                var entry = {
                    primitives: result.primitives,
                    box: result.box
                };
                _this._shapeGlyphCache.set(key, entry);
                return entry;
            });
            this.trackGlyphInflight(this._shapeGlyphInflight, key, render);
        }
        var pending = (inflight !== null && inflight !== void 0 ? inflight : this._shapeGlyphInflight.get(key)).then(function (entry) {
            _this.applyCachedGlyphs(entity, entry, position, stroke, fill);
        });
        this._pending.push(pending);
        return this.pushEntity(entity);
    };
    AcPdfRenderer.prototype.image = function (blob, style) {
        var entity = new AcPdfEntity();
        var pending = blobToImageOp(blob, style).then(function (op) {
            if (!op) {
                return;
            }
            entity.addOp(op);
            entity.box.expandByPoint({ x: op.x, y: op.y });
            entity.box.expandByPoint({ x: op.x + op.width, y: op.y + op.height });
        });
        this._pending.push(pending);
        return this.pushEntity(entity);
    };
    /**
     * Waits for async glyph / image work scheduled by {@link mtext},
     * {@link shape}, and {@link image}.
     *
     * Paper-space viewport export must call this before cloning model-space
     * drawables; otherwise TEXT/MTEXT clones capture empty op lists.
     */
    AcPdfRenderer.prototype.awaitPending = function () {
        return __awaiter(this, void 0, void 0, function () {
            var pending;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        if (this._pending.length === 0) {
                            return [2 /*return*/];
                        }
                        pending = this._pending.splice(0, this._pending.length);
                        return [4 /*yield*/, Promise.all(pending)
                            // Glyph callbacks may have scheduled follow-up work.
                        ];
                    case 1:
                        _a.sent();
                        if (!(this._pending.length > 0)) return [3 /*break*/, 3];
                        return [4 /*yield*/, this.awaitPending()];
                    case 2:
                        _a.sent();
                        _a.label = 3;
                    case 3: return [2 /*return*/];
                }
            });
        });
    };
    /** Keeps one in-flight render per key; drops the entry once settled. */
    AcPdfRenderer.prototype.trackGlyphInflight = function (map, key, promise) {
        map.set(key, promise);
        promise.then(function () {
            if (map.get(key) === promise) {
                map.delete(key);
            }
        }, function () {
            map.delete(key);
        });
    };
    /**
     * Paints a cached glyph result into `entity`, re-positioning the shared
     * text-local primitives at this instance's position via a translation
     * matrix (ops stay shared; the writer maps them with a Form XObject or a
     * numeric bake).
     */
    AcPdfRenderer.prototype.applyCachedGlyphs = function (entity, entry, position, stroke, fill) {
        applyGlyphs(entity, entry.primitives, entry.box, stroke, fill);
        if (position.x !== 0 || position.y !== 0) {
            entity.applyMatrix(new AcGeMatrix3d().makeTranslation(position.x, position.y, 0));
        }
    };
    AcPdfRenderer.prototype.exportAsync = function (roots) {
        return __awaiter(this, void 0, void 0, function () {
            var entities;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0: return [4 /*yield*/, this.awaitPending()
                        // Prefer explicit roots from top-level worldDraw return values. The
                        // internal `_entities` list is polluted by AcDbRenderingCache: it leaves
                        // untransformed block templates in place while returning applyMatrix'd
                        // clones that are never pushed here.
                    ];
                    case 1:
                        _a.sent();
                        entities = roots !== null && roots !== void 0 ? roots : this._entities;
                        return [2 /*return*/, AcPdfDocumentWriter.write(entities, this.writeOptions())];
                }
            });
        });
    };
    /**
     * Paints one layout page into an existing document.
     *
     * Multi-layout exports reuse one {@link PDFDocument} (plus a shared OCG
     * manager and image cache via `overrides`) so finished pages never have to
     * be serialized, re-parsed and copied into a merge document.
     */
    AcPdfRenderer.prototype.renderToDocument = function (doc_1, roots_1) {
        return __awaiter(this, arguments, void 0, function (doc, roots, overrides) {
            var entities;
            if (overrides === void 0) { overrides = {}; }
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0: return [4 /*yield*/, this.awaitPending()];
                    case 1:
                        _a.sent();
                        entities = roots !== null && roots !== void 0 ? roots : this._entities;
                        return [4 /*yield*/, AcPdfDocumentWriter.writePage(doc, entities, this.writeOptions(overrides))];
                    case 2:
                        _a.sent();
                        return [2 /*return*/];
                }
            });
        });
    };
    /**
     * Drops drawables accumulated in the renderer's internal entity list.
     *
     * Multi-page exports pass explicit roots to every page; the internal list
     * only retains block templates and stale drawables. Clearing it between
     * pages lets per-page paper-space geometry be collected while earlier
     * pages remain alive only inside the shared PDF document.
     */
    AcPdfRenderer.prototype.resetCollected = function () {
        this._entities.length = 0;
    };
    AcPdfRenderer.prototype.writeOptions = function (overrides) {
        if (overrides === void 0) { overrides = {}; }
        return __assign({ insunits: this._insunits, background: this._pageBackground, title: this._title, paper: this._paper, marginMm: this._marginMm, fitBox: this._fitBox, embedTextActualText: this._embedTextActualText, fonts: this._fonts }, overrides);
    };
    AcPdfRenderer.prototype.appendStrokedPoints = function (entity, pts, style) {
        var e_11, _a, e_12, _b;
        var _c;
        var pattern = this._subEntityTraits.lineType.pattern;
        if (isComplexLineType(pattern) && pattern) {
            var scale = this._ltscale *
                this._celtscale *
                this._subEntityTraits.lineTypeScale;
            var walked = walkLineType(pts, pattern, scale);
            var dashless_1 = __assign(__assign({}, style), { dashArray: undefined });
            try {
                for (var _d = __values(walked.strokes), _e = _d.next(); !_e.done; _e = _d.next()) {
                    var stroke = _e.value;
                    entity.addOp({ kind: 'stroke', points: stroke, style: dashless_1 });
                }
            }
            catch (e_11_1) { e_11 = { error: e_11_1 }; }
            finally {
                try {
                    if (_e && !_e.done && (_a = _d.return)) _a.call(_d);
                }
                finally { if (e_11) throw e_11.error; }
            }
            var _loop_2 = function (shape) {
                var ds = Math.max(style.lineWidth, 0.5) * 2;
                entity.addOp({
                    kind: 'stroke',
                    points: [
                        { x: shape.x, y: shape.y },
                        {
                            x: shape.x + Math.cos(shape.angle) * ds,
                            y: shape.y + Math.sin(shape.angle) * ds
                        }
                    ],
                    style: dashless_1
                });
                if (this_1._glyphProvider && (shape.element.shapeName || shape.element.text)) {
                    var pending_3 = Promise.resolve(this_1._glyphProvider.renderShape({
                        position: { x: shape.x, y: shape.y, z: 0 },
                        size: (_c = shape.element.scale) !== null && _c !== void 0 ? _c : ds,
                        rotation: shape.angle,
                        name: shape.element.shapeName,
                        shapeNumber: shape.element.shapeNumber,
                        widthFactor: 1,
                        obliqingAngle: 0
                    }, undefined)).then(function (result) {
                        // Glyph geometry is text-local; this entity mixes world-space
                        // stroke marks, so paint the glyph through a child entity that
                        // carries the placement translation on its own matrix.
                        var glyphNode = new AcPdfEntity();
                        applyGlyphs(glyphNode, result.primitives, result.box, dashless_1, {
                            rgb: dashless_1.rgb,
                            opacity: dashless_1.opacity
                        });
                        glyphNode.applyMatrix(new AcGeMatrix3d().makeTranslation(shape.x, shape.y, 0));
                        entity.addChild(glyphNode);
                    });
                    this_1._pending.push(pending_3);
                }
            };
            var this_1 = this;
            try {
                for (var _f = __values(walked.shapes), _g = _f.next(); !_g.done; _g = _f.next()) {
                    var shape = _g.value;
                    _loop_2(shape);
                }
            }
            catch (e_12_1) { e_12 = { error: e_12_1 }; }
            finally {
                try {
                    if (_g && !_g.done && (_b = _f.return)) _b.call(_f);
                }
                finally { if (e_12) throw e_12.error; }
            }
            return;
        }
        if (pts.length >= 2) {
            entity.addOp({ kind: 'stroke', points: pts, style: style });
        }
    };
    AcPdfRenderer.prototype.pushStrokedPolyline = function (points, box) {
        var entity = new AcPdfEntity();
        var pts = toPdfPoints(points);
        this.appendStrokedPoints(entity, pts, AcPdfStyleUtil.strokeStyle(this._subEntityTraits, this.styleContext));
        entity.box.min.set(box.min.x, box.min.y);
        entity.box.max.set(box.max.x, box.max.y);
        return this.pushEntity(entity);
    };
    AcPdfRenderer.prototype.resolveStyle = function (style) {
        var _a;
        var mappedFont = (_a = this._fontMapping[style.font]) !== null && _a !== void 0 ? _a : style.font;
        return mappedFont !== style.font ? __assign(__assign({}, style), { font: mappedFont }) : style;
    };
    return AcPdfRenderer;
}());
export { AcPdfRenderer };
function toPdfPoints(points) {
    return points.map(function (p) { return ({ x: p.x, y: p.y }); });
}
/**
 * Cache key covering every mtext field that changes rendered geometry.
 * `position` is excluded on purpose: reuse translates shared primitives.
 * `style.lastHeight` is a scratch value that does not affect rendering.
 */
function mtextGlyphKey(mtext, style) {
    var _a, _b, _c, _d, _e, _f, _g;
    return JSON.stringify([
        (_a = mtext.text) !== null && _a !== void 0 ? _a : '',
        mtext.height,
        mtext.width,
        (_b = mtext.rotation) !== null && _b !== void 0 ? _b : null,
        (_c = mtext.directionVector) !== null && _c !== void 0 ? _c : null,
        (_d = mtext.attachmentPoint) !== null && _d !== void 0 ? _d : null,
        (_e = mtext.drawingDirection) !== null && _e !== void 0 ? _e : null,
        (_f = mtext.lineSpaceFactor) !== null && _f !== void 0 ? _f : null,
        (_g = mtext.widthFactor) !== null && _g !== void 0 ? _g : null,
        glyphStyleKey(style)
    ]);
}
/** Cache key covering every shape field that changes rendered geometry. */
function shapeGlyphKey(shape, style) {
    var _a, _b, _c, _d, _e;
    return JSON.stringify([
        (_a = shape.name) !== null && _a !== void 0 ? _a : null,
        (_b = shape.shapeNumber) !== null && _b !== void 0 ? _b : null,
        shape.size,
        (_c = shape.rotation) !== null && _c !== void 0 ? _c : null,
        (_d = shape.directionVector) !== null && _d !== void 0 ? _d : null,
        (_e = shape.widthFactor) !== null && _e !== void 0 ? _e : null,
        style ? glyphStyleKey(style) : null
    ]);
}
function glyphStyleKey(style) {
    var _a;
    return [
        style.name,
        style.standardFlag,
        style.fixedTextHeight,
        style.widthFactor,
        style.obliqueAngle,
        style.textGenerationFlag,
        style.font,
        style.bigFont,
        (_a = style.extendedFont) !== null && _a !== void 0 ? _a : ''
    ].join('\u0000');
}
/**
 * Paints a rendered glyph set into `entity` as one compact op per primitive
 * kind. Geometry is text-local (relative to the text's insertion point); the
 * caller positions it via `entity.applyMatrix`.
 */
function applyGlyphs(entity, primitives, box, strokeStyle, fillStyle) {
    if (primitives.triangles.length >= 6) {
        entity.addOp({
            kind: 'triangles',
            data: primitives.triangles,
            style: fillStyle
        });
    }
    if (primitives.polylines.length >= 2) {
        entity.addOp({
            kind: 'polylines',
            data: primitives.polylines,
            style: strokeStyle
        });
    }
    entity.box.min.set(box.min.x, box.min.y);
    entity.box.max.set(box.max.x, box.max.y);
}
function blobToImageOp(blob, style) {
    return __awaiter(this, void 0, void 0, function () {
        var boundary, type, format, raster, converted, minX, minY, maxX, maxY, boundary_1, boundary_1_1, pt, bytes, _a;
        var e_13, _b;
        return __generator(this, function (_c) {
            switch (_c.label) {
                case 0:
                    boundary = style.boundary;
                    if (!boundary || boundary.length < 2) {
                        return [2 /*return*/, null];
                    }
                    type = blob.type || '';
                    if (type.includes('svg')) {
                        return [2 /*return*/, null];
                    }
                    raster = blob;
                    if (!(type.includes('jpeg') || type.includes('jpg'))) return [3 /*break*/, 1];
                    format = 'jpg';
                    return [3 /*break*/, 4];
                case 1:
                    if (!type.includes('png')) return [3 /*break*/, 2];
                    format = 'png';
                    return [3 /*break*/, 4];
                case 2: return [4 /*yield*/, convertToEmbeddablePng(blob)];
                case 3:
                    converted = _c.sent();
                    if (!converted) {
                        return [2 /*return*/, null];
                    }
                    raster = converted;
                    format = 'png';
                    _c.label = 4;
                case 4:
                    minX = Infinity;
                    minY = Infinity;
                    maxX = -Infinity;
                    maxY = -Infinity;
                    try {
                        for (boundary_1 = __values(boundary), boundary_1_1 = boundary_1.next(); !boundary_1_1.done; boundary_1_1 = boundary_1.next()) {
                            pt = boundary_1_1.value;
                            minX = Math.min(minX, pt.x);
                            minY = Math.min(minY, pt.y);
                            maxX = Math.max(maxX, pt.x);
                            maxY = Math.max(maxY, pt.y);
                        }
                    }
                    catch (e_13_1) { e_13 = { error: e_13_1 }; }
                    finally {
                        try {
                            if (boundary_1_1 && !boundary_1_1.done && (_b = boundary_1.return)) _b.call(boundary_1);
                        }
                        finally { if (e_13) throw e_13.error; }
                    }
                    _a = Uint8Array.bind;
                    return [4 /*yield*/, raster.arrayBuffer()];
                case 5:
                    bytes = new (_a.apply(Uint8Array, [void 0, _c.sent()]))();
                    return [2 /*return*/, {
                            kind: 'image',
                            bytes: bytes,
                            format: format,
                            x: minX,
                            y: minY,
                            width: maxX - minX,
                            height: maxY - minY
                        }];
            }
        });
    });
}
/**
 * Converts a non-embeddable image blob (BMP / GIF raster or WMF / EMF
 * metafile) to PNG. Returns `undefined` when no conversion path applies or
 * decoding fails — callers then skip the image instead of failing the export.
 */
function convertToEmbeddablePng(blob) {
    return __awaiter(this, void 0, void 0, function () {
        var type, _a;
        return __generator(this, function (_b) {
            switch (_b.label) {
                case 0:
                    _b.trys.push([0, 4, , 5]);
                    type = blob.type || '';
                    if (!(type === 'image/wmf' || type === 'image/emf')) return [3 /*break*/, 2];
                    return [4 /*yield*/, acdbRasterizeOleMetafile(blob)];
                case 1: return [2 /*return*/, _b.sent()];
                case 2: return [4 /*yield*/, decodeRasterBlobToPng(blob)];
                case 3: return [2 /*return*/, _b.sent()];
                case 4:
                    _a = _b.sent();
                    return [2 /*return*/, undefined];
                case 5: return [2 /*return*/];
            }
        });
    });
}
/**
 * Decodes a browser-supported raster blob (BMP, GIF, WebP, …) and re-encodes
 * its first frame as PNG through a canvas.
 */
function decodeRasterBlobToPng(blob) {
    return __awaiter(this, void 0, void 0, function () {
        var bitmap, _a, canvas, ctx, canvas_1, ctx;
        return __generator(this, function (_b) {
            switch (_b.label) {
                case 0:
                    if (typeof createImageBitmap !== 'function') {
                        return [2 /*return*/, undefined];
                    }
                    _b.label = 1;
                case 1:
                    _b.trys.push([1, 3, , 4]);
                    return [4 /*yield*/, createImageBitmap(blob)];
                case 2:
                    bitmap = _b.sent();
                    return [3 /*break*/, 4];
                case 3:
                    _a = _b.sent();
                    return [2 /*return*/, undefined];
                case 4:
                    _b.trys.push([4, , 9, 10]);
                    if (!(typeof OffscreenCanvas === 'function')) return [3 /*break*/, 6];
                    canvas = new OffscreenCanvas(bitmap.width, bitmap.height);
                    ctx = canvas.getContext('2d');
                    if (!ctx) {
                        return [2 /*return*/, undefined];
                    }
                    ctx.drawImage(bitmap, 0, 0);
                    return [4 /*yield*/, canvas.convertToBlob({ type: 'image/png' })];
                case 5: return [2 /*return*/, _b.sent()];
                case 6:
                    if (!(typeof document !== 'undefined')) return [3 /*break*/, 8];
                    canvas_1 = document.createElement('canvas');
                    canvas_1.width = bitmap.width;
                    canvas_1.height = bitmap.height;
                    ctx = canvas_1.getContext('2d');
                    if (!ctx) {
                        return [2 /*return*/, undefined];
                    }
                    ctx.drawImage(bitmap, 0, 0);
                    return [4 /*yield*/, new Promise(function (resolve) {
                            canvas_1.toBlob(function (out) { return resolve(out !== null && out !== void 0 ? out : undefined); }, 'image/png');
                        })];
                case 7: return [2 /*return*/, _b.sent()];
                case 8: return [2 /*return*/, undefined];
                case 9:
                    bitmap.close();
                    return [7 /*endfinally*/];
                case 10: return [2 /*return*/];
            }
        });
    });
}
//# sourceMappingURL=AcPdfRenderer.js.map