var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
import { AcTrBatchedGroup } from '@mlightcad/three-renderer';
import * as THREE from 'three';
/**
 * Represents a CAD layer for organizing and rendering entities in Three.js.
 *
 * This class manages a collection of CAD entities that belong to the same logical layer.
 * It provides:
 * - Entity organization and grouping
 * - Layer visibility control
 * - Efficient batched rendering through Three.js groups
 * - Performance monitoring and statistics
 *
 * ## Technical Notes
 * Unlike Three.js built-in layers (which only support 32 layers), this implementation
 * uses Three.js groups to represent AutoCAD layers, allowing unlimited layer support.
 * Each AutoCAD layer corresponds to one Three.js group containing all entities.
 *
 * ## Performance Benefits
 * - Batched rendering reduces draw calls
 * - Efficient visibility toggling for entire layers
 * - Optimized entity grouping for better GPU performance
 *
 * @example
 * ```typescript
 * // Create a new layer
 * const layer = new AcTrLayer('Dimensions');
 *
 * // Add entities to the layer
 * const line = new AcTrLine(startPoint, endPoint);
 * layer.add(line);
 *
 * // Control layer visibility
 * layer.visible = false; // Hide all entities in this layer
 *
 * // Get layer statistics
 * const stats = layer.stats;
 * console.log(`Layer ${stats.name} has ${stats.entityCount} entities`);
 * ```
 */
var AcTrLayer = /** @class */ (function () {
    /**
     * Construct one instance of this class
     * @param layer - Layer information
     */
    function AcTrLayer(layer) {
        this._group = new AcTrBatchedGroup();
        this._name = layer.name;
        this._cachedBox = new THREE.Box3();
        this._boxDirty = true;
        this._group.visible = AcTrLayer.isLayerVisible(layer);
    }
    /**
     * Resolves whether a layer should be visible in the view.
     *
     * Non-plottable layer suppression is handled in {@link AcDbEntity.worldDraw};
     * only freeze/off state is reflected here.
     */
    AcTrLayer.isLayerVisible = function (info) {
        return !(info.isFrozen || info.isOff);
    };
    Object.defineProperty(AcTrLayer.prototype, "name", {
        /**
         * Layer name
         */
        get: function () {
            return this._name;
        },
        set: function (value) {
            this._name = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrLayer.prototype, "box", {
        /**
         * Gets the bounding box that contains all entities in this layer.
         *
         * Derived from packed batch vertex buffers, not entity metadata boxes.
         *
         * @returns The layer's bounding box
         */
        get: function () {
            if (this._boxDirty) {
                this.computeBatchBoundingBox(this._cachedBox);
                this._boxDirty = false;
            }
            return this._cachedBox;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Recomputes bounds from packed batch vertex buffers in this layer.
     */
    AcTrLayer.prototype.computeBatchBoundingBox = function (target, excludeObjectIds, includeObjectIds) {
        if (target === void 0) { target = new THREE.Box3(); }
        if (!this.visible) {
            target.makeEmpty();
            return target;
        }
        return this._group.computeBoundingBox(target, {
            excludeObjectIds: excludeObjectIds,
            includeObjectIds: includeObjectIds
        });
    };
    Object.defineProperty(AcTrLayer.prototype, "visible", {
        get: function () {
            return this._group.visible;
        },
        set: function (value) {
            this._group.visible = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrLayer.prototype, "internalObject", {
        get: function () {
            return this._group;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrLayer.prototype, "stats", {
        /**
         * The statistics of this layer
         */
        get: function () {
            var batchedGroupStats = this._group.stats;
            return __assign({ name: this._name }, batchedGroupStats);
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcTrLayer.prototype, "entityCount", {
        /**
         * The number of entities stored in this layer
         */
        get: function () {
            return this._group.entityCount;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Update layer information of this layer
     * @param value - New layer information
     */
    AcTrLayer.prototype.update = function (value) {
        var wasVisible = this.visible;
        this._name = value.name;
        this._group.visible = AcTrLayer.isLayerVisible(value);
        if (wasVisible !== this.visible) {
            this._boxDirty = true;
        }
    };
    /**
     * Find entities associated with the specified material and replace their material with new material
     * @param oldId - Id of the old material
     * @param material - The new material associated with entities
     */
    AcTrLayer.prototype.updateMaterial = function (oldId, material) {
        this._group.updateMaterial(oldId, material);
    };
    /**
     * Refreshes drawable materials after a layer-table style change.
     *
     * Updates batched material ids when the style cache remaps instances, patches
     * unbatched drawables, and rematerializes text glyph hierarchies.
     *
     * @param layerName - Target layer whose drawables should be refreshed.
     * @param layerTraits - Resolved layer traits from the live layer-table record.
     * @param materials - Style-cache material map, keyed by previous material id.
     * @param renderer - Renderer used to rebind layer-bound materials.
     */
    AcTrLayer.prototype.syncAppearanceFromRecord = function (layerName, layerTraits, materials, renderer) {
        var needsIdPatch = Object.entries(materials).some(function (_a) {
            var _b = __read(_a, 2), oldId = _b[0], material = _b[1];
            return material.id !== Number(oldId);
        });
        // Always push cache materials into batch containers. Batches own private
        // material clones (highlight isolation); an in-place colour refresh on the
        // style-manager cache (same material id) would otherwise leave clones stale
        // after switchbg / ACI-7 foreground inversion.
        for (var id in materials) {
            var oldId = Number(id);
            var material = materials[id];
            this.updateMaterial(oldId, material);
        }
        this._group.syncAppearanceFromRecord(layerName, layerTraits, materials, needsIdPatch, function (material, boundLayerName, boundLayerTraits) {
            return renderer.getLayerBoundMaterial(material, boundLayerName, boundLayerTraits);
        }, renderer.styleManager);
    };
    /** Rebinds batched drawables whose materials follow live layer-table style. */
    AcTrLayer.prototype.rebindMaterialsForLayer = function (layerName, layerTraits, getLayerBoundMaterial, styleManager) {
        this._group.rebindMaterialsForLayer(layerName, layerTraits, getLayerBoundMaterial, styleManager);
    };
    /** Rematerializes MTEXT/TEXT drawables stored as unbatched glyph subtrees. */
    AcTrLayer.prototype.rematerializeLayerTextDrawables = function (layerName, styleManager, layerTraits) {
        this._group.rematerializeLayerTextDrawables(layerName, styleManager, layerTraits);
    };
    /**
     * Re-render points with latest point style settings
     * @param displayMode Input display mode of points
     * @param displaySize Input display size of points (`PDSIZE`)
     */
    AcTrLayer.prototype.rerenderPoints = function (displayMode, displaySize) {
        if (displaySize === void 0) { displaySize = 0; }
        this._group.rerenderPoints(displayMode, displaySize);
    };
    /**
     * Collects world-space AABBs for point / point-symbol batch slots in this layer.
     *
     * @param out - Map of object id to world AABB; values are unioned across layers.
     * @returns The same map instance passed in {@link out}.
     */
    AcTrLayer.prototype.collectPointObjectWorldBoxes = function (out) {
        if (out === void 0) { out = new Map(); }
        return this._group.collectPointObjectWorldBoxes(out);
    };
    /**
     * Applies ACI-7 / foreground colour to owned batch material clones in this layer.
     */
    AcTrLayer.prototype.repaintForegroundMaterials = function (color) {
        this._group.repaintForegroundMaterials(color);
    };
    /**
     * Return true if this layer contains the entity with the specified object id. Otherwise, return false.
     * @param objectId Input the object id of one entity
     * @returns Return true if this layer contains the entity with the specified object id. Otherwise,
     * return false.
     */
    AcTrLayer.prototype.hasEntity = function (objectId) {
        return this._group.hasEntity(objectId);
    };
    /**
     * Updates visibility for one entity without rebuilding batched geometry.
     */
    AcTrLayer.prototype.setEntityVisible = function (objectId, visible) {
        return this._group.setEntityVisible(objectId, visible);
    };
    /**
     * Returns the current scene visibility for one entity.
     */
    AcTrLayer.prototype.getEntityVisible = function (objectId) {
        return this._group.getEntityVisible(objectId);
    };
    /**
     * Builds a preview subset for entities stored in this layer group.
     *
     * @param entityIds - Database object ids to extract from this layer
     * @param options - Optional preview style, slot limits, and missing-entity policy
     * @returns Preview subset group, or `null` when extraction failed
     */
    AcTrLayer.prototype.createPreviewSubset = function (entityIds, options) {
        return this._group.createPreviewSubset(entityIds, options);
    };
    /**
     * Add one AutoCAD entity into this layer.
     * @param entity Input AutoCAD entity to be added into this layer.
     * @param extendBbox - Input the flag whether to extend the bounding box of the scene by union the bounding box
     * of the specified entity. Defaults to true.
     */
    AcTrLayer.prototype.addEntity = function (entity, _extendBbox) {
        if (_extendBbox === void 0) { _extendBbox = true; }
        this._group.addEntity(entity);
        this._boxDirty = true;
    };
    /**
     * Appends pre-built geometry directly into the layer batch (line / point / mesh).
     *
     * @returns `true` when the geometry was registered in the batched group.
     */
    AcTrLayer.prototype.addDirectEntity = function (meta) {
        var options = {
            objectId: meta.objectId,
            visible: meta.visible,
            position: meta.position
        };
        var appended = false;
        if (meta.kind === 'lineFat') {
            appended = this._group.appendLine2Geometry(meta.geometry, meta.material, meta.worldOffset, options);
        }
        else if (meta.kind === 'lineBasic') {
            appended = this._group.appendLineGeometry(meta.geometry, meta.material, meta.worldOffset, options);
        }
        else if (meta.kind === 'point') {
            appended = this._group.appendPointGeometry(meta.geometry, meta.material, meta.worldOffset, options);
        }
        else if (meta.kind === 'mesh') {
            appended = this._group.appendMeshGeometry(meta.geometry, meta.material, meta.worldOffset, options);
        }
        if (appended) {
            this._boxDirty = true;
        }
        return appended;
    };
    /**
     * Return true if the object with the specified object id is intersected with the ray by using raycast.
     * @param objectId  Input object id of object to check for intersection with the ray.
     * @param raycaster Input raycaster to check intersection
     */
    AcTrLayer.prototype.isIntersectWith = function (objectId, raycaster) {
        return this._group.isIntersectWith(objectId, raycaster);
    };
    /**
     * Remove the specified entity from this layer.
     * @param objectId Input the object id of the entity to remove
     * @returns Return true if remove the specified entity successfully. Otherwise, return false.
     */
    AcTrLayer.prototype.removeEntity = function (objectId) {
        var removed = this._group.removeEntity(objectId);
        if (removed) {
            this._boxDirty = true;
        }
        return removed;
    };
    /**
     * Update the specified entity in this layer.
     * @param entity Input the entity to update
     * @returns Return true if update the specified entity successfully. Otherwise, return false.
     */
    AcTrLayer.prototype.updateEntity = function (entity) {
        if (!entity.objectId) {
            return false;
        }
        var isRemoved = this._group.removeEntity(entity.objectId);
        if (isRemoved) {
            this._group.addEntity(entity);
            this._boxDirty = true;
            return true;
        }
        if (entity.visible) {
            this._group.addEntity(entity);
            this._boxDirty = true;
            return true;
        }
        return false;
    };
    /**
     * Clear all entities in this layer and release batched geometry resources.
     */
    AcTrLayer.prototype.clear = function () {
        this._group.clear();
        this._cachedBox.makeEmpty();
        this._boxDirty = true;
        this._group.removeFromParent();
    };
    /**
     * Hover the specified entities
     */
    AcTrLayer.prototype.hover = function (ids) {
        if (ids.length === 0) {
            return;
        }
        if (ids.length === 1) {
            this._group.hover(ids[0]);
            return;
        }
        this._group.hoverMany(ids);
    };
    /**
     * Unhover the specified entities
     */
    AcTrLayer.prototype.unhover = function (ids) {
        if (ids.length === 0) {
            return;
        }
        if (ids.length === 1) {
            this._group.unhover(ids[0]);
            return;
        }
        this._group.unhoverMany(ids);
    };
    /**
     * Select the specified entities
     */
    AcTrLayer.prototype.select = function (ids) {
        if (ids.length === 0) {
            return;
        }
        if (ids.length === 1) {
            this._group.select(ids[0]);
            return;
        }
        this._group.selectMany(ids);
    };
    /**
     * Unselect the specified entities
     */
    AcTrLayer.prototype.unselect = function (ids) {
        if (ids.length === 0) {
            return;
        }
        if (ids.length === 1) {
            this._group.unselect(ids[0]);
            return;
        }
        this._group.unselectMany(ids);
    };
    /**
     * Applies compare-display coloring to entities in this layer.
     *
     * @param options - Compare colors and per-entity role overrides.
     */
    AcTrLayer.prototype.setCompareDisplay = function (options) {
        this._group.setCompareDisplay(options);
    };
    return AcTrLayer;
}());
export { AcTrLayer };
//# sourceMappingURL=AcTrLayer.js.map