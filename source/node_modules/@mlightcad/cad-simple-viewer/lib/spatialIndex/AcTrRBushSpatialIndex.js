var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import RBush from 'rbush';
import { isFiniteSpatialBBox } from '../view/AcTrGroupWcsBboxAssert';
import { estimateSpatialItemsBytes, isSpatialBoxFullyInside } from './AcTrSpatialIndex';
/** Approx. Map entry overhead (key pointer + value pointer + slot). */
var ID_MAP_ENTRY_BYTES = 40;
/** Rough R-tree node overhead relative to leaf item payload. */
var RBUSH_TREE_OVERHEAD_FACTOR = 1.4;
var AcTrRBushSpatialIndex = /** @class */ (function () {
    function AcTrRBushSpatialIndex(maxEntries) {
        this.tree = new RBush(maxEntries);
        this.idMap = new Map();
    }
    AcTrRBushSpatialIndex.prototype.insert = function (item) {
        // RBush parent-node bounds use Math.min/max; a single NaN bbox poisons the
        // tree so every later search returns empty (pick / osnap fail globally).
        if (!isFiniteSpatialBBox(item)) {
            return;
        }
        var hasId = typeof item.id === 'string' && item.id.length > 0;
        // Empty ids (hatch fill islands) must not share one Map slot — otherwise
        // later inserts overwrite earlier islands and only the last stays pickable.
        if (hasId) {
            var existing = this.idMap.get(item.id);
            if (existing) {
                if (existing.minX === item.minX &&
                    existing.minY === item.minY &&
                    existing.maxX === item.maxX &&
                    existing.maxY === item.maxY) {
                    return;
                }
                this.remove(existing, function (a, b) { return a.id === b.id; });
                this.idMap.delete(item.id);
            }
        }
        this.tree.insert(item);
        if (hasId) {
            this.idMap.set(item.id, item);
        }
    };
    AcTrRBushSpatialIndex.prototype.load = function (items) {
        var e_1, _a;
        var finiteItems = items.filter(isFiniteSpatialBBox);
        this.tree.load(finiteItems);
        try {
            for (var finiteItems_1 = __values(finiteItems), finiteItems_1_1 = finiteItems_1.next(); !finiteItems_1_1.done; finiteItems_1_1 = finiteItems_1.next()) {
                var item = finiteItems_1_1.value;
                if (typeof item.id === 'string' && item.id.length > 0) {
                    this.idMap.set(item.id, item);
                }
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (finiteItems_1_1 && !finiteItems_1_1.done && (_a = finiteItems_1.return)) _a.call(finiteItems_1);
            }
            finally { if (e_1) throw e_1.error; }
        }
    };
    AcTrRBushSpatialIndex.prototype.remove = function (item, equals) {
        this.tree.remove(item, equals !== null && equals !== void 0 ? equals : (function (a, b) {
            return a === b ||
                (a.id === b.id &&
                    a.minX === b.minX &&
                    a.minY === b.minY &&
                    a.maxX === b.maxX &&
                    a.maxY === b.maxY);
        }));
        if (typeof item.id === 'string' && item.id.length > 0) {
            this.idMap.delete(item.id);
        }
    };
    AcTrRBushSpatialIndex.prototype.removeById = function (id) {
        if (!(typeof id === 'string' && id.length > 0)) {
            return;
        }
        // Set minX, minY, maxX, and maxY to 0 in order to pass build
        this.tree.remove({
            minX: 0,
            minY: 0,
            maxX: 0,
            maxY: 0,
            id: id
        }, function (a, b) { return a.id === b.id; });
        this.idMap.delete(id);
    };
    /**
     * Returns the item currently indexed for {@link id}, when present.
     */
    AcTrRBushSpatialIndex.prototype.getById = function (id) {
        if (!(typeof id === 'string' && id.length > 0)) {
            return undefined;
        }
        return this.idMap.get(id);
    };
    AcTrRBushSpatialIndex.prototype.clear = function () {
        this.tree.clear();
        this.idMap.clear();
    };
    AcTrRBushSpatialIndex.prototype.search = function (bbox, options) {
        var hits = this.tree.search(bbox);
        if ((options === null || options === void 0 ? void 0 : options.selectionMode) !== 'window') {
            return hits;
        }
        return hits.filter(function (item) { return isSpatialBoxFullyInside(item, bbox); });
    };
    AcTrRBushSpatialIndex.prototype.collides = function (bbox) {
        return this.tree.collides(bbox);
    };
    AcTrRBushSpatialIndex.prototype.all = function () {
        return this.tree.all();
    };
    AcTrRBushSpatialIndex.prototype.getStats = function () {
        var items = this.tree.all();
        var itemCount = items.length;
        var itemBytes = estimateSpatialItemsBytes(items);
        var idMapBytes = this.idMap.size * ID_MAP_ENTRY_BYTES;
        return {
            kind: 'rbush',
            itemCount: itemCount,
            estimatedBytes: Math.round(itemBytes * RBUSH_TREE_OVERHEAD_FACTOR + idMapBytes)
        };
    };
    return AcTrRBushSpatialIndex;
}());
export { AcTrRBushSpatialIndex };
//# sourceMappingURL=AcTrRBushSpatialIndex.js.map