/**
 * Lightweight themed icon toolbar with overflow menu and horizontal/vertical layout.
 *
 * @module AcUiSimpleToolbar
 * @packageDocumentation
 */
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { acedApplyUiTheme, resolveUiTheme } from '../editor/global/AcEdUiTheme';
import { createIconElement, ICON_MORE } from './icons';
var STYLE_ID = 'ml-ui-simple-toolbar-styles';
var TOOLBAR_CSS = "\n  .ml-ui-simple-toolbar {\n    display: inline-flex;\n    gap: 4px;\n    padding: 4px;\n    box-sizing: border-box;\n    border: 1px solid var(--ml-ui-border, #dcdfe6);\n    border-radius: 6px;\n    background: var(--ml-ui-bg, rgba(255, 255, 255, 0.96));\n    box-shadow: var(--ml-ui-shadow, 0 2px 6px rgba(0, 0, 0, 0.12));\n    color: var(--ml-ui-text, #303133);\n    --ml-ui-simple-toolbar-btn-size: 32px;\n    max-width: 100%;\n    max-height: 100%;\n    pointer-events: auto;\n  }\n  .ml-ui-simple-toolbar[hidden] {\n    display: none !important;\n  }\n  .ml-ui-simple-toolbar.is-horizontal {\n    flex-direction: row;\n    align-items: center;\n  }\n  .ml-ui-simple-toolbar.is-vertical {\n    flex-direction: column;\n    align-items: stretch;\n  }\n  .ml-ui-simple-toolbar__track {\n    display: flex;\n    gap: 4px;\n    min-width: 0;\n    min-height: 0;\n    overflow: hidden;\n  }\n  .ml-ui-simple-toolbar.is-horizontal .ml-ui-simple-toolbar__track {\n    flex-direction: row;\n    align-items: center;\n    flex: 1 1 auto;\n  }\n  .ml-ui-simple-toolbar.is-vertical .ml-ui-simple-toolbar__track {\n    flex-direction: column;\n    align-items: stretch;\n    flex: 1 1 auto;\n  }\n  .ml-ui-simple-toolbar__btn {\n    position: relative;\n    flex: 0 0 auto;\n    width: var(--ml-ui-simple-toolbar-btn-size);\n    height: var(--ml-ui-simple-toolbar-btn-size);\n    padding: 0;\n    display: inline-flex;\n    align-items: center;\n    justify-content: center;\n    border: 1px solid transparent;\n    border-radius: 4px;\n    background: transparent;\n    color: inherit;\n    cursor: pointer;\n    box-sizing: border-box;\n  }\n  .ml-ui-simple-toolbar__btn:hover:not(:disabled) {\n    background: var(--ml-ui-accent-soft, rgba(64, 158, 255, 0.12));\n    border-color: var(--ml-ui-border, #dcdfe6);\n  }\n  .ml-ui-simple-toolbar__btn:disabled {\n    opacity: 0.45;\n    cursor: default;\n  }\n  .ml-ui-simple-toolbar__btn .ml-ex-ui-icon,\n  .ml-ui-simple-toolbar__btn .ml-ui-simple-toolbar__icon {\n    display: inline-flex;\n    width: 18px;\n    height: 18px;\n    align-items: center;\n    justify-content: center;\n  }\n  .ml-ui-simple-toolbar__btn .ml-ex-ui-icon svg,\n  .ml-ui-simple-toolbar__btn .ml-ui-simple-toolbar__icon svg {\n    width: 18px;\n    height: 18px;\n  }\n  .ml-ui-simple-toolbar__overflow-btn[hidden] {\n    display: none !important;\n  }\n  .ml-ui-simple-toolbar__menu {\n    position: absolute;\n    z-index: 60;\n    min-width: 160px;\n    max-width: min(280px, 90vw);\n    padding: 4px;\n    margin: 0;\n    list-style: none;\n    border: 1px solid var(--ml-ui-border, #dcdfe6);\n    border-radius: 6px;\n    background: var(--ml-ui-bg, #fff);\n    box-shadow: var(--ml-ui-shadow, 0 4px 12px rgba(0, 0, 0, 0.16));\n    color: var(--ml-ui-text, #303133);\n  }\n  .ml-ui-simple-toolbar__menu[hidden] {\n    display: none !important;\n  }\n  .ml-ui-simple-toolbar__menu-item {\n    display: flex;\n    align-items: center;\n    gap: 8px;\n    width: 100%;\n    padding: 8px 10px;\n    border: none;\n    border-radius: 4px;\n    background: transparent;\n    color: inherit;\n    font: inherit;\n    font-size: 13px;\n    text-align: start;\n    cursor: pointer;\n    box-sizing: border-box;\n  }\n  .ml-ui-simple-toolbar__menu-item:hover:not(:disabled) {\n    background: var(--ml-ui-accent-soft, rgba(64, 158, 255, 0.12));\n  }\n  .ml-ui-simple-toolbar__menu-item:disabled {\n    opacity: 0.45;\n    cursor: default;\n  }\n  .ml-ui-simple-toolbar__menu-item .ml-ex-ui-icon,\n  .ml-ui-simple-toolbar__menu-item .ml-ui-simple-toolbar__icon {\n    flex: 0 0 auto;\n    width: 18px;\n    height: 18px;\n    display: inline-flex;\n    align-items: center;\n    justify-content: center;\n  }\n  .ml-ui-simple-toolbar__menu-label {\n    flex: 1 1 auto;\n    min-width: 0;\n    overflow: hidden;\n    text-overflow: ellipsis;\n    white-space: nowrap;\n  }\n";
function ensureStyles() {
    if (typeof document === 'undefined')
        return;
    if (document.getElementById(STYLE_ID))
        return;
    var style = document.createElement('style');
    style.id = STYLE_ID;
    style.textContent = TOOLBAR_CSS;
    document.head.appendChild(style);
}
/**
 * Framework-free icon toolbar with theme tokens and overflow "⋯" menu.
 */
var AcUiSimpleToolbar = /** @class */ (function () {
    /**
     * @param options - Host, items, and layout.
     */
    function AcUiSimpleToolbar(options) {
        var _this = this;
        var _a, _b, _c;
        this.coreItems = [];
        this.extensionItems = [];
        this.resizeObserver = null;
        this.menuOpen = false;
        this.disposed = false;
        ensureStyles();
        this.host = options.host;
        this.orientation = (_a = options.orientation) !== null && _a !== void 0 ? _a : 'horizontal';
        this.moreLabel = (_b = options.moreLabel) !== null && _b !== void 0 ? _b : 'More';
        this.coreItems = __spreadArray([], __read(((_c = options.items) !== null && _c !== void 0 ? _c : [])), false);
        this.root = document.createElement('div');
        this.root.className = [
            'ml-ui-simple-toolbar',
            this.orientation === 'vertical' ? 'is-vertical' : 'is-horizontal',
            options.className
        ]
            .filter(Boolean)
            .join(' ');
        this.root.setAttribute('role', 'toolbar');
        acedApplyUiTheme(resolveUiTheme(this.host), this.root);
        this.track = document.createElement('div');
        this.track.className = 'ml-ui-simple-toolbar__track';
        this.overflowButton = document.createElement('button');
        this.overflowButton.type = 'button';
        this.overflowButton.className =
            'ml-ui-simple-toolbar__btn ml-ui-simple-toolbar__overflow-btn';
        this.overflowButton.hidden = true;
        this.overflowButton.title = this.moreLabel;
        this.overflowButton.setAttribute('aria-label', this.moreLabel);
        this.overflowButton.setAttribute('aria-haspopup', 'menu');
        this.overflowButton.setAttribute('aria-expanded', 'false');
        this.overflowButton.appendChild(createIconElement(ICON_MORE));
        this.overflowButton.addEventListener('click', function (event) {
            event.stopPropagation();
            if (_this.overflowButton.hidden)
                return;
            _this.toggleMenu();
        });
        this.menu = document.createElement('div');
        this.menu.className = 'ml-ui-simple-toolbar__menu';
        this.menu.setAttribute('role', 'menu');
        this.menu.hidden = true;
        this.root.append(this.track, this.overflowButton);
        this.host.appendChild(this.root);
        document.body.appendChild(this.menu);
        this.onDocumentPointerDown = function (event) {
            if (!_this.menuOpen)
                return;
            var target = event.target;
            if (target &&
                (_this.menu.contains(target) || _this.overflowButton.contains(target))) {
                return;
            }
            _this.closeMenu();
        };
        document.addEventListener('pointerdown', this.onDocumentPointerDown, true);
        this.render();
        this.setupResizeObserver();
    }
    Object.defineProperty(AcUiSimpleToolbar.prototype, "visibleItems", {
        /**
         * Visible items: extension (e.g. selection style) first, then core,
         * excluding `visible: false`.
         */
        get: function () {
            return __spreadArray(__spreadArray([], __read(this.extensionItems), false), __read(this.coreItems), false).filter(function (item) { return item.visible !== false; });
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Replaces core items and re-renders.
     *
     * @param items - New core toolbar items.
     */
    AcUiSimpleToolbar.prototype.setItems = function (items) {
        this.coreItems = __spreadArray([], __read(items), false);
        this.render();
    };
    /**
     * Replaces extension items (e.g. selection accessory) prepended before core.
     *
     * @param items - Extension items; pass `[]` to clear.
     */
    AcUiSimpleToolbar.prototype.setExtensionItems = function (items) {
        this.extensionItems = __spreadArray([], __read(items), false);
        this.render();
    };
    /**
     * Patches a single item by id (core or extension).
     *
     * @param id - Item id.
     * @param patch - Partial fields to merge.
     */
    AcUiSimpleToolbar.prototype.updateItem = function (id, patch) {
        var patchList = function (list) {
            var index = list.findIndex(function (item) { return item.id === id; });
            if (index < 0)
                return false;
            list[index] = __assign(__assign({}, list[index]), patch);
            return true;
        };
        if (!patchList(this.coreItems) && !patchList(this.extensionItems))
            return;
        this.render();
    };
    /**
     * Switches horizontal / vertical layout.
     *
     * @param orientation - New orientation.
     */
    AcUiSimpleToolbar.prototype.setOrientation = function (orientation) {
        if (this.orientation === orientation)
            return;
        this.orientation = orientation;
        this.root.classList.toggle('is-horizontal', orientation === 'horizontal');
        this.root.classList.toggle('is-vertical', orientation === 'vertical');
        this.layoutOverflow();
    };
    /**
     * Updates the overflow button label.
     *
     * @param label - Accessible / tooltip label.
     */
    AcUiSimpleToolbar.prototype.setMoreLabel = function (label) {
        this.moreLabel = label;
        this.overflowButton.title = label;
        this.overflowButton.setAttribute('aria-label', label);
    };
    /** Shows or hides the whole toolbar. */
    AcUiSimpleToolbar.prototype.setVisible = function (visible) {
        this.root.hidden = !visible;
        if (!visible)
            this.closeMenu();
    };
    /** Re-applies UI theme tokens from the host ancestry. */
    AcUiSimpleToolbar.prototype.refreshTheme = function () {
        acedApplyUiTheme(resolveUiTheme(this.host), this.root);
        acedApplyUiTheme(resolveUiTheme(this.host), this.menu);
    };
    /** Tears down listeners, observers, and DOM. */
    AcUiSimpleToolbar.prototype.dispose = function () {
        var _a;
        if (this.disposed)
            return;
        this.disposed = true;
        this.closeMenu();
        document.removeEventListener('pointerdown', this.onDocumentPointerDown, true);
        (_a = this.resizeObserver) === null || _a === void 0 ? void 0 : _a.disconnect();
        this.resizeObserver = null;
        this.menu.remove();
        this.root.remove();
    };
    AcUiSimpleToolbar.prototype.setupResizeObserver = function () {
        var _this = this;
        if (typeof ResizeObserver === 'undefined') {
            this.layoutOverflow();
            return;
        }
        this.resizeObserver = new ResizeObserver(function () { return _this.layoutOverflow(); });
        this.resizeObserver.observe(this.root);
        if (this.host !== this.root)
            this.resizeObserver.observe(this.host);
    };
    AcUiSimpleToolbar.prototype.render = function () {
        var e_1, _a;
        var _this = this;
        this.closeMenu();
        this.track.replaceChildren();
        try {
            for (var _b = __values(this.visibleItems), _c = _b.next(); !_c.done; _c = _b.next()) {
                var item = _c.value;
                this.track.appendChild(this.createButton(item));
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_1) throw e_1.error; }
        }
        // Defer overflow measure until buttons have layout.
        requestAnimationFrame(function () { return _this.layoutOverflow(); });
    };
    AcUiSimpleToolbar.prototype.createButton = function (item) {
        var _this = this;
        var _a;
        var button = document.createElement('button');
        button.type = 'button';
        button.className = 'ml-ui-simple-toolbar__btn';
        button.dataset.toolbarItemId = item.id;
        var title = (_a = item.title) !== null && _a !== void 0 ? _a : item.label;
        button.title = title;
        button.setAttribute('aria-label', title);
        button.disabled = Boolean(item.disabled);
        button.appendChild(createIconElement(item.icon));
        button.addEventListener('click', function (event) {
            event.stopPropagation();
            if (button.disabled)
                return;
            _this.closeMenu();
            item.onClick(event);
        });
        return button;
    };
    AcUiSimpleToolbar.prototype.layoutOverflow = function () {
        var e_2, _a;
        if (this.disposed || this.root.hidden)
            return;
        var buttons = Array.from(this.track.querySelectorAll('.ml-ui-simple-toolbar__btn'));
        try {
            for (var buttons_1 = __values(buttons), buttons_1_1 = buttons_1.next(); !buttons_1_1.done; buttons_1_1 = buttons_1.next()) {
                var button = buttons_1_1.value;
                button.hidden = false;
            }
        }
        catch (e_2_1) { e_2 = { error: e_2_1 }; }
        finally {
            try {
                if (buttons_1_1 && !buttons_1_1.done && (_a = buttons_1.return)) _a.call(buttons_1);
            }
            finally { if (e_2) throw e_2.error; }
        }
        this.overflowButton.hidden = true;
        this.closeMenu();
        if (buttons.length === 0)
            return;
        var horizontal = this.orientation === 'horizontal';
        var gap = 4;
        var sizes = buttons.map(function (button) {
            return horizontal ? button.offsetWidth : button.offsetHeight;
        });
        var total = sizes.reduce(function (sum, size, index) { return sum + size + (index > 0 ? gap : 0); }, 0);
        var computed = getComputedStyle(this.root);
        var padding = horizontal
            ? (parseFloat(computed.paddingLeft) || 0) +
                (parseFloat(computed.paddingRight) || 0)
            : (parseFloat(computed.paddingTop) || 0) +
                (parseFloat(computed.paddingBottom) || 0);
        var available = (horizontal ? this.root.clientWidth : this.root.clientHeight) - padding;
        // Shrink-wrapped toolbars report client size ≈ content; allow 1px rounding.
        if (!(available > 0) || total <= available + 1)
            return;
        var overflowSize = horizontal
            ? this.overflowButton.offsetWidth || 32
            : this.overflowButton.offsetHeight || 32;
        var budget = available - overflowSize - gap;
        var used = 0;
        var firstHidden = buttons.length;
        for (var i = 0; i < buttons.length; i++) {
            var next = used + (i > 0 ? gap : 0) + sizes[i];
            if (next > budget && i > 0) {
                firstHidden = i;
                break;
            }
            used = next;
        }
        if (firstHidden >= buttons.length)
            return;
        this.overflowButton.hidden = false;
        for (var i = firstHidden; i < buttons.length; i++) {
            buttons[i].hidden = true;
        }
    };
    AcUiSimpleToolbar.prototype.toggleMenu = function () {
        if (this.menuOpen)
            this.closeMenu();
        else
            this.openMenu();
    };
    AcUiSimpleToolbar.prototype.openMenu = function () {
        var e_3, _a;
        var _this = this;
        var hiddenButtons = Array.from(this.track.querySelectorAll('.ml-ui-simple-toolbar__btn[hidden]'));
        if (hiddenButtons.length === 0)
            return;
        var itemsById = new Map(this.visibleItems.map(function (item) { return [item.id, item]; }));
        this.menu.replaceChildren();
        var _loop_1 = function (button) {
            var id = button.dataset.toolbarItemId;
            if (!id)
                return "continue";
            var item = itemsById.get(id);
            if (!item)
                return "continue";
            var row = document.createElement('button');
            row.type = 'button';
            row.className = 'ml-ui-simple-toolbar__menu-item';
            row.setAttribute('role', 'menuitem');
            row.disabled = Boolean(item.disabled);
            row.appendChild(createIconElement(item.icon));
            var label = document.createElement('span');
            label.className = 'ml-ui-simple-toolbar__menu-label';
            label.textContent = item.label;
            row.appendChild(label);
            row.addEventListener('click', function (event) {
                event.stopPropagation();
                if (row.disabled)
                    return;
                _this.closeMenu();
                item.onClick(event);
            });
            this_1.menu.appendChild(row);
        };
        var this_1 = this;
        try {
            for (var hiddenButtons_1 = __values(hiddenButtons), hiddenButtons_1_1 = hiddenButtons_1.next(); !hiddenButtons_1_1.done; hiddenButtons_1_1 = hiddenButtons_1.next()) {
                var button = hiddenButtons_1_1.value;
                _loop_1(button);
            }
        }
        catch (e_3_1) { e_3 = { error: e_3_1 }; }
        finally {
            try {
                if (hiddenButtons_1_1 && !hiddenButtons_1_1.done && (_a = hiddenButtons_1.return)) _a.call(hiddenButtons_1);
            }
            finally { if (e_3) throw e_3.error; }
        }
        acedApplyUiTheme(resolveUiTheme(this.host), this.menu);
        this.menu.hidden = false;
        this.menuOpen = true;
        this.overflowButton.setAttribute('aria-expanded', 'true');
        this.positionMenu();
    };
    AcUiSimpleToolbar.prototype.positionMenu = function () {
        var rect = this.overflowButton.getBoundingClientRect();
        var menuRect = this.menu.getBoundingClientRect();
        var left = rect.left;
        var top = rect.bottom + 4;
        if (this.orientation === 'vertical') {
            left = rect.right + 4;
            top = rect.top;
        }
        if (left + menuRect.width > window.innerWidth - 8) {
            left = Math.max(8, window.innerWidth - menuRect.width - 8);
        }
        if (top + menuRect.height > window.innerHeight - 8) {
            top = Math.max(8, rect.top - menuRect.height - 4);
        }
        this.menu.style.left = "".concat(left, "px");
        this.menu.style.top = "".concat(top, "px");
    };
    AcUiSimpleToolbar.prototype.closeMenu = function () {
        if (!this.menuOpen && this.menu.hidden)
            return;
        this.menuOpen = false;
        this.menu.hidden = true;
        this.overflowButton.setAttribute('aria-expanded', 'false');
    };
    return AcUiSimpleToolbar;
}());
export { AcUiSimpleToolbar };
//# sourceMappingURL=AcUiSimpleToolbar.js.map