import { AcUiDialog } from './AcUiDialog';
/**
 * About dialog showing mlightcad branding and product links.
 *
 * Extends {@link AcUiDialog}; styles are injected once per document.
 */
export declare class AcUiAboutDialog extends AcUiDialog {
    /** ID of About-specific style element. */
    static readonly aboutStyleId = "ml-ui-about-dialog-styles";
    private static aboutStylesInjected;
    private static openInstance;
    private constructor();
    /**
     * Opens the About dialog (replaces any already-open instance).
     *
     * @param host - Element that receives the dialog backdrop
     * @returns Promise that resolves when the dialog is closed
     */
    static open(host?: HTMLElement): Promise<void>;
    close(): void;
    private buildContent;
    private createLink;
    private static ensureAboutStyles;
}
//# sourceMappingURL=AcUiAboutDialog.d.ts.map