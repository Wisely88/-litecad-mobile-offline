/**
 * Dialog for choosing adaptive (screen px) vs custom WCS text height.
 *
 * Custom mode includes a small calculator that converts a desired on-screen
 * font size at the current zoom into a fixed world-space height.
 *
 * @module AcUiTextHeightDialog
 * @packageDocumentation
 */
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import { AcUiDialog } from './AcUiDialog';
var STYLE_ID = 'ml-ui-text-height-dialog-styles';
var DIALOG_CSS = "\n  .ml-ui-text-height-dialog .ml-ui-text-height-options {\n    display: flex;\n    flex-direction: column;\n    gap: 12px;\n  }\n  .ml-ui-text-height-dialog .ml-ui-text-height-option {\n    display: flex;\n    flex-direction: column;\n    gap: 8px;\n  }\n  .ml-ui-text-height-dialog .ml-ui-text-height-radio {\n    display: flex;\n    align-items: center;\n    gap: 8px;\n    font-size: 14px;\n    cursor: pointer;\n  }\n  .ml-ui-text-height-dialog .ml-ui-text-height-radio input {\n    margin: 0;\n  }\n  .ml-ui-text-height-dialog .ml-ui-text-height-custom-row {\n    display: flex;\n    align-items: center;\n    gap: 8px;\n    padding-left: 24px;\n  }\n  .ml-ui-text-height-dialog .ml-ui-text-height-input {\n    flex: 1 1 auto;\n    min-width: 0;\n    height: 32px;\n    padding: 0 8px;\n    border: 1px solid var(--ml-ui-border, #dcdfe6);\n    border-radius: 4px;\n    background: var(--ml-ui-bg, #fff);\n    color: inherit;\n    font-size: 13px;\n    box-sizing: border-box;\n  }\n  .ml-ui-text-height-dialog .ml-ui-text-height-input:disabled {\n    opacity: 0.55;\n  }\n  .ml-ui-text-height-dialog .ml-ui-text-height-calc {\n    margin-left: 24px;\n    padding: 10px 12px;\n    border: 1px solid var(--ml-ui-border, #dcdfe6);\n    border-radius: 6px;\n    background: var(--ml-ui-bg-muted, rgba(127, 127, 127, 0.08));\n    display: flex;\n    flex-direction: column;\n    gap: 8px;\n  }\n  .ml-ui-text-height-dialog .ml-ui-text-height-calc[data-disabled='true'] {\n    opacity: 0.55;\n    pointer-events: none;\n  }\n  .ml-ui-text-height-dialog .ml-ui-text-height-calc-title {\n    font-size: 13px;\n    font-weight: 600;\n    line-height: 1.3;\n  }\n  .ml-ui-text-height-dialog .ml-ui-text-height-calc-hint {\n    font-size: 12px;\n    line-height: 1.45;\n    opacity: 0.78;\n  }\n  .ml-ui-text-height-dialog .ml-ui-text-height-calc-row {\n    display: flex;\n    align-items: center;\n    gap: 8px;\n  }\n  .ml-ui-text-height-dialog .ml-ui-text-height-calc-row .ml-ui-text-height-input {\n    flex: 1 1 auto;\n    max-width: 120px;\n  }\n  .ml-ui-text-height-dialog .ml-ui-text-height-calc-unit {\n    flex: 0 0 auto;\n    font-size: 13px;\n    opacity: 0.8;\n  }\n  .ml-ui-text-height-dialog .ml-ui-text-height-convert {\n    flex: 0 0 auto;\n    height: 32px;\n    padding: 0 12px;\n    border: 1px solid var(--ml-ui-border, #dcdfe6);\n    border-radius: 4px;\n    background: var(--ml-ui-bg, #fff);\n    color: inherit;\n    font-size: 13px;\n    cursor: pointer;\n  }\n  .ml-ui-text-height-dialog .ml-ui-text-height-convert:disabled {\n    opacity: 0.55;\n    cursor: default;\n  }\n";
function ensureStyles() {
    if (typeof document === 'undefined')
        return;
    if (document.getElementById(STYLE_ID))
        return;
    var style = document.createElement('style');
    style.id = STYLE_ID;
    style.textContent = DIALOG_CSS;
    document.head.appendChild(style);
}
/** Formats a positive WCS height for the input field. */
function formatWcsHeight(value) {
    if (!(value > 0) || !Number.isFinite(value))
        return '';
    var rounded = Number(value.toPrecision(6));
    return String(rounded);
}
/**
 * Modal dialog: adaptive screen font size vs custom WCS text height.
 */
var AcUiTextHeightDialog = /** @class */ (function (_super) {
    __extends(AcUiTextHeightDialog, _super);
    function AcUiTextHeightDialog(options) {
        var _a;
        var _this = _super.call(this, {
            host: options.host,
            title: options.labels.title,
            closeLabel: options.labels.close,
            dialogClassName: 'ml-ui-text-height-dialog',
            theme: options.theme
        }) || this;
        _this.result = null;
        ensureStyles();
        _this.screenPxToWcs = options.screenPxToWcs;
        var groupName = "ml-ui-text-height-".concat(AcUiTextHeightDialog.nextGroupId++);
        var optionsRoot = document.createElement('div');
        optionsRoot.className = 'ml-ui-text-height-options';
        var adaptiveOption = document.createElement('div');
        adaptiveOption.className = 'ml-ui-text-height-option';
        var adaptiveLabel = document.createElement('label');
        adaptiveLabel.className = 'ml-ui-text-height-radio';
        _this.adaptiveRadio = document.createElement('input');
        _this.adaptiveRadio.type = 'radio';
        _this.adaptiveRadio.name = groupName;
        _this.adaptiveRadio.value = 'adaptive';
        adaptiveLabel.append(_this.adaptiveRadio, document.createTextNode(options.labels.adaptive));
        adaptiveOption.appendChild(adaptiveLabel);
        var customOption = document.createElement('div');
        customOption.className = 'ml-ui-text-height-option';
        var customLabel = document.createElement('label');
        customLabel.className = 'ml-ui-text-height-radio';
        _this.customRadio = document.createElement('input');
        _this.customRadio.type = 'radio';
        _this.customRadio.name = groupName;
        _this.customRadio.value = 'custom';
        customLabel.append(_this.customRadio, document.createTextNode(options.labels.custom));
        var customRow = document.createElement('div');
        customRow.className = 'ml-ui-text-height-custom-row';
        _this.customInput = document.createElement('input');
        _this.customInput.type = 'number';
        _this.customInput.min = '0';
        _this.customInput.step = 'any';
        _this.customInput.className = 'ml-ui-text-height-input';
        _this.customInput.placeholder = options.labels.customPlaceholder;
        customRow.appendChild(_this.customInput);
        _this.calcPanel = document.createElement('div');
        _this.calcPanel.className = 'ml-ui-text-height-calc';
        var calcTitle = document.createElement('div');
        calcTitle.className = 'ml-ui-text-height-calc-title';
        calcTitle.textContent = options.labels.fromScreen;
        var calcHint = document.createElement('div');
        calcHint.className = 'ml-ui-text-height-calc-hint';
        calcHint.textContent = options.labels.fromScreenHint;
        var calcRow = document.createElement('div');
        calcRow.className = 'ml-ui-text-height-calc-row';
        _this.screenPxInput = document.createElement('input');
        _this.screenPxInput.type = 'number';
        _this.screenPxInput.min = '1';
        _this.screenPxInput.step = '1';
        _this.screenPxInput.className = 'ml-ui-text-height-input';
        _this.screenPxInput.placeholder = options.labels.screenPxPlaceholder;
        var unit = document.createElement('span');
        unit.className = 'ml-ui-text-height-calc-unit';
        unit.textContent = options.labels.screenUnit;
        _this.convertButton = document.createElement('button');
        _this.convertButton.type = 'button';
        _this.convertButton.className = 'ml-ui-text-height-convert';
        _this.convertButton.textContent = options.labels.convert;
        calcRow.append(_this.screenPxInput, unit, _this.convertButton);
        _this.calcPanel.append(calcTitle, calcHint, calcRow);
        customOption.append(customLabel, customRow, _this.calcPanel);
        optionsRoot.append(adaptiveOption, customOption);
        _this.bodyEl.appendChild(optionsRoot);
        var initialMode = (_a = options.initialMode) !== null && _a !== void 0 ? _a : 'adaptive';
        _this.adaptiveRadio.checked = initialMode === 'adaptive';
        _this.customRadio.checked = initialMode === 'custom';
        if (options.initialTextHeightWcs != null &&
            options.initialTextHeightWcs > 0) {
            _this.customInput.value = formatWcsHeight(options.initialTextHeightWcs);
        }
        if (options.initialFontSizePx != null && options.initialFontSizePx > 0) {
            _this.screenPxInput.value = String(Math.round(options.initialFontSizePx));
        }
        var syncEnabled = function () {
            var custom = _this.customRadio.checked;
            _this.customInput.disabled = !custom;
            _this.screenPxInput.disabled = !custom;
            _this.convertButton.disabled = !custom;
            _this.calcPanel.dataset.disabled = custom ? 'false' : 'true';
        };
        _this.adaptiveRadio.addEventListener('change', syncEnabled);
        _this.customRadio.addEventListener('change', syncEnabled);
        syncEnabled();
        _this.convertButton.addEventListener('click', function () { return _this.applyScreenPx(); });
        _this.screenPxInput.addEventListener('keydown', function (event) {
            if (event.key === 'Enter') {
                event.preventDefault();
                _this.applyScreenPx();
            }
        });
        var cancelBtn = document.createElement('button');
        cancelBtn.type = 'button';
        cancelBtn.className = 'ml-ui-dialog-btn ml-ui-dialog-btn-secondary';
        cancelBtn.textContent = options.labels.cancel;
        cancelBtn.addEventListener('click', function () { return _this.close(); });
        var okBtn = document.createElement('button');
        okBtn.type = 'button';
        okBtn.className = 'ml-ui-dialog-btn ml-ui-dialog-btn-primary';
        okBtn.textContent = options.labels.ok;
        okBtn.addEventListener('click', function () { return _this.confirm(); });
        _this.footerEl.append(cancelBtn, okBtn);
        return _this;
    }
    /**
     * Opens the dialog and resolves with the confirmed values, or `null` if cancelled.
     *
     * @param options - Labels, initial values, and screen↔WCS conversion.
     */
    AcUiTextHeightDialog.open = function (options) {
        var dialog = new AcUiTextHeightDialog(options);
        return dialog.show().then(function () { return dialog.result; });
    };
    AcUiTextHeightDialog.prototype.confirm = function () {
        if (this.adaptiveRadio.checked) {
            this.result = { mode: 'adaptive' };
            this.close();
            return;
        }
        var raw = Number(this.customInput.value);
        if (!(raw > 0) || !Number.isFinite(raw)) {
            this.customInput.focus();
            return;
        }
        this.result = { mode: 'custom', textHeightWcs: raw };
        this.close();
    };
    AcUiTextHeightDialog.prototype.applyScreenPx = function () {
        if (!this.customRadio.checked)
            return;
        var px = Number(this.screenPxInput.value);
        if (!(px > 0) || !Number.isFinite(px)) {
            this.screenPxInput.focus();
            return;
        }
        var wcs = this.screenPxToWcs(px);
        if (!(wcs > 0) || !Number.isFinite(wcs)) {
            this.screenPxInput.focus();
            return;
        }
        this.customInput.value = formatWcsHeight(wcs);
        this.customInput.focus();
        this.customInput.select();
    };
    AcUiTextHeightDialog.nextGroupId = 0;
    return AcUiTextHeightDialog;
}(AcUiDialog));
export { AcUiTextHeightDialog };
//# sourceMappingURL=AcUiTextHeightDialog.js.map