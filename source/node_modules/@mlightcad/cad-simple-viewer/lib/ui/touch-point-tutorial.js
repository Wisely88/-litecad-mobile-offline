/**
 * Portable precise point-pick tutorial dialog (DOM + CSS animation only).
 *
 * Re-exported from `@mlightcad/cad-simple-viewer` for the live viewer and
 * offline HTML viewer (via {@link AcExHtmlSimpleViewerUi}).
 */
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import { AcUiDialog } from './AcUiDialog';
/** DOM id of tutorial-specific styles. */
var STYLE_ID = 'ml-ui-touch-point-tutorial-styles';
/** Loop length for the demo animation (ms). */
var DEMO_CYCLE_MS = 10000;
/** Road intersection center in canvas percentage coordinates. */
var ROAD_X_PCT = 38;
var ROAD_Y_PCT = 42;
/** Road half-width in viewBox units (160×100). */
var ROAD_HALF_W = 4;
/** Ring stroke draw duration (ms). */
var RING_DRAW_MS = 500;
/** Delay after snap box before cross commits green (ms). */
var SNAP_BOX_BEFORE_LIFT_MS = 500;
/** Cross stays green at snap corner before finger returns (ms). */
var CROSS_GREEN_HOLD_MS = 2000;
/** Finger returns to start while the touch zone fades out (ms). */
var RETURN_MS = 900;
/** Move from cross appear until cross top hits snap corner (ms). */
var MOVE_TO_SNAP_TOP_MS = 1800;
/** Move cross center onto snap corner after top touches (ms). */
var MOVE_TO_SNAP_CENTER_MS = 400;
/** Finger appears before the touch ring fill. */
var FINGER_APPEAR_MS = 280;
/** Gray fill fades in after the finger. */
var FILL_APPEAR_MS = 180;
/** Touch zone diameter (px); white circle is centered on the finger point. */
var TOUCH_ZONE_SIZE = 36;
var TOUCH_ZONE_RADIUS = TOUCH_ZONE_SIZE / 2;
/** Gap between cross bottom and touch circle top (px). */
var CROSS_ABOVE_CIRCLE_GAP = 5;
/** Crosshair size (px); CSS `top` anchors the cross center. */
var CROSS_SIZE = 18;
var CROSS_HALF_H = CROSS_SIZE / 2;
/** Cross center offset above the finger touch point (px). */
var CROSS_ABOVE_TOUCH_PX = TOUCH_ZONE_RADIUS + CROSS_ABOVE_CIRCLE_GAP + CROSS_HALF_H;
/**
 * Returns today's date as `YYYY-MM-DD` in local time.
 */
export function acuiLocalIsoDate(date) {
    if (date === void 0) { date = new Date(); }
    var y = date.getFullYear();
    var m = String(date.getMonth() + 1).padStart(2, '0');
    var d = String(date.getDate()).padStart(2, '0');
    return "".concat(y, "-").concat(m, "-").concat(d);
}
/**
 * Whether the tutorial should be shown from layout + preference state.
 */
export function acuiShouldShowTouchPointTutorialFromPrefs(shouldShowOnMobile, prefs) {
    if (!shouldShowOnMobile())
        return false;
    if (prefs.hideForever)
        return false;
    if (prefs.snoozeDate != null && prefs.snoozeDate === acuiLocalIsoDate()) {
        return false;
    }
    return true;
}
/**
 * Modal tutorial explaining long-press precise point picking on mobile.
 */
var AcUiTouchPointTutorial = /** @class */ (function (_super) {
    __extends(AcUiTouchPointTutorial, _super);
    function AcUiTouchPointTutorial(config) {
        var _this = _super.call(this, {
            host: config.host,
            title: config.labels.title,
            theme: config.theme,
            layoutWidth: false,
            showCloseButton: false,
            titleAlign: 'center',
            closeOnBackdrop: false,
            closeOnEscape: false,
            dialogClassName: 'ml-ui-touch-point-tutorial'
        }) || this;
        _this.config = config;
        AcUiTouchPointTutorial.ensureTutorialStyles(config.longPressMs);
        var description = document.createElement('p');
        description.className = 'ml-ui-touch-point-tutorial-desc';
        description.textContent = config.labels.description;
        _this.bodyEl.appendChild(description);
        _this.bodyEl.appendChild(AcUiTouchPointTutorial.createDemo());
        var options = document.createElement('div');
        options.className = 'ml-ui-touch-point-tutorial-options';
        _this.snoozeTodayCheckbox = document.createElement('input');
        _this.snoozeTodayCheckbox.type = 'checkbox';
        _this.snoozeTodayCheckbox.id = 'ml-touch-tutorial-snooze-today';
        _this.snoozeTodayCheckbox.checked = true;
        var snoozeLabel = document.createElement('label');
        snoozeLabel.className = 'ml-ui-touch-point-tutorial-option';
        snoozeLabel.htmlFor = _this.snoozeTodayCheckbox.id;
        snoozeLabel.append(_this.snoozeTodayCheckbox, document.createTextNode(''));
        snoozeLabel.lastChild.textContent = config.labels.snoozeToday;
        _this.hideForeverCheckbox = document.createElement('input');
        _this.hideForeverCheckbox.type = 'checkbox';
        _this.hideForeverCheckbox.id = 'ml-touch-tutorial-hide-forever';
        var hideLabel = document.createElement('label');
        hideLabel.className = 'ml-ui-touch-point-tutorial-option';
        hideLabel.htmlFor = _this.hideForeverCheckbox.id;
        hideLabel.append(_this.hideForeverCheckbox, document.createTextNode(''));
        hideLabel.lastChild.textContent = config.labels.hideForever;
        _this.hideForeverCheckbox.addEventListener('change', function () {
            if (_this.hideForeverCheckbox.checked) {
                _this.snoozeTodayCheckbox.checked = false;
            }
        });
        _this.snoozeTodayCheckbox.addEventListener('change', function () {
            if (_this.snoozeTodayCheckbox.checked) {
                _this.hideForeverCheckbox.checked = false;
            }
        });
        options.append(snoozeLabel, hideLabel);
        _this.bodyEl.appendChild(options);
        var okBtn = document.createElement('button');
        okBtn.type = 'button';
        okBtn.className = 'ml-ui-touch-point-tutorial-ok';
        okBtn.textContent = config.labels.ok;
        okBtn.addEventListener('click', function () { return _this.dismiss(); });
        _this.footerEl.appendChild(okBtn);
        _this.focusAfterOpen(okBtn);
        return _this;
    }
    /**
     * Opens the tutorial when {@link AcUiTouchPointTutorialConfig.shouldShow} is true.
     */
    AcUiTouchPointTutorial.maybeShow = function (config) {
        var _a;
        if (!config.shouldShow())
            return Promise.resolve();
        (_a = AcUiTouchPointTutorial.openInstance) === null || _a === void 0 ? void 0 : _a.close();
        var dialog = new AcUiTouchPointTutorial(config);
        AcUiTouchPointTutorial.openInstance = dialog;
        return dialog.show();
    };
    AcUiTouchPointTutorial.prototype.close = function () {
        if (AcUiTouchPointTutorial.openInstance === this) {
            AcUiTouchPointTutorial.openInstance = null;
        }
        _super.prototype.close.call(this);
    };
    AcUiTouchPointTutorial.prototype.dismiss = function () {
        var prefs = this.config.readPrefs();
        if (this.hideForeverCheckbox.checked) {
            this.config.writePrefs({ hideForever: true, snoozeDate: null });
        }
        else if (this.snoozeTodayCheckbox.checked) {
            this.config.writePrefs({
                hideForever: prefs.hideForever,
                snoozeDate: acuiLocalIsoDate()
            });
        }
        this.close();
    };
    AcUiTouchPointTutorial.createDemo = function () {
        var snap = roadSnapCornerPct();
        var demo = document.createElement('div');
        demo.className = 'ml-touch-tutorial-demo';
        demo.setAttribute('aria-hidden', 'true');
        var canvas = document.createElement('div');
        canvas.className = 'ml-touch-tutorial-canvas';
        canvas.style.setProperty('--ml-touch-snap-x', "".concat(snap.xPct, "%"));
        canvas.style.setProperty('--ml-touch-snap-y', "".concat(snap.yPct, "%"));
        canvas.style.setProperty('--ml-touch-cross-above', "".concat(CROSS_ABOVE_TOUCH_PX, "px"));
        canvas.style.setProperty('--ml-touch-cross-half', "".concat(CROSS_HALF_H, "px"));
        var roads = document.createElement('div');
        roads.className = 'ml-touch-tutorial-roads';
        roads.innerHTML = roadCrossSvg();
        var snapBox = document.createElement('div');
        snapBox.className = 'ml-touch-tutorial-snap-box';
        var touchGroup = document.createElement('div');
        touchGroup.className = 'ml-touch-tutorial-touch-group';
        var touchZone = document.createElement('div');
        touchZone.className = 'ml-touch-tutorial-touch-zone';
        touchZone.innerHTML = touchZoneSvg();
        var finger = document.createElement('div');
        finger.className = 'ml-touch-tutorial-finger';
        finger.innerHTML = fingerIconSvg();
        var cross = document.createElement('div');
        cross.className = 'ml-touch-tutorial-cross';
        cross.innerHTML = snapCrossSvg();
        touchGroup.append(touchZone, finger);
        canvas.append(roads, snapBox, touchGroup, cross);
        demo.appendChild(canvas);
        return demo;
    };
    AcUiTouchPointTutorial.pct = function (ms) {
        return ((ms / DEMO_CYCLE_MS) * 100).toFixed(2);
    };
    AcUiTouchPointTutorial.pctAfter = function (baseMs, deltaMs) {
        return AcUiTouchPointTutorial.pct(baseMs + deltaMs);
    };
    AcUiTouchPointTutorial.ensureTutorialStyles = function (longPressMs) {
        if (document.getElementById(STYLE_ID))
            return;
        var fingerIn = AcUiTouchPointTutorial.pct(FINGER_APPEAR_MS);
        var fillIn = AcUiTouchPointTutorial.pct(FINGER_APPEAR_MS + FILL_APPEAR_MS);
        var ringStart = fillIn;
        var ringEnd = AcUiTouchPointTutorial.pct(FINGER_APPEAR_MS + FILL_APPEAR_MS + RING_DRAW_MS);
        var crossIn = AcUiTouchPointTutorial.pct(longPressMs);
        var snapTopAt = AcUiTouchPointTutorial.pctAfter(longPressMs, MOVE_TO_SNAP_TOP_MS);
        var snapCenterAt = AcUiTouchPointTutorial.pctAfter(longPressMs, MOVE_TO_SNAP_TOP_MS + MOVE_TO_SNAP_CENTER_MS);
        var snapCommitAt = AcUiTouchPointTutorial.pctAfter(longPressMs, MOVE_TO_SNAP_TOP_MS + SNAP_BOX_BEFORE_LIFT_MS);
        var crossHoldEnd = AcUiTouchPointTutorial.pctAfter(longPressMs, MOVE_TO_SNAP_TOP_MS + SNAP_BOX_BEFORE_LIFT_MS + CROSS_GREEN_HOLD_MS);
        var returnStart = crossHoldEnd;
        var returnEnd = AcUiTouchPointTutorial.pctAfter(longPressMs, MOVE_TO_SNAP_TOP_MS + SNAP_BOX_BEFORE_LIFT_MS + CROSS_GREEN_HOLD_MS + RETURN_MS);
        var resetAt = AcUiTouchPointTutorial.pct(DEMO_CYCLE_MS - 400);
        var fillVisible = AcUiTouchPointTutorial.pctAfter(FINGER_APPEAR_MS + FILL_APPEAR_MS, 80);
        var crossVisible = AcUiTouchPointTutorial.pctAfter(longPressMs, 60);
        var snapBoxOn = AcUiTouchPointTutorial.pctAfter(longPressMs, MOVE_TO_SNAP_TOP_MS + 1);
        var crossGreen = AcUiTouchPointTutorial.pctAfter(longPressMs, MOVE_TO_SNAP_TOP_MS + SNAP_BOX_BEFORE_LIFT_MS + 1);
        var style = document.createElement('style');
        style.id = STYLE_ID;
        style.textContent = "\n.ml-ui-touch-point-tutorial {\n  padding: 16px 18px;\n}\n\n.ml-ui-touch-point-tutorial-desc {\n  margin: 0 0 12px;\n  font-size: 12px;\n  line-height: 1.55;\n  color: var(--ml-ui-text-muted, #606266);\n  text-align: center;\n}\n\n.ml-touch-tutorial-demo {\n  margin: 0 auto 14px;\n  max-width: 100%;\n}\n\n.ml-touch-tutorial-canvas {\n  position: relative;\n  width: 100%;\n  aspect-ratio: 16 / 10;\n  background: #0a0a0a;\n  border-radius: 4px;\n  overflow: hidden;\n}\n\n.ml-touch-tutorial-roads {\n  position: absolute;\n  inset: 0;\n  width: 100%;\n  height: 100%;\n  pointer-events: none;\n}\n\n.ml-touch-tutorial-roads svg {\n  display: block;\n  width: 100%;\n  height: 100%;\n}\n\n.ml-touch-tutorial-touch-group {\n  position: absolute;\n  left: 70%;\n  top: 76%;\n  width: 0;\n  height: 0;\n  animation: ml-touch-group ".concat(DEMO_CYCLE_MS, "ms ease-in-out infinite;\n  z-index: 4;\n}\n\n.ml-touch-tutorial-touch-zone {\n  position: absolute;\n  left: 0;\n  top: 0;\n  width: 36px;\n  height: 36px;\n  margin: -18px 0 0 -18px;\n  pointer-events: none;\n  opacity: 0;\n  animation: ml-touch-zone-opacity ").concat(DEMO_CYCLE_MS, "ms linear infinite;\n  z-index: 1;\n}\n\n.ml-touch-tutorial-touch-zone svg {\n  display: block;\n  width: 100%;\n  height: 100%;\n}\n\n.ml-touch-tutorial-touch-fill {\n  opacity: 1;\n}\n\n.ml-touch-tutorial-touch-ring {\n  fill: none;\n  stroke: rgba(255, 255, 255, 0.95);\n  stroke-width: 2;\n  stroke-dasharray: 113.1;\n  stroke-dashoffset: 113.1;\n  animation: ml-touch-ring ").concat(DEMO_CYCLE_MS, "ms linear infinite;\n}\n\n.ml-touch-tutorial-finger {\n  position: absolute;\n  left: 0;\n  top: 0;\n  width: 30px;\n  height: 38px;\n  margin: -6px 0 0 -15px;\n  opacity: 0;\n  animation: ml-touch-finger-opacity ").concat(DEMO_CYCLE_MS, "ms linear infinite;\n  z-index: 2;\n}\n\n.ml-touch-tutorial-finger svg {\n  display: block;\n  width: 100%;\n  height: 100%;\n  filter: drop-shadow(0 2px 4px rgba(0, 0, 0, 0.45));\n}\n\n.ml-touch-tutorial-cross {\n  position: absolute;\n  left: 70%;\n  top: calc(76% - var(--ml-touch-cross-above));\n  width: ").concat(CROSS_SIZE, "px;\n  height: ").concat(CROSS_SIZE, "px;\n  margin: calc(-1 * var(--ml-touch-cross-half)) 0 0 calc(-1 * var(--ml-touch-cross-half));\n  color: var(--ml-ui-accent, #0b84ff);\n  opacity: 0;\n  animation:\n    ml-touch-cross-move ").concat(DEMO_CYCLE_MS, "ms ease-in-out infinite,\n    ml-touch-cross-opacity ").concat(DEMO_CYCLE_MS, "ms linear infinite,\n    ml-touch-cross-color ").concat(DEMO_CYCLE_MS, "ms step-end infinite;\n  z-index: 5;\n}\n\n.ml-touch-tutorial-cross svg {\n  display: block;\n  width: 100%;\n  height: 100%;\n}\n\n.ml-touch-tutorial-snap-box {\n  position: absolute;\n  left: var(--ml-touch-snap-x);\n  top: var(--ml-touch-snap-y);\n  width: 22px;\n  height: 22px;\n  margin: -11px 0 0 -11px;\n  border: 2px solid #0f0;\n  border-radius: 1px;\n  box-sizing: border-box;\n  opacity: 0;\n  pointer-events: none;\n  animation: ml-touch-snap-box ").concat(DEMO_CYCLE_MS, "ms step-end infinite;\n  z-index: 3;\n}\n\n@keyframes ml-touch-group {\n  0%, ").concat(crossIn, "% {\n    left: 70%;\n    top: 76%;\n  }\n  ").concat(snapTopAt, "%, ").concat(crossHoldEnd, "% {\n    left: var(--ml-touch-snap-x);\n    top: calc(var(--ml-touch-snap-y) + var(--ml-touch-cross-above) + var(--ml-touch-cross-half));\n  }\n  ").concat(returnEnd, "%, 100% {\n    left: 70%;\n    top: 76%;\n  }\n}\n\n@keyframes ml-touch-zone-opacity {\n  0%, ").concat(fillIn, "% { opacity: 0; }\n  ").concat(fillVisible, "%, ").concat(returnStart, "% { opacity: 1; }\n  ").concat(returnEnd, "%, 100% { opacity: 0; }\n}\n\n@keyframes ml-touch-finger-opacity {\n  0%, 2% { opacity: 0; }\n  ").concat(fingerIn, "%, 99.99% { opacity: 1; }\n  100% { opacity: 0; }\n}\n\n@keyframes ml-touch-ring {\n  0%, ").concat(ringStart, "% { stroke-dashoffset: 113.1; }\n  ").concat(ringEnd, "%, ").concat(resetAt, "% { stroke-dashoffset: 0; }\n  100% { stroke-dashoffset: 113.1; }\n}\n\n@keyframes ml-touch-cross-move {\n  0%, ").concat(crossIn, "% {\n    left: 70%;\n    top: calc(76% - var(--ml-touch-cross-above));\n  }\n  ").concat(snapTopAt, "%, ").concat(snapCenterAt, "% {\n    left: var(--ml-touch-snap-x);\n    top: calc(var(--ml-touch-snap-y) + var(--ml-touch-cross-half));\n  }\n  ").concat(snapCenterAt, "%, 99.99% {\n    left: var(--ml-touch-snap-x);\n    top: var(--ml-touch-snap-y);\n  }\n  100% {\n    left: 70%;\n    top: calc(76% - var(--ml-touch-cross-above));\n  }\n}\n\n@keyframes ml-touch-cross-opacity {\n  0%, ").concat(crossIn, "% { opacity: 0; }\n  ").concat(crossVisible, "%, ").concat(returnEnd, "% { opacity: 1; }\n  ").concat(resetAt, "%, 100% { opacity: 0; }\n}\n\n@keyframes ml-touch-cross-color {\n  0%, ").concat(snapCommitAt, "% { color: var(--ml-ui-accent, #0b84ff); }\n  ").concat(crossGreen, "%, ").concat(returnEnd, "% { color: #0f0; }\n  100% { color: var(--ml-ui-accent, #0b84ff); }\n}\n\n@keyframes ml-touch-snap-box {\n  0%, ").concat(snapTopAt, "% { opacity: 0; }\n  ").concat(snapBoxOn, "%, ").concat(snapCommitAt, "% { opacity: 1; }\n  ").concat(crossGreen, "%, 100% { opacity: 0; }\n}\n\n.ml-ui-touch-point-tutorial-options {\n  display: flex;\n  flex-direction: column;\n  gap: 8px;\n  margin-bottom: 4px;\n}\n\n.ml-ui-touch-point-tutorial-option {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n  font-size: 12px;\n  color: var(--ml-ui-text, #303133);\n  cursor: pointer;\n  user-select: none;\n}\n\n.ml-ui-touch-point-tutorial-option input[type='checkbox'] {\n  width: 16px;\n  height: 16px;\n  margin: 0;\n  accent-color: var(--ml-ui-accent, #0b84ff);\n}\n\n.ml-ui-touch-point-tutorial .ml-ui-dialog-footer {\n  justify-content: center;\n  margin-top: 12px;\n  padding-top: 12px;\n  border-top: 1px solid var(--ml-ui-border, #ebeef5);\n}\n\n.ml-ui-touch-point-tutorial-ok {\n  border: none;\n  background: transparent;\n  color: var(--ml-ui-accent, #0b84ff);\n  font-size: 14px;\n  font-weight: 500;\n  cursor: pointer;\n  padding: 4px 16px;\n}\n\n.ml-ui-touch-point-tutorial-ok:hover {\n  filter: brightness(1.08);\n}\n").trim();
        document.head.appendChild(style);
    };
    AcUiTouchPointTutorial.openInstance = null;
    return AcUiTouchPointTutorial;
}(AcUiDialog));
export { AcUiTouchPointTutorial };
function roadSnapCornerPct() {
    var ix = (160 * ROAD_X_PCT) / 100;
    var iy = (100 * ROAD_Y_PCT) / 100;
    return {
        xPct: ((ix + ROAD_HALF_W) / 160) * 100,
        yPct: ((iy + ROAD_HALF_W) / 100) * 100
    };
}
function roadCrossSvg() {
    var ix = (160 * ROAD_X_PCT) / 100;
    var iy = (100 * ROAD_Y_PCT) / 100;
    var h = ROAD_HALF_W;
    var xl = ix - h;
    var xr = ix + h;
    var yt = iy - h;
    var yb = iy + h;
    var gray = 'rgba(210, 210, 210, 0.55)';
    var red = 'rgba(230, 70, 70, 0.95)';
    return "<svg viewBox=\"0 0 160 100\" preserveAspectRatio=\"none\" aria-hidden=\"true\">\n    <line x1=\"0\" y1=\"".concat(yt, "\" x2=\"").concat(xl, "\" y2=\"").concat(yt, "\" stroke=\"").concat(gray, "\" stroke-width=\"0.9\"/>\n    <line x1=\"").concat(xr, "\" y1=\"").concat(yt, "\" x2=\"160\" y2=\"").concat(yt, "\" stroke=\"").concat(gray, "\" stroke-width=\"0.9\"/>\n    <line x1=\"0\" y1=\"").concat(yb, "\" x2=\"").concat(xl, "\" y2=\"").concat(yb, "\" stroke=\"").concat(gray, "\" stroke-width=\"0.9\"/>\n    <line x1=\"").concat(xr, "\" y1=\"").concat(yb, "\" x2=\"160\" y2=\"").concat(yb, "\" stroke=\"").concat(gray, "\" stroke-width=\"0.9\"/>\n    <line x1=\"").concat(xl, "\" y1=\"0\" x2=\"").concat(xl, "\" y2=\"").concat(yt, "\" stroke=\"").concat(gray, "\" stroke-width=\"0.9\"/>\n    <line x1=\"").concat(xl, "\" y1=\"").concat(yb, "\" x2=\"").concat(xl, "\" y2=\"100\" stroke=\"").concat(gray, "\" stroke-width=\"0.9\"/>\n    <line x1=\"").concat(xr, "\" y1=\"0\" x2=\"").concat(xr, "\" y2=\"").concat(yt, "\" stroke=\"").concat(gray, "\" stroke-width=\"0.9\"/>\n    <line x1=\"").concat(xr, "\" y1=\"").concat(yb, "\" x2=\"").concat(xr, "\" y2=\"100\" stroke=\"").concat(gray, "\" stroke-width=\"0.9\"/>\n    <line x1=\"0\" y1=\"").concat(iy, "\" x2=\"160\" y2=\"").concat(iy, "\" stroke=\"").concat(red, "\" stroke-width=\"1.1\"/>\n    <line x1=\"").concat(ix, "\" y1=\"0\" x2=\"").concat(ix, "\" y2=\"100\" stroke=\"").concat(red, "\" stroke-width=\"1.1\"/>\n  </svg>");
}
function touchZoneSvg() {
    return "<svg viewBox=\"0 0 36 36\" aria-hidden=\"true\">\n    <circle class=\"ml-touch-tutorial-touch-fill\" cx=\"18\" cy=\"18\" r=\"16\"\n      fill=\"rgba(120, 120, 120, 0.42)\"/>\n    <circle class=\"ml-touch-tutorial-touch-ring\" cx=\"18\" cy=\"18\" r=\"18\"/>\n  </svg>";
}
function fingerIconSvg() {
    return "<svg viewBox=\"0 0 24 32\" aria-hidden=\"true\">\n    <path fill=\"#f5dcc8\" stroke=\"#c9a88a\" stroke-width=\"0.6\"\n      d=\"M12 2c-2.2 0-4 1.6-4 4.2v8.3c0 .6-.5 1-1 1s-1-.4-1-1V8.5C6 5.5 4 4 2 4.5 1 4.7.5 5.5.8 6.4c1.2 3.8 3.2 7.2 5.8 10.1 1.8 2 3.4 3.2 5.4 3.2 3.5 0 6-2.8 6-6.6V6.2C18 3.8 15.4 2 12 2z\"/>\n    <ellipse cx=\"12\" cy=\"28\" rx=\"9\" ry=\"2.5\" fill=\"rgba(0,0,0,0.25)\"/>\n  </svg>";
}
function snapCrossSvg() {
    return "<svg viewBox=\"0 0 16 16\" aria-hidden=\"true\">\n    <path fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\"\n      d=\"M8 2v12M2 8h12\"/>\n  </svg>";
}
//# sourceMappingURL=touch-point-tutorial.js.map