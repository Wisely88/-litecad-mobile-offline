/**
 * Shared ACI color palette DOM builders and styles.
 *
 * @module AcUiAciPaletteUi
 * @packageDocumentation
 */
/** Class that stretches the 24-column ACI grid to the host width. */
export declare const ML_ACI_STACKS_FILL_CLASS = "ml-aci-stacks--fill";
/**
 * Injects shared ACI palette CSS into `document.head` once.
 */
export declare function acuiEnsureAciPaletteStyles(): void;
/**
 * Pins a drop-up ACI popover to the viewport width above `anchor`.
 *
 * Uses `position: absolute` offsets (not `fixed`) so a transformed session
 * panel cannot trap the popover at 440px on pad layouts.
 *
 * @param panel - Color popover element.
 * @param anchor - Swatch wrapper (`position: relative`) that owns the popover.
 * @param gapPx - Space between the popover bottom and the swatch. @default 6
 */
export declare function acuiPinAciPanelToViewportWidth(panel: HTMLElement, anchor: HTMLElement, gapPx?: number): void;
/**
 * Clears inline pin styles set by {@link acuiPinAciPanelToViewportWidth}.
 *
 * @param panel - Color popover element.
 */
export declare function acuiClearAciPanelViewportPin(panel: HTMLElement): void;
/**
 * CSS color string for an ACI index.
 *
 * @param index - AutoCAD Color Index (1–255).
 */
export declare function acuiAciCssColor(index: number): string;
/**
 * Parses manual ACI input (index, BYLAYER, BYBLOCK, or a convertible color string).
 *
 * @param value - User input.
 * @returns ACI index in 0–256, or `null` when invalid.
 */
export declare function acuiParseAciManualInput(value: string): number | null;
/** Options for {@link acuiCreateAciPaletteStacks}. */
export interface AcUiAciPaletteStacksOptions {
    /** Called when the user commits an ACI swatch (1–255). */
    onSelect: (index: number, event: Event) => void;
    /** Initially selected ACI index, if any. */
    selectedIndex?: number | null;
    /**
     * Stretch the 10-row (24-column) palette across the host width. The 1–9
     * and gray rows stay left-aligned with the same cell size.
     */
    fill?: boolean;
}
/** Controller returned by {@link acuiCreateAciPaletteStacks}. */
export interface AcUiAciPaletteStacks {
    /** Root element containing large / small / gray palettes. */
    root: HTMLDivElement;
    /** Highlights the cell for `index`, or clears selection when nullish. */
    setSelected: (index: number | null | undefined) => void;
    /** Toggles {@link ML_ACI_STACKS_FILL_CLASS} on {@link root}. */
    setFill: (fill: boolean) => void;
    /** Tears down loupe listeners and DOM. */
    dispose: () => void;
}
/**
 * Builds the compact large + small + gray ACI palette stacks (no ByLayer row).
 *
 * @param options - Selection callback and optional initial selection.
 */
export declare function acuiCreateAciPaletteStacks(options: AcUiAciPaletteStacksOptions): AcUiAciPaletteStacks;
/** Localized labels for {@link acuiCreateAciIndexPicker}. */
export interface AcUiAciIndexPickerLabels {
    /** Prefix for the index line, e.g. `"Color Index: "`. */
    index: string;
    /** Prefix for the RGB line, e.g. `"RGB: "`. */
    rgb: string;
    /** Label beside the manual input. */
    input: string;
    /** Placeholder for the manual input. */
    inputPlaceholder: string;
    /** ByLayer button text. @default `'ByLayer'` */
    byLayer?: string;
    /** ByBlock button text. @default `'ByBlock'` */
    byBlock?: string;
}
/** Options for {@link acuiCreateAciIndexPicker}. */
export interface AcUiAciIndexPickerOptions {
    labels: AcUiAciIndexPickerLabels;
    /** Initial ACI index (0, 1–255, or 256). */
    initialIndex?: number | null;
    /** Fired whenever the selection changes. */
    onChange?: (index: number) => void;
    /**
     * Show ByLayer / ByBlock actions beside the 1–9 row.
     * @default true
     */
    showByLayerByBlock?: boolean;
}
/** Controller returned by {@link acuiCreateAciIndexPicker}. */
export interface AcUiAciIndexPicker {
    /** Root picker element. */
    root: HTMLDivElement;
    /** Current ACI index, or `null` when none. */
    getIndex: () => number | null;
    /** Programmatically updates the selection and info row. */
    setIndex: (index: number | null) => void;
    /** Removes loupe listeners; does not detach `root` from the DOM. */
    dispose: () => void;
}
/**
 * Builds a full ACI index picker (palettes, optional ByLayer/ByBlock, input,
 * preview).
 *
 * @param options - Labels, initial index, and change callback.
 */
export declare function acuiCreateAciIndexPicker(options: AcUiAciIndexPickerOptions): AcUiAciIndexPicker;
//# sourceMappingURL=AcUiAciPaletteUi.d.ts.map