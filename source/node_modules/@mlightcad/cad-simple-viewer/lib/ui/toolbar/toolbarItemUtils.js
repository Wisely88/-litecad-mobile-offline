var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
/**
 * Resolves how nested children are presented. Defaults to `'menu'`.
 *
 * @param item - Parent toolbar item.
 */
export function acuiResolveToolbarChildrenUi(item) {
    var _a;
    return (_a = item.childrenUi) !== null && _a !== void 0 ? _a : 'menu';
}
/** Returns whether {@link childrenUi} is an icon sub-toolbar (sticky or dismissible). */
export function acuiIsToolbarChildrenStrip(childrenUi) {
    return childrenUi === 'toolbar' || childrenUi === 'sticky-toolbar';
}
/** Returns whether a toolbar config entry is a visual separator. */
export function acuiIsToolbarSeparatorItem(item) {
    return 'type' in item && item.type === 'separator';
}
/** Returns whether a toolbar config entry references a built-in preset button. */
export function acuiIsToolbarPresetRef(item) {
    return 'preset' in item && typeof item.preset === 'string';
}
/**
 * Returns whether {@link children} is supplied by a getter rather than a static array.
 *
 * Dynamic children (for example drawing layouts) must not be snapshotted when
 * cloning or expanding toolbar items.
 *
 * @param item - Toolbar item to inspect.
 */
export function acuiIsDynamicToolbarChildren(item) {
    var _a;
    return (typeof ((_a = Object.getOwnPropertyDescriptor(item, 'children')) === null || _a === void 0 ? void 0 : _a.get) === 'function');
}
/**
 * Attaches a getter that rebuilds submenu children each time they are read.
 *
 * @param item - Parent toolbar item.
 * @param getChildren - Factory invoked on each `children` access.
 * @returns `item` with a live `children` getter.
 */
export function acuiCopyDynamicToolbarChildren(item, getChildren) {
    Object.defineProperty(item, 'children', {
        configurable: true,
        enumerable: true,
        get: getChildren
    });
    return item;
}
/**
 * Copies a live `children` getter from `source` onto `target` when present.
 *
 * @param target - Clone that should keep dynamic children.
 * @param source - Original item that may define a children getter.
 * @returns Whether a getter was copied.
 */
export function acuiPreserveDynamicToolbarChildren(target, source) {
    var descriptor = Object.getOwnPropertyDescriptor(source, 'children');
    if (typeof (descriptor === null || descriptor === void 0 ? void 0 : descriptor.get) !== 'function')
        return false;
    acuiCopyDynamicToolbarChildren(target, descriptor.get);
    return true;
}
/**
 * Creates a toolbar separator entry.
 *
 * @param id - Optional stable id for debugging.
 */
export function acuiCreateToolbarSeparator(id) {
    return { type: 'separator', id: id };
}
/**
 * References a built-in toolbar button by id when composing a custom layout.
 *
 * @param preset - Preset id such as `'pan'` or `'measure'`.
 */
export function acuiToolbarPreset(preset) {
    return { preset: preset };
}
/** Returns whether a resolved toolbar item list includes the given button id. */
export function acuiToolbarItemsIncludeItem(items, itemId) {
    return items.some(function (item) {
        if (acuiIsToolbarSeparatorItem(item))
            return false;
        if (item.id === itemId)
            return true;
        return item.children
            ? acuiToolbarItemsIncludeItem(item.children, itemId)
            : false;
    });
}
/** Registers button items (and nested children) in a preset lookup map. */
export function acuiIndexToolbarItems(items, map) {
    var e_1, _a;
    var _b;
    try {
        for (var items_1 = __values(items), items_1_1 = items_1.next(); !items_1_1.done; items_1_1 = items_1.next()) {
            var item = items_1_1.value;
            if (acuiIsToolbarSeparatorItem(item))
                continue;
            map.set(item.id, item);
            if (!acuiIsDynamicToolbarChildren(item) && ((_b = item.children) === null || _b === void 0 ? void 0 : _b.length)) {
                acuiIndexToolbarItems(item.children, map);
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (items_1_1 && !items_1_1.done && (_a = items_1.return)) _a.call(items_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
}
/**
 * Resolves preset references and nested children in a toolbar config list.
 *
 * @param items - Raw toolbar configuration entries.
 * @param presets - Built-in items keyed by id.
 */
export function acuiExpandToolbarItemConfigs(items, presets) {
    return items.flatMap(function (item) {
        var expanded = acuiExpandToolbarItemConfig(item, presets);
        return expanded ? [expanded] : [];
    });
}
function acuiExpandToolbarItemConfig(item, presets) {
    var _a, _b;
    if (acuiIsToolbarSeparatorItem(item)) {
        return {
            type: 'separator',
            id: (_a = item.id) !== null && _a !== void 0 ? _a : "separator-".concat(Math.random().toString(36).slice(2, 9))
        };
    }
    if (acuiIsToolbarPresetRef(item)) {
        var preset = presets.get(item.preset);
        if (!preset) {
            console.warn("[cad-simple-viewer] Unknown toolbar preset \"".concat(item.preset, "\"."));
            return null;
        }
        return acuiCloneToolbarItem(preset);
    }
    var buttonItem = item;
    if (acuiIsDynamicToolbarChildren(buttonItem)) {
        return acuiCloneToolbarItem(buttonItem);
    }
    if ((_b = buttonItem.children) === null || _b === void 0 ? void 0 : _b.length) {
        return __assign(__assign({}, buttonItem), { children: acuiExpandToolbarItemConfigs(buttonItem.children, presets) });
    }
    return buttonItem;
}
function acuiCloneToolbarItem(item) {
    var _a;
    var clone = __assign(__assign({}, item), { children: item.children });
    if (acuiPreserveDynamicToolbarChildren(clone, item)) {
        return clone;
    }
    if (!((_a = item.children) === null || _a === void 0 ? void 0 : _a.length)) {
        return clone;
    }
    return __assign(__assign({}, clone), { children: item.children.map(function (child) { return acuiCloneToolbarItem(child); }) });
}
//# sourceMappingURL=toolbarItemUtils.js.map