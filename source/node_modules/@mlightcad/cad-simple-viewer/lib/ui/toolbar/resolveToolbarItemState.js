/**
 * Pure helpers for toolbar item visibility, toggle resolution, and disabled state.
 *
 * @module resolveToolbarItemState
 * @packageDocumentation
 */
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { acuiIsDynamicToolbarChildren, acuiIsToolbarSeparatorItem } from './toolbarItemUtils';
/**
 * Returns whether a toolbar item should be shown for the given document open mode.
 *
 * @param item - Toolbar item to test.
 * @param openMode - Current document open mode.
 */
export function acuiIsToolbarItemVisible(item, openMode) {
    if (item.minOpenMode == null)
        return true;
    return openMode >= item.minOpenMode;
}
/**
 * Merges toggle branch fields into a toolbar item for rendering.
 *
 * @param item - Item that may define a `toggle` configuration.
 * @returns Item with effective label, icon, command, and action from the active branch.
 */
export function acuiResolveEffectiveToolbarItem(item) {
    if (acuiIsToolbarSeparatorItem(item))
        return item;
    if (!item.toggle)
        return item;
    var active = item.toggle.getValue();
    var branch = active ? item.toggle.on : item.toggle.off;
    return __assign(__assign(__assign({}, item), branch), { id: item.id, toggle: item.toggle });
}
/**
 * Resolves the active submenu child for a parent toolbar item.
 *
 * @param item - Parent item with `children`.
 * @param activeChildId - Runtime-selected child id, if any.
 */
export function acuiResolveSelectedChildItem(item, activeChildId) {
    var e_1, _a;
    var _b;
    if (!((_b = item.children) === null || _b === void 0 ? void 0 : _b.length))
        return undefined;
    var candidates = [activeChildId, item.selectedChildId].filter(function (id) { return Boolean(id); });
    var _loop_1 = function (id) {
        var match = item.children.find(function (child) { return child.id === id; });
        if (match)
            return { value: match };
    };
    try {
        for (var candidates_1 = __values(candidates), candidates_1_1 = candidates_1.next(); !candidates_1_1.done; candidates_1_1 = candidates_1.next()) {
            var id = candidates_1_1.value;
            var state_1 = _loop_1(id);
            if (typeof state_1 === "object")
                return state_1.value;
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (candidates_1_1 && !candidates_1_1.done && (_a = candidates_1.return)) _a.call(candidates_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
    return item.children[0];
}
/**
 * Applies parent-button display fields for submenu parents.
 *
 * When {@link AcUiToolbarItem.childIcon} is `'selected'`, the parent icon is
 * taken from the active submenu child while the parent label is unchanged.
 *
 * @param item - Parent toolbar item (may include `children`).
 * @param activeChildId - Runtime-selected child id, if any.
 */
export function acuiResolveParentToolbarDisplay(item, activeChildId) {
    var _a, _b;
    var effective = acuiResolveEffectiveToolbarItem(item);
    if (effective.childIcon !== 'selected' || !((_a = effective.children) === null || _a === void 0 ? void 0 : _a.length)) {
        return effective;
    }
    var child = acuiResolveSelectedChildItem(effective, activeChildId);
    if (!child)
        return effective;
    var resolvedChild = acuiResolveEffectiveToolbarItem(child);
    return __assign(__assign({}, effective), { icon: (_b = resolvedChild.icon) !== null && _b !== void 0 ? _b : effective.icon });
}
/**
 * Determines whether a toolbar item needs an open document to be enabled.
 *
 * @param item - Toolbar item to inspect.
 * @returns `requiresDocument` when set, otherwise `true` when `command` is set.
 */
export function acuiItemRequiresDocument(item) {
    if (acuiIsToolbarSeparatorItem(item))
        return false;
    if (item.requiresDocument != null)
        return item.requiresDocument;
    return Boolean(item.command || item.anchorAction);
}
/**
 * Evaluates the disabled state of a toolbar item.
 *
 * @param item - Toolbar item with optional static or dynamic `disabled`.
 */
export function acuiIsToolbarItemDisabled(item) {
    if (item.disabled == null)
        return false;
    return typeof item.disabled === 'function' ? item.disabled() : item.disabled;
}
/**
 * Filters toolbar items (and nested children) by open mode visibility.
 *
 * Parent items with only hidden children are removed unless they have their
 * own command, action, or toggle.
 *
 * @param items - Root toolbar items.
 * @param openMode - Current document open mode.
 */
export function acuiFilterVisibleToolbarItems(items, openMode) {
    return items
        .filter(function (item) {
        return acuiIsToolbarSeparatorItem(item) ||
            acuiIsToolbarItemVisible(item, openMode);
    })
        .map(function (item) {
        var _a;
        if (acuiIsToolbarSeparatorItem(item) ||
            acuiIsDynamicToolbarChildren(item) ||
            !((_a = item.children) === null || _a === void 0 ? void 0 : _a.length)) {
            return item;
        }
        var children = acuiFilterVisibleToolbarItems(item.children, openMode);
        return __assign(__assign({}, item), { children: children });
    })
        .filter(function (item) {
        if (acuiIsToolbarSeparatorItem(item))
            return true;
        return (acuiIsDynamicToolbarChildren(item) ||
            !item.children ||
            item.children.length > 0 ||
            item.command ||
            item.action ||
            item.anchorAction ||
            item.toggle);
    });
}
//# sourceMappingURL=resolveToolbarItemState.js.map