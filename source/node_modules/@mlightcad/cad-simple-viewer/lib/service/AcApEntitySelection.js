var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcApDocManager } from '../app';
import { AcEdPromptSelectionOptions, AcEdPromptStatus } from '../editor';
import { AcApI18n } from '../i18n';
/**
 * Prompts for a selection and returns entity ids when the user confirms.
 *
 * @param promptKey - I18n `sysCmdPrompt` key for the selection prompt.
 * @returns Selected ids, or `undefined` when the prompt is cancelled or empty.
 */
function promptForSelectionIds(promptKey) {
    return __awaiter(this, void 0, void 0, function () {
        var selectionResult;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0: return [4 /*yield*/, AcApDocManager.instance.editor.getSelection(new AcEdPromptSelectionOptions(AcApI18n.sysCmdPrompt(promptKey)))];
                case 1:
                    selectionResult = _a.sent();
                    if (selectionResult.status !== AcEdPromptStatus.OK ||
                        !selectionResult.value ||
                        selectionResult.value.count === 0) {
                        return [2 /*return*/, undefined];
                    }
                    return [2 /*return*/, selectionResult.value.ids];
            }
        });
    });
}
/**
 * Resolves selected entities from preselection or an interactive prompt.
 *
 * @param context - Application context with view, document, and editor access.
 * @param options - Prompt and empty-selection behavior.
 * @returns Resolved entities and ids, or `undefined` when nothing is selected.
 */
export function resolveSelectedEntities(context_1) {
    return __awaiter(this, arguments, void 0, function (context, options) {
        var selectionSet, blockTable, clearOnEmpty, selectionIds, entities, ids, selectionIds_1, selectionIds_1_1, id, entity;
        var e_1, _a;
        var _b;
        if (options === void 0) { options = {}; }
        return __generator(this, function (_c) {
            switch (_c.label) {
                case 0:
                    selectionSet = context.view.selectionSet;
                    blockTable = context.doc.database.tables.blockTable;
                    clearOnEmpty = (_b = options.clearOnEmpty) !== null && _b !== void 0 ? _b : true;
                    if (!(selectionSet.count > 0)) return [3 /*break*/, 1];
                    selectionIds = selectionSet.ids;
                    return [3 /*break*/, 3];
                case 1:
                    if (!options.promptKey) return [3 /*break*/, 3];
                    return [4 /*yield*/, promptForSelectionIds(options.promptKey)];
                case 2:
                    selectionIds = _c.sent();
                    _c.label = 3;
                case 3:
                    if (!selectionIds || selectionIds.length === 0)
                        return [2 /*return*/, undefined];
                    entities = [];
                    ids = [];
                    try {
                        for (selectionIds_1 = __values(selectionIds), selectionIds_1_1 = selectionIds_1.next(); !selectionIds_1_1.done; selectionIds_1_1 = selectionIds_1.next()) {
                            id = selectionIds_1_1.value;
                            entity = blockTable.getEntityById(id);
                            if (entity) {
                                entities.push(entity);
                                ids.push(id);
                            }
                        }
                    }
                    catch (e_1_1) { e_1 = { error: e_1_1 }; }
                    finally {
                        try {
                            if (selectionIds_1_1 && !selectionIds_1_1.done && (_a = selectionIds_1.return)) _a.call(selectionIds_1);
                        }
                        finally { if (e_1) throw e_1.error; }
                    }
                    if (entities.length === 0) {
                        if (clearOnEmpty)
                            selectionSet.clear();
                        return [2 /*return*/, undefined];
                    }
                    return [2 /*return*/, { entities: entities, ids: ids }];
            }
        });
    });
}
/**
 * Resolves entity ids from selection, supporting erase-style preselection handling.
 *
 * Uses the active selection set when non-empty; otherwise prompts with `promptKey`.
 *
 * @param context - Application context with view, document, and editor access.
 * @param promptKey - I18n `sysCmdPrompt` key for the selection prompt.
 * @returns Resolved entity ids, or `undefined` when nothing is selected.
 */
export function resolveSelectedIds(context, promptKey) {
    return __awaiter(this, void 0, void 0, function () {
        var selectionSet, selectionIds;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    selectionSet = context.view.selectionSet;
                    if (selectionSet.count > 0) {
                        return [2 /*return*/, selectionSet.ids];
                    }
                    return [4 /*yield*/, promptForSelectionIds(promptKey)];
                case 1:
                    selectionIds = _a.sent();
                    return [2 /*return*/, selectionIds && selectionIds.length > 0 ? selectionIds : undefined];
            }
        });
    });
}
//# sourceMappingURL=AcApEntitySelection.js.map