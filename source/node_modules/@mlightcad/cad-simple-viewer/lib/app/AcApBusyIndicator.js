var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
import { accmYieldForPaint } from '@mlightcad/data-model';
import { eventBus } from '../editor';
import { AcApProgress } from './AcApProgress';
/**
 * Reference-counted busy overlay for long-running application operations.
 *
 * Used by commands and plugins during blocking work such as export or conversion.
 * Emits {@link eventBus} `busy-indicator` events so host UI can stay in sync.
 */
var AcApBusyIndicator = /** @class */ (function () {
    /**
     * @param host - Element that receives the overlay (typically canvas or viewer shell)
     */
    function AcApBusyIndicator(host) {
        this._refCount = 0;
        this._message = '';
        this._progress = new AcApProgress({ host: host });
        this._progress.hide();
    }
    /**
     * Moves the overlay onto another host (e.g. the focused split pane).
     *
     * @param host - New parent element for the overlay.
     */
    AcApBusyIndicator.prototype.setHost = function (host) {
        this._progress.setHost(host);
    };
    /**
     * Shows the busy overlay, optionally with a message.
     *
     * Nested calls increment an internal reference count; the overlay stays visible
     * until the outermost matching {@link hide} runs.
     */
    AcApBusyIndicator.prototype.show = function (message) {
        if (message !== undefined) {
            this._message = message;
        }
        else if (this._refCount === 0) {
            this._message = '';
        }
        this._refCount++;
        if (this._refCount === 1) {
            this._progress.setMessage(this._message);
            this._progress.show();
            this.emitEvent(true);
        }
        else if (message !== undefined) {
            this._progress.setMessage(message);
            this.emitEvent(true);
        }
    };
    /**
     * Hides the busy overlay started by {@link show}.
     *
     * No-op when the reference count is already zero.
     */
    AcApBusyIndicator.prototype.hide = function () {
        if (this._refCount <= 0) {
            return;
        }
        this._refCount--;
        if (this._refCount === 0) {
            this._message = '';
            this._progress.hide();
            this.emitEvent(false);
        }
    };
    /**
     * Updates the message on the active busy overlay.
     */
    AcApBusyIndicator.prototype.setMessage = function (message) {
        this._message = message;
        if (this._refCount > 0) {
            this._progress.setMessage(message);
            this.emitEvent(true);
        }
    };
    /**
     * Runs {@link work} while the busy overlay is visible.
     *
     * Yields to the browser after {@link show} so the overlay can paint before
     * synchronous work blocks the main thread.
     */
    AcApBusyIndicator.prototype.withBusyIndicator = function (work, message) {
        return __awaiter(this, void 0, void 0, function () {
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        this.show(message);
                        _a.label = 1;
                    case 1:
                        _a.trys.push([1, , 4, 5]);
                        return [4 /*yield*/, accmYieldForPaint()];
                    case 2:
                        _a.sent();
                        return [4 /*yield*/, work()];
                    case 3: return [2 /*return*/, _a.sent()];
                    case 4:
                        this.hide();
                        return [7 /*endfinally*/];
                    case 5: return [2 /*return*/];
                }
            });
        });
    };
    AcApBusyIndicator.prototype.emitEvent = function (visible) {
        eventBus.emit('busy-indicator', {
            visible: visible,
            message: visible ? this._message || undefined : undefined
        });
    };
    return AcApBusyIndicator;
}());
export { AcApBusyIndicator };
//# sourceMappingURL=AcApBusyIndicator.js.map