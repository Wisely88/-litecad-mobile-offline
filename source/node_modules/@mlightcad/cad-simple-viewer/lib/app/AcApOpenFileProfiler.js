var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcCmPerformanceCollector, AcDbRenderingCache, AcDbSystemVariables, AcDbSysVarManager, log } from '@mlightcad/data-model';
import { getLastFontLoadStats } from './AcApFontLoader';
/**
 * Session open-file profiler.
 *
 * Always records PARSE/ENTITY (and other `openProgress` sub-stages),
 * wall-clock read vs scene convert time, and {@link AcDbRenderingCache}
 * hit/miss counters for the last open. Console output remains gated by the
 * {@link AcDbSystemVariables.OPENPROF} system variable.
 *
 * Stage durations use **active-stage wall time**: from the first event of a
 * sub-stage until the first event of a different sub-stage.
 *
 * Drawing fonts are loaded on demand during text draw, not during `db.read`.
 * Explicit {@link AcApFontLoader} loads still contribute via
 * {@link getLastFontLoadStats} in the console report.
 */
var AcApOpenFileProfiler = /** @class */ (function () {
    function AcApOpenFileProfiler() {
        this._active = false;
        this._printToConsole = false;
        this._t0 = 0;
        this._readEndMs = 0;
        this._stages = new Map();
        this._activeStageStartedAt = 0;
        this._finishScheduled = false;
    }
    /**
     * Returns the most recent completed open-file profile, if any.
     */
    AcApOpenFileProfiler.getLastSnapshot = function () {
        return AcApOpenFileProfiler._lastSnapshot;
    };
    /**
     * Returns whether OPENPROF console logging is enabled for `database`.
     */
    AcApOpenFileProfiler.isEnabled = function (database) {
        try {
            return (AcDbSysVarManager.instance().getVar(AcDbSystemVariables.OPENPROF, database) === true);
        }
        catch (_a) {
            return false;
        }
    };
    /**
     * Starts a profiling session for the next document open.
     *
     * Call from document-open start (before `db.read` / URI open). Always
     * collects a snapshot; console printing follows OPENPROF.
     *
     * @param database - Database that will emit `openProgress` for this open.
     */
    AcApOpenFileProfiler.prototype.begin = function (database) {
        var _this = this;
        this.cancel();
        this._active = true;
        this._printToConsole = AcApOpenFileProfiler.isEnabled(database);
        this._finishScheduled = false;
        this._t0 = performance.now();
        this._readEndMs = 0;
        this._stages.clear();
        this._activeStage = undefined;
        this._activeStageStartedAt = 0;
        this._database = database;
        AcDbRenderingCache.resetProfile();
        AcDbRenderingCache.profiling = true;
        this._progressListener = function (args) {
            _this.onProgress(args);
        };
        database.events.openProgress.addEventListener(this._progressListener);
    };
    /**
     * Marks the end of `db.read` / document open await and schedules the report
     * once the view has finished converting entities.
     *
     * @param view - Active 2d view whose entity convert queue is drained.
     */
    AcApOpenFileProfiler.prototype.markReadCompleteAndScheduleReport = function (view) {
        if (!this._active) {
            return;
        }
        this._readEndMs = performance.now();
        this.scheduleReport(view);
    };
    /**
     * Aborts an in-flight session without publishing (e.g. document replaced).
     */
    AcApOpenFileProfiler.prototype.cancel = function () {
        this.detachProgressListener();
        if (this._active) {
            AcDbRenderingCache.profiling = false;
        }
        this._active = false;
        this._printToConsole = false;
        this._finishScheduled = false;
        this._database = undefined;
        this._activeStage = undefined;
        this._stages.clear();
    };
    AcApOpenFileProfiler.prototype.onProgress = function (args) {
        var _a, _b;
        var name = String((_b = (_a = args.subStage) !== null && _a !== void 0 ? _a : args.stage) !== null && _b !== void 0 ? _b : 'UNKNOWN');
        var now = performance.now();
        if (this._activeStage == null) {
            this._activeStage = name;
            this._activeStageStartedAt = now;
            return;
        }
        if (name !== this._activeStage) {
            this.closeActiveStage(now);
            this._activeStage = name;
            this._activeStageStartedAt = now;
        }
    };
    AcApOpenFileProfiler.prototype.closeActiveStage = function (now) {
        if (this._activeStage == null) {
            return;
        }
        var stats = this._stages.get(this._activeStage);
        if (!stats) {
            stats = { durationMs: 0 };
            this._stages.set(this._activeStage, stats);
        }
        stats.durationMs += Math.max(0, now - this._activeStageStartedAt);
        this._activeStage = undefined;
    };
    AcApOpenFileProfiler.prototype.scheduleReport = function (view) {
        var _this = this;
        if (!this._active || this._finishScheduled) {
            return;
        }
        this._finishScheduled = true;
        void this.waitForIdle(view)
            .then(function () {
            if (!_this._active) {
                return;
            }
            _this.publishReport(performance.now(), view);
            _this.cancel();
        })
            .catch(function (error) {
            log.warn('OPENPROF: failed to finish open profile', error);
            _this.cancel();
        });
    };
    AcApOpenFileProfiler.prototype.waitForIdle = function (view) {
        return __awaiter(this, void 0, void 0, function () {
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0: 
                    // Allow any synchronously queued addEntity calls to bump the counter.
                    return [4 /*yield*/, Promise.resolve()];
                    case 1:
                        // Allow any synchronously queued addEntity calls to bump the counter.
                        _a.sent();
                        if (!view.isProcessingEntities) {
                            return [2 /*return*/];
                        }
                        return [4 /*yield*/, new Promise(function (resolve) {
                                var poll = function () {
                                    if (!view.isProcessingEntities) {
                                        resolve();
                                        return;
                                    }
                                    setTimeout(poll, 16);
                                };
                                poll();
                            })];
                    case 2:
                        _a.sent();
                        return [2 /*return*/];
                }
            });
        });
    };
    AcApOpenFileProfiler.prototype.publishReport = function (convertEndMs, view) {
        var e_1, _a, e_2, _b;
        var _c, _d, _e, _f, _g;
        // Close the last openProgress stage when read finished (usually END/ENTITY).
        this.closeActiveStage(this._readEndMs > 0 ? this._readEndMs : convertEndMs);
        var readMs = Math.max(0, this._readEndMs - this._t0);
        var convertMs = Math.max(0, convertEndMs - this._readEndMs);
        var totalMs = Math.max(0, convertEndMs - this._t0);
        var parseMs = (_d = (_c = this._stages.get('PARSE')) === null || _c === void 0 ? void 0 : _c.durationMs) !== null && _d !== void 0 ? _d : 0;
        var entityMs = (_f = (_e = this._stages.get('ENTITY')) === null || _e === void 0 ? void 0 : _e.durationMs) !== null && _f !== void 0 ? _f : 0;
        var cache = __assign({}, AcDbRenderingCache.profileStats);
        var tl = cache.topLevel;
        var progressiveStats = view.progressiveOpenStats;
        var progressiveEnabled = view.progressiveRendering;
        var slowBlocks = __spreadArray([], __read(cache.blockMisses), false).sort(function (a, b) { return b.buildMs + b.compactMs - (a.buildMs + a.compactMs); })
            .slice(0, 10)
            .map(function (block) { return ({
            blockName: block.blockName,
            buildMs: block.buildMs,
            compactMs: block.compactMs
        }); });
        var stages = __spreadArray([], __read(this._stages.entries()), false).filter(function (_a) {
            var _b = __read(_a, 2), stats = _b[1];
            return stats.durationMs > 0;
        })
            .sort(function (a, b) { return b[1].durationMs - a[1].durationMs; })
            .map(function (_a) {
            var _b = __read(_a, 2), name = _b[0], stats = _b[1];
            return ({ name: name, durationMs: stats.durationMs });
        });
        var snapshot = {
            collectedAt: Date.now(),
            fileName: (_g = this._database) === null || _g === void 0 ? void 0 : _g.dwgname,
            totalMs: totalMs,
            readMs: readMs,
            convertMs: convertMs,
            parseMs: parseMs,
            entityMs: entityMs,
            stages: stages,
            cache: {
                hits: cache.hits,
                misses: cache.misses,
                hitMs: cache.hitMs,
                cloneMs: cache.cloneMs,
                missBuildMs: cache.missBuildMs,
                missCompactMs: cache.missCompactMs,
                setCloneMs: cache.setCloneMs,
                applyMs: cache.applyMs,
                topLevel: __assign({}, tl)
            },
            slowBlocks: slowBlocks,
            progressive: {
                enabled: progressiveEnabled,
                paintCount: progressiveStats.paintCount,
                yieldCount: progressiveStats.yieldCount
            }
        };
        AcApOpenFileProfiler._lastSnapshot = snapshot;
        if (!this._printToConsole) {
            return;
        }
        var pct = function (part, total) {
            return total <= 0 ? '0%' : "".concat(((part / total) * 100).toFixed(1), "%");
        };
        var lines = [
            '',
            '========== OPENPROF (open-file profile) ==========',
            'Note: progress-bar % weights (PARSE~12%, ENTITY~98%) are not wall-time shares.',
            'Stage ms = active-stage wall time (first event of stage → first event of next stage).',
            "progressive:          ".concat(progressiveEnabled ? 'on' : 'off', " (mid-open paints=").concat(progressiveStats.paintCount, ", yields=").concat(progressiveStats.yieldCount, ")"),
            "wall clock total:     ".concat(totalMs.toFixed(0), " ms"),
            "  db.read:            ".concat(readMs.toFixed(0), " ms  (").concat(pct(readMs, totalMs), ")"),
            "    PARSE:            ".concat(parseMs.toFixed(0), " ms  (").concat(pct(parseMs, readMs), " of read)"),
            "    ENTITY flush:     ".concat(entityMs.toFixed(0), " ms  (").concat(pct(entityMs, readMs), " of read)"),
            "  scene convert:      ".concat(convertMs.toFixed(0), " ms  (").concat(pct(convertMs, totalMs), ")")
        ];
        var fontLoad = getLastFontLoadStats();
        if (fontLoad) {
            lines.push("  explicit font load: critical=".concat(fontLoad.criticalLoadMs.toFixed(0), "ms meta=").concat(fontLoad.metaMs.toFixed(0), "ms") +
                " deferredMesh=".concat(fontLoad.deferred ? 'yes' : 'no') +
                " (critical=[".concat(fontLoad.criticalFonts.join(','), "]") +
                " deferred=[".concat(fontLoad.deferredFonts.join(','), "])"));
        }
        lines.push('', '--- AcDbRenderingCache (top-level INSERT draws) ---', "hits:   ".concat(tl.hits, ", hit path ").concat(tl.hitMs.toFixed(0), " ms (clone ").concat(tl.cloneMs.toFixed(0), " ms)"), "misses: ".concat(tl.misses, ", build ").concat(tl.missBuildMs.toFixed(0), " ms (incl. nested), compact ").concat(tl.missCompactMs.toFixed(0), " ms, set/clone ").concat(tl.setCloneMs.toFixed(0), " ms"), "applyMatrix+attribs: ".concat(tl.applyMs.toFixed(0), " ms"), '', "--- AcDbRenderingCache (all depths: hits=".concat(cache.hits, ", misses=").concat(cache.misses, ") ---"), "hit ".concat(cache.hitMs.toFixed(0), " ms (clone ").concat(cache.cloneMs.toFixed(0), "), build ").concat(cache.missBuildMs.toFixed(0), ", compact ").concat(cache.missCompactMs.toFixed(0), ", set ").concat(cache.setCloneMs.toFixed(0), ", apply ").concat(cache.applyMs.toFixed(0)));
        if (slowBlocks.length > 0) {
            lines.push('', '--- Slowest block template misses (build+compact) ---');
            try {
                for (var slowBlocks_1 = __values(slowBlocks), slowBlocks_1_1 = slowBlocks_1.next(); !slowBlocks_1_1.done; slowBlocks_1_1 = slowBlocks_1.next()) {
                    var block = slowBlocks_1_1.value;
                    var name_1 = block.blockName.length > 40
                        ? "".concat(block.blockName.slice(0, 37), "...")
                        : block.blockName;
                    lines.push("".concat(name_1.padEnd(40), " build=").concat(block.buildMs.toFixed(0).padStart(7), "ms  compact=").concat(block.compactMs.toFixed(0).padStart(7), "ms  total=").concat((block.buildMs + block.compactMs).toFixed(0).padStart(7), "ms"));
                }
            }
            catch (e_1_1) { e_1 = { error: e_1_1 }; }
            finally {
                try {
                    if (slowBlocks_1_1 && !slowBlocks_1_1.done && (_a = slowBlocks_1.return)) _a.call(slowBlocks_1);
                }
                finally { if (e_1) throw e_1.error; }
            }
        }
        var otherStages = stages.filter(function (stage) { return !['PARSE', 'FONT', 'ENTITY', 'START', 'END'].includes(stage.name); });
        if (otherStages.length > 0) {
            lines.push('', '--- Other openProgress sub-stages ---');
            try {
                for (var otherStages_1 = __values(otherStages), otherStages_1_1 = otherStages_1.next(); !otherStages_1_1.done; otherStages_1_1 = otherStages_1.next()) {
                    var stage = otherStages_1_1.value;
                    lines.push("  ".concat(stage.name, ": ").concat(stage.durationMs.toFixed(0), " ms"));
                }
            }
            catch (e_2_1) { e_2 = { error: e_2_1 }; }
            finally {
                try {
                    if (otherStages_1_1 && !otherStages_1_1.done && (_b = otherStages_1.return)) _b.call(otherStages_1);
                }
                finally { if (e_2) throw e_2.error; }
            }
        }
        lines.push('==================================================', '');
        console.log(lines.join('\n'));
        // Classic converter also fills AcCmPerformanceCollector ("Load Database").
        try {
            var collector = AcCmPerformanceCollector.getInstance();
            if (collector.getAll().length > 0) {
                console.log('--- AcCmPerformanceCollector ---');
                collector.printAll();
            }
        }
        catch (_h) {
            // optional
        }
    };
    AcApOpenFileProfiler.prototype.detachProgressListener = function () {
        if (this._progressListener && this._database) {
            this._database.events.openProgress.removeEventListener(this._progressListener);
        }
        this._progressListener = undefined;
    };
    AcApOpenFileProfiler._lastSnapshot = null;
    return AcApOpenFileProfiler;
}());
export { AcApOpenFileProfiler };
//# sourceMappingURL=AcApOpenFileProfiler.js.map