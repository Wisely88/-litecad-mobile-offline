var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
import { MTEXT_RENDERER_WORKER_FILE } from './AcApWorkerAssets';
/** Default worker script URLs used when `webworkerFileUrls` is omitted. */
export var DEFAULT_WEBWORKER_FILE_URLS = {
    mtextRender: "./assets/".concat(MTEXT_RENDERER_WORKER_FILE)
};
/**
 * Resolves configured worker URLs to strings for readiness probes.
 *
 * Always includes the MTEXT worker. Includes `dwgParser` only when the host
 * provides one (DWG converters are optional / host-registered).
 */
export function resolveWebworkerFileUrls(webworkerFileUrls) {
    var _a;
    var urls = [
        (_a = webworkerFileUrls === null || webworkerFileUrls === void 0 ? void 0 : webworkerFileUrls.mtextRender) !== null && _a !== void 0 ? _a : DEFAULT_WEBWORKER_FILE_URLS.mtextRender
    ];
    if (webworkerFileUrls === null || webworkerFileUrls === void 0 ? void 0 : webworkerFileUrls.dwgParser) {
        urls.push(webworkerFileUrls.dwgParser);
    }
    return urls.map(function (url) { return String(url); });
}
var cachedReadinessKey;
var cachedReadinessResult;
/** Clears the module-level success cache (e.g. when the doc manager is destroyed). */
export function resetWebworkerReadinessCache() {
    cachedReadinessKey = undefined;
    cachedReadinessResult = undefined;
}
function isWorkerUrlReachable(url) {
    return __awaiter(this, void 0, void 0, function () {
        var response, fallback, _a;
        return __generator(this, function (_b) {
            switch (_b.label) {
                case 0:
                    _b.trys.push([0, 4, , 5]);
                    return [4 /*yield*/, fetch(url, { method: 'HEAD' })];
                case 1:
                    response = _b.sent();
                    if (response.ok) {
                        return [2 /*return*/, true];
                    }
                    if (!(response.status === 405)) return [3 /*break*/, 3];
                    return [4 /*yield*/, fetch(url, { headers: { Range: 'bytes=0-0' } })];
                case 2:
                    fallback = _b.sent();
                    return [2 /*return*/, fallback.ok || fallback.status === 206];
                case 3: return [2 /*return*/, false];
                case 4:
                    _a = _b.sent();
                    return [2 /*return*/, false];
                case 5: return [2 /*return*/];
            }
        });
    });
}
/**
 * Returns true when all configured worker scripts respond successfully.
 *
 * Uses HEAD requests to avoid downloading large worker bundles. Successful
 * results are cached for the current page lifecycle; failures are not cached
 * so transient network errors can be retried.
 */
export function checkWebworkerReadiness(webworkerFileUrls) {
    return __awaiter(this, void 0, void 0, function () {
        var urls, key, results, ready, _a;
        return __generator(this, function (_b) {
            switch (_b.label) {
                case 0:
                    urls = resolveWebworkerFileUrls(webworkerFileUrls);
                    key = urls.join('|');
                    if (cachedReadinessKey === key && cachedReadinessResult === true) {
                        return [2 /*return*/, true];
                    }
                    _b.label = 1;
                case 1:
                    _b.trys.push([1, 3, , 4]);
                    return [4 /*yield*/, Promise.all(urls.map(isWorkerUrlReachable))];
                case 2:
                    results = _b.sent();
                    ready = results.every(Boolean);
                    if (ready) {
                        cachedReadinessKey = key;
                        cachedReadinessResult = true;
                    }
                    return [2 /*return*/, ready];
                case 3:
                    _a = _b.sent();
                    return [2 /*return*/, false];
                case 4: return [2 /*return*/];
            }
        });
    });
}
//# sourceMappingURL=AcApWebworkerReadiness.js.map