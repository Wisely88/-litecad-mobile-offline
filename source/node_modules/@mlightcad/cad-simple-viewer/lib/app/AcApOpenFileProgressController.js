var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
import { accmYieldForPaint } from '@mlightcad/data-model';
import { eventBus } from '../editor';
import { AcApI18n } from '../i18n';
import { AcApProgress } from './AcApProgress';
import { isOpenFileProgressComplete } from './openFileProgress';
/**
 * Manages the open-file progress overlay and normalized progress events.
 *
 * Listens to database open progress callbacks (wired by {@link AcApDocManager}),
 * normalizes monotonic percentages, updates the canvas overlay, and emits
 * `open-file-progress` on the global event bus.
 *
 * Overlay DOM updates are deduplicated: `show()` runs once per open, and
 * `setMessage` only when the localized stage text changes. Progress events
 * still emit on every callback so listeners see fine-grained percentages.
 *
 * When progressive scene convert is still draining after CONVERSION `END`,
 * the overlay stays up (see-through) until {@link setSceneBusyGate} reports
 * idle so geometry can appear under the spinner. The gate typically tracks
 * entity convert only — deferred glyph jobs may continue after the overlay
 * hides.
 */
var AcApOpenFileProgressController = /** @class */ (function () {
    /**
     * @param host - Canvas container that receives the progress overlay
     */
    function AcApOpenFileProgressController(host) {
        this._peak = 0;
        this._overlayVisible = false;
        this._lastMessage = '';
        this._seeThrough = false;
        this._progress = new AcApProgress({
            host: host,
            overlayColor: AcApOpenFileProgressController.OVERLAY_DEFAULT
        });
        this._progress.hide();
    }
    /**
     * Moves the overlay onto another host (e.g. the canvas being opened).
     *
     * @param host - New parent element for the overlay.
     */
    AcApOpenFileProgressController.prototype.setHost = function (host) {
        this._progress.setHost(host);
    };
    /**
     * When true, uses a lighter overlay so progressive geometry is visible
     * under the spinner during open.
     */
    AcApOpenFileProgressController.prototype.setSeeThroughOverlay = function (enabled) {
        this._seeThrough = enabled;
        this._progress.setOverlayColor(enabled
            ? AcApOpenFileProgressController.OVERLAY_SEE_THROUGH
            : AcApOpenFileProgressController.OVERLAY_DEFAULT);
    };
    /**
     * Gate that returns true while the view still has entities to convert.
     * Used to keep the overlay until scene convert finishes (not deferred text).
     */
    AcApOpenFileProgressController.prototype.setSceneBusyGate = function (gate) {
        this._sceneBusyGate = gate;
    };
    /**
     * Called after the overlay is hidden (open finished or cancelled).
     *
     * @param callback - Handler, or `undefined` to clear.
     */
    AcApOpenFileProgressController.prototype.setOnHidden = function (callback) {
        this._onHidden = callback;
    };
    /**
     * Resets tracked progress for a new open operation.
     */
    AcApOpenFileProgressController.prototype.reset = function () {
        this.clearHoldPoll();
        this._peak = 0;
        this._stage = undefined;
        this._overlayVisible = false;
        this._lastMessage = '';
    };
    /**
     * Shows the open-file overlay immediately and yields so it can paint before
     * main-thread DXF parse work blocks the UI (native converter path).
     */
    AcApOpenFileProgressController.prototype.beginOpen = function (database) {
        return __awaiter(this, void 0, void 0, function () {
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        this.handle({
                            database: database,
                            percentage: 0,
                            stage: 'CONVERSION',
                            subStage: 'START',
                            subStageStatus: 'START'
                        });
                        return [4 /*yield*/, accmYieldForPaint()];
                    case 1:
                        _a.sent();
                        return [2 /*return*/];
                }
            });
        });
    };
    /**
     * Normalizes progress, emits `open-file-progress`, and updates the overlay.
     *
     * @returns Normalized progress payload (monotonic percentage)
     */
    AcApOpenFileProgressController.prototype.handle = function (data) {
        var progress = this.normalize(data);
        eventBus.emit('open-file-progress', progress);
        this.updateOverlay(progress);
        return progress;
    };
    /**
     * Returns monotonic open-file progress for UI display.
     *
     * Entity conversion reports 0–100% within the ENTITY sub-stage while the
     * pipeline accumulator is still ~33%; sub-stage END callbacks can therefore
     * briefly report a lower percentage after IN-PROGRESS already reached 100%.
     */
    AcApOpenFileProgressController.prototype.normalize = function (data) {
        var stage = data.stage;
        if (stage !== this._stage) {
            if (this._stage === 'FETCH_FILE' && stage === 'CONVERSION') {
                this._peak = 0;
            }
            this._stage = stage;
        }
        this._peak = Math.max(this._peak, data.percentage);
        return __assign(__assign({}, data), { percentage: this._peak });
    };
    AcApOpenFileProgressController.prototype.resolveMessage = function (data) {
        if (data.stage === 'CONVERSION') {
            if (data.subStage) {
                var key = 'main.progress.' + data.subStage.replace(/_/g, '').toLowerCase();
                return AcApI18n.t(key);
            }
            return undefined;
        }
        if (data.stage === 'FETCH_FILE') {
            return AcApI18n.t('main.message.fetchingDrawingFile');
        }
        return undefined;
    };
    AcApOpenFileProgressController.prototype.updateOverlay = function (data) {
        var _a;
        if (isOpenFileProgressComplete(data)) {
            if ((_a = this._sceneBusyGate) === null || _a === void 0 ? void 0 : _a.call(this)) {
                this.holdUntilSceneIdle();
                return;
            }
            this.hideAndReset();
            return;
        }
        this.clearHoldPoll();
        if (!this._overlayVisible) {
            this._progress.show();
            this._overlayVisible = true;
            if (this._seeThrough) {
                this._progress.setOverlayColor(AcApOpenFileProgressController.OVERLAY_SEE_THROUGH);
            }
        }
        var message = this.resolveMessage(data);
        if (message != null && message !== this._lastMessage) {
            this._progress.setMessage(message);
            this._lastMessage = message;
        }
    };
    AcApOpenFileProgressController.prototype.holdUntilSceneIdle = function () {
        var _this = this;
        if (!this._overlayVisible) {
            this._progress.show();
            this._overlayVisible = true;
        }
        this._progress.setOverlayColor(AcApOpenFileProgressController.OVERLAY_SEE_THROUGH);
        var message = AcApI18n.t('main.progress.rendering');
        if (message !== this._lastMessage) {
            this._progress.setMessage(message);
            this._lastMessage = message;
        }
        if (this._holdPollId != null) {
            return;
        }
        var poll = function () {
            var _a;
            if ((_a = _this._sceneBusyGate) === null || _a === void 0 ? void 0 : _a.call(_this)) {
                _this._holdPollId = setTimeout(poll, 50);
                return;
            }
            _this._holdPollId = undefined;
            _this.hideAndReset();
        };
        this._holdPollId = setTimeout(poll, 50);
    };
    AcApOpenFileProgressController.prototype.hideAndReset = function () {
        var _a;
        this.clearHoldPoll();
        this._progress.hide();
        this.reset();
        (_a = this._onHidden) === null || _a === void 0 ? void 0 : _a.call(this);
    };
    AcApOpenFileProgressController.prototype.clearHoldPoll = function () {
        if (this._holdPollId != null) {
            clearTimeout(this._holdPollId);
            this._holdPollId = undefined;
        }
    };
    AcApOpenFileProgressController.OVERLAY_DEFAULT = 'rgba(0,0,0,0.45)';
    AcApOpenFileProgressController.OVERLAY_SEE_THROUGH = 'rgba(0,0,0,0.16)';
    return AcApOpenFileProgressController;
}());
export { AcApOpenFileProgressController };
//# sourceMappingURL=AcApOpenFileProgressController.js.map