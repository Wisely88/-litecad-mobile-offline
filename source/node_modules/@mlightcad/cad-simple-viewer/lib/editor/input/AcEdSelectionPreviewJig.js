var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import { AcEdBatchedPreview } from './AcEdBatchPreview';
import { AcEdPreviewJig } from './AcEdPreviewJig';
/**
 * Static preview jig for selected entities.
 *
 * Keeps cloned source entities visible while prompts that do not modify
 * geometry are active (for example ROTATE base-point or reference-point input).
 */
var AcEdSelectionStaticPreviewJig = /** @class */ (function (_super) {
    __extends(AcEdSelectionStaticPreviewJig, _super);
    /**
     * Creates a static transient preview for the given source entities.
     *
     * @param view - Active editor view that renders transient entities.
     * @param sourceEntities - Original entities cloned for preview display.
     */
    function AcEdSelectionStaticPreviewJig(view, sourceEntities) {
        var _this = _super.call(this, view) || this;
        _this._transientsPublished = false;
        _this._view = view;
        _this.batchPreview = new AcEdBatchedPreview(view, sourceEntities.map(function (entity) { return entity.objectId; }));
        _this.previewEntities = _this.batchPreview.useBatchPreview
            ? []
            : sourceEntities
                .map(function (entity) { return entity.clone(); })
                .filter(function (entity) { return !!entity; });
        return _this;
    }
    /**
     * Accepts prompt updates without changing geometry because this preview is static.
     *
     * @param _value - Prompt value supplied by the editor, ignored by this jig.
     */
    AcEdSelectionStaticPreviewJig.prototype.update = function (_value) {
        // Static preview only.
    };
    /**
     * Adds the cloned entities to the view as transient preview graphics.
     */
    AcEdSelectionStaticPreviewJig.prototype.render = function () {
        if (this.batchPreview.useBatchPreview)
            return;
        if (this.previewEntities.length === 0)
            return;
        if (this._transientsPublished)
            return;
        this._view.addTransientEntity(this.previewEntities);
        this._transientsPublished = true;
    };
    /**
     * Removes every transient preview entity from the view.
     */
    AcEdSelectionStaticPreviewJig.prototype.end = function () {
        var _this = this;
        this.batchPreview.dispose(this._view);
        this.previewEntities.forEach(function (entity) {
            return _this._view.removeTransientEntity(entity.objectId);
        });
    };
    return AcEdSelectionStaticPreviewJig;
}(AcEdPreviewJig));
export { AcEdSelectionStaticPreviewJig };
/**
 * Transform preview jig for a set of selected entities.
 *
 * Reuses GPU-resident batched geometry when possible. The legacy transient
 * clone path publishes once and then updates transforms only.
 */
var AcEdSelectionTransformPreviewJig = /** @class */ (function (_super) {
    __extends(AcEdSelectionTransformPreviewJig, _super);
    /**
     * Creates a transform preview jig for the given source entities.
     *
     * @param view - Active editor view that renders transient entities.
     * @param sourceEntities - Original entities cloned for preview display.
     * @param copyCount - Number of copy placements for array previews (default `1`).
     */
    function AcEdSelectionTransformPreviewJig(view, sourceEntities, copyCount) {
        if (copyCount === void 0) { copyCount = 1; }
        var _this = _super.call(this, view) || this;
        _this._transientsPublished = false;
        _this._lastTransforms = [];
        _this._view = view;
        _this.copyCount = copyCount;
        _this.batchPreview = new AcEdBatchedPreview(view, sourceEntities.map(function (entity) { return entity.objectId; }), copyCount);
        _this.previewEntries = _this.batchPreview.useBatchPreview
            ? []
            : AcEdSelectionTransformPreviewJig.createPreviewEntries(sourceEntities, copyCount);
        return _this;
    }
    Object.defineProperty(AcEdSelectionTransformPreviewJig.prototype, "previewEntities", {
        /**
         * Flat list of transient preview entities derived from {@link previewEntries}.
         */
        get: function () {
            return this.previewEntries.map(function (entry) { return entry.entity; });
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Applies the latest transform to batch overlays or caches transforms for
     * the transient clone path.
     *
     * Subclasses with `copyCount > 1` batch previews should override this method
     * and call {@link AcEdBatchedPreview.updatePlacements} when appropriate.
     *
     * @param value - Current prompt input sampled by the editor.
     */
    AcEdSelectionTransformPreviewJig.prototype.update = function (value) {
        var transforms = this.buildTransforms(value);
        if (this.batchPreview.useBatchPreview) {
            if (this.copyCount === 1 && transforms.length > 0) {
                this.batchPreview.updateMatrix(this._view, transforms[0].matrix);
            }
            return;
        }
        this._lastTransforms = transforms;
    };
    /**
     * Publishes transient clones once and applies the latest cached transforms.
     */
    AcEdSelectionTransformPreviewJig.prototype.render = function () {
        if (this.batchPreview.useBatchPreview)
            return;
        if (this.previewEntries.length === 0)
            return;
        if (!this._transientsPublished) {
            this._view.addTransientEntity(this.previewEntities);
            this._transientsPublished = true;
        }
        if (this._lastTransforms.length > 0) {
            this._view.updateTransientPreviewTransforms(this._lastTransforms);
        }
    };
    /**
     * Disposes batch overlays and removes transient preview entities.
     */
    AcEdSelectionTransformPreviewJig.prototype.end = function () {
        var _this = this;
        this.batchPreview.dispose(this._view);
        this.previewEntities.forEach(function (entity) {
            return _this._view.removeTransientEntity(entity.objectId);
        });
    };
    /**
     * Clones source entities for transient preview rendering.
     *
     * @param sourceEntities - Database entities selected for preview.
     * @param copyCount - Number of array placements to clone.
     */
    AcEdSelectionTransformPreviewJig.createPreviewEntries = function (sourceEntities, copyCount) {
        var entries = [];
        var _loop_1 = function (factor) {
            sourceEntities
                .map(function (entity) { return entity.clone(); })
                .filter(function (entity) { return !!entity; })
                .forEach(function (entity) {
                entries.push({ entity: entity, copyFactor: factor });
            });
        };
        for (var factor = 1; factor <= copyCount; factor++) {
            _loop_1(factor);
        }
        return entries;
    };
    return AcEdSelectionTransformPreviewJig;
}(AcEdPreviewJig));
export { AcEdSelectionTransformPreviewJig };
//# sourceMappingURL=AcEdSelectionPreviewJig.js.map