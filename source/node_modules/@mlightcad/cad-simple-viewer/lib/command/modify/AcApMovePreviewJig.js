var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import { AcGeMatrix3d, AcGePoint3d } from '@mlightcad/data-model';
import { AcEdSelectionTransformPreviewJig } from '../../editor/input/AcEdSelectionPreviewJig';
/**
 * MOVE preview jig.
 *
 * Reuses GPU-resident batched geometry when possible. The legacy transient
 * clone path publishes once and then updates transforms only.
 */
var AcApMovePreviewJig = /** @class */ (function (_super) {
    __extends(AcApMovePreviewJig, _super);
    function AcApMovePreviewJig(view, sourceEntities, basePoint) {
        var _this = _super.call(this, view, sourceEntities) || this;
        _this._basePoint = new AcGePoint3d(basePoint);
        return _this;
    }
    AcApMovePreviewJig.prototype.buildTransforms = function (point) {
        var displacement = new AcGePoint3d(point.x - this._basePoint.x, point.y - this._basePoint.y, point.z - this._basePoint.z);
        var matrix = new AcGeMatrix3d().makeTranslation(displacement.x, displacement.y, displacement.z);
        var targets = this.previewEntries.length > 0
            ? this.previewEntries
            : [{ entity: { objectId: '' }, copyFactor: 1 }];
        return targets.map(function (entry) { return ({
            objectId: entry.entity.objectId,
            matrix: matrix
        }); });
    };
    return AcApMovePreviewJig;
}(AcEdSelectionTransformPreviewJig));
export { AcApMovePreviewJig };
//# sourceMappingURL=AcApMovePreviewJig.js.map