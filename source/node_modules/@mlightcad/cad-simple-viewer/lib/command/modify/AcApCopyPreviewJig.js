var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import { AcGeMatrix3d, AcGePoint3d } from '@mlightcad/data-model';
import { scaleCopyDisplacement } from '../../editor/input/AcEdBatchPreview';
import { AcEdSelectionTransformPreviewJig } from '../../editor/input/AcEdSelectionPreviewJig';
/**
 * COPY preview jig.
 *
 * Reuses GPU-resident batched geometry when possible. Array previews create one
 * overlay per copy placement, each with its own translation matrix. The legacy
 * transient clone path publishes once and then updates transforms only.
 */
var AcApCopyPreviewJig = /** @class */ (function (_super) {
    __extends(AcApCopyPreviewJig, _super);
    function AcApCopyPreviewJig(view, sourceEntities, basePoint, copyCount, fitMode) {
        if (copyCount === void 0) { copyCount = 1; }
        if (fitMode === void 0) { fitMode = false; }
        var _this = _super.call(this, view, sourceEntities, Math.max(0, copyCount)) || this;
        _this._basePoint = new AcGePoint3d(basePoint);
        _this._fitMode = fitMode;
        return _this;
    }
    AcApCopyPreviewJig.prototype.update = function (point) {
        var displacement = new AcGePoint3d(point.x - this._basePoint.x, point.y - this._basePoint.y, point.z - this._basePoint.z);
        if (this.batchPreview.useBatchPreview && this.copyCount > 1) {
            this.batchPreview.updatePlacements(this._view, displacement, this._fitMode);
            return;
        }
        _super.prototype.update.call(this, point);
    };
    AcApCopyPreviewJig.prototype.buildTransforms = function (point) {
        var _this = this;
        var displacement = new AcGePoint3d(point.x - this._basePoint.x, point.y - this._basePoint.y, point.z - this._basePoint.z);
        var targets = this.previewEntries.length > 0
            ? this.previewEntries
            : [{ entity: { objectId: '' }, copyFactor: 1 }];
        return targets.map(function (entry) {
            var scaled = scaleCopyDisplacement(displacement, entry.copyFactor, _this.copyCount, _this._fitMode);
            return {
                objectId: entry.entity.objectId,
                matrix: new AcGeMatrix3d().makeTranslation(scaled.x, scaled.y, scaled.z)
            };
        });
    };
    return AcApCopyPreviewJig;
}(AcEdSelectionTransformPreviewJig));
export { AcApCopyPreviewJig };
//# sourceMappingURL=AcApCopyPreviewJig.js.map