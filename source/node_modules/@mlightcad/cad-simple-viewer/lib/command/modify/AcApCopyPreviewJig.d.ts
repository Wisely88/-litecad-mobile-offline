import { AcDbEntity, AcGeMatrix3d, AcGePoint3dLike } from '@mlightcad/data-model';
import { AcEdSelectionTransformPreviewJig } from '../../editor/input/AcEdSelectionPreviewJig';
import { AcEdBaseView } from '../../editor/view/AcEdBaseView';
/**
 * COPY preview jig.
 *
 * Reuses GPU-resident batched geometry when possible. Array previews create one
 * overlay per copy placement, each with its own translation matrix. The legacy
 * transient clone path publishes once and then updates transforms only.
 */
export declare class AcApCopyPreviewJig extends AcEdSelectionTransformPreviewJig<AcGePoint3dLike> {
    private _basePoint;
    private _fitMode;
    constructor(view: AcEdBaseView, sourceEntities: AcDbEntity[], basePoint: AcGePoint3dLike, copyCount?: number, fitMode?: boolean);
    update(point: AcGePoint3dLike): void;
    protected buildTransforms(point: AcGePoint3dLike): {
        objectId: string;
        matrix: AcGeMatrix3d;
    }[];
}
//# sourceMappingURL=AcApCopyPreviewJig.d.ts.map