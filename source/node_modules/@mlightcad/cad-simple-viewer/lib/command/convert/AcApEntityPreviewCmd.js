var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
import { AcApDocManager } from '../../app';
import { AcEdCommand, AcEdOpenMode, AcEdPromptDoubleOptions, AcEdPromptSelectionOptions, AcEdPromptStatus } from '../../editor';
import { AcApI18n } from '../../i18n';
import { AcApEntityPreviewConvertor } from './AcApEntityPreviewConvertor';
var DEFAULT_LONG_SIDE_PX = 512;
/**
 * Command that exports a merged preview image for one or more selected entities.
 */
var AcApEntityPreviewCmd = /** @class */ (function (_super) {
    __extends(AcApEntityPreviewCmd, _super);
    function AcApEntityPreviewCmd() {
        var _this = _super.call(this) || this;
        _this.mode = AcEdOpenMode.Read;
        return _this;
    }
    /**
     * Prompts for entity selection and preview size, then downloads the PNG.
     *
     * @param context - Current application context
     */
    AcApEntityPreviewCmd.prototype.execute = function (context) {
        return __awaiter(this, void 0, void 0, function () {
            var selectionSet, objectIds, options, selectionResult, longSidePrompt, longSideResult, longSide, view, result, message;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        selectionSet = context.view.selectionSet;
                        objectIds = selectionSet.count > 0 ? selectionSet.ids : [];
                        if (!(objectIds.length === 0)) return [3 /*break*/, 2];
                        options = new AcEdPromptSelectionOptions(AcApI18n.sysCmdPrompt('entout'));
                        return [4 /*yield*/, AcApDocManager.instance.editor.getSelection(options)];
                    case 1:
                        selectionResult = _a.sent();
                        if (selectionResult.status !== AcEdPromptStatus.OK ||
                            !selectionResult.value ||
                            selectionResult.value.count === 0) {
                            return [2 /*return*/];
                        }
                        objectIds = selectionResult.value.ids;
                        _a.label = 2;
                    case 2:
                        longSidePrompt = new AcEdPromptDoubleOptions(AcApI18n.t('jig.entout.longSidePrompt'));
                        longSidePrompt.allowNone = true;
                        longSidePrompt.allowNegative = false;
                        longSidePrompt.allowZero = false;
                        longSidePrompt.defaultValue = DEFAULT_LONG_SIDE_PX;
                        longSidePrompt.useDefaultValue = true;
                        return [4 /*yield*/, AcApDocManager.instance.editor.getDouble(longSidePrompt)];
                    case 3:
                        longSideResult = _a.sent();
                        if (longSideResult.status === AcEdPromptStatus.Cancel ||
                            longSideResult.status === AcEdPromptStatus.Error) {
                            return [2 /*return*/];
                        }
                        longSide = longSideResult.status === AcEdPromptStatus.OK &&
                            longSideResult.value !== undefined
                            ? longSideResult.value
                            : DEFAULT_LONG_SIDE_PX;
                        view = AcApDocManager.instance.curView;
                        this.syncActiveLayoutViewSize(view);
                        return [4 /*yield*/, this.withBusyIndicator(function () { return new AcApEntityPreviewConvertor().export(objectIds, longSide); }, AcApI18n.t('main.message.exportingEntityPreview'))];
                    case 4:
                        result = _a.sent();
                        if (result.ok) {
                            message = "".concat(result.exportedCount, " ").concat(AcApI18n.t('jig.entout.exported'));
                            if (result.skippedCount > 0) {
                                message += ", ".concat(result.skippedCount, " ").concat(AcApI18n.t('jig.entout.skipped'));
                            }
                            this.showMessage(message, 'success');
                            return [2 /*return*/];
                        }
                        this.showMessage(AcApI18n.t("jig.entout.failed.".concat(result.reason)), 'error');
                        return [2 /*return*/];
                }
            });
        });
    };
    /**
     * Keeps active layout-view size in sync with current view size.
     */
    AcApEntityPreviewCmd.prototype.syncActiveLayoutViewSize = function (view) {
        var layoutView = view.activeLayoutView;
        if (!layoutView) {
            return;
        }
        layoutView.resize(view.width, view.height);
    };
    return AcApEntityPreviewCmd;
}(AcEdCommand));
export { AcApEntityPreviewCmd };
//# sourceMappingURL=AcApEntityPreviewCmd.js.map