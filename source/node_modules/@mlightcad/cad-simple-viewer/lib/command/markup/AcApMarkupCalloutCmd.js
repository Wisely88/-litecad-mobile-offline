var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
import { AcGePoint3d } from '@mlightcad/data-model';
import { AcTrHtmlCallout, AcTrHtmlDot } from '@mlightcad/three-renderer';
import { AcEdPreviewJig, AcEdPromptPointOptions, AcEdPromptStatus } from '../../editor';
import { acedIsMobileOrPadUi } from '../../editor/global/AcEdUiLayout';
import { AcApI18n } from '../../i18n';
import { pickAttachableShapeMarkupAt } from '../../view';
import { AcApHtmlLivePreview, acapStrokeLiveSegment } from '../overlay/AcApHtmlLivePreview';
import { acapSyncLiveOverlayTextHeight } from '../overlay/AcApOverlayDrawUtil';
import { createMarkupMeta, promptMarkupCapsuleText } from './AcApMarkupCmdUtil';
import { AcApMarkupDrawCmd } from './AcApMarkupDrawCmd';
import { isAttachableShapeMarkup, markupShapeOutlineFromGeometry } from './AcApMarkupGeometry';
import { attachCalloutToMarkup, commitMarkup } from './AcApMarkupPresenter';
import { promptAttachedCallout } from './AcApMarkupShapeCallout';
import { MARKUP_LIVE_LAYER } from './AcApMarkupStore';
import { defaultMarkupColor, defaultMarkupStyle, getMarkupFontSize, MARKUP_LINE_WEIGHT, markupCanvasLineWidth, subscribeMarkupDrawStyle } from './AcApMarkupUtil';
/**
 * Live preview while placing a callout text bubble:
 * tip marker + leader line + arrowhead + floating callout bubble.
 */
var AcApMarkupCalloutJig = /** @class */ (function (_super) {
    __extends(AcApMarkupCalloutJig, _super);
    function AcApMarkupCalloutJig(view, tip, text, color) {
        var _a;
        var _this = _super.call(this, view) || this;
        _this._capsuleRevealed = false;
        _this._view = view;
        _this._tip = { x: tip.x, y: tip.y, z: (_a = tip.z) !== null && _a !== void 0 ? _a : 0 };
        _this._anchor = __assign({}, _this._tip);
        _this._color = color;
        _this._ht = _this._view.htmlTransientManager;
        var layoutId = _this._view.activeLayoutBtrId;
        var stamp = Date.now();
        _this._tipDotId = "live-markup-callout-tip-".concat(stamp);
        _this._bubbleId = "live-markup-callout-bubble-".concat(stamp);
        _this._tipDot = new AcTrHtmlDot({
            id: _this._tipDotId,
            color: color,
            worldPosition: _this._tip,
            layer: MARKUP_LIVE_LAYER,
            layoutId: layoutId
        });
        _this._ht.add(_this._tipDot);
        _this._bubble = new AcTrHtmlCallout({
            id: _this._bubbleId,
            color: color,
            text: text,
            fontSize: getMarkupFontSize(),
            worldPosition: _this._tip,
            layer: MARKUP_LIVE_LAYER,
            layoutId: layoutId
        });
        _this._ht.add(_this._bubble);
        // `add()` applies layout visibility. Keep the empty capsule off until a
        // live preview starts (desktop mouse move / mobile long-press capture).
        _this._bubble.object.visible = false;
        acapSyncLiveOverlayTextHeight(_this._view, [_this._bubble], defaultMarkupStyle());
        _this._preview = new AcApHtmlLivePreview(_this._view, "live-markup-callout-leader-".concat(stamp), MARKUP_LIVE_LAYER);
        _this._unsubDrawStyle = subscribeMarkupDrawStyle(function () {
            return _this.applyCurrentStyle();
        });
        _this.armDesktopCapsulePreview();
        _this._view.isHtmlDirty = true;
        return _this;
    }
    Object.defineProperty(AcApMarkupCalloutJig.prototype, "entity", {
        /** HTML-only preview — no CAD transient. */
        get: function () {
            return null;
        },
        enumerable: false,
        configurable: true
    });
    AcApMarkupCalloutJig.prototype.update = function (anchor) {
        var _a, _b;
        this._anchor = { x: anchor.x, y: anchor.y, z: (_a = anchor.z) !== null && _a !== void 0 ? _a : 0 };
        this._color = defaultMarkupColor();
        this._bubble.setPosition(this._anchor);
        this.syncCapsulePreviewVisibility();
        var style = defaultMarkupStyle();
        this._bubble.setFontSize((_b = style.fontSize) !== null && _b !== void 0 ? _b : getMarkupFontSize());
        acapSyncLiveOverlayTextHeight(this._view, [this._bubble], style);
        this.paintLeader();
        this._view.isHtmlDirty = true;
    };
    Object.defineProperty(AcApMarkupCalloutJig.prototype, "capsule", {
        /** Capsule used for in-place text entry after the bubble is placed. */
        get: function () {
            return this._bubble;
        },
        enumerable: false,
        configurable: true
    });
    /** Show the capsule for text entry (or if the user confirmed without a drag). */
    AcApMarkupCalloutJig.prototype.revealCapsule = function () {
        this.disarmDesktopCapsulePreview();
        this._capsuleRevealed = true;
        this._bubble.object.visible = true;
        this._view.isHtmlDirty = true;
    };
    /**
     * Desktop: show after the user moves the mouse (ignore leftover cursor from
     * `showAt`). Mobile: show as soon as long-press capture starts jig updates.
     */
    AcApMarkupCalloutJig.prototype.syncCapsulePreviewVisibility = function () {
        if (!this._capsuleRevealed && acedIsMobileOrPadUi()) {
            this._capsuleRevealed = true;
        }
        this._bubble.object.visible = this._capsuleRevealed;
    };
    AcApMarkupCalloutJig.prototype.armDesktopCapsulePreview = function () {
        var _this = this;
        if (acedIsMobileOrPadUi())
            return;
        var canvas = this._view.canvas;
        this._desktopMoveArm = function (e) {
            if (e.pointerType === 'touch')
                return;
            // Only arm; `update()` applies visibility at the live cursor position.
            _this._capsuleRevealed = true;
            _this.disarmDesktopCapsulePreview();
        };
        canvas.addEventListener('pointermove', this._desktopMoveArm);
    };
    AcApMarkupCalloutJig.prototype.disarmDesktopCapsulePreview = function () {
        if (!this._desktopMoveArm)
            return;
        this._view.canvas.removeEventListener('pointermove', this._desktopMoveArm);
        this._desktopMoveArm = undefined;
    };
    /**
     * Called when point input ends. Keep leader + bubble visible while the user
     * types callout text; {@link disposePreview} cleans up afterwards.
     */
    AcApMarkupCalloutJig.prototype.end = function () {
        // Intentionally do not remove the HTML overlays.
    };
    /** Remove frozen preview graphics after text entry (or cancel). */
    AcApMarkupCalloutJig.prototype.disposePreview = function () {
        var _a;
        this.disarmDesktopCapsulePreview();
        (_a = this._unsubDrawStyle) === null || _a === void 0 ? void 0 : _a.call(this);
        this._unsubDrawStyle = undefined;
        this._preview.acapDispose();
        this._ht.remove(this._tipDotId);
        this._ht.remove(this._bubbleId);
        this._view.isHtmlDirty = true;
    };
    /** Apply session draw style to the frozen preview (including during text entry). */
    AcApMarkupCalloutJig.prototype.applyCurrentStyle = function () {
        var _a;
        this._color = defaultMarkupColor();
        this._tipDot.setColor(this._color);
        this._bubble.setColor(this._color);
        var style = defaultMarkupStyle();
        this._bubble.setFontSize((_a = style.fontSize) !== null && _a !== void 0 ? _a : getMarkupFontSize());
        acapSyncLiveOverlayTextHeight(this._view, [this._bubble], style);
        this.paintLeader();
        this._view.isHtmlDirty = true;
    };
    AcApMarkupCalloutJig.prototype.paintLeader = function () {
        var lineWidth = markupCanvasLineWidth(MARKUP_LINE_WEIGHT);
        var color = this._color;
        var tip = this._tip;
        var anchor = this._anchor;
        this._preview.acapSetDraw(function (ctx, view) {
            // Arrow at tip (leader begins at tip, bubble at anchor).
            acapStrokeLiveSegment(ctx, view, anchor, tip, color, lineWidth, {
                arrow: true
            });
        });
    };
    return AcApMarkupCalloutJig;
}(AcEdPreviewJig));
/**
 * Create a callout (leader tip + text bubble) with Design Review–style jig preview.
 *
 * Matches Autodesk Design Review 2D callout placement:
 * 1. Click where the leader / arrow tip begins
 * 2. Drag to place the callout text box (live leader + arrow + bubble preview)
 * 3. Type the callout text in the capsule
 *
 * @see Autodesk Design Review help — Create a Callout for 2D Content
 */
var AcApMarkupCalloutCmd = /** @class */ (function (_super) {
    __extends(AcApMarkupCalloutCmd, _super);
    function AcApMarkupCalloutCmd() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    AcApMarkupCalloutCmd.prototype.execute = function (context) {
        return __awaiter(this, void 0, void 0, function () {
            var _this = this;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0: return [4 /*yield*/, this.withMarkupInput(context, function () { return __awaiter(_this, void 0, void 0, function () {
                            var color, tipPrompt, tipResult, tip, view2d, host, outline, callout, jig, anchorPrompt, anchorResult, anchor, text, meta, record;
                            var _a;
                            return __generator(this, function (_b) {
                                switch (_b.label) {
                                    case 0:
                                        color = defaultMarkupColor();
                                        tipPrompt = new AcEdPromptPointOptions(AcApI18n.t('jig.markup.callout.tip'));
                                        return [4 /*yield*/, context.view.editor.getPoint(tipPrompt)];
                                    case 1:
                                        tipResult = _b.sent();
                                        if (tipResult.status !== AcEdPromptStatus.OK)
                                            return [2 /*return*/];
                                        tip = tipResult.value;
                                        view2d = context.view;
                                        host = pickAttachableShapeMarkupAt(view2d, { x: tip.x, y: tip.y });
                                        if (!(host && isAttachableShapeMarkup(host.geometry))) return [3 /*break*/, 3];
                                        outline = markupShapeOutlineFromGeometry(host.geometry);
                                        return [4 /*yield*/, promptAttachedCallout(context, outline, {
                                                force: true,
                                                previewShape: false,
                                                toward: { x: tip.x, y: tip.y }
                                            })];
                                    case 2:
                                        callout = _b.sent();
                                        if (callout) {
                                            attachCalloutToMarkup(context.view, host.id, callout);
                                        }
                                        return [2 /*return*/];
                                    case 3:
                                        jig = new AcApMarkupCalloutJig(context.view, tip, '', color);
                                        _b.label = 4;
                                    case 4:
                                        _b.trys.push([4, , 7, 8]);
                                        anchorPrompt = new AcEdPromptPointOptions(AcApI18n.t('jig.markup.callout.anchor'));
                                        anchorPrompt.basePoint = new AcGePoint3d(tip.x, tip.y, (_a = tip.z) !== null && _a !== void 0 ? _a : 0);
                                        anchorPrompt.useBasePoint = true;
                                        anchorPrompt.useDashedLine = false;
                                        anchorPrompt.jig = jig;
                                        return [4 /*yield*/, context.view.editor.getPoint(anchorPrompt)];
                                    case 5:
                                        anchorResult = _b.sent();
                                        if (anchorResult.status !== AcEdPromptStatus.OK)
                                            return [2 /*return*/];
                                        anchor = anchorResult.value;
                                        // 3. Type in the capsule (same as double-click edit); phone/pad uses
                                        // the session-panel string field.
                                        jig.revealCapsule();
                                        return [4 /*yield*/, promptMarkupCapsuleText(jig.capsule, {
                                                messageKey: 'jig.markup.callout.content'
                                            })];
                                    case 6:
                                        text = _b.sent();
                                        meta = createMarkupMeta('callout', context.view, context, { text: text || undefined });
                                        record = __assign(__assign({}, meta), { type: 'callout', geometry: {
                                                type: 'callout',
                                                tip: { x: tip.x, y: tip.y },
                                                anchor: { x: anchor.x, y: anchor.y }
                                            } });
                                        commitMarkup(context.view, record);
                                        return [3 /*break*/, 8];
                                    case 7:
                                        jig.disposePreview();
                                        return [7 /*endfinally*/];
                                    case 8: return [2 /*return*/];
                                }
                            });
                        }); })];
                    case 1:
                        _a.sent();
                        return [2 /*return*/];
                }
            });
        });
    };
    return AcApMarkupCalloutCmd;
}(AcApMarkupDrawCmd));
export { AcApMarkupCalloutCmd };
//# sourceMappingURL=AcApMarkupCalloutCmd.js.map