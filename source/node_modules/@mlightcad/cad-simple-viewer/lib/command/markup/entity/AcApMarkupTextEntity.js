var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcTrHtmlBadge } from '@mlightcad/three-renderer';
import { bindMarkupInlineTextEdit } from '../AcApMarkupTextEdit';
import { AcApMarkupEntity } from './AcApMarkupEntity';
import { bindMarkupCenterMove } from './AcApMarkupEntityGrips';
/**
 * Text note markup rendered as an {@link AcTrHtmlBadge}.
 *
 * Drag the badge to move; double-click to edit text inline.
 */
var AcApMarkupTextEntity = /** @class */ (function (_super) {
    __extends(AcApMarkupTextEntity, _super);
    /**
     * @param record - Store record whose geometry type must be `text`.
     */
    function AcApMarkupTextEntity(record) {
        return _super.call(this, record) || this;
    }
    /**
     * Publish a badge at the text position, bind drag-to-move and inline edit.
     *
     * @param view - Active 2D view.
     * @returns Group with the badge and dispose / grip binders.
     */
    AcApMarkupTextEntity.prototype.subWorldDraw = function (view) {
        var _this = this;
        var geom = this.record.geometry;
        if (geom.type !== 'text') {
            return this.emptyResult(this.createGroup());
        }
        var _a = this.style(), color = _a.color, layer = _a.layer, layoutId = _a.layoutId;
        var group = this.createGroup();
        /** Unbinders for inline text edit and center-move drag. */
        var cleanups = [];
        /** Grip binders deferred until after manager.add(group). */
        var pendingGrips = [];
        var badge = new AcTrHtmlBadge({
            id: "".concat(this.record.id, "-badge"),
            color: color,
            text: this.record.text || 'Note',
            fontSize: this.record.style.fontSize,
            worldPosition: geom.position,
            layer: layer,
            layoutId: layoutId
        });
        group.add(badge);
        this.seedOverlaySizes(view, [badge], []);
        cleanups.push(bindMarkupInlineTextEdit({
            view: view,
            el: badge.element,
            recordId: this.record.id
        }));
        pendingGrips.push(function () {
            cleanups.push(bindMarkupCenterMove({
                view: view,
                recordId: _this.record.id,
                centerEl: badge
            }));
        });
        return {
            group: group,
            entityIds: [],
            dispose: function () {
                var e_1, _a;
                try {
                    for (var cleanups_1 = __values(cleanups), cleanups_1_1 = cleanups_1.next(); !cleanups_1_1.done; cleanups_1_1 = cleanups_1.next()) {
                        var fn = cleanups_1_1.value;
                        try {
                            fn();
                        }
                        catch (_b) {
                            // ignore
                        }
                    }
                }
                catch (e_1_1) { e_1 = { error: e_1_1 }; }
                finally {
                    try {
                        if (cleanups_1_1 && !cleanups_1_1.done && (_a = cleanups_1.return)) _a.call(cleanups_1);
                    }
                    finally { if (e_1) throw e_1.error; }
                }
            },
            bindGrips: function () {
                var e_2, _a;
                try {
                    for (var pendingGrips_1 = __values(pendingGrips), pendingGrips_1_1 = pendingGrips_1.next(); !pendingGrips_1_1.done; pendingGrips_1_1 = pendingGrips_1.next()) {
                        var bind = pendingGrips_1_1.value;
                        bind();
                    }
                }
                catch (e_2_1) { e_2 = { error: e_2_1 }; }
                finally {
                    try {
                        if (pendingGrips_1_1 && !pendingGrips_1_1.done && (_a = pendingGrips_1.return)) _a.call(pendingGrips_1);
                    }
                    finally { if (e_2) throw e_2.error; }
                }
            }
        };
    };
    return AcApMarkupTextEntity;
}(AcApMarkupEntity));
export { AcApMarkupTextEntity };
//# sourceMappingURL=AcApMarkupTextEntity.js.map