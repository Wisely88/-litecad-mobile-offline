var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcTrHtmlCanvasOverlay, AcTrHtmlGrip } from '@mlightcad/three-renderer';
import { acapFitOverlayCanvas } from '../../overlay';
import { acapLiveRectCorners, acapStrokeLiveCircle, acapStrokeLivePolyline } from '../../overlay/AcApHtmlLivePreview';
import { strokeMarkupCloud } from '../AcApMarkupShapeBuilder';
import { AcApMarkupEntity } from './AcApMarkupEntity';
import { bindMarkupCenterMove, publishAttachedCallout } from './AcApMarkupEntityGrips';
/**
 * Cloud / rect / circle markup with optional attached callout.
 *
 * Builds an HTML canvas shape, a center move grip, and optionally a
 * shape-attached leader + bubble via {@link publishAttachedCallout}.
 * No CAD transient entities.
 */
var AcApMarkupShapeEntity = /** @class */ (function (_super) {
    __extends(AcApMarkupShapeEntity, _super);
    /**
     * @param record - Store record whose geometry is `cloud`, `rect`, or `circle`.
     */
    function AcApMarkupShapeEntity(record) {
        return _super.call(this, record) || this;
    }
    /**
     * Center grip for whole-shape move (delegates to the base implementation).
     *
     * @returns Geometry center as a single grip point.
     */
    AcApMarkupShapeEntity.prototype.subGetGripPoints = function () {
        return _super.prototype.subGetGripPoints.call(this);
    };
    /**
     * Publish canvas shape, center dot, optional attached callout, and center move.
     *
     * @param view - Active 2D view.
     * @returns Built visuals with deferred center-move grip binding.
     */
    AcApMarkupShapeEntity.prototype.subWorldDraw = function (view) {
        var _this = this;
        var geom = this.record.geometry;
        if (geom.type !== 'cloud' &&
            geom.type !== 'rect' &&
            geom.type !== 'circle') {
            return this.emptyResult(this.createGroup());
        }
        var _a = this.style(), color = _a.color, canvasLineWidth = _a.canvasLineWidth, layer = _a.layer, layoutId = _a.layoutId;
        var group = this.createGroup();
        /** Unbinders for viewChanged and grips. */
        var cleanups = [];
        /** Grip binders deferred until after manager.add(group). */
        var pendingGrips = [];
        /** World position of the center move grip. */
        var centerPos;
        /** Outline used to constrain an attached callout tip. */
        var outline;
        /** Live drag translation applied while moving the center grip. */
        var liveOffset = { dx: 0, dy: 0 };
        if (geom.type === 'cloud') {
            centerPos = {
                x: (geom.corner1.x + geom.corner2.x) / 2,
                y: (geom.corner1.y + geom.corner2.y) / 2
            };
            outline = {
                kind: 'cloud',
                corner1: geom.corner1,
                corner2: geom.corner2
            };
        }
        else if (geom.type === 'rect') {
            centerPos = {
                x: (geom.corner1.x + geom.corner2.x) / 2,
                y: (geom.corner1.y + geom.corner2.y) / 2
            };
            outline = {
                kind: 'rect',
                corner1: geom.corner1,
                corner2: geom.corner2
            };
        }
        else {
            centerPos = __assign({}, geom.center);
            outline = {
                kind: 'circle',
                center: geom.center,
                radius: geom.radius
            };
        }
        var container = view.container;
        var overlay = new AcTrHtmlCanvasOverlay({
            id: "".concat(this.record.id, "-shape"),
            container: container,
            layer: layer,
            layoutId: layoutId
        });
        group.addCanvas(overlay);
        var centerDot = new AcTrHtmlGrip({
            id: "".concat(this.record.id, "-dot"),
            color: color,
            worldPosition: centerPos,
            layer: layer,
            layoutId: layoutId
        });
        group.add(centerDot);
        var attached = geom.callout
            ? publishAttachedCallout({
                view: view,
                group: group,
                record: this.record,
                callout: geom.callout,
                style: this.style(),
                cleanups: cleanups,
                outline: outline
            })
            : undefined;
        this.seedOverlaySizes(view, attached ? [centerDot, attached.tipDot, attached.bubble] : [centerDot], [overlay.canvas]);
        /**
         * Redraw the shape stroke with the current {@link liveOffset}.
         */
        var redrawShape = function () {
            var ctx = acapFitOverlayCanvas(overlay.canvas, container);
            if (!ctx)
                return;
            var css = _this.record.style.color;
            if (geom.type === 'cloud') {
                strokeMarkupCloud(ctx, view, geom.corner1, geom.corner2, css, canvasLineWidth, liveOffset, _this.record.style.strokeWidthWcs);
            }
            else if (geom.type === 'rect') {
                var corners = acapLiveRectCorners(geom.corner1, geom.corner2).map(function (p) { return ({
                    x: p.x + liveOffset.dx,
                    y: p.y + liveOffset.dy
                }); });
                acapStrokeLivePolyline(ctx, view, corners, css, canvasLineWidth, {
                    closed: true
                });
            }
            else {
                acapStrokeLiveCircle(ctx, view, {
                    x: geom.center.x + liveOffset.dx,
                    y: geom.center.y + liveOffset.dy
                }, geom.radius, css, canvasLineWidth);
            }
        };
        redrawShape();
        view.events.viewChanged.addEventListener(redrawShape);
        cleanups.push(function () {
            return view.events.viewChanged.removeEventListener(redrawShape);
        });
        pendingGrips.push(function () {
            var _a;
            (_a = attached === null || attached === void 0 ? void 0 : attached.bindGrips) === null || _a === void 0 ? void 0 : _a.call(attached);
            cleanups.push(bindMarkupCenterMove({
                view: view,
                recordId: _this.record.id,
                centerEl: centerDot,
                attached: attached,
                useOsnap: true,
                onLiveOffset: function (dx, dy) {
                    liveOffset = { dx: dx, dy: dy };
                    redrawShape();
                }
            }));
        });
        return {
            group: group,
            entityIds: [],
            dispose: function () {
                var e_1, _a;
                try {
                    for (var cleanups_1 = __values(cleanups), cleanups_1_1 = cleanups_1.next(); !cleanups_1_1.done; cleanups_1_1 = cleanups_1.next()) {
                        var fn = cleanups_1_1.value;
                        try {
                            fn();
                        }
                        catch (_b) {
                            // ignore
                        }
                    }
                }
                catch (e_1_1) { e_1 = { error: e_1_1 }; }
                finally {
                    try {
                        if (cleanups_1_1 && !cleanups_1_1.done && (_a = cleanups_1.return)) _a.call(cleanups_1);
                    }
                    finally { if (e_1) throw e_1.error; }
                }
            },
            bindGrips: function () {
                var e_2, _a;
                try {
                    for (var pendingGrips_1 = __values(pendingGrips), pendingGrips_1_1 = pendingGrips_1.next(); !pendingGrips_1_1.done; pendingGrips_1_1 = pendingGrips_1.next()) {
                        var bind = pendingGrips_1_1.value;
                        bind();
                    }
                }
                catch (e_2_1) { e_2 = { error: e_2_1 }; }
                finally {
                    try {
                        if (pendingGrips_1_1 && !pendingGrips_1_1.done && (_a = pendingGrips_1.return)) _a.call(pendingGrips_1);
                    }
                    finally { if (e_2) throw e_2.error; }
                }
            }
        };
    };
    return AcApMarkupShapeEntity;
}(AcApMarkupEntity));
export { AcApMarkupShapeEntity };
//# sourceMappingURL=AcApMarkupShapeEntity.js.map