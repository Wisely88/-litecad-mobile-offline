import { AcDbDatabase, AcGePoint3dLike } from '@mlightcad/data-model';
import { AcApContext } from '../../app';
import { type AcApMeasurementStyle } from '../../util';
import { AcTrView2d } from '../../view';
import { AcApMeasureDrawCmd } from './AcApMeasureDrawCmd';
/**
 * Commit an angle measurement overlay (also used when importing a sidecar).
 */
export declare function placeAngleMeasurement(view: AcTrView2d, db: AcDbDatabase, vertex: AcGePoint3dLike, arm1: AcGePoint3dLike, arm2: AcGePoint3dLike, style: AcApMeasurementStyle, options?: {
    id?: string;
    layoutId?: string;
}): void;
/**
 * Command that measures the angle between two arms sharing a common vertex.
 *
 * Prompts the user to pick three world points: the vertex, a point on the
 * first arm, and a point on the second arm. Interactive preview is HTML-only
 * (canvas strokes + badge). Committed overlay is placed via
 * {@link placeAngleMeasurement}.
 */
export declare class AcApMeasureAngleCmd extends AcApMeasureDrawCmd {
    execute(context: AcApContext): Promise<void>;
}
//# sourceMappingURL=AcApMeasureAngleCmd.d.ts.map