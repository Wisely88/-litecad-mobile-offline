import { AcCmColor, AcDbDatabase, AcGePoint3dLike } from '@mlightcad/data-model';
import { AcApContext } from '../../app';
import { AcEdBaseView, AcEdPreviewJig } from '../../editor';
import { type AcApMeasurementStyle } from '../../util';
import { AcTrView2d } from '../../view';
import { AcApMeasureDrawCmd } from './AcApMeasureDrawCmd';
/**
 * Commit a distance measurement overlay (also used when importing a sidecar).
 */
export declare function placeDistanceMeasurement(view: AcTrView2d, db: AcDbDatabase, p1: AcGePoint3dLike, p2: AcGePoint3dLike, style: AcApMeasurementStyle, options?: {
    id?: string;
    layoutId?: string;
}): void;
/**
 * Preview jig for the distance measurement command.
 *
 * Renders a live HTML rubber-band segment and length badge (no AcDb preview).
 */
export declare class AcApMeasureDistanceJig extends AcEdPreviewJig<AcGePoint3dLike> {
    private readonly _view;
    private readonly _p1;
    private readonly _db;
    private readonly _htManager;
    private readonly _badge;
    private readonly _badgeId;
    private readonly _preview;
    private _color;
    private _p2;
    constructor(view: AcEdBaseView, db: AcDbDatabase, p1: AcGePoint3dLike, color: AcCmColor);
    /** HTML-only preview — no CAD transient. */
    get entity(): null;
    update(p2: AcGePoint3dLike): void;
    end(): void;
}
/**
 * Command that measures the straight-line distance between two points.
 *
 * Prompts for two world points, then commits a measurement overlay.
 * Interactive preview is HTML-only (canvas stroke + badge).
 */
export declare class AcApMeasureDistanceCmd extends AcApMeasureDrawCmd {
    execute(context: AcApContext): Promise<void>;
}
//# sourceMappingURL=AcApMeasureDistanceCmd.d.ts.map