var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
import { formatMeasurementValue } from '../../util/AcApMeasurementUnits';
import { acapCloneMeasurementStyle, acapMeasurementCanvasLineWidth, MEASUREMENT_FONT_SIZE, MEASUREMENT_LINE_WEIGHT } from '../../util/AcApMeasurementUtil';
import { ACAP_OVERLAY_ARROW_SIZE_PX, acapScreenPxToWcs, acapSeedOverlaySizesFromWcs } from '../overlay/AcApOverlayDrawUtil';
import { hitTestMeasurementGeometry, measurementFocusBox } from './AcApMeasurementGeometry';
import { runMeasurementEdit } from './AcApMeasurementHistory';
import { serializeMeasurementStyle } from './AcApMeasurementSidecar';
/** HTML transient layer for committed measurement overlays. */
export var MEASUREMENT_LAYER = 'measurement';
/** HTML transient layer for in-progress jig / live preview overlays. */
export var MEASUREMENT_LIVE_LAYER = 'measurement-live';
/** Session visibility for measurement overlays on the active layout. */
var measurementVisible = true;
/** Whether measurement overlays are currently shown. */
export function isMeasurementVisible() {
    return measurementVisible;
}
/** Restore the default (shown) session flag after a document open / close. */
export function resetMeasurementVisibility() {
    measurementVisible = true;
}
/**
 * Show or hide committed measurement overlays on the active layout, plus live
 * jig overlays.
 */
export function setMeasurementVisible(view, visible) {
    var e_1, _a;
    measurementVisible = visible;
    var layoutId = view.activeLayoutBtrId;
    var ht = view.htmlTransientManager;
    try {
        for (var _b = __values(ht.groupsOnLayer(MEASUREMENT_LAYER)), _c = _b.next(); !_c.done; _c = _b.next()) {
            var group = _c.value;
            if (group.layoutId == null || group.layoutId === layoutId) {
                group.setVisible(visible);
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_1) throw e_1.error; }
    }
    ht.setVisible(visible, MEASUREMENT_LIVE_LAYER);
    view.isHtmlDirty = true;
}
var extrasById = new Map();
/**
 * Per-group extras retained across {@link AcTrHtmlTransientManager.detach} so
 * undo/redo can restore geometry snapshots when groups with the same id are
 * swapped during grip edits.
 */
var extrasByGroup = new WeakMap();
var stylesById = new Map();
var selectionListeners = new Set();
var listListeners = new Set();
var selectedMeasurementId;
/** Style currently stored for a committed measurement group. */
export function getMeasurementStyle(id) {
    var style = stylesById.get(id);
    return style ? acapCloneMeasurementStyle(style) : undefined;
}
/** Id of the currently selected measurement group, if any. */
export function getSelectedMeasurementId() {
    return selectedMeasurementId;
}
/** Style of the currently selected measurement, if any. */
export function getActiveMeasurementStyle() {
    return selectedMeasurementId
        ? getMeasurementStyle(selectedMeasurementId)
        : undefined;
}
/** Notify when the selected measurement group changes. */
export function subscribeMeasurementSelection(listener) {
    selectionListeners.add(listener);
    return function () {
        selectionListeners.delete(listener);
    };
}
function notifyMeasurementSelection() {
    var e_2, _a;
    try {
        for (var selectionListeners_1 = __values(selectionListeners), selectionListeners_1_1 = selectionListeners_1.next(); !selectionListeners_1_1.done; selectionListeners_1_1 = selectionListeners_1.next()) {
            var listener = selectionListeners_1_1.value;
            listener();
        }
    }
    catch (e_2_1) { e_2 = { error: e_2_1 }; }
    finally {
        try {
            if (selectionListeners_1_1 && !selectionListeners_1_1.done && (_a = selectionListeners_1.return)) _a.call(selectionListeners_1);
        }
        finally { if (e_2) throw e_2.error; }
    }
}
/** Notify when committed measurements are added, removed, restored, or filtered by layout. */
export function subscribeMeasurements(listener) {
    listListeners.add(listener);
    return function () {
        listListeners.delete(listener);
    };
}
function notifyMeasurementsChanged() {
    var e_3, _a;
    try {
        for (var listListeners_1 = __values(listListeners), listListeners_1_1 = listListeners_1.next(); !listListeners_1_1.done; listListeners_1_1 = listListeners_1.next()) {
            var listener = listListeners_1_1.value;
            listener();
        }
    }
    catch (e_3_1) { e_3 = { error: e_3_1 }; }
    finally {
        try {
            if (listListeners_1_1 && !listListeners_1_1.done && (_a = listListeners_1.return)) _a.call(listListeners_1);
        }
        finally { if (e_3) throw e_3.error; }
    }
}
/**
 * Notify list subscribers that the active layout changed, so layout-filtered
 * views (the measurement palette) can re-query {@link listLayoutMeasurements}.
 */
export function notifyMeasurementLayoutChanged() {
    notifyMeasurementsChanged();
}
/**
 * Committed measurements on the active layout (records without `layoutId`
 * are treated as present on every layout).
 */
export function listLayoutMeasurements(view) {
    var layoutId = view.activeLayoutBtrId;
    return collectMeasurementRecords(view).filter(function (record) { return record.layoutId == null || record.layoutId === layoutId; });
}
/**
 * Formatted badge text for a committed measurement, or an empty string when
 * the value or drawing database is unavailable.
 */
export function getMeasurementValueText(id, db) {
    var extras = extrasById.get(id);
    if (!(extras === null || extras === void 0 ? void 0 : extras.value) || !db)
        return '';
    return formatMeasurementValue(db, extras.value);
}
/**
 * Select a measurement and zoom the view to its geometry plus HTML overlays.
 *
 * Coordinate measurements include the badge (capsule) so the camera frames
 * the label instead of zooming onto the point. Zooming to a 1-unit pad around
 * the point would scale the WCS-sized capsule over the canvas and steal
 * pointer events.
 */
export function focusMeasurement(view, record) {
    var box = measurementFocusBox(record.geometry, collectMeasurementOverlayRects(view, record.id), function (clientX, clientY) {
        var canvas = view.viewportToCanvas({ x: clientX, y: clientY });
        return view.screenToWorld(canvas);
    });
    if (!box)
        return;
    view.zoomTo(box, 1.5);
    view.htmlTransientManager.selectGroup(record.id);
}
/** Badge / callout client rects; skip endpoint grips that sit on geometry. */
function collectMeasurementOverlayRects(view, id) {
    var e_4, _a;
    var group = view.htmlTransientManager.getGroup(id);
    if (!group)
        return [];
    var rects = [];
    try {
        for (var _b = __values(group.children), _c = _b.next(); !_c.done; _c = _b.next()) {
            var child = _c.value;
            var el = child.element;
            if (el.classList.contains('ml-html-dot') ||
                el.classList.contains('ml-html-grip')) {
                continue;
            }
            var rect = el.getBoundingClientRect();
            if (rect.width <= 0 && rect.height <= 0)
                continue;
            rects.push(rect);
        }
    }
    catch (e_4_1) { e_4 = { error: e_4_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_4) throw e_4.error; }
    }
    return rects;
}
/**
 * Detach one committed measurement (undoable).
 */
export function removeMeasurement(view, id) {
    if (!view.htmlTransientManager.getGroup(id))
        return;
    runMeasurementEdit(view, 'Delete Measurement', function () {
        view.htmlTransientManager.detach(id);
    });
    view.isHtmlDirty = true;
}
function rememberStyle(id, style) {
    stylesById.set(id, acapCloneMeasurementStyle(style));
}
/**
 * Apply a style patch to one measurement group (HTML badges/dots and canvases).
 * Does not record undo; wrap with {@link runMeasurementEdit} from UI.
 *
 * Color and font size are independent: changing one does not rewrite the
 * other's world-space size from the current camera zoom. Overlay strokes stay
 * hairline ({@link MEASUREMENT_LINE_WEIGHT}); strokeWidthWcs is never written.
 */
export function applyMeasurementStyle(view, group, patch) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l;
    var prev = stylesById.get(group.id);
    var color = (_b = (_a = patch.color) === null || _a === void 0 ? void 0 : _a.clone()) !== null && _b !== void 0 ? _b : prev === null || prev === void 0 ? void 0 : prev.color.clone();
    if (!color)
        return;
    var next = {
        color: color,
        lineWeight: MEASUREMENT_LINE_WEIGHT,
        fontSize: (_d = (_c = patch.fontSize) !== null && _c !== void 0 ? _c : prev === null || prev === void 0 ? void 0 : prev.fontSize) !== null && _d !== void 0 ? _d : MEASUREMENT_FONT_SIZE,
        textHeightMode: (_f = (_e = patch.textHeightMode) !== null && _e !== void 0 ? _e : prev === null || prev === void 0 ? void 0 : prev.textHeightMode) !== null && _f !== void 0 ? _f : 'adaptive',
        textHeightWcs: (_g = patch.textHeightWcs) !== null && _g !== void 0 ? _g : (patch.textHeightMode === 'custom' ? undefined : prev === null || prev === void 0 ? void 0 : prev.textHeightWcs)
    };
    if (next.textHeightMode === 'custom' && patch.textHeightWcs != null) {
        next.textHeightWcs = patch.textHeightWcs;
    }
    var fontSizeChanged = (patch.fontSize != null &&
        (prev == null || patch.fontSize !== prev.fontSize)) ||
        (patch.textHeightWcs != null &&
            (prev == null || patch.textHeightWcs !== prev.textHeightWcs)) ||
        (patch.textHeightMode != null &&
            (prev == null || patch.textHeightMode !== prev.textHeightMode));
    rememberStyle(group.id, next);
    var extras = extrasById.get(group.id);
    if (extras) {
        extras.style = acapCloneMeasurementStyle(next);
        if (extras.snapshot) {
            var prevSnap = extras.snapshot.style;
            var base = serializeMeasurementStyle(next);
            var textHeightWcs = next.textHeightMode === 'custom' &&
                next.textHeightWcs != null &&
                next.textHeightWcs > 0
                ? next.textHeightWcs
                : resolveUpdatedTextHeightWcs(view, next.fontSize, prev === null || prev === void 0 ? void 0 : prev.fontSize, prevSnap.textHeightWcs, fontSizeChanged);
            extras.snapshot = __assign(__assign({}, extras.snapshot), { style: __assign(__assign(__assign({}, base), { textHeightWcs: textHeightWcs }), (prevSnap.arrowSizeWcs != null && prevSnap.arrowSizeWcs > 0
                    ? { arrowSizeWcs: prevSnap.arrowSizeWcs }
                    : {})) });
        }
    }
    if (fontSizeChanged) {
        var snap = (_j = (_h = extrasById.get(group.id)) === null || _h === void 0 ? void 0 : _h.snapshot) === null || _j === void 0 ? void 0 : _j.style;
        acapSeedOverlaySizesFromWcs(view, {
            textHeightWcs: snap === null || snap === void 0 ? void 0 : snap.textHeightWcs,
            fontSizePx: next.fontSize,
            strokeScreenPx: acapMeasurementCanvasLineWidth(MEASUREMENT_LINE_WEIGHT),
            elements: __spreadArray([], __read(((_k = group.children) !== null && _k !== void 0 ? _k : [])), false),
            canvases: ((_l = group.canvases) !== null && _l !== void 0 ? _l : []).map(function (c) { return c.canvas; })
        });
    }
    paintMeasurementGroup(view, group, next);
}
/**
 * Scale or recompute text height WCS when font size changes; otherwise keep.
 */
function resolveUpdatedTextHeightWcs(view, nextFontSize, prevFontSize, prevTextHeightWcs, fontSizeChanged) {
    if (fontSizeChanged &&
        prevTextHeightWcs != null &&
        prevTextHeightWcs > 0 &&
        prevFontSize != null &&
        prevFontSize > 0) {
        return prevTextHeightWcs * (nextFontSize / prevFontSize);
    }
    if (fontSizeChanged || prevTextHeightWcs == null || !(prevTextHeightWcs > 0)) {
        return acapScreenPxToWcs(nextFontSize, view);
    }
    return prevTextHeightWcs;
}
/**
 * Apply a style patch to every selected measurement group (undoable).
 */
export function applyMeasurementStyleToSelection(view, patch) {
    var groups = view.htmlTransientManager
        .getSelectedGroups()
        .filter(function (group) { return group.layer === MEASUREMENT_LAYER; });
    if (groups.length === 0)
        return;
    runMeasurementEdit(view, 'Measurement Style', function () {
        var e_5, _a;
        try {
            for (var groups_1 = __values(groups), groups_1_1 = groups_1.next(); !groups_1_1.done; groups_1_1 = groups_1.next()) {
                var group = groups_1_1.value;
                applyMeasurementStyle(view, group, patch);
            }
        }
        catch (e_5_1) { e_5 = { error: e_5_1 }; }
        finally {
            try {
                if (groups_1_1 && !groups_1_1.done && (_a = groups_1.return)) _a.call(groups_1);
            }
            finally { if (e_5) throw e_5.error; }
        }
    });
}
function paintMeasurementGroup(view, group, style) {
    var e_6, _a;
    var _b, _c, _d;
    try {
        for (var _e = __values(group.children), _f = _e.next(); !_f.done; _f = _e.next()) {
            var child = _f.value;
            var colorful = child;
            (_b = colorful.setColor) === null || _b === void 0 ? void 0 : _b.call(colorful, style.color);
            (_c = colorful.setFontSize) === null || _c === void 0 ? void 0 : _c.call(colorful, style.fontSize);
        }
    }
    catch (e_6_1) { e_6 = { error: e_6_1 }; }
    finally {
        try {
            if (_f && !_f.done && (_a = _e.return)) _a.call(_e);
        }
        finally { if (e_6) throw e_6.error; }
    }
    var extras = extrasById.get(group.id);
    (_d = extras === null || extras === void 0 ? void 0 : extras.redraw) === null || _d === void 0 ? void 0 : _d.call(extras, style);
    view.isHtmlDirty = true;
}
/**
 * Reformat committed measurement badges using the effective measurement units.
 * Notifies list subscribers so palettes refresh their cached value text.
 */
export function refreshMeasurementValueLabels(view, db) {
    var e_7, _a, e_8, _b;
    var _c;
    try {
        for (var _d = __values(view.htmlTransientManager.groupsOnLayer(MEASUREMENT_LAYER)), _e = _d.next(); !_e.done; _e = _d.next()) {
            var group = _e.value;
            var extras = extrasById.get(group.id);
            if (!(extras === null || extras === void 0 ? void 0 : extras.value))
                continue;
            var text = formatMeasurementValue(db, extras.value);
            try {
                for (var _f = (e_8 = void 0, __values(group.children)), _g = _f.next(); !_g.done; _g = _f.next()) {
                    var child = _g.value;
                    var badge = child;
                    (_c = badge.setText) === null || _c === void 0 ? void 0 : _c.call(badge, text);
                }
            }
            catch (e_8_1) { e_8 = { error: e_8_1 }; }
            finally {
                try {
                    if (_g && !_g.done && (_b = _f.return)) _b.call(_f);
                }
                finally { if (e_8) throw e_8.error; }
            }
        }
    }
    catch (e_7_1) { e_7 = { error: e_7_1 }; }
    finally {
        try {
            if (_e && !_e.done && (_a = _d.return)) _a.call(_d);
        }
        finally { if (e_7) throw e_7.error; }
    }
    view.isHtmlDirty = true;
    notifyMeasurementsChanged();
}
/** Serializable snapshots of committed measurements currently on the view. */
export function collectMeasurementRecords(view) {
    var e_9, _a;
    var _b, _c;
    var records = [];
    try {
        for (var _d = __values(view.htmlTransientManager.groupsOnLayer(MEASUREMENT_LAYER)), _e = _d.next(); !_e.done; _e = _d.next()) {
            var group = _e.value;
            var extras = extrasById.get(group.id);
            if (!(extras === null || extras === void 0 ? void 0 : extras.snapshot))
                continue;
            var live = (_b = extras.style) !== null && _b !== void 0 ? _b : stylesById.get(group.id);
            var snapStyle = extras.snapshot.style;
            var style = void 0;
            if (live) {
                // Keep live color / fontSize, force hairline lineWeight, preserve
                // creation-time textHeightWcs / arrowSizeWcs from the snapshot. Never
                // export strokeWidthWcs.
                var base = serializeMeasurementStyle(live);
                var arrowSizeWcs = snapStyle.arrowSizeWcs != null && snapStyle.arrowSizeWcs > 0
                    ? snapStyle.arrowSizeWcs
                    : extras.snapshot.type === 'distance'
                        ? acapScreenPxToWcs(ACAP_OVERLAY_ARROW_SIZE_PX, view)
                        : undefined;
                style = __assign(__assign(__assign({}, base), { lineWeight: MEASUREMENT_LINE_WEIGHT, textHeightWcs: (_c = snapStyle.textHeightWcs) !== null && _c !== void 0 ? _c : acapScreenPxToWcs(base.fontSize, view) }), (arrowSizeWcs != null && arrowSizeWcs > 0 ? { arrowSizeWcs: arrowSizeWcs } : {}));
            }
            else {
                var _omit = snapStyle.strokeWidthWcs, rest = __rest(snapStyle, ["strokeWidthWcs"]);
                style = __assign(__assign({}, rest), { lineWeight: MEASUREMENT_LINE_WEIGHT });
            }
            records.push(__assign(__assign({}, extras.snapshot), { id: group.id, layoutId: group.layoutId, style: style }));
        }
    }
    catch (e_9_1) { e_9 = { error: e_9_1 }; }
    finally {
        try {
            if (_e && !_e.done && (_a = _d.return)) _a.call(_d);
        }
        finally { if (e_9) throw e_9.error; }
    }
    return records;
}
/** Drop style / extras maps (document open). Attached groups are left for view.clear(). */
export function resetMeasurementStyleState() {
    extrasById.clear();
    stylesById.clear();
    selectedMeasurementId = undefined;
    notifyMeasurementSelection();
    notifyMeasurementsChanged();
}
/**
 * Re-bind extras for a group reattached by measurement undo/redo.
 * Call after {@link AcTrHtmlTransientManager.reattach}.
 */
export function restoreMeasurementGroupExtras(group) {
    var extras = extrasByGroup.get(group);
    if (!extras)
        return;
    extrasById.set(group.id, extras);
    if (extras.style) {
        rememberStyle(group.id, extras.style);
    }
    notifyMeasurementsChanged();
}
/**
 * Publishes a measurement {@link AcTrHtmlGroup} and wires measurement-specific
 * selection state.
 *
 * The group itself (children, canvases, click selection, Delete, layout
 * visibility) is handled by {@link AcTrHtmlTransientManager}.
 */
export function commitMeasurementGroup(view, group, extras) {
    var _a;
    var entityIds = (_a = extras === null || extras === void 0 ? void 0 : extras.entityIds) !== null && _a !== void 0 ? _a : [];
    var payload = extras !== null && extras !== void 0 ? extras : {};
    extrasById.set(group.id, payload);
    extrasByGroup.set(group, payload);
    if (extras === null || extras === void 0 ? void 0 : extras.style) {
        rememberStyle(group.id, extras.style);
    }
    var prevSelectedChanged = group.onSelectedChanged;
    group.onSelectedChanged = function (selected, g) {
        prevSelectedChanged === null || prevSelectedChanged === void 0 ? void 0 : prevSelectedChanged(selected, g);
        if (selected) {
            selectedMeasurementId = g.id;
            notifyMeasurementSelection();
            if (entityIds.length > 0)
                view.highlight(entityIds);
        }
        else {
            if (selectedMeasurementId === g.id) {
                selectedMeasurementId = undefined;
                notifyMeasurementSelection();
            }
            if (entityIds.length > 0)
                view.unhighlight(entityIds);
        }
    };
    var prevVisibleChanged = group.onVisibleChanged;
    group.onVisibleChanged = function (visible, g) {
        var e_10, _a;
        prevVisibleChanged === null || prevVisibleChanged === void 0 ? void 0 : prevVisibleChanged(visible, g);
        try {
            for (var entityIds_1 = __values(entityIds), entityIds_1_1 = entityIds_1.next(); !entityIds_1_1.done; entityIds_1_1 = entityIds_1.next()) {
                var objectId = entityIds_1_1.value;
                view.setTransientEntityVisible(objectId, visible);
            }
        }
        catch (e_10_1) { e_10 = { error: e_10_1 }; }
        finally {
            try {
                if (entityIds_1_1 && !entityIds_1_1.done && (_a = entityIds_1.return)) _a.call(entityIds_1);
            }
            finally { if (e_10) throw e_10.error; }
        }
    };
    var prevDispose = group.onDispose;
    group.onDispose = function () {
        var _a;
        prevDispose === null || prevDispose === void 0 ? void 0 : prevDispose();
        extrasById.delete(group.id);
        stylesById.delete(group.id);
        if (selectedMeasurementId === group.id) {
            selectedMeasurementId = undefined;
            notifyMeasurementSelection();
        }
        if (entityIds.length > 0) {
            view.unhighlight(entityIds);
        }
        try {
            (_a = extras === null || extras === void 0 ? void 0 : extras.dispose) === null || _a === void 0 ? void 0 : _a.call(extras);
        }
        catch (_b) {
            // Ignore dispose errors from domain cleanups.
        }
        notifyMeasurementsChanged();
    };
    runMeasurementEdit(view, 'Create Measurement', function () {
        view.htmlTransientManager.add(group);
    });
    if (!measurementVisible &&
        (group.layoutId == null || group.layoutId === view.activeLayoutBtrId)) {
        group.setVisible(false);
    }
    view.isHtmlDirty = true;
    notifyMeasurementsChanged();
}
/** Snapshot geometry for a committed measurement group, if available. */
export function getMeasurementGeometry(id) {
    var _a, _b;
    return (_b = (_a = extrasById.get(id)) === null || _a === void 0 ? void 0 : _a.snapshot) === null || _b === void 0 ? void 0 : _b.geometry;
}
/** Full sidecar snapshot for a committed measurement, if available. */
export function getMeasurementSnapshot(id) {
    var _a;
    var snap = (_a = extrasById.get(id)) === null || _a === void 0 ? void 0 : _a.snapshot;
    return snap ? __assign(__assign({}, snap), { geometry: snap.geometry }) : undefined;
}
/**
 * Pick a visible measurement group by clicking its drawn stroke / fill.
 *
 * @returns Group id when the canvas-space point is within `threshold` pixels.
 */
export function pickMeasurementAt(view, canvasX, canvasY, threshold) {
    if (!measurementVisible)
        return undefined;
    var canvas = { x: canvasX, y: canvasY };
    var worldToScreen = function (point) {
        return view.worldToScreen(point);
    };
    var groups = view.htmlTransientManager.groupsOnLayer(MEASUREMENT_LAYER);
    for (var i = groups.length - 1; i >= 0; i--) {
        var group = groups[i];
        if (!group.visible)
            continue;
        var geom = getMeasurementGeometry(group.id);
        if (!geom)
            continue;
        if (hitTestMeasurementGeometry(geom, canvas, worldToScreen, threshold)) {
            return group.id;
        }
    }
    return undefined;
}
//# sourceMappingURL=AcApMeasurementStore.js.map