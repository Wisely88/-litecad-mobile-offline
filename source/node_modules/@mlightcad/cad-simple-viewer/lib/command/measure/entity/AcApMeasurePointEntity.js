var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcTrHtmlBadge, AcTrHtmlGrip } from '@mlightcad/three-renderer';
import { formatMeasurementValue } from '../../../util';
import { acapBindOverlayPointerDrag, acapPlaceOverlayHtml } from '../../overlay';
import { runMeasurementEdit } from '../AcApMeasurementHistory';
import { republishMeasurement } from '../AcApMeasurementRepublish';
import { getMeasurementSnapshot, MEASUREMENT_LAYER } from '../AcApMeasurementStore';
import { AcApMeasureEntity } from './AcApMeasureEntity';
import { selectMeasurementGroup } from './AcApMeasureEntityGrips';
/**
 * Point / coordinate measurement overlay entity.
 *
 * Renders an HTML dot at the measured location and a badge showing formatted
 * X/Y coordinates. The endpoint grip moves the point and updates the label.
 */
var AcApMeasurePointEntity = /** @class */ (function (_super) {
    __extends(AcApMeasurePointEntity, _super);
    /**
     * Creates a point measure entity at the given location.
     *
     * @param point - World position to measure
     * @param options - Shared id, layout, and style options
     */
    function AcApMeasurePointEntity(point, options) {
        var _a;
        var _this = _super.call(this, (_a = options.id) !== null && _a !== void 0 ? _a : "point-".concat(Date.now()), options.layoutId, options.style, options.textHeightWcs, options.strokeWidthWcs) || this;
        _this.point = point;
        return _this;
    }
    /**
     * Factory that builds a point entity from a world position and style.
     *
     * @param point - World position to measure
     * @param style - Measurement visual style
     * @param options - Optional id and layout overrides
     * @returns New {@link AcApMeasurePointEntity}
     */
    AcApMeasurePointEntity.create = function (point, style, options) {
        return new AcApMeasurePointEntity(point, {
            style: style,
            id: options === null || options === void 0 ? void 0 : options.id,
            layoutId: options === null || options === void 0 ? void 0 : options.layoutId
        });
    };
    /**
     * Primary anchor for the measurement (the measured point itself).
     *
     * @returns Copy of {@link point} with `z: 0`
     */
    AcApMeasurePointEntity.prototype.primaryPoint = function () {
        return { x: this.point.x, y: this.point.y, z: 0 };
    };
    /**
     * Serializes this point measurement to a store/sidecar record.
     *
     * @param layoutId - Optional layout BTR id written onto the record
     * @param view - Optional view used to convert screen style to WCS
     * @returns Record with `type: 'point'` and position geometry
     */
    AcApMeasurePointEntity.prototype.toRecord = function (layoutId, view) {
        return {
            id: this.entityId,
            type: 'point',
            layoutId: layoutId,
            style: this.serializeStyle(view),
            geometry: {
                type: 'point',
                position: { x: this.point.x, y: this.point.y }
            }
        };
    };
    /**
     * Draws the coordinate dot, badge, and commit extras (no CAD transients).
     *
     * @param view - Active 2D view for HTML overlays
     * @param db - Database used to format the coordinate label
     * @returns World-draw result with empty `entityIds`
     */
    AcApMeasurePointEntity.prototype.subWorldDrawWithDb = function (view, db) {
        var _this = this;
        var live = { x: this.point.x, y: this.point.y };
        var layoutId = this.resolveLayoutId(view);
        var value = {
            kind: 'coordinate',
            x: live.x,
            y: live.y
        };
        var color = this.style.color;
        var dot = new AcTrHtmlGrip({
            id: "".concat(this.entityId, "-dot"),
            color: color,
            worldPosition: live,
            layer: MEASUREMENT_LAYER
        });
        var badge = new AcTrHtmlBadge({
            id: "".concat(this.entityId, "-badge"),
            color: color,
            text: formatMeasurementValue(db, value),
            worldPosition: live,
            layer: MEASUREMENT_LAYER,
            fontSize: this.style.fontSize,
            transform: 'translate(-50%, calc(-50% - 16px))'
        });
        this.seedOverlaySizes(view, [dot, badge]);
        var group = this.createGroup(view).add(dot, badge);
        var cleanups = [];
        var pendingGrips = [];
        var dragStart = __assign({}, live);
        var refreshLive = function () {
            value = { kind: 'coordinate', x: live.x, y: live.y };
            badge.setText(formatMeasurementValue(db, value));
            acapPlaceOverlayHtml(view, badge, live);
            view.isHtmlDirty = true;
        };
        pendingGrips.push(function () {
            cleanups.push(acapBindOverlayPointerDrag({
                view: view,
                el: dot.element,
                onDragStart: function () {
                    selectMeasurementGroup(view, _this.entityId);
                    dragStart = __assign({}, live);
                },
                onMove: function (point) {
                    live.x = point.x;
                    live.y = point.y;
                    acapPlaceOverlayHtml(view, dot, live);
                    refreshLive();
                },
                onCommit: function () {
                    if (Math.hypot(live.x - dragStart.x, live.y - dragStart.y) < 1e-9) {
                        refreshLive();
                        return;
                    }
                    var snap = getMeasurementSnapshot(_this.entityId);
                    var geometry = {
                        type: 'point',
                        position: { x: live.x, y: live.y }
                    };
                    var record = snap
                        ? __assign(__assign({}, snap), { geometry: geometry }) : {
                        id: _this.entityId,
                        type: 'point',
                        layoutId: layoutId,
                        style: _this.serializeStyle(view),
                        geometry: geometry
                    };
                    runMeasurementEdit(view, 'Move Point', function () {
                        republishMeasurement(view, db, record);
                    });
                }
            }));
        });
        return {
            group: group,
            entityIds: [],
            dispose: function () {
                var e_1, _a;
                try {
                    for (var cleanups_1 = __values(cleanups), cleanups_1_1 = cleanups_1.next(); !cleanups_1_1.done; cleanups_1_1 = cleanups_1.next()) {
                        var fn = cleanups_1_1.value;
                        try {
                            fn();
                        }
                        catch (_b) {
                            // ignore
                        }
                    }
                }
                catch (e_1_1) { e_1 = { error: e_1_1 }; }
                finally {
                    try {
                        if (cleanups_1_1 && !cleanups_1_1.done && (_a = cleanups_1.return)) _a.call(cleanups_1);
                    }
                    finally { if (e_1) throw e_1.error; }
                }
            },
            bindGrips: function () {
                var e_2, _a;
                try {
                    for (var pendingGrips_1 = __values(pendingGrips), pendingGrips_1_1 = pendingGrips_1.next(); !pendingGrips_1_1.done; pendingGrips_1_1 = pendingGrips_1.next()) {
                        var bind = pendingGrips_1_1.value;
                        bind();
                    }
                }
                catch (e_2_1) { e_2 = { error: e_2_1 }; }
                finally {
                    try {
                        if (pendingGrips_1_1 && !pendingGrips_1_1.done && (_a = pendingGrips_1.return)) _a.call(pendingGrips_1);
                    }
                    finally { if (e_2) throw e_2.error; }
                }
            },
            extras: {
                style: this.style,
                value: value,
                snapshot: this.toRecord(layoutId, view)
            }
        };
    };
    return AcApMeasurePointEntity;
}(AcApMeasureEntity));
export { AcApMeasurePointEntity };
//# sourceMappingURL=AcApMeasurePointEntity.js.map