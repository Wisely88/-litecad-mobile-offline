var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcTrHtmlBadge, AcTrHtmlCanvasOverlay, AcTrHtmlGrip } from '@mlightcad/three-renderer';
import { acapMeasurementCanvasLineWidth, formatMeasurementAngle } from '../../../util';
import { acapBindOverlayPointerDrag, acapPlaceOverlayHtml } from '../../overlay';
import { runMeasurementEdit } from '../AcApMeasurementHistory';
import { republishMeasurement } from '../AcApMeasurementRepublish';
import { getMeasurementSnapshot, getMeasurementStyle, MEASUREMENT_LAYER } from '../AcApMeasurementStore';
import { drawMeasureAngleArcOnCanvas, measureAngleDeg } from './AcApMeasureDrawUtil';
import { AcApMeasureEntity } from './AcApMeasureEntity';
import { measureAngleBadgeWorld, selectMeasurementGroup } from './AcApMeasureEntityGrips';
/**
 * Angle measurement overlay entity.
 *
 * Renders two arm lines and an angle arc on an HTML canvas, HTML dots at the
 * vertex and arm ends, and a degree badge along the angle bisector.
 * Endpoint grips update the measured value live and republish on commit.
 */
var AcApMeasureAngleEntity = /** @class */ (function (_super) {
    __extends(AcApMeasureAngleEntity, _super);
    /**
     * Creates an angle measure entity from a vertex and two arm endpoints.
     *
     * @param vertex - Angle vertex
     * @param arm1 - First arm endpoint
     * @param arm2 - Second arm endpoint
     * @param options - Shared id, layout, and style options
     */
    function AcApMeasureAngleEntity(vertex, arm1, arm2, options) {
        var _a;
        var _this = _super.call(this, (_a = options.id) !== null && _a !== void 0 ? _a : "angle-".concat(Date.now()), options.layoutId, options.style, options.textHeightWcs, options.strokeWidthWcs) || this;
        _this.vertex = vertex;
        _this.arm1 = arm1;
        _this.arm2 = arm2;
        return _this;
    }
    /**
     * Factory that builds an angle entity from vertex, arms, and style.
     *
     * @param vertex - Angle vertex
     * @param arm1 - First arm endpoint
     * @param arm2 - Second arm endpoint
     * @param style - Measurement visual style
     * @param options - Optional id and layout overrides
     * @returns New {@link AcApMeasureAngleEntity}
     */
    AcApMeasureAngleEntity.create = function (vertex, arm1, arm2, style, options) {
        return new AcApMeasureAngleEntity(vertex, arm1, arm2, {
            style: style,
            id: options === null || options === void 0 ? void 0 : options.id,
            layoutId: options === null || options === void 0 ? void 0 : options.layoutId
        });
    };
    /**
     * Primary anchor for the measurement (the angle vertex).
     *
     * @returns Copy of {@link vertex} with `z: 0`
     */
    AcApMeasureAngleEntity.prototype.primaryPoint = function () {
        return { x: this.vertex.x, y: this.vertex.y, z: 0 };
    };
    /**
     * Serializes this angle measurement to a store/sidecar record.
     *
     * @param layoutId - Optional layout BTR id written onto the record
     * @param view - Optional view used to convert screen style to WCS
     * @returns Record with `type: 'angle'` and vertex/arm geometry
     */
    AcApMeasureAngleEntity.prototype.toRecord = function (layoutId, view) {
        return {
            id: this.entityId,
            type: 'angle',
            layoutId: layoutId,
            style: this.serializeStyle(view),
            geometry: {
                type: 'angle',
                vertex: { x: this.vertex.x, y: this.vertex.y },
                arm1: { x: this.arm1.x, y: this.arm1.y },
                arm2: { x: this.arm2.x, y: this.arm2.y }
            }
        };
    };
    /**
     * Draws arm lines + arc canvas, dots, badge, and commit extras.
     *
     * @param view - Active 2D view for HTML overlays
     * @param db - Database used to format the angle label
     * @returns World-draw result including redraw/dispose hooks
     */
    AcApMeasureAngleEntity.prototype.subWorldDrawWithDb = function (view, db) {
        var _this = this;
        var live = {
            vertex: { x: this.vertex.x, y: this.vertex.y },
            arm1: { x: this.arm1.x, y: this.arm1.y },
            arm2: { x: this.arm2.x, y: this.arm2.y }
        };
        var degrees = measureAngleDeg(live.vertex, live.arm1, live.arm2);
        var color = this.style.color;
        var layoutId = this.resolveLayoutId(view);
        var persistOverlay = new AcTrHtmlCanvasOverlay({
            id: "angle-canvas-".concat(this.entityId),
            container: view.container,
            layer: MEASUREMENT_LAYER,
            layoutId: layoutId
        });
        var dotV = new AcTrHtmlGrip({
            id: "".concat(this.entityId, "-dotV"),
            color: color,
            worldPosition: live.vertex,
            layer: MEASUREMENT_LAYER
        });
        var dot1 = new AcTrHtmlGrip({
            id: "".concat(this.entityId, "-dot1"),
            color: color,
            worldPosition: live.arm1,
            layer: MEASUREMENT_LAYER
        });
        var dot2 = new AcTrHtmlGrip({
            id: "".concat(this.entityId, "-dot2"),
            color: color,
            worldPosition: live.arm2,
            layer: MEASUREMENT_LAYER
        });
        var badge = new AcTrHtmlBadge({
            id: "".concat(this.entityId, "-badge"),
            color: color,
            text: formatMeasurementAngle(db, (degrees * Math.PI) / 180),
            worldPosition: measureAngleBadgeWorld(live.vertex, live.arm1, live.arm2),
            layer: MEASUREMENT_LAYER,
            fontSize: this.style.fontSize
        });
        this.seedOverlaySizes(view, [dotV, dot1, dot2, badge], [persistOverlay.canvas]);
        var paintAngle = function (paintStyle) {
            if (paintStyle === void 0) { paintStyle = _this.style; }
            return drawMeasureAngleArcOnCanvas(persistOverlay.canvas, view, live.vertex, live.arm1, live.arm2, paintStyle.color, acapMeasurementCanvasLineWidth(paintStyle.lineWeight));
        };
        paintAngle();
        var redrawPersist = function () { var _a; return paintAngle((_a = getMeasurementStyle(_this.entityId)) !== null && _a !== void 0 ? _a : _this.style); };
        view.events.viewChanged.addEventListener(redrawPersist);
        var group = this.createGroup(view)
            .add(dotV, dot1, dot2, badge)
            .addCanvas(persistOverlay);
        var cleanups = [
            function () { return view.events.viewChanged.removeEventListener(redrawPersist); }
        ];
        var pendingGrips = [];
        var dragStart = {
            vertex: __assign({}, live.vertex),
            arm1: __assign({}, live.arm1),
            arm2: __assign({}, live.arm2)
        };
        var refreshLive = function () {
            var _a;
            degrees = measureAngleDeg(live.vertex, live.arm1, live.arm2);
            badge.setText(formatMeasurementAngle(db, (degrees * Math.PI) / 180));
            acapPlaceOverlayHtml(view, badge, measureAngleBadgeWorld(live.vertex, live.arm1, live.arm2));
            paintAngle((_a = getMeasurementStyle(_this.entityId)) !== null && _a !== void 0 ? _a : _this.style);
            view.isHtmlDirty = true;
        };
        var beginEndpointDrag = function () {
            selectMeasurementGroup(view, _this.entityId);
            dragStart = {
                vertex: __assign({}, live.vertex),
                arm1: __assign({}, live.arm1),
                arm2: __assign({}, live.arm2)
            };
        };
        var commitEndpoints = function () {
            var delta = Math.hypot(live.vertex.x - dragStart.vertex.x, live.vertex.y - dragStart.vertex.y) +
                Math.hypot(live.arm1.x - dragStart.arm1.x, live.arm1.y - dragStart.arm1.y) +
                Math.hypot(live.arm2.x - dragStart.arm2.x, live.arm2.y - dragStart.arm2.y);
            if (delta < 1e-9) {
                refreshLive();
                return;
            }
            var snap = getMeasurementSnapshot(_this.entityId);
            var geometry = {
                type: 'angle',
                vertex: __assign({}, live.vertex),
                arm1: __assign({}, live.arm1),
                arm2: __assign({}, live.arm2)
            };
            var record = snap
                ? __assign(__assign({}, snap), { geometry: geometry }) : {
                id: _this.entityId,
                type: 'angle',
                layoutId: layoutId,
                style: _this.serializeStyle(view),
                geometry: geometry
            };
            runMeasurementEdit(view, 'Move Angle', function () {
                republishMeasurement(view, db, record);
            });
        };
        var bindDot = function (dot, key) {
            return acapBindOverlayPointerDrag({
                view: view,
                el: dot.element,
                onDragStart: beginEndpointDrag,
                onMove: function (point) {
                    live[key] = point;
                    acapPlaceOverlayHtml(view, dot, point);
                    refreshLive();
                },
                onCommit: commitEndpoints
            });
        };
        pendingGrips.push(function () {
            cleanups.push(bindDot(dotV, 'vertex'), bindDot(dot1, 'arm1'), bindDot(dot2, 'arm2'));
        });
        return {
            group: group,
            entityIds: [],
            dispose: function () {
                var e_1, _a;
                try {
                    for (var cleanups_1 = __values(cleanups), cleanups_1_1 = cleanups_1.next(); !cleanups_1_1.done; cleanups_1_1 = cleanups_1.next()) {
                        var fn = cleanups_1_1.value;
                        try {
                            fn();
                        }
                        catch (_b) {
                            // ignore
                        }
                    }
                }
                catch (e_1_1) { e_1 = { error: e_1_1 }; }
                finally {
                    try {
                        if (cleanups_1_1 && !cleanups_1_1.done && (_a = cleanups_1.return)) _a.call(cleanups_1);
                    }
                    finally { if (e_1) throw e_1.error; }
                }
            },
            bindGrips: function () {
                var e_2, _a;
                try {
                    for (var pendingGrips_1 = __values(pendingGrips), pendingGrips_1_1 = pendingGrips_1.next(); !pendingGrips_1_1.done; pendingGrips_1_1 = pendingGrips_1.next()) {
                        var bind = pendingGrips_1_1.value;
                        bind();
                    }
                }
                catch (e_2_1) { e_2 = { error: e_2_1 }; }
                finally {
                    try {
                        if (pendingGrips_1_1 && !pendingGrips_1_1.done && (_a = pendingGrips_1.return)) _a.call(pendingGrips_1);
                    }
                    finally { if (e_2) throw e_2.error; }
                }
            },
            extras: {
                style: this.style,
                value: { kind: 'angle', radians: (degrees * Math.PI) / 180 },
                snapshot: this.toRecord(layoutId, view),
                redraw: paintAngle
            }
        };
    };
    return AcApMeasureAngleEntity;
}(AcApMeasureEntity));
export { AcApMeasureAngleEntity };
//# sourceMappingURL=AcApMeasureAngleEntity.js.map