var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcTrHtmlBadge, AcTrHtmlCanvasOverlay, AcTrHtmlGrip } from '@mlightcad/three-renderer';
import { acapMeasurementCanvasLineWidth, formatMeasurementLength } from '../../../util';
import { ACAP_OVERLAY_ARROW_SIZE_PX, acapBindOverlayPointerDrag, acapPlaceOverlayHtml, acapScreenPxToWcs } from '../../overlay';
import { runMeasurementEdit } from '../AcApMeasurementHistory';
import { republishMeasurement } from '../AcApMeasurementRepublish';
import { getMeasurementSnapshot, getMeasurementStyle, MEASUREMENT_LAYER } from '../AcApMeasurementStore';
import { drawMeasureSegmentOnCanvas } from './AcApMeasureDrawUtil';
import { AcApMeasureEntity, newMeasureOverlayId } from './AcApMeasureEntity';
import { selectMeasurementGroup } from './AcApMeasureEntityGrips';
/**
 * Euclidean distance between two points in the XY plane (Z ignored).
 *
 * @param p1 - First endpoint
 * @param p2 - Second endpoint
 * @returns Distance `√((Δx)² + (Δy)²)`
 */
function calcDist(p1, p2) {
    var dx = p2.x - p1.x;
    var dy = p2.y - p1.y;
    return Math.sqrt(dx * dx + dy * dy);
}
/**
 * Distance measurement overlay entity.
 *
 * Renders an HTML canvas segment with arrows at both endpoints, HTML dots at
 * each end, and a length badge at the midpoint. Endpoint grips update the
 * measured value live and republish on commit.
 */
var AcApMeasureDistanceEntity = /** @class */ (function (_super) {
    __extends(AcApMeasureDistanceEntity, _super);
    /**
     * Creates a distance measure entity between two points.
     *
     * @param p1 - First endpoint
     * @param p2 - Second endpoint
     * @param options - Shared id, layout, and style options
     */
    function AcApMeasureDistanceEntity(p1, p2, options) {
        var _a;
        var _this = _super.call(this, (_a = options.id) !== null && _a !== void 0 ? _a : newMeasureOverlayId('dist'), options.layoutId, options.style, options.textHeightWcs, options.strokeWidthWcs, options.arrowSizeWcs) || this;
        _this.p1 = p1;
        _this.p2 = p2;
        return _this;
    }
    /**
     * Factory that builds a distance entity from endpoints and style.
     *
     * @param p1 - First endpoint
     * @param p2 - Second endpoint
     * @param style - Measurement visual style
     * @param options - Optional id and layout overrides
     * @returns New {@link AcApMeasureDistanceEntity}
     */
    AcApMeasureDistanceEntity.create = function (p1, p2, style, options) {
        return new AcApMeasureDistanceEntity(p1, p2, {
            style: style,
            id: options === null || options === void 0 ? void 0 : options.id,
            layoutId: options === null || options === void 0 ? void 0 : options.layoutId
        });
    };
    /**
     * Midpoint of the measured segment (badge / primary anchor).
     *
     * @returns World point at the average of {@link p1} and {@link p2}, `z: 0`
     */
    AcApMeasureDistanceEntity.prototype.primaryPoint = function () {
        return {
            x: (this.p1.x + this.p2.x) / 2,
            y: (this.p1.y + this.p2.y) / 2,
            z: 0
        };
    };
    /**
     * Serializes this distance measurement to a store/sidecar record.
     *
     * @param layoutId - Optional layout BTR id written onto the record
     * @param view - Optional view used to convert screen style to WCS
     * @returns Record with `type: 'distance'` and start/end geometry
     */
    AcApMeasureDistanceEntity.prototype.toRecord = function (layoutId, view) {
        var _a;
        var style = this.serializeStyle(view);
        var arrowSizeWcs = (_a = this.arrowSizeWcs) !== null && _a !== void 0 ? _a : (view ? acapScreenPxToWcs(ACAP_OVERLAY_ARROW_SIZE_PX, view) : undefined);
        if (arrowSizeWcs != null && arrowSizeWcs > 0) {
            style.arrowSizeWcs = arrowSizeWcs;
        }
        return {
            id: this.entityId,
            type: 'distance',
            layoutId: layoutId,
            style: style,
            geometry: {
                type: 'distance',
                start: { x: this.p1.x, y: this.p1.y },
                end: { x: this.p2.x, y: this.p2.y }
            }
        };
    };
    /**
     * Draws the distance canvas stroke, endpoint dots, length badge, and extras.
     *
     * @param view - Active 2D view for HTML overlays
     * @param db - Database used to format the length label
     * @returns World-draw result with dispose for the viewChanged listener
     */
    AcApMeasureDistanceEntity.prototype.subWorldDrawWithDb = function (view, db) {
        var _this = this;
        var live = {
            start: { x: this.p1.x, y: this.p1.y },
            end: { x: this.p2.x, y: this.p2.y }
        };
        var dist = calcDist(live.start, live.end);
        var color = this.style.color;
        var layoutId = this.resolveLayoutId(view);
        var midOf = function (a, b) { return ({
            x: (a.x + b.x) / 2,
            y: (a.y + b.y) / 2
        }); };
        var persistOverlay = new AcTrHtmlCanvasOverlay({
            id: "dist-canvas-".concat(this.entityId),
            container: view.container,
            layer: MEASUREMENT_LAYER,
            layoutId: layoutId
        });
        var dot1 = new AcTrHtmlGrip({
            id: "".concat(this.entityId, "-dot1"),
            color: color,
            worldPosition: live.start,
            layer: MEASUREMENT_LAYER
        });
        var dot2 = new AcTrHtmlGrip({
            id: "".concat(this.entityId, "-dot2"),
            color: color,
            worldPosition: live.end,
            layer: MEASUREMENT_LAYER
        });
        var badge = new AcTrHtmlBadge({
            id: "".concat(this.entityId, "-badge"),
            color: color,
            text: formatMeasurementLength(db, dist),
            worldPosition: midOf(live.start, live.end),
            layer: MEASUREMENT_LAYER,
            fontSize: this.style.fontSize
        });
        this.seedOverlaySizes(view, [dot1, dot2, badge], [persistOverlay.canvas]);
        var paintSegment = function (paintStyle) {
            if (paintStyle === void 0) { paintStyle = _this.style; }
            return drawMeasureSegmentOnCanvas(persistOverlay.canvas, view, live.start, live.end, paintStyle.color, acapMeasurementCanvasLineWidth(paintStyle.lineWeight), _this.strokeWidthWcs, _this.arrowSizeWcs);
        };
        paintSegment();
        var redrawPersist = function () { var _a; return paintSegment((_a = getMeasurementStyle(_this.entityId)) !== null && _a !== void 0 ? _a : _this.style); };
        view.events.viewChanged.addEventListener(redrawPersist);
        var group = this.createGroup(view)
            .add(dot1, dot2, badge)
            .addCanvas(persistOverlay);
        var cleanups = [
            function () { return view.events.viewChanged.removeEventListener(redrawPersist); }
        ];
        var pendingGrips = [];
        var dragStart = {
            start: __assign({}, live.start),
            end: __assign({}, live.end)
        };
        var refreshLive = function () {
            var _a;
            dist = calcDist(live.start, live.end);
            badge.setText(formatMeasurementLength(db, dist));
            acapPlaceOverlayHtml(view, badge, midOf(live.start, live.end));
            paintSegment((_a = getMeasurementStyle(_this.entityId)) !== null && _a !== void 0 ? _a : _this.style);
            view.isHtmlDirty = true;
        };
        var beginEndpointDrag = function () {
            selectMeasurementGroup(view, _this.entityId);
            dragStart = {
                start: __assign({}, live.start),
                end: __assign({}, live.end)
            };
        };
        var commitEndpoints = function () {
            var startDelta = Math.hypot(live.start.x - dragStart.start.x, live.start.y - dragStart.start.y);
            var endDelta = Math.hypot(live.end.x - dragStart.end.x, live.end.y - dragStart.end.y);
            if (startDelta < 1e-9 && endDelta < 1e-9) {
                refreshLive();
                return;
            }
            var snap = getMeasurementSnapshot(_this.entityId);
            var record = snap
                ? __assign(__assign({}, snap), { geometry: {
                        type: 'distance',
                        start: __assign({}, live.start),
                        end: __assign({}, live.end)
                    } }) : {
                id: _this.entityId,
                type: 'distance',
                layoutId: layoutId,
                style: _this.serializeStyle(view),
                geometry: {
                    type: 'distance',
                    start: __assign({}, live.start),
                    end: __assign({}, live.end)
                }
            };
            runMeasurementEdit(view, 'Move Distance', function () {
                republishMeasurement(view, db, record);
            });
        };
        pendingGrips.push(function () {
            cleanups.push(acapBindOverlayPointerDrag({
                view: view,
                el: dot1.element,
                onDragStart: beginEndpointDrag,
                onMove: function (point) {
                    live.start = point;
                    acapPlaceOverlayHtml(view, dot1, point);
                    refreshLive();
                },
                onCommit: commitEndpoints
            }), acapBindOverlayPointerDrag({
                view: view,
                el: dot2.element,
                onDragStart: beginEndpointDrag,
                onMove: function (point) {
                    live.end = point;
                    acapPlaceOverlayHtml(view, dot2, point);
                    refreshLive();
                },
                onCommit: commitEndpoints
            }));
        });
        return {
            group: group,
            entityIds: [],
            dispose: function () {
                var e_1, _a;
                try {
                    for (var cleanups_1 = __values(cleanups), cleanups_1_1 = cleanups_1.next(); !cleanups_1_1.done; cleanups_1_1 = cleanups_1.next()) {
                        var fn = cleanups_1_1.value;
                        try {
                            fn();
                        }
                        catch (_b) {
                            // ignore
                        }
                    }
                }
                catch (e_1_1) { e_1 = { error: e_1_1 }; }
                finally {
                    try {
                        if (cleanups_1_1 && !cleanups_1_1.done && (_a = cleanups_1.return)) _a.call(cleanups_1);
                    }
                    finally { if (e_1) throw e_1.error; }
                }
            },
            bindGrips: function () {
                var e_2, _a;
                try {
                    for (var pendingGrips_1 = __values(pendingGrips), pendingGrips_1_1 = pendingGrips_1.next(); !pendingGrips_1_1.done; pendingGrips_1_1 = pendingGrips_1.next()) {
                        var bind = pendingGrips_1_1.value;
                        bind();
                    }
                }
                catch (e_2_1) { e_2 = { error: e_2_1 }; }
                finally {
                    try {
                        if (pendingGrips_1_1 && !pendingGrips_1_1.done && (_a = pendingGrips_1.return)) _a.call(pendingGrips_1);
                    }
                    finally { if (e_2) throw e_2.error; }
                }
            },
            extras: {
                style: this.style,
                value: { kind: 'length', value: dist },
                snapshot: this.toRecord(layoutId, view),
                redraw: paintSegment
            }
        };
    };
    return AcApMeasureDistanceEntity;
}(AcApMeasureEntity));
export { AcApMeasureDistanceEntity };
//# sourceMappingURL=AcApMeasureDistanceEntity.js.map