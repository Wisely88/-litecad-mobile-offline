var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcTrHtmlBadge, AcTrHtmlCanvasOverlay, AcTrHtmlGrip } from '@mlightcad/three-renderer';
import { acapMeasurementCanvasLineWidth, formatMeasurementArea } from '../../../util';
import { acapBindOverlayPointerDrag, acapPlaceOverlayHtml } from '../../overlay';
import { runMeasurementEdit } from '../AcApMeasurementHistory';
import { republishMeasurement } from '../AcApMeasurementRepublish';
import { getMeasurementSnapshot, getMeasurementStyle, MEASUREMENT_LAYER } from '../AcApMeasurementStore';
import { drawMeasureAreaOnCanvas, measureCentroid, measureShoelaceArea } from './AcApMeasureDrawUtil';
import { AcApMeasureEntity } from './AcApMeasureEntity';
import { selectMeasurementGroup } from './AcApMeasureEntityGrips';
/**
 * Area measurement overlay entity.
 *
 * Renders a filled canvas polygon, HTML dots at each vertex, and an area
 * badge at the vertex centroid. Vertex grips update the measured value live
 * and republish on commit.
 */
var AcApMeasureAreaEntity = /** @class */ (function (_super) {
    __extends(AcApMeasureAreaEntity, _super);
    /**
     * Creates an area measure entity from a polygon vertex list.
     *
     * @param points - Polygon vertices in world coordinates
     * @param options - Shared id, layout, and style options
     */
    function AcApMeasureAreaEntity(points, options) {
        var _a;
        var _this = _super.call(this, (_a = options.id) !== null && _a !== void 0 ? _a : "area-".concat(Date.now()), options.layoutId, options.style, options.textHeightWcs, options.strokeWidthWcs) || this;
        _this.points = points;
        return _this;
    }
    /**
     * Factory that builds an area entity from vertices and style.
     *
     * @param points - Polygon vertices in world coordinates
     * @param style - Measurement visual style
     * @param options - Optional id and layout overrides
     * @returns New {@link AcApMeasureAreaEntity}
     */
    AcApMeasureAreaEntity.create = function (points, style, options) {
        return new AcApMeasureAreaEntity(points, {
            style: style,
            id: options === null || options === void 0 ? void 0 : options.id,
            layoutId: options === null || options === void 0 ? void 0 : options.layoutId
        });
    };
    /**
     * Primary anchor for the measurement (vertex centroid).
     *
     * @returns Centroid with `z: 0`, or `undefined` when there are no points
     */
    AcApMeasureAreaEntity.prototype.primaryPoint = function () {
        if (this.points.length === 0)
            return undefined;
        var c = measureCentroid(this.points);
        return { x: c.x, y: c.y, z: 0 };
    };
    /**
     * Serializes this area measurement to a store/sidecar record.
     *
     * @param layoutId - Optional layout BTR id written onto the record
     * @param view - Optional view used to convert screen style to WCS
     * @returns Record with `type: 'area'` and polygon point geometry
     */
    AcApMeasureAreaEntity.prototype.toRecord = function (layoutId, view) {
        return {
            id: this.entityId,
            type: 'area',
            layoutId: layoutId,
            style: this.serializeStyle(view),
            geometry: {
                type: 'area',
                points: this.points.map(function (p) { return ({ x: p.x, y: p.y }); })
            }
        };
    };
    /**
     * Draws the filled polygon canvas, vertex dots, area badge, and extras.
     *
     * When fewer than three points are present, returns an empty group and
     * empty extras without registering view listeners.
     *
     * @param view - Active 2D view for HTML overlays
     * @param db - Database used to format the area label
     * @returns World-draw result including redraw/dispose hooks when valid
     */
    AcApMeasureAreaEntity.prototype.subWorldDrawWithDb = function (view, db) {
        var _a;
        var _this = this;
        if (this.points.length < 3) {
            var group_1 = this.createGroup(view);
            return {
                group: group_1,
                entityIds: [],
                dispose: function () { return undefined; },
                extras: {}
            };
        }
        var live = this.points.map(function (p) { return ({ x: p.x, y: p.y }); });
        var area = measureShoelaceArea(live);
        var color = this.style.color;
        var layoutId = this.resolveLayoutId(view);
        var persistOverlay = new AcTrHtmlCanvasOverlay({
            id: "area-canvas-".concat(this.entityId),
            container: view.container,
            layer: MEASUREMENT_LAYER,
            layoutId: layoutId
        });
        var badge = new AcTrHtmlBadge({
            id: "".concat(this.entityId, "-badge"),
            color: color,
            text: formatMeasurementArea(db, area),
            worldPosition: measureCentroid(live),
            layer: MEASUREMENT_LAYER,
            fontSize: this.style.fontSize
        });
        var dots = live.map(function (p, i) {
            return new AcTrHtmlGrip({
                id: "".concat(_this.entityId, "-dot").concat(i),
                color: color,
                worldPosition: p,
                layer: MEASUREMENT_LAYER
            });
        });
        this.seedOverlaySizes(view, __spreadArray([badge], __read(dots), false), [persistOverlay.canvas]);
        var paintArea = function (paintStyle) {
            if (paintStyle === void 0) { paintStyle = _this.style; }
            return drawMeasureAreaOnCanvas(persistOverlay.canvas, view, live, paintStyle.color, acapMeasurementCanvasLineWidth(paintStyle.lineWeight));
        };
        paintArea();
        var redrawPersist = function () { var _a; return paintArea((_a = getMeasurementStyle(_this.entityId)) !== null && _a !== void 0 ? _a : _this.style); };
        view.events.viewChanged.addEventListener(redrawPersist);
        var group = (_a = this.createGroup(view))
            .add.apply(_a, __spreadArray([badge], __read(dots), false)).addCanvas(persistOverlay);
        var cleanups = [
            function () { return view.events.viewChanged.removeEventListener(redrawPersist); }
        ];
        var pendingGrips = [];
        var dragStart = live.map(function (p) { return (__assign({}, p)); });
        var refreshLive = function () {
            var _a;
            area = measureShoelaceArea(live);
            badge.setText(formatMeasurementArea(db, area));
            acapPlaceOverlayHtml(view, badge, measureCentroid(live));
            paintArea((_a = getMeasurementStyle(_this.entityId)) !== null && _a !== void 0 ? _a : _this.style);
            view.isHtmlDirty = true;
        };
        var beginEndpointDrag = function () {
            selectMeasurementGroup(view, _this.entityId);
            dragStart = live.map(function (p) { return (__assign({}, p)); });
        };
        var commitEndpoints = function () {
            var delta = 0;
            for (var i = 0; i < live.length; i++) {
                delta += Math.hypot(live[i].x - dragStart[i].x, live[i].y - dragStart[i].y);
            }
            if (delta < 1e-9) {
                refreshLive();
                return;
            }
            var snap = getMeasurementSnapshot(_this.entityId);
            var geometry = {
                type: 'area',
                points: live.map(function (p) { return ({ x: p.x, y: p.y }); })
            };
            var record = snap
                ? __assign(__assign({}, snap), { geometry: geometry }) : {
                id: _this.entityId,
                type: 'area',
                layoutId: layoutId,
                style: _this.serializeStyle(view),
                geometry: geometry
            };
            runMeasurementEdit(view, 'Move Area', function () {
                republishMeasurement(view, db, record);
            });
        };
        pendingGrips.push(function () {
            var _loop_1 = function (i) {
                var index = i;
                var dot = dots[index];
                cleanups.push(acapBindOverlayPointerDrag({
                    view: view,
                    el: dot.element,
                    onDragStart: beginEndpointDrag,
                    onMove: function (point) {
                        live[index] = point;
                        acapPlaceOverlayHtml(view, dot, point);
                        refreshLive();
                    },
                    onCommit: commitEndpoints
                }));
            };
            for (var i = 0; i < dots.length; i++) {
                _loop_1(i);
            }
        });
        return {
            group: group,
            entityIds: [],
            dispose: function () {
                var e_1, _a;
                try {
                    for (var cleanups_1 = __values(cleanups), cleanups_1_1 = cleanups_1.next(); !cleanups_1_1.done; cleanups_1_1 = cleanups_1.next()) {
                        var fn = cleanups_1_1.value;
                        try {
                            fn();
                        }
                        catch (_b) {
                            // ignore
                        }
                    }
                }
                catch (e_1_1) { e_1 = { error: e_1_1 }; }
                finally {
                    try {
                        if (cleanups_1_1 && !cleanups_1_1.done && (_a = cleanups_1.return)) _a.call(cleanups_1);
                    }
                    finally { if (e_1) throw e_1.error; }
                }
            },
            bindGrips: function () {
                var e_2, _a;
                try {
                    for (var pendingGrips_1 = __values(pendingGrips), pendingGrips_1_1 = pendingGrips_1.next(); !pendingGrips_1_1.done; pendingGrips_1_1 = pendingGrips_1.next()) {
                        var bind = pendingGrips_1_1.value;
                        bind();
                    }
                }
                catch (e_2_1) { e_2 = { error: e_2_1 }; }
                finally {
                    try {
                        if (pendingGrips_1_1 && !pendingGrips_1_1.done && (_a = pendingGrips_1.return)) _a.call(pendingGrips_1);
                    }
                    finally { if (e_2) throw e_2.error; }
                }
            },
            extras: {
                style: this.style,
                value: { kind: 'area', value: area },
                snapshot: this.toRecord(layoutId, view),
                redraw: paintArea
            }
        };
    };
    return AcApMeasureAreaEntity;
}(AcApMeasureEntity));
export { AcApMeasureAreaEntity };
//# sourceMappingURL=AcApMeasureAreaEntity.js.map