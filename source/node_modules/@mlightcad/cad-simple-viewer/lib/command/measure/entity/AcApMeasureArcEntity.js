var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcGeCircArc2d } from '@mlightcad/data-model';
import { AcTrHtmlBadge, AcTrHtmlCanvasOverlay, AcTrHtmlGrip } from '@mlightcad/three-renderer';
import { acapMeasurementCanvasLineWidth, formatMeasurementLength } from '../../../util';
import { acapBindOverlayPointerDrag, acapPlaceOverlayHtml } from '../../overlay';
import { runMeasurementEdit } from '../AcApMeasurementHistory';
import { republishMeasurement } from '../AcApMeasurementRepublish';
import { getMeasurementSnapshot, getMeasurementStyle, MEASUREMENT_LAYER } from '../AcApMeasurementStore';
import { drawMeasureArcOnCanvas } from './AcApMeasureDrawUtil';
import { AcApMeasureEntity } from './AcApMeasureEntity';
import { selectMeasurementGroup } from './AcApMeasureEntityGrips';
/**
 * Project a free point onto a circle (for legacy short-arc records).
 */
function projectOntoCircle(geom, point) {
    var dx = point.x - geom.cx;
    var dy = point.y - geom.cy;
    var len = Math.hypot(dx, dy);
    if (!(len > 1e-12)) {
        return { x: geom.cx + geom.r, y: geom.cy };
    }
    var s = geom.r / len;
    return { x: geom.cx + dx * s, y: geom.cy + dy * s };
}
/**
 * Arc-length measurement overlay entity.
 *
 * Renders a canvas stroke of the measured arc, HTML dots at the control
 * points, and a length badge at the arc midpoint. Endpoint grips update the
 * measured value live and republish on commit.
 */
var AcApMeasureArcEntity = /** @class */ (function (_super) {
    __extends(AcApMeasureArcEntity, _super);
    /**
     * Creates an arc-length measure entity on the given circle.
     *
     * @param geom - Circle center and radius
     * @param start - Arc start point in world XY
     * @param end - Arc end point in world XY
     * @param options - Shared id, layout, and style options
     * @param through - Point on the measured sweep; omit for legacy short-arc records
     */
    function AcApMeasureArcEntity(geom, start, end, options, through) {
        var _a;
        var _this = _super.call(this, (_a = options.id) !== null && _a !== void 0 ? _a : "arc-".concat(Date.now()), options.layoutId, options.style, options.textHeightWcs, options.strokeWidthWcs) || this;
        _this.geom = geom;
        _this.start = start;
        _this.end = end;
        _this.through = through;
        return _this;
    }
    /**
     * Reconstructs the measured sweep from stored control points.
     */
    AcApMeasureArcEntity.prototype.resolveMeasuredArc = function (geom, start, end, through) {
        if (through) {
            return AcGeCircArc2d.tryCreateByThreePoints(start, through, end);
        }
        return AcGeCircArc2d.tryCreateShorterArc(start, end, {
            x: geom.cx,
            y: geom.cy
        });
    };
    /**
     * Factory that builds an arc entity from circle geometry, endpoints, and style.
     *
     * @param geom - Circle center and radius
     * @param start - Arc start point in world XY
     * @param end - Arc end point in world XY
     * @param style - Measurement visual style
     * @param options - Optional id, layout, and through-point overrides
     * @returns New {@link AcApMeasureArcEntity}
     */
    AcApMeasureArcEntity.create = function (geom, start, end, style, options) {
        return new AcApMeasureArcEntity(geom, start, end, {
            style: style,
            id: options === null || options === void 0 ? void 0 : options.id,
            layoutId: options === null || options === void 0 ? void 0 : options.layoutId
        }, options === null || options === void 0 ? void 0 : options.through);
    };
    /**
     * Primary anchor for the measurement (midpoint of the measured sweep).
     *
     * @returns World point on the arc between {@link start} and {@link end}
     */
    AcApMeasureArcEntity.prototype.primaryPoint = function () {
        var _a;
        var mid = (_a = this.resolveMeasuredArc(this.geom, this.start, this.end, this.through)) === null || _a === void 0 ? void 0 : _a.midPoint;
        return mid
            ? { x: mid.x, y: mid.y, z: 0 }
            : { x: this.start.x, y: this.start.y, z: 0 };
    };
    /**
     * Serializes this arc measurement to a store/sidecar record.
     *
     * @param layoutId - Optional layout BTR id written onto the record
     * @param view - Optional view used to convert screen style to WCS
     * @returns Record with `type: 'arc'` and center/radius/start/through/end geometry
     */
    AcApMeasureArcEntity.prototype.toRecord = function (layoutId, view) {
        return {
            id: this.entityId,
            type: 'arc',
            layoutId: layoutId,
            style: this.serializeStyle(view),
            geometry: __assign({ type: 'arc', center: { x: this.geom.cx, y: this.geom.cy }, radius: this.geom.r, start: { x: this.start.x, y: this.start.y }, end: { x: this.end.x, y: this.end.y } }, (this.through
                ? { through: { x: this.through.x, y: this.through.y } }
                : {}))
        };
    };
    /**
     * Draws the arc canvas stroke, control-point dots, length badge, and extras.
     *
     * @param view - Active 2D view for HTML overlays
     * @param db - Database used to format the arc-length label
     * @returns World-draw result including redraw/dispose hooks
     */
    AcApMeasureArcEntity.prototype.subWorldDrawWithDb = function (view, db) {
        var _a;
        var _this = this;
        var _b, _c;
        var hasThrough = this.through != null;
        var live = {
            geom: __assign({}, this.geom),
            start: __assign({}, this.start),
            end: __assign({}, this.end),
            through: this.through ? __assign({}, this.through) : undefined
        };
        var resolveArc = function () {
            return _this.resolveMeasuredArc(live.geom, live.start, live.end, live.through);
        };
        var syncGeomFromThreePoints = function () {
            if (!live.through)
                return true;
            var arc = AcGeCircArc2d.tryCreateByThreePoints(live.start, live.through, live.end);
            if (!arc)
                return false;
            live.geom = {
                cx: arc.center.x,
                cy: arc.center.y,
                r: arc.radius
            };
            return true;
        };
        var arcLen = (_c = (_b = resolveArc()) === null || _b === void 0 ? void 0 : _b.length) !== null && _c !== void 0 ? _c : 0;
        var midPoint = function () {
            var _a;
            var mid = (_a = resolveArc()) === null || _a === void 0 ? void 0 : _a.midPoint;
            return mid
                ? { x: mid.x, y: mid.y }
                : { x: live.start.x, y: live.start.y };
        };
        var color = this.style.color;
        var layoutId = this.resolveLayoutId(view);
        var persistOverlay = new AcTrHtmlCanvasOverlay({
            id: "arc-canvas-".concat(this.entityId),
            container: view.container,
            layer: MEASUREMENT_LAYER,
            layoutId: layoutId
        });
        var dot1 = new AcTrHtmlGrip({
            id: "".concat(this.entityId, "-dot1"),
            color: color,
            worldPosition: live.start,
            layer: MEASUREMENT_LAYER
        });
        var dotThrough = live.through
            ? new AcTrHtmlGrip({
                id: "".concat(this.entityId, "-dot-through"),
                color: color,
                worldPosition: live.through,
                layer: MEASUREMENT_LAYER
            })
            : undefined;
        var dot2 = new AcTrHtmlGrip({
            id: "".concat(this.entityId, "-dot2"),
            color: color,
            worldPosition: live.end,
            layer: MEASUREMENT_LAYER
        });
        var badge = new AcTrHtmlBadge({
            id: "".concat(this.entityId, "-badge"),
            color: color,
            text: formatMeasurementLength(db, arcLen),
            worldPosition: midPoint(),
            layer: MEASUREMENT_LAYER,
            fontSize: this.style.fontSize
        });
        this.seedOverlaySizes(view, dotThrough ? [dot1, dotThrough, dot2, badge] : [dot1, dot2, badge], [persistOverlay.canvas]);
        var paintArc = function (paintStyle) {
            if (paintStyle === void 0) { paintStyle = _this.style; }
            return drawMeasureArcOnCanvas(persistOverlay.canvas, view, live.geom, live.start, live.end, paintStyle.color, acapMeasurementCanvasLineWidth(paintStyle.lineWeight), live.through);
        };
        paintArc();
        var redrawPersist = function () { var _a; return paintArc((_a = getMeasurementStyle(_this.entityId)) !== null && _a !== void 0 ? _a : _this.style); };
        view.events.viewChanged.addEventListener(redrawPersist);
        var group = (_a = this.createGroup(view))
            .add.apply(_a, __spreadArray(__spreadArray([dot1], __read((dotThrough ? [dotThrough] : [])), false), [dot2,
            badge], false)).addCanvas(persistOverlay);
        var cleanups = [
            function () { return view.events.viewChanged.removeEventListener(redrawPersist); }
        ];
        var pendingGrips = [];
        var dragStart = {
            start: __assign({}, live.start),
            end: __assign({}, live.end),
            through: live.through ? __assign({}, live.through) : undefined,
            geom: __assign({}, live.geom)
        };
        var refreshLive = function () {
            var _a, _b;
            var valid = syncGeomFromThreePoints();
            var arc = valid ? resolveArc() : null;
            arcLen = (_a = arc === null || arc === void 0 ? void 0 : arc.length) !== null && _a !== void 0 ? _a : 0;
            badge.setText(formatMeasurementLength(db, arcLen));
            acapPlaceOverlayHtml(view, badge, midPoint());
            paintArc((_b = getMeasurementStyle(_this.entityId)) !== null && _b !== void 0 ? _b : _this.style);
            view.isHtmlDirty = true;
        };
        var restoreDragStart = function () {
            live.start = __assign({}, dragStart.start);
            live.end = __assign({}, dragStart.end);
            live.through = dragStart.through ? __assign({}, dragStart.through) : undefined;
            live.geom = __assign({}, dragStart.geom);
            acapPlaceOverlayHtml(view, dot1, live.start);
            acapPlaceOverlayHtml(view, dot2, live.end);
            if (dotThrough && live.through) {
                acapPlaceOverlayHtml(view, dotThrough, live.through);
            }
            refreshLive();
        };
        var beginEndpointDrag = function () {
            selectMeasurementGroup(view, _this.entityId);
            dragStart = {
                start: __assign({}, live.start),
                end: __assign({}, live.end),
                through: live.through ? __assign({}, live.through) : undefined,
                geom: __assign({}, live.geom)
            };
        };
        var commitEndpoints = function () {
            var delta = Math.hypot(live.start.x - dragStart.start.x, live.start.y - dragStart.start.y) +
                Math.hypot(live.end.x - dragStart.end.x, live.end.y - dragStart.end.y) +
                (live.through && dragStart.through
                    ? Math.hypot(live.through.x - dragStart.through.x, live.through.y - dragStart.through.y)
                    : 0);
            if (delta < 1e-9) {
                refreshLive();
                return;
            }
            if (!syncGeomFromThreePoints()) {
                restoreDragStart();
                return;
            }
            var snap = getMeasurementSnapshot(_this.entityId);
            var geometry = __assign({ type: 'arc', center: { x: live.geom.cx, y: live.geom.cy }, radius: live.geom.r, start: __assign({}, live.start), end: __assign({}, live.end) }, (live.through ? { through: __assign({}, live.through) } : {}));
            var record = snap
                ? __assign(__assign({}, snap), { geometry: geometry }) : {
                id: _this.entityId,
                type: 'arc',
                layoutId: layoutId,
                style: _this.serializeStyle(view),
                geometry: geometry
            };
            runMeasurementEdit(view, 'Move Arc', function () {
                republishMeasurement(view, db, record);
            });
        };
        var constrainEndpoint = function (point) {
            return hasThrough ? point : projectOntoCircle(live.geom, point);
        };
        pendingGrips.push(function () {
            cleanups.push(acapBindOverlayPointerDrag({
                view: view,
                el: dot1.element,
                onDragStart: beginEndpointDrag,
                onMove: function (point) {
                    live.start = constrainEndpoint(point);
                    acapPlaceOverlayHtml(view, dot1, live.start);
                    refreshLive();
                },
                onCommit: commitEndpoints
            }), acapBindOverlayPointerDrag({
                view: view,
                el: dot2.element,
                onDragStart: beginEndpointDrag,
                onMove: function (point) {
                    live.end = constrainEndpoint(point);
                    acapPlaceOverlayHtml(view, dot2, live.end);
                    refreshLive();
                },
                onCommit: commitEndpoints
            }));
            if (dotThrough && live.through) {
                cleanups.push(acapBindOverlayPointerDrag({
                    view: view,
                    el: dotThrough.element,
                    onDragStart: beginEndpointDrag,
                    onMove: function (point) {
                        live.through = point;
                        acapPlaceOverlayHtml(view, dotThrough, point);
                        refreshLive();
                    },
                    onCommit: commitEndpoints
                }));
            }
        });
        return {
            group: group,
            entityIds: [],
            dispose: function () {
                var e_1, _a;
                try {
                    for (var cleanups_1 = __values(cleanups), cleanups_1_1 = cleanups_1.next(); !cleanups_1_1.done; cleanups_1_1 = cleanups_1.next()) {
                        var fn = cleanups_1_1.value;
                        try {
                            fn();
                        }
                        catch (_b) {
                            // ignore
                        }
                    }
                }
                catch (e_1_1) { e_1 = { error: e_1_1 }; }
                finally {
                    try {
                        if (cleanups_1_1 && !cleanups_1_1.done && (_a = cleanups_1.return)) _a.call(cleanups_1);
                    }
                    finally { if (e_1) throw e_1.error; }
                }
            },
            bindGrips: function () {
                var e_2, _a;
                try {
                    for (var pendingGrips_1 = __values(pendingGrips), pendingGrips_1_1 = pendingGrips_1.next(); !pendingGrips_1_1.done; pendingGrips_1_1 = pendingGrips_1.next()) {
                        var bind = pendingGrips_1_1.value;
                        bind();
                    }
                }
                catch (e_2_1) { e_2 = { error: e_2_1 }; }
                finally {
                    try {
                        if (pendingGrips_1_1 && !pendingGrips_1_1.done && (_a = pendingGrips_1.return)) _a.call(pendingGrips_1);
                    }
                    finally { if (e_2) throw e_2.error; }
                }
            },
            extras: {
                style: this.style,
                value: { kind: 'length', value: arcLen },
                snapshot: this.toRecord(layoutId, view),
                redraw: paintArc
            }
        };
    };
    return AcApMeasureArcEntity;
}(AcApMeasureEntity));
export { AcApMeasureArcEntity };
//# sourceMappingURL=AcApMeasureArcEntity.js.map