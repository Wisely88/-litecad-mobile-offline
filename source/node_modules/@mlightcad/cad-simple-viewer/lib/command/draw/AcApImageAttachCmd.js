var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
import { AcDbRasterImage, AcDbRasterImageDef, AcGePoint3d } from '@mlightcad/data-model';
import { AcApDocManager } from '../../app';
import { AcEdCommand, AcEdOpenMode, AcEdPreviewJig, AcEdPromptAngleOptions, AcEdPromptDoubleOptions, AcEdPromptPointOptions, AcEdPromptStatus } from '../../editor';
import { AcApI18n } from '../../i18n';
import { acapRunDatabaseEdit } from '../../util/AcApDatabaseEdit';
/** Accept list for raster image file pickers (AutoCAD IMAGEATTACH formats). */
var IMAGE_FILE_ACCEPT = '.png,.jpg,.jpeg,.bmp,.gif,.tif,.tiff';
/** Max side length (drawing units) used when scale factor is 1. */
var DEFAULT_MAX_SIDE = 100;
function uniqueImageDefName(db, fileName) {
    if (!db.objects.imageDefinition.has(fileName))
        return fileName;
    var dot = fileName.lastIndexOf('.');
    var stem = dot > 0 ? fileName.slice(0, dot) : fileName;
    var ext = dot > 0 ? fileName.slice(dot) : '';
    var index = 1;
    while (db.objects.imageDefinition.has("".concat(stem, "_").concat(index).concat(ext))) {
        index++;
    }
    return "".concat(stem, "_").concat(index).concat(ext);
}
function computeBaseSize(pixelWidth, pixelHeight) {
    var safeWidth = Math.max(1, pixelWidth);
    var safeHeight = Math.max(1, pixelHeight);
    var maxSide = Math.max(safeWidth, safeHeight);
    var factor = maxSide > 0 ? DEFAULT_MAX_SIDE / maxSide : 1;
    return {
        width: Math.max(1, safeWidth * factor),
        height: Math.max(1, safeHeight * factor)
    };
}
function decodeImageSize(file) {
    return __awaiter(this, void 0, void 0, function () {
        var objectUrl;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    objectUrl = URL.createObjectURL(file);
                    _a.label = 1;
                case 1:
                    _a.trys.push([1, , 3, 4]);
                    return [4 /*yield*/, new Promise(function (resolve, reject) {
                            var img = new Image();
                            img.onload = function () {
                                return resolve({
                                    width: img.naturalWidth || 100,
                                    height: img.naturalHeight || 100
                                });
                            };
                            img.onerror = function () { return reject(new Error('Failed to decode image')); };
                            img.src = objectUrl;
                        })];
                case 2: return [2 /*return*/, _a.sent()];
                case 3:
                    URL.revokeObjectURL(objectUrl);
                    return [7 /*endfinally*/];
                case 4: return [2 /*return*/];
            }
        });
    });
}
function pickImageFile() {
    return new Promise(function (resolve) {
        var input = document.createElement('input');
        input.type = 'file';
        input.accept = IMAGE_FILE_ACCEPT;
        input.style.display = 'none';
        document.body.appendChild(input);
        var settled = false;
        var finish = function (file) {
            if (settled)
                return;
            settled = true;
            input.removeEventListener('change', onChange);
            input.removeEventListener('cancel', onCancel);
            input.remove();
            resolve(file);
        };
        var onChange = function () { var _a; return finish((_a = input.files) === null || _a === void 0 ? void 0 : _a[0]); };
        var onCancel = function () { return finish(undefined); };
        input.addEventListener('change', onChange);
        input.addEventListener('cancel', onCancel);
        input.click();
    });
}
/**
 * Preview jig for IMAGEATTACH — shows the raster frame while the user picks
 * insertion point, scale, or rotation.
 */
var AcApImageAttachJig = /** @class */ (function (_super) {
    __extends(AcApImageAttachJig, _super);
    function AcApImageAttachJig(view, file, baseWidth, baseHeight) {
        var _this = _super.call(this, view) || this;
        _this._mode = 'point';
        _this._position = new AcGePoint3d();
        _this._scale = 1;
        _this._rotationRad = 0;
        _this._baseWidth = baseWidth;
        _this._baseHeight = baseHeight;
        _this._entity = new AcDbRasterImage();
        _this._entity.image = file;
        _this._entity.width = baseWidth;
        _this._entity.height = baseHeight;
        _this.applyGeometry();
        return _this;
    }
    Object.defineProperty(AcApImageAttachJig.prototype, "entity", {
        get: function () {
            return this._entity;
        },
        enumerable: false,
        configurable: true
    });
    AcApImageAttachJig.prototype.setMode = function (mode) {
        this._mode = mode;
    };
    AcApImageAttachJig.prototype.setPosition = function (point) {
        this._position = new AcGePoint3d(point);
        this.applyGeometry();
    };
    AcApImageAttachJig.prototype.setScale = function (scale) {
        this._scale = scale;
        this.applyGeometry();
    };
    AcApImageAttachJig.prototype.setRotationRad = function (rotationRad) {
        this._rotationRad = rotationRad;
        this.applyGeometry();
    };
    AcApImageAttachJig.prototype.update = function (value) {
        if (this._mode === 'point' && typeof value !== 'number') {
            this.setPosition(value);
            return;
        }
        if (typeof value !== 'number')
            return;
        if (this._mode === 'scale') {
            this.setScale(value > 0 ? value : this._scale);
            return;
        }
        if (this._mode === 'rotation') {
            // Angle prompts provide degrees.
            this.setRotationRad((value * Math.PI) / 180);
        }
    };
    AcApImageAttachJig.prototype.applyGeometry = function () {
        this._entity.position = new AcGePoint3d(this._position);
        this._entity.width = Math.max(1e-6, this._baseWidth * this._scale);
        this._entity.height = Math.max(1e-6, this._baseHeight * this._scale);
        this._entity.rotation = this._rotationRad;
    };
    return AcApImageAttachJig;
}(AcEdPreviewJig));
/**
 * IMAGEATTACH (alias: IAT) — attaches a raster image as an external reference.
 *
 * Flow (AutoCAD-aligned):
 * 1. Pick an image file (JPG / PNG / BMP / TIF / …)
 * 2. Specify insertion point
 * 3. Specify scale factor (default 1)
 * 4. Specify rotation angle (default 0)
 * 5. Append `AcDbRasterImage` linked to a new `AcDbRasterImageDef`
 */
var AcApImageAttachCmd = /** @class */ (function (_super) {
    __extends(AcApImageAttachCmd, _super);
    function AcApImageAttachCmd() {
        var _this = _super.call(this) || this;
        _this.mode = AcEdOpenMode.Write;
        return _this;
    }
    AcApImageAttachCmd.prototype.execute = function (context) {
        return __awaiter(this, void 0, void 0, function () {
            var file, pixelSize, _a, baseSize, jig, insertionPrompt, insertionResult, insertionPoint_1, scalePrompt, scaleResult, scale, rotationPrompt, rotationResult, rotationDeg, rotationRad_1, db_1, defName_1, width_1, height_1;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0: return [4 /*yield*/, pickImageFile()];
                    case 1:
                        file = _b.sent();
                        if (!file)
                            return [2 /*return*/];
                        _b.label = 2;
                    case 2:
                        _b.trys.push([2, 4, , 5]);
                        return [4 /*yield*/, decodeImageSize(file)];
                    case 3:
                        pixelSize = _b.sent();
                        return [3 /*break*/, 5];
                    case 4:
                        _a = _b.sent();
                        this.showMessage(AcApI18n.t('jig.imageattach.decodeFailed'), 'error');
                        return [2 /*return*/];
                    case 5:
                        baseSize = computeBaseSize(pixelSize.width, pixelSize.height);
                        jig = new AcApImageAttachJig(context.view, file, baseSize.width, baseSize.height);
                        _b.label = 6;
                    case 6:
                        _b.trys.push([6, , 10, 11]);
                        jig.setMode('point');
                        insertionPrompt = new AcEdPromptPointOptions(AcApI18n.t('jig.imageattach.insertionPoint'));
                        insertionPrompt.jig = jig;
                        return [4 /*yield*/, AcApDocManager.instance.editor.getPoint(insertionPrompt)];
                    case 7:
                        insertionResult = _b.sent();
                        if (insertionResult.status !== AcEdPromptStatus.OK ||
                            !insertionResult.value) {
                            return [2 /*return*/];
                        }
                        insertionPoint_1 = new AcGePoint3d(insertionResult.value);
                        jig.setPosition(insertionPoint_1);
                        jig.setMode('scale');
                        scalePrompt = new AcEdPromptDoubleOptions(AcApI18n.t('jig.imageattach.scale'));
                        scalePrompt.allowNone = true;
                        scalePrompt.allowNegative = false;
                        scalePrompt.allowZero = false;
                        scalePrompt.useDefaultValue = true;
                        scalePrompt.defaultValue = 1;
                        scalePrompt.jig = jig;
                        return [4 /*yield*/, AcApDocManager.instance.editor.getDouble(scalePrompt)];
                    case 8:
                        scaleResult = _b.sent();
                        if (scaleResult.status === AcEdPromptStatus.Cancel ||
                            scaleResult.status === AcEdPromptStatus.Error) {
                            return [2 /*return*/];
                        }
                        scale = scaleResult.status === AcEdPromptStatus.OK &&
                            scaleResult.value !== undefined
                            ? scaleResult.value
                            : 1;
                        if (!(scale > 0)) {
                            this.showMessage(AcApI18n.t('jig.imageattach.invalidScale'), 'warning');
                            return [2 /*return*/];
                        }
                        jig.setScale(scale);
                        jig.setMode('rotation');
                        rotationPrompt = new AcEdPromptAngleOptions(AcApI18n.t('jig.imageattach.rotation'));
                        rotationPrompt.allowNone = true;
                        rotationPrompt.allowNegative = true;
                        rotationPrompt.allowZero = true;
                        rotationPrompt.useDefaultValue = true;
                        rotationPrompt.defaultValue = 0;
                        rotationPrompt.useBasePoint = true;
                        rotationPrompt.useDashedLine = true;
                        rotationPrompt.basePoint = insertionPoint_1;
                        rotationPrompt.jig = jig;
                        return [4 /*yield*/, AcApDocManager.instance.editor.getAngle(rotationPrompt)];
                    case 9:
                        rotationResult = _b.sent();
                        if (rotationResult.status === AcEdPromptStatus.Cancel ||
                            rotationResult.status === AcEdPromptStatus.Error) {
                            return [2 /*return*/];
                        }
                        rotationDeg = rotationResult.status === AcEdPromptStatus.OK &&
                            rotationResult.value !== undefined
                            ? rotationResult.value
                            : 0;
                        rotationRad_1 = (rotationDeg * Math.PI) / 180;
                        db_1 = context.doc.database;
                        defName_1 = uniqueImageDefName(db_1, file.name);
                        width_1 = Math.max(1e-6, baseSize.width * scale);
                        height_1 = Math.max(1e-6, baseSize.height * scale);
                        acapRunDatabaseEdit(db_1, 'IMAGEATTACH', function () {
                            var imageDef = new AcDbRasterImageDef();
                            imageDef.sourceFileName = file.name;
                            db_1.objects.imageDefinition.setAt(defName_1, imageDef);
                            var image = new AcDbRasterImage();
                            image.position = insertionPoint_1;
                            image.width = width_1;
                            image.height = height_1;
                            image.rotation = rotationRad_1;
                            image.image = file;
                            image.imageDefId = imageDef.objectId;
                            db_1.tables.blockTable.modelSpace.appendEntity(image);
                        });
                        return [3 /*break*/, 11];
                    case 10:
                        jig.end();
                        return [7 /*endfinally*/];
                    case 11: return [2 /*return*/];
                }
            });
        });
    };
    return AcApImageAttachCmd;
}(AcEdCommand));
export { AcApImageAttachCmd };
//# sourceMappingURL=AcApImageAttachCmd.js.map