var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcDbDatabase, AcDbFileType, AcDbPolyline, AcDbViewport, AcGeBox3d, AcGePoint2d, AcGePoint3d } from '@mlightcad/data-model';
import { AcApDocManager, AcApXrefManager } from '../../app';
import { AcEdCommand, AcEdOpenMode, AcEdPreviewJig, AcEdPromptAngleOptions, AcEdPromptDoubleOptions, AcEdPromptPointOptions, AcEdPromptStatus, eventBus } from '../../editor';
import { AcApI18n } from '../../i18n';
import { acapRunDatabaseEdit } from '../../util/AcApDatabaseEdit';
import { acapWithSecondaryDatabase } from '../../util/AcApSecondaryDatabase';
/** Accept list for drawing file pickers (AutoCAD XATTACH formats). */
var XREF_FILE_ACCEPT = '.dwg,.dxf';
/** Fallback preview size when the source drawing has empty extents. */
var DEFAULT_EXTENT_SIZE = 100;
/**
 * Resolves a local filesystem path for a browser/Electron {@link File}.
 * Falls back to {@link File.name} when the absolute path is unavailable.
 */
function resolveLocalFilePath(file) {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    var injected = (_c = (_b = (_a = window).mlightcadGetLocalFilePath) === null || _b === void 0 ? void 0 : _b.call(_a, file)) === null || _c === void 0 ? void 0 : _c.trim();
    if (injected)
        return injected;
    var electronWebUtils = (_d = window.electron) === null || _d === void 0 ? void 0 : _d.webUtils;
    var fromElectron = (_f = (_e = electronWebUtils === null || electronWebUtils === void 0 ? void 0 : electronWebUtils.getPathForFile) === null || _e === void 0 ? void 0 : _e.call(electronWebUtils, file)) === null || _f === void 0 ? void 0 : _f.trim();
    if (fromElectron)
        return fromElectron;
    var legacy = file;
    var fromPath = (_g = legacy.path) === null || _g === void 0 ? void 0 : _g.trim();
    if (fromPath)
        return fromPath;
    var fromMoz = (_h = legacy.mozFullPath) === null || _h === void 0 ? void 0 : _h.trim();
    if (fromMoz)
        return fromMoz;
    return file.name;
}
function xrefBaseName(fileName) {
    return fileName.replace(/\.(dwg|dxf)$/i, '') || fileName;
}
function uniqueXrefName(db, fileName) {
    var base = xrefBaseName(fileName);
    if (!db.tables.blockTable.has(base))
        return base;
    var index = 1;
    while (db.tables.blockTable.has("".concat(base, "_").concat(index))) {
        index++;
    }
    return "".concat(base, "_").concat(index);
}
function pickXrefFile() {
    return new Promise(function (resolve) {
        var input = document.createElement('input');
        input.type = 'file';
        input.accept = XREF_FILE_ACCEPT;
        input.style.display = 'none';
        document.body.appendChild(input);
        var settled = false;
        var finish = function (file) {
            if (settled)
                return;
            settled = true;
            input.removeEventListener('change', onChange);
            input.removeEventListener('cancel', onCancel);
            input.remove();
            resolve(file);
        };
        var onChange = function () { var _a; return finish((_a = input.files) === null || _a === void 0 ? void 0 : _a[0]); };
        var onCancel = function () { return finish(undefined); };
        input.addEventListener('change', onChange);
        input.addEventListener('cancel', onCancel);
        input.click();
    });
}
function resolveSourceExtents(sourceDb) {
    var e_1, _a;
    var _b, _c, _d;
    var extents = (_d = (_c = (_b = sourceDb.extents) === null || _b === void 0 ? void 0 : _b.clone) === null || _c === void 0 ? void 0 : _c.call(_b)) !== null && _d !== void 0 ? _d : new AcGeBox3d();
    if (!extents.isEmpty())
        return extents;
    var box = new AcGeBox3d();
    try {
        for (var _e = __values(sourceDb.tables.blockTable.modelSpace.newIterator()), _f = _e.next(); !_f.done; _f = _e.next()) {
            var entity = _f.value;
            if (entity instanceof AcDbViewport)
                continue;
            try {
                var entityBox = entity.geometricExtents;
                if (entityBox && !entityBox.isEmpty()) {
                    box.expandByPoint(entityBox.min);
                    box.expandByPoint(entityBox.max);
                }
            }
            catch (_g) {
                // Skip entities that cannot report extents.
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (_f && !_f.done && (_a = _e.return)) _a.call(_e);
        }
        finally { if (e_1) throw e_1.error; }
    }
    if (!box.isEmpty())
        return box;
    return new AcGeBox3d(new AcGePoint3d(0, 0, 0), new AcGePoint3d(DEFAULT_EXTENT_SIZE, DEFAULT_EXTENT_SIZE, 0));
}
/**
 * Preview jig for XATTACH — shows the xref extents frame while the user picks
 * insertion point, scale, or rotation.
 */
var AcApXAttachJig = /** @class */ (function (_super) {
    __extends(AcApXAttachJig, _super);
    function AcApXAttachJig(view, extents) {
        var _this = _super.call(this, view) || this;
        _this._mode = 'point';
        _this._position = new AcGePoint3d();
        _this._scale = 1;
        _this._rotationRad = 0;
        var min = extents.min;
        var max = extents.max;
        _this._localCorners = [
            new AcGePoint2d(min.x, min.y),
            new AcGePoint2d(max.x, min.y),
            new AcGePoint2d(max.x, max.y),
            new AcGePoint2d(min.x, max.y)
        ];
        _this._entity = AcDbPolyline.from2dPoints(_this._localCorners.map(function (p) { return p.clone(); }), true);
        _this.applyGeometry();
        return _this;
    }
    Object.defineProperty(AcApXAttachJig.prototype, "entity", {
        get: function () {
            return this._entity;
        },
        enumerable: false,
        configurable: true
    });
    AcApXAttachJig.prototype.setMode = function (mode) {
        this._mode = mode;
    };
    AcApXAttachJig.prototype.setPosition = function (point) {
        this._position = new AcGePoint3d(point);
        this.applyGeometry();
    };
    AcApXAttachJig.prototype.setScale = function (scale) {
        this._scale = scale;
        this.applyGeometry();
    };
    AcApXAttachJig.prototype.setRotationRad = function (rotationRad) {
        this._rotationRad = rotationRad;
        this.applyGeometry();
    };
    AcApXAttachJig.prototype.update = function (value) {
        if (this._mode === 'point' && typeof value !== 'number') {
            this.setPosition(value);
            return;
        }
        if (typeof value !== 'number')
            return;
        if (this._mode === 'scale') {
            this.setScale(value > 0 ? value : this._scale);
            return;
        }
        if (this._mode === 'rotation') {
            this.setRotationRad((value * Math.PI) / 180);
        }
    };
    AcApXAttachJig.prototype.applyGeometry = function () {
        var _this = this;
        var cos = Math.cos(this._rotationRad);
        var sin = Math.sin(this._rotationRad);
        var scale = this._scale;
        var worldPoints = this._localCorners.map(function (local) {
            var x = local.x * scale;
            var y = local.y * scale;
            return new AcGePoint2d(_this._position.x + x * cos - y * sin, _this._position.y + x * sin + y * cos);
        });
        this._entity.reset(false);
        worldPoints.forEach(function (point, index) {
            _this._entity.addVertexAt(index, point);
        });
        this._entity.closed = true;
    };
    return AcApXAttachJig;
}(AcEdPreviewJig));
/**
 * XATTACH (alias: XA) — attaches a DWG/DXF drawing as an external reference.
 *
 * Flow (AutoCAD-aligned):
 * 1. Pick a DWG/DXF file
 * 2. Specify insertion point
 * 3. Specify scale factor (default 1)
 * 4. Specify rotation angle (default 0)
 * 5. Create an empty xref {@link AcDbBlockTableRecord} + INSERT on the host
 *    (metadata only), and render source geometry as a read-only overlay.
 */
var AcApXAttachCmd = /** @class */ (function (_super) {
    __extends(AcApXAttachCmd, _super);
    function AcApXAttachCmd() {
        var _this = _super.call(this) || this;
        _this.mode = AcEdOpenMode.Write;
        return _this;
    }
    AcApXAttachCmd.prototype.execute = function (context) {
        return __awaiter(this, void 0, void 0, function () {
            var file, extension, sourceDb, buffer_1, _a, extents, jig, insertionPrompt, insertionResult, insertionPoint, scalePrompt, scaleResult, scale, rotationPrompt, rotationResult, rotationDeg, rotationRad, db_1, blockName_1, filePath_1, transform_1, session_1;
            var _this = this;
            var _b;
            return __generator(this, function (_c) {
                switch (_c.label) {
                    case 0: return [4 /*yield*/, pickXrefFile()];
                    case 1:
                        file = _c.sent();
                        if (!file)
                            return [2 /*return*/];
                        extension = (_b = file.name.split('.').pop()) === null || _b === void 0 ? void 0 : _b.toLocaleLowerCase();
                        if (extension !== 'dwg' && extension !== 'dxf') {
                            this.showMessage(AcApI18n.t('jig.xattach.unsupportedFile'), 'warning');
                            return [2 /*return*/];
                        }
                        _c.label = 2;
                    case 2:
                        _c.trys.push([2, 5, , 6]);
                        return [4 /*yield*/, file.arrayBuffer()];
                    case 3:
                        buffer_1 = _c.sent();
                        return [4 /*yield*/, AcApDocManager.instance.withBusyIndicator(function () { return __awaiter(_this, void 0, void 0, function () {
                                var db;
                                var _this = this;
                                return __generator(this, function (_a) {
                                    switch (_a.label) {
                                        case 0:
                                            db = new AcDbDatabase();
                                            return [4 /*yield*/, acapWithSecondaryDatabase(db, function () { return __awaiter(_this, void 0, void 0, function () {
                                                    return __generator(this, function (_a) {
                                                        switch (_a.label) {
                                                            case 0: return [4 /*yield*/, db.read(buffer_1, { readOnly: true }, extension === 'dwg' ? AcDbFileType.DWG : AcDbFileType.DXF)];
                                                            case 1:
                                                                _a.sent();
                                                                return [2 /*return*/];
                                                        }
                                                    });
                                                }); })];
                                        case 1:
                                            _a.sent();
                                            return [2 /*return*/, db];
                                    }
                                });
                            }); }, AcApI18n.t('jig.xattach.loading'))];
                    case 4:
                        sourceDb = _c.sent();
                        return [3 /*break*/, 6];
                    case 5:
                        _a = _c.sent();
                        this.showMessage(AcApI18n.t('jig.xattach.loadFailed'), 'error');
                        return [2 /*return*/];
                    case 6:
                        extents = resolveSourceExtents(sourceDb);
                        jig = new AcApXAttachJig(context.view, extents);
                        _c.label = 7;
                    case 7:
                        _c.trys.push([7, , 12, 13]);
                        jig.setMode('point');
                        insertionPrompt = new AcEdPromptPointOptions(AcApI18n.t('jig.xattach.insertionPoint'));
                        insertionPrompt.allowNone = true;
                        insertionPrompt.useDefaultValue = true;
                        insertionPrompt.defaultValue = new AcGePoint3d(0, 0, 0);
                        insertionPrompt.jig = jig;
                        return [4 /*yield*/, AcApDocManager.instance.editor.getPoint(insertionPrompt)];
                    case 8:
                        insertionResult = _c.sent();
                        if (insertionResult.status !== AcEdPromptStatus.OK &&
                            insertionResult.status !== AcEdPromptStatus.None) {
                            return [2 /*return*/];
                        }
                        insertionPoint = insertionResult.status === AcEdPromptStatus.OK && insertionResult.value
                            ? new AcGePoint3d(insertionResult.value)
                            : new AcGePoint3d(0, 0, 0);
                        jig.setPosition(insertionPoint);
                        jig.setMode('scale');
                        scalePrompt = new AcEdPromptDoubleOptions(AcApI18n.t('jig.xattach.scale'));
                        scalePrompt.allowNone = true;
                        scalePrompt.allowNegative = false;
                        scalePrompt.allowZero = false;
                        scalePrompt.useDefaultValue = true;
                        scalePrompt.defaultValue = 1;
                        scalePrompt.jig = jig;
                        return [4 /*yield*/, AcApDocManager.instance.editor.getDouble(scalePrompt)];
                    case 9:
                        scaleResult = _c.sent();
                        if (scaleResult.status === AcEdPromptStatus.Cancel ||
                            scaleResult.status === AcEdPromptStatus.Error) {
                            return [2 /*return*/];
                        }
                        scale = scaleResult.status === AcEdPromptStatus.OK &&
                            scaleResult.value !== undefined
                            ? scaleResult.value
                            : 1;
                        if (!(scale > 0)) {
                            this.showMessage(AcApI18n.t('jig.xattach.invalidScale'), 'warning');
                            return [2 /*return*/];
                        }
                        jig.setScale(scale);
                        jig.setMode('rotation');
                        rotationPrompt = new AcEdPromptAngleOptions(AcApI18n.t('jig.xattach.rotation'));
                        rotationPrompt.allowNone = true;
                        rotationPrompt.allowNegative = true;
                        rotationPrompt.allowZero = true;
                        rotationPrompt.useDefaultValue = true;
                        rotationPrompt.defaultValue = 0;
                        rotationPrompt.useBasePoint = true;
                        rotationPrompt.useDashedLine = true;
                        rotationPrompt.basePoint = insertionPoint;
                        rotationPrompt.jig = jig;
                        return [4 /*yield*/, AcApDocManager.instance.editor.getAngle(rotationPrompt)];
                    case 10:
                        rotationResult = _c.sent();
                        if (rotationResult.status === AcEdPromptStatus.Cancel ||
                            rotationResult.status === AcEdPromptStatus.Error) {
                            return [2 /*return*/];
                        }
                        rotationDeg = rotationResult.status === AcEdPromptStatus.OK &&
                            rotationResult.value !== undefined
                            ? rotationResult.value
                            : 0;
                        rotationRad = (rotationDeg * Math.PI) / 180;
                        db_1 = context.doc.database;
                        blockName_1 = uniqueXrefName(db_1, file.name);
                        filePath_1 = resolveLocalFilePath(file);
                        transform_1 = {
                            position: insertionPoint,
                            scale: scale,
                            rotationRad: rotationRad
                        };
                        return [4 /*yield*/, AcApXrefManager.instance.attachOverlay({
                                blockName: blockName_1,
                                fileName: file.name,
                                sourceDb: sourceDb,
                                sourcePath: filePath_1,
                                transform: transform_1
                            })];
                    case 11:
                        session_1 = _c.sent();
                        try {
                            acapRunDatabaseEdit(db_1, 'XATTACH', function () {
                                var insert = AcApXrefManager.createHostXrefInsert(db_1, blockName_1, filePath_1, transform_1, sourceDb.tables.blockTable.modelSpace.origin).insert;
                                session_1.insertId = insert.objectId;
                            });
                        }
                        catch (error) {
                            AcApXrefManager.instance.unload(session_1.id);
                            throw error;
                        }
                        eventBus.emit('missed-data-changed', {});
                        return [3 /*break*/, 13];
                    case 12:
                        jig.end();
                        return [7 /*endfinally*/];
                    case 13: return [2 /*return*/];
                }
            });
        });
    };
    return AcApXAttachCmd;
}(AcEdCommand));
export { AcApXAttachCmd };
//# sourceMappingURL=AcApXAttachCmd.js.map