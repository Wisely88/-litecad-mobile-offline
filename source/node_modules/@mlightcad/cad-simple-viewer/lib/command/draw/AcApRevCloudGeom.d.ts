import { AcDbEntity, AcDbPolyline, AcGePoint2d, AcGePoint2dLike } from '@mlightcad/data-model';
import type { AcEdBaseView } from '../../editor';
/** Classic AutoCAD scallop: 180° arc (`bulge = tan(π/4)`). */
export declare const REVCLOUD_BULGE = 1;
export type AcApRevCloudStyle = 'normal' | 'calligraphy';
export interface AcApRevCloudBuildOptions {
    /** Approximate chord length of each cloud arc. */
    arcLength: number;
    /** When true, arcs bulge inward instead of outward. */
    reverse?: boolean;
    /** Normal (constant width) or calligraphy (tapered) style. */
    style?: AcApRevCloudStyle;
    /**
     * When true, chord lengths vary for a hand-drawn look
     * (`REVCLOUDARCVARIANCE` On).
     */
    variance?: boolean;
}
/**
 * World distance of the current view diagonal.
 */
export declare function viewDiagonalLength(view: AcEdBaseView): number;
/**
 * AutoCAD-like default arc length: a small percentage of the view diagonal.
 */
export declare function defaultRevCloudArcLength(view: AcEdBaseView): number;
export declare function distance2d(a: AcGePoint2dLike, b: AcGePoint2dLike): number;
/**
 * Rectangle outline in CCW order from two opposite corners.
 */
export declare function rectanglePath(first: AcGePoint2dLike, second: AcGePoint2dLike): AcGePoint2d[];
/**
 * Signed polygon area. Positive means counterclockwise.
 */
export declare function signedArea(points: AcGePoint2dLike[]): number;
/**
 * Returns a CCW copy of a closed path so positive bulges face outward.
 */
export declare function ensureCounterClockwise(points: AcGePoint2dLike[]): AcGePoint2d[];
/**
 * Rebuilds `cloud` as a revision-cloud polyline along `path`.
 *
 * @returns `false` when the path is degenerate and no cloud was built.
 */
export declare function buildRevCloud(cloud: AcDbPolyline, path: AcGePoint2dLike[], closed: boolean, options: AcApRevCloudBuildOptions): boolean;
/**
 * Samples a supported curve into a path that can be turned into a revision cloud.
 */
export declare function sampleEntityPath(entity: AcDbEntity, spacing: number): {
    points: AcGePoint2d[];
    closed: boolean;
} | undefined;
/**
 * True when the cursor is close enough to the start point to close a freehand cloud.
 */
export declare function isRevCloudCloseToStart(start: AcGePoint2dLike, current: AcGePoint2dLike, arcLength: number): boolean;
//# sourceMappingURL=AcApRevCloudGeom.d.ts.map