import { AcDbPolyline, AcGePoint2dLike } from '@mlightcad/data-model';
import { AcApContext } from '../../app';
import { AcEdBaseView, AcEdCommand, AcEdPreviewJig } from '../../editor';
import { type AcApRevCloudStyle } from './AcApRevCloudGeom';
type RevCloudMode = 'rectangular' | 'polygonal' | 'freehand';
interface RevCloudSettings {
    mode: RevCloudMode;
    arcLength?: number;
    style: AcApRevCloudStyle;
    variance: boolean;
}
/**
 * Live preview for rectangular / polygonal / freehand revision clouds.
 */
export declare class AcApRevCloudJig extends AcEdPreviewJig<AcGePoint2dLike> {
    private _cloud;
    private _path;
    private _closed;
    private _settings;
    constructor(view: AcEdBaseView, path: AcGePoint2dLike[], closed: boolean, settings: RevCloudSettings);
    get entity(): AcDbPolyline;
    updatePath(path: AcGePoint2dLike[], closed: boolean): void;
    update(point: AcGePoint2dLike): void;
    private rebuild;
}
/**
 * Command to create a revision cloud, aligned with AutoCAD REVCLOUD.
 *
 * Supports Rectangular (default), Polygonal, Freehand, Object conversion,
 * Arc length, Style (Normal/Calligraphy), and Reverse direction.
 */
export declare class AcApRevCloudCmd extends AcEdCommand {
    private static _settings;
    constructor();
    execute(context: AcApContext): Promise<void>;
    private promptArcLength;
    private promptStyle;
    private drawRectangular;
    private drawPolygonal;
    private drawFreehand;
    private convertObject;
    private commitCloud;
    private promptReverse;
    private promptPoint;
    private eraseEntity;
}
export {};
//# sourceMappingURL=AcApRevCloudCmd.d.ts.map