var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcDbLine, AcDbPolyline, AcDbSpline, AcGePoint2d, AcGePoint3d, AcGeTol } from '@mlightcad/data-model';
import { AcApDocManager } from '../../app';
import { AcEdCommand, AcEdOpenMode, AcEdPreviewJig, AcEdPromptDistanceOptions, AcEdPromptKeywordOptions, AcEdPromptPointOptions, AcEdPromptStatus } from '../../editor';
import { AcApI18n } from '../../i18n';
var DEFAULT_SETTINGS = {
    type: 'line',
    increment: 0.1,
    tolerance: 0.5
};
function addKeyword(prompt, key) {
    prompt.keywords.add(AcApI18n.t("jig.sketch.keywords.".concat(key, ".display")), AcApI18n.t("jig.sketch.keywords.".concat(key, ".global")), AcApI18n.t("jig.sketch.keywords.".concat(key, ".local")));
}
function distance2d(a, b) {
    return Math.hypot(b.x - a.x, b.y - a.y);
}
function toPoint2d(point) {
    return new AcGePoint2d(point.x, point.y);
}
function toPoint3d(point) {
    return new AcGePoint3d(point.x, point.y, 0);
}
/**
 * Keeps a vertex when it is at least `increment` from the previous kept point.
 * The first point is always kept; the last point is appended if distinct.
 */
export function accumulateSketchPoint(points, point, increment) {
    var next = toPoint2d(point);
    if (points.length === 0) {
        points.push(next);
        return true;
    }
    var last = points[points.length - 1];
    if (distance2d(last, next) < increment)
        return false;
    points.push(next);
    return true;
}
/**
 * Downsamples sketch points so consecutive kept vertices are at least
 * `tolerance` apart, always retaining the first and last points.
 */
export function simplifySketchPoints(points, tolerance) {
    if (points.length <= 2) {
        return points.map(toPoint2d);
    }
    var minDist = Math.max(tolerance, 1e-8);
    var simplified = [toPoint2d(points[0])];
    for (var i = 1; i < points.length - 1; i++) {
        var last_1 = simplified[simplified.length - 1];
        if (distance2d(last_1, points[i]) >= minDist) {
            simplified.push(toPoint2d(points[i]));
        }
    }
    var end = toPoint2d(points[points.length - 1]);
    var last = simplified[simplified.length - 1];
    if (distance2d(last, end) > 1e-8) {
        simplified.push(end);
    }
    return simplified;
}
/**
 * Preview jig that records freehand movement once the pointer travels farther
 * than the current sketch increment.
 */
var AcApSketchJig = /** @class */ (function (_super) {
    __extends(AcApSketchJig, _super);
    function AcApSketchJig(view, start, increment) {
        var _this = _super.call(this, view) || this;
        _this._polyline = new AcDbPolyline();
        _this._points = [toPoint2d(start)];
        _this._increment = Math.max(increment, 1e-8);
        _this._polyline.addVertexAt(0, _this._points[0]);
        return _this;
    }
    Object.defineProperty(AcApSketchJig.prototype, "entity", {
        get: function () {
            return this._polyline;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcApSketchJig.prototype, "points", {
        get: function () {
            return this._points;
        },
        enumerable: false,
        configurable: true
    });
    AcApSketchJig.prototype.update = function (currentPoint) {
        if (!accumulateSketchPoint(this._points, currentPoint, this._increment)) {
            return;
        }
        this._polyline.addVertexAt(this._points.length - 1, this._points[this._points.length - 1]);
    };
    return AcApSketchJig;
}(AcEdPreviewJig));
export { AcApSketchJig };
/**
 * Keeps completed sketch strokes visible while SKETCH is still running.
 *
 * The live stroke jig is torn down when a point prompt ends, and keyword
 * prompts never refresh jigs. Without this overlay, returning to
 * "Specify sketch or [Type/Increment/toLerance]" hides the sketch preview.
 */
var AcApSketchStrokePreview = /** @class */ (function () {
    function AcApSketchStrokePreview(view) {
        this.view = view;
        this._entities = [];
    }
    AcApSketchStrokePreview.prototype.addStroke = function (points) {
        if (points.length < 2)
            return;
        var polyline = new AcDbPolyline();
        points.forEach(function (point, index) {
            return polyline.addVertexAt(index, toPoint2d(point));
        });
        this._entities.push(polyline);
        this.view.addTransientEntity(polyline);
    };
    /**
     * Re-publishes transients. Keyword and distance prompts do not drive jigs.
     */
    AcApSketchStrokePreview.prototype.render = function () {
        var _this = this;
        this._entities.forEach(function (entity) { return _this.view.addTransientEntity(entity); });
    };
    AcApSketchStrokePreview.prototype.dispose = function () {
        var _this = this;
        this._entities.forEach(function (entity) {
            return _this.view.removeTransientEntity(entity.objectId);
        });
        this._entities.length = 0;
    };
    return AcApSketchStrokePreview;
}());
/**
 * Point/distance prompt adapter that keeps {@link AcApSketchStrokePreview}
 * visible. Prompt cleanup calls `end()`, but this jig has no owned entity, so
 * completed strokes are not removed.
 */
var AcApSketchRetainJig = /** @class */ (function (_super) {
    __extends(AcApSketchRetainJig, _super);
    function AcApSketchRetainJig(view, preview) {
        var _this = _super.call(this, view) || this;
        _this.preview = preview;
        return _this;
    }
    AcApSketchRetainJig.prototype.update = function (_value) {
        // Completed strokes are static; the preview is only re-published.
    };
    AcApSketchRetainJig.prototype.render = function () {
        this.preview.render();
    };
    return AcApSketchRetainJig;
}(AcEdPreviewJig));
/**
 * Command to create freehand sketches, aligned with AutoCAD SKETCH.
 *
 * Prompts: Specify sketch or [Type/Increment/toLerance]. Click to start a
 * stroke, move the pointer, click to stop. Enter ends the command.
 */
var AcApSketchCmd = /** @class */ (function (_super) {
    __extends(AcApSketchCmd, _super);
    function AcApSketchCmd() {
        var _this = _super.call(this) || this;
        _this.mode = AcEdOpenMode.Write;
        return _this;
    }
    AcApSketchCmd.prototype.execute = function (context) {
        return __awaiter(this, void 0, void 0, function () {
            var settings, strokes, preview, prompt_1, result, keyword, accepted, accepted, accepted, points;
            var _a;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        settings = AcApSketchCmd._settings;
                        strokes = [];
                        preview = new AcApSketchStrokePreview(context.view);
                        _b.label = 1;
                    case 1:
                        _b.trys.push([1, , 13, 14]);
                        _b.label = 2;
                    case 2:
                        if (!true) return [3 /*break*/, 12];
                        preview.render();
                        prompt_1 = new AcEdPromptPointOptions(AcApI18n.t('jig.sketch.specifySketch'));
                        addKeyword(prompt_1, 'type');
                        addKeyword(prompt_1, 'increment');
                        addKeyword(prompt_1, 'tolerance');
                        prompt_1.allowNone = true;
                        prompt_1.disableOSnap = true;
                        prompt_1.jig = new AcApSketchRetainJig(context.view, preview);
                        return [4 /*yield*/, AcApDocManager.instance.editor.getPoint(prompt_1)];
                    case 3:
                        result = _b.sent();
                        if (!(result.status === AcEdPromptStatus.Keyword)) return [3 /*break*/, 10];
                        keyword = (_a = result.stringResult) !== null && _a !== void 0 ? _a : '';
                        if (!(keyword === 'Type')) return [3 /*break*/, 5];
                        return [4 /*yield*/, this.promptType(settings, preview)];
                    case 4:
                        accepted = _b.sent();
                        if (!accepted)
                            return [2 /*return*/];
                        return [3 /*break*/, 2];
                    case 5:
                        if (!(keyword === 'Increment')) return [3 /*break*/, 7];
                        return [4 /*yield*/, this.promptIncrement(settings, preview)];
                    case 6:
                        accepted = _b.sent();
                        if (!accepted)
                            return [2 /*return*/];
                        return [3 /*break*/, 2];
                    case 7:
                        if (!(keyword === 'Tolerance')) return [3 /*break*/, 9];
                        return [4 /*yield*/, this.promptTolerance(settings, preview)];
                    case 8:
                        accepted = _b.sent();
                        if (!accepted)
                            return [2 /*return*/];
                        return [3 /*break*/, 2];
                    case 9: return [3 /*break*/, 2];
                    case 10:
                        if (result.status === AcEdPromptStatus.None) {
                            this.commitStrokes(context, strokes);
                            return [2 /*return*/];
                        }
                        if (result.status !== AcEdPromptStatus.OK || !result.value)
                            return [2 /*return*/];
                        return [4 /*yield*/, this.captureStroke(context, settings, result.value, preview)];
                    case 11:
                        points = _b.sent();
                        if (points === false)
                            return [2 /*return*/];
                        if (points.length >= 2) {
                            strokes.push({
                                points: points,
                                type: settings.type,
                                tolerance: settings.tolerance
                            });
                            preview.addStroke(points);
                        }
                        return [3 /*break*/, 2];
                    case 12: return [3 /*break*/, 14];
                    case 13:
                        preview.dispose();
                        return [7 /*endfinally*/];
                    case 14: return [2 /*return*/];
                }
            });
        });
    };
    AcApSketchCmd.prototype.promptType = function (settings, preview) {
        return __awaiter(this, void 0, void 0, function () {
            var prompt, line, polyline, spline, result;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        preview === null || preview === void 0 ? void 0 : preview.render();
                        prompt = new AcEdPromptKeywordOptions(AcApI18n.t('jig.sketch.type'));
                        prompt.allowNone = true;
                        if (preview) {
                            prompt.jig = new AcApSketchRetainJig(preview.view, preview);
                        }
                        line = prompt.keywords.add(AcApI18n.t('jig.sketch.keywords.line.display'), AcApI18n.t('jig.sketch.keywords.line.global'), AcApI18n.t('jig.sketch.keywords.line.local'));
                        polyline = prompt.keywords.add(AcApI18n.t('jig.sketch.keywords.polyline.display'), AcApI18n.t('jig.sketch.keywords.polyline.global'), AcApI18n.t('jig.sketch.keywords.polyline.local'));
                        spline = prompt.keywords.add(AcApI18n.t('jig.sketch.keywords.spline.display'), AcApI18n.t('jig.sketch.keywords.spline.global'), AcApI18n.t('jig.sketch.keywords.spline.local'));
                        prompt.keywords.default =
                            settings.type === 'polyline'
                                ? polyline
                                : settings.type === 'spline'
                                    ? spline
                                    : line;
                        return [4 /*yield*/, AcApDocManager.instance.editor.getKeywords(prompt)];
                    case 1:
                        result = _a.sent();
                        if (result.status === AcEdPromptStatus.Cancel)
                            return [2 /*return*/, false];
                        if (result.status === AcEdPromptStatus.OK ||
                            result.status === AcEdPromptStatus.Keyword) {
                            if (result.stringResult === 'Polyline')
                                settings.type = 'polyline';
                            else if (result.stringResult === 'Spline')
                                settings.type = 'spline';
                            else if (result.stringResult === 'Line' ||
                                result.stringResult === 'Lines') {
                                settings.type = 'line';
                            }
                        }
                        return [2 /*return*/, true];
                }
            });
        });
    };
    AcApSketchCmd.prototype.promptIncrement = function (settings, preview) {
        return __awaiter(this, void 0, void 0, function () {
            var prompt, result, value;
            var _a;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        preview === null || preview === void 0 ? void 0 : preview.render();
                        prompt = new AcEdPromptDistanceOptions(AcApI18n.t('jig.sketch.increment'));
                        prompt.allowNegative = false;
                        prompt.allowZero = false;
                        prompt.useDefaultValue = true;
                        prompt.defaultValue = settings.increment;
                        if (preview) {
                            prompt.jig = new AcApSketchRetainJig(preview.view, preview);
                        }
                        return [4 /*yield*/, AcApDocManager.instance.editor.getDistance(prompt)];
                    case 1:
                        result = _b.sent();
                        if (result.status === AcEdPromptStatus.Cancel)
                            return [2 /*return*/, false];
                        value = (_a = result.value) !== null && _a !== void 0 ? _a : settings.increment;
                        if (AcGeTol.isPositive(value)) {
                            settings.increment = value;
                        }
                        return [2 /*return*/, true];
                }
            });
        });
    };
    AcApSketchCmd.prototype.promptTolerance = function (settings, preview) {
        return __awaiter(this, void 0, void 0, function () {
            var prompt, result, value;
            var _a;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        preview === null || preview === void 0 ? void 0 : preview.render();
                        prompt = new AcEdPromptDistanceOptions(AcApI18n.t('jig.sketch.tolerance'));
                        prompt.allowNegative = false;
                        prompt.allowZero = true;
                        prompt.useDefaultValue = true;
                        prompt.defaultValue = settings.tolerance;
                        if (preview) {
                            prompt.jig = new AcApSketchRetainJig(preview.view, preview);
                        }
                        return [4 /*yield*/, AcApDocManager.instance.editor.getDistance(prompt)];
                    case 1:
                        result = _b.sent();
                        if (result.status === AcEdPromptStatus.Cancel)
                            return [2 /*return*/, false];
                        value = (_a = result.value) !== null && _a !== void 0 ? _a : settings.tolerance;
                        if (value >= 0 && Number.isFinite(value)) {
                            settings.tolerance = value;
                        }
                        return [2 /*return*/, true];
                }
            });
        });
    };
    /**
     * Records one freehand stroke: click starts, move draws, click or Enter stops.
     *
     * @returns `false` when the user cancels the command entirely; otherwise the
     * captured points (possibly fewer than two if the stroke is too short).
     */
    AcApSketchCmd.prototype.captureStroke = function (context, settings, start, preview) {
        return __awaiter(this, void 0, void 0, function () {
            var jig, prompt, result, points, last;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        preview.render();
                        jig = new AcApSketchJig(context.view, start, settings.increment);
                        prompt = new AcEdPromptPointOptions(AcApI18n.t('jig.sketch.sketching'));
                        prompt.jig = jig;
                        prompt.useDashedLine = false;
                        prompt.useBasePoint = true;
                        prompt.basePoint = toPoint3d(start);
                        prompt.allowNone = true;
                        prompt.disableOSnap = true;
                        return [4 /*yield*/, AcApDocManager.instance.editor.getPoint(prompt)];
                    case 1:
                        result = _a.sent();
                        if (result.status === AcEdPromptStatus.Cancel)
                            return [2 /*return*/, false];
                        points = __spreadArray([], __read(jig.points), false);
                        if (result.status === AcEdPromptStatus.OK && result.value) {
                            last = points[points.length - 1];
                            if (!last || distance2d(last, result.value) > 1e-8) {
                                points.push(toPoint2d(result.value));
                            }
                        }
                        return [2 /*return*/, points];
                }
            });
        });
    };
    AcApSketchCmd.prototype.commitStrokes = function (context, strokes) {
        var e_1, _a;
        try {
            for (var strokes_1 = __values(strokes), strokes_1_1 = strokes_1.next(); !strokes_1_1.done; strokes_1_1 = strokes_1.next()) {
                var stroke = strokes_1_1.value;
                this.appendStroke(context, stroke);
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (strokes_1_1 && !strokes_1_1.done && (_a = strokes_1.return)) _a.call(strokes_1);
            }
            finally { if (e_1) throw e_1.error; }
        }
    };
    AcApSketchCmd.prototype.appendStroke = function (context, stroke) {
        var modelSpace = context.doc.database.tables.blockTable.modelSpace;
        var points = stroke.points;
        if (stroke.type === 'line') {
            for (var i = 1; i < points.length; i++) {
                var line = new AcDbLine(toPoint3d(points[i - 1]), toPoint3d(points[i]));
                modelSpace.appendEntity(line);
                context.view.addEntity(line);
            }
            return;
        }
        if (stroke.type === 'spline') {
            var fitPoints = simplifySketchPoints(points, stroke.tolerance);
            if (fitPoints.length < 2)
                return;
            var points3d = fitPoints.map(toPoint3d);
            var degree = Math.min(3, Math.max(1, points3d.length - 1));
            var spline = new AcDbSpline(points3d, 'Chord', degree, false);
            modelSpace.appendEntity(spline);
            context.view.addEntity(spline);
            return;
        }
        var polyline = new AcDbPolyline();
        points.forEach(function (point, index) { return polyline.addVertexAt(index, point); });
        modelSpace.appendEntity(polyline);
        context.view.addEntity(polyline);
    };
    AcApSketchCmd._settings = __assign({}, DEFAULT_SETTINGS);
    return AcApSketchCmd;
}(AcEdCommand));
export { AcApSketchCmd };
//# sourceMappingURL=AcApSketchCmd.js.map