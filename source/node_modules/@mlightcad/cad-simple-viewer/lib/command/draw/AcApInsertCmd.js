var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
import { AcDbBlockReference, AcGePoint3d } from '@mlightcad/data-model';
import { AcApDocManager } from '../../app';
import { AcEdCommand, AcEdOpenMode, AcEdPreviewJig, AcEdPromptAngleOptions, AcEdPromptDoubleOptions, AcEdPromptPointOptions, AcEdPromptStatus, AcEdPromptStringOptions } from '../../editor';
import { AcApI18n } from '../../i18n';
import { acapRunDatabaseEdit } from '../../util/AcApDatabaseEdit';
import { AcApBlockInsertSession } from './AcApBlockInsertSession';
import { isInsertableBlockName } from './isInsertableBlockName';
/**
 * Preview jig for `-INSERT` — shows the block reference while the user picks
 * insertion point, scale, or rotation.
 */
var AcApInsertJig = /** @class */ (function (_super) {
    __extends(AcApInsertJig, _super);
    function AcApInsertJig(view, blockName) {
        var _this = _super.call(this, view) || this;
        _this._mode = 'point';
        _this._position = new AcGePoint3d();
        _this._scaleX = 1;
        _this._scaleY = 1;
        _this._scaleZ = 1;
        _this._rotationRad = 0;
        _this._entity = new AcDbBlockReference(blockName);
        _this.applyGeometry();
        return _this;
    }
    Object.defineProperty(AcApInsertJig.prototype, "entity", {
        get: function () {
            return this._entity;
        },
        enumerable: false,
        configurable: true
    });
    AcApInsertJig.prototype.setMode = function (mode) {
        this._mode = mode;
    };
    AcApInsertJig.prototype.setPosition = function (point) {
        this._position = new AcGePoint3d(point);
        this.applyGeometry();
    };
    AcApInsertJig.prototype.setUniformScale = function (scale) {
        this._scaleX = scale;
        this._scaleY = scale;
        this._scaleZ = scale;
        this.applyGeometry();
    };
    AcApInsertJig.prototype.setScaleFactors = function (scaleX, scaleY, scaleZ) {
        this._scaleX = scaleX;
        this._scaleY = scaleY;
        this._scaleZ = scaleZ;
        this.applyGeometry();
    };
    AcApInsertJig.prototype.setRotationRad = function (rotationRad) {
        this._rotationRad = rotationRad;
        this.applyGeometry();
    };
    AcApInsertJig.prototype.update = function (value) {
        if (this._mode === 'point' && typeof value !== 'number') {
            this.setPosition(value);
            return;
        }
        if (typeof value !== 'number')
            return;
        if (this._mode === 'scale') {
            this.setUniformScale(value > 0 ? value : this._scaleX);
            return;
        }
        if (this._mode === 'rotation') {
            // Angle prompts provide degrees.
            this.setRotationRad((value * Math.PI) / 180);
        }
    };
    AcApInsertJig.prototype.applyGeometry = function () {
        this._entity.position = new AcGePoint3d(this._position);
        this._entity.scaleFactors = new AcGePoint3d(this._scaleX, this._scaleY, this._scaleZ);
        this._entity.rotation = this._rotationRad;
    };
    return AcApInsertJig;
}(AcEdPreviewJig));
/**
 * `-INSERT` (alias: `I`) — inserts an existing block definition into model space.
 *
 * Flow (AutoCAD-aligned command-line INSERT):
 * 1. Enter block name
 * 2. Specify insertion point
 * 3. Specify scale factor (default 1) — skipped when session options fix scale
 * 4. Specify rotation angle (default 0) — skipped when session options fix rotation
 * 5. Append {@link AcDbBlockReference} to model space
 *
 * When started from the Blocks palette, {@link AcApBlockInsertSession} may
 * supply fixed scale/rotation and repeat-placement behavior.
 *
 * Attribute dialogs (ATTDIA / ATTREQ) are out of scope for this implementation.
 *
 * @see https://help.autodesk.com/view/ACD/2024/ENU/?caas=caas/documentation/ACDLT/2014/ENU/files/GUID-BB831F94-6385-4490-8DE9-7C565CD1B639-htm.html
 */
var AcApInsertCmd = /** @class */ (function (_super) {
    __extends(AcApInsertCmd, _super);
    function AcApInsertCmd() {
        var _this = _super.call(this) || this;
        _this.mode = AcEdOpenMode.Write;
        return _this;
    }
    AcApInsertCmd.prototype.execute = function (context) {
        return __awaiter(this, void 0, void 0, function () {
            var db, blockTable, session, namePrompt, nameResult, blockName, btr, jig, placed;
            var _a;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        db = context.doc.database;
                        blockTable = db.tables.blockTable;
                        session = AcApBlockInsertSession.consume();
                        namePrompt = new AcEdPromptStringOptions(AcApI18n.t('jig.insert.blockName'));
                        return [4 /*yield*/, AcApDocManager.instance.editor.getString(namePrompt)];
                    case 1:
                        nameResult = _b.sent();
                        if (nameResult.status !== AcEdPromptStatus.OK) {
                            return [2 /*return*/];
                        }
                        blockName = String((_a = nameResult.stringResult) !== null && _a !== void 0 ? _a : '').trim();
                        if (!isInsertableBlockName(blockName)) {
                            this.showMessage(AcApI18n.t('jig.insert.invalidBlockName'), 'warning');
                            return [2 /*return*/];
                        }
                        if (!blockTable.has(blockName)) {
                            this.showMessage("".concat(AcApI18n.t('jig.insert.blockNotFound'), ": ").concat(blockName), 'warning');
                            return [2 /*return*/];
                        }
                        btr = blockTable.getAt(blockName);
                        if (btr === null || btr === void 0 ? void 0 : btr.isXref) {
                            this.showMessage(AcApI18n.t('jig.insert.xrefNotAllowed'), 'warning');
                            return [2 /*return*/];
                        }
                        jig = new AcApInsertJig(context.view, blockName);
                        _b.label = 2;
                    case 2:
                        _b.trys.push([2, , 7, 8]);
                        _b.label = 3;
                    case 3: return [4 /*yield*/, this.placeOne(context, blockName, jig, session)];
                    case 4:
                        placed = _b.sent();
                        if (!placed)
                            return [2 /*return*/];
                        _b.label = 5;
                    case 5:
                        if (session === null || session === void 0 ? void 0 : session.repeatPlacement) return [3 /*break*/, 3];
                        _b.label = 6;
                    case 6: return [3 /*break*/, 8];
                    case 7:
                        jig.end();
                        return [7 /*endfinally*/];
                    case 8: return [2 /*return*/];
                }
            });
        });
    };
    /**
     * Prompts for one insertion (point + optional scale/rotation) and commits.
     *
     * @returns `true` when an instance was appended; `false` on cancel/error
     */
    AcApInsertCmd.prototype.placeOne = function (context, blockName, jig, session) {
        return __awaiter(this, void 0, void 0, function () {
            var db, insertionPrompt, insertionResult, insertionPoint, scaleX, scaleY, scaleZ, specifyScale, scalePrompt, scaleResult, scale, rotationDeg, specifyRotation, rotationPrompt, rotationResult, rotationRad;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        db = context.doc.database;
                        jig.setMode('point');
                        insertionPrompt = new AcEdPromptPointOptions(AcApI18n.t('jig.insert.insertionPoint'));
                        insertionPrompt.jig = jig;
                        return [4 /*yield*/, AcApDocManager.instance.editor.getPoint(insertionPrompt)];
                    case 1:
                        insertionResult = _a.sent();
                        if (insertionResult.status !== AcEdPromptStatus.OK ||
                            !insertionResult.value) {
                            return [2 /*return*/, false];
                        }
                        insertionPoint = new AcGePoint3d(insertionResult.value);
                        jig.setPosition(insertionPoint);
                        scaleX = 1;
                        scaleY = 1;
                        scaleZ = 1;
                        specifyScale = (session === null || session === void 0 ? void 0 : session.specifyScale) !== false;
                        if (!(!specifyScale && session)) return [3 /*break*/, 2];
                        scaleX = positiveOr(session.scaleX, 1);
                        scaleY = positiveOr(session.scaleY, scaleX);
                        scaleZ = positiveOr(session.scaleZ, scaleX);
                        jig.setScaleFactors(scaleX, scaleY, scaleZ);
                        return [3 /*break*/, 4];
                    case 2:
                        jig.setMode('scale');
                        scalePrompt = new AcEdPromptDoubleOptions(AcApI18n.t('jig.insert.scale'));
                        scalePrompt.allowNone = true;
                        scalePrompt.allowNegative = false;
                        scalePrompt.allowZero = false;
                        scalePrompt.useDefaultValue = true;
                        scalePrompt.defaultValue = 1;
                        scalePrompt.jig = jig;
                        return [4 /*yield*/, AcApDocManager.instance.editor.getDouble(scalePrompt)];
                    case 3:
                        scaleResult = _a.sent();
                        if (scaleResult.status === AcEdPromptStatus.Cancel ||
                            scaleResult.status === AcEdPromptStatus.Error) {
                            return [2 /*return*/, false];
                        }
                        scale = scaleResult.status === AcEdPromptStatus.OK &&
                            scaleResult.value !== undefined
                            ? scaleResult.value
                            : 1;
                        if (!(scale > 0)) {
                            this.showMessage(AcApI18n.t('jig.insert.invalidScale'), 'warning');
                            return [2 /*return*/, false];
                        }
                        scaleX = scaleY = scaleZ = scale;
                        jig.setUniformScale(scale);
                        _a.label = 4;
                    case 4:
                        rotationDeg = 0;
                        specifyRotation = (session === null || session === void 0 ? void 0 : session.specifyRotation) !== false;
                        if (!(!specifyRotation && session)) return [3 /*break*/, 5];
                        rotationDeg = Number.isFinite(session.rotationDeg)
                            ? session.rotationDeg
                            : 0;
                        jig.setRotationRad((rotationDeg * Math.PI) / 180);
                        return [3 /*break*/, 7];
                    case 5:
                        jig.setMode('rotation');
                        rotationPrompt = new AcEdPromptAngleOptions(AcApI18n.t('jig.insert.rotation'));
                        rotationPrompt.allowNone = true;
                        rotationPrompt.allowNegative = true;
                        rotationPrompt.allowZero = true;
                        rotationPrompt.useDefaultValue = true;
                        rotationPrompt.defaultValue = 0;
                        rotationPrompt.useBasePoint = true;
                        rotationPrompt.useDashedLine = true;
                        rotationPrompt.basePoint = insertionPoint;
                        rotationPrompt.jig = jig;
                        return [4 /*yield*/, AcApDocManager.instance.editor.getAngle(rotationPrompt)];
                    case 6:
                        rotationResult = _a.sent();
                        if (rotationResult.status === AcEdPromptStatus.Cancel ||
                            rotationResult.status === AcEdPromptStatus.Error) {
                            return [2 /*return*/, false];
                        }
                        rotationDeg =
                            rotationResult.status === AcEdPromptStatus.OK &&
                                rotationResult.value !== undefined
                                ? rotationResult.value
                                : 0;
                        _a.label = 7;
                    case 7:
                        rotationRad = (rotationDeg * Math.PI) / 180;
                        acapRunDatabaseEdit(db, 'INSERT', function () {
                            var insert = new AcDbBlockReference(blockName);
                            insert.position = insertionPoint;
                            insert.scaleFactors = new AcGePoint3d(scaleX, scaleY, scaleZ);
                            insert.rotation = rotationRad;
                            db.tables.blockTable.modelSpace.appendEntity(insert);
                        });
                        return [2 /*return*/, true];
                }
            });
        });
    };
    return AcApInsertCmd;
}(AcEdCommand));
export { AcApInsertCmd };
function positiveOr(value, fallback) {
    return value !== undefined && value > 0 ? value : fallback;
}
//# sourceMappingURL=AcApInsertCmd.js.map