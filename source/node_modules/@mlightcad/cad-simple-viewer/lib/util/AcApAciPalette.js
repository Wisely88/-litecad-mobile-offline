var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
/**
 * Ones-digit row order used by AutoCAD's ACI color dialog for indices 10–249.
 * Even digits descend (8→0), then odd digits ascend (1→9).
 */
var ACI_LARGE_ONES_ORDER = [8, 6, 4, 2, 0, 1, 3, 5, 7, 9];
/**
 * Builds ACI indices 10–249 in AutoCAD Color dialog order.
 *
 * Layout is 10 rows × 24 columns: each column is a decade group (10, 20, …,
 * 240); each row uses ones digits `8, 6, 4, 2, 0, 1, 3, 5, 7, 9`.
 *
 * @returns 240 indices suitable for a `grid-template-columns: repeat(24, …)` palette.
 */
function buildAciLargePaletteIndices() {
    var e_1, _a;
    var indices = [];
    try {
        for (var ACI_LARGE_ONES_ORDER_1 = __values(ACI_LARGE_ONES_ORDER), ACI_LARGE_ONES_ORDER_1_1 = ACI_LARGE_ONES_ORDER_1.next(); !ACI_LARGE_ONES_ORDER_1_1.done; ACI_LARGE_ONES_ORDER_1_1 = ACI_LARGE_ONES_ORDER_1.next()) {
            var ones = ACI_LARGE_ONES_ORDER_1_1.value;
            for (var decade = 1; decade <= 24; decade++) {
                indices.push(decade * 10 + ones);
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (ACI_LARGE_ONES_ORDER_1_1 && !ACI_LARGE_ONES_ORDER_1_1.done && (_a = ACI_LARGE_ONES_ORDER_1.return)) _a.call(ACI_LARGE_ONES_ORDER_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
    return indices;
}
/** ACI indices 1–9 (standard small palette). */
export var ACI_SMALL_PALETTE_INDICES = Array.from({ length: 9 }, function (_, i) { return i + 1; });
/** ACI indices 10–249 in AutoCAD dialog order (see {@link buildAciLargePaletteIndices}). */
export var ACI_LARGE_PALETTE_INDICES = buildAciLargePaletteIndices();
/** ACI indices 250–255 (gray ramp). */
export var ACI_GRAY_PALETTE_INDICES = Array.from({ length: 6 }, function (_, i) { return i + 250; });
//# sourceMappingURL=AcApAciPalette.js.map