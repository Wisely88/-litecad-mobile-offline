import { type AcDbDatabase, type AcDbFormatterOptions } from '@mlightcad/data-model';
/** Linear/angular unit settings used when formatting measurement labels. */
export interface AcApMeasurementUnits {
    /** Linear display format (LUNITS). */
    lunits: number;
    /** Linear display precision (LUPREC). */
    luprec: number;
    /** Angular display format (AUNITS). */
    aunits: number;
    /** Angular display precision (AUPREC). */
    auprec: number;
    /** Length display unit (INSUNITS code) or {@link MEASUREMENT_LENGTH_UNIT_FOLLOW_DRAWING}. */
    lengthUnit: number;
}
/** Sentinel length unit meaning "follow the drawing units" (no conversion). */
export declare const MEASUREMENT_LENGTH_UNIT_FOLLOW_DRAWING = -1;
/** Scale a value expressed in `fromUnit` so it is expressed in `toUnit`. */
export declare function convertLengthValue(value: number, fromUnit: number, toUnit: number): number;
/** Scale an area expressed in `fromUnit` so it is expressed in `toUnit`. */
export declare function convertAreaValue(value: number, fromUnit: number, toUnit: number): number;
/** Short symbol for a length-unit code, or empty when unknown / follow-drawing. */
export declare function measurementLengthUnitSymbol(unit: number): string;
/** Numeric value stored on a committed measurement so its badge can be reformatted. */
export type AcApMeasurementValue = {
    kind: 'length';
    value: number;
} | {
    kind: 'area';
    value: number;
} | {
    kind: 'angle';
    radians: number;
} | {
    kind: 'coordinate';
    x: number;
    y: number;
};
/** Display flags for length / area / coordinate measurement labels. */
export declare const MEASUREMENT_LENGTH_FORMAT_OPTIONS: AcDbFormatterOptions;
/** Display flags for included-angle measurement labels. */
export declare const MEASUREMENT_ANGLE_FORMAT_OPTIONS: AcDbFormatterOptions;
/**
 * Effective measurement units: session override when the user changed a ribbon
 * control, otherwise the current drawing units.
 */
export declare function getEffectiveMeasurementUnits(db: AcDbDatabase): AcApMeasurementUnits;
/** Fields the user has overridden on the Measurement ribbon unit panels. */
export declare function getMeasurementUnitOverride(): Partial<AcApMeasurementUnits>;
/**
 * Record a user override for one or more measurement unit fields.
 * Does not change drawing system variables.
 */
export declare function setMeasurementUnitOverride(patch: Partial<AcApMeasurementUnits>): void;
/** Clear session measurement unit overrides (document open / tests). */
export declare function resetMeasurementUnitOverride(): void;
/** Format a linear distance using effective measurement units. */
export declare function formatMeasurementLength(db: AcDbDatabase, value: number, options?: AcDbFormatterOptions): string;
/** Format an area using effective measurement units (length unit squared). */
export declare function formatMeasurementArea(db: AcDbDatabase, value: number, options?: AcDbFormatterOptions): string;
/** Format an angle in radians using effective measurement units. */
export declare function formatMeasurementAngle(db: AcDbDatabase, radians: number, options?: AcDbFormatterOptions): string;
/** Format a stored measurement value for a badge label. */
export declare function formatMeasurementValue(db: AcDbDatabase, value: AcApMeasurementValue): string;
//# sourceMappingURL=AcApMeasurementUnits.d.ts.map