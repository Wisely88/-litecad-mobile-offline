var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
/**
 * Parses AutoCAD-style command scripts (`.scr`) into Enter-separated lines.
 *
 * - Normalizes `\r\n` / `\r` and `\n`-escaped single-line scripts
 * - Drops full-line `;` comments
 * - Keeps blank lines (empty strings) so prompts can treat them as Enter / None
 */
export function parseScriptLines(script) {
    var e_1, _a;
    if (!script) {
        return [];
    }
    var source = script.includes('\n') || script.includes('\r')
        ? script
        : script.replace(/\\n/g, '\n');
    var rawLines = source.replace(/\r\n/g, '\n').replace(/\r/g, '\n').split('\n');
    var lines = [];
    try {
        for (var rawLines_1 = __values(rawLines), rawLines_1_1 = rawLines_1.next(); !rawLines_1_1.done; rawLines_1_1 = rawLines_1.next()) {
            var line = rawLines_1_1.value;
            var trimmedStart = line.trimStart();
            if (trimmedStart.startsWith(';')) {
                continue;
            }
            lines.push(line);
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (rawLines_1_1 && !rawLines_1_1.done && (_a = rawLines_1.return)) _a.call(rawLines_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
    return lines;
}
/**
 * Returns true when the token is a script terminator (`QUIT` / `EXIT`).
 */
export function isScriptQuitCommand(commandName) {
    var upper = commandName.trim().toUpperCase();
    return upper === 'QUIT' || upper === 'EXIT';
}
//# sourceMappingURL=AcApScriptParser.js.map