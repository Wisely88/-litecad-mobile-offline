var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { AcDbLinearUnits } from '@mlightcad/data-model';
/** Sentinel length unit meaning "follow the drawing units" (no conversion). */
export var MEASUREMENT_LENGTH_UNIT_FOLLOW_DRAWING = -1;
/** AutoCAD INSUNITS code → scale factor expressed in millimeters. */
var ACAD_UNIT_TO_MM = {
    0: 1,
    1: 25.4,
    2: 304.8,
    3: 1609344,
    4: 1,
    5: 10,
    6: 1000,
    7: 1000000,
    8: 0.0000254,
    9: 0.0254,
    10: 914.4,
    11: 0.0000001,
    12: 0.000001,
    13: 0.001,
    14: 100,
    15: 10000,
    16: 100000,
    17: 1000000000000,
    18: 149597870700000,
    19: 9.4607304725808e18,
    20: 3.085677581491367e19,
    21: 304.80060960121926,
    22: 25.400050800101603,
    23: 914.4018288036576,
    24: 1609347.2186944375
};
/** Short Latin suffixes shown after converted measurement lengths. */
var ACAD_UNIT_SYMBOLS = {
    1: 'in',
    2: 'ft',
    4: 'mm',
    5: 'cm',
    6: 'm',
    7: 'km',
    10: 'yd'
};
function acapUnitToMm(unit) {
    var _a;
    if (!Number.isFinite(unit))
        return 1;
    return (_a = ACAD_UNIT_TO_MM[Math.trunc(unit)]) !== null && _a !== void 0 ? _a : 1;
}
/** Scale a value expressed in `fromUnit` so it is expressed in `toUnit`. */
export function convertLengthValue(value, fromUnit, toUnit) {
    var toMm = acapUnitToMm(toUnit);
    if (toMm === 0)
        return value;
    return value * (acapUnitToMm(fromUnit) / toMm);
}
/** Scale an area expressed in `fromUnit` so it is expressed in `toUnit`. */
export function convertAreaValue(value, fromUnit, toUnit) {
    var factor = convertLengthValue(1, fromUnit, toUnit);
    return value * factor * factor;
}
/** Short symbol for a length-unit code, or empty when unknown / follow-drawing. */
export function measurementLengthUnitSymbol(unit) {
    var _a;
    return (_a = ACAD_UNIT_SYMBOLS[Math.trunc(unit)]) !== null && _a !== void 0 ? _a : '';
}
/** Display flags for length / area / coordinate measurement labels. */
export var MEASUREMENT_LENGTH_FORMAT_OPTIONS = {
    showUnits: true,
    showApproximate: true
};
/**
 * Display flags for a length already rescaled to another physical unit. The
 * formatter must not add the drawing suffix — the target unit symbol is
 * appended by the caller.
 */
var MEASUREMENT_CONVERTED_LENGTH_FORMAT_OPTIONS = {
    showUnits: false,
    showApproximate: true
};
/** Display flags for included-angle measurement labels. */
export var MEASUREMENT_ANGLE_FORMAT_OPTIONS = {
    showUnits: true,
    showApproximate: true,
    applyAngbaseAngdir: false
};
/**
 * Session overrides for measurement display units. `undefined` fields follow
 * the current drawing (LUNITS / LUPREC / AUNITS / AUPREC).
 */
var measurementUnitOverride = {};
function asUnitHeader(db) {
    return db;
}
/**
 * Effective measurement units: session override when the user changed a ribbon
 * control, otherwise the current drawing units.
 */
export function getEffectiveMeasurementUnits(db) {
    var _a, _b, _c, _d, _e;
    return {
        lunits: (_a = measurementUnitOverride.lunits) !== null && _a !== void 0 ? _a : db.lunits,
        luprec: (_b = measurementUnitOverride.luprec) !== null && _b !== void 0 ? _b : db.luprec,
        aunits: (_c = measurementUnitOverride.aunits) !== null && _c !== void 0 ? _c : db.aunits,
        auprec: (_d = measurementUnitOverride.auprec) !== null && _d !== void 0 ? _d : db.auprec,
        lengthUnit: (_e = measurementUnitOverride.lengthUnit) !== null && _e !== void 0 ? _e : MEASUREMENT_LENGTH_UNIT_FOLLOW_DRAWING
    };
}
/** Fields the user has overridden on the Measurement ribbon unit panels. */
export function getMeasurementUnitOverride() {
    return __assign({}, measurementUnitOverride);
}
/**
 * Record a user override for one or more measurement unit fields.
 * Does not change drawing system variables.
 */
export function setMeasurementUnitOverride(patch) {
    if (patch.lunits != null)
        measurementUnitOverride.lunits = patch.lunits;
    if (patch.luprec != null)
        measurementUnitOverride.luprec = patch.luprec;
    if (patch.aunits != null)
        measurementUnitOverride.aunits = patch.aunits;
    if (patch.auprec != null)
        measurementUnitOverride.auprec = patch.auprec;
    if (patch.lengthUnit != null)
        measurementUnitOverride.lengthUnit = patch.lengthUnit;
}
/** Clear session measurement unit overrides (document open / tests). */
export function resetMeasurementUnitOverride() {
    measurementUnitOverride = {};
}
/**
 * True when a ribbon override differs from the drawing header. Formatting then
 * patches private header fields for the duration of one formatter call and
 * restores them — it never writes LUNITS / LUPREC / AUNITS / AUPREC sysvars.
 */
function hasEffectiveUnitOverride(db) {
    var units = getEffectiveMeasurementUnits(db);
    return (units.lunits !== db.lunits ||
        units.luprec !== db.luprec ||
        units.aunits !== db.aunits ||
        units.auprec !== db.auprec);
}
/** LUNITS values that assume drawing INSUNITS, not arbitrary physical units. */
function isDrawingNativeLengthFormat(lunits) {
    return (lunits === AcDbLinearUnits.Architectural ||
        lunits === AcDbLinearUnits.Engineering);
}
/**
 * Run `fn` with the formatter reading effective measurement units.
 * Drawing system variables are not changed.
 */
function withMeasurementFormatContext(db, fn, formatPatch) {
    var units = getEffectiveMeasurementUnits(db);
    var lunitsForFormat = (formatPatch === null || formatPatch === void 0 ? void 0 : formatPatch.forceDecimalLength) &&
        isDrawingNativeLengthFormat(units.lunits)
        ? AcDbLinearUnits.Decimal
        : units.lunits;
    var needsPatch = hasEffectiveUnitOverride(db) || lunitsForFormat !== units.lunits;
    if (!needsPatch)
        return fn();
    var header = asUnitHeader(db);
    var prev = {
        lunits: header._lunits,
        luprec: header._luprec,
        aunits: header._aunits,
        auprec: header._auprec
    };
    header._lunits = lunitsForFormat;
    header._luprec = units.luprec;
    header._aunits = units.aunits;
    header._auprec = units.auprec;
    try {
        return fn();
    }
    finally {
        header._lunits = prev.lunits;
        header._luprec = prev.luprec;
        header._aunits = prev.aunits;
        header._auprec = prev.auprec;
    }
}
/** Format a linear value without applying a physical-unit conversion. */
function formatMeasurementLengthRaw(db, value, options, formatPatch) {
    if (options === void 0) { options = MEASUREMENT_LENGTH_FORMAT_OPTIONS; }
    return withMeasurementFormatContext(db, function () { return db.formatter.formatLength(value, options); }, formatPatch);
}
/** Merge caller flags with converted-length defaults (units suffix always off). */
function convertedLengthFormatOptions(options) {
    return __assign(__assign(__assign({}, MEASUREMENT_CONVERTED_LENGTH_FORMAT_OPTIONS), options), { showUnits: false });
}
/**
 * Length unit code to convert measurement values to, or `undefined` when labels
 * should stay in drawing units (no override selected, the override equals the
 * drawing's INSUNITS, INSUNITS is undefined, or the follow-drawing sentinel is
 * active).
 */
function convertedLengthUnit(db) {
    var _a;
    var fromUnit = (_a = db.insunits) !== null && _a !== void 0 ? _a : 0;
    if (fromUnit === 0)
        return undefined;
    var target = measurementUnitOverride.lengthUnit;
    if (target == null ||
        target === fromUnit ||
        target === MEASUREMENT_LENGTH_UNIT_FOLLOW_DRAWING)
        return undefined;
    return target;
}
/** Format a linear distance using effective measurement units. */
export function formatMeasurementLength(db, value, options) {
    var _a;
    if (options === void 0) { options = MEASUREMENT_LENGTH_FORMAT_OPTIONS; }
    var target = convertedLengthUnit(db);
    if (target == null) {
        return formatMeasurementLengthRaw(db, value, options);
    }
    var fromUnit = (_a = db.insunits) !== null && _a !== void 0 ? _a : 0;
    var scaled = convertLengthValue(value, fromUnit, target);
    var text = formatMeasurementLengthRaw(db, scaled, convertedLengthFormatOptions(options), { forceDecimalLength: true });
    var symbol = measurementLengthUnitSymbol(target);
    return symbol ? "".concat(text, " ").concat(symbol) : text;
}
/** Format an area using effective measurement units (length unit squared). */
export function formatMeasurementArea(db, value, options) {
    var _a;
    if (options === void 0) { options = MEASUREMENT_LENGTH_FORMAT_OPTIONS; }
    var target = convertedLengthUnit(db);
    if (target == null) {
        return "".concat(formatMeasurementLengthRaw(db, value, options), "\u00B2");
    }
    var fromUnit = (_a = db.insunits) !== null && _a !== void 0 ? _a : 0;
    var scaled = convertAreaValue(value, fromUnit, target);
    var text = formatMeasurementLengthRaw(db, scaled, convertedLengthFormatOptions(options), { forceDecimalLength: true });
    var symbol = measurementLengthUnitSymbol(target);
    return symbol ? "".concat(text, " ").concat(symbol, "\u00B2") : "".concat(text, "\u00B2");
}
/** Format an angle in radians using effective measurement units. */
export function formatMeasurementAngle(db, radians, options) {
    if (options === void 0) { options = MEASUREMENT_ANGLE_FORMAT_OPTIONS; }
    return withMeasurementFormatContext(db, function () {
        return db.formatter.formatAngle(radians, options);
    });
}
/** Format a stored measurement value for a badge label. */
export function formatMeasurementValue(db, value) {
    switch (value.kind) {
        case 'length':
            return formatMeasurementLength(db, value.value);
        case 'area':
            return formatMeasurementArea(db, value.value);
        case 'angle':
            return formatMeasurementAngle(db, value.radians);
        case 'coordinate': {
            var x = formatMeasurementLength(db, value.x);
            var y = formatMeasurementLength(db, value.y);
            return "X ".concat(x, "  Y ").concat(y);
        }
    }
}
//# sourceMappingURL=AcApMeasurementUnits.js.map