/** 2D point in screen / canvas pixels. */
export interface AcApScreenPoint {
    x: number;
    y: number;
}
/**
 * Shortest distance from a point to a line segment, in pixels.
 */
export declare function distPointToSegmentPx(px: number, py: number, ax: number, ay: number, bx: number, by: number): number;
/**
 * Minimum distance from a point to a polyline (open or closed).
 */
export declare function distPointToPolylinePx(px: number, py: number, verts: AcApScreenPoint[], closed?: boolean): number;
/**
 * Distance from a point to a rectangle outline (not the interior).
 */
export declare function distPointToRectOutlinePx(px: number, py: number, a: AcApScreenPoint, b: AcApScreenPoint): number;
/**
 * Whether a point lies inside an axis-aligned rectangle (inclusive).
 */
export declare function pointInRectPx(px: number, py: number, a: AcApScreenPoint, b: AcApScreenPoint): boolean;
/**
 * Absolute difference between a point's distance to `center` and `radius`.
 * Zero means the point is on the circumference.
 */
export declare function distPointToCirclePx(px: number, py: number, cx: number, cy: number, radius: number): number;
/**
 * Ray-casting point-in-polygon test in screen space.
 */
export declare function pointInPolygonPx(px: number, py: number, verts: AcApScreenPoint[]): boolean;
/**
 * Screen distance from a point to a circular arc.
 *
 * `antiClockwise` selects the decreasing-angle sweep from `startAngle` to
 * `endAngle`; otherwise the increasing-angle sweep is used. Both sweeps wrap
 * through `2π` so major arcs (greater than 180 degrees) can be hit-tested.
 */
export declare function distPointToArcPx(px: number, py: number, cx: number, cy: number, r: number, startAngle: number, endAngle: number, antiClockwise: boolean): number;
//# sourceMappingURL=AcApScreenHitTest.d.ts.map