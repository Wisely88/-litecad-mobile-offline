var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
import { AcCmErrors } from '@mlightcad/common';
import { AcGeBox2d, AcGePoint2d, AcGeVector2d } from '../math';
import { acgeClamp, AcGeMathUtil, FLOAT_TOL, TAU } from '../util';
import { acgeTryCreateArcByCenterStartChord, acgeTryCreateArcByCenterStartEnd, acgeTryCreateArcByCenterStartProjectedEnd, acgeTryCreateArcByCenterStartSweep, acgeTryCreateArcByStartEndAngle, acgeTryCreateArcByStartEndDirection, acgeTryCreateArcByStartEndRadius, acgeTryCreateArcByThreePoints, acgeTryCreateCircle, acgeTryCreateCircleByDiameter, acgeTryCreateCircleByThreePoints, acgeTryCreateShorterArc } from './AcGeCircArc2dFactory';
import { acgeComputeCircumcircle2d, acgeIsBetterDistanceAlign, acgePointLiesOnCircle2d, acgeProjectPointOntoCircle2d, acgeSameCircle2d } from './AcGeCircArcUtil';
import { AcGeCurve2d } from './AcGeCurve2d';
/**
 * Represent a circular arc.
 *
 * The angle system behavior depends on the clockwise property:
 * - If clockwise = false (counterclockwise): Angles are stored in normal mathematical sense
 *   (0° = +X axis, 90° = +Y axis, 180° = -X axis, 270° = -Y axis)
 * - If clockwise = true: Angles are stored in a mirrored system where positive angles go clockwise
 *   (0° = +X axis, 270° = +Y axis, 180° = -X axis, 90° = -Y axis)
 *
 * This means a "90° above X axis" in counterclockwise mode becomes "270°" in clockwise mode.
 */
var AcGeCircArc2d = /** @class */ (function (_super) {
    __extends(AcGeCircArc2d, _super);
    function AcGeCircArc2d(a, b, c, d, e) {
        var _this = _super.call(this) || this;
        var argsLength = +(a !== undefined) +
            +(b !== undefined) +
            +(c !== undefined) +
            +(d !== undefined) +
            +(e !== undefined);
        if (argsLength == 3) {
            if (typeof a == 'object' &&
                typeof b == 'object' &&
                typeof c == 'object') {
                _this.createByThreePoints(a, b, c);
            }
            else {
                _this.createByStartEndPointsAndBulge(a, b, c);
            }
        }
        else if (argsLength == 5) {
            var center = a;
            _this.center = new AcGePoint2d(center.x, center.y);
            _this.radius = b;
            _this._clockwise = e;
            // Store internal angles (unmirrored)
            _this._startAngle = _this._clockwise
                ? _this._mirrorAngle(AcGeMathUtil.normalizeAngle(c))
                : AcGeMathUtil.normalizeAngle(c);
            _this._endAngle = _this._clockwise
                ? _this._mirrorAngle(AcGeMathUtil.normalizeAngle(d))
                : AcGeMathUtil.normalizeAngle(d);
        }
        else {
            throw AcCmErrors.ILLEGAL_PARAMETERS;
        }
        return _this;
    }
    /**
     * Create an arc from mathematical (unmirrored) polar angles.
     *
     * `startAngle` / `endAngle` are `atan2` angles in the XY plane (0 = +X,
     * 90° = +Y). This differs from the five-argument constructor, whose angles
     * are mirrored when `clockwise` is true.
     *
     * @param center Input arc center
     * @param radius Input arc radius
     * @param startAngle Input start angle in radians (`atan2`)
     * @param endAngle Input end angle in radians (`atan2`)
     * @param clockwise Input true to sweep clockwise from start to end
     */
    AcGeCircArc2d.fromMathAngles = function (center, radius, startAngle, endAngle, clockwise) {
        var arc = new AcGeCircArc2d(center, radius, AcGeMathUtil.normalizeAngle(startAngle), AcGeMathUtil.normalizeAngle(endAngle), false);
        arc._clockwise = clockwise;
        arc._boundingBoxNeedsUpdate = true;
        return arc;
    };
    /**
     * Return the circumcircle of three XY points, or `null` when they are collinear.
     */
    AcGeCircArc2d.computeCircumcircle = function (p1, p2, p3) {
        return acgeComputeCircumcircle2d(p1, p2, p3);
    };
    /**
     * Project `point` radially onto the circle `(center, radius)`.
     */
    AcGeCircArc2d.projectPoint = function (center, radius, point) {
        return acgeProjectPointOntoCircle2d(center, radius, point);
    };
    /**
     * True when `point` lies on the circle `(center, radius)` within a radial
     * tolerance (default `max(1e-6, radius * 1e-5)`).
     */
    AcGeCircArc2d.pointLiesOnCircle = function (point, center, radius, eps) {
        return acgePointLiesOnCircle2d(point, center, radius, eps);
    };
    /**
     * True when two circles share the same center and radius within `eps`
     * (default `1e-8`).
     */
    AcGeCircArc2d.sameCircle = function (center1, radius1, center2, radius2, eps) {
        return acgeSameCircle2d(center1, radius1, center2, radius2, eps);
    };
    /**
     * Lexicographic pick among nearest-point candidates: smaller `distSq` wins;
     * on a near-tie, larger `align` wins.
     */
    AcGeCircArc2d.isBetterDistanceAlign = function (distSq, align, bestDistSq, bestAlign) {
        return acgeIsBetterDistanceAlign(distSq, align, bestDistSq, bestAlign);
    };
    /**
     * Create the unique arc from `start` through `through` to `end`.
     *
     * The through point selects the major or minor sweep, including arcs greater
     * than 180°. Return `null` when the points are collinear.
     *
     * @param reverseDirection Input true to take the complementary sweep
     */
    AcGeCircArc2d.tryCreateByThreePoints = function (start, through, end, reverseDirection) {
        if (reverseDirection === void 0) { reverseDirection = false; }
        return acgeTryCreateArcByThreePoints(start, through, end, reverseDirection);
    };
    /**
     * Create a full circle from center and radius.
     */
    AcGeCircArc2d.tryCreateCircle = function (center, radius) {
        return acgeTryCreateCircle(center, radius);
    };
    /**
     * Create a full circle whose diameter is the segment `p1`–`p2`.
     */
    AcGeCircArc2d.tryCreateCircleByDiameter = function (p1, p2) {
        return acgeTryCreateCircleByDiameter(p1, p2);
    };
    /**
     * Create a full circle through three non-collinear points.
     */
    AcGeCircArc2d.tryCreateCircleByThreePoints = function (p1, p2, p3) {
        return acgeTryCreateCircleByThreePoints(p1, p2, p3);
    };
    /**
     * Create the shorter arc from `start` to `end` on the circle at `center`.
     */
    AcGeCircArc2d.tryCreateShorterArc = function (start, end, center) {
        return acgeTryCreateShorterArc(start, end, center);
    };
    /**
     * Create an arc from center, start, and end with an explicit orientation.
     */
    AcGeCircArc2d.tryCreateByCenterStartEnd = function (center, start, end, clockwise) {
        return acgeTryCreateArcByCenterStartEnd(center, start, end, clockwise);
    };
    /**
     * Create a center-start arc whose end is the radial projection of `rawEnd`.
     */
    AcGeCircArc2d.tryCreateByCenterStartProjectedEnd = function (center, start, rawEnd, clockwise) {
        return acgeTryCreateArcByCenterStartProjectedEnd(center, start, rawEnd, clockwise);
    };
    /**
     * Create a center-start arc from a signed included angle.
     * Positive sweep is counterclockwise.
     */
    AcGeCircArc2d.tryCreateByCenterStartSweep = function (center, start, sweepRad) {
        return acgeTryCreateArcByCenterStartSweep(center, start, sweepRad);
    };
    /**
     * Create a center-start arc from a signed chord length.
     */
    AcGeCircArc2d.tryCreateByCenterStartChord = function (center, start, chordLength) {
        return acgeTryCreateArcByCenterStartChord(center, start, chordLength);
    };
    /**
     * Create a start-end arc from a signed included angle.
     * Positive sweep is counterclockwise.
     */
    AcGeCircArc2d.tryCreateByStartEndAngle = function (start, end, sweepRad) {
        return acgeTryCreateArcByStartEndAngle(start, end, sweepRad);
    };
    /**
     * Create a start-end arc from a tangent direction at the start point.
     */
    AcGeCircArc2d.tryCreateByStartEndDirection = function (start, end, directionRad) {
        return acgeTryCreateArcByStartEndDirection(start, end, directionRad);
    };
    /**
     * Create a start-end arc from a signed radius.
     * Positive radius is counterclockwise.
     */
    AcGeCircArc2d.tryCreateByStartEndRadius = function (start, end, radius) {
        return acgeTryCreateArcByStartEndRadius(start, end, radius);
    };
    /**
     * Create arc by three points
     * @param p1 Input the start point
     * @param p2 Input one point between the start point and the end point
     * @param p3 Input the end point
     */
    AcGeCircArc2d.prototype.createByThreePoints = function (p1, p2, p3) {
        var arc = acgeTryCreateArcByThreePoints(p1, p2, p3);
        if (!arc)
            throw AcCmErrors.ILLEGAL_PARAMETERS;
        this.center = arc.center;
        this.radius = arc.radius;
        this._clockwise = arc._clockwise;
        this._startAngle = arc._startAngle;
        this._endAngle = arc._endAngle;
        this._boundingBoxNeedsUpdate = true;
    };
    /**
     * Create circular arc by two points and one bugle factor
     * @param from Input start point
     * @param to Input end point
     * @param bulge Input the bulge factor used to indicate how much of an arc segment is present at this
     * vertex. The bulge factor is the tangent of one fourth the included angle for an arc segment, made
     * negative if the arc goes clockwise from the start point to the endpoint. A bulge of 0 indicates a
     * straight segment, and a bulge of 1 is a semicircle. Get more details from the following links.
     * - https://ezdxf.readthedocs.io/en/stable/dxfentities/lwpolyline.html
     * - https://www.afralisp.net/archive/lisp/Bulges1.htm
     */
    AcGeCircArc2d.prototype.createByStartEndPointsAndBulge = function (from, to, bulge) {
        var theta;
        var a;
        var b;
        if (bulge < 0) {
            theta = Math.atan(-bulge) * 4;
            a = new AcGeVector2d(from);
            b = new AcGeVector2d(to);
        }
        else {
            // Default is counter-clockwise
            theta = Math.atan(bulge) * 4;
            a = new AcGeVector2d(to);
            b = new AcGeVector2d(from);
        }
        var ab = new AcGeVector2d().subVectors(b, a);
        var lengthAB = ab.length();
        var c = new AcGeVector2d().addVectors(a, ab.multiplyScalar(0.5));
        // Distance from center of arc to line between form and to points
        var lengthCD = Math.abs(lengthAB / 2 / Math.tan(theta / 2));
        var normAB = ab.normalize();
        // Calculate perpendicular vector: counter-clockwise for positive bulge, clockwise for negative bulge
        var angle = bulge < 0 ? -Math.PI / 2 : Math.PI / 2;
        var normPerp = new AcGeVector2d(normAB.x * Math.cos(angle) - normAB.y * Math.sin(angle), normAB.y * Math.cos(angle) + normAB.x * Math.sin(angle));
        var d;
        if (theta < Math.PI) {
            // d is the center of the arc
            if (bulge < 0) {
                d = c.add(normPerp.multiplyScalar(lengthCD));
            }
            else {
                d = c.add(normPerp.multiplyScalar(-lengthCD));
            }
        }
        else {
            // d is the center of the arc
            if (bulge < 0) {
                d = c.add(normPerp.multiplyScalar(-lengthCD));
            }
            else {
                d = c.add(normPerp.multiplyScalar(lengthCD));
            }
        }
        // Add points between start start and eng angle relative
        // to the center point
        if (bulge < 0) {
            this._startAngle = Math.atan2(a.y - d.y, a.x - d.x);
            this._endAngle = Math.atan2(b.y - d.y, b.x - d.x);
        }
        else {
            this._startAngle = Math.atan2(b.y - d.y, b.x - d.x);
            this._endAngle = Math.atan2(a.y - d.y, a.x - d.x);
        }
        this._clockwise = bulge < 0;
        this.center = d;
        this.radius = b.sub(d).length();
    };
    Object.defineProperty(AcGeCircArc2d.prototype, "center", {
        /**
         * Center of circular arc
         */
        get: function () {
            return this._center;
        },
        set: function (value) {
            this._center = new AcGePoint2d(value.x, value.y);
            this._boundingBoxNeedsUpdate = true;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcGeCircArc2d.prototype, "radius", {
        /**
         * Radius of circular arc
         */
        get: function () {
            return this._radius;
        },
        set: function (value) {
            this._radius = value;
            this._boundingBoxNeedsUpdate = true;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcGeCircArc2d.prototype, "startAngle", {
        /**
         * Start angle in radians of circular arc in the range 0 to 2 * PI.
         * If clockwise=true, angles are mirrored (0 = +X, 270° = +Y, 180° = -X, 90° = -Y).
         * If clockwise=false, angles are in normal mathematical sense (0 = +X, 90° = +Y, 180° = -X, 270° = -Y).
         */
        get: function () {
            return this._clockwise
                ? this._mirrorAngle(this._startAngle)
                : this._startAngle;
        },
        set: function (value) {
            this._startAngle = this._clockwise
                ? this._mirrorAngle(AcGeMathUtil.normalizeAngle(value))
                : AcGeMathUtil.normalizeAngle(value);
            this._boundingBoxNeedsUpdate = true;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcGeCircArc2d.prototype, "endAngle", {
        /**
         * End angle in radians of circular arc in the range 0 to 2 * PI.
         * If clockwise=true, angles are mirrored (0 = +X, 270° = +Y, 180° = -X, 90° = -Y).
         * If clockwise=false, angles are in normal mathematical sense (0 = +X, 90° = +Y, 180° = -X, 270° = -Y).
         */
        get: function () {
            return this._clockwise ? this._mirrorAngle(this._endAngle) : this._endAngle;
        },
        set: function (value) {
            var normalizedValue = this.startAngle == 0 && value == TAU
                ? value
                : AcGeMathUtil.normalizeAngle(value);
            this._endAngle = this._clockwise
                ? this._mirrorAngle(normalizedValue)
                : normalizedValue;
            this._boundingBoxNeedsUpdate = true;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Mirror an angle for clockwise mode: 0° stays 0°, 90° becomes 270°, 180° stays 180°, 270° becomes 90°
     * @param angle Input angle in radians
     * @returns Mirrored angle in radians
     */
    AcGeCircArc2d.prototype._mirrorAngle = function (angle) {
        // Convert to degrees for easier calculation
        var degrees = (angle * 180) / Math.PI;
        // Mirror: 0°→0°, 90°→270°, 180°→180°, 270°→90°
        var mirroredDegrees = (360 - degrees) % 360;
        return (mirroredDegrees * Math.PI) / 180;
    };
    /**
     * Get the internal (unmirrored) angle for calculations
     * @param angle Input angle (may be mirrored)
     * @returns Internal angle for calculations
     */
    AcGeCircArc2d.prototype._getInternalAngle = function (angle) {
        return this._clockwise ? this._mirrorAngle(angle) : angle;
    };
    Object.defineProperty(AcGeCircArc2d.prototype, "deltaAngle", {
        /**
         * Angle between endAngle and startAngle in range 0 to 2*PI
         */
        get: function () {
            var internalStartAngle = this._getInternalAngle(this.startAngle);
            var internalEndAngle = this._getInternalAngle(this.endAngle);
            return this.clockwise
                ? AcGeMathUtil.normalizeAngle(internalStartAngle - internalEndAngle)
                : AcGeMathUtil.normalizeAngle(internalEndAngle - internalStartAngle);
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcGeCircArc2d.prototype, "clockwise", {
        /**
         * Rotation direction of the arc.
         */
        get: function () {
            return this._clockwise;
        },
        set: function (value) {
            this._clockwise = value;
            this._boundingBoxNeedsUpdate = true;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcGeCircArc2d.prototype, "startPoint", {
        /**
         * Start point of circular arc
         */
        get: function () {
            return this.getPointAtAngle(this.startAngle);
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcGeCircArc2d.prototype, "endPoint", {
        /**
         * End point of circular arc
         */
        get: function () {
            return this.getPointAtAngle(this.endAngle);
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcGeCircArc2d.prototype, "midPoint", {
        /**
         * Middle point of circular arc
         */
        get: function () {
            var internalStartAngle = this._getInternalAngle(this.startAngle);
            var internalMidAngle = this._clockwise
                ? AcGeMathUtil.normalizeAngle(internalStartAngle - this.deltaAngle / 2)
                : AcGeMathUtil.normalizeAngle(internalStartAngle + this.deltaAngle / 2);
            var midAngle = this._clockwise
                ? this._mirrorAngle(internalMidAngle)
                : internalMidAngle;
            return this.getPointAtAngle(midAngle);
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcGeCircArc2d.prototype, "closed", {
        /**
         * Return true if its start point is identical to its end point. Otherwise, return false.
         */
        get: function () {
            var internalStartAngle = this._getInternalAngle(this.startAngle);
            var internalEndAngle = this._getInternalAngle(this.endAngle);
            return (Math.abs(internalEndAngle - internalStartAngle) / Math.PI) % 2 == 0;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * @inheritdoc
     */
    AcGeCircArc2d.prototype.calculateBoundingBox = function () {
        var e_1, _a;
        var points = [this.startPoint, this.endPoint];
        var criticalAngles = [0, Math.PI / 2, Math.PI, (3 * Math.PI) / 2];
        try {
            for (var criticalAngles_1 = __values(criticalAngles), criticalAngles_1_1 = criticalAngles_1.next(); !criticalAngles_1_1.done; criticalAngles_1_1 = criticalAngles_1.next()) {
                var angle = criticalAngles_1_1.value;
                var internalAngle = this._getInternalAngle(angle);
                if (AcGeMathUtil.isBetweenAngle(internalAngle, this._getInternalAngle(this.startAngle), this._getInternalAngle(this.endAngle), this.clockwise)) {
                    points.push(this.getPointAtAngle(angle));
                }
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (criticalAngles_1_1 && !criticalAngles_1_1.done && (_a = criticalAngles_1.return)) _a.call(criticalAngles_1);
            }
            finally { if (e_1) throw e_1.error; }
        }
        var xValues = points.map(function (p) { return p.x; });
        var yValues = points.map(function (p) { return p.y; });
        return new AcGeBox2d(new AcGePoint2d(Math.min.apply(Math, __spreadArray([], __read(xValues), false)), Math.min.apply(Math, __spreadArray([], __read(yValues), false))), new AcGePoint2d(Math.max.apply(Math, __spreadArray([], __read(xValues), false)), Math.max.apply(Math, __spreadArray([], __read(yValues), false))));
    };
    Object.defineProperty(AcGeCircArc2d.prototype, "length", {
        /**
         * Get length of circular arc
         */
        get: function () {
            return Math.abs(this.deltaAngle * this.radius);
        },
        enumerable: false,
        configurable: true
    });
    /**
     * @inheritdoc
     */
    AcGeCircArc2d.prototype.transform = function (_matrix) {
        var _this = this;
        var matrix = _matrix;
        var transformedCenter = this.center.clone().applyMatrix2d(matrix);
        var transformedStart = this.startPoint.clone().applyMatrix2d(matrix);
        if (this.closed) {
            this.center = transformedCenter;
            this.radius = transformedCenter.distanceTo(transformedStart);
            this._startAngle = Math.atan2(transformedStart.y - transformedCenter.y, transformedStart.x - transformedCenter.x);
            this._endAngle = this._startAngle;
            this._clockwise =
                matrix.determinant() < 0 ? !this._clockwise : this._clockwise;
            this._boundingBoxNeedsUpdate = true;
            return this;
        }
        var transformedMid = this.midPoint.clone().applyMatrix2d(matrix);
        var transformedEnd = this.endPoint.clone().applyMatrix2d(matrix);
        var transformedArc = new AcGeCircArc2d(transformedStart, transformedMid, transformedEnd);
        var clockwise = matrix.determinant() < 0 ? !this.clockwise : this.clockwise;
        var toPublicAngle = function (angle) {
            var normalized = AcGeMathUtil.normalizeAngle(angle);
            return clockwise ? _this._mirrorAngle(normalized) : normalized;
        };
        this.center = transformedArc.center;
        this.radius = transformedArc.radius;
        this.clockwise = clockwise;
        this.startAngle = toPublicAngle(Math.atan2(transformedStart.y - transformedArc.center.y, transformedStart.x - transformedArc.center.x));
        this.endAngle = toPublicAngle(Math.atan2(transformedEnd.y - transformedArc.center.y, transformedEnd.x - transformedArc.center.x));
        this._boundingBoxNeedsUpdate = true;
        return this;
    };
    /**
     * @inheritdoc
     */
    AcGeCircArc2d.prototype.clone = function () {
        return new AcGeCircArc2d(this.center.clone(), this.radius, this._startAngle, this._endAngle, this.clockwise);
    };
    /**
     * Calculate a point on the ellipse at a given angle.
     * @param angle Input the angle in radians where the point is to be calculated.
     * @returns Return the 2d coordinates of the point on the circular arc.
     */
    AcGeCircArc2d.prototype.getPointAtAngle = function (angle) {
        var internalAngle = this._getInternalAngle(angle);
        var x = this.center.x + this.radius * Math.cos(internalAngle);
        var y = this.center.y + this.radius * Math.sin(internalAngle);
        return new AcGePoint2d(x, y);
    };
    /**
     * Returns quadrant snap points (0°, 90°, 180°, 270°) that lie on this arc.
     * For a full circle, all four points are returned.
     */
    AcGeCircArc2d.prototype.getQuadrantPoints = function () {
        var e_2, _a;
        var points = [];
        var internalStart = this._getInternalAngle(this.startAngle);
        var internalEnd = this._getInternalAngle(this.endAngle);
        var criticalAngles = [0, Math.PI / 2, Math.PI, (3 * Math.PI) / 2];
        try {
            for (var criticalAngles_2 = __values(criticalAngles), criticalAngles_2_1 = criticalAngles_2.next(); !criticalAngles_2_1.done; criticalAngles_2_1 = criticalAngles_2.next()) {
                var angle = criticalAngles_2_1.value;
                var internalAngle = this._getInternalAngle(angle);
                if (AcGeMathUtil.isBetweenAngle(internalAngle, internalStart, internalEnd, this.clockwise)) {
                    points.push(this.getPointAtAngle(angle));
                }
            }
        }
        catch (e_2_1) { e_2 = { error: e_2_1 }; }
        finally {
            try {
                if (criticalAngles_2_1 && !criticalAngles_2_1.done && (_a = criticalAngles_2.return)) _a.call(criticalAngles_2);
            }
            finally { if (e_2) throw e_2.error; }
        }
        return points;
    };
    /**
     * Returns the nearest point on this arc (or full circle) to the given point.
     *
     * @param point - Query point in WCS (XY).
     */
    AcGeCircArc2d.prototype.nearestPoint = function (point) {
        var p = new AcGePoint2d(point.x, point.y);
        var dx = p.x - this.center.x;
        var dy = p.y - this.center.y;
        var dist = Math.hypot(dx, dy);
        if (dist < 1e-12) {
            return this.startPoint.clone();
        }
        var theta = Math.atan2(dy, dx);
        var publicAngle = this.clockwise
            ? this._mirrorAngle(AcGeMathUtil.normalizeAngle(theta))
            : AcGeMathUtil.normalizeAngle(theta);
        var internalAngle = this._getInternalAngle(publicAngle);
        var internalStart = this._getInternalAngle(this.startAngle);
        var internalEnd = this._getInternalAngle(this.endAngle);
        if (AcGeMathUtil.isBetweenAngle(internalAngle, internalStart, internalEnd, this.clockwise)) {
            return this.getPointAtAngle(publicAngle);
        }
        var dStart = p.distanceToSquared(this.startPoint);
        var dEnd = p.distanceToSquared(this.endPoint);
        return dStart <= dEnd ? this.startPoint.clone() : this.endPoint.clone();
    };
    /**
     * How much `query - onCurve` points into this circular arc.
     *
     * Zero when `onCurve` is not an endpoint (within a relative tolerance), or
     * when `query` coincides with `onCurve`. A positive value means `query` lies
     * on this arc's interior side of `onCurve` — useful when two arcs share a
     * vertex and nearest-point distances are equal.
     */
    AcGeCircArc2d.prototype.inwardAlignment = function (onCurve, query) {
        var mx = query.x - onCurve.x;
        var my = query.y - onCurve.y;
        if (mx * mx + my * my < 1e-24)
            return 0;
        var r = this.radius > 0 ? this.radius : 1;
        var endTolSq = Math.max(1e-16, 1e-12 * r * r);
        var atStart = this.startPoint.distanceToSquared(onCurve) <= endTolSq;
        var atEnd = this.endPoint.distanceToSquared(onCurve) <= endTolSq;
        if (!atStart && !atEnd)
            return 0;
        var pts = this.getPoints(8);
        if (pts.length < 3)
            return 0;
        var inward = atStart && !atEnd ? pts[1] : pts[pts.length - 2];
        var ix = inward.x - onCurve.x;
        var iy = inward.y - onCurve.y;
        var ilen = Math.hypot(ix, iy);
        if (!(ilen > 1e-18))
            return 0;
        return (mx * ix + my * iy) / ilen;
    };
    /**
     * Returns perpendicular snap point(s) on this arc from the given point.
     */
    AcGeCircArc2d.prototype.perpendicularPoints = function (point) {
        var e_3, _a;
        var p = new AcGePoint2d(point.x, point.y);
        var dx = p.x - this.center.x;
        var dy = p.y - this.center.y;
        var dist = Math.hypot(dx, dy);
        if (dist < 1e-12)
            return [];
        var nx = dx / dist;
        var ny = dy / dist;
        var candidates = [
            new AcGePoint2d(this.center.x + nx * this.radius, this.center.y + ny * this.radius),
            new AcGePoint2d(this.center.x - nx * this.radius, this.center.y - ny * this.radius)
        ];
        var result = [];
        var internalStart = this._getInternalAngle(this.startAngle);
        var internalEnd = this._getInternalAngle(this.endAngle);
        try {
            for (var candidates_1 = __values(candidates), candidates_1_1 = candidates_1.next(); !candidates_1_1.done; candidates_1_1 = candidates_1.next()) {
                var candidate = candidates_1_1.value;
                var theta = Math.atan2(candidate.y - this.center.y, candidate.x - this.center.x);
                var publicAngle = this.clockwise
                    ? this._mirrorAngle(AcGeMathUtil.normalizeAngle(theta))
                    : AcGeMathUtil.normalizeAngle(theta);
                var internalAngle = this._getInternalAngle(publicAngle);
                if (this.closed ||
                    AcGeMathUtil.isBetweenAngle(internalAngle, internalStart, internalEnd, this.clockwise)) {
                    result.push(candidate);
                }
            }
        }
        catch (e_3_1) { e_3 = { error: e_3_1 }; }
        finally {
            try {
                if (candidates_1_1 && !candidates_1_1.done && (_a = candidates_1.return)) _a.call(candidates_1);
            }
            finally { if (e_3) throw e_3.error; }
        }
        return result;
    };
    /**
     * Returns tangent snap point(s) from the given point to this arc.
     */
    AcGeCircArc2d.prototype.tangentPoints = function (point) {
        var e_4, _a;
        var result = [];
        var dx = point.x - this.center.x;
        var dy = point.y - this.center.y;
        var d = Math.hypot(dx, dy);
        var r = this.radius;
        if (d < r)
            return result;
        var alpha = Math.acos(r / d);
        var baseAngle = Math.atan2(dy, dx);
        var angles = [baseAngle + alpha, baseAngle - alpha];
        var internalStart = this._getInternalAngle(this.startAngle);
        var internalEnd = this._getInternalAngle(this.endAngle);
        try {
            for (var angles_1 = __values(angles), angles_1_1 = angles_1.next(); !angles_1_1.done; angles_1_1 = angles_1.next()) {
                var theta = angles_1_1.value;
                var publicAngle = this.clockwise
                    ? this._mirrorAngle(AcGeMathUtil.normalizeAngle(theta))
                    : AcGeMathUtil.normalizeAngle(theta);
                var internalAngle = this._getInternalAngle(publicAngle);
                if (this.closed ||
                    AcGeMathUtil.isBetweenAngle(internalAngle, internalStart, internalEnd, this.clockwise)) {
                    result.push(this.getPointAtAngle(publicAngle));
                }
            }
        }
        catch (e_4_1) { e_4 = { error: e_4_1 }; }
        finally {
            try {
                if (angles_1_1 && !angles_1_1.done && (_a = angles_1.return)) _a.call(angles_1);
            }
            finally { if (e_4) throw e_4.error; }
        }
        return result;
    };
    /**
     * Divide this arc into the specified nubmer of points and return those points as an array of points.
     * @param numPoints Input the nubmer of points returned
     * @returns Return an array of points
     */
    AcGeCircArc2d.prototype.getPoints = function (numPoints) {
        if (numPoints === void 0) { numPoints = 100; }
        var points = [];
        var deltaAngle = this.deltaAngle;
        var internalStartAngle = this._getInternalAngle(this.startAngle);
        if (this.closed) {
            deltaAngle = TAU;
            internalStartAngle = 0;
        }
        if (this.clockwise) {
            for (var i = 0; i <= numPoints; i++) {
                var internalAngle = internalStartAngle - deltaAngle * (i / numPoints);
                var angle = this._clockwise
                    ? this._mirrorAngle(internalAngle)
                    : internalAngle;
                var point = this.getPointAtAngle(angle);
                points.push(new AcGePoint2d(point.x, point.y));
            }
        }
        else {
            for (var i = 0; i <= numPoints; i++) {
                var internalAngle = internalStartAngle + deltaAngle * (i / numPoints);
                var angle = this._clockwise
                    ? this._mirrorAngle(internalAngle)
                    : internalAngle;
                var point = this.getPointAtAngle(angle);
                points.push(new AcGePoint2d(point.x, point.y));
            }
        }
        return points;
    };
    /**
     * Sample this arc to a polyline whose chord height is bounded by `options`.
     *
     * Uses a closed-form segment count, then {@link getPoints}. A full circle
     * with default options still uses 100 segments; a short arc uses fewer.
     *
     * @param options - Chord-height tessellation options
     */
    AcGeCircArc2d.prototype.tessellate = function (options) {
        var _a;
        var sweep = this.closed ? TAU : this.deltaAngle;
        var numPoints = AcGeCircArc2d.segmentCount(this.radius, sweep, __assign(__assign({}, options), { minSegments: (_a = options === null || options === void 0 ? void 0 : options.minSegments) !== null && _a !== void 0 ? _a : (this.closed ? 8 : 3) }));
        return this.getPoints(numPoints);
    };
    /**
     * Clamp a VIEWRES-like side count into the legal VPORT range.
     *
     * @param circleSides - Raw side count; non-finite values fall back to the default
     */
    AcGeCircArc2d.resolveCircleSides = function (circleSides) {
        if (circleSides == null || !Number.isFinite(circleSides)) {
            return AcGeCircArc2d.DEFAULT_CIRCLE_SIDES;
        }
        return acgeClamp(Math.round(circleSides), AcGeCircArc2d.MIN_CIRCLE_SIDES, AcGeCircArc2d.MAX_CIRCLE_SIDES);
    };
    /**
     * Fill in `circleSides` and `maxSegments`, leaving per-curve deviation and
     * `minSegments` for the caller to resolve.
     */
    AcGeCircArc2d.resolveTessellateOptions = function (options) {
        var _a;
        var circleSides = AcGeCircArc2d.resolveCircleSides(options === null || options === void 0 ? void 0 : options.circleSides);
        var maxSegments = Math.max(1, Math.round((_a = options === null || options === void 0 ? void 0 : options.maxSegments) !== null && _a !== void 0 ? _a : circleSides));
        return {
            deviation: options === null || options === void 0 ? void 0 : options.deviation,
            circleSides: circleSides,
            minSegments: options === null || options === void 0 ? void 0 : options.minSegments,
            maxSegments: maxSegments
        };
    };
    /**
     * Chord height of a full circle tessellated into `circleSides` equal segments.
     *
     * `s = r * (1 - cos(π / n))`. Used as the default world-space deviation so a
     * full circle still uses about `circleSides` segments.
     */
    AcGeCircArc2d.chordDeviationFromRadius = function (radius, circleSides) {
        if (circleSides === void 0) { circleSides = AcGeCircArc2d.DEFAULT_CIRCLE_SIDES; }
        var r = Math.abs(radius);
        var n = AcGeCircArc2d.resolveCircleSides(circleSides);
        if (r <= FLOAT_TOL) {
            return FLOAT_TOL;
        }
        return Math.max(FLOAT_TOL, r * (1 - Math.cos(Math.PI / n)));
    };
    /**
     * Number of equal circular-arc segments whose chord height is at most the
     * requested deviation.
     *
     * For the default deviation this equals `ceil(|sweep| / τ * circleSides)`,
     * so a full circle keeps the historical 100-segment look while a short arc
     * uses proportionally fewer evaluations.
     *
     * @param radius - Arc radius in world units
     * @param sweep - Signed or unsigned included angle in radians
     * @param options - Tessellation options
     * @returns Segment count in `[minSegments, maxSegments]`
     */
    AcGeCircArc2d.segmentCount = function (radius, sweep, options) {
        var _a, _b;
        var resolved = AcGeCircArc2d.resolveTessellateOptions(options);
        var sweepAbs = Math.abs(sweep);
        var isFullCircle = sweepAbs >= TAU - 1e-8;
        var minSegments = Math.max(1, Math.round((_a = resolved.minSegments) !== null && _a !== void 0 ? _a : (isFullCircle ? 8 : 3)));
        var maxSegments = Math.max(minSegments, resolved.maxSegments);
        var r = Math.abs(radius);
        if (r <= FLOAT_TOL || sweepAbs <= FLOAT_TOL) {
            return minSegments;
        }
        var deviation = (_b = resolved.deviation) !== null && _b !== void 0 ? _b : AcGeCircArc2d.chordDeviationFromRadius(r, resolved.circleSides);
        var ratio = 1 - deviation / r;
        if (!Number.isFinite(ratio) || ratio >= 1) {
            return maxSegments;
        }
        if (ratio <= -1) {
            return minSegments;
        }
        var theta = 2 * Math.acos(ratio);
        if (theta <= FLOAT_TOL) {
            return maxSegments;
        }
        return acgeClamp(Math.ceil(sweepAbs / theta), minSegments, maxSegments);
    };
    /**
     * Default tessellation side count for a full circle.
     * Matches the historical `getPoints(100)` sampling path.
     */
    AcGeCircArc2d.DEFAULT_CIRCLE_SIDES = 100;
    /** Lower bound for {@link AcGeTessellateOptions.circleSides}. */
    AcGeCircArc2d.MIN_CIRCLE_SIDES = 8;
    /** Upper bound for {@link AcGeTessellateOptions.circleSides} (DXF VPORT range). */
    AcGeCircArc2d.MAX_CIRCLE_SIDES = 20000;
    return AcGeCircArc2d;
}(AcGeCurve2d));
export { AcGeCircArc2d };
//# sourceMappingURL=AcGeCircArc2d.js.map