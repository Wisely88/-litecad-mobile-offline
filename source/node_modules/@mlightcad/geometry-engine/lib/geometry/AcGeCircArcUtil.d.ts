import { AcGePoint2d, AcGePoint2dLike, AcGePoint3d, AcGePoint3dLike } from '../math';
/**
 * Circumcircle of three points in the XY plane.
 */
export interface AcGeCircumcircle2d {
    center: AcGePoint2d;
    radius: number;
}
/**
 * Circumcircle of three non-collinear points in 3d.
 */
export interface AcGeCircumcircle3d {
    center: AcGePoint3d;
    radius: number;
}
/**
 * Return the XY circumcircle of three points, or `null` when they are collinear.
 */
export declare function acgeComputeCircumcircle2d(p1: AcGePoint2dLike, p2: AcGePoint2dLike, p3: AcGePoint2dLike): AcGeCircumcircle2d | null;
/**
 * Return the unique circumcircle of three 3d points, or `null` when they are collinear.
 */
export declare function acgeComputeCircumcircle3d(a: AcGePoint3dLike, b: AcGePoint3dLike, c: AcGePoint3dLike): AcGeCircumcircle3d | null;
/**
 * Return the mathematical polar angle of `point` around `center` in XY.
 */
export declare function acgeAngleAtCenter2d(center: AcGePoint2dLike, point: AcGePoint2dLike): number;
/**
 * XY distance between two points.
 */
export declare function acgeDistance2d(p1: AcGePoint2dLike, p2: AcGePoint2dLike): number;
/**
 * Project `point` radially onto the circle `(center, radius)`.
 * Return `null` when the point coincides with the center.
 */
export declare function acgeProjectPointOntoCircle2d(center: AcGePoint2dLike, radius: number, point: AcGePoint2dLike): AcGePoint2d | null;
/**
 * True when `point` lies on the circle `(center, radius)` within a radial
 * tolerance. The default tolerance is `max(FLOAT_TOL, radius * 1e-5)`.
 */
export declare function acgePointLiesOnCircle2d(point: AcGePoint2dLike, center: AcGePoint2dLike, radius: number, eps?: number): boolean;
/**
 * True when two circles share the same center and radius within `eps`
 * (default `1e-8`).
 */
export declare function acgeSameCircle2d(center1: AcGePoint2dLike, radius1: number, center2: AcGePoint2dLike, radius2: number, eps?: number): boolean;
/**
 * Lexicographic pick among nearest-point candidates: smaller `distSq` wins;
 * on a near-tie, larger `align` wins.
 *
 * Use this when two geometries share a vertex so nearest-point distances are
 * equal and a later-wins compare would always take the second candidate.
 */
export declare function acgeIsBetterDistanceAlign(distSq: number, align: number, bestDistSq: number, bestAlign: number): boolean;
//# sourceMappingURL=AcGeCircArcUtil.d.ts.map