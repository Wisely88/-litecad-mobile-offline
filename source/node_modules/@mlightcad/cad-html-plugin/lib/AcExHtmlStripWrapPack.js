/**
 * Phone sub-toolbar packing for the offline HTML viewer.
 *
 * Matches cad-simple-ui-plugin wrap-pack:
 * - A single incomplete row stretches so buttons share the strip evenly.
 * - Wrapped rows use a portrait slot; a short last row stays left-aligned.
 *
 * @module acexHtmlStripWrapPack
 * @packageDocumentation
 */
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
import { acexHtmlIsPhoneLayout } from './AcExHtmlDrawerSheet';
var STRIP_IDS = [
    'mlcad-snap-strip',
    'mlcad-measure-strip',
    'mlcad-markup-strip',
    'mlcad-zoom-strip',
    'mlcad-settings-strip',
    'mlcad-locale-strip'
];
var MIN_SLOT = 24;
/** Extra inset vs button height so phone slots stay narrower than simple-ui. */
var HEIGHT_INSET = 16;
/**
 * Computes how many portrait slots fit, and whether a short strip should
 * stretch across the full row.
 */
export function acexHtmlComputeWrapPackSlot(containerWidth, buttonHeight, buttonCount) {
    if (buttonCount === void 0) { buttonCount = 0; }
    var preferredMaxWidth = Math.max(MIN_SLOT, buttonHeight - HEIGHT_INSET);
    if (containerWidth <= 0) {
        return { perRow: 1, slotWidth: preferredMaxWidth, preferredMaxWidth: preferredMaxWidth };
    }
    var perRow = Math.max(1, Math.floor(containerWidth / preferredMaxWidth));
    var count = Math.max(0, buttonCount);
    if (count > 0 && count <= perRow) {
        return {
            perRow: perRow,
            slotWidth: containerWidth / count,
            preferredMaxWidth: preferredMaxWidth
        };
    }
    return {
        perRow: perRow,
        slotWidth: containerWidth / perRow,
        preferredMaxWidth: preferredMaxWidth
    };
}
/**
 * Applies wrap-pack column counts to visible phone strips. No-ops (and
 * clears inline columns) on desktop.
 */
export function acexHtmlSyncStripWrapPack() {
    var e_1, _a;
    try {
        for (var STRIP_IDS_1 = __values(STRIP_IDS), STRIP_IDS_1_1 = STRIP_IDS_1.next(); !STRIP_IDS_1_1.done; STRIP_IDS_1_1 = STRIP_IDS_1.next()) {
            var id = STRIP_IDS_1_1.value;
            var strip = document.getElementById(id);
            if (strip)
                applyStripWrapPack(strip);
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (STRIP_IDS_1_1 && !STRIP_IDS_1_1.done && (_a = STRIP_IDS_1.return)) _a.call(STRIP_IDS_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
}
function applyStripWrapPack(strip) {
    var wrap = strip.parentElement;
    if (!acexHtmlIsPhoneLayout() || (wrap === null || wrap === void 0 ? void 0 : wrap.hidden)) {
        strip.style.removeProperty('grid-template-columns');
        return;
    }
    var buttons = Array.from(strip.querySelectorAll(':scope > .mlcad-tool-btn'));
    if (buttons.length === 0) {
        strip.style.removeProperty('grid-template-columns');
        return;
    }
    var containerWidth = strip.clientWidth;
    if (containerWidth <= 0) {
        // Unhiding a wrap often reports width 0 on the first layout pass.
        // Leave CSS auto-fit in place until a later sync measures the strip.
        return;
    }
    var height = Math.max.apply(Math, __spreadArray(__spreadArray([], __read(buttons.map(function (button) { return button.offsetHeight; })), false), [1], false));
    var perRow = acexHtmlComputeWrapPackSlot(containerWidth, height, buttons.length).perRow;
    var columns = buttons.length <= perRow ? buttons.length : perRow;
    strip.style.gridTemplateColumns = "repeat(".concat(columns, ", minmax(0, 1fr))");
}
//# sourceMappingURL=AcExHtmlStripWrapPack.js.map