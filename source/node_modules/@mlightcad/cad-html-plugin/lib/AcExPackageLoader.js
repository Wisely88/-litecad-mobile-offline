var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
import { decodeChunkGzip } from './AcExChunkBinaryCodec';
import { acexGlobalFetch } from './AcExHtmlPackageBootstrap';
import { decodeOsnapCatalogGzip } from './AcExOsnapCatalogCodec';
import { ACEX_PACKAGE_VERSION, ACEX_SNAPSHOT_VERSION } from './AcExPackageTypes';
import { ACEX_MAX_COMPRESSED_BYTES } from './AcExSnapshotCompression';
/**
 * Default chunk download window for remote (HTTP) packages.
 *
 * Matches the common per-origin HTTP/1.1 connection budget and stays polite
 * on HTTP/2. Only compressed bytes are buffered, so the window costs little
 * memory; inflate/paint remains strictly serial and ordered.
 */
export var ACEX_GEOMETRY_CHUNK_FETCH_CONCURRENCY = 6;
/**
 * Creates {@link AcExOrderedBytePrefetcher}. The first fetch window starts
 * immediately; a new fetch is launched each time the consumer takes an item,
 * keeping the pipeline full until every item has been requested.
 */
export function createAcExOrderedBytePrefetcher(items, concurrency, fetchBytes) {
    var total = items.length;
    // Never launch for an empty item list: the window must stay 0 so
    // `fetchBytes` is only ever invoked with a valid index.
    var windowSize = total > 0
        ? Math.max(1, Math.min(Number.isFinite(concurrency) ? Math.floor(concurrency) : 1, total))
        : 0;
    var inflight = new Array(total);
    var launched = 0;
    var consumed = 0;
    var launch = function (index) {
        var promise = Promise.resolve().then(function () {
            return fetchBytes(items[index], index);
        });
        // The consumer aborts on the first failure and never awaits trailing
        // window requests; attach a no-op handler so they do not surface as
        // unhandled promise rejections. The ordered await still receives the
        // rejection when that entry is consumed.
        promise.catch(function () { });
        inflight[index] = promise;
        launched += 1;
    };
    while (launched < windowSize) {
        launch(launched);
    }
    return {
        next: function () {
            return __awaiter(this, void 0, void 0, function () {
                var index, bytes;
                return __generator(this, function (_a) {
                    switch (_a.label) {
                        case 0:
                            if (consumed >= total) {
                                return [2 /*return*/, null];
                            }
                            index = consumed;
                            consumed += 1;
                            return [4 /*yield*/, inflight[index]];
                        case 1:
                            bytes = _a.sent();
                            if (launched < total) {
                                launch(launched);
                            }
                            return [2 /*return*/, { item: items[index], index: index, bytes: bytes }];
                    }
                });
            });
        }
    };
}
var SAFE_PACKAGE_HREF = /^(?:\.[/\\])?[A-Za-z0-9._-]+(?:[/\\][A-Za-z0-9._-]+)*$/;
/**
 * Returns true when `href` is a relative package path (no scheme, `..`, or
 * absolute URL). Used for manifest chunk refs and package `manifestUrl`.
 */
export function isSafePackageHref(href) {
    var trimmed = href.trim();
    if (!trimmed || trimmed !== href) {
        return false;
    }
    if (trimmed.startsWith('/') ||
        trimmed.startsWith('\\') ||
        trimmed.startsWith('//') ||
        /^[a-zA-Z][a-zA-Z0-9+.-]*:/.test(trimmed)) {
        return false;
    }
    if (trimmed.includes('..') || trimmed.includes('\\')) {
        return false;
    }
    return SAFE_PACKAGE_HREF.test(trimmed);
}
function assertSafePackageHref(href, label) {
    if (!isSafePackageHref(href)) {
        throw new Error("Invalid ".concat(label, ": must be a relative package path"));
    }
}
/**
 * Parses and validates a package manifest JSON value.
 */
export function parseAcExPackageManifest(data) {
    var e_1, _a, e_2, _b;
    var _c;
    if (!data || typeof data !== 'object') {
        throw new Error('Invalid package manifest');
    }
    var manifest = data;
    if (manifest.format !== 'acex-package') {
        throw new Error('Unsupported package format');
    }
    if (manifest.packageVersion !== ACEX_PACKAGE_VERSION) {
        throw new Error("Unsupported package version: ".concat(String(manifest.packageVersion)));
    }
    if (manifest.snapshotVersion !== ACEX_SNAPSHOT_VERSION) {
        throw new Error("Unsupported snapshot version: ".concat(String(manifest.snapshotVersion)));
    }
    if (!Array.isArray(manifest.layouts) || !Array.isArray(manifest.chunks)) {
        throw new Error('Invalid package manifest structure');
    }
    try {
        for (var _d = __values(manifest.chunks), _e = _d.next(); !_e.done; _e = _d.next()) {
            var chunk = _e.value;
            if (!chunk || typeof chunk !== 'object') {
                throw new Error('Invalid package chunk entry');
            }
            assertSafePackageHref(chunk.href, 'chunk href');
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (_e && !_e.done && (_a = _d.return)) _a.call(_d);
        }
        finally { if (e_1) throw e_1.error; }
    }
    try {
        for (var _f = __values((_c = manifest.osnapChunks) !== null && _c !== void 0 ? _c : []), _g = _f.next(); !_g.done; _g = _f.next()) {
            var chunk = _g.value;
            if (!chunk || typeof chunk !== 'object') {
                throw new Error('Invalid package osnap chunk entry');
            }
            assertSafePackageHref(chunk.href, 'osnap chunk href');
        }
    }
    catch (e_2_1) { e_2 = { error: e_2_1 }; }
    finally {
        try {
            if (_g && !_g.done && (_b = _f.return)) _b.call(_f);
        }
        finally { if (e_2) throw e_2.error; }
    }
    return manifest;
}
/**
 * Builds an empty {@link AcExSnapshot} skeleton from a manifest (no geometry yet).
 * OSNAP catalogs are loaded later via {@link loadAcExPackageLayoutOsnap}.
 */
export function snapshotSkeletonFromManifest(manifest) {
    return {
        version: ACEX_SNAPSHOT_VERSION,
        meta: manifest.meta,
        layers: manifest.layers,
        activeLayoutBtrId: manifest.activeLayoutBtrId,
        layouts: manifest.layouts.map(function (layout) { return ({
            btrId: layout.btrId,
            name: layout.name,
            isModelSpace: layout.isModelSpace,
            lineBatches: [],
            meshBatches: [],
            viewports: layout.viewports
        }); })
    };
}
/**
 * Resolves a chunk href against the manifest URL.
 * Only relative package paths under the manifest directory are allowed.
 */
export function resolveChunkUrl(manifestUrl, href) {
    assertSafePackageHref(href, 'chunk href');
    var resolved;
    var manifest;
    try {
        resolved = new URL(href, manifestUrl);
        manifest = new URL(manifestUrl);
    }
    catch (_a) {
        throw new Error('Invalid chunk or manifest URL');
    }
    if (resolved.protocol !== manifest.protocol ||
        resolved.host !== manifest.host) {
        throw new Error('Chunk URL must share the manifest origin');
    }
    var basePath = manifest.pathname.replace(/[^/]*$/, '');
    var resolvedPath = resolved.pathname;
    if (!resolvedPath.startsWith(basePath)) {
        throw new Error('Chunk URL escapes package directory');
    }
    // Reject encoded `..` segments after URL normalization.
    if (resolvedPath.split('/').includes('..')) {
        throw new Error('Chunk URL escapes package directory');
    }
    return resolved.toString();
}
/**
 * Resolves a package `manifestUrl` from viewer.html config against the page URL.
 * Absolute and cross-origin URLs are rejected.
 */
export function resolvePackageManifestUrl(manifestUrl, pageUrl) {
    assertSafePackageHref(manifestUrl, 'manifestUrl');
    var resolved;
    var page;
    try {
        resolved = new URL(manifestUrl, pageUrl);
        page = new URL(pageUrl);
    }
    catch (_a) {
        throw new Error('Invalid manifestUrl');
    }
    if (resolved.protocol !== page.protocol || resolved.host !== page.host) {
        throw new Error('manifestUrl must be same-origin');
    }
    return resolved.toString();
}
function fetchCompressedBytes(fetchImpl, url, label) {
    return __awaiter(this, void 0, void 0, function () {
        var response, contentLength, declared, buffer;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0: return [4 /*yield*/, fetchImpl(url)];
                case 1:
                    response = _a.sent();
                    if (!response.ok) {
                        throw new Error("Failed to load ".concat(label, " (").concat(response.status, ")"));
                    }
                    contentLength = response.headers.get('content-length');
                    if (contentLength != null) {
                        declared = Number(contentLength);
                        if (Number.isFinite(declared) && declared > ACEX_MAX_COMPRESSED_BYTES) {
                            throw new Error("".concat(label, " exceeds size limit"));
                        }
                    }
                    return [4 /*yield*/, response.arrayBuffer()];
                case 2:
                    buffer = _a.sent();
                    if (buffer.byteLength > ACEX_MAX_COMPRESSED_BYTES) {
                        throw new Error("".concat(label, " exceeds size limit"));
                    }
                    return [2 /*return*/, new Uint8Array(buffer)];
            }
        });
    });
}
/**
 * Fetches the package manifest, then progressively downloads geometry chunks.
 *
 * Chunk bytes download through a bounded parallel window (see
 * {@link createAcExOrderedBytePrefetcher}), but gunzip, batch append, and the
 * `onChunk` paint callback run strictly one at a time in manifest order, so
 * progressive rendering is unchanged while network time is overlapped.
 * OSNAP sidecars load afterward (unless {@link AcExPackageLoaderOptions.loadOsnap}
 * is `false`) so display data is not blocked by snap catalogs.
 */
export function loadAcExPackage(options) {
    return __awaiter(this, void 0, void 0, function () {
        var fetchImpl, loadOsnap, concurrency, manifestResponse, manifest, _a, snapshot, layoutById, filter, chunksToLoad, prefetcher, loadedChunks, entry, chunkRef, layout, decoded, _b, _c, layoutRef, layout, e_3_1;
        var e_3, _d;
        var _e, _f, _g;
        return __generator(this, function (_h) {
            switch (_h.label) {
                case 0:
                    fetchImpl = (_e = options.fetchImpl) !== null && _e !== void 0 ? _e : acexGlobalFetch;
                    loadOsnap = options.loadOsnap !== false;
                    concurrency = (_f = options.fetchConcurrency) !== null && _f !== void 0 ? _f : ACEX_GEOMETRY_CHUNK_FETCH_CONCURRENCY;
                    return [4 /*yield*/, fetchImpl(options.manifestUrl)];
                case 1:
                    manifestResponse = _h.sent();
                    if (!manifestResponse.ok) {
                        throw new Error("Failed to load package manifest (".concat(manifestResponse.status, ")"));
                    }
                    _a = parseAcExPackageManifest;
                    return [4 /*yield*/, manifestResponse.json()];
                case 2:
                    manifest = _a.apply(void 0, [_h.sent()]);
                    snapshot = snapshotSkeletonFromManifest(manifest);
                    layoutById = new Map(snapshot.layouts.map(function (layout) { return [layout.btrId, layout]; }));
                    filter = options.layoutFilter == null
                        ? null
                        : options.layoutFilter instanceof Set
                            ? options.layoutFilter
                            : new Set(options.layoutFilter);
                    chunksToLoad = manifest.chunks.filter(function (chunk) { return filter == null || filter.has(chunk.layoutBtrId); });
                    prefetcher = createAcExOrderedBytePrefetcher(chunksToLoad, concurrency, function (chunkRef) {
                        return fetchCompressedBytes(fetchImpl, resolveChunkUrl(options.manifestUrl, chunkRef.href), 'geometry chunk');
                    });
                    loadedChunks = 0;
                    _h.label = 3;
                case 3: return [4 /*yield*/, prefetcher.next()];
                case 4:
                    entry = _h.sent();
                    if (!entry) {
                        return [3 /*break*/, 7];
                    }
                    chunkRef = entry.item;
                    layout = layoutById.get(chunkRef.layoutBtrId);
                    if (!layout) {
                        throw new Error('Unknown layout for package chunk');
                    }
                    decoded = decodeChunkGzip(entry.bytes);
                    if (decoded.layoutBtrId !== chunkRef.layoutBtrId) {
                        throw new Error('Chunk layout mismatch');
                    }
                    appendBatches(layout, decoded.lineBatches, decoded.meshBatches);
                    loadedChunks += 1;
                    return [4 /*yield*/, ((_g = options.onChunk) === null || _g === void 0 ? void 0 : _g.call(options, layout, chunkRef, {
                            loadedChunks: loadedChunks,
                            totalChunks: chunksToLoad.length,
                            layoutBtrId: chunkRef.layoutBtrId,
                            chunkId: chunkRef.id
                        }))];
                case 5:
                    _h.sent();
                    _h.label = 6;
                case 6: return [3 /*break*/, 3];
                case 7:
                    if (!loadOsnap) return [3 /*break*/, 15];
                    _h.label = 8;
                case 8:
                    _h.trys.push([8, 13, 14, 15]);
                    _b = __values(manifest.layouts), _c = _b.next();
                    _h.label = 9;
                case 9:
                    if (!!_c.done) return [3 /*break*/, 12];
                    layoutRef = _c.value;
                    if (filter != null && !filter.has(layoutRef.btrId)) {
                        return [3 /*break*/, 11];
                    }
                    layout = layoutById.get(layoutRef.btrId);
                    if (!layout) {
                        return [3 /*break*/, 11];
                    }
                    return [4 /*yield*/, loadAcExPackageLayoutOsnap(manifest, options.manifestUrl, layoutRef.btrId, layout, { fetchImpl: fetchImpl })];
                case 10:
                    _h.sent();
                    _h.label = 11;
                case 11:
                    _c = _b.next();
                    return [3 /*break*/, 9];
                case 12: return [3 /*break*/, 15];
                case 13:
                    e_3_1 = _h.sent();
                    e_3 = { error: e_3_1 };
                    return [3 /*break*/, 15];
                case 14:
                    try {
                        if (_c && !_c.done && (_d = _b.return)) _d.call(_b);
                    }
                    finally { if (e_3) throw e_3.error; }
                    return [7 /*endfinally*/];
                case 15: return [2 /*return*/, snapshot];
            }
        });
    });
}
/**
 * Loads geometry chunks for a single layout into an existing snapshot skeleton.
 * Does not load OSNAP — call {@link loadAcExPackageLayoutOsnap} after paint.
 */
export function loadAcExPackageLayout(manifest_1, manifestUrl_1, layoutBtrId_1, layout_1) {
    return __awaiter(this, arguments, void 0, function (manifest, manifestUrl, layoutBtrId, layout, options) {
        var fetchImpl, concurrency, layoutRef, chunkById, chunks, prefetcher, loadedChunks, entry, chunkRef, decoded;
        var _a, _b, _c;
        if (options === void 0) { options = {}; }
        return __generator(this, function (_d) {
            switch (_d.label) {
                case 0:
                    fetchImpl = (_a = options.fetchImpl) !== null && _a !== void 0 ? _a : acexGlobalFetch;
                    concurrency = (_b = options.fetchConcurrency) !== null && _b !== void 0 ? _b : ACEX_GEOMETRY_CHUNK_FETCH_CONCURRENCY;
                    layoutRef = manifest.layouts.find(function (l) { return l.btrId === layoutBtrId; });
                    if (!layoutRef) {
                        throw new Error("Layout not found: ".concat(layoutBtrId));
                    }
                    chunkById = new Map(manifest.chunks.map(function (c) { return [c.id, c]; }));
                    chunks = layoutRef.chunkIds
                        .map(function (id) { return chunkById.get(id); })
                        .filter(function (c) { return c != null; });
                    prefetcher = createAcExOrderedBytePrefetcher(chunks, concurrency, function (chunkRef) {
                        return fetchCompressedBytes(fetchImpl, resolveChunkUrl(manifestUrl, chunkRef.href), 'geometry chunk');
                    });
                    loadedChunks = 0;
                    _d.label = 1;
                case 1: return [4 /*yield*/, prefetcher.next()];
                case 2:
                    entry = _d.sent();
                    if (!entry) {
                        return [3 /*break*/, 5];
                    }
                    chunkRef = entry.item;
                    decoded = decodeChunkGzip(entry.bytes);
                    appendBatches(layout, decoded.lineBatches, decoded.meshBatches);
                    loadedChunks += 1;
                    return [4 /*yield*/, ((_c = options.onChunk) === null || _c === void 0 ? void 0 : _c.call(options, layout, chunkRef, {
                            loadedChunks: loadedChunks,
                            totalChunks: chunks.length,
                            layoutBtrId: layoutBtrId,
                            chunkId: chunkRef.id
                        }))];
                case 3:
                    _d.sent();
                    _d.label = 4;
                case 4: return [3 /*break*/, 1];
                case 5: return [2 /*return*/];
            }
        });
    });
}
/**
 * Loads OSNAP ACEO chunks for one layout (no-op when absent).
 *
 * Compressed chunks download through a bounded parallel window
 * ({@link createAcExOrderedBytePrefetcher}), but gunzip/decode never runs on
 * more than one chunk at a time and primitives are assembled in
 * `osnapChunkIds` order — parallel decode of dozens of ~500 KiB ACEO payloads
 * would freeze the main thread on large drawings.
 */
export function loadAcExPackageLayoutOsnap(manifest_1, manifestUrl_1, layoutBtrId_1, layout_1) {
    return __awaiter(this, arguments, void 0, function (manifest, manifestUrl, layoutBtrId, layout, options) {
        var layoutRef, ids, osnapById, refs, fetchImpl, concurrency, yieldFn, totalPrimitives, primitives, writeOffset, prefetcher, loadedChunks, entry, chunkRef, decoded, slice, p, slice_1, slice_1_1, primitive;
        var e_4, _a;
        var _b, _c, _d, _e, _f;
        if (options === void 0) { options = {}; }
        return __generator(this, function (_g) {
            switch (_g.label) {
                case 0:
                    if (layout.osnap) {
                        return [2 /*return*/];
                    }
                    layoutRef = manifest.layouts.find(function (l) { return l.btrId === layoutBtrId; });
                    if (!layoutRef) {
                        return [2 /*return*/];
                    }
                    ids = layoutRef.osnapChunkIds;
                    if (!ids || ids.length === 0) {
                        return [2 /*return*/];
                    }
                    osnapById = new Map(((_b = manifest.osnapChunks) !== null && _b !== void 0 ? _b : []).map(function (chunk) { return [chunk.id, chunk]; }));
                    refs = ids
                        .map(function (id) { return osnapById.get(id); })
                        .filter(function (c) { return c != null; });
                    if (refs.length === 0) {
                        return [2 /*return*/];
                    }
                    fetchImpl = (_c = options.fetchImpl) !== null && _c !== void 0 ? _c : acexGlobalFetch;
                    concurrency = (_d = options.fetchConcurrency) !== null && _d !== void 0 ? _d : ACEX_GEOMETRY_CHUNK_FETCH_CONCURRENCY;
                    yieldFn = (_e = options.yieldFn) !== null && _e !== void 0 ? _e : (function () {
                        return new Promise(function (resolve) {
                            setTimeout(resolve, 0);
                        });
                    });
                    totalPrimitives = refs.reduce(function (sum, ref) { return sum + ref.primitiveCount; }, 0);
                    primitives = totalPrimitives > 0 ? new Array(totalPrimitives) : [];
                    writeOffset = 0;
                    prefetcher = createAcExOrderedBytePrefetcher(refs, concurrency, function (chunkRef) {
                        return fetchCompressedBytes(fetchImpl, resolveChunkUrl(manifestUrl, chunkRef.href), 'osnap chunk');
                    });
                    loadedChunks = 0;
                    _g.label = 1;
                case 1: return [4 /*yield*/, prefetcher.next()];
                case 2:
                    entry = _g.sent();
                    if (!entry) {
                        return [3 /*break*/, 6];
                    }
                    chunkRef = entry.item;
                    decoded = decodeOsnapCatalogGzip(entry.bytes);
                    slice = decoded.primitives;
                    if (totalPrimitives > 0) {
                        for (p = 0; p < slice.length; p++) {
                            primitives[writeOffset++] = slice[p];
                        }
                    }
                    else {
                        try {
                            for (slice_1 = (e_4 = void 0, __values(slice)), slice_1_1 = slice_1.next(); !slice_1_1.done; slice_1_1 = slice_1.next()) {
                                primitive = slice_1_1.value;
                                primitives.push(primitive);
                            }
                        }
                        catch (e_4_1) { e_4 = { error: e_4_1 }; }
                        finally {
                            try {
                                if (slice_1_1 && !slice_1_1.done && (_a = slice_1.return)) _a.call(slice_1);
                            }
                            finally { if (e_4) throw e_4.error; }
                        }
                    }
                    loadedChunks += 1;
                    return [4 /*yield*/, ((_f = options.onChunk) === null || _f === void 0 ? void 0 : _f.call(options, {
                            loadedChunks: loadedChunks,
                            totalChunks: refs.length,
                            layoutBtrId: layoutBtrId,
                            chunkId: chunkRef.id
                        }))];
                case 3:
                    _g.sent();
                    return [4 /*yield*/, yieldFn()];
                case 4:
                    _g.sent();
                    _g.label = 5;
                case 5: return [3 /*break*/, 1];
                case 6:
                    if (totalPrimitives > 0 && writeOffset !== totalPrimitives) {
                        primitives.length = writeOffset;
                    }
                    layout.osnap = { primitives: primitives };
                    return [2 /*return*/];
            }
        });
    });
}
function appendBatches(layout, lineBatches, meshBatches) {
    var _a, _b;
    (_a = layout.lineBatches).push.apply(_a, __spreadArray([], __read(lineBatches), false));
    (_b = layout.meshBatches).push.apply(_b, __spreadArray([], __read(meshBatches), false));
}
//# sourceMappingURL=AcExPackageLoader.js.map