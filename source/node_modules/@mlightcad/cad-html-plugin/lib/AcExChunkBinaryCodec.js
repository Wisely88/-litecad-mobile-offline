var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { readLineBatch, readMeshBatch, writeLineBatch, writeMeshBatch } from './AcExBatchBinaryCodec';
import { AcExBinaryReader, AcExBinaryWriter } from './AcExBinaryIO';
import { compressSnapshotBinary, decompressSnapshotBinary } from './AcExSnapshotCompression';
import { ACEX_SNAPSHOT_VERSION } from './AcExSnapshotTypes';
/** Magic for ACEC geometry chunks (`ACEC` little-endian). */
export var ACEC_CHUNK_MAGIC = 0x43454341;
/** Hard cap on line/mesh batch count inside one ACEC chunk. */
export var ACEX_MAX_BATCHES_PER_CHUNK = 100000;
/**
 * Encodes an uncompressed ACEC geometry chunk.
 */
export function encodeChunkBinary(chunk) {
    var e_1, _a, e_2, _b;
    if (chunk.version !== ACEX_SNAPSHOT_VERSION) {
        throw new Error("Unsupported chunk version: ".concat(chunk.version));
    }
    var writer = new AcExBinaryWriter();
    writer.writeU32(ACEC_CHUNK_MAGIC);
    writer.writeU8(ACEX_SNAPSHOT_VERSION);
    writer.writeU8(0);
    writer.writeU8(0);
    writer.writeU8(0);
    writer.writeString(chunk.layoutBtrId);
    writer.writeU32(chunk.lineBatches.length);
    try {
        for (var _c = __values(chunk.lineBatches), _d = _c.next(); !_d.done; _d = _c.next()) {
            var batch = _d.value;
            writeLineBatch(writer, batch);
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
        }
        finally { if (e_1) throw e_1.error; }
    }
    writer.writeU32(chunk.meshBatches.length);
    try {
        for (var _e = __values(chunk.meshBatches), _f = _e.next(); !_f.done; _f = _e.next()) {
            var batch = _f.value;
            writeMeshBatch(writer, batch);
        }
    }
    catch (e_2_1) { e_2 = { error: e_2_1 }; }
    finally {
        try {
            if (_f && !_f.done && (_b = _e.return)) _b.call(_e);
        }
        finally { if (e_2) throw e_2.error; }
    }
    return writer.toUint8Array();
}
/**
 * Decodes an uncompressed ACEC geometry chunk.
 */
export function decodeChunkBinary(bytes) {
    var reader = new AcExBinaryReader(bytes);
    var magic = reader.readU32();
    if (magic !== ACEC_CHUNK_MAGIC) {
        throw new Error('Invalid chunk magic');
    }
    var version = reader.readU8();
    reader.readU8();
    reader.readU8();
    reader.readU8();
    if (version !== ACEX_SNAPSHOT_VERSION) {
        throw new Error("Unsupported chunk version: ".concat(version));
    }
    var layoutBtrId = reader.readString();
    var lineBatchCount = reader.readU32();
    if (lineBatchCount > ACEX_MAX_BATCHES_PER_CHUNK) {
        throw new Error('Chunk line batch count exceeds limit');
    }
    var lineBatches = [];
    for (var i = 0; i < lineBatchCount; i++) {
        lineBatches.push(readLineBatch(reader));
    }
    var meshBatchCount = reader.readU32();
    if (meshBatchCount > ACEX_MAX_BATCHES_PER_CHUNK) {
        throw new Error('Chunk mesh batch count exceeds limit');
    }
    var meshBatches = [];
    for (var i = 0; i < meshBatchCount; i++) {
        meshBatches.push(readMeshBatch(reader));
    }
    return {
        version: ACEX_SNAPSHOT_VERSION,
        layoutBtrId: layoutBtrId,
        lineBatches: lineBatches,
        meshBatches: meshBatches
    };
}
/** Encodes and gzip-compresses a geometry chunk (`.acex.gz` payload). */
export function encodeChunkGzip(chunk) {
    var uncompressed = encodeChunkBinary(chunk);
    var compressed = compressSnapshotBinary(uncompressed).bytes;
    return { uncompressed: uncompressed, compressed: compressed };
}
/** Gunzips and decodes a `.acex.gz` geometry chunk payload. */
export function decodeChunkGzip(compressed) {
    return decodeChunkBinary(decompressSnapshotBinary(compressed));
}
//# sourceMappingURL=AcExChunkBinaryCodec.js.map