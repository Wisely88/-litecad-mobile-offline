var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
import { unzipSync, zipSync } from 'fflate';
function isSafeZipPath(path) {
    if (!path || path.startsWith('/') || path.includes('\\') || path.includes('\0')) {
        return false;
    }
    var parts = path.split('/');
    return parts.every(function (part) {
        return part.length > 0 &&
            part !== '.' &&
            part !== '..' &&
            /^[A-Za-z0-9._-]+$/.test(part);
    });
}
/**
 * Zips multi-file package contents for a single browser download.
 * Paths inside the archive match the hosted directory layout.
 */
export function zipAcExPackageFiles(pkg) {
    var e_1, _a;
    var entries = {};
    try {
        for (var _b = __values(pkg.files), _c = _b.next(); !_c.done; _c = _b.next()) {
            var file = _c.value;
            if (!isSafeZipPath(file.path)) {
                throw new Error("Unsafe package path: ".concat(file.path));
            }
            entries[file.path] = file.bytes;
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_1) throw e_1.error; }
    }
    return zipSync(entries, { level: 6 });
}
/**
 * Unzips a package archive produced by {@link zipAcExPackageFiles}.
 * Entries with path traversal or absolute paths are rejected.
 */
export function unzipAcExPackageFiles(bytes) {
    var e_2, _a;
    var entries = unzipSync(bytes);
    var files = [];
    try {
        for (var _b = __values(Object.entries(entries)), _c = _b.next(); !_c.done; _c = _b.next()) {
            var _d = __read(_c.value, 2), path = _d[0], data = _d[1];
            if (!isSafeZipPath(path)) {
                throw new Error('Unsafe path in package archive');
            }
            files.push({ path: path, bytes: data });
        }
    }
    catch (e_2_1) { e_2 = { error: e_2_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_2) throw e_2.error; }
    }
    return files;
}
//# sourceMappingURL=AcExPackageZip.js.map