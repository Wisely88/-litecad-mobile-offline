var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { acexHtmlIsMobileNavUi } from './AcExHtmlDrawerSheet';
/**
 * Temporary plus marks at confirmed pick points during multi-step measure /
 * markup tools on phone and pad.
 *
 * Desktop never shows these marks. Uses {@link acexHtmlIsMobileNavUi} (compact
 * width or coarse pointer) instead of `acedIsMobileOrPadUi` so measure /
 * markup controllers stay free of the `cad-simple-viewer` barrel and stay
 * aligned with HTML mobile pick chrome (snap loupe, hidden Select/Pan).
 * There is no override API in the offline HTML viewer (unlike
 * `AcEdPromptPointOptions.showConfirmedPointMark` in the live CAD viewer).
 */
var AcExConfirmedPointMarks = /** @class */ (function () {
    /**
     * @param host - Overlay host (usually `#mlcad-root` or a measure/markup layer).
     * @param wcsToScreen - Converts WCS to host-relative CSS pixels.
     */
    function AcExConfirmedPointMarks(host, wcsToScreen) {
        this.markers = [];
        this.worldPoints = [];
        this.host = host;
        this.wcsToScreen = wcsToScreen;
        AcExConfirmedPointMarks.injectCss();
        if (getComputedStyle(host).position === 'static') {
            host.style.position = 'relative';
        }
    }
    /**
     * Replaces the mark set with the given world points.
     *
     * No-ops visually on desktop (clears any existing marks).
     */
    AcExConfirmedPointMarks.prototype.setWorldPoints = function (points) {
        if (!acexHtmlIsMobileNavUi()) {
            this.clear();
            return;
        }
        var next = points.map(function (p) { return ({ x: p.x, y: p.y }); });
        var reuse = Math.min(this.markers.length, next.length);
        for (var i = 0; i < reuse; i++) {
            this.place(this.markers[i], next[i]);
        }
        if (next.length < this.markers.length) {
            for (var i = next.length; i < this.markers.length; i++) {
                this.markers[i].remove();
            }
            this.markers.length = next.length;
        }
        else {
            for (var i = reuse; i < next.length; i++) {
                var el = document.createElement('div');
                el.className = 'mlcad-confirmed-point-mark';
                this.host.appendChild(el);
                this.place(el, next[i]);
                this.markers.push(el);
            }
        }
        this.worldPoints = next;
    };
    /** Repositions existing marks after pan/zoom. */
    AcExConfirmedPointMarks.prototype.sync = function () {
        for (var i = 0; i < this.markers.length; i++) {
            var pos = this.worldPoints[i];
            if (!pos)
                continue;
            this.place(this.markers[i], pos);
        }
    };
    /** Removes all marks. */
    AcExConfirmedPointMarks.prototype.clear = function () {
        var e_1, _a;
        try {
            for (var _b = __values(this.markers), _c = _b.next(); !_c.done; _c = _b.next()) {
                var el = _c.value;
                el.remove();
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_1) throw e_1.error; }
        }
        this.markers.length = 0;
        this.worldPoints = [];
    };
    AcExConfirmedPointMarks.prototype.place = function (el, world) {
        var screen = this.wcsToScreen(world);
        el.style.left = "".concat(screen.x, "px");
        el.style.top = "".concat(screen.y, "px");
    };
    AcExConfirmedPointMarks.injectCss = function () {
        if (document.getElementById('mlcad-confirmed-point-mark-style'))
            return;
        var style = document.createElement('style');
        style.id = 'mlcad-confirmed-point-mark-style';
        style.textContent = "\n      .mlcad-confirmed-point-mark {\n        position: absolute;\n        pointer-events: none;\n        z-index: 5;\n        width: 12px;\n        height: 12px;\n        transform: translate(-50%, -50%);\n        box-sizing: border-box;\n        color: var(--mlcad-measure-accent, #08e8de);\n      }\n      .mlcad-confirmed-point-mark::before,\n      .mlcad-confirmed-point-mark::after {\n        content: '';\n        position: absolute;\n        top: 50%;\n        left: 50%;\n        background: currentColor;\n        transform-origin: center;\n      }\n      .mlcad-confirmed-point-mark::before {\n        width: 100%;\n        height: 2px;\n        transform: translate(-50%, -50%);\n      }\n      .mlcad-confirmed-point-mark::after {\n        width: 2px;\n        height: 100%;\n        transform: translate(-50%, -50%);\n      }\n    ";
        document.head.appendChild(style);
    };
    return AcExConfirmedPointMarks;
}());
export { AcExConfirmedPointMarks };
//# sourceMappingURL=AcExConfirmedPointMarks.js.map