var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcEdCommandStack } from '@mlightcad/cad-simple-viewer';
import packageJson from '../package.json';
import { AcApExportHtmlCmd } from './AcApExportHtmlCmd';
/**
 * HTML export plugin for cad-simple-viewer.
 *
 * Registers `-chtml` when loaded. Register this plugin lazily via
 * {@link registerLazyHtmlPlugin} so the export bundle is fetched on demand.
 */
var AcApHtmlPlugin = /** @class */ (function () {
    /**
     * @param options - HTML export options (e.g. where to fetch `viewer-runtime.iife.js`)
     */
    function AcApHtmlPlugin(options) {
        if (options === void 0) { options = {}; }
        this.options = options;
        /** @inheritdoc */
        this.name = 'HtmlPlugin';
        /** @inheritdoc */
        this.version = packageJson.version;
        /** @inheritdoc */
        this.description = 'HTML export (-chtml) command';
        /** Commands registered in {@link onLoad} for cleanup in {@link onUnload}. */
        this.registeredCommands = [];
    }
    /**
     * Registers the `-chtml` system command (command-line, no dialog).
     *
     * @param _context - Application context (unused)
     * @param commandManager - Command stack used to register the HTML export command
     */
    AcApHtmlPlugin.prototype.onLoad = function (_context, commandManager) {
        var group = AcEdCommandStack.SYSTEMT_COMMAND_GROUP_NAME;
        var exportCmd = new AcApExportHtmlCmd(this.options);
        commandManager.addCommand(group, '-chtml', '-chtml', exportCmd);
        this.registeredCommands.push({ group: group, name: '-chtml' });
        if (!commandManager.lookupGlobalCmd('chtml')) {
            commandManager.addCommand(group, 'chtml', 'chtml', exportCmd);
            this.registeredCommands.push({ group: group, name: 'chtml' });
        }
    };
    /**
     * Removes commands registered in {@link onLoad}.
     *
     * @param _context - Application context (unused)
     * @param commandManager - Command stack used to unregister the HTML export command
     */
    AcApHtmlPlugin.prototype.onUnload = function (_context, commandManager) {
        var e_1, _a;
        try {
            for (var _b = __values(this.registeredCommands), _c = _b.next(); !_c.done; _c = _b.next()) {
                var cmd = _c.value;
                commandManager.removeCmd(cmd.group, cmd.name);
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_1) throw e_1.error; }
        }
        this.registeredCommands = [];
    };
    return AcApHtmlPlugin;
}());
export { AcApHtmlPlugin };
//# sourceMappingURL=AcApHtmlPlugin.js.map