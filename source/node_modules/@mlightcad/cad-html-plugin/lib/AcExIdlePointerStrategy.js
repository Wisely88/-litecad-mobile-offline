var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import { acexHtmlIsMobileNavUi } from './AcExHtmlDrawerSheet';
/**
 * Idle (non-drawing) pointer policy. Desktop and mobile implementations
 * differ in which nav modes start a touch long-press box.
 */
var AcExIdlePointerStrategy = /** @class */ (function () {
    function AcExIdlePointerStrategy() {
    }
    /** One-finger OrbitControls pan while waiting for a long-press box. */
    AcExIdlePointerStrategy.prototype.enablesIdleTouchPan = function (navMode) {
        return this.allowsTouchBox(navMode);
    };
    /**
     * Handles idle pointerdown. Drawing tools must be dispatched by the
     * caller first.
     *
     * @returns True when the event was consumed by idle nav / selection.
     */
    AcExIdlePointerStrategy.prototype.onPointerDown = function (event, host) {
        if (event.pointerType === 'mouse' && host.shouldIgnoreCompatMouse()) {
            return true;
        }
        var navMode = host.navMode();
        if (event.pointerType === 'touch' && navMode) {
            var kind = this.touchBoxKind(navMode);
            if (kind) {
                host.startTouchBox(kind, event);
                return true;
            }
        }
        if (event.pointerType !== 'touch' && navMode === 'select') {
            host.startMouseBox(event);
            return true;
        }
        if (host.handleNavPointerDown(event))
            return true;
        return host.applyClickSelect(event);
    };
    /**
     * Completes a mouse/pen box. Touch boxes end through the touch session.
     *
     * @returns True when a mouse/pen box consumed the event.
     */
    AcExIdlePointerStrategy.prototype.onPointerUp = function (event, host) {
        if (event.pointerType === 'touch')
            return false;
        return host.finishMouseBox(event, true);
    };
    /**
     * Aborts a mouse/pen box.
     *
     * @returns True when a mouse/pen box consumed the event.
     */
    AcExIdlePointerStrategy.prototype.onPointerCancel = function (event, host) {
        if (event.pointerType === 'touch')
            return false;
        return host.finishMouseBox(event, false);
    };
    AcExIdlePointerStrategy.prototype.touchBoxKind = function (navMode) {
        if (!this.allowsTouchBox(navMode))
            return null;
        return navMode === 'zoom-window' ? 'zoom-window' : 'select';
    };
    return AcExIdlePointerStrategy;
}());
export { AcExIdlePointerStrategy };
/** Select / zoom-window long-press only; pan is left to OrbitControls. */
var AcExDesktopIdlePointerStrategy = /** @class */ (function (_super) {
    __extends(AcExDesktopIdlePointerStrategy, _super);
    function AcExDesktopIdlePointerStrategy() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    AcExDesktopIdlePointerStrategy.prototype.allowsTouchBox = function (navMode) {
        return navMode === 'select' || navMode === 'zoom-window';
    };
    return AcExDesktopIdlePointerStrategy;
}(AcExIdlePointerStrategy));
export { AcExDesktopIdlePointerStrategy };
/**
 * Unified idle touch: drag pans, long-press boxes, including while the
 * hidden-toolbar default mode is pan.
 */
var AcExMobileIdlePointerStrategy = /** @class */ (function (_super) {
    __extends(AcExMobileIdlePointerStrategy, _super);
    function AcExMobileIdlePointerStrategy() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    AcExMobileIdlePointerStrategy.prototype.allowsTouchBox = function (navMode) {
        return (navMode === 'select' || navMode === 'zoom-window' || navMode === 'pan');
    };
    return AcExMobileIdlePointerStrategy;
}(AcExIdlePointerStrategy));
export { AcExMobileIdlePointerStrategy };
var desktopStrategy = new AcExDesktopIdlePointerStrategy();
var mobileStrategy = new AcExMobileIdlePointerStrategy();
/**
 * Idle pointer strategy for the current HTML layout.
 *
 * Re-evaluated each call so rotating a tablet or resizing the window
 * switches rules without rebinding listeners.
 */
export function acexIdlePointerStrategy() {
    return acexHtmlIsMobileNavUi() ? mobileStrategy : desktopStrategy;
}
//# sourceMappingURL=AcExIdlePointerStrategy.js.map