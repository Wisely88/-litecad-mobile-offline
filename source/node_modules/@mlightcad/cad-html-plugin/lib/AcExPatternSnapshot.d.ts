import * as THREE from 'three';
import type { AcExGradientFill, AcExHatchPattern, AcExLineBatch, AcExLinePattern, AcExMeshBatch } from './AcExSnapshotTypes';
/** Shared camera zoom uniform updated by the offline HTML viewer. */
export declare const AcExCameraZoomUniform: {
    value: number;
};
/**
 * Reads a serialized linetype from a live export-time shader material.
 */
export declare function extractLinePattern(material: THREE.Material): AcExLinePattern | undefined;
/**
 * Reads a serialized hatch pattern from a live export-time shader material.
 */
export declare function extractHatchPattern(material: THREE.Material): AcExHatchPattern | undefined;
/**
 * Reads a serialized gradient fill from a live export-time shader material.
 */
export declare function extractGradientFill(material: THREE.Material): AcExGradientFill | undefined;
/**
 * Computes cumulative `lineDistance` values for line-segment geometry.
 */
export declare function computeLineDistancesForSegments(positions: Float32Array): Float32Array;
/**
 * Copies the `lineDistance` attribute honoring the geometry draw range.
 */
export declare function exportLineDistanceSlice(geometry: THREE.BufferGeometry): Float32Array | undefined;
/**
 * Copies a per-vertex buffer attribute honoring the geometry draw range.
 */
export declare function exportVertexAttributeSlice(geometry: THREE.BufferGeometry, attributeName: string): Float32Array | undefined;
/**
 * Creates a viewer material for one exported line batch.
 */
export declare function createViewerLineMaterial(batch: AcExLineBatch, resolution?: THREE.Vector2): THREE.Material;
/**
 * Creates a viewer material for one exported point batch.
 */
export declare function createViewerPointsMaterial(batch: AcExMeshBatch): THREE.PointsMaterial;
/**
 * Re-aligns hatch pattern coordinates after hatch meshes were world-baked
 * during batch-group cloning (pattern fills stay on the unbatched draw path).
 */
export declare function transformHatchPatternToWorldSpace(pattern: AcExHatchPattern, matrixElements: number[]): AcExHatchPattern;
/**
 * Moves a world-space hatch pattern into the same local frame as rebased mesh
 * vertices (`batch.offset`), then period-wraps each line base so the offline
 * hatch shader keeps float32 precision at large survey coordinates.
 *
 * {@link AcExHatchPatternLine.offset} is already in the hatch-shader uniform
 * frame (rotated by `-line.angle` when the live material was built, then
 * optionally transformed by {@link transformHatchPatternToWorldSpace}). Do
 * **not** rotate it again before {@link wrapPatternBaseToLocalFrame} — that
 * helper expects the pre-rotated offset, matching
 * `AcTrFillMaterialManager.createHatchShaderMaterial`.
 */
export declare function rebaseHatchPatternToLocalOffset(pattern: AcExHatchPattern, offset: [number, number, number]): AcExHatchPattern;
/** Optional hooks when building viewer materials from exported mesh batches. */
export interface AcExViewerMeshMaterialOptions {
    /**
     * Called after an async IMAGE/OLE texture becomes ready. Offline viewers should
     * pass their `render()` so the mesh is not stuck at opacity 0 until the next
     * pan/zoom.
     */
    onTextureLoad?: () => void;
}
/**
 * Creates a viewer material for one exported mesh batch.
 */
export declare function createViewerMeshMaterial(batch: AcExMeshBatch, options?: AcExViewerMeshMaterialOptions): THREE.Material;
//# sourceMappingURL=AcExPatternSnapshot.d.ts.map