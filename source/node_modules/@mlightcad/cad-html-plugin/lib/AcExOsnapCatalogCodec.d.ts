import type { AcExOsnapCatalog, AcExOsnapPrimitive } from './AcExOsnapPrimitiveTypes';
/** Magic for ACEO osnap catalog payloads (`ACEO` little-endian). */
export declare const ACEO_OSNAP_MAGIC = 1329939265;
/**
 * Current ACEO payload schema version.
 *
 * Kind `7` (`path`) is included in version 1; the format has not had a formal
 * release, so adding a kind does not require a version bump.
 */
export declare const ACEO_OSNAP_VERSION: 1;
/** Hard cap on primitives inside one ACEO chunk. */
export declare const ACEX_MAX_OSNAP_PRIMITIVES_PER_CHUNK = 500000;
/** Hard cap on layer dictionary entries inside one ACEO chunk. */
export declare const ACEX_MAX_OSNAP_LAYERS_PER_CHUNK = 100000;
/**
 * Rough uncompressed ACEO size estimate for one primitive (used when splitting
 * catalogs into downloadable chunks). Prefer slightly overestimating.
 */
export declare function estimateOsnapPrimitiveBytes(primitive: AcExOsnapPrimitive): number;
/**
 * Splits OSNAP primitives into slices that stay under `maxChunkBytes` (estimate).
 */
export declare function splitOsnapPrimitives(primitives: AcExOsnapPrimitive[], maxChunkBytes: number): AcExOsnapPrimitive[][];
/**
 * Encodes an analytic OSNAP catalog to uncompressed ACEO bytes.
 * Coordinates stay float64; layer names are dictionary-compressed.
 */
export declare function encodeOsnapCatalogBinary(catalog: AcExOsnapCatalog): Uint8Array;
/**
 * Decodes an uncompressed ACEO osnap catalog.
 */
export declare function decodeOsnapCatalogBinary(bytes: Uint8Array): AcExOsnapCatalog;
/** Encodes and gzip-compresses an OSNAP catalog (`.osnap.gz` payload). */
export declare function encodeOsnapCatalogGzip(catalog: AcExOsnapCatalog): {
    uncompressed: Uint8Array;
    compressed: Uint8Array;
};
/** Gunzips and decodes a `.osnap.gz` catalog payload. */
export declare function decodeOsnapCatalogGzip(compressed: Uint8Array): AcExOsnapCatalog;
//# sourceMappingURL=AcExOsnapCatalogCodec.d.ts.map