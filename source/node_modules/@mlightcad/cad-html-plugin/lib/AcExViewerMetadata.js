import { AcCmColor, AcCmColorMethod, AcDbSystemVariables, AcDbSysVarManager } from '@mlightcad/data-model';
var DEFAULT_GRIP = {
    size: 8,
    colorCss: '#0080ff',
    hotColorCss: '#ff0000'
};
function aciIndexToCss(index) {
    var _a;
    var color = new AcCmColor(AcCmColorMethod.ByACI, index);
    return (_a = color.cssColor) !== null && _a !== void 0 ? _a : "rgb(".concat(color.red, ", ").concat(color.green, ", ").concat(color.blue, ")");
}
/**
 * Reads grip appearance from the drawing database for HTML export.
 * Falls back to AutoCAD-like defaults when sysvars are unavailable.
 */
function readExportGripAppearance(database) {
    try {
        var manager = AcDbSysVarManager.instance();
        var size = manager.getVar(AcDbSystemVariables.GRIPSIZE, database);
        var gripColor = manager.getVar(AcDbSystemVariables.GRIPCOLOR, database);
        var gripHot = manager.getVar(AcDbSystemVariables.GRIPHOT, database);
        if (!(size > 0) || !Number.isFinite(size))
            return DEFAULT_GRIP;
        return {
            size: size,
            colorCss: aciIndexToCss(gripColor),
            hotColorCss: aciIndexToCss(gripHot)
        };
    }
    catch (_a) {
        return DEFAULT_GRIP;
    }
}
/**
 * Extracts viewer metadata from an open drawing database (units, extents).
 * Does not serialize entities or DXF/DWG content.
 *
 * Object-snap (OSNAP) curve/point definitions are **not** part of this metadata
 * object. They are stored per layout in {@link AcExLayoutSnapshot.osnap}
 * (curves/points only; straight lines come from `lineBatches` at runtime).
 *
 * @param database - Open `AcDbDatabase` to read sysvars and extents from.
 * @param options - Optional title override and background color (default `0x000000`).
 * @returns Metadata object suitable for {@link AcExSnapshot.meta}.
 */
export function buildViewerMetadata(database, options) {
    var _a;
    var extmin = database.extmin;
    var extmax = database.extmax;
    var grip = readExportGripAppearance(database);
    return {
        title: options === null || options === void 0 ? void 0 : options.title,
        extents: {
            minX: extmin.x,
            minY: extmin.y,
            maxX: extmax.x,
            maxY: extmax.y
        },
        units: {
            insunits: database.insunits,
            lunits: database.lunits,
            luprec: database.luprec,
            aunits: database.aunits,
            auprec: database.auprec,
            measurement: database.measurement,
            ltscale: database.ltscale,
            angbase: database.angbase,
            angdir: database.angdir
        },
        grip: grip,
        background: (_a = options === null || options === void 0 ? void 0 : options.background) !== null && _a !== void 0 ? _a : 0x000000
    };
}
//# sourceMappingURL=AcExViewerMetadata.js.map