var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { strFromU8, strToU8 } from 'fflate';
/**
 * Hard cap for length-prefixed strings inside ACEX binary payloads.
 * Align with the decompressed ACEX payload ceiling (`512 MiB`). Layout OSNAP
 * catalogs in monolithic ACEX are stored as ACEO bytes (not JSON); legacy
 * JSON osnap blobs may still appear when reading older exports.
 */
export var ACEX_MAX_BINARY_STRING_BYTES = 512 * 1024 * 1024;
/**
 * Hard cap for a single length-prefixed typed-array payload (`Float32Array` /
 * `Uint32Array`). Large site plans can put tens of millions of vertices in one
 * line/mesh batch; keep this aligned with the ACEX decompressed payload ceiling
 * (`512 MiB`).
 */
export var ACEX_MAX_BINARY_ARRAY_BYTES = 512 * 1024 * 1024;
/**
 * Little-endian binary writer used by ACEX snapshot and ACEC chunk codecs.
 * Geometry buffers are length-prefixed and 4-byte aligned.
 */
var AcExBinaryWriter = /** @class */ (function () {
    function AcExBinaryWriter() {
        this.chunks = [];
        this.length = 0;
    }
    AcExBinaryWriter.prototype.writeU8 = function (value) {
        var chunk = new Uint8Array(1);
        chunk[0] = value & 0xff;
        this.chunks.push(chunk);
        this.length += 1;
    };
    AcExBinaryWriter.prototype.writeU32 = function (value) {
        var chunk = new Uint8Array(4);
        new DataView(chunk.buffer).setUint32(0, value >>> 0, true);
        this.chunks.push(chunk);
        this.length += 4;
    };
    AcExBinaryWriter.prototype.writeI32 = function (value) {
        var chunk = new Uint8Array(4);
        new DataView(chunk.buffer).setInt32(0, value | 0, true);
        this.chunks.push(chunk);
        this.length += 4;
    };
    AcExBinaryWriter.prototype.writeF32 = function (value) {
        var chunk = new Uint8Array(4);
        new DataView(chunk.buffer).setFloat32(0, value, true);
        this.chunks.push(chunk);
        this.length += 4;
    };
    AcExBinaryWriter.prototype.writeF64 = function (value) {
        var chunk = new Uint8Array(8);
        new DataView(chunk.buffer).setFloat64(0, value, true);
        this.chunks.push(chunk);
        this.length += 8;
    };
    AcExBinaryWriter.prototype.writeBytes = function (bytes) {
        this.chunks.push(bytes);
        this.length += bytes.length;
    };
    AcExBinaryWriter.prototype.writeString = function (value) {
        var bytes = strToU8(value);
        this.writeU32(bytes.length);
        this.writeBytes(bytes);
    };
    AcExBinaryWriter.prototype.writeJson = function (value) {
        this.writeString(JSON.stringify(value));
    };
    AcExBinaryWriter.prototype.writeFloat32Array = function (array) {
        this.alignTo(4);
        var bytes = new Uint8Array(array.buffer, array.byteOffset, array.byteLength);
        this.writeU32(bytes.length);
        this.writeBytes(bytes);
    };
    AcExBinaryWriter.prototype.writeUint32Array = function (array) {
        this.alignTo(4);
        var bytes = new Uint8Array(array.buffer, array.byteOffset, array.byteLength);
        this.writeU32(bytes.length);
        this.writeBytes(bytes);
    };
    AcExBinaryWriter.prototype.alignTo = function (alignment) {
        var remainder = this.length % alignment;
        if (remainder === 0) {
            return;
        }
        var pad = alignment - remainder;
        for (var i = 0; i < pad; i++) {
            this.writeU8(0);
        }
    };
    AcExBinaryWriter.prototype.toUint8Array = function () {
        var e_1, _a;
        var result = new Uint8Array(this.length);
        var offset = 0;
        try {
            for (var _b = __values(this.chunks), _c = _b.next(); !_c.done; _c = _b.next()) {
                var chunk = _c.value;
                result.set(chunk, offset);
                offset += chunk.length;
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_1) throw e_1.error; }
        }
        return result;
    };
    return AcExBinaryWriter;
}());
export { AcExBinaryWriter };
/**
 * Little-endian binary reader paired with {@link AcExBinaryWriter}.
 */
var AcExBinaryReader = /** @class */ (function () {
    function AcExBinaryReader(bytes) {
        this.bytes = bytes;
        this.offset = 0;
    }
    AcExBinaryReader.prototype.ensureAvailable = function (byteCount) {
        if (byteCount < 0 || !Number.isFinite(byteCount)) {
            throw new Error('Invalid binary length');
        }
        if (this.offset + byteCount > this.bytes.length) {
            throw new Error('Unexpected end of binary buffer');
        }
    };
    AcExBinaryReader.prototype.readU8 = function () {
        this.ensureAvailable(1);
        return this.bytes[this.offset++];
    };
    AcExBinaryReader.prototype.readU32 = function () {
        this.ensureAvailable(4);
        var view = new DataView(this.bytes.buffer, this.bytes.byteOffset + this.offset, 4);
        var value = view.getUint32(0, true);
        this.offset += 4;
        return value;
    };
    AcExBinaryReader.prototype.readI32 = function () {
        this.ensureAvailable(4);
        var view = new DataView(this.bytes.buffer, this.bytes.byteOffset + this.offset, 4);
        var value = view.getInt32(0, true);
        this.offset += 4;
        return value;
    };
    AcExBinaryReader.prototype.readF32 = function () {
        this.ensureAvailable(4);
        var view = new DataView(this.bytes.buffer, this.bytes.byteOffset + this.offset, 4);
        var value = view.getFloat32(0, true);
        this.offset += 4;
        return value;
    };
    AcExBinaryReader.prototype.readF64 = function () {
        this.ensureAvailable(8);
        var view = new DataView(this.bytes.buffer, this.bytes.byteOffset + this.offset, 8);
        var value = view.getFloat64(0, true);
        this.offset += 8;
        return value;
    };
    AcExBinaryReader.prototype.readBytes = function (length) {
        this.ensureAvailable(length);
        var slice = this.bytes.subarray(this.offset, this.offset + length);
        this.offset += length;
        return slice;
    };
    AcExBinaryReader.prototype.readString = function () {
        var length = this.readU32();
        if (length === 0) {
            return '';
        }
        if (length > ACEX_MAX_BINARY_STRING_BYTES) {
            throw new Error('Binary string exceeds size limit');
        }
        return strFromU8(this.readBytes(length));
    };
    AcExBinaryReader.prototype.readJson = function () {
        var text = this.readString();
        if (text.length === 0) {
            throw new Error('Expected JSON payload');
        }
        return JSON.parse(text);
    };
    AcExBinaryReader.prototype.alignTo = function (alignment) {
        var remainder = this.offset % alignment;
        if (remainder === 0) {
            return;
        }
        var pad = alignment - remainder;
        this.ensureAvailable(pad);
        this.offset += pad;
    };
    AcExBinaryReader.prototype.readFloat32Array = function () {
        this.alignTo(4);
        var byteLength = this.readU32();
        if (byteLength === 0) {
            return new Float32Array(0);
        }
        if (byteLength % 4 !== 0) {
            throw new Error('Invalid float32 buffer length');
        }
        if (byteLength > ACEX_MAX_BINARY_ARRAY_BYTES) {
            throw new Error('Float32 buffer exceeds size limit');
        }
        var bytes = this.readBytes(byteLength);
        if (bytes.byteOffset % 4 === 0) {
            return new Float32Array(bytes.buffer, bytes.byteOffset, byteLength / 4);
        }
        var copy = new ArrayBuffer(byteLength);
        new Uint8Array(copy).set(bytes);
        return new Float32Array(copy);
    };
    AcExBinaryReader.prototype.readUint32Array = function () {
        this.alignTo(4);
        var byteLength = this.readU32();
        if (byteLength === 0) {
            return new Uint32Array(0);
        }
        if (byteLength % 4 !== 0) {
            throw new Error('Invalid uint32 buffer length');
        }
        if (byteLength > ACEX_MAX_BINARY_ARRAY_BYTES) {
            throw new Error('Uint32 buffer exceeds size limit');
        }
        var bytes = this.readBytes(byteLength);
        if (bytes.byteOffset % 4 === 0) {
            return new Uint32Array(bytes.buffer, bytes.byteOffset, byteLength / 4);
        }
        var copy = new ArrayBuffer(byteLength);
        new Uint8Array(copy).set(bytes);
        return new Uint32Array(copy);
    };
    return AcExBinaryReader;
}());
export { AcExBinaryReader };
//# sourceMappingURL=AcExBinaryIO.js.map