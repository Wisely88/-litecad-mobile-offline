var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { ACEX_DOCS_PATH_MAGNIFIER, acexDocsUrl } from './AcExDocsUrl';
import { acexHtmlIsPhoneLayout } from './AcExHtmlDrawerSheet';
import { ML_UI_MOBILE_MAX_WIDTH } from './AcExHtmlShell';
import { AcUiMobileSessionPanel } from './AcExHtmlSimpleViewerUi';
/**
 * Bottom session panel for the offline HTML viewer.
 *
 * Thin adapter around shared {@link AcUiMobileSessionPanel}.
 */
var AcExCommandSessionPanel = /** @class */ (function () {
    /**
     * @param host - Element that receives the overlay (typically `#mlcad-canvas-host`
     *   or `#mlcad-root`).
     * @param i18n - Offline HTML i18n.
     */
    function AcExCommandSessionPanel(host, i18n) {
        var _this = this;
        this.handlers = null;
        this.lastState = null;
        this.accessory = null;
        this.layoutMql = null;
        this.onLayoutChange = function () {
            _this.reapplyLastState();
        };
        this.i18n = i18n;
        this.rootEl = host;
        this.syncCollapsedHeightVar();
        this.panel = new AcUiMobileSessionPanel({
            host: host,
            activeClass: 'ml-mobile-cmd-active',
            isPhoneLayout: acexHtmlIsPhoneLayout,
            helpDocsUrl: function () {
                return acexDocsUrl(ACEX_DOCS_PATH_MAGNIFIER, _this.i18n.locale);
            },
            helpHost: function () { var _a; return (_a = document.getElementById('mlcad-root')) !== null && _a !== void 0 ? _a : document.body; },
            labels: function () { return sessionLabels(_this.i18n); }
        });
        if (typeof window !== 'undefined' && window.matchMedia) {
            this.layoutMql = window.matchMedia("(max-width: ".concat(ML_UI_MOBILE_MAX_WIDTH, "px)"));
            this.layoutMql.addEventListener('change', this.onLayoutChange);
        }
    }
    Object.defineProperty(AcExCommandSessionPanel.prototype, "panelElement", {
        /** Underlying shared panel root (for drawer inset measurement). */
        get: function () {
            return this.rootEl.querySelector('.ml-mobile-cmd-panel');
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcExCommandSessionPanel.prototype, "isOpen", {
        /** Whether the session panel is currently shown. */
        get: function () {
            return this.panel.isOpen;
        },
        enumerable: false,
        configurable: true
    });
    /** Wires confirm / cancel / chip clicks. */
    AcExCommandSessionPanel.prototype.setHandlers = function (handlers) {
        this.handlers = handlers;
    };
    /** Applies session UI or hides the panel when `state` is null. */
    AcExCommandSessionPanel.prototype.setState = function (state) {
        var _a;
        this.lastState = state;
        var active = state != null;
        (_a = document
            .getElementById('mlcad-root')) === null || _a === void 0 ? void 0 : _a.classList.toggle('mlcad-session-active', active);
        var statusEl = document.getElementById('mlcad-status-bar');
        if (statusEl) {
            // Session prompt lives in the panel; avoid a duplicate top banner.
            if (active) {
                statusEl.hidden = true;
                statusEl.textContent = '';
            }
        }
        if (!state) {
            this.setAccessory(null);
            this.panel.hide();
            return;
        }
        this.applyState(state);
    };
    /**
     * Mounts widgets at the top of the session panel (left of the help icon).
     * Pass `null` to clear custom content; the help icon remains while the
     * panel is open. Same `id` is a no-op so live metric updates do not remount.
     */
    AcExCommandSessionPanel.prototype.setAccessory = function (next) {
        var _a, _b, _c, _d;
        if (((_b = (_a = this.accessory) === null || _a === void 0 ? void 0 : _a.id) !== null && _b !== void 0 ? _b : null) === ((_c = next === null || next === void 0 ? void 0 : next.id) !== null && _c !== void 0 ? _c : null))
            return;
        (_d = this.accessory) === null || _d === void 0 ? void 0 : _d.unmount();
        this.panel.clearAccessory();
        this.accessory = next;
        if (next) {
            this.panel.prepareAccessory();
            next.mount(this.panel.accessoryHost);
        }
    };
    /** Re-applies metric labels after a locale change. */
    AcExCommandSessionPanel.prototype.refreshLabels = function () {
        this.panel.refreshLabels();
        this.reapplyLastState();
    };
    /** Removes listeners and DOM. */
    AcExCommandSessionPanel.prototype.dispose = function () {
        var _a;
        (_a = this.layoutMql) === null || _a === void 0 ? void 0 : _a.removeEventListener('change', this.onLayoutChange);
        this.setAccessory(null);
        this.panel.dispose();
    };
    /** Current session string field value. */
    AcExCommandSessionPanel.prototype.getStringValue = function () {
        return this.panel.getStringValue();
    };
    /** Focuses the session string field when string input mode is active. */
    AcExCommandSessionPanel.prototype.focusStringInput = function () {
        this.panel.focusStringInput();
    };
    /**
     * Re-shows {@link lastState}, copying the live string field into state first
     * so layout / locale remounts do not wipe typed text.
     */
    AcExCommandSessionPanel.prototype.reapplyLastState = function () {
        if (!this.lastState)
            return;
        if (this.lastState.stringInput != null) {
            this.lastState = __assign(__assign({}, this.lastState), { stringInput: __assign(__assign({}, this.lastState.stringInput), { value: this.panel.getStringValue() }) });
        }
        this.applyState(this.lastState);
    };
    AcExCommandSessionPanel.prototype.applyState = function (state) {
        var _a, _b;
        this.syncCollapsedHeightVar();
        var handlers = this.handlers;
        var stringMode = state.stringInput != null;
        this.panel.show({
            prompt: state.prompt,
            allowNone: stringMode ? true : state.confirmEnabled,
            showMetrics: !stringMode,
            showStringInput: stringMode,
            stringValue: (_a = state.stringInput) === null || _a === void 0 ? void 0 : _a.value,
            stringPlaceholder: (_b = state.stringInput) === null || _b === void 0 ? void 0 : _b.placeholder,
            keywords: state.chips.map(function (chip) { return ({
                displayName: chip.label,
                id: chip.id,
                enabled: true
            }); })
        }, {
            onConfirm: function () { return handlers === null || handlers === void 0 ? void 0 : handlers.onConfirm(); },
            onCancel: function () { return handlers === null || handlers === void 0 ? void 0 : handlers.onCancel(); },
            onKeyword: function (id) { return handlers === null || handlers === void 0 ? void 0 : handlers.onChip(id); }
        });
        this.panel.prepareAccessory();
        if (stringMode)
            return;
        if (state.metrics) {
            this.panel.setMetrics(state.metrics.hasBasePoint, {
                length: state.metrics.lengthText,
                angle: state.metrics.angleText,
                dx: state.metrics.dxText,
                dy: state.metrics.dyText,
                x: state.metrics.xText,
                y: state.metrics.yText
            });
        }
        else {
            // No rubber-band yet: absolute X/Y labels (matches prior HTML behavior).
            this.panel.setMetrics(false, {
                length: '0',
                angle: '0',
                dx: '0',
                dy: '0',
                x: '0',
                y: '0'
            });
        }
    };
    AcExCommandSessionPanel.prototype.syncCollapsedHeightVar = function () {
        var fromToolbar = getComputedStyle(document.documentElement)
            .getPropertyValue('--mlcad-toolbar-phone-height')
            .trim() || '56px';
        this.rootEl.style.setProperty('--ml-mobile-cmd-collapsed-height', fromToolbar);
    };
    return AcExCommandSessionPanel;
}());
export { AcExCommandSessionPanel };
function sessionLabels(i18n) {
    return {
        length: i18n.t('session.length'),
        angle: i18n.t('session.angle'),
        dx: i18n.t('session.dx'),
        dy: i18n.t('session.dy'),
        x: i18n.t('session.x'),
        y: i18n.t('session.y'),
        confirm: i18n.t('session.confirm'),
        cancel: i18n.t('session.cancel'),
        help: i18n.t('session.help'),
        back: i18n.t('session.back'),
        collapse: i18n.t('session.collapse'),
        expand: i18n.t('session.expand')
    };
}
//# sourceMappingURL=AcExCommandSessionPanel.js.map