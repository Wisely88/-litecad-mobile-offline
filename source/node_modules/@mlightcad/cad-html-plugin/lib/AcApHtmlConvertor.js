var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
import { AcApDocManager, AcTrView2d, getDrawingExportBaseName, resolveExportDownloadName } from '@mlightcad/cad-simple-viewer';
import { accmYieldForPaint } from '@mlightcad/data-model';
import { captureAcApHtmlViewState, resolveAcApHtmlExportOptions } from './AcApHtmlExportOptions';
import { resolveViewerRuntimeUrl } from './AcApHtmlPluginOptions';
import { AcApHtmlSnapshotBuilder } from './AcApHtmlSnapshotBuilder';
import { protectAcExHtmlEncodedSnapshot, resolveAcApHtmlExpiresAt } from './AcExHtmlAccess';
import { packHtmlEmbeddedPackage, shouldEmbedAcExChunks } from './AcExHtmlEmbeddedPackage';
import { packHtml } from './AcExHtmlPackager';
import { buildAcExPackage } from './AcExPackageBuilder';
import { zipAcExPackageFiles } from './AcExPackageZip';
import { encodeSnapshot } from './AcExSnapshotCodec';
/**
 * Orchestrates export of the active drawing to a downloadable HTML file
 * or multi-file ACEX package zip.
 *
 * Workflow:
 * 1. Build a display-only {@link AcExSnapshot} from the current scene and database.
 * 2. Fetch the IIFE viewer runtime (inlined into the HTML).
 * 3. Package as self-contained HTML (`single`) or zip of package files (`multi`).
 *    For `single`, large drawings automatically embed progressive ACEC chunks;
 *    small drawings keep a monolithic ACEX payload.
 *
 * A busy indicator is shown for the duration of the operation. The UI thread
 * is yielded between heavy steps so the browser can repaint.
 *
 * The runtime URL is configured on this plugin (`viewerRuntimeUrl`), not on
 * `AcApDocManager` — see {@link AcApHtmlPluginOptions}.
 */
var AcApHtmlConvertor = /** @class */ (function () {
    /**
     * @param options - Plugin options; `viewerRuntimeUrl` overrides module defaults
     */
    function AcApHtmlConvertor(options) {
        if (options === void 0) { options = {}; }
        this.options = options;
        /** Collects geometry and metadata from the live Three.js scene. */
        this._snapshotBuilder = new AcApHtmlSnapshotBuilder();
    }
    /**
     * Prepares the active 2D view for HTML snapshot export.
     *
     * Ensures drawable entities skipped during interactive viewing (for example on
     * off layers) are converted into the scene. Converted geometry remains in the
     * live scene after export completes.
     */
    AcApHtmlConvertor.prototype.prepareAcTrView2dForHtmlExport = function (view_1) {
        return __awaiter(this, arguments, void 0, function (view, options) {
            var resolved, conversionOptions;
            if (options === void 0) { options = {}; }
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        if (!view || !('cadScene' in view) || !view.cadScene) {
                            throw new Error('CAD scene is not available. Open a drawing before exporting to HTML.');
                        }
                        if (!(view instanceof AcTrView2d)) {
                            throw new Error('HTML export requires a 2D CAD view. Open a drawing before exporting.');
                        }
                        resolved = resolveAcApHtmlExportOptions(options);
                        conversionOptions = {
                            includeInvisibleLayers: resolved.exportInvisibleLayers,
                            includeLayouts: resolved.exportLayouts
                        };
                        return [4 /*yield*/, view.ensureEntitiesConvertedForExport(conversionOptions)];
                    case 1:
                        _a.sent();
                        return [4 /*yield*/, accmYieldForPaint()];
                    case 2:
                        _a.sent();
                        return [2 /*return*/, view];
                }
            });
        });
    };
    /**
     * Exports the document currently open in {@link AcApDocManager}.
     *
     * @param fileName - Optional base name for the download (without extension).
     *   When omitted, the active document's `fileName` is used.
     * @param options - Export options including {@link AcApHtmlExportOptions.exportFormat}.
     * @param view - Optional view to export from. Defaults to the active view.
     * @returns Resolves when packaging and download complete.
     */
    AcApHtmlConvertor.prototype.convert = function (fileName_1) {
        return __awaiter(this, arguments, void 0, function (fileName, options, view) {
            var docManager, resolved;
            var _this = this;
            if (options === void 0) { options = {}; }
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        docManager = AcApDocManager.instance;
                        resolved = resolveAcApHtmlExportOptions(options);
                        return [4 /*yield*/, docManager.withBusyIndicator(function () { return __awaiter(_this, void 0, void 0, function () {
                                var document, exportView, sourceName, baseName, snapshot, viewerRuntime, pkg, zipBytes, expiresAt, html;
                                return __generator(this, function (_a) {
                                    switch (_a.label) {
                                        case 0: return [4 /*yield*/, accmYieldForPaint()];
                                        case 1:
                                            _a.sent();
                                            document = docManager.curDocument;
                                            return [4 /*yield*/, this.prepareAcTrView2dForHtmlExport(view !== null && view !== void 0 ? view : docManager.curView, resolved)];
                                        case 2:
                                            exportView = _a.sent();
                                            sourceName = fileName || document.fileName || document.docTitle;
                                            baseName = getDrawingExportBaseName(sourceName);
                                            return [4 /*yield*/, this._snapshotBuilder.buildAsync(exportView.cadScene, document.database, {
                                                    title: baseName,
                                                    background: exportView.backgroundColor,
                                                    exportInvisibleLayers: resolved.exportInvisibleLayers,
                                                    exportLayouts: resolved.exportLayouts,
                                                    initialView: resolved.initialView,
                                                    viewerMode: resolved.viewerMode,
                                                    viewState: resolved.initialView === 'current' &&
                                                        (resolved.exportLayouts ||
                                                            exportView.activeLayoutBtrId === exportView.modelSpaceBtrId)
                                                        ? captureAcApHtmlViewState(exportView)
                                                        : undefined
                                                })];
                                        case 3:
                                            snapshot = _a.sent();
                                            return [4 /*yield*/, accmYieldForPaint()];
                                        case 4:
                                            _a.sent();
                                            return [4 /*yield*/, this.loadViewerRuntime()];
                                        case 5:
                                            viewerRuntime = _a.sent();
                                            return [4 /*yield*/, accmYieldForPaint()];
                                        case 6:
                                            _a.sent();
                                            if (!(resolved.exportFormat === 'multi')) return [3 /*break*/, 8];
                                            pkg = buildAcExPackage(snapshot, {
                                                viewerRuntime: viewerRuntime,
                                                baseName: baseName
                                            });
                                            zipBytes = zipAcExPackageFiles(pkg);
                                            return [4 /*yield*/, accmYieldForPaint()];
                                        case 7:
                                            _a.sent();
                                            this.downloadBytes(zipBytes, resolveExportDownloadName(sourceName, 'zip'), 'application/zip');
                                            return [2 /*return*/];
                                        case 8:
                                            expiresAt = resolveAcApHtmlExpiresAt(resolved.expiryDays, Date.now(), resolved.expiresAt);
                                            return [4 /*yield*/, this.packSelfContainedHtml(snapshot, viewerRuntime, {
                                                    expiresAt: expiresAt,
                                                    password: resolved.password || undefined
                                                })];
                                        case 9:
                                            html = _a.sent();
                                            return [4 /*yield*/, accmYieldForPaint()];
                                        case 10:
                                            _a.sent();
                                            this.downloadHtml(html, resolveExportDownloadName(sourceName, 'html'));
                                            return [2 /*return*/];
                                    }
                                });
                            }); })];
                    case 1:
                        _a.sent();
                        return [2 /*return*/];
                }
            });
        });
    };
    /**
     * Packages a pre-built snapshot into HTML and downloads it.
     *
     * Skips scene collection; useful for tests, CLI tooling, or re-exporting a
     * snapshot produced elsewhere.
     *
     * @param snapshot - Complete snapshot to embed in the HTML.
     * @param downloadName - File name passed to the browser download API (should
     *   include the `.html` extension).
     * @returns Resolves when packaging and download complete.
     */
    AcApHtmlConvertor.prototype.packSnapshot = function (snapshot, downloadName) {
        return __awaiter(this, void 0, void 0, function () {
            var docManager;
            var _this = this;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        docManager = AcApDocManager.instance;
                        return [4 /*yield*/, docManager.withBusyIndicator(function () { return __awaiter(_this, void 0, void 0, function () {
                                var viewerRuntime, html;
                                return __generator(this, function (_a) {
                                    switch (_a.label) {
                                        case 0: return [4 /*yield*/, accmYieldForPaint()];
                                        case 1:
                                            _a.sent();
                                            return [4 /*yield*/, this.loadViewerRuntime()];
                                        case 2:
                                            viewerRuntime = _a.sent();
                                            return [4 /*yield*/, accmYieldForPaint()];
                                        case 3:
                                            _a.sent();
                                            return [4 /*yield*/, this.packSelfContainedHtml(snapshot, viewerRuntime, {
                                                    expiresAt: null
                                                })];
                                        case 4:
                                            html = _a.sent();
                                            return [4 /*yield*/, accmYieldForPaint()];
                                        case 5:
                                            _a.sent();
                                            this.downloadHtml(html, downloadName);
                                            return [2 /*return*/];
                                    }
                                });
                            }); })];
                    case 1:
                        _a.sent();
                        return [2 /*return*/];
                }
            });
        });
    };
    /**
     * Packages a snapshot as self-contained HTML.
     * Large drawings embed progressive ACEC chunks; small ones keep a monolithic ACEX.
     */
    AcApHtmlConvertor.prototype.packSelfContainedHtml = function (snapshot, viewerRuntime, options) {
        return __awaiter(this, void 0, void 0, function () {
            var protectedSnapshot;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        if (shouldEmbedAcExChunks(snapshot)) {
                            return [2 /*return*/, packHtmlEmbeddedPackage(snapshot, {
                                    title: snapshot.meta.title,
                                    viewerRuntime: viewerRuntime,
                                    expiresAt: options.expiresAt,
                                    password: options.password
                                })];
                        }
                        return [4 /*yield*/, protectAcExHtmlEncodedSnapshot(encodeSnapshot(snapshot), {
                                expiresAt: options.expiresAt,
                                password: options.password
                            })];
                    case 1:
                        protectedSnapshot = _a.sent();
                        return [2 /*return*/, packHtml(snapshot, {
                                title: snapshot.meta.title,
                                viewerRuntime: viewerRuntime,
                                encoded: protectedSnapshot.encoded,
                                accessManifest: protectedSnapshot.manifest
                            })];
                }
            });
        });
    };
    /**
     * Fetches the offline viewer runtime as source text for inlining.
     *
     * @returns The runtime script body as a string.
     * @throws If the HTTP response is not OK (missing build artifact, CORS, etc.).
     */
    AcApHtmlConvertor.prototype.loadViewerRuntime = function () {
        return __awaiter(this, void 0, void 0, function () {
            var runtimeUrl, response;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        runtimeUrl = resolveViewerRuntimeUrl(this.options.viewerRuntimeUrl);
                        return [4 /*yield*/, fetch(runtimeUrl)];
                    case 1:
                        response = _a.sent();
                        if (!response.ok) {
                            throw new Error("Failed to load HTML viewer runtime from \"".concat(runtimeUrl, "\" (").concat(response.status, "). ") +
                                'Install @mlightcad/cad-html-plugin, copy viewer-runtime.iife.js to your app assets, ' +
                                'and set viewerRuntimeUrl on registerLazyHtmlPlugin / createHtmlPlugin / AcApHtmlConvertor.');
                        }
                        return [2 /*return*/, response.text()];
                }
            });
        });
    };
    /**
     * Triggers a client-side download of the generated HTML string.
     *
     * @param content - Full HTML document text.
     * @param downloadName - Value for the anchor `download` attribute.
     */
    AcApHtmlConvertor.prototype.downloadHtml = function (content, downloadName) {
        this.downloadBytes(new TextEncoder().encode(content), downloadName, 'text/html;charset=utf-8');
    };
    /**
     * Triggers a client-side download of raw bytes.
     */
    AcApHtmlConvertor.prototype.downloadBytes = function (bytes, downloadName, mimeType) {
        var copy = new Uint8Array(bytes.byteLength);
        copy.set(bytes);
        var blob = new Blob([copy], { type: mimeType });
        var url = URL.createObjectURL(blob);
        var link = document.createElement('a');
        link.href = url;
        link.download = downloadName;
        document.body.appendChild(link);
        link.click();
        document.body.removeChild(link);
        window.setTimeout(function () { return URL.revokeObjectURL(url); }, 60000);
    };
    return AcApHtmlConvertor;
}());
export { AcApHtmlConvertor };
//# sourceMappingURL=AcApHtmlConvertor.js.map