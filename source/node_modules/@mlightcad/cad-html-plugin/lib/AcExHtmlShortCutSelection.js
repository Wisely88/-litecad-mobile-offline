/**
 * Selection color / text-height buttons for the HTML shortcut toolbar.
 *
 * @module AcExHtmlShortCutSelection
 * @packageDocumentation
 */
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
import { AcCmColor } from '@mlightcad/data-model';
import { acexScreenPxToWcs } from './AcExHtmlOverlayDom';
import { AcUiAciColorDialog, ICON_TEXT_HEIGHT } from './AcExHtmlSimpleViewerUi';
import { AcUiTextHeightDialog } from './AcExHtmlSimpleViewerUi';
function colorFromAci(index) {
    var color = new AcCmColor();
    color.colorIndex = index;
    return color;
}
function createColorIcon(css) {
    var wrap = document.createElement('span');
    wrap.className = 'ml-ui-simple-toolbar__icon';
    wrap.style.cssText =
        'display:inline-flex;width:18px;height:18px;align-items:center;justify-content:center';
    var fill = document.createElement('span');
    fill.style.cssText = "display:block;width:12px;height:12px;border-radius:50%;border:1px solid #666;background:".concat(css);
    wrap.appendChild(fill);
    return wrap;
}
/**
 * Rebuilds shortcut extension items from the current markup/measure selection.
 */
export function acexSyncHtmlShortCutSelection(ctx) {
    var kind = ctx.getKind();
    if (kind == null || !ctx.hasSelection(kind)) {
        ctx.setExtensionItems([]);
        return;
    }
    var style = ctx.getStyle(kind);
    ctx.setExtensionItems([
        {
            id: 'selection-color',
            icon: function () { return createColorIcon(style.color); },
            label: ctx.i18n.t('drawStyle.color'),
            onClick: function () {
                void openColor(ctx, kind, style.color);
            }
        },
        {
            id: 'selection-text-height',
            icon: ICON_TEXT_HEIGHT,
            label: ctx.i18n.t('drawStyle.fontSize'),
            onClick: function () {
                void openTextHeight(ctx, kind, style);
            }
        }
    ]);
}
function openColor(ctx, kind, currentCss) {
    return __awaiter(this, void 0, void 0, function () {
        var theme, initial, color, index, next, css;
        var _a, _b;
        return __generator(this, function (_c) {
            switch (_c.label) {
                case 0:
                    theme = document.documentElement.getAttribute('data-mlcad-theme') === 'light'
                        ? 'light'
                        : 'dark';
                    initial = null;
                    try {
                        color = new AcCmColor().setRGBFromCss(currentCss);
                        if (color.isByACI && color.colorIndex != null)
                            initial = color.colorIndex;
                    }
                    catch (_d) {
                        /* ignore */
                    }
                    return [4 /*yield*/, AcUiAciColorDialog.open({
                            host: (_a = document.getElementById('mlcad-canvas-host')) !== null && _a !== void 0 ? _a : document.body,
                            theme: theme,
                            initialIndex: initial,
                            labels: {
                                title: ctx.i18n.t('drawStyle.pickerTitle'),
                                close: ctx.i18n.t('drawStyle.close'),
                                ok: ctx.i18n.t('drawStyle.ok'),
                                cancel: ctx.i18n.t('drawStyle.cancel'),
                                index: ctx.i18n.t('drawStyle.index'),
                                rgb: ctx.i18n.t('drawStyle.rgb'),
                                input: ctx.i18n.t('drawStyle.input'),
                                inputPlaceholder: ctx.i18n.t('drawStyle.inputPlaceholder')
                            }
                        })];
                case 1:
                    index = _c.sent();
                    if (index == null)
                        return [2 /*return*/];
                    next = colorFromAci(index);
                    css = (_b = next.cssColor) !== null && _b !== void 0 ? _b : "rgb(".concat(next.red, ", ").concat(next.green, ", ").concat(next.blue, ")");
                    ctx.applyStyle(kind, { color: css });
                    acexSyncHtmlShortCutSelection(ctx);
                    return [2 /*return*/];
            }
        });
    });
}
function openTextHeight(ctx, kind, style) {
    return __awaiter(this, void 0, void 0, function () {
        var theme, result, fontSize, perPx;
        var _a, _b;
        return __generator(this, function (_c) {
            switch (_c.label) {
                case 0:
                    theme = document.documentElement.getAttribute('data-mlcad-theme') === 'light'
                        ? 'light'
                        : 'dark';
                    return [4 /*yield*/, AcUiTextHeightDialog.open({
                            host: (_a = document.getElementById('mlcad-canvas-host')) !== null && _a !== void 0 ? _a : document.body,
                            theme: theme,
                            initialMode: (_b = style.textHeightMode) !== null && _b !== void 0 ? _b : 'adaptive',
                            initialFontSizePx: style.fontSize,
                            initialTextHeightWcs: style.textHeightWcs,
                            screenPxToWcs: function (px) {
                                return ctx.wcsToScreen ? acexScreenPxToWcs(px, ctx.wcsToScreen) : px;
                            },
                            labels: {
                                title: ctx.i18n.t('textHeight.title'),
                                close: ctx.i18n.t('textHeight.close'),
                                ok: ctx.i18n.t('textHeight.ok'),
                                cancel: ctx.i18n.t('textHeight.cancel'),
                                adaptive: ctx.i18n.t('textHeight.adaptive'),
                                custom: ctx.i18n.t('textHeight.custom'),
                                customPlaceholder: ctx.i18n.t('textHeight.customPlaceholder'),
                                fromScreen: ctx.i18n.t('textHeight.fromScreen'),
                                fromScreenHint: ctx.i18n.t('textHeight.fromScreenHint'),
                                screenPxPlaceholder: ctx.i18n.t('textHeight.screenPxPlaceholder'),
                                screenUnit: ctx.i18n.t('textHeight.screenUnit'),
                                convert: ctx.i18n.t('textHeight.convert')
                            }
                        })];
                case 1:
                    result = _c.sent();
                    if (!result)
                        return [2 /*return*/];
                    if (result.mode === 'custom' && result.textHeightWcs != null) {
                        fontSize = style.fontSize;
                        if (ctx.wcsToScreen) {
                            perPx = acexScreenPxToWcs(1, ctx.wcsToScreen);
                            if (perPx > 0) {
                                fontSize = Math.max(1, Math.round(result.textHeightWcs / perPx));
                            }
                        }
                        ctx.applyStyle(kind, {
                            textHeightMode: 'custom',
                            textHeightWcs: result.textHeightWcs,
                            fontSize: fontSize
                        });
                    }
                    else {
                        ctx.applyStyle(kind, {
                            textHeightMode: 'adaptive',
                            fontSize: style.fontSize
                        });
                    }
                    acexSyncHtmlShortCutSelection(ctx);
                    return [2 /*return*/];
            }
        });
    });
}
//# sourceMappingURL=AcExHtmlShortCutSelection.js.map