/**
 * Measurement list drawer for the offline HTML viewer.
 *
 * Lists committed measurements, zooms to a row on click, and supports
 * per-item delete plus clear-all.
 *
 * @module AcExHtmlMeasurePanel
 * @packageDocumentation
 */
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var TYPE_I18N = {
    distance: 'toolbar.measureDistance',
    angle: 'toolbar.measureAngle',
    area: 'toolbar.measureArea',
    arc: 'toolbar.measureArc',
    point: 'toolbar.measureCoordinate'
};
var FILTER_TYPES = ['distance', 'arc', 'angle', 'area'];
var FILTER_I18N = {
    distance: 'measurePanel.filterDistance',
    arc: 'measurePanel.filterArc',
    angle: 'measurePanel.filterAngle',
    area: 'measurePanel.filterArea'
};
/**
 * Wires the measurement drawer to the measure controller.
 *
 * @returns Controller, or `null` when the shell markup is missing.
 */
export function setupAcExHtmlMeasurePanel(options) {
    var i18n = options.i18n, getMeasure = options.getMeasure, closeOtherDrawers = options.closeOtherDrawers, onPhoneOpen = options.onPhoneOpen;
    var drawer = document.getElementById('mlcad-measure-drawer');
    var closeBtn = document.getElementById('mlcad-measure-close');
    var sheetCloseBtn = drawer === null || drawer === void 0 ? void 0 : drawer.querySelector('.mlcad-drawer-sheet-close');
    if (!drawer)
        return null;
    var filterGroup = drawer.querySelector('.mlcad-measure-filter');
    var filterButtons = Array.from(drawer.querySelectorAll('[data-measure-filter]'));
    var clearBtn = drawer.querySelector('.mlcad-measure-clear');
    var tbody = drawer.querySelector('tbody');
    if (!filterGroup || filterButtons.length === 0 || !clearBtn || !tbody) {
        return null;
    }
    var unsubscribe;
    var tableContentKey = '';
    var activeFilter;
    var typeLabel = function (type) {
        return i18n.t(TYPE_I18N[type]);
    };
    var isFilterType = function (value) {
        return FILTER_TYPES.includes(value);
    };
    var syncFilterButtons = function () {
        var e_1, _a;
        try {
            for (var filterButtons_1 = __values(filterButtons), filterButtons_1_1 = filterButtons_1.next(); !filterButtons_1_1.done; filterButtons_1_1 = filterButtons_1.next()) {
                var button = filterButtons_1_1.value;
                var type = button.dataset.measureFilter;
                var active = isFilterType(type) && activeFilter === type;
                button.classList.toggle('is-active', active);
                button.setAttribute('aria-pressed', String(active));
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (filterButtons_1_1 && !filterButtons_1_1.done && (_a = filterButtons_1.return)) _a.call(filterButtons_1);
            }
            finally { if (e_1) throw e_1.error; }
        }
    };
    var renderTable = function () {
        var e_2, _a;
        var _b;
        var measure = getMeasure();
        var records = (_b = measure === null || measure === void 0 ? void 0 : measure.list()) !== null && _b !== void 0 ? _b : [];
        var selectedId = measure === null || measure === void 0 ? void 0 : measure.selectedId;
        var rows = activeFilter
            ? records.filter(function (record) { return record.type === activeFilter; })
            : records;
        tbody.replaceChildren();
        clearBtn.disabled = records.length === 0;
        if (rows.length === 0) {
            var emptyRow = document.createElement('tr');
            emptyRow.className = 'mlcad-measure-empty';
            var cell = document.createElement('td');
            cell.colSpan = 3;
            cell.textContent = i18n.t('measurePanel.empty');
            emptyRow.appendChild(cell);
            tbody.appendChild(emptyRow);
            return;
        }
        var deleteLabel = i18n.t('measurePanel.delete');
        try {
            for (var rows_1 = __values(rows), rows_1_1 = rows_1.next(); !rows_1_1.done; rows_1_1 = rows_1.next()) {
                var record = rows_1_1.value;
                var tr = document.createElement('tr');
                tr.dataset.measureId = record.id;
                if (record.id === selectedId)
                    tr.classList.add('is-selected');
                var typeCell = document.createElement('td');
                typeCell.textContent = typeLabel(record.type);
                var valueCell = document.createElement('td');
                var value = record.valueText || '—';
                valueCell.textContent = value;
                valueCell.title = value;
                var actionsCell = document.createElement('td');
                var deleteBtn = document.createElement('button');
                deleteBtn.type = 'button';
                deleteBtn.className = 'mlcad-measure-row-delete';
                deleteBtn.dataset.measureDelete = record.id;
                deleteBtn.textContent = deleteLabel;
                deleteBtn.title = deleteLabel;
                deleteBtn.setAttribute('aria-label', deleteLabel);
                actionsCell.appendChild(deleteBtn);
                tr.append(typeCell, valueCell, actionsCell);
                tbody.appendChild(tr);
            }
        }
        catch (e_2_1) { e_2 = { error: e_2_1 }; }
        finally {
            try {
                if (rows_1_1 && !rows_1_1.done && (_a = rows_1.return)) _a.call(rows_1);
            }
            finally { if (e_2) throw e_2.error; }
        }
    };
    var recordsKey = function () {
        var _a, _b;
        return ((_b = (_a = getMeasure()) === null || _a === void 0 ? void 0 : _a.list()) !== null && _b !== void 0 ? _b : [])
            .map(function (record) { return "".concat(record.id, "\t").concat(record.type, "\t").concat(record.valueText); })
            .join('\n');
    };
    var syncRowSelection = function () {
        var _a;
        var selectedId = (_a = getMeasure()) === null || _a === void 0 ? void 0 : _a.selectedId;
        tbody.querySelectorAll('tr[data-measure-id]').forEach(function (row) {
            row.classList.toggle('is-selected', row.getAttribute('data-measure-id') === selectedId);
        });
    };
    var refresh = function () {
        var key = recordsKey();
        if (key === tableContentKey &&
            tbody.querySelector('tr[data-measure-id], .mlcad-measure-empty')) {
            syncRowSelection();
            return;
        }
        tableContentKey = key;
        renderTable();
    };
    var rowMeasureId = function (event) {
        var target = event.target;
        if (!(target instanceof Element))
            return undefined;
        var row = target.closest('tr[data-measure-id]');
        if (!row || !tbody.contains(row))
            return undefined;
        return row instanceof HTMLElement ? row.dataset.measureId : undefined;
    };
    tbody.addEventListener('click', function (event) {
        var _a, _b;
        var target = event.target;
        if (target instanceof Element) {
            var deleteBtn = target.closest('button[data-measure-delete]');
            if (deleteBtn instanceof HTMLElement && tbody.contains(deleteBtn)) {
                var id_1 = deleteBtn.dataset.measureDelete;
                if (id_1)
                    (_a = getMeasure()) === null || _a === void 0 ? void 0 : _a.removeMeasurement(id_1);
                return;
            }
        }
        var id = rowMeasureId(event);
        if (!id)
            return;
        (_b = getMeasure()) === null || _b === void 0 ? void 0 : _b.focus(id);
    });
    var setOpen = function (open) {
        if (open) {
            closeOtherDrawers();
            onPhoneOpen === null || onPhoneOpen === void 0 ? void 0 : onPhoneOpen(drawer);
        }
        drawer.hidden = !open;
        document.querySelectorAll('[data-action="measure-panel"]').forEach(function (btn) {
            btn.classList.toggle('active', open);
            btn.setAttribute('aria-pressed', String(open));
        });
        if (open) {
            var measure = getMeasure();
            unsubscribe === null || unsubscribe === void 0 ? void 0 : unsubscribe();
            unsubscribe = measure === null || measure === void 0 ? void 0 : measure.subscribe(refresh);
            refresh();
        }
        else {
            unsubscribe === null || unsubscribe === void 0 ? void 0 : unsubscribe();
            unsubscribe = undefined;
        }
    };
    filterGroup.addEventListener('click', function (event) {
        var target = event.target;
        if (!(target instanceof Element))
            return;
        var button = target.closest('[data-measure-filter]');
        if (!(button instanceof HTMLElement) || !filterGroup.contains(button)) {
            return;
        }
        var type = button.dataset.measureFilter;
        if (!isFilterType(type))
            return;
        activeFilter = activeFilter === type ? undefined : type;
        syncFilterButtons();
        renderTable();
    });
    clearBtn.addEventListener('click', function () { var _a; return (_a = getMeasure()) === null || _a === void 0 ? void 0 : _a.clearAll(); });
    closeBtn === null || closeBtn === void 0 ? void 0 : closeBtn.addEventListener('click', function () { return setOpen(false); });
    sheetCloseBtn === null || sheetCloseBtn === void 0 ? void 0 : sheetCloseBtn.addEventListener('click', function () { return setOpen(false); });
    var refreshLabels = function () {
        var e_3, _a;
        filterGroup.setAttribute('aria-label', i18n.t('measurePanel.filterGroup'));
        try {
            for (var filterButtons_2 = __values(filterButtons), filterButtons_2_1 = filterButtons_2.next(); !filterButtons_2_1.done; filterButtons_2_1 = filterButtons_2.next()) {
                var button = filterButtons_2_1.value;
                var type = button.dataset.measureFilter;
                if (!isFilterType(type))
                    continue;
                var label = i18n.t(FILTER_I18N[type]);
                button.textContent = label;
                button.title = label;
                button.setAttribute('aria-label', label);
            }
        }
        catch (e_3_1) { e_3 = { error: e_3_1 }; }
        finally {
            try {
                if (filterButtons_2_1 && !filterButtons_2_1.done && (_a = filterButtons_2.return)) _a.call(filterButtons_2);
            }
            finally { if (e_3) throw e_3.error; }
        }
        clearBtn.textContent = i18n.t('measurePanel.clear');
        var typeHeader = drawer.querySelector('[data-measure-col="type"]');
        var valueHeader = drawer.querySelector('[data-measure-col="value"]');
        if (typeHeader)
            typeHeader.textContent = i18n.t('measurePanel.type');
        if (valueHeader)
            valueHeader.textContent = i18n.t('measurePanel.value');
        var title = drawer.querySelector('[data-i18n-key="measurePanel.title"]');
        if (title)
            title.textContent = i18n.t('measurePanel.title');
        renderTable();
    };
    refreshLabels();
    return {
        refreshLabels: refreshLabels,
        close: function () { return setOpen(false); },
        setOpen: setOpen
    };
}
//# sourceMappingURL=AcExHtmlMeasurePanel.js.map