/**
 * Shared drawer-sheet chrome for HTML layer / review / measurement drawers:
 * full-width sheet above the toolbar on phone, grabber to resize height,
 * close arrow. Results drawers live inside tool-strip wraps; opening one
 * parks the drawer on the sidebar so dismissing the strip does not hide it.
 *
 * @module acexHtmlDrawerSheet
 * @packageDocumentation
 */
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { ML_UI_COMPACT_MAX_WIDTH, ML_UI_MOBILE_MAX_WIDTH } from './AcExHtmlShell';
var MIN_HEIGHT = 160;
var DEFAULT_HEIGHT_VH = 0.42;
/** Whether the offline HTML chrome is using the phone breakpoint. */
export function acexHtmlIsPhoneLayout() {
    var _a;
    return (typeof window !== 'undefined' &&
        ((_a = window.matchMedia) === null || _a === void 0 ? void 0 : _a.call(window, "(max-width: ".concat(ML_UI_MOBILE_MAX_WIDTH, "px)")).matches) ===
            true);
}
/** Whether the offline HTML chrome is using the phone or pad breakpoint. */
export function acexHtmlIsCompactLayout() {
    var _a;
    return (typeof window !== 'undefined' &&
        ((_a = window.matchMedia) === null || _a === void 0 ? void 0 : _a.call(window, "(max-width: ".concat(ML_UI_COMPACT_MAX_WIDTH, "px)")).matches) ===
            true);
}
/**
 * Whether idle touch should unify pan + long-press box select (and hide
 * Select / Pan), and whether confirmed-point plus marks should show.
 * Compact width covers phone and pad; coarse pointer covers wider tablets
 * such as iPad landscape.
 */
export function acexHtmlIsMobileNavUi() {
    var _a;
    return (acexHtmlIsCompactLayout() ||
        (typeof window !== 'undefined' &&
            ((_a = window.matchMedia) === null || _a === void 0 ? void 0 : _a.call(window, '(pointer: coarse)').matches) === true));
}
/**
 * Enables height-resize on drawer grabbers and pins sheets above phone chrome.
 */
export function setupAcExHtmlDrawerSheets(options) {
    var e_1, _a;
    var drawers = [
        document.getElementById('mlcad-layer-drawer'),
        document.getElementById('mlcad-review-drawer'),
        document.getElementById('mlcad-measure-drawer')
    ].filter(function (el) { return el != null; });
    var homeParent = new WeakMap();
    try {
        for (var drawers_1 = __values(drawers), drawers_1_1 = drawers_1.next(); !drawers_1_1.done; drawers_1_1 = drawers_1.next()) {
            var drawer = drawers_1_1.value;
            if (drawer.parentElement)
                homeParent.set(drawer, drawer.parentElement);
            bindDrawerResize(drawer);
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (drawers_1_1 && !drawers_1_1.done && (_a = drawers_1.return)) _a.call(drawers_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
    var restoreIfDesktop = function () {
        var e_2, _a;
        if (acexHtmlIsPhoneLayout())
            return;
        var sidebar = document.getElementById('mlcad-sidebar');
        try {
            for (var drawers_2 = __values(drawers), drawers_2_1 = drawers_2.next(); !drawers_2_1.done; drawers_2_1 = drawers_2.next()) {
                var drawer = drawers_2_1.value;
                drawer.style.height = '';
                drawer.style.maxHeight = '';
                var home = homeParent.get(drawer);
                if (!home)
                    continue;
                // Open parks the drawer on the sidebar and closes strip wraps.
                // Reparenting an open drawer into a hidden wrap would hide it while it
                // stays logically open — keep it on the sidebar until the wrap is shown
                // or the drawer is closed.
                var trappedInHiddenWrap = home.hidden && !drawer.hidden;
                if (trappedInHiddenWrap) {
                    if (sidebar && drawer.parentElement !== sidebar) {
                        sidebar.appendChild(drawer);
                    }
                    continue;
                }
                if (drawer.parentElement !== home) {
                    home.appendChild(drawer);
                }
            }
        }
        catch (e_2_1) { e_2 = { error: e_2_1 }; }
        finally {
            try {
                if (drawers_2_1 && !drawers_2_1.done && (_a = drawers_2.return)) _a.call(drawers_2);
            }
            finally { if (e_2) throw e_2.error; }
        }
    };
    var syncInset = function () {
        var _a, _b;
        var toolbar = document.getElementById('mlcad-toolbar');
        var inset = (_a = toolbar === null || toolbar === void 0 ? void 0 : toolbar.offsetHeight) !== null && _a !== void 0 ? _a : 56;
        var wraps = document.querySelectorAll('#mlcad-measure-strip-wrap, #mlcad-markup-strip-wrap, #mlcad-snap-strip-wrap, #mlcad-zoom-strip-wrap, #mlcad-settings-strip-wrap, #mlcad-locale-strip-wrap');
        wraps.forEach(function (wrap) {
            if (wrap.hidden)
                return;
            inset += wrap.offsetHeight;
        });
        var session = document.querySelector('.ml-mobile-cmd-panel');
        if (session &&
            !((_b = session.closest('.ml-mobile-cmd')) === null || _b === void 0 ? void 0 : _b.hasAttribute('hidden')) &&
            acexHtmlIsPhoneLayout()) {
            // Session replaces the toolbar (which stays in layout via visibility:
            // hidden). Pin the drawer to the session height only.
            inset = session.offsetHeight;
        }
        document.documentElement.style.setProperty('--mlcad-phone-drawer-bottom', "".concat(inset, "px"));
    };
    /**
     * Moves the drawer onto the sidebar before it is shown. Measure / review
     * drawers are born inside dismissible strip wraps; without reparenting,
     * closing the strip (pad/desktop child-click, or phone strip dismiss)
     * would hide an open results panel.
     */
    var preparePhoneOpen = function (drawer) {
        var _a;
        var sidebar = document.getElementById('mlcad-sidebar');
        if (sidebar && drawer.parentElement !== sidebar) {
            sidebar.appendChild(drawer);
        }
        if (acexHtmlIsPhoneLayout()) {
            (_a = options === null || options === void 0 ? void 0 : options.closeStrips) === null || _a === void 0 ? void 0 : _a.call(options);
            syncInset();
        }
    };
    syncInset();
    window.addEventListener('resize', function () {
        restoreIfDesktop();
        syncInset();
    });
    return { syncInset: syncInset, preparePhoneOpen: preparePhoneOpen };
}
function bindDrawerResize(drawer) {
    var grabber = drawer.querySelector('.mlcad-drawer-grabber');
    if (!(grabber instanceof HTMLElement))
        return;
    var pointerId;
    var startY = 0;
    var startHeight = 0;
    var onMove = function (event) {
        if (pointerId !== event.pointerId)
            return;
        event.preventDefault();
        var maxHeight = Math.max(MIN_HEIGHT, window.innerHeight * 0.75);
        var next = Math.max(MIN_HEIGHT, Math.min(maxHeight, startHeight + (startY - event.clientY)));
        drawer.style.height = "".concat(next, "px");
        drawer.style.maxHeight = 'none';
    };
    var onUp = function (event) {
        if (pointerId !== event.pointerId)
            return;
        pointerId = undefined;
        if (grabber.hasPointerCapture(event.pointerId)) {
            grabber.releasePointerCapture(event.pointerId);
        }
        document.removeEventListener('pointermove', onMove);
        document.removeEventListener('pointerup', onUp);
        document.removeEventListener('pointercancel', onUp);
    };
    grabber.addEventListener('pointerdown', function (event) {
        if (event.button !== 0)
            return;
        event.preventDefault();
        pointerId = event.pointerId;
        startY = event.clientY;
        startHeight =
            drawer.getBoundingClientRect().height ||
                window.innerHeight * DEFAULT_HEIGHT_VH;
        grabber.setPointerCapture(event.pointerId);
        document.addEventListener('pointermove', onMove);
        document.addEventListener('pointerup', onUp);
        document.addEventListener('pointercancel', onUp);
    });
}
//# sourceMappingURL=AcExHtmlDrawerSheet.js.map