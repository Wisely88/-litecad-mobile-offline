/**
 * Color / text-height controls for the offline HTML session accessory slot.
 *
 * @module AcExSessionDrawStyle
 * @packageDocumentation
 */
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
import { AcCmColor, AcCmColorUtil } from '@mlightcad/data-model';
import { acexScreenPxToWcs } from './AcExHtmlOverlayDom';
import { AcUiAciColorDialog, AcUiTextHeightDialog, createIconElement, ICON_TEXT_HEIGHT } from './AcExHtmlSimpleViewerUi';
var STYLE_ID = 'mlcad-session-style-styles';
var SESSION_STYLE_CSS = "\n.mlcad-session-style {\n  display: flex;\n  align-items: center;\n  gap: 8px;\n}\n.mlcad-session-style__swatch {\n  position: relative;\n  width: 28px;\n  height: 28px;\n  padding: 0;\n  border: 1px solid rgba(255, 255, 255, 0.22);\n  border-radius: 4px;\n  background: rgba(255, 255, 255, 0.06);\n  cursor: pointer;\n}\n.mlcad-session-style__swatch-fill {\n  display: block;\n  width: 14px;\n  height: 14px;\n  margin: 0 auto;\n  border-radius: 50%;\n  border: 1px solid #999;\n}\n.mlcad-session-style__text-height {\n  width: 28px;\n  height: 28px;\n  padding: 0;\n  display: inline-flex;\n  align-items: center;\n  justify-content: center;\n  border: 1px solid rgba(255, 255, 255, 0.22);\n  border-radius: 4px;\n  background: rgba(255, 255, 255, 0.06);\n  color: inherit;\n  cursor: pointer;\n  box-sizing: border-box;\n}\n.mlcad-session-style__text-height .ml-ex-ui-icon {\n  display: inline-flex;\n  width: 18px;\n  height: 18px;\n}\n.mlcad-session-style__text-height .ml-ex-ui-icon svg {\n  width: 18px;\n  height: 18px;\n}\n";
function colorFromAci(index) {
    var color = new AcCmColor();
    color.colorIndex = index;
    return color;
}
function cssColor(color) {
    var _a;
    return (_a = color.cssColor) !== null && _a !== void 0 ? _a : "rgb(".concat(color.red, ", ").concat(color.green, ", ").concat(color.blue, ")");
}
function preferExactAciColor(color) {
    if (!color.isByColor)
        return color;
    var rgb = color.RGB;
    if (rgb == null)
        return color;
    var index = AcCmColorUtil.getIndexByColor(rgb);
    if (index == null)
        return color;
    var aci = new AcCmColor();
    aci.colorIndex = index;
    return aci;
}
function cssToColor(css) {
    var trimmed = css.trim();
    var skipFromString = trimmed.startsWith('#') || /^(rgb|rgba|hsl|hsla)\(/i.test(trimmed);
    if (!skipFromString) {
        try {
            var fromString = AcCmColor.fromString(trimmed);
            if (fromString)
                return preferExactAciColor(fromString);
        }
        catch (_a) {
            // Fall through to setRGBFromCss.
        }
    }
    try {
        return preferExactAciColor(new AcCmColor().setRGBFromCss(trimmed));
    }
    catch (_b) {
        var fallback = new AcCmColor();
        fallback.setRGB(8, 232, 222);
        return fallback;
    }
}
function aciIndexOf(color) {
    if (color.isByACI && color.colorIndex != null && color.colorIndex >= 1) {
        return color.colorIndex;
    }
    return undefined;
}
function ensureStyles() {
    if (typeof document === 'undefined')
        return;
    var style = document.getElementById(STYLE_ID);
    if (!style) {
        style = document.createElement('style');
        style.id = STYLE_ID;
        document.head.appendChild(style);
    }
    style.textContent = SESSION_STYLE_CSS;
}
function resolvePatchFromDialog(result, fallbackFontSize, wcsToScreen) {
    if (result.mode === 'custom' && result.textHeightWcs != null) {
        var fontSize = fallbackFontSize;
        if (wcsToScreen) {
            var px = acexScreenPxToWcs(1, wcsToScreen);
            if (px > 0)
                fontSize = Math.max(1, Math.round(result.textHeightWcs / px));
        }
        return {
            textHeightMode: 'custom',
            textHeightWcs: result.textHeightWcs,
            fontSize: fontSize
        };
    }
    return {
        textHeightMode: 'adaptive',
        fontSize: fallbackFontSize
    };
}
/**
 * Builds color / text-height controls that mount into the session accessory.
 */
export function setupAcExSessionDrawStyle(ctx) {
    var _this = this;
    ensureStyles();
    var controlsRow = document.createElement('div');
    controlsRow.className = 'mlcad-session-style';
    controlsRow.setAttribute('role', 'toolbar');
    var swatch = document.createElement('button');
    swatch.type = 'button';
    swatch.className = 'mlcad-session-style__swatch';
    var swatchFill = document.createElement('span');
    swatchFill.className = 'mlcad-session-style__swatch-fill';
    swatch.appendChild(swatchFill);
    var textHeightButton = document.createElement('button');
    textHeightButton.type = 'button';
    textHeightButton.className = 'mlcad-session-style__text-height';
    textHeightButton.appendChild(createIconElement(ICON_TEXT_HEIGHT));
    var colorDialogOpen = false;
    var textHeightDialogOpen = false;
    var currentKind;
    var sessionMounted = false;
    var currentStyle = { color: '#08e8de', fontSize: 12 };
    controlsRow.append(swatch, textHeightButton);
    var paint = function (style) {
        currentStyle = style;
        var color = cssToColor(style.color);
        swatchFill.style.background = cssColor(color);
    };
    var relabel = function () {
        swatch.title = ctx.i18n.t('drawStyle.color');
        textHeightButton.title = ctx.i18n.t('drawStyle.fontSize');
        textHeightButton.setAttribute('aria-label', ctx.i18n.t('drawStyle.fontSize'));
    };
    var refresh = function () {
        currentKind = ctx.getKind();
        if (!currentKind)
            return;
        paint(ctx.getStyle(currentKind));
        relabel();
    };
    var mountSession = function (host) {
        sessionMounted = true;
        host.appendChild(controlsRow);
        refresh();
    };
    var unmountSession = function () {
        if (!sessionMounted)
            return;
        sessionMounted = false;
        controlsRow.remove();
    };
    var colorDialogLabels = function () { return ({
        title: ctx.i18n.t('drawStyle.pickerTitle'),
        close: ctx.i18n.t('drawStyle.close'),
        ok: ctx.i18n.t('drawStyle.ok'),
        cancel: ctx.i18n.t('drawStyle.cancel'),
        index: ctx.i18n.t('drawStyle.index'),
        rgb: ctx.i18n.t('drawStyle.rgb'),
        input: ctx.i18n.t('drawStyle.input'),
        inputPlaceholder: ctx.i18n.t('drawStyle.inputPlaceholder')
    }); };
    var htmlUiTheme = function () {
        return document.documentElement.getAttribute('data-mlcad-theme') === 'light'
            ? 'light'
            : 'dark';
    };
    var openSessionColorDialog = function () { return __awaiter(_this, void 0, void 0, function () {
        var initial, index, css;
        var _a;
        return __generator(this, function (_b) {
            switch (_b.label) {
                case 0:
                    if (colorDialogOpen || !currentKind)
                        return [2 /*return*/];
                    colorDialogOpen = true;
                    _b.label = 1;
                case 1:
                    _b.trys.push([1, , 3, 4]);
                    initial = aciIndexOf(cssToColor(currentStyle.color));
                    return [4 /*yield*/, AcUiAciColorDialog.open({
                            host: (_a = document.getElementById('mlcad-canvas-host')) !== null && _a !== void 0 ? _a : document.body,
                            theme: htmlUiTheme(),
                            initialIndex: initial !== null && initial !== void 0 ? initial : null,
                            labels: colorDialogLabels()
                        })];
                case 2:
                    index = _b.sent();
                    if (index == null || !currentKind)
                        return [2 /*return*/];
                    css = cssColor(colorFromAci(index));
                    swatchFill.style.background = css;
                    ctx.applyStyle(currentKind, { color: css });
                    return [3 /*break*/, 4];
                case 3:
                    colorDialogOpen = false;
                    return [7 /*endfinally*/];
                case 4: return [2 /*return*/];
            }
        });
    }); };
    var openTextHeightDialog = function () { return __awaiter(_this, void 0, void 0, function () {
        var result, patch;
        var _a, _b;
        return __generator(this, function (_c) {
            switch (_c.label) {
                case 0:
                    if (textHeightDialogOpen || !currentKind)
                        return [2 /*return*/];
                    textHeightDialogOpen = true;
                    _c.label = 1;
                case 1:
                    _c.trys.push([1, , 3, 4]);
                    return [4 /*yield*/, AcUiTextHeightDialog.open({
                            host: (_a = document.getElementById('mlcad-canvas-host')) !== null && _a !== void 0 ? _a : document.body,
                            theme: htmlUiTheme(),
                            initialMode: (_b = currentStyle.textHeightMode) !== null && _b !== void 0 ? _b : 'adaptive',
                            initialFontSizePx: currentStyle.fontSize,
                            initialTextHeightWcs: currentStyle.textHeightWcs,
                            screenPxToWcs: function (px) {
                                return ctx.wcsToScreen
                                    ? acexScreenPxToWcs(px, ctx.wcsToScreen)
                                    : px;
                            },
                            labels: {
                                title: ctx.i18n.t('textHeight.title'),
                                close: ctx.i18n.t('textHeight.close'),
                                ok: ctx.i18n.t('textHeight.ok'),
                                cancel: ctx.i18n.t('textHeight.cancel'),
                                adaptive: ctx.i18n.t('textHeight.adaptive'),
                                custom: ctx.i18n.t('textHeight.custom'),
                                customPlaceholder: ctx.i18n.t('textHeight.customPlaceholder'),
                                fromScreen: ctx.i18n.t('textHeight.fromScreen'),
                                fromScreenHint: ctx.i18n.t('textHeight.fromScreenHint'),
                                screenPxPlaceholder: ctx.i18n.t('textHeight.screenPxPlaceholder'),
                                screenUnit: ctx.i18n.t('textHeight.screenUnit'),
                                convert: ctx.i18n.t('textHeight.convert')
                            }
                        })];
                case 2:
                    result = _c.sent();
                    if (!result || !currentKind)
                        return [2 /*return*/];
                    patch = resolvePatchFromDialog(result, currentStyle.fontSize, ctx.wcsToScreen);
                    ctx.applyStyle(currentKind, patch);
                    paint(__assign(__assign({}, currentStyle), patch));
                    return [3 /*break*/, 4];
                case 3:
                    textHeightDialogOpen = false;
                    return [7 /*endfinally*/];
                case 4: return [2 /*return*/];
            }
        });
    }); };
    swatch.addEventListener('click', function (event) {
        event.preventDefault();
        event.stopPropagation();
        void openSessionColorDialog();
    });
    textHeightButton.addEventListener('click', function (event) {
        event.preventDefault();
        event.stopPropagation();
        void openTextHeightDialog();
    });
    controlsRow.addEventListener('pointerdown', function (event) { return event.stopPropagation(); });
    controlsRow.addEventListener('mousedown', function (event) { return event.stopPropagation(); });
    relabel();
    return {
        refresh: refresh,
        refreshLabels: function () {
            relabel();
        },
        createSessionAccessory: function () { return ({
            id: 'draw-style',
            mount: mountSession,
            unmount: unmountSession
        }); },
        dispose: function () {
            unmountSession();
        }
    };
}
//# sourceMappingURL=AcExSessionDrawStyle.js.map