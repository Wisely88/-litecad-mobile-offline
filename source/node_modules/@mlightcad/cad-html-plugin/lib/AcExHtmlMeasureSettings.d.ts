import { type AcExHtmlI18n } from './AcExHtmlI18n';
import type { AcExMeasureController } from './AcExMeasurement';
import type { AcExTrackingOptions } from './AcExMeasureTracking';
/** Default measurement accent color (`--mlcad-accent`). */
export declare const ACEX_DEFAULT_MEASURE_COLOR = 583902;
/** `localStorage` key for persisted measure settings. */
export declare const ACEX_MEASURE_SETTINGS_STORAGE_KEY = "mlcad-html-measure-settings";
/** Common polar angle increments for the offline HTML viewer. */
export declare const ACEX_POLAR_ANGLE_INCREMENTS: readonly [90, 45, 30, 23, 18, 10, 5];
/** Polar increment equivalent to orthogonal mode. */
export declare const ACEX_ORTHO_POLAR_ANGLE = 90;
/** Default polar increment when polar tracking is enabled. */
export declare const ACEX_DEFAULT_POLAR_ANGLE = 90;
/** Persisted measure settings for the offline HTML viewer. */
export interface AcExMeasureSettingsState {
    measureColor: number;
    ortho: boolean;
    polar: boolean;
    polarAng: number;
}
/** Dependencies for {@link setupAcExHtmlMeasureSettings}. */
export interface AcExHtmlMeasureSettingsContext {
    i18n: AcExHtmlI18n;
    measure: AcExMeasureController;
    angbase: number;
    angdir: number;
}
/**
 * Dismissible language strip shown beside the language parent button.
 */
export declare function buildAcExHtmlLocaleStrip(): string;
/**
 * Builds the polar-angle panel used by measure settings (opened from the snap
 * sticky sub-toolbar).
 */
export declare function buildAcExHtmlPolarAnglesPanel(): string;
/**
 * @deprecated Snap tools now live under AcUiToolbar settings. Prefer
 * {@link buildAcExHtmlPolarAnglesPanel}.
 */
export declare function buildAcExHtmlSnapStrip(): string;
/** Live measure settings exposed to the measurement controller. */
export interface AcExHtmlMeasureSettingsController {
    /** Current settings snapshot. */
    readonly state: AcExMeasureSettingsState;
    /** Tracking options derived from state and snapshot angle metadata. */
    getTrackingOptions(): AcExTrackingOptions;
    /** Reapplies i18n labels after locale change. */
    refreshLabels: () => void;
    /** Closes the polar-angle panel. */
    close: () => void;
    /** Toggles orthogonal mode (closes the polar panel). */
    toggleOrtho: () => void;
    /** Whether orthogonal mode is enabled. */
    isOrtho: () => boolean;
    /**
     * Toggles the polar-angle panel. Returns whether the panel is open afterwards.
     */
    togglePolarPanel: () => boolean;
    /** Whether the polar-angle panel is currently open. */
    isPolarPanelOpen: () => boolean;
}
/**
 * Wires ortho / polar tracking state and the polar-angle panel.
 *
 * Ortho / polar toggles are driven by {@link AcUiToolbar} (or legacy strip
 * buttons when present). Drawing color / font size live on the session panel.
 */
export declare function setupAcExHtmlMeasureSettings(ctx: AcExHtmlMeasureSettingsContext): AcExHtmlMeasureSettingsController;
//# sourceMappingURL=AcExHtmlMeasureSettings.d.ts.map