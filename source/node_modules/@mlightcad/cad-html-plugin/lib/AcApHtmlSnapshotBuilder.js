var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
import { acapGetDocsBaseUrl, AcApI18n } from '@mlightcad/cad-simple-viewer';
import { accmYieldForPaint } from '@mlightcad/data-model';
import { acexGetDocsBaseUrl, acexSetDocsBaseUrl } from './AcExDocsUrl';
import { computeLayoutViewExtents } from './AcExLayerExtents';
import { buildOsnapCatalog } from './AcExOsnapPrimitiveBuilder';
import { collectLayoutViewports } from './AcExPaperViewportCollector';
import { collectBatchesFromObject3D } from './AcExSceneBatchCollector';
import { ACEX_SNAPSHOT_VERSION } from './AcExSnapshotTypes';
import { buildViewerMetadata } from './AcExViewerMetadata';
/**
 * Serializes the live Three.js scene into a versioned {@link AcExSnapshot}.
 *
 * The snapshot contains layer visibility, per-layout line/mesh batches, and
 * viewer metadata (extents, units, background). It is display-only: no DXF/DWG
 * entities or edit capability are preserved.
 */
var AcApHtmlSnapshotBuilder = /** @class */ (function () {
    function AcApHtmlSnapshotBuilder() {
    }
    /**
     * Builds a snapshot synchronously in one pass.
     *
     * Prefer {@link buildAsync} for interactive export so the UI can stay responsive.
     *
     * @param scene - Current renderer scene (layouts, layers, active layout).
     * @param database - Open drawing database used for layout names and metadata.
     * @param options - Optional title, background, and locale overrides.
     * @returns A complete v1 snapshot ready for `packHtml`.
     */
    AcApHtmlSnapshotBuilder.prototype.build = function (scene, database, options) {
        if (options === void 0) { options = {}; }
        return this.buildSync(scene, database, options);
    };
    /**
     * Builds a snapshot incrementally, yielding to the main thread between layouts.
     *
     * Geometry is collected per layout layer so a busy indicator can repaint
     * during large drawings.
     *
     * @param scene - Current renderer scene (layouts, layers, active layout).
     * @param database - Open drawing database used for layout names and metadata.
     * @param options - Optional title, background, and locale overrides.
     * @returns A complete v1 snapshot ready for `packHtml`.
     */
    AcApHtmlSnapshotBuilder.prototype.buildAsync = function (scene_1, database_1) {
        return __awaiter(this, arguments, void 0, function (scene, database, options) {
            var exportInvisibleLayers, exportLayouts, includeLayer, meta, layers, tableLayouts, layoutNames, activeLayoutBtrId, layouts, _a, _b, btrId, e_1_1;
            var e_1, _c;
            if (options === void 0) { options = {}; }
            return __generator(this, function (_d) {
                switch (_d.label) {
                    case 0: return [4 /*yield*/, accmYieldForPaint()];
                    case 1:
                        _d.sent();
                        exportInvisibleLayers = options.exportInvisibleLayers !== false;
                        exportLayouts = options.exportLayouts !== false;
                        includeLayer = exportInvisibleLayers
                            ? undefined
                            : function (layerName) {
                                return shouldExportLayer(scene, layerName, exportInvisibleLayers);
                            };
                        meta = buildViewerMetadata(database, {
                            title: options.title,
                            background: options.background
                        });
                        layers = [];
                        scene.layers.forEach(function (layer) {
                            var _a;
                            if (!shouldExportLayer(scene, layer.name, exportInvisibleLayers)) {
                                return;
                            }
                            layers.push({
                                name: layer.name,
                                color: (_a = layer.color.RGB) !== null && _a !== void 0 ? _a : 0xffffff,
                                visible: !layer.isOff && !layer.isFrozen
                            });
                        });
                        tableLayouts = listDatabaseLayouts(database);
                        layoutNames = new Map(tableLayouts.map(function (layout) { return [layout.blockTableRecordId, layout.name]; }));
                        activeLayoutBtrId = resolveExportActiveLayoutBtrId(scene, exportLayouts);
                        layouts = [];
                        _d.label = 2;
                    case 2:
                        _d.trys.push([2, 7, 8, 9]);
                        _a = __values(listExportLayoutBtrIds(scene, tableLayouts, exportLayouts)), _b = _a.next();
                        _d.label = 3;
                    case 3:
                        if (!!_b.done) return [3 /*break*/, 6];
                        btrId = _b.value;
                        layouts.push(collectLayoutSnapshot(scene, database, btrId, layoutNames, options, includeLayer));
                        return [4 /*yield*/, accmYieldForPaint()];
                    case 4:
                        _d.sent();
                        _d.label = 5;
                    case 5:
                        _b = _a.next();
                        return [3 /*break*/, 3];
                    case 6: return [3 /*break*/, 9];
                    case 7:
                        e_1_1 = _d.sent();
                        e_1 = { error: e_1_1 };
                        return [3 /*break*/, 9];
                    case 8:
                        try {
                            if (_b && !_b.done && (_c = _a.return)) _c.call(_a);
                        }
                        finally { if (e_1) throw e_1.error; }
                        return [7 /*endfinally*/];
                    case 9: return [2 /*return*/, {
                            version: ACEX_SNAPSHOT_VERSION,
                            meta: buildSnapshotMeta(meta, options, layouts, activeLayoutBtrId),
                            layers: layers,
                            layouts: layouts,
                            activeLayoutBtrId: activeLayoutBtrId
                        }];
                }
            });
        });
    };
    /**
     * Synchronous implementation shared by {@link build} and {@link buildAsync}.
     *
     * @param scene - Current renderer scene.
     * @param database - Open drawing database.
     * @param options - Snapshot overrides.
     * @returns A complete v1 snapshot.
     */
    AcApHtmlSnapshotBuilder.prototype.buildSync = function (scene, database, options) {
        var e_2, _a;
        var exportInvisibleLayers = options.exportInvisibleLayers !== false;
        var exportLayouts = options.exportLayouts !== false;
        var includeLayer = exportInvisibleLayers
            ? undefined
            : function (layerName) {
                return shouldExportLayer(scene, layerName, exportInvisibleLayers);
            };
        var meta = buildViewerMetadata(database, {
            title: options.title,
            background: options.background
        });
        var layers = [];
        scene.layers.forEach(function (layer) {
            var _a;
            if (!shouldExportLayer(scene, layer.name, exportInvisibleLayers)) {
                return;
            }
            layers.push({
                name: layer.name,
                color: (_a = layer.color.RGB) !== null && _a !== void 0 ? _a : 0xffffff,
                visible: !layer.isOff && !layer.isFrozen
            });
        });
        var tableLayouts = listDatabaseLayouts(database);
        var layoutNames = new Map(tableLayouts.map(function (layout) { return [layout.blockTableRecordId, layout.name]; }));
        var activeLayoutBtrId = resolveExportActiveLayoutBtrId(scene, exportLayouts);
        var layouts = [];
        try {
            for (var _b = __values(listExportLayoutBtrIds(scene, tableLayouts, exportLayouts)), _c = _b.next(); !_c.done; _c = _b.next()) {
                var btrId = _c.value;
                layouts.push(collectLayoutSnapshot(scene, database, btrId, layoutNames, options, includeLayer));
            }
        }
        catch (e_2_1) { e_2 = { error: e_2_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_2) throw e_2.error; }
        }
        return {
            version: ACEX_SNAPSHOT_VERSION,
            meta: buildSnapshotMeta(meta, options, layouts, activeLayoutBtrId),
            layers: layers,
            layouts: layouts,
            activeLayoutBtrId: activeLayoutBtrId
        };
    };
    return AcApHtmlSnapshotBuilder;
}());
export { AcApHtmlSnapshotBuilder };
/**
 * Merges database-derived viewer metadata with export-time options.
 *
 * @param meta - Extents, units, and background from {@link buildViewerMetadata}.
 * @param options - User overrides (title is taken from `meta`; locale from options).
 * @param layouts - Exported layouts used to derive {@link AcExSnapshot.meta.viewExtents}.
 * @param activeLayoutBtrId - Layout shown when the HTML file is first opened.
 * @returns The `meta` block stored on {@link AcExSnapshot}.
 */
function buildSnapshotMeta(meta, options, layouts, activeLayoutBtrId) {
    var _a, _b, _c, _d;
    var activeLayout = (_a = layouts.find(function (layout) { return layout.btrId === activeLayoutBtrId; })) !== null && _a !== void 0 ? _a : layouts[0];
    var viewExtents = activeLayout
        ? computeLayoutViewExtents(activeLayout)
        : null;
    var initialView = (_b = options.initialView) !== null && _b !== void 0 ? _b : 'fit';
    return {
        title: meta.title,
        createdAt: new Date().toISOString(),
        extents: meta.extents,
        viewExtents: viewExtents !== null && viewExtents !== void 0 ? viewExtents : undefined,
        units: meta.units,
        grip: meta.grip,
        background: meta.background,
        locale: (_c = options.locale) !== null && _c !== void 0 ? _c : AcApI18n.currentLocale,
        initialView: initialView,
        viewState: initialView === 'current' ? options.viewState : undefined,
        viewerMode: (_d = options.viewerMode) !== null && _d !== void 0 ? _d : 'measure',
        exportLayouts: options.exportLayouts !== false,
        docsBaseUrl: resolveDocsBaseUrlForExport()
    };
}
/**
 * Prefer the live viewer's configured docs root when exporting HTML.
 * Falls back to the offline-viewer default when unavailable.
 */
function resolveDocsBaseUrlForExport() {
    try {
        acexSetDocsBaseUrl(acapGetDocsBaseUrl());
    }
    catch (_a) {
        // Live viewer docs helper unavailable — keep Acex default.
    }
    return acexGetDocsBaseUrl();
}
function shouldExportOsnap(options) {
    var _a;
    return ((_a = options.viewerMode) !== null && _a !== void 0 ? _a : 'measure') === 'measure';
}
function shouldExportLayer(scene, layerName, exportInvisibleLayers) {
    if (exportInvisibleLayers) {
        return true;
    }
    var layer = scene.layers.get(layerName);
    if (!layer) {
        return true;
    }
    return !layer.isOff && !layer.isFrozen;
}
/**
 * Lists layouts from the drawing's layout table, including model space,
 * sorted by tab order. Used so HTML export covers paper-space tabs that
 * were never visited (and may be missing from {@link AcTrScene.layouts}).
 *
 * @param database - Open drawing database.
 * @returns Layouts in tab order; empty when the layout table is unavailable.
 */
export function listDatabaseLayouts(database) {
    var e_3, _a;
    var _b, _c;
    var layoutTable = (_b = database.objects) === null || _b === void 0 ? void 0 : _b.layout;
    if (!(layoutTable === null || layoutTable === void 0 ? void 0 : layoutTable.newIterator))
        return [];
    var layouts = [];
    try {
        for (var _d = __values(layoutTable.newIterator()), _e = _d.next(); !_e.done; _e = _d.next()) {
            var layout = _e.value;
            var blockTableRecordId = layout.blockTableRecordId;
            if (!blockTableRecordId)
                continue;
            layouts.push({
                name: layout.layoutName || blockTableRecordId,
                tabOrder: (_c = layout.tabOrder) !== null && _c !== void 0 ? _c : 0,
                blockTableRecordId: blockTableRecordId
            });
        }
    }
    catch (e_3_1) { e_3 = { error: e_3_1 }; }
    finally {
        try {
            if (_e && !_e.done && (_a = _d.return)) _a.call(_d);
        }
        finally { if (e_3) throw e_3.error; }
    }
    layouts.sort(function (a, b) { return a.tabOrder - b.tabOrder; });
    return layouts;
}
/**
 * Ordered BTR ids to export: layout-table tabs first (tab order), then any
 * extra scene layouts that are not in the table. When `exportLayouts` is
 * `false`, only model space is included.
 */
function listExportLayoutBtrIds(scene, tableLayouts, exportLayouts) {
    var e_4, _a, e_5, _b;
    if (!exportLayouts) {
        return scene.modelSpaceBtrId ? [scene.modelSpaceBtrId] : [];
    }
    var seen = new Set();
    var ids = [];
    try {
        for (var tableLayouts_1 = __values(tableLayouts), tableLayouts_1_1 = tableLayouts_1.next(); !tableLayouts_1_1.done; tableLayouts_1_1 = tableLayouts_1.next()) {
            var layout = tableLayouts_1_1.value;
            if (seen.has(layout.blockTableRecordId))
                continue;
            seen.add(layout.blockTableRecordId);
            ids.push(layout.blockTableRecordId);
        }
    }
    catch (e_4_1) { e_4 = { error: e_4_1 }; }
    finally {
        try {
            if (tableLayouts_1_1 && !tableLayouts_1_1.done && (_a = tableLayouts_1.return)) _a.call(tableLayouts_1);
        }
        finally { if (e_4) throw e_4.error; }
    }
    try {
        for (var _c = __values(scene.layouts.keys()), _d = _c.next(); !_d.done; _d = _c.next()) {
            var btrId = _d.value;
            if (seen.has(btrId))
                continue;
            seen.add(btrId);
            ids.push(btrId);
        }
    }
    catch (e_5_1) { e_5 = { error: e_5_1 }; }
    finally {
        try {
            if (_d && !_d.done && (_b = _c.return)) _b.call(_c);
        }
        finally { if (e_5) throw e_5.error; }
    }
    return ids;
}
function resolveExportActiveLayoutBtrId(scene, exportLayouts) {
    if (!exportLayouts) {
        return scene.modelSpaceBtrId;
    }
    return scene.activeLayoutBtrId || scene.modelSpaceBtrId;
}
function collectLayoutSnapshot(scene, database, btrId, layoutNames, options, includeLayer) {
    var e_6, _a;
    var _b;
    var lineBatches = [];
    var meshBatches = [];
    var layout = scene.layouts.get(btrId);
    if (layout) {
        try {
            for (var _c = __values(layout.layers), _d = _c.next(); !_d.done; _d = _c.next()) {
                var _e = __read(_d.value, 2), layer = _e[1];
                if (includeLayer && !includeLayer(layer.name)) {
                    continue;
                }
                var collected = collectBatchesFromObject3D(layer.internalObject);
                lineBatches.push.apply(lineBatches, __spreadArray([], __read(collected.lineBatches), false));
                meshBatches.push.apply(meshBatches, __spreadArray([], __read(collected.meshBatches), false));
            }
        }
        catch (e_6_1) { e_6 = { error: e_6_1 }; }
        finally {
            try {
                if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
            }
            finally { if (e_6) throw e_6.error; }
        }
    }
    var isModelSpace = btrId === scene.modelSpaceBtrId;
    return {
        btrId: btrId,
        name: (_b = layoutNames.get(btrId)) !== null && _b !== void 0 ? _b : resolveBlockName(database, btrId),
        isModelSpace: isModelSpace,
        lineBatches: lineBatches,
        meshBatches: meshBatches,
        osnap: shouldExportOsnap(options)
            ? buildOsnapCatalog(database, btrId, { includeLayer: includeLayer })
            : undefined,
        viewports: collectLayoutViewports(database, btrId, isModelSpace)
    };
}
/**
 * Resolves a layout BTR id to the block-table record name when the layout
 * table has no matching row.
 */
function resolveBlockName(database, btrId) {
    var e_7, _a;
    var _b;
    var blockTable = (_b = database.tables) === null || _b === void 0 ? void 0 : _b.blockTable;
    if (blockTable === null || blockTable === void 0 ? void 0 : blockTable.newIterator) {
        try {
            for (var _c = __values(blockTable.newIterator()), _d = _c.next(); !_d.done; _d = _c.next()) {
                var block = _d.value;
                if (block.objectId === btrId) {
                    return block.name;
                }
            }
        }
        catch (e_7_1) { e_7 = { error: e_7_1 }; }
        finally {
            try {
                if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
            }
            finally { if (e_7) throw e_7.error; }
        }
    }
    return btrId;
}
//# sourceMappingURL=AcApHtmlSnapshotBuilder.js.map