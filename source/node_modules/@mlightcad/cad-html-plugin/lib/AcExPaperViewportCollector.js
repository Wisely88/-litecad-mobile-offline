var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcDbViewport } from '@mlightcad/data-model';
import { AcTrViewportView } from '@mlightcad/three-renderer';
/**
 * Collects user-created paper-space viewports for HTML export.
 *
 * Skips the default `*Paper_Space` viewport via
 * {@link AcTrViewportView.isDefaultPaperSpaceViewport}. Model-space layouts
 * return `undefined`.
 *
 * @param database - Open drawing database.
 * @param layoutBtrId - Block-table-record id of the layout.
 * @param isModelSpace - When `true`, no viewports are collected.
 */
export function collectLayoutViewports(database, layoutBtrId, isModelSpace) {
    var e_1, _a;
    if (isModelSpace)
        return undefined;
    var block = resolveLayoutBlock(database, layoutBtrId);
    if (!(block === null || block === void 0 ? void 0 : block.newIterator))
        return undefined;
    var viewports = [];
    try {
        for (var _b = __values(block.newIterator()), _c = _b.next(); !_c.done; _c = _b.next()) {
            var entity = _c.value;
            if (!(entity instanceof AcDbViewport))
                continue;
            if (AcTrViewportView.isDefaultPaperSpaceViewport(entity))
                continue;
            if (typeof entity.toGiViewport !== 'function')
                continue;
            var gi = entity.toGiViewport();
            var paperBox = gi.box;
            var modelBox = gi.viewBox;
            var paper = extentsFromBox2d(paperBox);
            var model = extentsFromBox2d(modelBox);
            if (!paper || !model)
                continue;
            var twist = Number.isFinite(gi.viewTwistAngle)
                ? gi.viewTwistAngle
                : entity.viewTwistAngle;
            viewports.push(Number.isFinite(twist) && Math.abs(twist) > 1e-12
                ? { paper: paper, model: model, twist: twist }
                : { paper: paper, model: model });
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_1) throw e_1.error; }
    }
    return viewports.length > 0 ? viewports : undefined;
}
function extentsFromBox2d(box) {
    if (typeof box.isEmpty === 'function' && box.isEmpty())
        return undefined;
    var minX = box.min.x;
    var minY = box.min.y;
    var maxX = box.max.x;
    var maxY = box.max.y;
    if (!Number.isFinite(minX) ||
        !Number.isFinite(minY) ||
        !Number.isFinite(maxX) ||
        !Number.isFinite(maxY) ||
        maxX - minX <= 0 ||
        maxY - minY <= 0) {
        return undefined;
    }
    return { minX: minX, minY: minY, maxX: maxX, maxY: maxY };
}
function resolveLayoutBlock(database, layoutBtrId) {
    var e_2, _a;
    var _b;
    var blockTable = (_b = database.tables) === null || _b === void 0 ? void 0 : _b.blockTable;
    if (!blockTable)
        return undefined;
    var direct = typeof blockTable.getIdAt === 'function'
        ? blockTable.getIdAt(layoutBtrId)
        : undefined;
    if (direct)
        return direct;
    if (typeof blockTable.newIterator === 'function') {
        try {
            for (var _c = __values(blockTable.newIterator()), _d = _c.next(); !_d.done; _d = _c.next()) {
                var block = _d.value;
                if (block.objectId === layoutBtrId) {
                    return block;
                }
            }
        }
        catch (e_2_1) { e_2 = { error: e_2_1 }; }
        finally {
            try {
                if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
            }
            finally { if (e_2) throw e_2.error; }
        }
    }
    var modelSpace = blockTable.modelSpace;
    if ((modelSpace === null || modelSpace === void 0 ? void 0 : modelSpace.objectId) === layoutBtrId) {
        return modelSpace;
    }
    return undefined;
}
//# sourceMappingURL=AcExPaperViewportCollector.js.map