var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
import { FLOAT_TOL } from '@mlightcad/data-model';
import RBush from 'rbush';
import { toWcsCoord } from './AcExBatchBuffers';
import { collectPrimitiveDiscreteSnapCandidates, collectPrimitiveNearestSnapCandidate, distSq, inwardArcAlignment, isBetterArcLock } from './AcExOsnapGeometry';
import { ACEX_MAX_INTERSECTION_SOURCES, intersectionGeomToleranceForSnap, intersectionToleranceForExtent, intersectLineSegmentPoints, intersectPrimitivePair, isIntersectionCapablePrimitive } from './AcExOsnapIntersections';
import { expandOsnapPath, isOsnapPathPrimitive, pathEdgeNearAperture, pathPrimitiveBounds } from './AcExOsnapPath';
import { primitiveToAcGeCurve } from './AcExOsnapPrimitiveToAcGe';
import { ACEX_DEFAULT_OSNAP_MODES } from './AcExOsnapPrimitiveTypes';
export { ACEX_DEFAULT_OSNAP_MODES } from './AcExOsnapPrimitiveTypes';
/**
 * How often to sample the wall-clock budget while walking segments / building
 * RBush entries. `performance.now()` is cheap; checking only every 8192 items
 * let a single batch overshoot the slice by tens of milliseconds.
 */
var OSNAP_INDEX_YIELD_CHECK_EVERY = 1024;
/**
 * Target main-thread slice before yielding.
 *
 * 200ms is already a Chrome "long task" (~12 frames at 60Hz). 100ms still
 * hitchs slightly but keeps million-edge indexes from turning into a rAF
 * wait-per-chunk slog.
 */
var OSNAP_INDEX_YIELD_BUDGET_MS = 100;
/** Bulk-load this many RBush entries per slice so `load()` itself can yield. */
var OSNAP_RBUSH_LOAD_CHUNK = 32768;
function yieldToBrowser() {
    return new Promise(function (resolve) {
        setTimeout(resolve, 0);
    });
}
/**
 * Yields only after {@link OSNAP_INDEX_YIELD_BUDGET_MS} of continuous work,
 * sampled every {@link OSNAP_INDEX_YIELD_CHECK_EVERY} items.
 * Returns a promise only when a yield is actually scheduled (callers should
 * `await` only when non-null) so tight loops avoid millions of Promise allocs.
 */
function createOsnapYieldScheduler(yieldFn) {
    var itemsSinceCheck = 0;
    var sliceStart = performance.now();
    return {
        afterItem: function () {
            itemsSinceCheck += 1;
            if (itemsSinceCheck < OSNAP_INDEX_YIELD_CHECK_EVERY) {
                return undefined;
            }
            itemsSinceCheck = 0;
            if (performance.now() - sliceStart < OSNAP_INDEX_YIELD_BUDGET_MS) {
                return undefined;
            }
            return yieldFn().then(function () {
                sliceStart = performance.now();
            });
        }
    };
}
function searchRbushForest(tree, extras, box) {
    var e_1, _a;
    if (extras.length === 0) {
        return tree.search(box);
    }
    var hits = tree.search(box).slice();
    try {
        for (var extras_1 = __values(extras), extras_1_1 = extras_1.next(); !extras_1_1.done; extras_1_1 = extras_1.next()) {
            var extra = extras_1_1.value;
            var part = extra.search(box);
            for (var i = 0; i < part.length; i++) {
                hits.push(part[i]);
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (extras_1_1 && !extras_1_1.done && (_a = extras_1.return)) _a.call(extras_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
    return hits;
}
function bulkLoadRbush(tree, extras, entries, yieldFn) {
    return __awaiter(this, void 0, void 0, function () {
        var offset, chunk, extra;
        return __generator(this, function (_a) {
            switch (_a.label) {
                case 0:
                    extras.length = 0;
                    tree.clear();
                    if (entries.length === 0) {
                        return [2 /*return*/];
                    }
                    offset = 0;
                    _a.label = 1;
                case 1:
                    if (!(offset < entries.length)) return [3 /*break*/, 4];
                    chunk = entries.slice(offset, offset + OSNAP_RBUSH_LOAD_CHUNK);
                    if (offset === 0) {
                        tree.load(chunk);
                    }
                    else {
                        extra = new RBush();
                        extra.load(chunk);
                        extras.push(extra);
                    }
                    if (!(offset + OSNAP_RBUSH_LOAD_CHUNK < entries.length)) return [3 /*break*/, 3];
                    return [4 /*yield*/, yieldFn()];
                case 2:
                    _a.sent();
                    _a.label = 3;
                case 3:
                    offset += OSNAP_RBUSH_LOAD_CHUNK;
                    return [3 /*break*/, 1];
                case 4: return [2 /*return*/];
            }
        });
    });
}
function modePriority(mode) {
    switch (mode) {
        case 'endpoint':
        case 'midpoint':
        case 'center':
        case 'intersection':
            return 0;
        case 'quadrant':
        case 'node':
            return 1;
        case 'nearest':
            return 2;
        default:
            return 1;
    }
}
function closestPointOnSegment(px, py, seg) {
    var dx = seg.x1 - seg.x0;
    var dy = seg.y1 - seg.y0;
    var lenSq = dx * dx + dy * dy;
    if (lenSq < 1e-18) {
        return {
            x: seg.x0,
            y: seg.y0,
            distSq: distSq(px, py, seg.x0, seg.y0)
        };
    }
    var t = ((px - seg.x0) * dx + (py - seg.y0) * dy) / lenSq;
    t = Math.max(0, Math.min(1, t));
    var x = seg.x0 + t * dx;
    var y = seg.y0 + t * dy;
    return { x: x, y: y, distSq: distSq(px, py, x, y) };
}
function appendSegments(target, source) {
    var e_2, _a;
    try {
        for (var source_1 = __values(source), source_1_1 = source_1.next(); !source_1_1.done; source_1_1 = source_1.next()) {
            var seg = source_1_1.value;
            target.push(seg);
        }
    }
    catch (e_2_1) { e_2 = { error: e_2_1 }; }
    finally {
        try {
            if (source_1_1 && !source_1_1.done && (_a = source_1.return)) _a.call(source_1);
        }
        finally { if (e_2) throw e_2.error; }
    }
}
function segmentsFromIterable(source) {
    var result = [];
    appendSegments(result, source);
    return result;
}
function iterLineSegments(batch) {
    var _a, ox, oy, p, i, i0, i1, i;
    return __generator(this, function (_b) {
        switch (_b.label) {
            case 0:
                _a = __read(batch.offset, 2), ox = _a[0], oy = _a[1];
                p = batch.positions;
                if (!(batch.indices && batch.indices.length >= 2)) return [3 /*break*/, 5];
                i = 0;
                _b.label = 1;
            case 1:
                if (!(i + 1 < batch.indices.length)) return [3 /*break*/, 4];
                i0 = batch.indices[i] * 3;
                i1 = batch.indices[i + 1] * 3;
                return [4 /*yield*/, {
                        x0: toWcsCoord(p[i0], ox),
                        y0: toWcsCoord(p[i0 + 1], oy),
                        x1: toWcsCoord(p[i1], ox),
                        y1: toWcsCoord(p[i1 + 1], oy)
                    }];
            case 2:
                _b.sent();
                _b.label = 3;
            case 3:
                i += 2;
                return [3 /*break*/, 1];
            case 4: return [2 /*return*/];
            case 5:
                i = 0;
                _b.label = 6;
            case 6:
                if (!(i + 5 < p.length)) return [3 /*break*/, 9];
                return [4 /*yield*/, {
                        x0: toWcsCoord(p[i], ox),
                        y0: toWcsCoord(p[i + 1], oy),
                        x1: toWcsCoord(p[i + 3], ox),
                        y1: toWcsCoord(p[i + 4], oy)
                    }];
            case 7:
                _b.sent();
                _b.label = 8;
            case 8:
                i += 6;
                return [3 /*break*/, 6];
            case 9: return [2 /*return*/];
        }
    });
}
/**
 * Returns whether two WCS XY points coincide within a tolerance.
 *
 * @param x1 - First point X.
 * @param y1 - First point Y.
 * @param x2 - Second point X.
 * @param y2 - Second point Y.
 * @param tol - Maximum distance treated as coincident (drawing units).
 * @returns `true` when the Euclidean distance is at most `tol`.
 * @internal
 */
function pointsEqual(x1, y1, x2, y2, tol) {
    return Math.hypot(x1 - x2, y1 - y2) <= tol;
}
/**
 * Merges line segments that share endpoints into longer logical segments.
 *
 * Non-indexed {@link AcExLineBatch} geometry stores disconnected vertex pairs
 * (one pair per rendered edge). When {@link AcExLineBatch.linePattern} is set,
 * consecutive pairs that meet at a common endpoint belong to the same CAD entity
 * and should snap as one line, not as independent dash or tessellation fragments.
 *
 * The algorithm greedily extends each unused seed segment forward and backward
 * by attaching neighbors whose endpoints coincide within `tol`.
 *
 * @param segments - Tessellated WCS segments, typically from {@link iterLineSegments}.
 * @param tol - Endpoint coincidence tolerance in drawing units; defaults to {@link FLOAT_TOL}.
 * @returns One segment per connected chain; input order does not affect the result.
 * @internal
 */
export function mergeConnectedSegments(segments, tol) {
    if (tol === void 0) { tol = FLOAT_TOL; }
    if (segments.length <= 1) {
        return segments;
    }
    var used = new Array(segments.length).fill(false);
    var merged = [];
    for (var start = 0; start < segments.length; start++) {
        if (used[start])
            continue;
        var seed = segments[start];
        var x0 = seed.x0;
        var y0 = seed.y0;
        var x1 = seed.x1;
        var y1 = seed.y1;
        used[start] = true;
        var extended = true;
        while (extended) {
            extended = false;
            for (var i = 0; i < segments.length; i++) {
                if (used[i])
                    continue;
                var seg = segments[i];
                if (pointsEqual(x1, y1, seg.x0, seg.y0, tol)) {
                    x1 = seg.x1;
                    y1 = seg.y1;
                    used[i] = true;
                    extended = true;
                }
                else if (pointsEqual(x1, y1, seg.x1, seg.y1, tol)) {
                    x1 = seg.x0;
                    y1 = seg.y0;
                    used[i] = true;
                    extended = true;
                }
            }
        }
        extended = true;
        while (extended) {
            extended = false;
            for (var i = 0; i < segments.length; i++) {
                if (used[i])
                    continue;
                var seg = segments[i];
                if (pointsEqual(x0, y0, seg.x1, seg.y1, tol)) {
                    x0 = seg.x0;
                    y0 = seg.y0;
                    used[i] = true;
                    extended = true;
                }
                else if (pointsEqual(x0, y0, seg.x0, seg.y0, tol)) {
                    x0 = seg.x1;
                    y0 = seg.y1;
                    used[i] = true;
                    extended = true;
                }
            }
        }
        merged.push({ x0: x0, y0: y0, x1: x1, y1: y1 });
    }
    return merged;
}
/**
 * Extracts WCS snap segments from one exported {@link AcExLineBatch}.
 *
 * Always walks stored segment pairs / index edges via {@link iterLineSegments}.
 * Patterned (dashed) batches are **not** collapsed to first/last chain ends —
 * that dropped intermediate WidePolyline / LWPOLYLINE vertices. Linetype gaps
 * are shader-only; the vertex chain is the entity geometry AutoCAD snaps to.
 *
 * @param batch - One line batch from {@link AcExLayoutSnapshot.lineBatches}.
 * @returns Snap segments in WCS; may be empty when the batch has no geometry.
 */
export function extractLineBatchSnapSegments(batch) {
    return segmentsFromIterable(iterLineSegments(batch));
}
/**
 * Collects tessellated snap segments from drawable {@link AcExLineBatch}s.
 *
 * Mesh / hatch triangulation (`meshBatches`) is intentionally skipped: those
 * edges are display fills, not CAD snap targets, and indexing every triangle
 * edge on large drawings can take minutes and flood RBush.
 *
 * Segments with non-finite endpoints are dropped so one NaN polyline cannot
 * poison RBush parent bounds (same failure mode as the main viewer spatial index).
 *
 * @param layout - Active layout snapshot.
 * @returns Flat list of WCS segments and parallel layer names for spatial indexing.
 * @internal
 */
function collectBatchSegments(layout) {
    var e_3, _a, e_4, _b;
    var segments = [];
    var segmentLayers = [];
    var pushSegment = function (seg, layer) {
        if (!isFiniteSegment(seg))
            return;
        segments.push(seg);
        segmentLayers.push(layer);
    };
    try {
        for (var _c = __values(layout.lineBatches), _d = _c.next(); !_d.done; _d = _c.next()) {
            var batch = _d.value;
            if (batch.excludeFromOsnap)
                continue;
            try {
                for (var _e = (e_4 = void 0, __values(extractLineBatchSnapSegments(batch))), _f = _e.next(); !_f.done; _f = _e.next()) {
                    var seg = _f.value;
                    pushSegment(seg, batch.layer);
                }
            }
            catch (e_4_1) { e_4 = { error: e_4_1 }; }
            finally {
                try {
                    if (_f && !_f.done && (_b = _e.return)) _b.call(_e);
                }
                finally { if (e_4) throw e_4.error; }
            }
        }
    }
    catch (e_3_1) { e_3 = { error: e_3_1 }; }
    finally {
        try {
            if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
        }
        finally { if (e_3) throw e_3.error; }
    }
    return { segments: segments, segmentLayers: segmentLayers };
}
function layoutHasDrawableLineBatches(layout) {
    var e_5, _a;
    try {
        for (var _b = __values(layout.lineBatches), _c = _b.next(); !_c.done; _c = _b.next()) {
            var batch = _c.value;
            if (batch.excludeFromOsnap)
                continue;
            if (batch.positions.length >= 6) {
                return true;
            }
        }
    }
    catch (e_5_1) { e_5 = { error: e_5_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_5) throw e_5.error; }
    }
    return false;
}
/**
 * Like {@link collectBatchSegments}, but time-slices the walk so the UI stays
 * responsive without yielding on every few thousand edges (rAF-per-chunk made
 * large drawings take minutes).
 */
function collectBatchSegmentsAsync(layout, yieldFn) {
    return __awaiter(this, void 0, void 0, function () {
        var segments, segmentLayers, schedule, _a, _b, batch, source, source_2, source_2_1, seg, wait, e_6_1, e_7_1;
        var e_7, _c, e_6, _d;
        return __generator(this, function (_e) {
            switch (_e.label) {
                case 0:
                    segments = [];
                    segmentLayers = [];
                    schedule = createOsnapYieldScheduler(yieldFn);
                    _e.label = 1;
                case 1:
                    _e.trys.push([1, 12, 13, 14]);
                    _a = __values(layout.lineBatches), _b = _a.next();
                    _e.label = 2;
                case 2:
                    if (!!_b.done) return [3 /*break*/, 11];
                    batch = _b.value;
                    if (batch.excludeFromOsnap)
                        return [3 /*break*/, 10];
                    source = iterLineSegments(batch);
                    _e.label = 3;
                case 3:
                    _e.trys.push([3, 8, 9, 10]);
                    source_2 = (e_6 = void 0, __values(source)), source_2_1 = source_2.next();
                    _e.label = 4;
                case 4:
                    if (!!source_2_1.done) return [3 /*break*/, 7];
                    seg = source_2_1.value;
                    if (!isFiniteSegment(seg))
                        return [3 /*break*/, 6];
                    segments.push(seg);
                    segmentLayers.push(batch.layer);
                    wait = schedule.afterItem();
                    if (!wait) return [3 /*break*/, 6];
                    return [4 /*yield*/, wait];
                case 5:
                    _e.sent();
                    _e.label = 6;
                case 6:
                    source_2_1 = source_2.next();
                    return [3 /*break*/, 4];
                case 7: return [3 /*break*/, 10];
                case 8:
                    e_6_1 = _e.sent();
                    e_6 = { error: e_6_1 };
                    return [3 /*break*/, 10];
                case 9:
                    try {
                        if (source_2_1 && !source_2_1.done && (_d = source_2.return)) _d.call(source_2);
                    }
                    finally { if (e_6) throw e_6.error; }
                    return [7 /*endfinally*/];
                case 10:
                    _b = _a.next();
                    return [3 /*break*/, 2];
                case 11: return [3 /*break*/, 14];
                case 12:
                    e_7_1 = _e.sent();
                    e_7 = { error: e_7_1 };
                    return [3 /*break*/, 14];
                case 13:
                    try {
                        if (_b && !_b.done && (_c = _a.return)) _c.call(_a);
                    }
                    finally { if (e_7) throw e_7.error; }
                    return [7 /*endfinally*/];
                case 14: return [2 /*return*/, { segments: segments, segmentLayers: segmentLayers }];
            }
        });
    });
}
function isFiniteSegment(seg) {
    return (Number.isFinite(seg.x0) &&
        Number.isFinite(seg.y0) &&
        Number.isFinite(seg.x1) &&
        Number.isFinite(seg.y1));
}
function emptyPackedSegments() {
    return {
        coords: new Float64Array(0),
        layerIds: new Uint16Array(0),
        layerNames: []
    };
}
function packSegments(segments, segmentLayers) {
    var count = segments.length;
    if (count === 0)
        return emptyPackedSegments();
    var layerNames = [];
    var layerIndex = new Map();
    var layerIds = new Uint16Array(count);
    var coords = new Float64Array(count * 4);
    for (var i = 0; i < count; i++) {
        var layer = segmentLayers[i];
        var id = layerIndex.get(layer);
        if (id == null) {
            id = layerNames.length;
            if (id > 0xffff) {
                throw new Error('Osnap layer dictionary exceeds Uint16 range');
            }
            layerNames.push(layer);
            layerIndex.set(layer, id);
        }
        layerIds[i] = id;
        var seg = segments[i];
        var o = i * 4;
        coords[o] = seg.x0;
        coords[o + 1] = seg.y0;
        coords[o + 2] = seg.x1;
        coords[o + 3] = seg.y1;
    }
    return { coords: coords, layerIds: layerIds, layerNames: layerNames };
}
function packedSegmentCount(store) {
    return store.layerIds.length;
}
function packedSegmentAt(store, index) {
    var o = index * 4;
    return {
        x0: store.coords[o],
        y0: store.coords[o + 1],
        x1: store.coords[o + 2],
        y1: store.coords[o + 3]
    };
}
function packedSegmentLayerAt(store, index) {
    return store.layerNames[store.layerIds[index]];
}
function packedSegmentBounds(store, index) {
    var o = index * 4;
    var x0 = store.coords[o];
    var y0 = store.coords[o + 1];
    var x1 = store.coords[o + 2];
    var y1 = store.coords[o + 3];
    return {
        minX: Math.min(x0, x1),
        minY: Math.min(y0, y1),
        maxX: Math.max(x0, x1),
        maxY: Math.max(y0, y1)
    };
}
/**
 * Rough item count for deciding whether to show a "building OSNAP" status.
 * Counts analytic primitives + line-batch edges only (meshes are not indexed).
 */
export function estimateOsnapRebuildWork(layout) {
    var e_8, _a;
    var _b, _c;
    var n = (_c = (_b = layout.osnap) === null || _b === void 0 ? void 0 : _b.primitives.length) !== null && _c !== void 0 ? _c : 0;
    try {
        for (var _d = __values(layout.lineBatches), _e = _d.next(); !_e.done; _e = _d.next()) {
            var batch = _e.value;
            if (batch.excludeFromOsnap)
                continue;
            if (batch.indices && batch.indices.length >= 2) {
                n += (batch.indices.length / 2) | 0;
            }
            else {
                n += (batch.positions.length / 6) | 0;
            }
        }
    }
    catch (e_8_1) { e_8 = { error: e_8_1 }; }
    finally {
        try {
            if (_e && !_e.done && (_a = _d.return)) _a.call(_d);
        }
        finally { if (e_8) throw e_8.error; }
    }
    return n;
}
/** Squared distance from a point to an axis-aligned box exterior (0 when inside). @internal */
function distSqToBounds(px, py, minX, minY, maxX, maxY) {
    var dx = px < minX ? minX - px : px > maxX ? px - maxX : 0;
    var dy = py < minY ? minY - py : py > maxY ? py - maxY : 0;
    return dx * dx + dy * dy;
}
function searchBox(px, py, threshold) {
    return {
        minX: px - threshold,
        minY: py - threshold,
        maxX: px + threshold,
        maxY: py + threshold
    };
}
function isFiniteBounds(box) {
    return (Number.isFinite(box.minX) &&
        Number.isFinite(box.minY) &&
        Number.isFinite(box.maxX) &&
        Number.isFinite(box.maxY));
}
function primitiveBounds(prim) {
    switch (prim.kind) {
        case 'line':
            return {
                minX: Math.min(prim.x0, prim.x1),
                minY: Math.min(prim.y0, prim.y1),
                maxX: Math.max(prim.x0, prim.x1),
                maxY: Math.max(prim.y0, prim.y1)
            };
        case 'circle':
        case 'arc':
            return {
                minX: prim.cx - prim.r,
                minY: prim.cy - prim.r,
                maxX: prim.cx + prim.r,
                maxY: prim.cy + prim.r
            };
        case 'ellipse':
            return {
                minX: prim.cx - prim.majorR,
                minY: prim.cy - prim.majorR,
                maxX: prim.cx + prim.majorR,
                maxY: prim.cy + prim.majorR
            };
        case 'spline': {
            var minX = Infinity;
            var minY = Infinity;
            var maxX = -Infinity;
            var maxY = -Infinity;
            for (var i = 0; i + 1 < prim.controlPoints.length; i += 2) {
                var x = prim.controlPoints[i];
                var y = prim.controlPoints[i + 1];
                minX = Math.min(minX, x);
                minY = Math.min(minY, y);
                maxX = Math.max(maxX, x);
                maxY = Math.max(maxY, y);
            }
            return { minX: minX, minY: minY, maxX: maxX, maxY: maxY };
        }
        case 'path':
            return pathPrimitiveBounds(prim);
        case 'point':
            return { minX: prim.x, minY: prim.y, maxX: prim.x, maxY: prim.y };
    }
}
/**
 * Spatial index for object snap in the offline HTML viewer.
 *
 * {@link AcExOsnapIndex.rebuild} loads analytic primitives and tessellated
 * segments into RBush trees only. Discrete snap points, nearest, and
 * intersection candidates are computed on pointer query from nearby geometry.
 */
var AcExOsnapIndex = /** @class */ (function () {
    /**
     * @param modes - Enabled snap modes; defaults to {@link ACEX_DEFAULT_OSNAP_MODES}.
     */
    function AcExOsnapIndex(modes) {
        if (modes === void 0) { modes = ACEX_DEFAULT_OSNAP_MODES; }
        this.packedSegments = emptyPackedSegments();
        this.primitives = [];
        this.primitiveTree = new RBush();
        this.segmentTree = new RBush();
        /** Extra bulk-loaded trees when async rebuild splits {@link OSNAP_RBUSH_LOAD_CHUNK}. */
        this.primitiveTreeExtras = [];
        this.segmentTreeExtras = [];
        this.hiddenLayers = new Set();
        this.modes = new Set(modes);
    }
    /**
     * Marks one layer hidden or visible for object snap without rebuilding the index.
     *
     * @param layerName - Layer to update.
     * @param hidden - When `true`, snap skips geometry on this layer.
     */
    AcExOsnapIndex.prototype.setLayerHidden = function (layerName, hidden) {
        if (hidden) {
            this.hiddenLayers.add(layerName);
        }
        else {
            this.hiddenLayers.delete(layerName);
        }
    };
    /** Makes every indexed layer eligible for object snap. */
    AcExOsnapIndex.prototype.showAllLayers = function () {
        this.hiddenLayers.clear();
    };
    /**
     * Excludes every listed layer from object snap.
     *
     * @param layerNames - Layers to hide from snap queries.
     */
    AcExOsnapIndex.prototype.hideAllLayers = function (layerNames) {
        var e_9, _a;
        this.hiddenLayers.clear();
        try {
            for (var layerNames_1 = __values(layerNames), layerNames_1_1 = layerNames_1.next(); !layerNames_1_1.done; layerNames_1_1 = layerNames_1.next()) {
                var name_1 = layerNames_1_1.value;
                this.hiddenLayers.add(name_1);
            }
        }
        catch (e_9_1) { e_9 = { error: e_9_1 }; }
        finally {
            try {
                if (layerNames_1_1 && !layerNames_1_1.done && (_a = layerNames_1.return)) _a.call(layerNames_1);
            }
            finally { if (e_9) throw e_9.error; }
        }
    };
    /**
     * Builds spatial indexes from the active layout snapshot.
     *
     * Loads analytic curve/point primitives (when present) and tessellated line
     * segments from {@link AcExLineBatch} into RBush trees. Straight edges are
     * expected from batches; ACEO catalogs omit `line` kinds to avoid duplication.
     *
     * Prefer {@link rebuildAsync} for large layouts so the UI thread can paint
     * between batches.
     *
     * @param layout - Active layout snapshot (batches + optional {@link AcExLayoutSnapshot.osnap}).
     */
    AcExOsnapIndex.prototype.rebuild = function (layout) {
        this.resetFromLayout(layout);
        this.collectSegmentsFromLayout(layout);
        this.loadPrimitiveTreeSync();
        this.loadSegmentTreeSync();
    };
    /**
     * Like {@link rebuild}, but yields while collecting batch segments, filtering
     * primitives, preparing RBush entries, and bulk-loading the trees in chunks.
     *
     * @param layout - Active layout snapshot.
     * @param yieldFn - Called between work batches (defaults to a macrotask yield).
     */
    AcExOsnapIndex.prototype.rebuildAsync = function (layout_1) {
        return __awaiter(this, arguments, void 0, function (layout, yieldFn) {
            var _a, _b;
            if (yieldFn === void 0) { yieldFn = yieldToBrowser; }
            return __generator(this, function (_c) {
                switch (_c.label) {
                    case 0:
                        this.resetIndexState();
                        return [4 /*yield*/, this.filterPrimitivesAsync((_b = (_a = layout.osnap) === null || _a === void 0 ? void 0 : _a.primitives) !== null && _b !== void 0 ? _b : [], yieldFn)];
                    case 1:
                        _c.sent();
                        return [4 /*yield*/, this.collectSegmentsFromLayoutAsync(layout, yieldFn)];
                    case 2:
                        _c.sent();
                        return [4 /*yield*/, this.loadPrimitiveTreeAsync(yieldFn)];
                    case 3:
                        _c.sent();
                        return [4 /*yield*/, this.loadSegmentTreeAsync(yieldFn)];
                    case 4:
                        _c.sent();
                        return [2 /*return*/];
                }
            });
        });
    };
    AcExOsnapIndex.prototype.resetIndexState = function () {
        this.primitiveTree.clear();
        this.segmentTree.clear();
        this.primitiveTreeExtras = [];
        this.segmentTreeExtras = [];
        this.hiddenLayers.clear();
        this.packedSegments = emptyPackedSegments();
    };
    AcExOsnapIndex.prototype.resetFromLayout = function (layout) {
        var _a, _b;
        this.resetIndexState();
        this.primitives = ((_b = (_a = layout.osnap) === null || _a === void 0 ? void 0 : _a.primitives) !== null && _b !== void 0 ? _b : []).filter(function (prim) {
            return isFiniteBounds(primitiveBounds(prim));
        });
    };
    AcExOsnapIndex.prototype.filterPrimitivesAsync = function (source, yieldFn) {
        return __awaiter(this, void 0, void 0, function () {
            var filtered, schedule, i, prim, wait;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        if (source.length === 0) {
                            this.primitives = [];
                            return [2 /*return*/];
                        }
                        filtered = [];
                        schedule = createOsnapYieldScheduler(yieldFn);
                        i = 0;
                        _a.label = 1;
                    case 1:
                        if (!(i < source.length)) return [3 /*break*/, 4];
                        prim = source[i];
                        if (isFiniteBounds(primitiveBounds(prim))) {
                            filtered.push(prim);
                        }
                        wait = schedule.afterItem();
                        if (!wait) return [3 /*break*/, 3];
                        return [4 /*yield*/, wait];
                    case 2:
                        _a.sent();
                        _a.label = 3;
                    case 3:
                        i++;
                        return [3 /*break*/, 1];
                    case 4:
                        this.primitives = filtered;
                        return [2 /*return*/];
                }
            });
        });
    };
    AcExOsnapIndex.prototype.collectSegmentsFromLayout = function (layout) {
        if (!layoutHasDrawableLineBatches(layout)) {
            return;
        }
        var collected = collectBatchSegments(layout);
        this.packedSegments = packSegments(collected.segments, collected.segmentLayers);
    };
    AcExOsnapIndex.prototype.collectSegmentsFromLayoutAsync = function (layout, yieldFn) {
        return __awaiter(this, void 0, void 0, function () {
            var collected;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        if (!layoutHasDrawableLineBatches(layout)) {
                            return [2 /*return*/];
                        }
                        return [4 /*yield*/, collectBatchSegmentsAsync(layout, yieldFn)];
                    case 1:
                        collected = _a.sent();
                        this.packedSegments = packSegments(collected.segments, collected.segmentLayers);
                        return [2 /*return*/];
                }
            });
        });
    };
    AcExOsnapIndex.prototype.loadPrimitiveTreeSync = function () {
        if (this.primitives.length === 0) {
            return;
        }
        var primitiveEntries = [];
        for (var i = 0; i < this.primitives.length; i++) {
            var bounds = primitiveBounds(this.primitives[i]);
            // One NaN bbox poisons RBush so every later findSnap returns empty.
            if (!isFiniteBounds(bounds)) {
                continue;
            }
            primitiveEntries.push(__assign(__assign({}, bounds), { index: i }));
        }
        if (primitiveEntries.length > 0) {
            this.primitiveTree.load(primitiveEntries);
        }
    };
    AcExOsnapIndex.prototype.loadSegmentTreeSync = function () {
        var count = packedSegmentCount(this.packedSegments);
        if (count === 0) {
            return;
        }
        var segmentEntries = [];
        for (var i = 0; i < count; i++) {
            var bounds = packedSegmentBounds(this.packedSegments, i);
            if (!isFiniteBounds(bounds)) {
                continue;
            }
            segmentEntries.push(__assign(__assign({}, bounds), { index: i }));
        }
        if (segmentEntries.length > 0) {
            this.segmentTree.load(segmentEntries);
        }
    };
    AcExOsnapIndex.prototype.loadPrimitiveTreeAsync = function (yieldFn) {
        return __awaiter(this, void 0, void 0, function () {
            var count, schedule, primitiveEntries, i, bounds, wait;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        count = this.primitives.length;
                        if (count === 0) {
                            return [2 /*return*/];
                        }
                        schedule = createOsnapYieldScheduler(yieldFn);
                        primitiveEntries = [];
                        i = 0;
                        _a.label = 1;
                    case 1:
                        if (!(i < count)) return [3 /*break*/, 4];
                        bounds = primitiveBounds(this.primitives[i]);
                        if (!isFiniteBounds(bounds)) {
                            return [3 /*break*/, 3];
                        }
                        primitiveEntries.push(__assign(__assign({}, bounds), { index: i }));
                        wait = schedule.afterItem();
                        if (!wait) return [3 /*break*/, 3];
                        return [4 /*yield*/, wait];
                    case 2:
                        _a.sent();
                        _a.label = 3;
                    case 3:
                        i++;
                        return [3 /*break*/, 1];
                    case 4:
                        if (!(primitiveEntries.length > 0)) return [3 /*break*/, 6];
                        return [4 /*yield*/, bulkLoadRbush(this.primitiveTree, this.primitiveTreeExtras, primitiveEntries, yieldFn)];
                    case 5:
                        _a.sent();
                        _a.label = 6;
                    case 6: return [2 /*return*/];
                }
            });
        });
    };
    AcExOsnapIndex.prototype.loadSegmentTreeAsync = function (yieldFn) {
        return __awaiter(this, void 0, void 0, function () {
            var count, schedule, segmentEntries, i, bounds, wait;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        count = packedSegmentCount(this.packedSegments);
                        if (count === 0) {
                            return [2 /*return*/];
                        }
                        schedule = createOsnapYieldScheduler(yieldFn);
                        segmentEntries = [];
                        i = 0;
                        _a.label = 1;
                    case 1:
                        if (!(i < count)) return [3 /*break*/, 4];
                        bounds = packedSegmentBounds(this.packedSegments, i);
                        if (!isFiniteBounds(bounds)) {
                            return [3 /*break*/, 3];
                        }
                        segmentEntries.push(__assign(__assign({}, bounds), { index: i }));
                        wait = schedule.afterItem();
                        if (!wait) return [3 /*break*/, 3];
                        return [4 /*yield*/, wait];
                    case 2:
                        _a.sent();
                        _a.label = 3;
                    case 3:
                        i++;
                        return [3 /*break*/, 1];
                    case 4:
                        if (!(segmentEntries.length > 0)) return [3 /*break*/, 6];
                        return [4 /*yield*/, bulkLoadRbush(this.segmentTree, this.segmentTreeExtras, segmentEntries, yieldFn)];
                    case 5:
                        _a.sent();
                        _a.label = 6;
                    case 6: return [2 /*return*/];
                }
            });
        });
    };
    /**
     * Finds the closest circle or circular-arc primitive whose curve is within
     * `threshold` of `(px, py)`.
     *
     * Used by arc-length measurement to lock subsequent picks onto that circle
     * when the first click lands on a `CIRCLE` / `ARC` (including polyline bulges).
     *
     * @param px - Cursor X in drawing units (WCS).
     * @param py - Cursor Y in drawing units (WCS).
     * @param threshold - Maximum distance in drawing units.
     * @returns Circle center, radius, and the nearest point on the drawn
     *   stroke, or `undefined` when none is close enough. `x`/`y` lie on the
     *   curve (including arc endpoints), not a radial projection onto the
     *   complementary full circle.
     */
    AcExOsnapIndex.prototype.findCircleOrArcNear = function (px, py, threshold) {
        var e_10, _a, e_11, _b;
        if (threshold <= 0 || this.primitives.length === 0)
            return undefined;
        var threshSq = threshold * threshold;
        var box = searchBox(px, py, threshold);
        var bestDistSq = threshSq;
        var bestAlign = -Infinity;
        var best;
        var mouse = { x: px, y: py };
        try {
            for (var _c = __values(searchRbushForest(this.primitiveTree, this.primitiveTreeExtras, box)), _d = _c.next(); !_d.done; _d = _c.next()) {
                var hit = _d.value;
                var prim = this.primitives[hit.index];
                if (this.hiddenLayers.has(prim.layer))
                    continue;
                var arcs = prim.kind === 'path'
                    ? expandOsnapPath(prim).filter(function (edge) { return edge.kind === 'arc'; })
                    : prim.kind === 'circle' || prim.kind === 'arc'
                        ? [prim]
                        : [];
                try {
                    for (var arcs_1 = (e_11 = void 0, __values(arcs)), arcs_1_1 = arcs_1.next(); !arcs_1_1.done; arcs_1_1 = arcs_1.next()) {
                        var arcPrim = arcs_1_1.value;
                        if (arcPrim.kind !== 'circle' && arcPrim.kind !== 'arc')
                            continue;
                        var geo = primitiveToAcGeCurve(arcPrim);
                        if (geo.kind !== 'circArc')
                            continue;
                        var nearest = geo.curve.nearestPoint({ x: px, y: py });
                        var d2 = distSq(px, py, nearest.x, nearest.y);
                        if (d2 > threshSq)
                            continue;
                        var align = inwardArcAlignment(geo.curve, nearest, mouse);
                        if (!best || isBetterArcLock(d2, align, bestDistSq, bestAlign)) {
                            bestDistSq = d2;
                            bestAlign = align;
                            best = {
                                cx: arcPrim.cx,
                                cy: arcPrim.cy,
                                r: arcPrim.r,
                                x: nearest.x,
                                y: nearest.y
                            };
                        }
                    }
                }
                catch (e_11_1) { e_11 = { error: e_11_1 }; }
                finally {
                    try {
                        if (arcs_1_1 && !arcs_1_1.done && (_b = arcs_1.return)) _b.call(arcs_1);
                    }
                    finally { if (e_11) throw e_11.error; }
                }
            }
        }
        catch (e_10_1) { e_10 = { error: e_10_1 }; }
        finally {
            try {
                if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
            }
            finally { if (e_10) throw e_10.error; }
        }
        return best;
    };
    /**
     * Finds the best snap point near the cursor in WCS.
     *
     * Queries analytic {@link AcExLayoutSnapshot.osnap} curve primitives together
     * with tessellated {@link AcExLineBatch} segments (lines are derived from
     * geometry, not duplicated in ACEO). Hatch/mesh triangulation is not indexed.
     *
     * Uses AutoCAD-style mode priority: endpoint / midpoint / center beat
     * quadrant / node, which beat nearest. Within the same
     * priority tier, the closest candidate within `threshold` wins.
     *
     * @param px - Cursor X in drawing units (WCS).
     * @param py - Cursor Y in drawing units (WCS).
     * @param threshold - Maximum snap distance in drawing units (aperture radius).
     * @returns The winning snap point, or `undefined` if nothing is within range.
     */
    AcExOsnapIndex.prototype.findSnap = function (px, py, threshold) {
        if (threshold <= 0)
            return undefined;
        if (this.primitives.length === 0 &&
            packedSegmentCount(this.packedSegments) === 0) {
            return undefined;
        }
        var discrete = this.findDiscreteSnap(px, py, threshold);
        var intersection = this.modes.has('intersection')
            ? this.findIntersectionSnap(px, py, threshold)
            : undefined;
        var bestDiscrete = this.pickBestSnapPoint(px, py, threshold, [
            discrete,
            intersection
        ]);
        if (bestDiscrete) {
            return bestDiscrete;
        }
        if (!this.modes.has('nearest')) {
            return undefined;
        }
        return this.findNearestSnap(px, py, threshold);
    };
    AcExOsnapIndex.prototype.pickBestSnapPoint = function (px, py, threshold, candidates) {
        var e_12, _a;
        var threshSq = threshold * threshold;
        var bestPriority = Number.MAX_VALUE;
        var bestDistSq = Number.MAX_VALUE;
        var best;
        try {
            for (var candidates_1 = __values(candidates), candidates_1_1 = candidates_1.next(); !candidates_1_1.done; candidates_1_1 = candidates_1.next()) {
                var candidate = candidates_1_1.value;
                if (!candidate)
                    continue;
                var d2 = distSq(px, py, candidate.x, candidate.y);
                if (d2 > threshSq)
                    continue;
                var priority = modePriority(candidate.mode);
                if (priority < bestPriority ||
                    (priority === bestPriority && d2 < bestDistSq)) {
                    bestPriority = priority;
                    bestDistSq = d2;
                    best = candidate;
                }
            }
        }
        catch (e_12_1) { e_12 = { error: e_12_1 }; }
        finally {
            try {
                if (candidates_1_1 && !candidates_1_1.done && (_a = candidates_1.return)) _a.call(candidates_1);
            }
            finally { if (e_12) throw e_12.error; }
        }
        return best;
    };
    /**
     * Finds an intersection snap near the cursor by testing pairs of geometry
     * sources whose bounds overlap the osnap aperture (RBush-filtered).
     */
    AcExOsnapIndex.prototype.findIntersectionSnap = function (px, py, threshold) {
        var e_13, _a, e_14, _b, e_15, _c, e_16, _d, e_17, _e, e_18, _f, e_19, _g, e_20, _h;
        var box = searchBox(px, py, threshold);
        var primHits = searchRbushForest(this.primitiveTree, this.primitiveTreeExtras, box);
        var segHits = searchRbushForest(this.segmentTree, this.segmentTreeExtras, box);
        if (primHits.length === 0 && segHits.length === 0)
            return undefined;
        var threshSq = threshold * threshold;
        var extent = Math.max(box.maxX - box.minX, box.maxY - box.minY, 1);
        var paramTol = intersectionToleranceForExtent(extent);
        var geomTol = intersectionGeomToleranceForSnap(extent, threshold);
        var segIndices = [];
        var primSeen = new Set();
        var segSeen = new Set();
        var workPrims = [];
        try {
            for (var primHits_1 = __values(primHits), primHits_1_1 = primHits_1.next(); !primHits_1_1.done; primHits_1_1 = primHits_1.next()) {
                var hit = primHits_1_1.value;
                var prim = this.primitives[hit.index];
                if (this.hiddenLayers.has(prim.layer))
                    continue;
                if (primSeen.has(hit.index))
                    continue;
                primSeen.add(hit.index);
                if (isOsnapPathPrimitive(prim)) {
                    try {
                        for (var _j = (e_14 = void 0, __values(expandOsnapPath(prim))), _k = _j.next(); !_k.done; _k = _j.next()) {
                            var edge = _k.value;
                            if (!pathEdgeNearAperture(edge, px, py, threshold))
                                continue;
                            if (workPrims.length >= ACEX_MAX_INTERSECTION_SOURCES)
                                break;
                            workPrims.push(edge);
                        }
                    }
                    catch (e_14_1) { e_14 = { error: e_14_1 }; }
                    finally {
                        try {
                            if (_k && !_k.done && (_b = _j.return)) _b.call(_j);
                        }
                        finally { if (e_14) throw e_14.error; }
                    }
                    continue;
                }
                if (!isIntersectionCapablePrimitive(prim))
                    continue;
                if (workPrims.length >= ACEX_MAX_INTERSECTION_SOURCES)
                    continue;
                workPrims.push(prim);
            }
        }
        catch (e_13_1) { e_13 = { error: e_13_1 }; }
        finally {
            try {
                if (primHits_1_1 && !primHits_1_1.done && (_a = primHits_1.return)) _a.call(primHits_1);
            }
            finally { if (e_13) throw e_13.error; }
        }
        try {
            for (var segHits_1 = __values(segHits), segHits_1_1 = segHits_1.next(); !segHits_1_1.done; segHits_1_1 = segHits_1.next()) {
                var hit = segHits_1_1.value;
                var layer = packedSegmentLayerAt(this.packedSegments, hit.index);
                if (this.hiddenLayers.has(layer))
                    continue;
                if (segSeen.has(hit.index))
                    continue;
                if (segIndices.length >= ACEX_MAX_INTERSECTION_SOURCES)
                    continue;
                segSeen.add(hit.index);
                segIndices.push(hit.index);
            }
        }
        catch (e_15_1) { e_15 = { error: e_15_1 }; }
        finally {
            try {
                if (segHits_1_1 && !segHits_1_1.done && (_c = segHits_1.return)) _c.call(segHits_1);
            }
            finally { if (e_15) throw e_15.error; }
        }
        var bestDistSq = threshSq;
        var best;
        for (var i = 0; i < workPrims.length; i++) {
            var primA = workPrims[i];
            for (var j = i + 1; j < workPrims.length; j++) {
                var primB = workPrims[j];
                if (this.hiddenLayers.has(primA.layer) ||
                    this.hiddenLayers.has(primB.layer)) {
                    continue;
                }
                try {
                    for (var _l = (e_16 = void 0, __values(intersectPrimitivePair(primA, primB, paramTol, geomTol))), _m = _l.next(); !_m.done; _m = _l.next()) {
                        var point = _m.value;
                        var d2 = distSq(px, py, point.x, point.y);
                        if (d2 <= bestDistSq) {
                            bestDistSq = d2;
                            best = { x: point.x, y: point.y, mode: 'intersection' };
                        }
                    }
                }
                catch (e_16_1) { e_16 = { error: e_16_1 }; }
                finally {
                    try {
                        if (_m && !_m.done && (_d = _l.return)) _d.call(_l);
                    }
                    finally { if (e_16) throw e_16.error; }
                }
            }
        }
        for (var i = 0; i < segIndices.length; i++) {
            var indexA = segIndices[i];
            var segA = packedSegmentAt(this.packedSegments, indexA);
            var layerA = packedSegmentLayerAt(this.packedSegments, indexA);
            for (var j = i + 1; j < segIndices.length; j++) {
                var indexB = segIndices[j];
                var segB = packedSegmentAt(this.packedSegments, indexB);
                var layerB = packedSegmentLayerAt(this.packedSegments, indexB);
                if (this.hiddenLayers.has(layerA) || this.hiddenLayers.has(layerB)) {
                    continue;
                }
                try {
                    for (var _o = (e_17 = void 0, __values(intersectLineSegmentPoints(segA, segB, paramTol, geomTol))), _p = _o.next(); !_p.done; _p = _o.next()) {
                        var point = _p.value;
                        var d2 = distSq(px, py, point.x, point.y);
                        if (d2 <= bestDistSq) {
                            bestDistSq = d2;
                            best = { x: point.x, y: point.y, mode: 'intersection' };
                        }
                    }
                }
                catch (e_17_1) { e_17 = { error: e_17_1 }; }
                finally {
                    try {
                        if (_p && !_p.done && (_e = _o.return)) _e.call(_o);
                    }
                    finally { if (e_17) throw e_17.error; }
                }
            }
        }
        try {
            // Hybrid: ACEO curves/path edges × display lineBatches.
            for (var workPrims_1 = __values(workPrims), workPrims_1_1 = workPrims_1.next(); !workPrims_1_1.done; workPrims_1_1 = workPrims_1.next()) {
                var prim = workPrims_1_1.value;
                if (this.hiddenLayers.has(prim.layer))
                    continue;
                if (prim.kind !== 'circle' &&
                    prim.kind !== 'arc' &&
                    prim.kind !== 'line') {
                    continue;
                }
                try {
                    for (var segIndices_1 = (e_19 = void 0, __values(segIndices)), segIndices_1_1 = segIndices_1.next(); !segIndices_1_1.done; segIndices_1_1 = segIndices_1.next()) {
                        var segIndex = segIndices_1_1.value;
                        var layer = packedSegmentLayerAt(this.packedSegments, segIndex);
                        if (this.hiddenLayers.has(layer))
                            continue;
                        var seg = packedSegmentAt(this.packedSegments, segIndex);
                        var asLine = {
                            kind: 'line',
                            layer: layer,
                            x0: seg.x0,
                            y0: seg.y0,
                            x1: seg.x1,
                            y1: seg.y1
                        };
                        try {
                            for (var _q = (e_20 = void 0, __values(intersectPrimitivePair(prim, asLine, paramTol, geomTol))), _r = _q.next(); !_r.done; _r = _q.next()) {
                                var point = _r.value;
                                var d2 = distSq(px, py, point.x, point.y);
                                if (d2 <= bestDistSq) {
                                    bestDistSq = d2;
                                    best = { x: point.x, y: point.y, mode: 'intersection' };
                                }
                            }
                        }
                        catch (e_20_1) { e_20 = { error: e_20_1 }; }
                        finally {
                            try {
                                if (_r && !_r.done && (_h = _q.return)) _h.call(_q);
                            }
                            finally { if (e_20) throw e_20.error; }
                        }
                    }
                }
                catch (e_19_1) { e_19 = { error: e_19_1 }; }
                finally {
                    try {
                        if (segIndices_1_1 && !segIndices_1_1.done && (_g = segIndices_1.return)) _g.call(segIndices_1);
                    }
                    finally { if (e_19) throw e_19.error; }
                }
            }
        }
        catch (e_18_1) { e_18 = { error: e_18_1 }; }
        finally {
            try {
                if (workPrims_1_1 && !workPrims_1_1.done && (_f = workPrims_1.return)) _f.call(workPrims_1);
            }
            finally { if (e_18) throw e_18.error; }
        }
        return best;
    };
    AcExOsnapIndex.prototype.discreteModes = function () {
        var discreteModes = new Set(this.modes);
        discreteModes.delete('nearest');
        discreteModes.delete('intersection');
        return discreteModes;
    };
    AcExOsnapIndex.prototype.considerDiscreteCandidate = function (px, py, threshSq, candidate, layer, state) {
        if (!this.modes.has(candidate.mode))
            return;
        if (this.hiddenLayers.has(layer))
            return;
        var d2 = distSq(px, py, candidate.x, candidate.y);
        if (d2 > threshSq)
            return;
        var priority = modePriority(candidate.mode);
        if (priority < state.bestPriority ||
            (priority === state.bestPriority && d2 < state.bestDistSq)) {
            state.bestPriority = priority;
            state.bestDistSq = d2;
            state.best = candidate;
        }
    };
    AcExOsnapIndex.prototype.findDiscreteSnap = function (px, py, threshold) {
        var e_21, _a, e_22, _b, e_23, _c;
        var discreteModes = this.discreteModes();
        if (discreteModes.size === 0)
            return undefined;
        var threshSq = threshold * threshold;
        var box = searchBox(px, py, threshold);
        var state = {
            bestPriority: Number.MAX_VALUE,
            bestDistSq: Number.MAX_VALUE,
            best: undefined
        };
        try {
            for (var _d = __values(searchRbushForest(this.primitiveTree, this.primitiveTreeExtras, box)), _e = _d.next(); !_e.done; _e = _d.next()) {
                var hit = _e.value;
                var prim = this.primitives[hit.index];
                try {
                    for (var _f = (e_22 = void 0, __values(collectPrimitiveDiscreteSnapCandidates(prim, discreteModes))), _g = _f.next(); !_g.done; _g = _f.next()) {
                        var candidate = _g.value;
                        this.considerDiscreteCandidate(px, py, threshSq, candidate, prim.layer, state);
                    }
                }
                catch (e_22_1) { e_22 = { error: e_22_1 }; }
                finally {
                    try {
                        if (_g && !_g.done && (_b = _f.return)) _b.call(_f);
                    }
                    finally { if (e_22) throw e_22.error; }
                }
            }
        }
        catch (e_21_1) { e_21 = { error: e_21_1 }; }
        finally {
            try {
                if (_e && !_e.done && (_a = _d.return)) _a.call(_d);
            }
            finally { if (e_21) throw e_21.error; }
        }
        try {
            for (var _h = __values(searchRbushForest(this.segmentTree, this.segmentTreeExtras, box)), _j = _h.next(); !_j.done; _j = _h.next()) {
                var hit = _j.value;
                var seg = packedSegmentAt(this.packedSegments, hit.index);
                var layer = packedSegmentLayerAt(this.packedSegments, hit.index);
                if (discreteModes.has('endpoint')) {
                    this.considerDiscreteCandidate(px, py, threshSq, { x: seg.x0, y: seg.y0, mode: 'endpoint' }, layer, state);
                    this.considerDiscreteCandidate(px, py, threshSq, { x: seg.x1, y: seg.y1, mode: 'endpoint' }, layer, state);
                }
                if (discreteModes.has('midpoint')) {
                    this.considerDiscreteCandidate(px, py, threshSq, {
                        x: (seg.x0 + seg.x1) * 0.5,
                        y: (seg.y0 + seg.y1) * 0.5,
                        mode: 'midpoint'
                    }, layer, state);
                }
            }
        }
        catch (e_23_1) { e_23 = { error: e_23_1 }; }
        finally {
            try {
                if (_j && !_j.done && (_c = _h.return)) _c.call(_h);
            }
            finally { if (e_23) throw e_23.error; }
        }
        return state.best;
    };
    AcExOsnapIndex.prototype.findNearestSnap = function (px, py, threshold) {
        var e_24, _a, e_25, _b;
        var threshSq = threshold * threshold;
        var box = searchBox(px, py, threshold);
        var bestDistSq = Number.MAX_VALUE;
        var best;
        try {
            for (var _c = __values(searchRbushForest(this.primitiveTree, this.primitiveTreeExtras, box)), _d = _c.next(); !_d.done; _d = _c.next()) {
                var hit = _d.value;
                if (distSqToBounds(px, py, hit.minX, hit.minY, hit.maxX, hit.maxY) >
                    threshSq) {
                    continue;
                }
                var prim = this.primitives[hit.index];
                if (this.hiddenLayers.has(prim.layer))
                    continue;
                if (prim.kind === 'point')
                    continue;
                var nearest = prim.kind === 'path'
                    ? collectPrimitiveNearestSnapCandidate(prim, px, py)
                    : collectPrimitiveNearestSnapCandidate(prim, px, py, primitiveToAcGeCurve(prim));
                if (!nearest)
                    continue;
                var d2 = distSq(px, py, nearest.x, nearest.y);
                if (d2 <= threshSq && d2 < bestDistSq) {
                    bestDistSq = d2;
                    best = nearest;
                }
            }
        }
        catch (e_24_1) { e_24 = { error: e_24_1 }; }
        finally {
            try {
                if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
            }
            finally { if (e_24) throw e_24.error; }
        }
        try {
            for (var _e = __values(searchRbushForest(this.segmentTree, this.segmentTreeExtras, box)), _f = _e.next(); !_f.done; _f = _e.next()) {
                var hit = _f.value;
                if (distSqToBounds(px, py, hit.minX, hit.minY, hit.maxX, hit.maxY) >
                    threshSq) {
                    continue;
                }
                var layer = packedSegmentLayerAt(this.packedSegments, hit.index);
                if (this.hiddenLayers.has(layer))
                    continue;
                var near = closestPointOnSegment(px, py, packedSegmentAt(this.packedSegments, hit.index));
                if (near.distSq <= threshSq && near.distSq < bestDistSq) {
                    bestDistSq = near.distSq;
                    best = { x: near.x, y: near.y, mode: 'nearest' };
                }
            }
        }
        catch (e_25_1) { e_25 = { error: e_25_1 }; }
        finally {
            try {
                if (_f && !_f.done && (_b = _e.return)) _b.call(_e);
            }
            finally { if (e_25) throw e_25.error; }
        }
        return best;
    };
    return AcExOsnapIndex;
}());
export { AcExOsnapIndex };
/**
 * Maps an {@link AcExOsnapMode} to the on-screen marker glyph in the offline viewer.
 *
 * @param mode - Active snap mode from {@link findSnap} or measurement UI.
 * @returns CSS shape key used by {@link AcExOsnapMarker}.
 */
export function acexOsnapModeToMarkerType(mode) {
    switch (mode) {
        case 'endpoint':
            return 'rect';
        case 'midpoint':
            return 'triangle';
        case 'center':
            return 'circle';
        case 'quadrant':
            return 'diamond';
        case 'nearest':
            return 'x';
        case 'intersection':
            return 'intersection';
        case 'node':
        default:
            return 'rect';
    }
}
//# sourceMappingURL=AcExOsnap.js.map