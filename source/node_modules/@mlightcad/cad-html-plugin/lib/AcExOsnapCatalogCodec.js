var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcExBinaryReader, AcExBinaryWriter } from './AcExBinaryIO';
import { compressSnapshotBinary, decompressSnapshotBinary } from './AcExSnapshotCompression';
/** Magic for ACEO osnap catalog payloads (`ACEO` little-endian). */
export var ACEO_OSNAP_MAGIC = 0x4f454341;
/**
 * Current ACEO payload schema version.
 *
 * Kind `7` (`path`) is included in version 1; the format has not had a formal
 * release, so adding a kind does not require a version bump.
 */
export var ACEO_OSNAP_VERSION = 1;
/** Hard cap on primitives inside one ACEO chunk. */
export var ACEX_MAX_OSNAP_PRIMITIVES_PER_CHUNK = 500000;
/** Hard cap on layer dictionary entries inside one ACEO chunk. */
export var ACEX_MAX_OSNAP_LAYERS_PER_CHUNK = 100000;
var KIND_LINE = 1;
var KIND_CIRCLE = 2;
var KIND_ARC = 3;
var KIND_ELLIPSE = 4;
var KIND_SPLINE = 5;
var KIND_POINT = 6;
var KIND_PATH = 7;
/**
 * Rough uncompressed ACEO size estimate for one primitive (used when splitting
 * catalogs into downloadable chunks). Prefer slightly overestimating.
 */
export function estimateOsnapPrimitiveBytes(primitive) {
    var _a, _b;
    // kind(1) + layerIndex(4) + payload; ignore shared layer-dictionary overhead.
    switch (primitive.kind) {
        case 'line':
            return 1 + 4 + 32;
        case 'circle':
            return 1 + 4 + 24 + 1;
        case 'arc':
            return 1 + 4 + 40 + 1;
        case 'ellipse':
            return 1 + 4 + 64 + 1 + 1;
        case 'point':
            return 1 + 4 + 16;
        case 'path':
            return 1 + 4 + 1 + 4 + primitive.vertices.length * 8;
        case 'spline': {
            var floats = primitive.controlPoints.length +
                primitive.knots.length +
                primitive.weights.length +
                ((_b = (_a = primitive.fitPoints) === null || _a === void 0 ? void 0 : _a.length) !== null && _b !== void 0 ? _b : 0);
            return 1 + 4 + 4 + 1 + floats * 8 + 16;
        }
        default: {
            var _exhaustive = primitive;
            return _exhaustive;
        }
    }
}
/**
 * Splits OSNAP primitives into slices that stay under `maxChunkBytes` (estimate).
 */
export function splitOsnapPrimitives(primitives, maxChunkBytes) {
    var e_1, _a;
    if (primitives.length === 0) {
        return [];
    }
    var slices = [];
    var current = [];
    var estimated = 64;
    var flush = function () {
        if (current.length === 0)
            return;
        slices.push(current);
        current = [];
        estimated = 64;
    };
    try {
        for (var primitives_1 = __values(primitives), primitives_1_1 = primitives_1.next(); !primitives_1_1.done; primitives_1_1 = primitives_1.next()) {
            var primitive = primitives_1_1.value;
            var size = estimateOsnapPrimitiveBytes(primitive);
            if (current.length > 0 &&
                estimated + size > maxChunkBytes) {
                flush();
            }
            current.push(primitive);
            estimated += size;
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (primitives_1_1 && !primitives_1_1.done && (_a = primitives_1.return)) _a.call(primitives_1);
        }
        finally { if (e_1) throw e_1.error; }
    }
    flush();
    return slices;
}
/**
 * Encodes an analytic OSNAP catalog to uncompressed ACEO bytes.
 * Coordinates stay float64; layer names are dictionary-compressed.
 */
export function encodeOsnapCatalogBinary(catalog) {
    var e_2, _a, e_3, _b, e_4, _c;
    var writer = new AcExBinaryWriter();
    writer.writeU32(ACEO_OSNAP_MAGIC);
    writer.writeU8(ACEO_OSNAP_VERSION);
    writer.writeU8(0);
    writer.writeU8(0);
    writer.writeU8(0);
    var layers = [];
    var layerIndex = new Map();
    var layerId = function (name) {
        var existing = layerIndex.get(name);
        if (existing != null)
            return existing;
        var id = layers.length;
        layers.push(name);
        layerIndex.set(name, id);
        return id;
    };
    try {
        // First pass collects layers in encounter order so encode is deterministic.
        for (var _d = __values(catalog.primitives), _e = _d.next(); !_e.done; _e = _d.next()) {
            var primitive = _e.value;
            layerId(primitive.layer);
        }
    }
    catch (e_2_1) { e_2 = { error: e_2_1 }; }
    finally {
        try {
            if (_e && !_e.done && (_a = _d.return)) _a.call(_d);
        }
        finally { if (e_2) throw e_2.error; }
    }
    writer.writeU32(layers.length);
    try {
        for (var layers_1 = __values(layers), layers_1_1 = layers_1.next(); !layers_1_1.done; layers_1_1 = layers_1.next()) {
            var layer = layers_1_1.value;
            writer.writeString(layer);
        }
    }
    catch (e_3_1) { e_3 = { error: e_3_1 }; }
    finally {
        try {
            if (layers_1_1 && !layers_1_1.done && (_b = layers_1.return)) _b.call(layers_1);
        }
        finally { if (e_3) throw e_3.error; }
    }
    writer.writeU32(catalog.primitives.length);
    try {
        for (var _f = __values(catalog.primitives), _g = _f.next(); !_g.done; _g = _f.next()) {
            var primitive = _g.value;
            writePrimitive(writer, primitive, layerId(primitive.layer));
        }
    }
    catch (e_4_1) { e_4 = { error: e_4_1 }; }
    finally {
        try {
            if (_g && !_g.done && (_c = _f.return)) _c.call(_f);
        }
        finally { if (e_4) throw e_4.error; }
    }
    return writer.toUint8Array();
}
/**
 * Decodes an uncompressed ACEO osnap catalog.
 */
export function decodeOsnapCatalogBinary(bytes) {
    var reader = new AcExBinaryReader(bytes);
    var magic = reader.readU32();
    if (magic !== ACEO_OSNAP_MAGIC) {
        throw new Error('Invalid osnap catalog magic');
    }
    var version = reader.readU8();
    reader.readU8();
    reader.readU8();
    reader.readU8();
    if (version !== ACEO_OSNAP_VERSION) {
        throw new Error("Unsupported osnap catalog version: ".concat(version));
    }
    var layerCount = reader.readU32();
    if (layerCount > ACEX_MAX_OSNAP_LAYERS_PER_CHUNK) {
        throw new Error('Osnap layer count exceeds limit');
    }
    var layers = [];
    for (var i = 0; i < layerCount; i++) {
        layers.push(reader.readString());
    }
    var primitiveCount = reader.readU32();
    if (primitiveCount > ACEX_MAX_OSNAP_PRIMITIVES_PER_CHUNK) {
        throw new Error('Osnap primitive count exceeds limit');
    }
    var primitives = [];
    for (var i = 0; i < primitiveCount; i++) {
        primitives.push(readPrimitive(reader, layers));
    }
    return { primitives: primitives };
}
/** Encodes and gzip-compresses an OSNAP catalog (`.osnap.gz` payload). */
export function encodeOsnapCatalogGzip(catalog) {
    var uncompressed = encodeOsnapCatalogBinary(catalog);
    var compressed = compressSnapshotBinary(uncompressed).bytes;
    return { uncompressed: uncompressed, compressed: compressed };
}
/** Gunzips and decodes a `.osnap.gz` catalog payload. */
export function decodeOsnapCatalogGzip(compressed) {
    return decodeOsnapCatalogBinary(decompressSnapshotBinary(compressed));
}
function writeNormalSign(writer, sign) {
    writer.writeU8(sign < 0 ? 0 : 1);
}
function readNormalSign(reader) {
    return reader.readU8() === 0 ? -1 : 1;
}
function writeF64Array(writer, values) {
    var e_5, _a;
    writer.writeU32(values.length);
    try {
        for (var values_1 = __values(values), values_1_1 = values_1.next(); !values_1_1.done; values_1_1 = values_1.next()) {
            var value = values_1_1.value;
            writer.writeF64(value);
        }
    }
    catch (e_5_1) { e_5 = { error: e_5_1 }; }
    finally {
        try {
            if (values_1_1 && !values_1_1.done && (_a = values_1.return)) _a.call(values_1);
        }
        finally { if (e_5) throw e_5.error; }
    }
}
function readF64Array(reader) {
    var count = reader.readU32();
    if (count > ACEX_MAX_OSNAP_PRIMITIVES_PER_CHUNK) {
        throw new Error('Float64 array count exceeds limit');
    }
    var values = [];
    for (var i = 0; i < count; i++) {
        values.push(reader.readF64());
    }
    return values;
}
function writePrimitive(writer, primitive, layer) {
    var _a, _b;
    switch (primitive.kind) {
        case 'line':
            writer.writeU8(KIND_LINE);
            writer.writeU32(layer);
            writer.writeF64(primitive.x0);
            writer.writeF64(primitive.y0);
            writer.writeF64(primitive.x1);
            writer.writeF64(primitive.y1);
            return;
        case 'circle':
            writer.writeU8(KIND_CIRCLE);
            writer.writeU32(layer);
            writer.writeF64(primitive.cx);
            writer.writeF64(primitive.cy);
            writer.writeF64(primitive.r);
            writeNormalSign(writer, primitive.normalSign);
            return;
        case 'arc':
            writer.writeU8(KIND_ARC);
            writer.writeU32(layer);
            writer.writeF64(primitive.cx);
            writer.writeF64(primitive.cy);
            writer.writeF64(primitive.r);
            writer.writeF64(primitive.startAngle);
            writer.writeF64(primitive.endAngle);
            writeNormalSign(writer, primitive.normalSign);
            return;
        case 'ellipse':
            writer.writeU8(KIND_ELLIPSE);
            writer.writeU32(layer);
            writer.writeF64(primitive.cx);
            writer.writeF64(primitive.cy);
            writer.writeF64(primitive.majorX);
            writer.writeF64(primitive.majorY);
            writer.writeF64(primitive.majorR);
            writer.writeF64(primitive.minorR);
            writer.writeF64(primitive.startAngle);
            writer.writeF64(primitive.endAngle);
            writer.writeU8(primitive.closed ? 1 : 0);
            writeNormalSign(writer, (_a = primitive.normalSign) !== null && _a !== void 0 ? _a : 1);
            return;
        case 'spline':
            writer.writeU8(KIND_SPLINE);
            writer.writeU32(layer);
            writeF64Array(writer, primitive.controlPoints);
            writer.writeU32(primitive.degree);
            writeF64Array(writer, primitive.knots);
            writeF64Array(writer, primitive.weights);
            writer.writeU8(primitive.closed ? 1 : 0);
            writeF64Array(writer, (_b = primitive.fitPoints) !== null && _b !== void 0 ? _b : []);
            return;
        case 'point':
            writer.writeU8(KIND_POINT);
            writer.writeU32(layer);
            writer.writeF64(primitive.x);
            writer.writeF64(primitive.y);
            return;
        case 'path':
            writer.writeU8(KIND_PATH);
            writer.writeU32(layer);
            writer.writeU8(primitive.closed ? 1 : 0);
            writeF64Array(writer, primitive.vertices);
            return;
        default: {
            var _exhaustive = primitive;
            throw new Error("Unsupported osnap primitive: ".concat(String(_exhaustive)));
        }
    }
}
function readPrimitive(reader, layers) {
    var kind = reader.readU8();
    var layerIdx = reader.readU32();
    var layer = layers[layerIdx];
    if (layer == null) {
        throw new Error("Invalid osnap layer index: ".concat(layerIdx));
    }
    switch (kind) {
        case KIND_LINE:
            return {
                kind: 'line',
                layer: layer,
                x0: reader.readF64(),
                y0: reader.readF64(),
                x1: reader.readF64(),
                y1: reader.readF64()
            };
        case KIND_CIRCLE:
            return {
                kind: 'circle',
                layer: layer,
                cx: reader.readF64(),
                cy: reader.readF64(),
                r: reader.readF64(),
                normalSign: readNormalSign(reader)
            };
        case KIND_ARC:
            return {
                kind: 'arc',
                layer: layer,
                cx: reader.readF64(),
                cy: reader.readF64(),
                r: reader.readF64(),
                startAngle: reader.readF64(),
                endAngle: reader.readF64(),
                normalSign: readNormalSign(reader)
            };
        case KIND_ELLIPSE:
            return {
                kind: 'ellipse',
                layer: layer,
                cx: reader.readF64(),
                cy: reader.readF64(),
                majorX: reader.readF64(),
                majorY: reader.readF64(),
                majorR: reader.readF64(),
                minorR: reader.readF64(),
                startAngle: reader.readF64(),
                endAngle: reader.readF64(),
                closed: reader.readU8() !== 0,
                normalSign: readNormalSign(reader)
            };
        case KIND_SPLINE: {
            var controlPoints = readF64Array(reader);
            var degree = reader.readU32();
            var knots = readF64Array(reader);
            var weights = readF64Array(reader);
            var closed_1 = reader.readU8() !== 0;
            var fitPoints = readF64Array(reader);
            return __assign({ kind: 'spline', layer: layer, controlPoints: controlPoints, degree: degree, knots: knots, weights: weights, closed: closed_1 }, (fitPoints.length > 0 ? { fitPoints: fitPoints } : {}));
        }
        case KIND_POINT:
            return {
                kind: 'point',
                layer: layer,
                x: reader.readF64(),
                y: reader.readF64()
            };
        case KIND_PATH: {
            var closed_2 = reader.readU8() !== 0;
            var vertices = readF64Array(reader);
            return {
                kind: 'path',
                layer: layer,
                closed: closed_2,
                vertices: vertices
            };
        }
        default:
            throw new Error("Unsupported osnap primitive kind: ".concat(kind));
    }
}
//# sourceMappingURL=AcExOsnapCatalogCodec.js.map