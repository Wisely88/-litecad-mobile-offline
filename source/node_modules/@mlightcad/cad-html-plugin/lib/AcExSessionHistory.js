/**
 * Snapshot undo/redo for offline HTML markup + measurement overlays.
 *
 * Mirrors cad-simple-viewer {@link AcApMarkupHistory} / {@link AcApSessionUndo}
 * without DocManager, database transactions, or AcTrHtmlGroup detach/reattach.
 * HTML controllers own DOM/canvas visuals; history only stores sidecar records
 * and asks controllers to {@link AcExHistoryTarget.restore}.
 *
 * @module AcExSessionHistory
 * @packageDocumentation
 */
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
/**
 * One overlay domain's undo/redo stack (markup or measure).
 */
var AcExSnapshotStack = /** @class */ (function () {
    function AcExSnapshotStack() {
        this.undoStack = [];
        this.redoStack = [];
    }
    AcExSnapshotStack.prototype.canUndo = function () {
        return this.undoStack.length > 0;
    };
    AcExSnapshotStack.prototype.canRedo = function () {
        return this.redoStack.length > 0;
    };
    AcExSnapshotStack.prototype.clearRedo = function () {
        this.redoStack.length = 0;
    };
    AcExSnapshotStack.prototype.clear = function () {
        this.undoStack.length = 0;
        this.redoStack.length = 0;
    };
    AcExSnapshotStack.prototype.push = function (entry) {
        this.undoStack.push(entry);
        this.redoStack.length = 0;
    };
    AcExSnapshotStack.prototype.undo = function (restore) {
        var entry = this.undoStack.pop();
        if (!entry)
            return false;
        restore(entry.before);
        this.redoStack.push(entry);
        return true;
    };
    AcExSnapshotStack.prototype.redo = function (restore) {
        var entry = this.redoStack.pop();
        if (!entry)
            return false;
        restore(entry.after);
        this.undoStack.push(entry);
        return true;
    };
    return AcExSnapshotStack;
}());
function snapshotsEqual(a, b) {
    return JSON.stringify(a) === JSON.stringify(b);
}
/**
 * Chronological undo coordinator for HTML markup and measurement edits.
 */
var AcExSessionHistory = /** @class */ (function () {
    function AcExSessionHistory() {
        this.markupTarget = null;
        this.measureTarget = null;
        this.markupStack = new AcExSnapshotStack();
        this.measureStack = new AcExSnapshotStack();
        this.undoKinds = [];
        this.redoKinds = [];
        this.listeners = new Set();
        /** Nesting depth of {@link runMarkup} / {@link runMeasure}. */
        this.depth = 0;
        /** In-progress grip/text capture (before snapshot), by domain. */
        this.markupCapture = null;
        this.measureCapture = null;
    }
    Object.defineProperty(AcExSessionHistory.prototype, "isBusy", {
        /** True while applying undo/redo or nested inside a recorded edit. */
        get: function () {
            return this.depth > 0;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Binds the markup controller snapshot/restore API.
     *
     * @param target - Markup history target.
     */
    AcExSessionHistory.prototype.attachMarkup = function (target) {
        this.markupTarget = target;
    };
    /**
     * Binds the measurement controller snapshot/restore API.
     *
     * @param target - Measurement history target.
     */
    AcExSessionHistory.prototype.attachMeasure = function (target) {
        this.measureTarget = target;
    };
    /**
     * Subscribe to stack changes (shortcut toolbar enable state).
     *
     * @returns Unsubscriber.
     */
    AcExSessionHistory.prototype.subscribe = function (listener) {
        var _this = this;
        this.listeners.add(listener);
        return function () {
            _this.listeners.delete(listener);
        };
    };
    /** Whether any markup/measure session op can be undone. */
    AcExSessionHistory.prototype.canUndo = function () {
        return (this.undoKinds.length > 0 ||
            this.markupStack.canUndo() ||
            this.measureStack.canUndo());
    };
    /** Whether any markup/measure session op can be redone. */
    AcExSessionHistory.prototype.canRedo = function () {
        return (this.redoKinds.length > 0 ||
            this.markupStack.canRedo() ||
            this.measureStack.canRedo());
    };
    /**
     * Run a markup mutation and record one undo step when records change.
     *
     * @param _label - Human-readable undo label (kept for parity / debugging).
     * @param mutate - Mutation against the markup controller.
     */
    AcExSessionHistory.prototype.runMarkup = function (_label, mutate) {
        this.run('markup', _label, mutate);
    };
    /**
     * Run a measurement mutation and record one undo step when records change.
     *
     * @param _label - Human-readable undo label.
     * @param mutate - Mutation against the measurement controller.
     */
    AcExSessionHistory.prototype.runMeasure = function (_label, mutate) {
        this.run('measure', _label, mutate);
    };
    /**
     * Snapshot markup before an in-place grip / text edit.
     * Pair with {@link commitMarkupCapture}.
     */
    AcExSessionHistory.prototype.beginMarkupCapture = function () {
        if (this.depth > 0 || !this.markupTarget || this.markupCapture != null) {
            return;
        }
        this.markupCapture = this.markupTarget.snapshot();
    };
    /**
     * Commit an in-place markup edit started with {@link beginMarkupCapture}.
     *
     * @param label - Human-readable undo label.
     */
    AcExSessionHistory.prototype.commitMarkupCapture = function (label) {
        if (!this.markupTarget || this.markupCapture == null) {
            this.markupCapture = null;
            return;
        }
        var before = this.markupCapture;
        this.markupCapture = null;
        var after = this.markupTarget.snapshot();
        if (snapshotsEqual(before, after))
            return;
        this.markupStack.push({ label: label, before: before, after: after });
        this.undoKinds.push('markup');
        this.redoKinds.length = 0;
        this.measureStack.clearRedo();
        this.notify();
    };
    /** Snapshot measure before an in-place grip edit. */
    AcExSessionHistory.prototype.beginMeasureCapture = function () {
        if (this.depth > 0 || !this.measureTarget || this.measureCapture != null) {
            return;
        }
        this.measureCapture = this.measureTarget.snapshot();
    };
    /**
     * Commit an in-place measure edit started with {@link beginMeasureCapture}.
     *
     * @param label - Human-readable undo label.
     */
    AcExSessionHistory.prototype.commitMeasureCapture = function (label) {
        if (!this.measureTarget || this.measureCapture == null) {
            this.measureCapture = null;
            return;
        }
        var before = this.measureCapture;
        this.measureCapture = null;
        var after = this.measureTarget.snapshot();
        if (snapshotsEqual(before, after))
            return;
        this.measureStack.push({ label: label, before: before, after: after });
        this.undoKinds.push('measure');
        this.redoKinds.length = 0;
        this.markupStack.clearRedo();
        this.notify();
    };
    /**
     * Undo the chronologically last overlay edit.
     *
     * @returns Kind undone, or `false` when nothing changed.
     */
    AcExSessionHistory.prototype.undo = function () {
        var _this = this;
        while (true) {
            var kind = this.undoKinds.pop();
            if (kind === 'markup') {
                if (this.markupTarget &&
                    this.markupStack.undo(function (records) { return _this.markupTarget.restore(records); })) {
                    this.redoKinds.push('markup');
                    this.notify();
                    return 'markup';
                }
                continue;
            }
            if (kind === 'measure') {
                if (this.measureTarget &&
                    this.measureStack.undo(function (records) { return _this.measureTarget.restore(records); })) {
                    this.redoKinds.push('measure');
                    this.notify();
                    return 'measure';
                }
                continue;
            }
            if (this.markupTarget &&
                this.markupStack.undo(function (records) { return _this.markupTarget.restore(records); })) {
                this.redoKinds.push('markup');
                this.notify();
                return 'markup';
            }
            if (this.measureTarget &&
                this.measureStack.undo(function (records) { return _this.measureTarget.restore(records); })) {
                this.redoKinds.push('measure');
                this.notify();
                return 'measure';
            }
            return false;
        }
    };
    /**
     * Redo the chronologically last undone overlay edit.
     *
     * @returns Kind redone, or `false` when nothing changed.
     */
    AcExSessionHistory.prototype.redo = function () {
        var _this = this;
        while (true) {
            var kind = this.redoKinds.pop();
            if (kind === 'markup') {
                if (this.markupTarget &&
                    this.markupStack.redo(function (records) { return _this.markupTarget.restore(records); })) {
                    this.undoKinds.push('markup');
                    this.notify();
                    return 'markup';
                }
                continue;
            }
            if (kind === 'measure') {
                if (this.measureTarget &&
                    this.measureStack.redo(function (records) { return _this.measureTarget.restore(records); })) {
                    this.undoKinds.push('measure');
                    this.notify();
                    return 'measure';
                }
                continue;
            }
            if (this.markupTarget &&
                this.markupStack.redo(function (records) { return _this.markupTarget.restore(records); })) {
                this.undoKinds.push('markup');
                this.notify();
                return 'markup';
            }
            if (this.measureTarget &&
                this.measureStack.redo(function (records) { return _this.measureTarget.restore(records); })) {
                this.undoKinds.push('measure');
                this.notify();
                return 'measure';
            }
            return false;
        }
    };
    /** Drop all history (e.g. when tearing down the viewer). */
    AcExSessionHistory.prototype.clear = function () {
        this.undoKinds.length = 0;
        this.redoKinds.length = 0;
        this.markupStack.clear();
        this.measureStack.clear();
        this.markupCapture = null;
        this.measureCapture = null;
        this.notify();
    };
    AcExSessionHistory.prototype.run = function (kind, label, mutate) {
        var target = kind === 'markup' ? this.markupTarget : this.measureTarget;
        var stack = kind === 'markup' ? this.markupStack : this.measureStack;
        var otherStack = kind === 'markup' ? this.measureStack : this.markupStack;
        if (!target || this.depth > 0) {
            mutate();
            return;
        }
        var before = target.snapshot();
        this.depth++;
        try {
            mutate();
        }
        finally {
            this.depth--;
        }
        var after = target.snapshot();
        if (snapshotsEqual(before, after))
            return;
        stack.push({ label: label, before: before, after: after });
        this.undoKinds.push(kind);
        this.redoKinds.length = 0;
        otherStack.clearRedo();
        this.notify();
    };
    AcExSessionHistory.prototype.notify = function () {
        var e_1, _a;
        try {
            for (var _b = __values(this.listeners), _c = _b.next(); !_c.done; _c = _b.next()) {
                var listener = _c.value;
                listener();
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_1) throw e_1.error; }
        }
    };
    return AcExSessionHistory;
}());
export { AcExSessionHistory };
/** Creates a fresh session history for one offline HTML viewer instance. */
export function createAcExSessionHistory() {
    return new AcExSessionHistory();
}
//# sourceMappingURL=AcExSessionHistory.js.map