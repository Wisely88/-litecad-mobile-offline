/**
 * Review / markup list drawer for the offline HTML viewer.
 *
 * Mirrors cad-simple-ui-plugin's review palette: search, table, details,
 * zoom-to, delete, and clear-all.
 *
 * @module AcExHtmlReviewPanel
 * @packageDocumentation
 */
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var STATUSES = [
    'open',
    'question',
    'answered',
    'closed'
];
var TYPE_I18N = {
    cloud: 'toolbar.markupCloud',
    callout: 'toolbar.markupCallout',
    text: 'toolbar.markupText',
    rect: 'toolbar.markupRect',
    circle: 'toolbar.markupCircle',
    arrow: 'toolbar.markupArrow',
    stamp: 'toolbar.markupStamp'
};
/**
 * Wires the review drawer to the markup controller.
 *
 * @returns Controller, or `null` when the shell markup is missing.
 */
export function setupAcExHtmlReviewPanel(options) {
    var i18n = options.i18n, getMarkup = options.getMarkup, closeOtherDrawers = options.closeOtherDrawers, onPhoneOpen = options.onPhoneOpen;
    var drawer = document.getElementById('mlcad-review-drawer');
    var closeBtn = document.getElementById('mlcad-review-close');
    var sheetCloseBtn = drawer === null || drawer === void 0 ? void 0 : drawer.querySelector('.mlcad-drawer-sheet-close');
    if (!drawer)
        return null;
    var searchInput = drawer.querySelector('.mlcad-review-search');
    var clearBtn = drawer.querySelector('.mlcad-review-clear');
    var tbody = drawer.querySelector('tbody');
    var detail = drawer.querySelector('.mlcad-review-detail');
    var statusSelect = drawer.querySelector('.mlcad-review-status');
    var authorInput = drawer.querySelector('.mlcad-review-author');
    var textInput = drawer.querySelector('.mlcad-review-text');
    var commentInput = drawer.querySelector('.mlcad-review-comment');
    var zoomBtn = drawer.querySelector('.mlcad-review-zoom');
    var deleteBtn = drawer.querySelector('.mlcad-review-delete');
    var closeDetailsBtn = drawer.querySelector('.mlcad-review-detail-close');
    if (!searchInput ||
        !clearBtn ||
        !tbody ||
        !detail ||
        !statusSelect ||
        !authorInput ||
        !textInput ||
        !commentInput ||
        !zoomBtn ||
        !deleteBtn ||
        !closeDetailsBtn) {
        return null;
    }
    var detailsOpen = true;
    var unsubscribe;
    var typeLabel = function (type) {
        var key = TYPE_I18N[type];
        return key ? i18n.t(key) : type;
    };
    var statusLabel = function (status) {
        return i18n.t("review.statusValues.".concat(status));
    };
    var rebuildStatusOptions = function () {
        var e_1, _a;
        var current = statusSelect.value;
        statusSelect.replaceChildren();
        try {
            for (var STATUSES_1 = __values(STATUSES), STATUSES_1_1 = STATUSES_1.next(); !STATUSES_1_1.done; STATUSES_1_1 = STATUSES_1.next()) {
                var status_1 = STATUSES_1_1.value;
                var option = document.createElement('option');
                option.value = status_1;
                option.textContent = statusLabel(status_1);
                statusSelect.appendChild(option);
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (STATUSES_1_1 && !STATUSES_1_1.done && (_a = STATUSES_1.return)) _a.call(STATUSES_1);
            }
            finally { if (e_1) throw e_1.error; }
        }
        if (current)
            statusSelect.value = current;
    };
    var selectedRecord = function () {
        var markup = getMarkup();
        if (!markup)
            return undefined;
        var id = markup.selectedId;
        if (!id)
            return undefined;
        return markup.list().find(function (record) { return record.id === id; });
    };
    var renderTable = function () {
        var e_2, _a;
        var _b;
        var markup = getMarkup();
        var records = (_b = markup === null || markup === void 0 ? void 0 : markup.list()) !== null && _b !== void 0 ? _b : [];
        var selectedId = markup === null || markup === void 0 ? void 0 : markup.selectedId;
        var query = searchInput.value.trim().toLowerCase();
        var rows = query
            ? records.filter(function (record) {
                var _a;
                var hay = "".concat(record.type, " ").concat(record.status, " ").concat(record.author, " ").concat((_a = record.text) !== null && _a !== void 0 ? _a : '', " ").concat(record.comment);
                return hay.toLowerCase().includes(query);
            })
            : records;
        tbody.replaceChildren();
        clearBtn.disabled = records.length === 0;
        if (rows.length === 0) {
            var emptyRow = document.createElement('tr');
            emptyRow.className = 'mlcad-review-empty';
            var cell = document.createElement('td');
            cell.colSpan = 4;
            cell.textContent = i18n.t('review.empty');
            emptyRow.appendChild(cell);
            tbody.appendChild(emptyRow);
            return;
        }
        try {
            for (var rows_1 = __values(rows), rows_1_1 = rows_1.next(); !rows_1_1.done; rows_1_1 = rows_1.next()) {
                var record = rows_1_1.value;
                var tr = document.createElement('tr');
                tr.dataset.markupId = record.id;
                if (record.id === selectedId)
                    tr.classList.add('is-selected');
                var typeCell = document.createElement('td');
                typeCell.textContent = typeLabel(record.type);
                var statusCell = document.createElement('td');
                statusCell.textContent = statusLabel(record.status);
                var authorCell = document.createElement('td');
                authorCell.textContent = record.author;
                authorCell.title = record.author;
                var summaryCell = document.createElement('td');
                var summary = record.text || record.comment || '—';
                summaryCell.textContent = summary;
                summaryCell.title = summary;
                tr.append(typeCell, statusCell, authorCell, summaryCell);
                tbody.appendChild(tr);
            }
        }
        catch (e_2_1) { e_2 = { error: e_2_1 }; }
        finally {
            try {
                if (rows_1_1 && !rows_1_1.done && (_a = rows_1.return)) _a.call(rows_1);
            }
            finally { if (e_2) throw e_2.error; }
        }
    };
    var updateDetail = function () {
        var _a, _b;
        var selected = selectedRecord();
        var show = Boolean(selected && detailsOpen);
        detail.hidden = !show;
        if (!selected || !show)
            return;
        statusSelect.value = selected.status;
        authorInput.value = selected.author;
        if (document.activeElement !== textInput) {
            textInput.value = (_a = selected.text) !== null && _a !== void 0 ? _a : '';
        }
        if (document.activeElement !== commentInput) {
            commentInput.value = (_b = selected.comment) !== null && _b !== void 0 ? _b : '';
        }
    };
    var recordsKey = function (records) {
        return records
            .map(function (record) { var _a; return "".concat(record.id, "\t").concat(record.type, "\t").concat(record.status, "\t").concat(record.author, "\t").concat((_a = record.text) !== null && _a !== void 0 ? _a : '', "\t").concat(record.comment); })
            .join('\n');
    };
    var tableContentKey = '';
    var syncRowSelection = function () {
        var _a;
        var selectedId = (_a = getMarkup()) === null || _a === void 0 ? void 0 : _a.selectedId;
        tbody.querySelectorAll('tr[data-markup-id]').forEach(function (row) {
            row.classList.toggle('is-selected', row.getAttribute('data-markup-id') === selectedId);
        });
    };
    var refresh = function () {
        var _a, _b;
        var records = (_b = (_a = getMarkup()) === null || _a === void 0 ? void 0 : _a.list()) !== null && _b !== void 0 ? _b : [];
        var key = recordsKey(records);
        if (key === tableContentKey &&
            tbody.querySelector('tr[data-markup-id], .mlcad-review-empty')) {
            syncRowSelection();
        }
        else {
            tableContentKey = key;
            renderTable();
        }
        updateDetail();
    };
    var rowMarkupId = function (event) {
        var target = event.target;
        if (!(target instanceof Element))
            return undefined;
        var row = target.closest('tr[data-markup-id]');
        if (!row || !tbody.contains(row))
            return undefined;
        return row instanceof HTMLElement ? row.dataset.markupId : undefined;
    };
    tbody.addEventListener('click', function (event) {
        var _a;
        var id = rowMarkupId(event);
        if (!id)
            return;
        detailsOpen = true;
        (_a = getMarkup()) === null || _a === void 0 ? void 0 : _a.selectFromPanel(id);
        updateDetail();
    });
    tbody.addEventListener('dblclick', function (event) {
        var _a;
        var id = rowMarkupId(event);
        if (!id)
            return;
        detailsOpen = true;
        (_a = getMarkup()) === null || _a === void 0 ? void 0 : _a.focus(id);
        updateDetail();
    });
    var setOpen = function (open) {
        if (open) {
            closeOtherDrawers();
            onPhoneOpen === null || onPhoneOpen === void 0 ? void 0 : onPhoneOpen(drawer);
        }
        drawer.hidden = !open;
        document
            .querySelectorAll('[data-action="markup-panel"]')
            .forEach(function (btn) {
            btn.classList.toggle('active', open);
            btn.setAttribute('aria-pressed', String(open));
        });
        if (open) {
            var markup = getMarkup();
            unsubscribe === null || unsubscribe === void 0 ? void 0 : unsubscribe();
            unsubscribe = markup === null || markup === void 0 ? void 0 : markup.subscribe(refresh);
            refresh();
        }
        else {
            unsubscribe === null || unsubscribe === void 0 ? void 0 : unsubscribe();
            unsubscribe = undefined;
        }
    };
    searchInput.addEventListener('input', function () { return renderTable(); });
    clearBtn.addEventListener('click', function () { var _a; return (_a = getMarkup()) === null || _a === void 0 ? void 0 : _a.clearAll(); });
    closeBtn === null || closeBtn === void 0 ? void 0 : closeBtn.addEventListener('click', function () { return setOpen(false); });
    sheetCloseBtn === null || sheetCloseBtn === void 0 ? void 0 : sheetCloseBtn.addEventListener('click', function () { return setOpen(false); });
    statusSelect.addEventListener('change', function () {
        var _a;
        var selected = selectedRecord();
        if (!selected)
            return;
        (_a = getMarkup()) === null || _a === void 0 ? void 0 : _a.updateMeta(selected.id, {
            status: statusSelect.value
        });
    });
    textInput.addEventListener('blur', function () {
        var _a, _b;
        var selected = selectedRecord();
        if (!selected)
            return;
        var next = textInput.value;
        if (next === ((_a = selected.text) !== null && _a !== void 0 ? _a : ''))
            return;
        (_b = getMarkup()) === null || _b === void 0 ? void 0 : _b.updateMeta(selected.id, { text: next });
    });
    commentInput.addEventListener('blur', function () {
        var _a;
        var selected = selectedRecord();
        if (!selected)
            return;
        var next = commentInput.value;
        if (next === selected.comment)
            return;
        (_a = getMarkup()) === null || _a === void 0 ? void 0 : _a.updateMeta(selected.id, { comment: next });
    });
    zoomBtn.addEventListener('click', function () {
        var _a;
        var selected = selectedRecord();
        if (selected)
            (_a = getMarkup()) === null || _a === void 0 ? void 0 : _a.focus(selected.id);
    });
    deleteBtn.addEventListener('click', function () {
        var _a;
        var selected = selectedRecord();
        if (selected)
            (_a = getMarkup()) === null || _a === void 0 ? void 0 : _a.removeMarkup(selected.id);
    });
    closeDetailsBtn.addEventListener('click', function () {
        detailsOpen = false;
        updateDetail();
    });
    var refreshLabels = function () {
        searchInput.placeholder = i18n.t('review.searchPlaceholder');
        clearBtn.textContent = i18n.t('review.clear');
        var typeHeader = drawer.querySelector('[data-review-col="type"]');
        var statusHeader = drawer.querySelector('[data-review-col="status"]');
        var authorHeader = drawer.querySelector('[data-review-col="author"]');
        var summaryHeader = drawer.querySelector('[data-review-col="summary"]');
        if (typeHeader)
            typeHeader.textContent = i18n.t('review.type');
        if (statusHeader)
            statusHeader.textContent = i18n.t('review.status');
        if (authorHeader)
            authorHeader.textContent = i18n.t('review.author');
        if (summaryHeader)
            summaryHeader.textContent = i18n.t('review.summary');
        var detailTitle = drawer.querySelector('.mlcad-review-detail-title');
        if (detailTitle)
            detailTitle.textContent = i18n.t('review.details');
        var closeDetailsLabel = i18n.t('review.closeDetails');
        closeDetailsBtn.title = closeDetailsLabel;
        closeDetailsBtn.setAttribute('aria-label', closeDetailsLabel);
        var statusField = drawer.querySelector('[data-review-field="status"]');
        var authorField = drawer.querySelector('[data-review-field="author"]');
        var labelField = drawer.querySelector('[data-review-field="label"]');
        var commentField = drawer.querySelector('[data-review-field="comment"]');
        if (statusField)
            statusField.textContent = i18n.t('review.status');
        if (authorField)
            authorField.textContent = i18n.t('review.author');
        if (labelField)
            labelField.textContent = i18n.t('review.label');
        if (commentField)
            commentField.textContent = i18n.t('review.comment');
        zoomBtn.textContent = i18n.t('review.zoomTo');
        deleteBtn.textContent = i18n.t('review.delete');
        rebuildStatusOptions();
        renderTable();
        updateDetail();
    };
    rebuildStatusOptions();
    refreshLabels();
    return {
        refreshLabels: refreshLabels,
        close: function () { return setOpen(false); },
        setOpen: setOpen
    };
}
//# sourceMappingURL=AcExHtmlReviewPanel.js.map