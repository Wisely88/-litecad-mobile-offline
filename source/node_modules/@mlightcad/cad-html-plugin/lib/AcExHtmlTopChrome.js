/**
 * Shared top-of-canvas chrome layout for the offline HTML viewer.
 *
 * Message bar + expiry badge share one row; shortcut toolbar and snap loupe
 * sit below that row (or at the canvas top inset when the row is empty).
 *
 * @module AcExHtmlTopChrome
 * @packageDocumentation
 */
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
/** Gap between the top chrome row and controls stacked beneath it. */
export var ACEX_HTML_TOP_CHROME_GAP_PX = 8;
/** Default top inset when the message / expiry row is hidden. */
export var ACEX_HTML_TOP_CHROME_NEAR_TOP_PX = 12;
/**
 * Whether a top-chrome child is currently contributing to the row height.
 */
export function acexHtmlTopChromeChildVisible(el) {
    var _a;
    if (el.hidden) {
        return false;
    }
    if (el.id === 'mlcad-status-bar' && !((_a = el.textContent) === null || _a === void 0 ? void 0 : _a.trim())) {
        return false;
    }
    var style = getComputedStyle(el);
    return style.display !== 'none' && style.visibility !== 'hidden';
}
/**
 * True when the top chrome row has at least one visible child.
 */
export function acexHtmlTopChromeHasVisibleContent(chrome) {
    var e_1, _a;
    if (!chrome) {
        return false;
    }
    try {
        for (var _b = __values(Array.from(chrome.children)), _c = _b.next(); !_c.done; _c = _b.next()) {
            var child = _c.value;
            if (child instanceof HTMLElement && acexHtmlTopChromeChildVisible(child)) {
                return true;
            }
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_1) throw e_1.error; }
    }
    return false;
}
/**
 * Resolves `#mlcad-top-chrome` from the document or a canvas host.
 */
export function getAcExHtmlTopChrome(host) {
    if (host) {
        var scoped = host.querySelector('#mlcad-top-chrome');
        if (scoped instanceof HTMLElement) {
            return scoped;
        }
    }
    var global = document.getElementById('mlcad-top-chrome');
    return global instanceof HTMLElement ? global : null;
}
/**
 * CSS `top` for a control positioned under the top chrome row, relative to
 * `positionedAncestor` (typically `#mlcad-root` or `#mlcad-canvas-host`).
 *
 * When the row is empty / hidden, returns `nearTopPx`.
 */
export function acexHtmlTopChromeBottomOffset(positionedAncestor, options) {
    var _a, _b, _c;
    var gapPx = (_a = options === null || options === void 0 ? void 0 : options.gapPx) !== null && _a !== void 0 ? _a : ACEX_HTML_TOP_CHROME_GAP_PX;
    var nearTopPx = (_b = options === null || options === void 0 ? void 0 : options.nearTopPx) !== null && _b !== void 0 ? _b : ACEX_HTML_TOP_CHROME_NEAR_TOP_PX;
    var chrome = (_c = options === null || options === void 0 ? void 0 : options.chrome) !== null && _c !== void 0 ? _c : getAcExHtmlTopChrome(positionedAncestor);
    if (!acexHtmlTopChromeHasVisibleContent(chrome)) {
        return nearTopPx;
    }
    var ancestorTop = positionedAncestor.getBoundingClientRect().top;
    return Math.max(nearTopPx, chrome.getBoundingClientRect().bottom - ancestorTop + gapPx);
}
//# sourceMappingURL=AcExHtmlTopChrome.js.map