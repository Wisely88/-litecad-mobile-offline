const l = "HtmlPlugin", o = ["-chtml", "chtml"];
let t = {};
function s(r, e) {
  e && (t = { ...t, ...e });
  const n = { ...t };
  r.registerLazyPlugin({
    name: l,
    triggers: [...o],
    loader: async () => {
      const { createHtmlPlugin: i } = await import("@mlightcad/cad-html-plugin");
      return i(n);
    }
  });
}
export {
  l as HTML_PLUGIN_NAME,
  o as HTML_PLUGIN_TRIGGERS,
  s as registerLazyHtmlPlugin
};
