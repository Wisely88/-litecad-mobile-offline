var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcDbObject } from '../base/AcDbObject';
/**
 * Nongraphical SORTENTSTABLE object (draw-order soft-pointers).
 */
var AcDbSortentsTable = /** @class */ (function (_super) {
    __extends(AcDbSortentsTable, _super);
    function AcDbSortentsTable() {
        var _this = _super.apply(this, __spreadArray([], __read(arguments), false)) || this;
        _this._entityIds = [];
        return _this;
    }
    Object.defineProperty(AcDbSortentsTable.prototype, "entityIds", {
        get: function () {
            return this._entityIds;
        },
        set: function (value) {
            this._entityIds = __spreadArray([], __read(value), false);
        },
        enumerable: false,
        configurable: true
    });
    AcDbSortentsTable.prototype.addEntityId = function (id) {
        this._entityIds.push(id);
    };
    AcDbSortentsTable.prototype.dxfOutFields = function (filer) {
        var e_1, _a;
        _super.prototype.dxfOutFields.call(this, filer);
        filer.writeSubclassMarker('AcDbSortentsTable');
        try {
            for (var _b = __values(this._entityIds), _c = _b.next(); !_c.done; _c = _b.next()) {
                var id = _c.value;
                filer.writeObjectId(331, id);
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_1) throw e_1.error; }
        }
        return this;
    };
    AcDbSortentsTable.prototype.dxfInFields = function (filer) {
        _super.prototype.dxfInFields.call(this, filer);
        filer.atSubclassData('AcDbSortentsTable');
        this._entityIds = [];
        while (!filer.atEndOfObject && !filer.atEof && !filer.atExtendedData) {
            var item = filer.readItem();
            if (!item)
                break;
            var code = Number(item.code);
            if (code === 100) {
                filer.pushBackItem(item);
                break;
            }
            switch (code) {
                case 331:
                    this._entityIds.push(String(item.value));
                    break;
                default:
                    // Tolerate unknown SORTENTSTABLE fields (e.g. paired handles).
                    break;
            }
        }
        return this;
    };
    return AcDbSortentsTable;
}(AcDbObject));
export { AcDbSortentsTable };
//# sourceMappingURL=AcDbSortentsTable.js.map