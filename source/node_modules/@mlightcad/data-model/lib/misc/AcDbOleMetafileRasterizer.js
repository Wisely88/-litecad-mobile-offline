/**
 * Rasterizes Windows metafile blobs extracted from OLE frames (WMF / EMF)
 * into browser-decodable PNG images via `emf-converter`.
 *
 * AutoCAD Excel OLE previews are typically stored as CF_ENHMETAFILE / WMF
 * presentation streams. Browsers cannot decode those formats natively, so
 * viewers must convert them to PNG (or similar) before texturing.
 */
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
import { convertEmfToDataUrl, convertWmfToDataUrl } from 'emf-converter';
import { ACDB_OLE_METAFILE_EMF_MIME, ACDB_OLE_METAFILE_WMF_MIME, acdbIsOleMetafileMimeType, acdbLooksLikeEmf, acdbLooksLikeWmf } from './AcDbOleMetafileDetect';
/**
 * Default Windows face-name → CSS font-family map for OLE metafile text.
 *
 * Excel table previews frequently embed ExtTextOut runs that name `宋体` /
 * `SimSun`. Without a remapping, browsers that lack that exact face still
 * measure CJK via the default sans fallback in some paths but may fail to
 * paint glyphs inside clipped EMF cells — keep a conservative CJK stack.
 */
var ACDB_OLE_DEFAULT_FONT_FAMILY_MAP = {
    宋体: '"SimSun", "NSimSun", "Microsoft YaHei", "Noto Sans SC", "Source Han Sans SC", sans-serif',
    simsun: '"SimSun", "NSimSun", "Microsoft YaHei", "Noto Sans SC", sans-serif',
    nsimsun: '"NSimSun", "SimSun", "Microsoft YaHei", sans-serif',
    黑体: '"SimHei", "Microsoft YaHei", "Noto Sans SC", sans-serif',
    simhei: '"SimHei", "Microsoft YaHei", sans-serif',
    微软雅黑: '"Microsoft YaHei", "Noto Sans SC", sans-serif',
    'microsoft yahei': '"Microsoft YaHei", "Noto Sans SC", sans-serif'
};
/**
 * Converts a WMF/EMF {@link Blob} (or raw bytes) to an `image/png` blob.
 *
 * @returns PNG blob, or `undefined` when conversion fails / canvas is unavailable.
 */
export function acdbRasterizeOleMetafile(input_1) {
    return __awaiter(this, arguments, void 0, function (input, options) {
        var bytes, _a, _b, mime, convertOptions, buffer, tryEmfFirst, dataUrl;
        var _c;
        if (options === void 0) { options = {}; }
        return __generator(this, function (_d) {
            switch (_d.label) {
                case 0:
                    if (!(input instanceof Uint8Array)) return [3 /*break*/, 1];
                    _a = input;
                    return [3 /*break*/, 3];
                case 1:
                    _b = Uint8Array.bind;
                    return [4 /*yield*/, input.arrayBuffer()];
                case 2:
                    _a = new (_b.apply(Uint8Array, [void 0, _d.sent()]))();
                    _d.label = 3;
                case 3:
                    bytes = _a;
                    if (!bytes.length) {
                        return [2 /*return*/, undefined];
                    }
                    mime = input instanceof Blob && acdbIsOleMetafileMimeType(input.type)
                        ? input.type
                        : undefined;
                    convertOptions = {
                        maxWidth: options.maxWidth,
                        maxHeight: options.maxHeight,
                        dpiScale: (_c = options.dpiScale) !== null && _c !== void 0 ? _c : 1,
                        maxCanvasDimension: options.maxCanvasDimension,
                        // Excel OLE previews commonly use 宋体/SimSun. Map those face names to a
                        // CSS stack so Chromium can fall back when the exact Windows face is
                        // missing (otherwise ExtTextOut glyphs silently vanish).
                        fontFamilyMap: __assign(__assign({}, ACDB_OLE_DEFAULT_FONT_FAMILY_MAP), options.fontFamilyMap)
                    };
                    buffer = bytes.buffer.slice(bytes.byteOffset, bytes.byteOffset + bytes.byteLength);
                    tryEmfFirst = mime === ACDB_OLE_METAFILE_EMF_MIME ||
                        (mime == null && acdbLooksLikeEmf(bytes) && !acdbLooksLikeWmf(bytes));
                    dataUrl = null;
                    if (!tryEmfFirst) return [3 /*break*/, 7];
                    return [4 /*yield*/, convertEmfToDataUrl(buffer, convertOptions)];
                case 4:
                    dataUrl = _d.sent();
                    if (!(!dataUrl && acdbLooksLikeWmf(bytes))) return [3 /*break*/, 6];
                    return [4 /*yield*/, convertWmfToDataUrl(buffer, convertOptions)];
                case 5:
                    dataUrl = _d.sent();
                    _d.label = 6;
                case 6: return [3 /*break*/, 11];
                case 7:
                    if (!(mime === ACDB_OLE_METAFILE_WMF_MIME || acdbLooksLikeWmf(bytes))) return [3 /*break*/, 9];
                    return [4 /*yield*/, convertWmfToDataUrl(buffer, convertOptions)];
                case 8:
                    dataUrl = _d.sent();
                    _d.label = 9;
                case 9:
                    if (!(!dataUrl && acdbLooksLikeEmf(bytes))) return [3 /*break*/, 11];
                    return [4 /*yield*/, convertEmfToDataUrl(buffer, convertOptions)];
                case 10:
                    dataUrl = _d.sent();
                    _d.label = 11;
                case 11:
                    if (!dataUrl) {
                        return [2 /*return*/, undefined];
                    }
                    // Metafile canvases are often transparent; CAD model space is usually dark.
                    // Composite onto white so Excel table paper stays readable.
                    return [2 /*return*/, compositePngOntoWhite(dataUrl)];
            }
        });
    });
}
/**
 * True when `blob` looks like an OLE metafile that needs canvas rasterization
 * before it can be used as a WebGL texture.
 */
export function acdbOleBlobNeedsMetafileRasterization(blob) {
    return acdbIsOleMetafileMimeType(blob.type);
}
function dataUrlToPngBlob(dataUrl) {
    var comma = dataUrl.indexOf(',');
    if (!dataUrl.startsWith('data:') || comma < 0) {
        return undefined;
    }
    var meta = dataUrl.slice(5, comma); // after "data:"
    var data = dataUrl.slice(comma + 1);
    var parts = meta.split(';');
    var mime = parts[0] || 'image/png';
    var isBase64 = parts.includes('base64');
    if (isBase64) {
        var binary = atob(data);
        var bytes = new Uint8Array(binary.length);
        for (var i = 0; i < binary.length; i++) {
            bytes[i] = binary.charCodeAt(i);
        }
        return new Blob([bytes], { type: mime });
    }
    var decoded = decodeURIComponent(data);
    return new Blob([decoded], { type: mime });
}
/**
 * Draws a PNG data-URL onto an opaque white canvas and re-exports PNG.
 * Falls back to the original data-URL blob when no canvas API is available.
 */
function compositePngOntoWhite(dataUrl) {
    return __awaiter(this, void 0, void 0, function () {
        var canvasApi, image, width, height, canvas, ctx, htmlCanvas_1, _a;
        return __generator(this, function (_b) {
            switch (_b.label) {
                case 0:
                    canvasApi = getCanvasFactory();
                    if (!canvasApi) {
                        return [2 /*return*/, dataUrlToPngBlob(dataUrl)];
                    }
                    _b.label = 1;
                case 1:
                    _b.trys.push([1, 7, , 8]);
                    return [4 /*yield*/, canvasApi.decodeImage(dataUrl)];
                case 2:
                    image = _b.sent();
                    width = Math.max(1, image.width);
                    height = Math.max(1, image.height);
                    canvas = canvasApi.createCanvas(width, height);
                    ctx = canvas.getContext('2d');
                    if (!ctx) {
                        return [2 /*return*/, dataUrlToPngBlob(dataUrl)];
                    }
                    ctx.fillStyle = '#ffffff';
                    ctx.fillRect(0, 0, width, height);
                    ctx.drawImage(image, 0, 0);
                    if (!(canvas instanceof OffscreenCanvas)) return [3 /*break*/, 4];
                    return [4 /*yield*/, canvas.convertToBlob({ type: 'image/png' })];
                case 3: return [2 /*return*/, _b.sent()];
                case 4:
                    htmlCanvas_1 = canvas;
                    if (!(typeof htmlCanvas_1.toBlob === 'function')) return [3 /*break*/, 6];
                    return [4 /*yield*/, new Promise(function (resolve) {
                            htmlCanvas_1.toBlob(function (blob) { return resolve(blob || undefined); }, 'image/png');
                        })];
                case 5: return [2 /*return*/, _b.sent()];
                case 6:
                    if (typeof htmlCanvas_1.toDataURL === 'function') {
                        return [2 /*return*/, dataUrlToPngBlob(htmlCanvas_1.toDataURL('image/png'))];
                    }
                    return [3 /*break*/, 8];
                case 7:
                    _a = _b.sent();
                    return [3 /*break*/, 8];
                case 8: return [2 /*return*/, dataUrlToPngBlob(dataUrl)];
            }
        });
    });
}
function getCanvasFactory() {
    var _this = this;
    if (typeof OffscreenCanvas !== 'undefined') {
        return {
            createCanvas: function (w, h) { return new OffscreenCanvas(w, h); },
            decodeImage: function (dataUrl) { return __awaiter(_this, void 0, void 0, function () {
                var response, blob;
                return __generator(this, function (_a) {
                    switch (_a.label) {
                        case 0: return [4 /*yield*/, fetch(dataUrl)];
                        case 1:
                            response = _a.sent();
                            return [4 /*yield*/, response.blob()];
                        case 2:
                            blob = _a.sent();
                            return [2 /*return*/, createImageBitmap(blob)];
                    }
                });
            }); }
        };
    }
    if (typeof document !== 'undefined' && document.createElement) {
        return {
            createCanvas: function (w, h) {
                var canvas = document.createElement('canvas');
                canvas.width = w;
                canvas.height = h;
                return canvas;
            },
            decodeImage: function (dataUrl) {
                return new Promise(function (resolve, reject) {
                    var image = new Image();
                    image.onload = function () { return resolve(image); };
                    image.onerror = function () { return reject(new Error('Failed to decode PNG')); };
                    image.src = dataUrl;
                });
            }
        };
    }
    return undefined;
}
//# sourceMappingURL=AcDbOleMetafileRasterizer.js.map