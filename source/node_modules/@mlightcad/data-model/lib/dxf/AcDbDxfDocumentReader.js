var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
import { ACCM_DEFAULT_UI_YIELD_BUDGET_MS, AcCmUiYieldGate, accmYieldToUi } from '@mlightcad/common';
import { AcGePoint3d } from '@mlightcad/geometry-engine';
import { AcDbBlockTableRecord, AcDbBlockTableRecordFlag } from '../database/AcDbBlockTableRecord';
import { AcDbDimStyleTableRecord } from '../database/AcDbDimStyleTableRecord';
import { AcDbLayerTableRecord } from '../database/AcDbLayerTableRecord';
import { AcDbLinetypeTableRecord } from '../database/AcDbLinetypeTableRecord';
import { AcDbRegAppTableRecord } from '../database/AcDbRegAppTableRecord';
import { AcDbTextStyleTableRecord } from '../database/AcDbTextStyleTableRecord';
import { AcDbUcsTableRecord } from '../database/AcDbUcsTableRecord';
import { AcDbViewportTableRecord } from '../database/AcDbViewportTableRecord';
import { AcDbViewTableRecord } from '../database/AcDbViewTableRecord';
import { AcDb3dSolid } from '../entity/AcDb3dSolid';
import { AcDbAttribute } from '../entity/AcDbAttribute';
import { AcDbBlockReference } from '../entity/AcDbBlockReference';
import { acdbCombineDxfBinaryChunks } from '../misc/proxyGraphic';
import { acdbDxfInAcdsData, acdbNormalizeDxfHandle } from './AcDbDxfAcdsDataReader';
import { acdbDxfInEntity } from './AcDbDxfEntityFactory';
import { acdbDxfInHeader } from './AcDbDxfHeaderReader';
import { AcDbDxfObjectsReader } from './AcDbDxfObjectsReader';
/**
 * Single-pass streaming DXF document reader.
 *
 * Walks HEADER → CLASSES → TABLES → BLOCKS → ENTITIES → OBJECTS in file
 * order and writes directly into {@link AcDbDatabase}. Does not buffer the
 * whole file as tag groups.
 */
var AcDbDxfDocumentReader = /** @class */ (function () {
    function AcDbDxfDocumentReader(_db, _options) {
        if (_options === void 0) { _options = {}; }
        var _a;
        this._db = _db;
        this._options = _options;
        this._unknownEntityCount = 0;
        this._unknownObjectCount = 0;
        /** ATTRIB entities waiting for their owning INSERT (keyed by INSERT objectId). */
        this._attributeMap = new Map();
        this._yieldGate = new AcCmUiYieldGate((_a = this._options.yieldBudgetMs) !== null && _a !== void 0 ? _a : ACCM_DEFAULT_UI_YIELD_BUDGET_MS);
    }
    Object.defineProperty(AcDbDxfDocumentReader.prototype, "unknownEntityCount", {
        get: function () {
            return this._unknownEntityCount;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDxfDocumentReader.prototype, "unknownObjectCount", {
        get: function () {
            return this._unknownObjectCount;
        },
        enumerable: false,
        configurable: true
    });
    AcDbDxfDocumentReader.prototype.read = function (filer) {
        return __awaiter(this, void 0, void 0, function () {
            var item, name_1, sectionName;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        this._unknownEntityCount = 0;
                        this._unknownObjectCount = 0;
                        this._attributeMap.clear();
                        filer.database = this._db;
                        _a.label = 1;
                    case 1:
                        if (!!filer.atEof) return [3 /*break*/, 4];
                        item = filer.readItem();
                        if (!item)
                            return [3 /*break*/, 4];
                        if (Number(item.code) !== 0)
                            return [3 /*break*/, 1];
                        name_1 = String(item.value).toUpperCase();
                        if (name_1 === 'EOF')
                            return [3 /*break*/, 4];
                        if (!(name_1 === 'SECTION')) return [3 /*break*/, 3];
                        sectionName = this.readSectionName(filer);
                        return [4 /*yield*/, this.readSection(filer, sectionName)];
                    case 2:
                        _a.sent();
                        return [3 /*break*/, 1];
                    case 3: return [3 /*break*/, 1];
                    case 4:
                        this.flushRemainingAttributes();
                        return [2 /*return*/, {
                                unknownEntityCount: this._unknownEntityCount,
                                unknownObjectCount: this._unknownObjectCount
                            }];
                }
            });
        });
    };
    AcDbDxfDocumentReader.prototype.readSectionName = function (filer) {
        var item = filer.readItem();
        if (item && Number(item.code) === 2) {
            return String(item.value).toUpperCase();
        }
        if (item)
            filer.pushBackItem(item);
        return '';
    };
    AcDbDxfDocumentReader.prototype.readSection = function (filer, section) {
        return __awaiter(this, void 0, void 0, function () {
            var _a, objectsReader;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        _a = section;
                        switch (_a) {
                            case 'HEADER': return [3 /*break*/, 1];
                            case 'CLASSES': return [3 /*break*/, 3];
                            case 'TABLES': return [3 /*break*/, 5];
                            case 'BLOCKS': return [3 /*break*/, 7];
                            case 'ENTITIES': return [3 /*break*/, 10];
                            case 'OBJECTS': return [3 /*break*/, 13];
                            case 'ACDSDATA': return [3 /*break*/, 15];
                            case 'THUMBNAILIMAGE': return [3 /*break*/, 17];
                        }
                        return [3 /*break*/, 19];
                    case 1:
                        acdbDxfInHeader(filer, this._db);
                        return [4 /*yield*/, this.reportParseProgress(filer)];
                    case 2:
                        _b.sent();
                        return [3 /*break*/, 21];
                    case 3:
                        this.readClassesSection(filer);
                        return [4 /*yield*/, this.reportParseProgress(filer)];
                    case 4:
                        _b.sent();
                        return [3 /*break*/, 21];
                    case 5:
                        this.readTablesSection(filer);
                        return [4 /*yield*/, this.reportParseProgress(filer)];
                    case 6:
                        _b.sent();
                        return [3 /*break*/, 21];
                    case 7: return [4 /*yield*/, this.readBlocksSection(filer)];
                    case 8:
                        _b.sent();
                        return [4 /*yield*/, this.reportParseProgress(filer)];
                    case 9:
                        _b.sent();
                        return [3 /*break*/, 21];
                    case 10: return [4 /*yield*/, this.readEntitiesSection(filer)];
                    case 11:
                        _b.sent();
                        return [4 /*yield*/, this.reportParseProgress(filer)];
                    case 12:
                        _b.sent();
                        return [3 /*break*/, 21];
                    case 13:
                        {
                            objectsReader = new AcDbDxfObjectsReader(this._db);
                            objectsReader.read(filer);
                            this._unknownObjectCount += objectsReader.unknownObjectCount;
                        }
                        return [4 /*yield*/, this.reportParseProgress(filer)];
                    case 14:
                        _b.sent();
                        return [3 /*break*/, 21];
                    case 15:
                        this.applyAcdsDataToSolids(acdbDxfInAcdsData(filer));
                        return [4 /*yield*/, this.reportParseProgress(filer)];
                    case 16:
                        _b.sent();
                        return [3 /*break*/, 21];
                    case 17:
                        this.readThumbnailImageSection(filer);
                        return [4 /*yield*/, this.reportParseProgress(filer)];
                    case 18:
                        _b.sent();
                        return [3 /*break*/, 21];
                    case 19:
                        this.skipUntilEndSec(filer);
                        return [4 /*yield*/, this.reportParseProgress(filer)];
                    case 20:
                        _b.sent();
                        return [3 /*break*/, 21];
                    case 21: return [2 /*return*/];
                }
            });
        });
    };
    /**
     * Attaches ACDSDATA `ASM_Data` SAB payloads to matching {@link AcDb3dSolid}
     * entities (keyed by entity handle / group 5).
     *
     * Modern DXF files often store 3DSOLID geometry only in ACDSDATA rather than
     * inline encrypted SAT groups 1/3 — without this step solids render empty.
     */
    AcDbDxfDocumentReader.prototype.applyAcdsDataToSolids = function (section) {
        var e_1, _a, e_2, _b, e_3, _c;
        var ownerHandles = Object.keys(section.byOwnerHandle);
        if (ownerHandles.length === 0)
            return;
        // Build a handle → solid map once (handles may differ in case from DXF).
        var solidsByHandle = new Map();
        try {
            for (var _d = __values(this._db.tables.blockTable.newIterator()), _e = _d.next(); !_e.done; _e = _d.next()) {
                var btr = _e.value;
                try {
                    for (var _f = (e_2 = void 0, __values(btr.newIterator())), _g = _f.next(); !_g.done; _g = _f.next()) {
                        var entity = _g.value;
                        if (entity instanceof AcDb3dSolid) {
                            solidsByHandle.set(acdbNormalizeDxfHandle(entity.objectId), entity);
                        }
                    }
                }
                catch (e_2_1) { e_2 = { error: e_2_1 }; }
                finally {
                    try {
                        if (_g && !_g.done && (_b = _f.return)) _b.call(_f);
                    }
                    finally { if (e_2) throw e_2.error; }
                }
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (_e && !_e.done && (_a = _d.return)) _a.call(_d);
            }
            finally { if (e_1) throw e_1.error; }
        }
        try {
            for (var ownerHandles_1 = __values(ownerHandles), ownerHandles_1_1 = ownerHandles_1.next(); !ownerHandles_1_1.done; ownerHandles_1_1 = ownerHandles_1.next()) {
                var ownerHandle = ownerHandles_1_1.value;
                var sabBytes = section.byOwnerHandle[ownerHandle];
                if (!sabBytes)
                    continue;
                var solid = solidsByHandle.get(acdbNormalizeDxfHandle(ownerHandle));
                if (solid) {
                    solid.setSabBytes(sabBytes);
                }
            }
        }
        catch (e_3_1) { e_3 = { error: e_3_1 }; }
        finally {
            try {
                if (ownerHandles_1_1 && !ownerHandles_1_1.done && (_c = ownerHandles_1.return)) _c.call(ownerHandles_1);
            }
            finally { if (e_3) throw e_3.error; }
        }
    };
    AcDbDxfDocumentReader.prototype.readThumbnailImageSection = function (filer) {
        var expectedLength = 0;
        var dataChunks = [];
        while (!filer.atEof) {
            var item = filer.readItem();
            if (!item)
                break;
            if (Number(item.code) === 0) {
                var name_2 = String(item.value).toUpperCase();
                if (name_2 === 'ENDSEC')
                    break;
                // Unexpected — push back and stop
                filer.pushBackItem(item);
                break;
            }
            if (Number(item.code) === 90) {
                expectedLength = Number(item.value) || 0;
            }
            else if (Number(item.code) === 310) {
                // ASCII readers may leave hex as string; typed binary pairs yield Uint8Array.
                if (item.value instanceof Uint8Array) {
                    dataChunks.push(item.value);
                }
                else {
                    dataChunks.push(String(item.value));
                }
            }
        }
        if (dataChunks.length === 0)
            return;
        var bytes = acdbCombineDxfBinaryChunks(dataChunks);
        if (!bytes || bytes.length === 0)
            return;
        this._db.thumbnailImage =
            expectedLength > 0 && bytes.length > expectedLength
                ? bytes.subarray(0, expectedLength)
                : bytes;
    };
    AcDbDxfDocumentReader.prototype.readClassesSection = function (filer) {
        var classes = [];
        while (!filer.atEof) {
            var item = filer.peekItem();
            if (!item)
                break;
            if (Number(item.code) !== 0) {
                filer.readItem();
                continue;
            }
            var name_3 = String(item.value).toUpperCase();
            if (name_3 === 'ENDSEC') {
                filer.readItem();
                break;
            }
            if (name_3 === 'CLASS') {
                filer.readItem();
                var dxfClass = this.readClassRecord(filer);
                if (dxfClass.name || dxfClass.cppClassName) {
                    classes.push(dxfClass);
                }
                continue;
            }
            filer.readItem();
            filer.skipToEndOfObject();
        }
        if (classes.length > 0) {
            this._db.classes = classes;
        }
    };
    AcDbDxfDocumentReader.prototype.readClassRecord = function (filer) {
        var result = {
            name: '',
            cppClassName: '',
            appName: '',
            proxyFlag: 0,
            instanceCount: 0,
            wasProxy: false,
            isEntity: false
        };
        while (!filer.atEndOfObject && !filer.atEof) {
            var item = filer.readItem();
            if (!item)
                break;
            var code = Number(item.code);
            switch (code) {
                case 1:
                    result.name = String(item.value);
                    break;
                case 2:
                    result.cppClassName = String(item.value);
                    break;
                case 3:
                    result.appName = String(item.value);
                    break;
                case 90:
                    result.proxyFlag = Number(item.value);
                    break;
                case 91:
                    result.instanceCount = Number(item.value);
                    break;
                case 280:
                    result.wasProxy = Number(item.value) !== 0;
                    break;
                case 281:
                    result.isEntity = Number(item.value) !== 0;
                    break;
                default:
                    // Tolerate unknown CLASS fields without ending early on soft codes.
                    break;
            }
        }
        return result;
    };
    AcDbDxfDocumentReader.prototype.readTablesSection = function (filer) {
        while (!filer.atEof) {
            var item = filer.peekItem();
            if (!item)
                break;
            if (Number(item.code) !== 0) {
                filer.readItem();
                continue;
            }
            var name_4 = String(item.value).toUpperCase();
            if (name_4 === 'ENDSEC') {
                filer.readItem();
                break;
            }
            if (name_4 === 'TABLE') {
                filer.readItem();
                var tableName = this.readTableName(filer);
                this.readTable(filer, tableName);
                continue;
            }
            // Unexpected — skip object
            filer.readItem();
            filer.skipToEndOfObject();
        }
        this._db.ensureTextStyleDefaults();
    };
    AcDbDxfDocumentReader.prototype.readTableName = function (filer) {
        while (!filer.atEof) {
            var item = filer.peekItem();
            if (!item)
                return '';
            var code = Number(item.code);
            if (code === 0)
                return '';
            filer.readItem();
            if (code === 2)
                return String(item.value).toUpperCase();
        }
        return '';
    };
    AcDbDxfDocumentReader.prototype.readTable = function (filer, tableName) {
        var _this = this;
        switch (tableName) {
            case 'LAYER':
                this.readNamedTable(filer, 'LAYER', function () { return new AcDbLayerTableRecord(); }, function (r) {
                    if (r.name)
                        _this._db.tables.layerTable.add(r);
                });
                break;
            case 'LTYPE':
                this.readNamedTable(filer, 'LTYPE', function () { return new AcDbLinetypeTableRecord(); }, function (r) {
                    if (r.name)
                        _this._db.tables.linetypeTable.add(r);
                });
                break;
            case 'STYLE':
                this.readNamedTable(filer, 'STYLE', function () { return new AcDbTextStyleTableRecord(); }, function (r) {
                    // Shape-file STYLE entries often have an empty name.
                    _this._db.tables.textStyleTable.add(r);
                });
                break;
            case 'DIMSTYLE':
                this.readNamedTable(filer, 'DIMSTYLE', function () { return new AcDbDimStyleTableRecord(); }, function (r) {
                    if (r.name)
                        _this._db.tables.dimStyleTable.add(r);
                });
                break;
            case 'VPORT':
                this.readNamedTable(filer, 'VPORT', function () { return new AcDbViewportTableRecord(); }, function (r) {
                    if (r.name)
                        _this._db.tables.viewportTable.add(r);
                });
                break;
            case 'APPID':
                this.readNamedTable(filer, 'APPID', function () { return new AcDbRegAppTableRecord(); }, function (r) {
                    if (r.name)
                        _this._db.tables.appIdTable.add(r);
                });
                break;
            case 'VIEW':
                this.readNamedTable(filer, 'VIEW', function () { return new AcDbViewTableRecord(); }, function (r) {
                    if (r.name)
                        _this._db.tables.viewTable.add(r);
                });
                break;
            case 'UCS':
                this.readNamedTable(filer, 'UCS', function () { return new AcDbUcsTableRecord(); }, function (r) {
                    if (r.name)
                        _this._db.tables.ucsTable.add(r);
                });
                break;
            case 'BLOCK_RECORD':
                this.readBlockRecordTable(filer);
                break;
            default:
                this.skipUntilEndTab(filer);
                break;
        }
    };
    AcDbDxfDocumentReader.prototype.readNamedTable = function (filer, recordType, create, add) {
        while (!filer.atEof) {
            var item = filer.peekItem();
            if (!item)
                break;
            if (Number(item.code) !== 0) {
                filer.readItem();
                continue;
            }
            var name_5 = String(item.value).toUpperCase();
            if (name_5 === 'ENDTAB') {
                filer.readItem();
                break;
            }
            if (name_5 === 'ENDSEC') {
                break;
            }
            if (name_5 === recordType) {
                filer.readItem();
                var record = create();
                record.dxfIn(filer);
                add(record);
                continue;
            }
            filer.readItem();
            filer.skipToEndOfObject();
        }
    };
    AcDbDxfDocumentReader.prototype.readBlockRecordTable = function (filer) {
        var cleared = false;
        while (!filer.atEof) {
            var item = filer.peekItem();
            if (!item)
                break;
            if (Number(item.code) !== 0) {
                filer.readItem();
                continue;
            }
            var name_6 = String(item.value).toUpperCase();
            if (name_6 === 'ENDTAB') {
                filer.readItem();
                break;
            }
            if (name_6 === 'ENDSEC') {
                break;
            }
            if (name_6 === 'BLOCK_RECORD') {
                filer.readItem();
                if (!cleared) {
                    this._db.tables.blockTable.removeAll();
                    cleared = true;
                }
                var record = new AcDbBlockTableRecord();
                record.dxfIn(filer);
                if (record.name) {
                    this._db.tables.blockTable.add(record);
                }
                continue;
            }
            filer.readItem();
            filer.skipToEndOfObject();
        }
    };
    AcDbDxfDocumentReader.prototype.skipUntilEndTab = function (filer) {
        while (!filer.atEof) {
            var item = filer.readItem();
            if (!item)
                break;
            if (Number(item.code) === 0 &&
                String(item.value).toUpperCase() === 'ENDTAB') {
                break;
            }
        }
    };
    AcDbDxfDocumentReader.prototype.readBlocksSection = function (filer) {
        return __awaiter(this, void 0, void 0, function () {
            var batchSize, sinceYield, item, name_7, yielded;
            var _a;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        batchSize = Math.max(1, (_a = this._options.entityBatchSize) !== null && _a !== void 0 ? _a : 200);
                        sinceYield = 0;
                        _b.label = 1;
                    case 1:
                        if (!!filer.atEof) return [3 /*break*/, 4];
                        item = filer.peekItem();
                        if (!item)
                            return [3 /*break*/, 4];
                        if (Number(item.code) !== 0) {
                            filer.readItem();
                            return [3 /*break*/, 1];
                        }
                        name_7 = String(item.value).toUpperCase();
                        if (name_7 === 'ENDSEC') {
                            filer.readItem();
                            return [3 /*break*/, 4];
                        }
                        if (!(name_7 === 'BLOCK')) return [3 /*break*/, 3];
                        filer.readItem();
                        return [4 /*yield*/, this.readBlockDefinition(filer, batchSize, sinceYield)];
                    case 2:
                        yielded = _b.sent();
                        sinceYield = yielded;
                        return [3 /*break*/, 1];
                    case 3:
                        filer.readItem();
                        filer.skipToEndOfObject();
                        return [3 /*break*/, 1];
                    case 4: return [2 /*return*/];
                }
            });
        });
    };
    /**
     * Reads one BLOCK … ENDBLK definition: sync BTR metadata, then stream entities.
     * @returns Updated sinceYield counter for batching.
     */
    AcDbDxfDocumentReader.prototype.readBlockDefinition = function (filer, batchSize, sinceYield) {
        return __awaiter(this, void 0, void 0, function () {
            var header, btr, item, typeName, entity;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        header = this.readBlockBeginFields(filer);
                        btr = this.ensureBlockTableRecord(header);
                        _a.label = 1;
                    case 1:
                        if (!!filer.atEof) return [3 /*break*/, 7];
                        item = filer.peekItem();
                        if (!item)
                            return [3 /*break*/, 7];
                        if (Number(item.code) !== 0) {
                            filer.readItem();
                            return [3 /*break*/, 1];
                        }
                        typeName = String(item.value).toUpperCase();
                        if (typeName === 'ENDBLK') {
                            filer.readItem();
                            filer.skipToEndOfObject();
                            return [3 /*break*/, 7];
                        }
                        if (typeName === 'ENDSEC') {
                            return [3 /*break*/, 7];
                        }
                        filer.readItem();
                        if (!this.consumeSeqend(filer, typeName)) return [3 /*break*/, 4];
                        sinceYield += 1;
                        if (!(sinceYield >= batchSize)) return [3 /*break*/, 3];
                        sinceYield = 0;
                        return [4 /*yield*/, this.yieldAndReportProgress(filer)];
                    case 2:
                        _a.sent();
                        _a.label = 3;
                    case 3: return [3 /*break*/, 1];
                    case 4:
                        entity = acdbDxfInEntity(filer, typeName);
                        if (entity) {
                            // Model/paper geometry lives in ENTITIES; skip space BTRs here to
                            // avoid duplicating entities that also appear with group 67 / owner.
                            if (this.shouldAcceptBlockEntity(btr)) {
                                this.acceptEntity(entity, btr);
                            }
                        }
                        else {
                            this._unknownEntityCount += 1;
                            filer.skipToEndOfObject();
                        }
                        sinceYield += 1;
                        if (!(sinceYield >= batchSize)) return [3 /*break*/, 6];
                        sinceYield = 0;
                        return [4 /*yield*/, this.yieldAndReportProgress(filer)];
                    case 5:
                        _a.sent();
                        _a.label = 6;
                    case 6: return [3 /*break*/, 1];
                    case 7: return [2 /*return*/, sinceYield];
                }
            });
        });
    };
    /**
     * Block definitions may own entities; *Model_Space / *Paper_Space contents
     * are streamed from the ENTITIES section in modern DXF.
     */
    AcDbDxfDocumentReader.prototype.shouldAcceptBlockEntity = function (btr) {
        return !btr.isModelSapce && !btr.isPaperSapce;
    };
    /** Consume a SEQEND object after its `(0, SEQEND)` pair was already read. */
    AcDbDxfDocumentReader.prototype.consumeSeqend = function (filer, typeName) {
        if (typeName !== 'SEQEND')
            return false;
        filer.skipToEndOfObject();
        return true;
    };
    AcDbDxfDocumentReader.prototype.readBlockBeginFields = function (filer) {
        var origin = new AcGePoint3d();
        var name = '';
        var flags = 0;
        var pathName = '';
        var objectId;
        var ownerId;
        while (!filer.atEndOfObject && !filer.atEof && !filer.atExtendedData) {
            var item = filer.readItem();
            if (!item)
                break;
            var code = Number(item.code);
            if (code === 100) {
                // Consume AcDbEntity / AcDbBlockBegin markers; continue fields after.
                continue;
            }
            var n = Number(item.value);
            switch (code) {
                case 5:
                    objectId = String(item.value);
                    break;
                case 330:
                    ownerId = String(item.value);
                    break;
                case 2:
                case 3:
                    if (!name)
                        name = String(item.value);
                    break;
                case 70:
                    flags = n;
                    break;
                case 10:
                    origin.x = n;
                    break;
                case 20:
                    origin.y = n;
                    break;
                case 30:
                    origin.z = n;
                    break;
                case 1:
                    pathName = String(item.value);
                    break;
                case 8:
                case 67:
                case 410:
                    // Entity common fields on BLOCK — ignore for BTR metadata.
                    break;
                default:
                    // Unknown BLOCK begin field — keep scanning until end of object.
                    break;
            }
        }
        return { name: name, flags: flags, origin: origin, pathName: pathName, objectId: objectId, ownerId: ownerId };
    };
    AcDbDxfDocumentReader.prototype.ensureBlockTableRecord = function (header) {
        var btr = header.name
            ? this._db.tables.blockTable.getAt(header.name)
            : undefined;
        if (!btr) {
            btr = new AcDbBlockTableRecord();
            btr.name = header.name || '*UNKNOWN';
            if (header.objectId) {
                btr.objectId = header.objectId;
            }
            this._db.tables.blockTable.add(btr);
        }
        btr.origin.copy(header.origin);
        var flags = AcDbBlockTableRecord.sanitizeImportedFlags(header.flags);
        if (header.pathName) {
            btr.pathName = header.pathName;
            if ((flags &
                (AcDbBlockTableRecordFlag.Xref |
                    AcDbBlockTableRecordFlag.XrefOverlay)) ===
                0) {
                flags = flags | AcDbBlockTableRecordFlag.Xref;
            }
        }
        btr.flags = flags;
        return btr;
    };
    AcDbDxfDocumentReader.prototype.readEntitiesSection = function (filer) {
        return __awaiter(this, void 0, void 0, function () {
            var batchSize, sinceYield, modelSpace, item, name_8, entity;
            var _a;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        batchSize = Math.max(1, (_a = this._options.entityBatchSize) !== null && _a !== void 0 ? _a : 200);
                        sinceYield = 0;
                        modelSpace = this._db.tables.blockTable.modelSpace;
                        _b.label = 1;
                    case 1:
                        if (!!filer.atEof) return [3 /*break*/, 7];
                        item = filer.peekItem();
                        if (!item)
                            return [3 /*break*/, 7];
                        if (Number(item.code) !== 0) {
                            filer.readItem();
                            return [3 /*break*/, 1];
                        }
                        name_8 = String(item.value).toUpperCase();
                        if (name_8 === 'ENDSEC') {
                            filer.readItem();
                            return [3 /*break*/, 7];
                        }
                        filer.readItem();
                        if (!this.consumeSeqend(filer, name_8)) return [3 /*break*/, 4];
                        sinceYield += 1;
                        if (!(sinceYield >= batchSize)) return [3 /*break*/, 3];
                        sinceYield = 0;
                        return [4 /*yield*/, this.yieldAndReportProgress(filer)];
                    case 2:
                        _b.sent();
                        _b.label = 3;
                    case 3: return [3 /*break*/, 1];
                    case 4:
                        entity = acdbDxfInEntity(filer, name_8);
                        if (entity) {
                            this.acceptEntity(entity, this.resolveEntityOwner(entity, modelSpace));
                        }
                        else {
                            this._unknownEntityCount += 1;
                            filer.skipToEndOfObject();
                        }
                        sinceYield += 1;
                        if (!(sinceYield >= batchSize)) return [3 /*break*/, 6];
                        sinceYield = 0;
                        return [4 /*yield*/, this.yieldAndReportProgress(filer)];
                    case 5:
                        _b.sent();
                        _b.label = 6;
                    case 6: return [3 /*break*/, 1];
                    case 7: return [2 /*return*/];
                }
            });
        });
    };
    /**
     * Chooses model vs paper space BTR for an ENTITIES-section entity.
     * Prefers ownerId (330) when it names a space BTR; falls back to DXF group 67.
     */
    AcDbDxfDocumentReader.prototype.resolveEntityOwner = function (entity, modelSpace) {
        var ownerId = entity.getAttrWithoutException('ownerId');
        if (ownerId) {
            var owner = this._db.tables.blockTable.getIdAt(ownerId);
            if (owner && (owner.isModelSapce || owner.isPaperSapce)) {
                return owner;
            }
        }
        if (entity.dxfPaperSpace) {
            var paper = this._db.tables.blockTable.getAt(AcDbBlockTableRecord.PAPER_SPACE_NAME_PREFIX);
            if (paper)
                return paper;
        }
        return modelSpace;
    };
    /**
     * Appends an entity (or links ATTRIB → INSERT).
     */
    AcDbDxfDocumentReader.prototype.acceptEntity = function (entity, owner) {
        var e_4, _a;
        if (entity instanceof AcDbAttribute) {
            this.linkOrDeferAttribute(entity, owner);
            return;
        }
        owner.appendEntity(entity);
        if (entity instanceof AcDbBlockReference) {
            var pending_2 = this._attributeMap.get(entity.objectId);
            if (pending_2 && pending_2.length > 0) {
                try {
                    for (var pending_1 = __values(pending_2), pending_1_1 = pending_1.next(); !pending_1_1.done; pending_1_1 = pending_1.next()) {
                        var attrib = pending_1_1.value;
                        entity.appendAttributes(attrib);
                    }
                }
                catch (e_4_1) { e_4 = { error: e_4_1 }; }
                finally {
                    try {
                        if (pending_1_1 && !pending_1_1.done && (_a = pending_1.return)) _a.call(pending_1);
                    }
                    finally { if (e_4) throw e_4.error; }
                }
                this._attributeMap.delete(entity.objectId);
            }
        }
    };
    AcDbDxfDocumentReader.prototype.linkOrDeferAttribute = function (attrib, ownerBtr) {
        var _a;
        var insertId = attrib.getAttrWithoutException('ownerId');
        if (!insertId || insertId === '0')
            return;
        var insert = (_a = ownerBtr.getIdAt(insertId)) !== null && _a !== void 0 ? _a : this._db.tables.blockTable.getEntityById(insertId);
        if (insert instanceof AcDbBlockReference) {
            insert.appendAttributes(attrib);
            return;
        }
        var list = this._attributeMap.get(insertId);
        if (!list) {
            list = [];
            this._attributeMap.set(insertId, list);
        }
        list.push(attrib);
    };
    AcDbDxfDocumentReader.prototype.flushRemainingAttributes = function () {
        var e_5, _a, e_6, _b;
        try {
            for (var _c = __values(this._attributeMap), _d = _c.next(); !_d.done; _d = _c.next()) {
                var _e = __read(_d.value, 2), insertId = _e[0], attribs = _e[1];
                var insert = this._db.tables.blockTable.getEntityById(insertId);
                if (insert instanceof AcDbBlockReference) {
                    try {
                        for (var attribs_1 = (e_6 = void 0, __values(attribs)), attribs_1_1 = attribs_1.next(); !attribs_1_1.done; attribs_1_1 = attribs_1.next()) {
                            var attrib = attribs_1_1.value;
                            insert.appendAttributes(attrib);
                        }
                    }
                    catch (e_6_1) { e_6 = { error: e_6_1 }; }
                    finally {
                        try {
                            if (attribs_1_1 && !attribs_1_1.done && (_b = attribs_1.return)) _b.call(attribs_1);
                        }
                        finally { if (e_6) throw e_6.error; }
                    }
                }
            }
        }
        catch (e_5_1) { e_5 = { error: e_5_1 }; }
        finally {
            try {
                if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
            }
            finally { if (e_5) throw e_5.error; }
        }
        this._attributeMap.clear();
    };
    AcDbDxfDocumentReader.prototype.skipUntilEndSec = function (filer) {
        while (!filer.atEof) {
            var item = filer.readItem();
            if (!item)
                break;
            if (Number(item.code) === 0 &&
                String(item.value).toUpperCase() === 'ENDSEC') {
                break;
            }
        }
    };
    AcDbDxfDocumentReader.prototype.reportParseProgress = function (filer) {
        return __awaiter(this, void 0, void 0, function () {
            var _a, onProgress, totalBytes, ratio;
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        _a = this._options, onProgress = _a.onProgress, totalBytes = _a.totalBytes;
                        if (!onProgress || !totalBytes || totalBytes <= 0)
                            return [2 /*return*/];
                        ratio = Math.min(1, Math.max(0, filer.position().byteOffset / totalBytes));
                        return [4 /*yield*/, onProgress(ratio)];
                    case 1:
                        _b.sent();
                        return [2 /*return*/];
                }
            });
        });
    };
    AcDbDxfDocumentReader.prototype.yieldAndReportProgress = function (filer) {
        return __awaiter(this, void 0, void 0, function () {
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0: return [4 /*yield*/, this.reportParseProgress(filer)
                        // Progress may fire every batch; UI yield is time-budgeted so large DXFs
                        // are not dominated by per-batch requestAnimationFrame waits.
                    ];
                    case 1:
                        _a.sent();
                        // Progress may fire every batch; UI yield is time-budgeted so large DXFs
                        // are not dominated by per-batch requestAnimationFrame waits.
                        return [4 /*yield*/, this._yieldGate.maybeYield(accmYieldToUi)];
                    case 2:
                        // Progress may fire every batch; UI yield is time-budgeted so large DXFs
                        // are not dominated by per-batch requestAnimationFrame waits.
                        _a.sent();
                        return [2 /*return*/];
                }
            });
        });
    };
    return AcDbDxfDocumentReader;
}());
export { AcDbDxfDocumentReader };
//# sourceMappingURL=AcDbDxfDocumentReader.js.map