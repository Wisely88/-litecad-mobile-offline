import { acdbDxfValueType } from '../base/AcDbDxfGroupCodeTypes';
/**
 * In-memory pair reader used to replay one object's tags (e.g. DIMENSION
 * subclass selection) without buffering the whole DXF file.
 */
var AcDbDxfPairArrayReader = /** @class */ (function () {
    function AcDbDxfPairArrayReader(_pairs) {
        this._pairs = _pairs;
        this.kind = 'ascii';
        this._index = 0;
    }
    AcDbDxfPairArrayReader.prototype.next = function () {
        if (this._index >= this._pairs.length)
            return undefined;
        return this._pairs[this._index++];
    };
    AcDbDxfPairArrayReader.prototype.peek = function () {
        if (this._index >= this._pairs.length)
            return undefined;
        return this._pairs[this._index];
    };
    AcDbDxfPairArrayReader.prototype.position = function () {
        return { byteOffset: this._index };
    };
    return AcDbDxfPairArrayReader;
}());
export { AcDbDxfPairArrayReader };
/** Build a typed {@link AcDbDxfPair} from a filer item. */
export function acdbTypedValueToDxfPair(item) {
    var code = Number(item.code);
    var value = item.value;
    var type = acdbDxfValueType(code);
    if (type === 'comment') {
        return { code: code, type: 'string', value: String(value !== null && value !== void 0 ? value : '') };
    }
    if (type === 'bool') {
        return { code: code, type: 'bool', value: Boolean(value) };
    }
    if (type === 'binary') {
        if (value instanceof Uint8Array)
            return { code: code, type: 'binary', value: value };
        return { code: code, type: 'string', value: String(value !== null && value !== void 0 ? value : '') };
    }
    if (type === 'handle') {
        return { code: code, type: 'handle', value: String(value !== null && value !== void 0 ? value : '') };
    }
    if (type === 'int' || type === 'long') {
        return { code: code, type: type === 'long' ? 'long' : 'int', value: Number(value) };
    }
    if (type === 'double') {
        return { code: code, type: 'double', value: Number(value) };
    }
    return { code: code, type: 'string', value: String(value !== null && value !== void 0 ? value : '') };
}
/**
 * Drain pairs from the current object (until code 0), excluding XData so
 * {@link AcDbObject.dxfIn} can still consume it afterward.
 */
export function acdbDrainDxfObjectPairs(filer) {
    var pairs = [];
    while (!filer.atEndOfObject && !filer.atEof && !filer.atExtendedData) {
        var item = filer.readItem();
        if (!item)
            break;
        pairs.push(acdbTypedValueToDxfPair(item));
    }
    return pairs;
}
//# sourceMappingURL=AcDbDxfPairArrayReader.js.map