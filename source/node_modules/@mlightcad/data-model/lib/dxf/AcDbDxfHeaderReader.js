import { AcGePoint3d } from '@mlightcad/geometry-engine';
import { AcDbSystemVariables } from '../database/AcDbSystemVariables';
import { AcDbSysVarManager } from '../database/AcDbSysVarManager';
import { ACDB_COMPAREHATCH_MAX, ACDB_COMPAREHATCH_MIN, ACDB_COMPARERCMARGIN_MAX, ACDB_COMPARERCMARGIN_MIN, ACDB_COMPARETEXT_MAX, ACDB_COMPARETEXT_MIN, ACDB_COMPARETOLERANCE_MAX, ACDB_COMPARETOLERANCE_MIN, acdbIntegerSysVarIfInRange, ByLayer, DEFAULT_MLEADER_STYLE, DEFAULT_MLINE_STYLE, DEFAULT_TEXT_STYLE } from '../misc/AcDbConstants';
/**
 * Stream HEADER section variables into {@link AcDbDatabase}.
 * Expects the filer to be positioned after `(0,SECTION)/(2,HEADER)`.
 * Stops at `(0,ENDSEC)` without consuming pairs belonging to the next section.
 */
export function acdbDxfInHeader(filer, db) {
    while (!filer.atEof) {
        var item = filer.peekItem();
        if (!item)
            break;
        if (Number(item.code) === 0) {
            var name_1 = String(item.value).toUpperCase();
            if (name_1 === 'ENDSEC') {
                filer.readItem();
                break;
            }
            // Unexpected object start inside HEADER — stop.
            break;
        }
        filer.readItem();
        if (Number(item.code) !== 9)
            continue;
        var varName = String(item.value).toUpperCase();
        applyHeaderVar(filer, db, varName);
    }
}
function applyHeaderVar(filer, db, varName) {
    var _a, _b, _c, _d, _e, _f, _g, _h, _j, _k, _l;
    switch (varName) {
        case '$ACADVER': {
            var v = readHeaderString(filer);
            if (v)
                db.version = v;
            break;
        }
        case '$CECOLOR': {
            var n = readHeaderNumber(filer);
            db.cecolor.colorIndex = n !== null && n !== void 0 ? n : 256;
            break;
        }
        case '$ANGBASE':
            db.angbase = (_a = readHeaderNumber(filer)) !== null && _a !== void 0 ? _a : 0;
            break;
        case '$ANGDIR':
            db.angdir = (_b = readHeaderNumber(filer)) !== null && _b !== void 0 ? _b : 0;
            break;
        case '$AUNITS': {
            var n = readHeaderNumber(filer);
            if (n != null)
                db.aunits = n;
            break;
        }
        case '$AUPREC': {
            var n = readHeaderNumber(filer);
            if (n != null)
                db.auprec = n;
            break;
        }
        case '$LUNITS': {
            var n = readHeaderNumber(filer);
            if (n != null)
                db.lunits = n;
            break;
        }
        case '$LUPREC': {
            var n = readHeaderNumber(filer);
            if (n != null)
                db.luprec = n;
            break;
        }
        case '$UNITMODE': {
            var n = readHeaderNumber(filer);
            if (n != null)
                db.unitmode = n;
            break;
        }
        case '$MEASUREMENT': {
            var n = readHeaderNumber(filer);
            if (n != null)
                db.measurement = n;
            break;
        }
        case '$CELTYPE':
            db.celtype = readHeaderString(filer) || ByLayer;
            break;
        case '$CETRANSPARENCY': {
            readTransparencyHeaderVar(filer, db, AcDbSystemVariables.CETRANSPARENCY);
            break;
        }
        case '$CELTSCALE':
            db.celtscale = (_c = readHeaderNumber(filer)) !== null && _c !== void 0 ? _c : 1;
            break;
        case '$CELWEIGHT': {
            var n = readHeaderNumber(filer);
            if (n != null)
                db.celweight = n;
            break;
        }
        case '$LWDISPLAY':
            db.lwdisplay = ((_d = readHeaderNumber(filer)) !== null && _d !== void 0 ? _d : 0) !== 0;
            break;
        case '$TILEMODE':
            db.tilemode = ((_e = readHeaderNumber(filer)) !== null && _e !== void 0 ? _e : 1) !== 0;
            break;
        case '$PSLTSCALE':
            db.psltscale = ((_f = readHeaderNumber(filer)) !== null && _f !== void 0 ? _f : 1) !== 0;
            break;
        case '$CMLSTYLE':
        case 'CMLSTYLE':
            db.cmlstyle = readHeaderString(filer) || DEFAULT_MLINE_STYLE;
            break;
        case '$CMLSCALE':
        case 'CMLSCALE': {
            var n = readHeaderNumber(filer);
            if (n != null && Number.isFinite(n))
                db.cmlscale = n;
            break;
        }
        case '$CMLEADERSTYLE':
        case 'CMLEADERSTYLE':
            db.cmleaderstyle = readHeaderString(filer) || DEFAULT_MLEADER_STYLE;
            break;
        case '$HPLAYER':
        case 'HPLAYER':
            db.hplayer = readHeaderString(filer) || '.';
            break;
        case '$HPTRANSPARENCY': {
            readTransparencyHeaderVar(filer, db, AcDbSystemVariables.HPTRANSPARENCY);
            break;
        }
        case '$LTSCALE':
            db.ltscale = (_g = readHeaderNumber(filer)) !== null && _g !== void 0 ? _g : 1;
            break;
        case '$EXTMAX': {
            var p = readHeaderPoint3d(filer);
            if (p)
                db.extmax = p;
            break;
        }
        case '$EXTMIN': {
            var p = readHeaderPoint3d(filer);
            if (p)
                db.extmin = p;
            break;
        }
        case '$INSUNITS': {
            var n = readHeaderNumber(filer);
            if (n != null)
                db.insunits = n;
            break;
        }
        case '$OSMODE':
            db.osmode = (_h = readHeaderNumber(filer)) !== null && _h !== void 0 ? _h : 0;
            break;
        case '$ORTHOMODE':
            db.orthomode = (_j = readHeaderNumber(filer)) !== null && _j !== void 0 ? _j : 0;
            break;
        case '$COMPAREHATCH': {
            var n = acdbIntegerSysVarIfInRange(readHeaderNumber(filer), ACDB_COMPAREHATCH_MIN, ACDB_COMPAREHATCH_MAX);
            if (n != null)
                db.comparehatch = n;
            break;
        }
        case '$COMPARERCMARGIN': {
            var n = acdbIntegerSysVarIfInRange(readHeaderNumber(filer), ACDB_COMPARERCMARGIN_MIN, ACDB_COMPARERCMARGIN_MAX);
            if (n != null)
                db.comparercmargin = n;
            break;
        }
        case '$COMPARETEXT': {
            var n = acdbIntegerSysVarIfInRange(readHeaderNumber(filer), ACDB_COMPARETEXT_MIN, ACDB_COMPARETEXT_MAX);
            if (n != null)
                db.comparetext = n;
            break;
        }
        case '$COMPARETOLERANCE': {
            var n = acdbIntegerSysVarIfInRange(readHeaderNumber(filer), ACDB_COMPARETOLERANCE_MIN, ACDB_COMPARETOLERANCE_MAX);
            if (n != null)
                db.comparetolerance = n;
            break;
        }
        case '$PDMODE':
            db.pdmode = (_k = readHeaderNumber(filer)) !== null && _k !== void 0 ? _k : 0;
            break;
        case '$PDSIZE':
            db.pdsize = (_l = readHeaderNumber(filer)) !== null && _l !== void 0 ? _l : 0;
            break;
        case '$TEXTSTYLE':
            db.textstyle = readHeaderString(filer) || DEFAULT_TEXT_STYLE;
            break;
        case '$DIMSTYLE':
            db.dimstyle = readHeaderString(filer) || DEFAULT_TEXT_STYLE;
            break;
        case '$CLAYER':
            db.clayer = readHeaderString(filer) || '0';
            break;
        case '$HANDSEED': {
            var v = readHeaderString(filer);
            if (v)
                db.initializeHandleSeed(v);
            break;
        }
        default:
            // Skip unknown header value pair(s). Point vars may span multiple codes;
            // consume until the next code 9 or 0.
            skipHeaderValue(filer);
            break;
    }
}
function readHeaderString(filer) {
    var item = filer.readItem();
    if (!item)
        return undefined;
    if (Number(item.code) === 9 || Number(item.code) === 0) {
        filer.pushBackItem(item);
        return undefined;
    }
    return String(item.value);
}
function readHeaderNumber(filer) {
    var item = filer.readItem();
    if (!item)
        return undefined;
    if (Number(item.code) === 9 || Number(item.code) === 0) {
        filer.pushBackItem(item);
        return undefined;
    }
    var n = Number(item.value);
    return Number.isFinite(n) ? n : undefined;
}
/**
 * Reads a transparency header variable (`$CETRANSPARENCY` / `$HPTRANSPARENCY`).
 *
 * Spec-compliant writers (and this library's own `dxfOut`) store these as
 * group-440 32-bit bitfield integers (high byte = method, low byte = alpha).
 * Feeding that integer as a *string* into `AcCmTransparency.fromString`
 * rejects values above 255 and throws "Invalid transparency value!" — so a
 * file written by `dxfOut` could not be re-opened.
 *
 * Dispatch by group code, not by `Number()` heuristics:
 * - **440** → pass as a number so `setVar` takes the `deserialize` path
 * - **anything else** (textual "ByLayer"/"ByBlock"/percentage) → string /
 *   `fromString` path. Do not coerce bare percentages like `"25"` via
 *   `Number()`, which would incorrectly go through `deserialize`.
 * - **missing value** → fall back to `'ByLayer'` (matches prior reader
 *   behavior and AutoCAD's default).
 */
function readTransparencyHeaderVar(filer, db, varName) {
    var item = filer.readItem();
    if (!item || Number(item.code) === 9 || Number(item.code) === 0) {
        if (item)
            filer.pushBackItem(item);
        AcDbSysVarManager.instance().setVar(varName, 'ByLayer', db);
        return;
    }
    var code = Number(item.code);
    var raw = String(item.value).trim();
    if (raw === '') {
        AcDbSysVarManager.instance().setVar(varName, 'ByLayer', db);
        return;
    }
    if (code === 440) {
        var n = Number(raw);
        AcDbSysVarManager.instance().setVar(varName, Number.isFinite(n) ? n : 'ByLayer', db);
        return;
    }
    AcDbSysVarManager.instance().setVar(varName, raw, db);
}
function readHeaderPoint3d(filer) {
    var x = 0;
    var y = 0;
    var z = 0;
    var got = false;
    while (!filer.atEof) {
        var item = filer.peekItem();
        if (!item)
            break;
        var code = Number(item.code);
        if (code === 9 || code === 0)
            break;
        filer.readItem();
        var n = Number(item.value);
        if (code === 10) {
            x = n;
            got = true;
        }
        else if (code === 20) {
            y = n;
            got = true;
        }
        else if (code === 30) {
            z = n;
            got = true;
        }
    }
    return got ? new AcGePoint3d(x, y, z) : undefined;
}
function skipHeaderValue(filer) {
    while (!filer.atEof) {
        var item = filer.peekItem();
        if (!item)
            break;
        var code = Number(item.code);
        if (code === 9 || code === 0)
            break;
        filer.readItem();
    }
}
//# sourceMappingURL=AcDbDxfHeaderReader.js.map