var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
import { ACCM_DEFAULT_UI_YIELD_BUDGET_MS, AcCmUiYieldGate, accmYieldForPaint } from '@mlightcad/common';
import { AcDbDxfFiler } from '../base/AcDbDxfFiler';
import { AcDbDatabaseConverter } from '../database/AcDbDatabaseConverter';
import { AcDbDxfDocumentReader } from './AcDbDxfDocumentReader';
/**
 * Progress weights mirror typical open cost: a small PARSE slice, then most of
 * the bar for ENTITY add/render (same idea as the old converter where ENTITY
 * carried the largest step).
 */
var PARSE_START_PCT = 1;
var PARSE_END_PCT = 18;
var ENTITY_START_PCT = 20;
var ENTITY_END_PCT = 98;
/**
 * Native DXF → database converter.
 *
 * Streams DXF pairs through {@link AcDbDxfFiler} / {@link AcDbDxfDocumentReader}
 * directly into {@link AcDbDatabase}, avoiding the ParsedDxf JSON intermediate
 * used by `@mlightcad/dxf-json-converter`.
 *
 * Conversion stages: `START → PARSE → ENTITY → END`. The reader fills the
 * database quietly (entity-appended events batched); then the batch flushes so
 * the first draw happens after parse completes. Fonts are loaded on demand by
 * the mtext renderer when a font is first needed.
 *
 * Mid-PARSE byte progress, chunked ENTITY flush, and time-budgeted UI yields
 * keep the status bar / spinner responsive without stalling large files on
 * per-chunk `requestAnimationFrame` waits.
 */
var AcDbNativeDxfConverter = /** @class */ (function (_super) {
    __extends(AcDbNativeDxfConverter, _super);
    function AcDbNativeDxfConverter(config) {
        if (config === void 0) { config = {}; }
        return _super.call(this, __assign({ useWorker: false }, config)) || this;
    }
    AcDbNativeDxfConverter.prototype.read = function (data_1, db_1) {
        return __awaiter(this, arguments, void 0, function (data, db, options) {
            var _a, minimumChunkSize, progress, encoding, emit, batchOpen, filer, totalBytes, lastParsePct_1, reader, result, chunkSize, lastEntityPct_1, yieldGate_1, error_1;
            var _this = this;
            if (options === void 0) { options = {}; }
            return __generator(this, function (_b) {
                switch (_b.label) {
                    case 0:
                        _a = options.minimumChunkSize, minimumChunkSize = _a === void 0 ? 10 : _a, progress = options.progress, encoding = options.encoding;
                        this.progress = progress;
                        emit = function (percentage, stage, status, stageData) { return __awaiter(_this, void 0, void 0, function () {
                            return __generator(this, function (_a) {
                                switch (_a.label) {
                                    case 0:
                                        if (!progress)
                                            return [2 /*return*/];
                                        return [4 /*yield*/, progress(percentage, stage, status, stageData)];
                                    case 1:
                                        _a.sent();
                                        return [2 /*return*/];
                                }
                            });
                        }); };
                        return [4 /*yield*/, emit(0, 'START', 'START')];
                    case 1:
                        _b.sent();
                        return [4 /*yield*/, emit(PARSE_START_PCT, 'PARSE', 'START')
                            // Let the open-file overlay paint before sync-heavy parse work (once).
                        ];
                    case 2:
                        _b.sent();
                        // Let the open-file overlay paint before sync-heavy parse work (once).
                        return [4 /*yield*/, accmYieldForPaint()
                            // Suppress entityAppended (and related) until parse finishes so the viewer
                            // does not worldDraw mid-stream while the open-file progress bar is still
                            // in PARSE.
                        ];
                    case 3:
                        // Let the open-file overlay paint before sync-heavy parse work (once).
                        _b.sent();
                        // Suppress entityAppended (and related) until parse finishes so the viewer
                        // does not worldDraw mid-stream while the open-file progress bar is still
                        // in PARSE.
                        db.beginEventBatch();
                        batchOpen = true;
                        _b.label = 4;
                    case 4:
                        _b.trys.push([4, 11, , 12]);
                        filer = AcDbDxfFiler.fromBuffer(data, {
                            database: db,
                            encoding: encoding
                        });
                        totalBytes = data.byteLength;
                        lastParsePct_1 = PARSE_START_PCT;
                        reader = new AcDbDxfDocumentReader(db, {
                            entityBatchSize: Math.max(1, minimumChunkSize || 200),
                            yieldBudgetMs: ACCM_DEFAULT_UI_YIELD_BUDGET_MS,
                            totalBytes: totalBytes,
                            onProgress: function (ratio) { return __awaiter(_this, void 0, void 0, function () {
                                var pct;
                                return __generator(this, function (_a) {
                                    switch (_a.label) {
                                        case 0:
                                            pct = Math.min(PARSE_END_PCT - 1, PARSE_START_PCT +
                                                Math.floor(ratio * (PARSE_END_PCT - PARSE_START_PCT)));
                                            if (pct <= lastParsePct_1)
                                                return [2 /*return*/];
                                            lastParsePct_1 = pct;
                                            return [4 /*yield*/, emit(pct, 'PARSE', 'IN-PROGRESS')];
                                        case 1:
                                            _a.sent();
                                            return [2 /*return*/];
                                    }
                                });
                            }); }
                        });
                        return [4 /*yield*/, reader.read(filer)];
                    case 5:
                        result = _b.sent();
                        return [4 /*yield*/, emit(PARSE_END_PCT, 'PARSE', 'END', {
                                unknownEntityCount: result.unknownEntityCount
                            })];
                    case 6:
                        _b.sent();
                        return [4 /*yield*/, emit(ENTITY_START_PCT, 'ENTITY', 'START')];
                    case 7:
                        _b.sent();
                        chunkSize = Math.max(1, minimumChunkSize || 200);
                        lastEntityPct_1 = ENTITY_START_PCT;
                        yieldGate_1 = new AcCmUiYieldGate(ACCM_DEFAULT_UI_YIELD_BUDGET_MS);
                        // Flush queued entityAppended in chunks while advancing most of the
                        // open-file progress bar.
                        return [4 /*yield*/, db.endEventBatchChunked(chunkSize, function (flushed, total) { return __awaiter(_this, void 0, void 0, function () {
                                var pct;
                                return __generator(this, function (_a) {
                                    switch (_a.label) {
                                        case 0:
                                            pct = total <= 0
                                                ? ENTITY_END_PCT
                                                : Math.min(ENTITY_END_PCT, ENTITY_START_PCT +
                                                    Math.floor((flushed / total) * (ENTITY_END_PCT - ENTITY_START_PCT)));
                                            if (!(pct > lastEntityPct_1)) return [3 /*break*/, 2];
                                            lastEntityPct_1 = pct;
                                            return [4 /*yield*/, emit(pct, 'ENTITY', 'IN-PROGRESS')];
                                        case 1:
                                            _a.sent();
                                            _a.label = 2;
                                        case 2: 
                                        // Time-budgeted single-frame yield — not per progress percent.
                                        return [4 /*yield*/, yieldGate_1.maybeYield()];
                                        case 3:
                                            // Time-budgeted single-frame yield — not per progress percent.
                                            _a.sent();
                                            return [2 /*return*/];
                                    }
                                });
                            }); })];
                    case 8:
                        // Flush queued entityAppended in chunks while advancing most of the
                        // open-file progress bar.
                        _b.sent();
                        batchOpen = false;
                        return [4 /*yield*/, emit(ENTITY_END_PCT, 'ENTITY', 'END')];
                    case 9:
                        _b.sent();
                        return [4 /*yield*/, emit(100, 'END', 'END')];
                    case 10:
                        _b.sent();
                        return [3 /*break*/, 12];
                    case 11:
                        error_1 = _b.sent();
                        if (batchOpen) {
                            db.endEventBatch();
                        }
                        throw error_1;
                    case 12: return [2 /*return*/];
                }
            });
        });
    };
    AcDbNativeDxfConverter.prototype.parse = function (_data) {
        return __awaiter(this, void 0, void 0, function () {
            return __generator(this, function (_a) {
                return [2 /*return*/, { model: null, data: { unknownEntityCount: 0 } }];
            });
        });
    };
    return AcDbNativeDxfConverter;
}(AcDbDatabaseConverter));
export { AcDbNativeDxfConverter };
//# sourceMappingURL=AcDbNativeDxfConverter.js.map