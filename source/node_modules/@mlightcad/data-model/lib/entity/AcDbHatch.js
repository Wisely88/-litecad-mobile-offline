var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcCmColor, AcCmColorMethod, AcCmColorUtil, AcCmTransparency } from '@mlightcad/common';
import { AcGeArea2d, AcGeBox3d, AcGeCircArc2d, AcGeEllipseArc2d, AcGeLine2d, AcGeLoop2d, AcGeMatrix2d, AcGePoint2d, AcGePoint3d, AcGePolyline2d, AcGeSpline3d, acgeTransformOcsPointToWcs, AcGeVector3d } from '@mlightcad/geometry-engine';
import { AcDbSystemVariables } from '../database/AcDbSystemVariables';
import { AcDbSysVarManager } from '../database/AcDbSysVarManager';
import { acdbIsSolidHatchPatternName, DEFAULT_GRADIENT_HATCH_NAME, HATCH_PATTERN_SOLID, HATCH_PATTERN_USER } from '../misc/AcDbConstants';
import { AcDbOsnapMode } from '../misc/AcDbOsnapMode';
import { AcDbPredefinedAcadIsoPat } from '../misc/pat/AcDbPatPredefined';
import { AcDbEntity } from './AcDbEntity';
import { acdbForEachGripIndex, acdbMovePolyline2dVertexAt } from './AcDbGripHelpers';
import { acdbIntersectPrimitivesFromAreaLoops } from './AcDbIntersectHelpers';
import { acdbCollectPolyline2dSegmentOsnapPoints, acdbPickNearestOsnapPoint } from './AcDbOsnapHelpers';
/**
 * Defines the type of hatch pattern.
 */
export var AcDbHatchPatternType;
(function (AcDbHatchPatternType) {
    /**
     * A user-defined pattern provides a direct method to define a simple hatch pattern using a specified
     * hatch entity linetype. The definition data for user-defined hatch pattern include angle, space and
     * double. "Angle" specifies an angle for the hatch pattern relative to the X axis of the hatch plane
     * in OCS. "Space" defines the vertical distance between two consecutive pattern lines. "Double"
     * specifies that a second set of lines is to be drawn at 90 degrees to the original lines. When
     * specifying a user-defined hatch pattern, you don't need to set the pattern name. AutoCAD designates
     * a default pattern name "U" for all user-defined patterns.
     */
    AcDbHatchPatternType[AcDbHatchPatternType["UserDefined"] = 0] = "UserDefined";
    /**
     * A predefined pattern type allows you to select a hatch pattern from the AutoCAD standard hatch
     * pattern file acad.pat in the "support" directory. The file contains many predefined hatch patterns,
     * including ANGLE, ANSI31, BRICK, CLAY, etc. When you use a predefined pattern, you can also specify
     * a scale and angle in order to modify the hatch's appearance. Solid fill is a new predefined pattern
     * type that enables the application to fill in the hatch area with a specified color. The reserved
     * name for this new pattern is "SOLID." SOLID does not appear in the file acad.pat because it has no
     * definition data. To specify a solid, use the keyword "SOLID".
     */
    AcDbHatchPatternType[AcDbHatchPatternType["Predefined"] = 1] = "Predefined";
    /**
     * A custom-defined pattern type stores the pattern in its own PAT file, in which the name of the
     * hatch pattern must match the name of the file. For instance, you must store the TEST hatch pattern
     * in a file named test.pat, and the file must be located in the ACAD search path. When you use a
     * custom-defined pattern, you can also specify a scale and angle in order to modify the hatch's
     * appearance.
     */
    AcDbHatchPatternType[AcDbHatchPatternType["Custom"] = 2] = "Custom";
})(AcDbHatchPatternType || (AcDbHatchPatternType = {}));
/**
 * Defines the hatch style for determining which areas to hatch.
 */
export var AcDbHatchStyle;
(function (AcDbHatchStyle) {
    /**
     * Normal hatch style will hatch inward from the outer loop. If it encounters an internal intersection,
     * it turns off hatching until it encounters another intersection. Thus, areas separated from the
     * outside of the hatched area by an odd number of intersections are hatched, while areas separated by
     * an even number of intersections are not.
     */
    AcDbHatchStyle[AcDbHatchStyle["Normal"] = 0] = "Normal";
    /**
     * Outer hatch style will hatch inward from the outer loop. It turns off hatching if it encounters an
     * intersection and does not turn it back on. Because this process starts from both ends of each hatch
     * line, only the outmost level of the structure is hatched, and the internal structure is left blank.
     */
    AcDbHatchStyle[AcDbHatchStyle["Outer"] = 1] = "Outer";
    /**
     * Ignore hatch style will hatch inward from the outer loop and ignores all internal loops.
     */
    AcDbHatchStyle[AcDbHatchStyle["Ignore"] = 2] = "Ignore";
})(AcDbHatchStyle || (AcDbHatchStyle = {}));
export var AcDbHatchObjectType;
(function (AcDbHatchObjectType) {
    /**
     * Indicates that the object is currently a classic hatch
     */
    AcDbHatchObjectType[AcDbHatchObjectType["HatchObject"] = 0] = "HatchObject";
    /**
     * Indicates that the object is currently a color gradient
     */
    AcDbHatchObjectType[AcDbHatchObjectType["GradientObject"] = 1] = "GradientObject";
})(AcDbHatchObjectType || (AcDbHatchObjectType = {}));
/**
 * Defines the gradient pattern type for hatch.
 */
export var AcDbGradientPatternType;
(function (AcDbGradientPatternType) {
    /**
     * Indicates that the gradient name refers to one of the predefined gradient patterns
     */
    AcDbGradientPatternType[AcDbGradientPatternType["PreDefinedGradient"] = 0] = "PreDefinedGradient";
    /**
     * Indicates that the gradient name refers to one of the user-defined gradient patterns.
     */
    AcDbGradientPatternType[AcDbGradientPatternType["UserDefinedGradient"] = 1] = "UserDefinedGradient";
})(AcDbGradientPatternType || (AcDbGradientPatternType = {}));
/**
 * Represents a hatch entity in AutoCAD.
 *
 * A hatch is a 2D geometric object that fills an area with a pattern of lines, dots, or other shapes.
 * Hatches are commonly used to represent materials, textures, or to distinguish different areas in drawings.
 *
 * @example
 * ```typescript
 * // Create a hatch entity
 * const hatch = new AcDbHatch();
 * hatch.patternName = "ANSI31";
 * hatch.patternType = AcDbHatchPatternType.Predefined;
 * hatch.patternScale = 1.0;
 * hatch.patternAngle = 0;
 * hatch.hatchStyle = AcDbHatchStyle.Normal;
 *
 * // Add a loop to define the hatch boundary
 * const loop = new AcGeLoop2d();
 * loop.add(new AcGePoint2d(0, 0));
 * loop.add(new AcGePoint2d(10, 0));
 * loop.add(new AcGePoint2d(10, 5));
 * loop.add(new AcGePoint2d(0, 5));
 * hatch.add(loop);
 *
 * // Access hatch properties
 * console.log(`Pattern name: ${hatch.patternName}`);
 * console.log(`Pattern scale: ${hatch.patternScale}`);
 * ```
 */
var AcDbHatch = /** @class */ (function (_super) {
    __extends(AcDbHatch, _super);
    /**
     * Creates a new hatch entity.
     *
     * This constructor initializes a hatch with default values.
     * The elevation is 0, pattern type is Predefined, pattern scale is 1,
     * pattern angle is 0, and hatch style is Normal.
     *
     * @example
     * ```typescript
     * const hatch = new AcDbHatch();
     * hatch.patternName = "ANSI31";
     * hatch.patternScale = 2.0;
     * ```
     */
    function AcDbHatch() {
        var _this = _super.call(this) || this;
        /**
         * Cached result of {@link buildAreasFromLoops}. Cleared when loops change
         * (`add`, `transformBy`, grip edits).
         */
        _this._areasFromLoopsCache = null;
        /** Extrusion direction / plane normal (DXF group 210). */
        _this._normal = new AcGeVector3d(0, 0, 1);
        /** The angle, in radians, at which the current gradient definition is applied. */
        _this._gradientAngle = 0;
        _this._elevation = 0;
        _this._geo = new AcGeArea2d();
        _this._isSolidFill = false;
        _this._definitionLines = [];
        _this._definitionLinesAutoGenerated = false;
        _this._definitionLinesPatternName = '';
        _this._definitionLinesPatternScale = 1;
        _this._patternName = '';
        _this._patternNameSet = false;
        _this._patternType = AcDbHatchPatternType.Predefined;
        _this._patternAngle = 0;
        _this._patternAngleSet = false;
        _this._patternScale = 1;
        _this._patternScaleSet = false;
        _this._associative = false;
        _this._associativeSet = false;
        _this._backgroundColor = undefined;
        _this._backgroundColorSet = false;
        _this._patternDouble = false;
        _this._patternDoubleSet = false;
        _this._hatchStyle = AcDbHatchStyle.Normal;
        _this._hatchStyleSet = false;
        _this._hatchObjectType = AcDbHatchObjectType.HatchObject;
        _this._gradientAngle = 0;
        _this._gradientShift = 0;
        _this._shadeTintValue = 0;
        _this._gradientStartColor = undefined;
        _this._gradientEndColor = undefined;
        _this._pixelSize = undefined;
        _this._gradientType = AcDbGradientPatternType.PreDefinedGradient;
        _this._gradientName = '';
        _this._gradientOneColorMode = false;
        return _this;
    }
    Object.defineProperty(AcDbHatch.prototype, "dxfTypeName", {
        get: function () {
            return 'HATCH';
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "isGradient", {
        /**
         * Gets whether the hatch object is currently a gradient object.
         */
        get: function () {
            return this._hatchObjectType === AcDbHatchObjectType.GradientObject;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "isHatch", {
        /**
         * Gets whether the hatch object is currently using a hatched pattern.
         */
        get: function () {
            return this._hatchObjectType === AcDbHatchObjectType.HatchObject;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "isSolidFill", {
        /**
         * Gets whether the hatch object is configured for solid fill.
         */
        get: function () {
            return (this._isSolidFill ||
                acdbIsSolidHatchPatternName(this.getEffectivePatternName()));
        },
        /**
         * Sets whether the hatch object is configured for solid fill.
         */
        set: function (value) {
            this._isSolidFill = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "color", {
        /**
         * Gets the effective hatch color.
         *
         * When the hatch does not have an explicit entity color, hatch color follows
         * HPCOLOR first and falls back to CECOLOR when HPCOLOR is unset.
         */
        get: function () {
            if (this.hasExplicitColor()) {
                return this.getEntityColor();
            }
            var db = this.database;
            var hpColor = AcDbSysVarManager.instance().getVar(AcDbSystemVariables.HPCOLOR, db);
            if (hpColor instanceof AcCmColor &&
                hpColor.colorMethod !== AcCmColorMethod.None) {
                return hpColor.clone();
            }
            return db.cecolor.clone();
        },
        /**
         * Sets an explicit hatch entity color.
         */
        set: function (value) {
            this.setEntityColor(value);
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "definitionLines", {
        /**
         * Gets the definition lines for the hatch pattern.
         *
         * @returns Array of hatch pattern lines
         *
         * @example
         * ```typescript
         * const definitionLines = hatch.definitionLines;
         * console.log(`Number of definition lines: ${definitionLines.length}`);
         * ```
         */
        get: function () {
            return this._definitionLines;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "patternName", {
        /**
         * The pattern name of this hatch.
         */
        get: function () {
            return this._patternName;
        },
        set: function (value) {
            this._patternName = value !== null && value !== void 0 ? value : '';
            this._isSolidFill = acdbIsSolidHatchPatternName(this._patternName);
            this._patternNameSet = true;
            this.updatePredefinedPatternDefinitionLines();
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "patternType", {
        /**
         * The pattern name of this hatch.
         */
        get: function () {
            return this._patternType;
        },
        set: function (value) {
            this._patternType = value;
            if (value !== AcDbHatchPatternType.Predefined &&
                this._definitionLinesAutoGenerated) {
                this._definitionLines = [];
                this._definitionLinesAutoGenerated = false;
                this._definitionLinesPatternName = '';
                this._definitionLinesPatternScale = 1;
            }
            this.updatePredefinedPatternDefinitionLines();
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "patternAngle", {
        /**
         * The pattern angle (in radians) of this hatch.
         */
        get: function () {
            return this._patternAngle;
        },
        set: function (value) {
            this._patternAngle = value;
            this._patternAngleSet = true;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "patternScale", {
        /**
         * The pattern scale of the hatch entity. It is a non-zero positive number.
         */
        get: function () {
            return this._patternScale;
        },
        set: function (value) {
            this._patternScale = value;
            this._patternScaleSet = true;
            this.updatePredefinedPatternDefinitionLines();
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "associative", {
        /**
         * Indicates whether this hatch is associative with its defining boundary objects.
         */
        get: function () {
            return this._associative;
        },
        set: function (value) {
            this._associative = !!value;
            this._associativeSet = true;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "backgroundColor", {
        /**
         * Optional background color of this hatch pattern.
         */
        get: function () {
            return this._backgroundColor;
        },
        set: function (value) {
            this._backgroundColor = value === null || value === void 0 ? void 0 : value.clone();
            this._backgroundColorSet = true;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "patternDouble", {
        /**
         * Indicates whether user-defined hatch pattern doubling is enabled.
         */
        get: function () {
            return this._patternDouble;
        },
        set: function (value) {
            this._patternDouble = !!value;
            this._patternDoubleSet = true;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "hatchStyle", {
        /**
         * The pattern style of the hatch entity.
         */
        get: function () {
            return this._hatchStyle;
        },
        set: function (value) {
            this._hatchStyle = value;
            this._hatchStyleSet = true;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "elevation", {
        /**
         * The elevation (Z-coordinate) of the hatch plane.
         */
        get: function () {
            return this._elevation;
        },
        set: function (value) {
            this._elevation = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "normal", {
        /**
         * Extrusion direction / plane normal (DXF group 210).
         */
        get: function () {
            return this._normal;
        },
        set: function (value) {
            this._normal.copy(value);
            if (this._normal.lengthSq() > 0) {
                this._normal.normalize();
            }
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "hatchObjectType", {
        /**
         * Gets the current state of the gradient object.
         */
        get: function () {
            return this._hatchObjectType;
        },
        /**
         * Sets the current state of the gradient object.
         */
        set: function (value) {
            this._hatchObjectType = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "gradientAngle", {
        /**
         * Gets the angle, in radians, at which the current gradient definition is applied.
         */
        get: function () {
            return this._gradientAngle;
        },
        /**
         * Sets the angle, in radians, at which the current gradient definition is applied.
         */
        set: function (value) {
            this._gradientAngle = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "gradientShift", {
        /**
         * Gets the current interpolation value between the gradient definition's default and shifted
         * values. The default is 0.0f.
         */
        get: function () {
            return this._gradientShift;
        },
        /**
         * Sets the current interpolation value between the gradient definition's default and shifted
         * values. The default is 0.0f.
         */
        set: function (value) {
            this._gradientShift = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "gradientType", {
        /**
         * Gets the type of the gradient pattern.
         */
        get: function () {
            return this._gradientType;
        },
        /**
         * Sets the type of the gradient pattern.
         */
        set: function (value) {
            this._gradientType = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "gradientName", {
        /**
         * Gets the name of the current gradient.
         */
        get: function () {
            if (this.isGradient && this._gradientName.trim() === '') {
                return DEFAULT_GRADIENT_HATCH_NAME;
            }
            return this._gradientName;
        },
        /**
         * Sets the name of the current gradient.
         */
        set: function (value) {
            this._gradientName = value !== null && value !== void 0 ? value : '';
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "gradientOneColorMode", {
        /**
         * Gets whether the gradient hatch is transitioning from a start to a stop color (two-color).
         */
        get: function () {
            return this._gradientOneColorMode;
        },
        /**
         * Sets whether the gradient hatch is transitioning from a start to a stop color (two-color)
         * or from a color to an adjusted luminance version of the same color (one-color). In the latter
         * case, the full luminance version is the "tint" and the zero luminance version is the "shade."
         */
        set: function (value) {
            this._gradientOneColorMode = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "shadeTintValue", {
        /**
         * Gets the one-color tint shade (luminance) value.
         */
        get: function () {
            return this._shadeTintValue;
        },
        /**
         * Sets the one-color tint shade (luminance) value. If the gradient is using one-color mode,
         * this function sets the luminance value applied to the first color.
         */
        set: function (value) {
            this._shadeTintValue = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "gradientStartColor", {
        /**
         * Gets the optional first gradient color as a packed RGB value.
         */
        get: function () {
            return this._gradientStartColor;
        },
        /**
         * Sets the optional first gradient color as a packed RGB value.
         */
        set: function (value) {
            this._gradientStartColor =
                value == null || !Number.isFinite(value) ? undefined : value & 0xffffff;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "gradientEndColor", {
        /**
         * Gets the optional second gradient color as a packed RGB value.
         */
        get: function () {
            return this._gradientEndColor;
        },
        /**
         * Sets the optional second gradient color as a packed RGB value.
         */
        set: function (value) {
            this._gradientEndColor =
                value == null || !Number.isFinite(value) ? undefined : value & 0xffffff;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "pixelSize", {
        /**
         * Gets the pixel size used for intersection/ray-casting density
         * calculations (DXF group 47), for associative or Flood-method hatches.
         */
        get: function () {
            return this._pixelSize;
        },
        /**
         * Sets the pixel size used for intersection/ray-casting density
         * calculations (DXF group 47).
         */
        set: function (value) {
            this._pixelSize = value;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Applies the current hatch-related system variables as persistent defaults
     * when this hatch is first added to a database.
     *
     * Imported hatches and programmatically assigned hatch values keep their own
     * explicit settings.
     *
     * @internal
     */
    AcDbHatch.prototype.applyPatternDefaultsFromSysVars = function (db) {
        var manager = AcDbSysVarManager.instance();
        if (!this.hasExplicitLayer()) {
            var layer = manager.getVar(AcDbSystemVariables.HPLAYER, db);
            if (this.shouldUseSysVarOverride(layer)) {
                this.layer = layer;
            }
        }
        if (!this.hasExplicitTransparency()) {
            var transparency = this.parseHpTransparency(manager.getVar(AcDbSystemVariables.HPTRANSPARENCY, db));
            if (transparency) {
                this.transparency = transparency;
            }
        }
        if (!this._backgroundColorSet) {
            this._backgroundColor = this.parseHpBackgroundColor(manager.getVar(AcDbSystemVariables.HPBACKGROUNDCOLOR, db));
            this._backgroundColorSet = true;
        }
        if (!this._patternNameSet) {
            var patternName = manager.getVar(AcDbSystemVariables.HPNAME, db);
            this._patternName = typeof patternName === 'string' ? patternName : '';
            this._patternNameSet = true;
        }
        if (!this._patternAngleSet) {
            var patternAngle = manager.getVar(AcDbSystemVariables.HPANG, db);
            this._patternAngle =
                typeof patternAngle === 'number' && Number.isFinite(patternAngle)
                    ? patternAngle
                    : 0;
            this._patternAngleSet = true;
        }
        if (!this._patternScaleSet) {
            this._patternScale = this.normalizePatternScale(manager.getVar(AcDbSystemVariables.HPSCALE, db));
            this._patternScaleSet = true;
        }
        if (!this._associativeSet) {
            var hpAssoc = manager.getVar(AcDbSystemVariables.HPASSOC, db);
            this._associative = typeof hpAssoc === 'number' ? hpAssoc !== 0 : false;
            this._associativeSet = true;
        }
        if (!this._hatchStyleSet) {
            this._hatchStyle = this.normalizeHatchStyle(manager.getVar(AcDbSystemVariables.HPISLANDDETECTION, db));
            this._hatchStyleSet = true;
        }
        if (!this._patternDoubleSet) {
            var hpDouble = manager.getVar(AcDbSystemVariables.HPDOUBLE, db);
            this._patternDouble =
                typeof hpDouble === 'number' ? hpDouble !== 0 : false;
            this._patternDoubleSet = true;
        }
        this.updatePredefinedPatternDefinitionLines();
    };
    AcDbHatch.prototype.getEffectivePatternName = function () {
        if (this._patternNameSet) {
            return this._patternName;
        }
        var db = this.database;
        var value = AcDbSysVarManager.instance().getVar(AcDbSystemVariables.HPNAME, db);
        return typeof value === 'string' ? value : this._patternName;
    };
    AcDbHatch.prototype.shouldResolveColorFromCecolor = function () {
        return false;
    };
    AcDbHatch.prototype.getEffectivePatternAngle = function () {
        if (this._patternAngleSet) {
            return this._patternAngle;
        }
        var db = this.database;
        var value = AcDbSysVarManager.instance().getVar(AcDbSystemVariables.HPANG, db);
        return typeof value === 'number' && Number.isFinite(value)
            ? value
            : this._patternAngle;
    };
    /**
     * Returns the pattern angle consumed by renderers.
     *
     * Explicit definition lines from DXF (or assigned directly) already include
     * pattern rotation in their angles and offsets. Only lines expanded from
     * bundled PAT entries need the entity-level pattern angle applied again.
     */
    AcDbHatch.prototype.getRenderablePatternAngle = function () {
        if (!this._definitionLinesAutoGenerated &&
            this._definitionLines.length > 0) {
            return 0;
        }
        return this.getEffectivePatternAngle();
    };
    AcDbHatch.prototype.getEffectivePatternScale = function () {
        if (this._patternScaleSet) {
            return this._patternScale;
        }
        var db = this.database;
        return this.normalizePatternScale(AcDbSysVarManager.instance().getVar(AcDbSystemVariables.HPSCALE, db));
    };
    AcDbHatch.prototype.getEffectiveAssociative = function () {
        if (this._associativeSet) {
            return this._associative;
        }
        var db = this.database;
        var value = AcDbSysVarManager.instance().getVar(AcDbSystemVariables.HPASSOC, db);
        return typeof value === 'number' ? value !== 0 : this._associative;
    };
    AcDbHatch.prototype.normalizePatternScale = function (value) {
        return typeof value === 'number' && Number.isFinite(value) && value > 0
            ? value
            : 1;
    };
    AcDbHatch.prototype.normalizeHatchStyle = function (value) {
        if (typeof value !== 'number' || !Number.isFinite(value)) {
            return AcDbHatchStyle.Outer;
        }
        if (value === AcDbHatchStyle.Normal)
            return AcDbHatchStyle.Normal;
        if (value === AcDbHatchStyle.Ignore)
            return AcDbHatchStyle.Ignore;
        return AcDbHatchStyle.Outer;
    };
    AcDbHatch.prototype.shouldUseSysVarOverride = function (value) {
        if (typeof value !== 'string')
            return false;
        var normalized = value.trim().toLowerCase();
        return (normalized !== '' && normalized !== '.' && normalized !== 'use current');
    };
    AcDbHatch.prototype.parseHpBackgroundColor = function (value) {
        if (value instanceof AcCmColor) {
            return value.colorMethod === AcCmColorMethod.None
                ? undefined
                : value.clone();
        }
        if (typeof value !== 'string')
            return undefined;
        var normalized = value.trim().toLowerCase();
        if (!normalized || normalized === '.' || normalized === 'none') {
            return undefined;
        }
        return AcCmColor.fromString(value);
    };
    AcDbHatch.prototype.parseHpTransparency = function (value) {
        if (value instanceof AcCmTransparency) {
            return value.clone();
        }
        if (!this.shouldUseSysVarOverride(value)) {
            return undefined;
        }
        var text = value.trim();
        if (/^bylayer$/i.test(text) || /^byblock$/i.test(text)) {
            return AcCmTransparency.fromString(text);
        }
        var percentage = Number(text);
        if (Number.isInteger(percentage) && percentage >= 0 && percentage <= 90) {
            var transparency = new AcCmTransparency();
            transparency.percentage = percentage;
            return transparency;
        }
        return undefined;
    };
    /**
     * Populates renderable pattern definition lines for predefined hatch names.
     *
     * `patternName`, `patternScale`, and `patternType` are the persistent hatch
     * metadata. Renderers consume `definitionLines`, so newly-created predefined
     * hatches need those lines expanded from the bundled PAT library. Imported
     * files usually already contain explicit definition lines; those are kept
     * intact unless they were previously auto-generated by this object.
     */
    AcDbHatch.prototype.updatePredefinedPatternDefinitionLines = function (patternName, patternScale) {
        if (patternName === void 0) { patternName = this.getEffectivePatternName(); }
        if (patternScale === void 0) { patternScale = this.getEffectivePatternScale(); }
        if (this.patternType !== AcDbHatchPatternType.Predefined)
            return;
        var canReplace = this._definitionLinesAutoGenerated || this._definitionLines.length === 0;
        if (!canReplace)
            return;
        var normalizedPatternName = patternName.trim().toUpperCase();
        if (!normalizedPatternName ||
            acdbIsSolidHatchPatternName(normalizedPatternName)) {
            this._definitionLines = [];
            this._definitionLinesAutoGenerated = false;
            this._definitionLinesPatternName = '';
            this._definitionLinesPatternScale = 1;
            return;
        }
        var scale = this.normalizePatternScale(patternScale);
        if (this._definitionLinesAutoGenerated &&
            this._definitionLinesPatternName === normalizedPatternName &&
            this._definitionLinesPatternScale === scale) {
            return;
        }
        var pattern = AcDbPredefinedAcadIsoPat.patterns.find(function (item) { return item.name.trim().toUpperCase() === normalizedPatternName; });
        if (!pattern) {
            if (this._definitionLinesAutoGenerated) {
                this._definitionLines = [];
                this._definitionLinesAutoGenerated = false;
                this._definitionLinesPatternName = '';
                this._definitionLinesPatternScale = 1;
            }
            return;
        }
        this._definitionLines = pattern.lines.map(function (line) { return ({
            angle: (line.angle * Math.PI) / 180,
            base: {
                x: line.originX * scale,
                y: line.originY * scale
            },
            offset: {
                x: line.deltaX * scale,
                y: line.deltaY * scale
            },
            dashLengths: line.dashes.map(function (dash) { return dash * scale; })
        }); });
        this._definitionLinesAutoGenerated = true;
        this._definitionLinesPatternName = normalizedPatternName;
        this._definitionLinesPatternScale = scale;
    };
    /**
     * Append one loop to loops of this area. If it is the first loop added, it is the outter loop.
     * Otherwise, it is an inner loop.
     * @param loop Input the loop to append
     */
    AcDbHatch.prototype.add = function (loop) {
        this._geo.add(loop);
        this.invalidateAreasFromLoopsCache();
    };
    AcDbHatch.prototype.invalidateAreasFromLoopsCache = function () {
        this._areasFromLoopsCache = null;
    };
    AcDbHatch.prototype.buildAreasFromLoops = function () {
        if (this._areasFromLoopsCache != null) {
            return this._areasFromLoopsCache;
        }
        var loops = this._geo.loops;
        if (loops.length === 0) {
            this._areasFromLoopsCache = [];
            return this._areasFromLoopsCache;
        }
        if (loops.length === 1) {
            this._areasFromLoopsCache = [this._geo];
            return this._areasFromLoopsCache;
        }
        var hierarchy = this._geo.buildHierarchy();
        var areas = [];
        var visit = function (node, depth) {
            if (node.index >= 0 && depth % 2 === 0) {
                var area_1 = new AcGeArea2d();
                area_1.add(loops[node.index]);
                node.children.forEach(function (child) {
                    if (child.index >= 0) {
                        area_1.add(loops[child.index]);
                    }
                });
                areas.push(area_1);
            }
            node.children.forEach(function (child) { return visit(child, depth + 1); });
        };
        hierarchy.children.forEach(function (child) { return visit(child, 0); });
        this._areasFromLoopsCache = areas.length > 0 ? areas : [this._geo];
        return this._areasFromLoopsCache;
    };
    AcDbHatch.prototype.getCalculatedAreaValue = function () {
        var areas = this.buildAreasFromLoops();
        if (areas.length === 0)
            return 0;
        return areas.reduce(function (sum, area) { return sum + area.area; }, 0);
    };
    Object.defineProperty(AcDbHatch.prototype, "area", {
        /**
         * The area enclosed by this hatch, including holes.
         */
        get: function () {
            return this.getCalculatedAreaValue();
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbHatch.prototype, "geometricExtents", {
        /**
         * @inheritdoc
         */
        get: function () {
            var _this = this;
            var areas = this.buildAreasFromLoops();
            if (areas.length === 0) {
                return new AcGeBox3d();
            }
            var extents = new AcGeBox3d();
            areas.forEach(function (area) {
                var box = area.box;
                extents.union(new AcGeBox3d().setFromPoints([
                    acgeTransformOcsPointToWcs({ x: box.min.x, y: box.min.y, z: _this._elevation }, _this._normal),
                    acgeTransformOcsPointToWcs({ x: box.max.x, y: box.min.y, z: _this._elevation }, _this._normal),
                    acgeTransformOcsPointToWcs({ x: box.max.x, y: box.max.y, z: _this._elevation }, _this._normal),
                    acgeTransformOcsPointToWcs({ x: box.min.x, y: box.max.y, z: _this._elevation }, _this._normal)
                ]));
            });
            return extents;
        },
        enumerable: false,
        configurable: true
    });
    /** @inheritdoc */
    AcDbHatch.prototype.subGetIntersectCurves = function () {
        return acdbIntersectPrimitivesFromAreaLoops(this._geo.loops, this._elevation, this._normal);
    };
    Object.defineProperty(AcDbHatch.prototype, "properties", {
        /**
         * Returns the full property definition for this hatch entity, including
         * general group, pattern group, and geometry group.
         *
         * The geometry group exposes editable start/end coordinates via
         * {@link AcDbPropertyAccessor} so the property palette can update
         * the hatch in real-time.
         *
         * Each property is an {@link AcDbEntityRuntimeProperty}.
         */
        get: function () {
            var _this = this;
            return {
                type: this.type,
                groups: [
                    this.getGeneralProperties(),
                    {
                        groupName: 'pattern',
                        properties: [
                            {
                                name: 'patternType',
                                type: 'enum',
                                editable: true,
                                options: [
                                    { label: AcDbHatchPatternType[0], value: 0 },
                                    { label: AcDbHatchPatternType[1], value: 1 },
                                    { label: AcDbHatchPatternType[2], value: 2 }
                                ],
                                accessor: {
                                    get: function () { return _this.patternType; },
                                    set: function (v) {
                                        _this.patternType = v;
                                    }
                                }
                            },
                            {
                                name: 'patternName',
                                type: 'string',
                                editable: true,
                                accessor: {
                                    get: function () { return _this.patternName; },
                                    set: function (v) {
                                        _this.patternName = v;
                                    }
                                }
                            },
                            {
                                name: 'patternAngle',
                                type: 'float',
                                editable: true,
                                accessor: {
                                    get: function () { return _this.patternAngle; },
                                    set: function (v) {
                                        _this.patternAngle = v;
                                    }
                                }
                            },
                            {
                                name: 'patternScale',
                                type: 'float',
                                editable: true,
                                accessor: {
                                    get: function () { return _this.patternScale; },
                                    set: function (v) {
                                        _this.patternScale = v;
                                    }
                                }
                            },
                            {
                                name: 'associative',
                                type: 'boolean',
                                editable: true,
                                accessor: {
                                    get: function () { return _this.associative; },
                                    set: function (v) {
                                        _this.associative = v;
                                    }
                                }
                            },
                            {
                                name: 'backgroundColor',
                                type: 'color',
                                editable: true,
                                accessor: {
                                    get: function () { return _this.backgroundColor; },
                                    set: function (v) {
                                        _this.backgroundColor = v;
                                    }
                                }
                            },
                            {
                                name: 'patternDouble',
                                type: 'boolean',
                                editable: true,
                                accessor: {
                                    get: function () { return _this.patternDouble; },
                                    set: function (v) {
                                        _this.patternDouble = v;
                                    }
                                }
                            }
                        ]
                    },
                    {
                        groupName: 'geometry',
                        properties: [
                            {
                                name: 'elevation',
                                type: 'float',
                                editable: true,
                                accessor: {
                                    get: function () { return _this.elevation; },
                                    set: function (v) {
                                        _this.elevation = v;
                                    }
                                }
                            },
                            {
                                name: 'area',
                                type: 'float',
                                editable: false,
                                accessor: {
                                    get: function () { return _this.area; }
                                }
                            }
                        ]
                    }
                ]
            };
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Gets the grip points for this hatch.
     *
     * Associative hatches do not expose editable grips in AutoCAD. Non-associative
     * hatches return boundary definition points from all loops.
     *
     * @returns Array of grip points as 3D points
     */
    AcDbHatch.prototype.subGetGripPoints = function () {
        if (this.associative) {
            return [];
        }
        return this.collectBoundaryGripPoints();
    };
    /** @inheritdoc */
    AcDbHatch.prototype.subMoveGripPointsAt = function (indices, offset) {
        var _this = this;
        if (this.associative) {
            return this;
        }
        acdbForEachGripIndex(indices, function (index) {
            _this.moveBoundaryGripPointAt(index, offset);
        });
        this.invalidateAreasFromLoopsCache();
        return this;
    };
    /**
     * Collects editable grip points from hatch boundary loops.
     */
    AcDbHatch.prototype.collectBoundaryGripPoints = function () {
        var gripPoints = new Array();
        var elevation = this._elevation;
        var seen = new Set();
        var appendPoint = function (point) {
            var _a, _b;
            var key = "".concat(point.x, ",").concat(point.y, ",").concat((_a = point.z) !== null && _a !== void 0 ? _a : 0);
            if (seen.has(key))
                return;
            seen.add(key);
            gripPoints.push(new AcGePoint3d(point.x, point.y, (_b = point.z) !== null && _b !== void 0 ? _b : elevation));
        };
        this._geo.loops.forEach(function (loop) {
            if (loop instanceof AcGePolyline2d) {
                var vertexCount = loop.numberOfVertices;
                for (var index = 0; index < vertexCount; index++) {
                    var vertex = loop.getPointAt(index);
                    appendPoint({ x: vertex.x, y: vertex.y, z: elevation });
                }
                return;
            }
            loop.curves.forEach(function (curve) {
                if (curve instanceof AcGeLine2d) {
                    appendPoint({
                        x: curve.startPoint.x,
                        y: curve.startPoint.y,
                        z: elevation
                    });
                    appendPoint({
                        x: curve.endPoint.x,
                        y: curve.endPoint.y,
                        z: elevation
                    });
                }
                else if (curve instanceof AcGeCircArc2d) {
                    appendPoint({
                        x: curve.startPoint.x,
                        y: curve.startPoint.y,
                        z: elevation
                    });
                    appendPoint({
                        x: curve.endPoint.x,
                        y: curve.endPoint.y,
                        z: elevation
                    });
                    appendPoint({
                        x: curve.midPoint.x,
                        y: curve.midPoint.y,
                        z: elevation
                    });
                }
                else if (curve instanceof AcGeEllipseArc2d) {
                    appendPoint({
                        x: curve.startPoint.x,
                        y: curve.startPoint.y,
                        z: elevation
                    });
                    appendPoint({
                        x: curve.endPoint.x,
                        y: curve.endPoint.y,
                        z: elevation
                    });
                    var mid = curve.getPoint(0.5);
                    appendPoint({ x: mid.x, y: mid.y, z: elevation });
                }
            });
        });
        return gripPoints;
    };
    /**
     * Moves one boundary grip point using the same index order as
     * {@link collectBoundaryGripPoints}.
     */
    AcDbHatch.prototype.moveBoundaryGripPointAt = function (gripIndex, offset) {
        var _this = this;
        var currentIndex = 0;
        var elevation = this._elevation;
        var seen = new Set();
        var tryMove = function (point, move) {
            var _a;
            var key = "".concat(point.x, ",").concat(point.y, ",").concat((_a = point.z) !== null && _a !== void 0 ? _a : 0);
            if (seen.has(key))
                return;
            seen.add(key);
            if (currentIndex === gripIndex) {
                move();
            }
            currentIndex++;
        };
        this._geo.loops.forEach(function (loop) {
            if (loop instanceof AcGePolyline2d) {
                var vertexCount = loop.numberOfVertices;
                var _loop_1 = function (index) {
                    var vertex = loop.vertices[index];
                    if (!vertex)
                        return "continue";
                    tryMove({ x: vertex.x, y: vertex.y, z: elevation }, function () {
                        acdbMovePolyline2dVertexAt(loop.vertices, index, offset);
                    });
                };
                for (var index = 0; index < vertexCount; index++) {
                    _loop_1(index);
                }
                return;
            }
            loop.curves.forEach(function (curve) {
                if (curve instanceof AcGeLine2d) {
                    tryMove({ x: curve.startPoint.x, y: curve.startPoint.y, z: elevation }, function () {
                        curve.startPoint.x += offset.x;
                        curve.startPoint.y += offset.y;
                    });
                    tryMove({ x: curve.endPoint.x, y: curve.endPoint.y, z: elevation }, function () {
                        curve.endPoint.x += offset.x;
                        curve.endPoint.y += offset.y;
                    });
                }
                else if (curve instanceof AcGeCircArc2d) {
                    tryMove({ x: curve.startPoint.x, y: curve.startPoint.y, z: elevation }, function () { return _this.moveCircArc2dGripPoint(curve, 'start', offset); });
                    tryMove({ x: curve.endPoint.x, y: curve.endPoint.y, z: elevation }, function () { return _this.moveCircArc2dGripPoint(curve, 'end', offset); });
                    tryMove({ x: curve.midPoint.x, y: curve.midPoint.y, z: elevation }, function () { return _this.moveCircArc2dGripPoint(curve, 'mid', offset); });
                }
                else if (curve instanceof AcGeEllipseArc2d) {
                    tryMove({ x: curve.startPoint.x, y: curve.startPoint.y, z: elevation }, function () { return _this.moveEllipseArc2dGripPoint(curve, 'start', offset); });
                    tryMove({ x: curve.endPoint.x, y: curve.endPoint.y, z: elevation }, function () { return _this.moveEllipseArc2dGripPoint(curve, 'end', offset); });
                    var mid = curve.getPoint(0.5);
                    tryMove({ x: mid.x, y: mid.y, z: elevation }, function () {
                        return _this.moveEllipseArc2dGripPoint(curve, 'mid', offset);
                    });
                }
            });
        });
    };
    AcDbHatch.prototype.moveCircArc2dGripPoint = function (arc, which, offset) {
        var center = arc.center;
        switch (which) {
            case 'start': {
                var point = arc.startPoint;
                arc.startAngle = Math.atan2(point.y + offset.y - center.y, point.x + offset.x - center.x);
                break;
            }
            case 'end': {
                var point = arc.endPoint;
                arc.endAngle = Math.atan2(point.y + offset.y - center.y, point.x + offset.x - center.x);
                break;
            }
            case 'mid': {
                var mid = arc.midPoint;
                var movedArc = new AcGeCircArc2d(arc.startPoint, { x: mid.x + offset.x, y: mid.y + offset.y }, arc.endPoint);
                arc.center = movedArc.center;
                arc.radius = movedArc.radius;
                arc.startAngle = movedArc.startAngle;
                arc.endAngle = movedArc.endAngle;
                arc.clockwise = movedArc.clockwise;
                break;
            }
        }
    };
    AcDbHatch.prototype.moveEllipseArc2dGripPoint = function (arc, which, offset) {
        var center = arc.center;
        switch (which) {
            case 'start': {
                var point = arc.startPoint;
                arc.startAngle = Math.atan2(point.y + offset.y - center.y, point.x + offset.x - center.x);
                break;
            }
            case 'end': {
                var point = arc.endPoint;
                arc.endAngle = Math.atan2(point.y + offset.y - center.y, point.x + offset.x - center.x);
                break;
            }
            case 'mid': {
                var mid = arc.getPoint(0.5);
                var dx = mid.x + offset.x - arc.center.x;
                var dy = mid.y + offset.y - arc.center.y;
                var currentRadius = Math.hypot(mid.x - arc.center.x, mid.y - arc.center.y);
                var targetRadius = Math.hypot(dx, dy);
                if (currentRadius > 0 && targetRadius > 0) {
                    var scale = targetRadius / currentRadius;
                    arc.majorAxisRadius *= scale;
                    arc.minorAxisRadius *= scale;
                }
                break;
            }
        }
    };
    /**
     * Gets the object snap points for hatch boundary geometry.
     */
    AcDbHatch.prototype.subGetOsnapPoints = function (osnapMode, pickPoint, _lastPoint, snapPoints) {
        var elevation = this._elevation;
        var nearestCandidates = [];
        this._geo.loops.forEach(function (loop) {
            var _a;
            if (loop instanceof AcGePolyline2d) {
                var geo = loop;
                var vertexCount = geo.numberOfVertices;
                if (vertexCount === 0)
                    return;
                switch (osnapMode) {
                    case AcDbOsnapMode.EndPoint:
                        for (var index = 0; index < vertexCount; index++) {
                            var vertex = geo.getPointAt(index);
                            snapPoints.push(new AcGePoint3d(vertex.x, vertex.y, elevation));
                        }
                        break;
                    case AcDbOsnapMode.MidPoint:
                    case AcDbOsnapMode.Nearest:
                    case AcDbOsnapMode.Perpendicular:
                    case AcDbOsnapMode.Tangent: {
                        var segmentCount = loop.closed ? vertexCount : vertexCount - 1;
                        for (var index = 0; index < segmentCount; index++) {
                            var segmentSnaps = [];
                            acdbCollectPolyline2dSegmentOsnapPoints(geo.getPointAt(index), geo.getPointAt((index + 1) % vertexCount), (_a = geo.vertices[index]) === null || _a === void 0 ? void 0 : _a.bulge, elevation, osnapMode, pickPoint, segmentSnaps);
                            if (osnapMode === AcDbOsnapMode.MidPoint) {
                                snapPoints.push.apply(snapPoints, __spreadArray([], __read(segmentSnaps), false));
                            }
                            else {
                                nearestCandidates.push.apply(nearestCandidates, __spreadArray([], __read(segmentSnaps), false));
                            }
                        }
                        break;
                    }
                    default:
                        break;
                }
                return;
            }
            loop.curves.forEach(function (curve) {
                if (curve instanceof AcGeLine2d) {
                    var segmentSnaps = [];
                    acdbCollectPolyline2dSegmentOsnapPoints(curve.startPoint, curve.endPoint, undefined, elevation, osnapMode, pickPoint, segmentSnaps);
                    if (osnapMode === AcDbOsnapMode.MidPoint) {
                        snapPoints.push.apply(snapPoints, __spreadArray([], __read(segmentSnaps), false));
                    }
                    else if (osnapMode === AcDbOsnapMode.Nearest ||
                        osnapMode === AcDbOsnapMode.Perpendicular ||
                        osnapMode === AcDbOsnapMode.Tangent) {
                        nearestCandidates.push.apply(nearestCandidates, __spreadArray([], __read(segmentSnaps), false));
                    }
                    else {
                        snapPoints.push.apply(snapPoints, __spreadArray([], __read(segmentSnaps), false));
                    }
                }
                else if (curve instanceof AcGeCircArc2d) {
                    switch (osnapMode) {
                        case AcDbOsnapMode.EndPoint:
                            snapPoints.push(new AcGePoint3d(curve.startPoint.x, curve.startPoint.y, elevation), new AcGePoint3d(curve.endPoint.x, curve.endPoint.y, elevation));
                            break;
                        case AcDbOsnapMode.MidPoint:
                            snapPoints.push(new AcGePoint3d(curve.midPoint.x, curve.midPoint.y, elevation));
                            break;
                        case AcDbOsnapMode.Nearest: {
                            var nearest = curve.nearestPoint({
                                x: pickPoint.x,
                                y: pickPoint.y
                            });
                            nearestCandidates.push(new AcGePoint3d(nearest.x, nearest.y, elevation));
                            break;
                        }
                        case AcDbOsnapMode.Perpendicular: {
                            var perpPoints = curve.perpendicularPoints({
                                x: pickPoint.x,
                                y: pickPoint.y
                            });
                            perpPoints.forEach(function (point) {
                                return nearestCandidates.push(new AcGePoint3d(point.x, point.y, elevation));
                            });
                            break;
                        }
                        case AcDbOsnapMode.Tangent: {
                            var tangentPoints = curve.tangentPoints({
                                x: pickPoint.x,
                                y: pickPoint.y
                            });
                            tangentPoints.forEach(function (point) {
                                return nearestCandidates.push(new AcGePoint3d(point.x, point.y, elevation));
                            });
                            break;
                        }
                        default:
                            break;
                    }
                }
                else if (curve instanceof AcGeEllipseArc2d) {
                    switch (osnapMode) {
                        case AcDbOsnapMode.EndPoint:
                            snapPoints.push(new AcGePoint3d(curve.startPoint.x, curve.startPoint.y, elevation), new AcGePoint3d(curve.endPoint.x, curve.endPoint.y, elevation));
                            break;
                        case AcDbOsnapMode.MidPoint: {
                            var mid = curve.getPoint(0.5);
                            snapPoints.push(new AcGePoint3d(mid.x, mid.y, elevation));
                            break;
                        }
                        case AcDbOsnapMode.Nearest: {
                            var nearest = curve.nearestPoint({
                                x: pickPoint.x,
                                y: pickPoint.y
                            });
                            nearestCandidates.push(new AcGePoint3d(nearest.x, nearest.y, elevation));
                            break;
                        }
                        case AcDbOsnapMode.Perpendicular: {
                            var perpPoints = curve.perpendicularPoints({
                                x: pickPoint.x,
                                y: pickPoint.y
                            });
                            perpPoints.forEach(function (point) {
                                return nearestCandidates.push(new AcGePoint3d(point.x, point.y, elevation));
                            });
                            break;
                        }
                        case AcDbOsnapMode.Tangent: {
                            var tangentPoints = curve.tangentPoints({
                                x: pickPoint.x,
                                y: pickPoint.y
                            });
                            tangentPoints.forEach(function (point) {
                                return nearestCandidates.push(new AcGePoint3d(point.x, point.y, elevation));
                            });
                            break;
                        }
                        default:
                            break;
                    }
                }
            });
        });
        if ((osnapMode === AcDbOsnapMode.Nearest ||
            osnapMode === AcDbOsnapMode.Perpendicular ||
            osnapMode === AcDbOsnapMode.Tangent) &&
            nearestCandidates.length > 0) {
            var nearest = acdbPickNearestOsnapPoint(pickPoint, nearestCandidates);
            snapPoints.length = 0;
            if (nearest)
                snapPoints.push(nearest);
        }
    };
    Object.defineProperty(AcDbHatch.prototype, "directBatchPrimitive", {
        /**
         * Solid (non-gradient) hatches that draw as a single `area` call.
         * Multi-loop hatches that become a `group` of areas stay on the legacy path.
         *
         * @internal
         */
        get: function () {
            if (!this.isSolidFill || this.isGradient) {
                return null;
            }
            var areas = this.buildAreasFromLoops();
            if (areas.length > 1) {
                return null;
            }
            return 'area';
        },
        enumerable: false,
        configurable: true
    });
    /**
     * @inheritdoc
     */
    AcDbHatch.prototype.subWorldDraw = function (renderer) {
        var _a;
        this.updatePredefinedPatternDefinitionLines();
        var traits = renderer.subEntityTraits;
        traits.fillType = {
            solidFill: this.isSolidFill,
            patternAngle: this.getRenderablePatternAngle(),
            definitionLines: this.definitionLines,
            backgroundColor: (_a = this.backgroundColor) === null || _a === void 0 ? void 0 : _a.clone(),
            gradient: this.isGradient
                ? {
                    name: this.gradientName,
                    angle: this.gradientAngle,
                    shift: this.gradientShift,
                    oneColorMode: this.gradientOneColorMode,
                    shadeTintValue: this.shadeTintValue,
                    startColor: this.gradientStartColor,
                    endColor: this.gradientEndColor
                }
                : undefined
        };
        traits.drawOrder = -1;
        var areas = this.buildAreasFromLoops();
        if (areas.length === 0) {
            return renderer.area(this._geo);
        }
        if (areas.length === 1) {
            return renderer.area(areas[0]);
        }
        var entities = areas.map(function (area) { return renderer.area(area); });
        return renderer.group(entities);
    };
    /**
     * Transforms this hatch by the specified matrix.
     */
    AcDbHatch.prototype.transformBy = function (matrix) {
        var te = matrix.elements;
        var matrix2d = new AcGeMatrix2d(te[0], te[4], te[12], te[1], te[5], te[13], 0, 0, 1);
        this._geo.transform(matrix2d);
        this.invalidateAreasFromLoopsCache();
        this._elevation = new AcGePoint3d(0, 0, this._elevation).applyMatrix4(matrix).z;
        this._normal.transformDirection(matrix);
        if (this._normal.lengthSq() > 0) {
            this._normal.normalize();
        }
        var xAxis = new AcGePoint3d(1, 0, 0).applyMatrix4(matrix);
        var origin = new AcGePoint3d().applyMatrix4(matrix);
        var xVector = new AcGePoint3d(xAxis).sub(origin);
        if (xVector.length() > 0) {
            var rotation = Math.atan2(xVector.y, xVector.x);
            this._patternAngle = this.getEffectivePatternAngle() + rotation;
            this._gradientAngle += rotation;
            this._patternScale = this.getEffectivePatternScale() * xVector.length();
            this._patternAngleSet = true;
            this._patternScaleSet = true;
            this.updatePredefinedPatternDefinitionLines();
        }
        return this;
    };
    /**
     * Writes DXF fields for this object.
     *
     * @param filer - DXF output writer.
     * @returns The instance (for chaining).
     */
    AcDbHatch.prototype.dxfOutFields = function (filer) {
        _super.prototype.dxfOutFields.call(this, filer);
        var loops = this._geo.loops;
        var patternName = this.getEffectivePatternName();
        var patternAngle = this.getEffectivePatternAngle();
        var patternScale = this.getEffectivePatternScale();
        this.updatePredefinedPatternDefinitionLines(patternName, patternScale);
        filer.writeSubclassMarker('AcDbHatch');
        filer.writePoint3d(10, { x: 0, y: 0, z: this.elevation });
        filer.writeVector3d(210, this.normal);
        filer.writeString(2, patternName ||
            (this.isSolidFill ? HATCH_PATTERN_SOLID : HATCH_PATTERN_USER));
        filer.writeInt16(70, this.isSolidFill ? 1 : 0);
        filer.writeInt16(71, this.getEffectiveAssociative() ? 1 : 0);
        filer.writeInt16(91, loops.length);
        loops.forEach(function (loop, index) {
            var e_1, _a, e_2, _b;
            var _c, _d;
            var isExternal = index === 0;
            if (loop instanceof AcGePolyline2d) {
                var vertices = loop.vertices;
                var hasBulge = vertices.some(function (vertex) { var _a; return ((_a = vertex.bulge) !== null && _a !== void 0 ? _a : 0) !== 0; });
                var boundaryFlag = 2;
                filer.writeInt16(92, boundaryFlag);
                filer.writeInt16(72, hasBulge ? 1 : 0);
                filer.writeInt16(73, loop.closed ? 1 : 0);
                filer.writeInt16(93, vertices.length);
                try {
                    for (var vertices_1 = __values(vertices), vertices_1_1 = vertices_1.next(); !vertices_1_1.done; vertices_1_1 = vertices_1.next()) {
                        var vertex = vertices_1_1.value;
                        filer.writePoint2d(10, vertex);
                        if (hasBulge) {
                            filer.writeDouble(42, (_c = vertex.bulge) !== null && _c !== void 0 ? _c : 0);
                        }
                    }
                }
                catch (e_1_1) { e_1 = { error: e_1_1 }; }
                finally {
                    try {
                        if (vertices_1_1 && !vertices_1_1.done && (_a = vertices_1.return)) _a.call(vertices_1);
                    }
                    finally { if (e_1) throw e_1.error; }
                }
                filer.writeInt16(97, 0);
                return;
            }
            if (loop instanceof AcGeLoop2d) {
                var boundaryFlag = isExternal ? 1 : 0;
                filer.writeInt16(92, boundaryFlag);
                filer.writeInt16(93, loop.numberOfEdges);
                var _loop_2 = function (edge) {
                    if (edge instanceof AcGeLine2d) {
                        filer.writeInt16(72, 1);
                        filer.writePoint2d(10, edge.startPoint);
                        filer.writePoint2d(11, edge.endPoint);
                        return "continue";
                    }
                    if (edge instanceof AcGeCircArc2d) {
                        filer.writeInt16(72, 2);
                        filer.writePoint2d(10, edge.center);
                        filer.writeDouble(40, edge.radius);
                        // A full-circle edge normalizes to start == end (0 ≡ 2π) in
                        // AcGeCircArc2d. Writing both verbatim emits a degenerate
                        // zero-length arc; strict DXF readers (ezdxf, AutoCAD) then treat
                        // the boundary as open and drop the fill. A closed arc edge in a
                        // hatch boundary loop can only be a full circle — restore the
                        // 360° span.
                        filer.writeAngle(50, edge.startAngle);
                        filer.writeAngle(51, edge.closed ? edge.startAngle + Math.PI * 2 : edge.endAngle);
                        filer.writeInt16(73, edge.clockwise ? 0 : 1);
                        return "continue";
                    }
                    if (edge instanceof AcGeEllipseArc2d) {
                        filer.writeInt16(72, 3);
                        filer.writePoint2d(10, edge.center);
                        var majorAxisVector = new AcGePoint2d(edge.majorAxisRadius * Math.cos(edge.rotation), edge.majorAxisRadius * Math.sin(edge.rotation));
                        filer.writePoint2d(11, majorAxisVector);
                        var ratio = edge.majorAxisRadius === 0
                            ? 0
                            : edge.minorAxisRadius / edge.majorAxisRadius;
                        filer.writeDouble(40, ratio);
                        // Same full-span restore as circular arcs: a closed ellipse edge
                        // (start ≡ end mod 2π, including the constructor's 0..2π form
                        // where deltaAngle normalizes to 0) must write a 360° span, or
                        // strict DXF readers treat the boundary as open and drop the fill.
                        filer.writeAngle(50, edge.startAngle);
                        filer.writeAngle(51, edge.closed ? edge.startAngle + Math.PI * 2 : edge.endAngle);
                        filer.writeInt16(73, edge.clockwise ? 0 : 1);
                        return "continue";
                    }
                    if (edge instanceof AcGeSpline3d) {
                        var knots = edge.knots;
                        var controlPoints = edge.controlPoints;
                        var weights_1 = edge.weights;
                        var fitPoints = edge.fitPoints;
                        var isRational_1 = weights_1.some(function (weight) { return weight !== 1; });
                        filer.writeInt16(72, 4);
                        filer.writeInt16(94, edge.degree);
                        filer.writeInt16(73, isRational_1 ? 1 : 0);
                        filer.writeInt16(74, edge.closed ? 1 : 0);
                        filer.writeInt16(95, knots.length);
                        filer.writeInt16(96, controlPoints.length);
                        knots.forEach(function (knot) { return filer.writeDouble(40, knot); });
                        controlPoints.forEach(function (point, pointIndex) {
                            var _a;
                            filer.writePoint2d(10, point);
                            if (isRational_1) {
                                filer.writeDouble(42, (_a = weights_1[pointIndex]) !== null && _a !== void 0 ? _a : 1);
                            }
                        });
                        filer.writeInt16(97, (_d = fitPoints === null || fitPoints === void 0 ? void 0 : fitPoints.length) !== null && _d !== void 0 ? _d : 0);
                        fitPoints === null || fitPoints === void 0 ? void 0 : fitPoints.forEach(function (point) { return filer.writePoint2d(11, point); });
                    }
                };
                try {
                    for (var _e = __values(loop.curves), _f = _e.next(); !_f.done; _f = _e.next()) {
                        var edge = _f.value;
                        _loop_2(edge);
                    }
                }
                catch (e_2_1) { e_2 = { error: e_2_1 }; }
                finally {
                    try {
                        if (_f && !_f.done && (_b = _e.return)) _b.call(_e);
                    }
                    finally { if (e_2) throw e_2.error; }
                }
                filer.writeInt16(97, 0);
            }
        });
        filer.writeInt16(75, this.hatchStyle);
        filer.writeInt16(76, this.patternType);
        filer.writeAngle(52, patternAngle);
        filer.writeDouble(41, patternScale);
        filer.writeInt16(77, 0);
        filer.writeInt16(78, this.definitionLines.length);
        this.definitionLines.forEach(function (line) {
            // Hatch pattern lines use 43/44 and 45/46 (not code+10 point pairs).
            filer.writeAngle(53, line.angle);
            filer.writeDouble(43, line.base.x);
            filer.writeDouble(44, line.base.y);
            filer.writeDouble(45, line.offset.x);
            filer.writeDouble(46, line.offset.y);
            filer.writeInt16(79, line.dashLengths.length);
            line.dashLengths.forEach(function (length) { return filer.writeDouble(49, length); });
        });
        if (this._pixelSize != null) {
            filer.writeDouble(47, this._pixelSize);
        }
        // Gradient fill parameters (DXF group 460 is radians, not degrees).
        if (this.isGradient) {
            filer.writeInt16(450, this._hatchObjectType);
            filer.writeInt16(451, 0);
            filer.writeDouble(460, this._gradientAngle);
            filer.writeDouble(461, this._gradientShift);
            filer.writeInt16(452, this._gradientOneColorMode ? 1 : 0);
            filer.writeDouble(462, this._shadeTintValue);
            var startColor = this._gradientStartColor;
            var endColor = this._gradientEndColor;
            var colorCount = startColor != null && endColor != null
                ? 2
                : startColor != null || endColor != null
                    ? 1
                    : 0;
            filer.writeInt16(453, colorCount);
            var writeGradientColor = function (position, rgb) {
                filer.writeDouble(463, position);
                var aci = AcCmColorUtil.getIndexByColor(rgb);
                if (aci != null) {
                    filer.writeInt16(63, aci);
                }
                filer.writeInt32(421, rgb);
            };
            if (startColor != null) {
                writeGradientColor(0, startColor);
            }
            if (endColor != null) {
                writeGradientColor(1, endColor);
            }
            filer.writeString(470, this.gradientName);
        }
        // TODO: Write the number of seed points
        filer.writeInt16(98, 0);
        return this;
    };
    AcDbHatch.prototype.dxfInFields = function (filer) {
        var _this = this;
        _super.prototype.dxfInFields.call(this, filer);
        filer.atSubclassData('AcDbHatch');
        this.definitionLines.length = 0;
        var elevation = this.elevation;
        var nx = this.normal.x;
        var ny = this.normal.y;
        var nz = this.normal.z;
        var patternAngleDeg = (this.patternAngle * 180) / Math.PI;
        var boundaryFlag = 0;
        var isPolylineBoundary = false;
        var hasBulge = false;
        var polyClosed = false;
        var expectedVertices = 0;
        var expectedEdges = 0;
        var pendingVertices = [];
        var pendingVertex = null;
        var edges = [];
        var currentEdge = null;
        var edgePhase = 'type';
        var loopsRemaining = 0;
        var inPatternLines = false;
        var currentPatternLine = null;
        var expectedDashes = 0;
        void expectedDashes;
        var afterBoundaries = false;
        var gradientColorIndex = 0;
        var pendingGradientAci;
        var applyGradientRgb = function (rgb) {
            if (gradientColorIndex === 0) {
                _this.gradientStartColor = rgb;
            }
            else {
                _this.gradientEndColor = rgb;
            }
            gradientColorIndex++;
            pendingGradientAci = undefined;
        };
        var flushPendingGradientAci = function () {
            if (pendingGradientAci == null)
                return;
            var rgb = AcCmColorUtil.getColorByIndex(pendingGradientAci);
            if (rgb != null) {
                applyGradientRgb(rgb);
            }
            else {
                pendingGradientAci = undefined;
            }
        };
        var flushVertex = function () {
            if (pendingVertex) {
                pendingVertices.push(pendingVertex);
                pendingVertex = null;
            }
        };
        var flushPolylineLoop = function () {
            flushVertex();
            if (pendingVertices.length === 0)
                return;
            var polyline = new AcGePolyline2d();
            polyline.closed = polyClosed;
            pendingVertices.forEach(function (vertex, index) {
                var _a;
                polyline.addVertexAt(index, {
                    x: vertex.x,
                    y: vertex.y,
                    bulge: (_a = vertex.bulge) !== null && _a !== void 0 ? _a : 0
                });
            });
            _this.add(polyline);
            pendingVertices = [];
        };
        var flushEdgeLoop = function () {
            var e_3, _a;
            if (edges.length === 0)
                return;
            var built = [];
            // Convert edges to geometry types (mirrors AcDbEntityConverter.convertHatch).
            var geoEdges = [];
            var _loop_3 = function (edge) {
                if (edge.kind === 'line') {
                    geoEdges.push(new AcGeLine2d({ x: edge.sx, y: edge.sy }, { x: edge.ex, y: edge.ey }));
                }
                else if (edge.kind === 'arc') {
                    geoEdges.push(new AcGeCircArc2d({ x: edge.cx, y: edge.cy }, edge.radius, (edge.startDeg * Math.PI) / 180, (edge.endDeg * Math.PI) / 180, !edge.isCcw));
                }
                else if (edge.kind === 'ellipse') {
                    var majorAxisRadius = Math.hypot(edge.mx, edge.my);
                    var minorAxisRadius = majorAxisRadius * edge.ratio;
                    var startAngle = (edge.startDeg * Math.PI) / 180;
                    var endAngle = (edge.endDeg * Math.PI) / 180;
                    var rotation = Math.atan2(edge.my, edge.mx);
                    if (!edge.isCcw) {
                        startAngle = Math.PI * 2 - startAngle;
                        endAngle = Math.PI * 2 - endAngle;
                    }
                    geoEdges.push(new AcGeEllipseArc2d({ x: edge.cx, y: edge.cy, z: 0 }, majorAxisRadius, minorAxisRadius, startAngle, endAngle, !edge.isCcw, rotation));
                }
                else {
                    var spline = AcGeSpline3d.fromDwgSplineEdge({
                        degree: edge.degree,
                        numberOfControlPoints: edge.controlPoints.length,
                        numberOfKnots: edge.knots.length,
                        numberOfFitData: edge.fitPoints.length,
                        knots: edge.knots,
                        controlPoints: edge.controlPoints.map(function (p) {
                            var _a;
                            return ({
                                x: p.x,
                                y: p.y,
                                z: 0,
                                weight: edge.rational ? ((_a = p.weight) !== null && _a !== void 0 ? _a : 1) : undefined
                            });
                        }),
                        fitDatum: edge.fitPoints.map(function (p) { return ({ x: p.x, y: p.y, z: 0 }); }),
                        startTangent: null,
                        endTangent: null
                    });
                    if (spline)
                        geoEdges.push(spline);
                }
            };
            try {
                for (var edges_1 = __values(edges), edges_1_1 = edges_1.next(); !edges_1_1.done; edges_1_1 = edges_1.next()) {
                    var edge = edges_1_1.value;
                    _loop_3(edge);
                }
            }
            catch (e_3_1) { e_3 = { error: e_3_1 }; }
            finally {
                try {
                    if (edges_1_1 && !edges_1_1.done && (_a = edges_1.return)) _a.call(edges_1);
                }
                finally { if (e_3) throw e_3.error; }
            }
            var loops = AcGeLoop2d.buildFromEdges(geoEdges);
            if (loops.length === 0 && geoEdges.length > 0) {
                _this.add(new AcGeLoop2d(geoEdges));
            }
            else {
                loops.forEach(function (loop) { return _this.add(loop); });
            }
            void built;
            edges = [];
            currentEdge = null;
        };
        var flushCurrentLoop = function () {
            if (isPolylineBoundary) {
                flushPolylineLoop();
            }
            else {
                flushEdgeLoop();
            }
        };
        /** Commit the in-progress edge into `edges` before ending a path. */
        var commitCurrentEdge = function () {
            if (currentEdge) {
                edges.push(currentEdge);
                currentEdge = null;
            }
        };
        /**
         * End the current boundary path (source-boundary count / next path).
         * Matches dxf-json: spline edge owns its own group 97 (fit count); the
         * following group 97 is the path-level source-boundary object count.
         */
        var endCurrentPath = function () {
            commitCurrentEdge();
            flushCurrentLoop();
            loopsRemaining = Math.max(0, loopsRemaining - 1);
            if (loopsRemaining === 0) {
                afterBoundaries = true;
            }
        };
        while (!filer.atEndOfObject && !filer.atEof && !filer.atExtendedData) {
            var item = filer.readItem();
            if (!item)
                break;
            var code = Number(item.code);
            var n = Number(item.value);
            // Pattern definition lines after boundaries.
            if (inPatternLines || afterBoundaries) {
                switch (code) {
                    case 75:
                        this.hatchStyle = n;
                        afterBoundaries = true;
                        break;
                    case 76:
                        this.patternType = n;
                        break;
                    case 52:
                        patternAngleDeg = n;
                        break;
                    case 41:
                        this.patternScale = n;
                        break;
                    case 77:
                        break;
                    case 78:
                        inPatternLines = true;
                        break;
                    case 53:
                        if (currentPatternLine) {
                            this.definitionLines.push(currentPatternLine);
                        }
                        currentPatternLine = {
                            angle: (n * Math.PI) / 180,
                            base: { x: 0, y: 0 },
                            offset: { x: 0, y: 0 },
                            dashLengths: []
                        };
                        break;
                    case 43:
                        if (currentPatternLine)
                            currentPatternLine.base.x = n;
                        break;
                    case 44:
                        if (currentPatternLine)
                            currentPatternLine.base.y = n;
                        break;
                    case 45:
                        if (currentPatternLine)
                            currentPatternLine.offset.x = n;
                        break;
                    case 46:
                        if (currentPatternLine)
                            currentPatternLine.offset.y = n;
                        break;
                    case 79:
                        expectedDashes = n;
                        break;
                    case 49:
                        if (currentPatternLine) {
                            currentPatternLine.dashLengths.push(n);
                        }
                        break;
                    case 47:
                        this.pixelSize = n;
                        break;
                    case 99:
                        // MPolygon-only: number of degenerate boundary paths; not
                        // applicable to HATCH entities.
                        break;
                    case 450:
                        this.hatchObjectType = n;
                        this.gradientStartColor = undefined;
                        this.gradientEndColor = undefined;
                        gradientColorIndex = 0;
                        pendingGradientAci = undefined;
                        break;
                    case 451:
                        break;
                    case 452:
                        this.gradientOneColorMode = n !== 0;
                        break;
                    case 453:
                        // Number of gradient colors (0 or 2); colors follow via 463/63/421.
                        break;
                    case 460:
                        // DXF group 460 stores gradient rotation in radians.
                        this.gradientAngle = n;
                        break;
                    case 461:
                        this.gradientShift = n;
                        break;
                    case 462:
                        this.shadeTintValue = n;
                        break;
                    case 463:
                        flushPendingGradientAci();
                        break;
                    case 63:
                        // Gradient color ACI (optional when true-color 421 follows).
                        pendingGradientAci = n;
                        break;
                    case 421:
                        applyGradientRgb(n);
                        break;
                    case 470:
                        flushPendingGradientAci();
                        this.gradientName = String(item.value);
                        break;
                    case 98:
                        // Seed point count — skip seed points.
                        break;
                    case 10:
                    case 20:
                        // Seed points after 98 — ignore.
                        break;
                    default:
                        break;
                }
                continue;
            }
            switch (code) {
                case 10:
                    if (isPolylineBoundary ||
                        (currentEdge &&
                            currentEdge.kind !== 'line' &&
                            currentEdge.kind !== 'arc' &&
                            currentEdge.kind !== 'ellipse' &&
                            currentEdge.kind !== 'spline')) {
                        // handled below in boundary context
                    }
                    if (loopsRemaining > 0 && isPolylineBoundary) {
                        flushVertex();
                        pendingVertex = { x: n, y: 0 };
                    }
                    else if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'line' && edgePhase === 'type') {
                        currentEdge.sx = n;
                    }
                    else if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'arc') {
                        currentEdge.cx = n;
                    }
                    else if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'ellipse') {
                        currentEdge.cx = n;
                    }
                    else if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'spline') {
                        currentEdge.controlPoints.push({ x: n, y: 0 });
                    }
                    else if (loopsRemaining <= 0) {
                        // Elevation point (0,0,elevation) or seed — use z via 30.
                    }
                    // First 10 before boundaries is elevation origin x (ignored).
                    break;
                case 20:
                    if (loopsRemaining > 0 && isPolylineBoundary && pendingVertex) {
                        pendingVertex.y = n;
                    }
                    else if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'line' && edgePhase === 'type') {
                        currentEdge.sy = n;
                    }
                    else if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'arc') {
                        currentEdge.cy = n;
                    }
                    else if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'ellipse') {
                        currentEdge.cy = n;
                    }
                    else if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'spline' &&
                        currentEdge.controlPoints.length > 0) {
                        currentEdge.controlPoints[currentEdge.controlPoints.length - 1].y =
                            n;
                    }
                    break;
                case 30:
                    if (loopsRemaining <= 0 && !afterBoundaries) {
                        elevation = n;
                    }
                    break;
                case 2:
                    this.patternName = String(item.value);
                    break;
                case 70:
                    this.isSolidFill = n !== 0;
                    break;
                case 71:
                    this.associative = n !== 0;
                    break;
                case 91:
                    loopsRemaining = n;
                    break;
                case 92:
                    if (loopsRemaining > 0 &&
                        (pendingVertices.length > 0 || edges.length > 0 || currentEdge)) {
                        commitCurrentEdge();
                        flushCurrentLoop();
                    }
                    boundaryFlag = n;
                    isPolylineBoundary = (boundaryFlag & 0x02) !== 0;
                    hasBulge = false;
                    polyClosed = false;
                    expectedVertices = 0;
                    expectedEdges = 0;
                    pendingVertices = [];
                    edges = [];
                    currentEdge = null;
                    edgePhase = 'type';
                    break;
                case 72:
                    if (isPolylineBoundary) {
                        hasBulge = n !== 0;
                    }
                    else {
                        // Edge type.
                        commitCurrentEdge();
                        if (n === 1) {
                            currentEdge = {
                                kind: 'line',
                                sx: 0,
                                sy: 0,
                                ex: 0,
                                ey: 0
                            };
                            edgePhase = 'type';
                        }
                        else if (n === 2) {
                            currentEdge = {
                                kind: 'arc',
                                cx: 0,
                                cy: 0,
                                radius: 0,
                                startDeg: 0,
                                endDeg: 0,
                                isCcw: true
                            };
                        }
                        else if (n === 3) {
                            currentEdge = {
                                kind: 'ellipse',
                                cx: 0,
                                cy: 0,
                                mx: 0,
                                my: 0,
                                ratio: 1,
                                startDeg: 0,
                                endDeg: 0,
                                isCcw: true
                            };
                        }
                        else if (n === 4) {
                            currentEdge = {
                                kind: 'spline',
                                degree: 3,
                                rational: false,
                                closed: false,
                                knots: [],
                                controlPoints: [],
                                fitPoints: [],
                                expectedKnots: 0,
                                expectedControls: 0,
                                expectedFits: 0,
                                fitCountSeen: false
                            };
                        }
                        else {
                            currentEdge = null;
                        }
                    }
                    break;
                case 73:
                    if (isPolylineBoundary) {
                        polyClosed = n !== 0;
                    }
                    else if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'arc') {
                        currentEdge.isCcw = n !== 0;
                    }
                    else if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'ellipse') {
                        currentEdge.isCcw = n !== 0;
                    }
                    else if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'spline') {
                        currentEdge.rational = n !== 0;
                    }
                    break;
                case 74:
                    if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'spline') {
                        currentEdge.closed = n !== 0;
                    }
                    break;
                case 93:
                    if (isPolylineBoundary) {
                        expectedVertices = n;
                    }
                    else {
                        expectedEdges = n;
                    }
                    void expectedVertices;
                    void expectedEdges;
                    break;
                case 42:
                    if (isPolylineBoundary && hasBulge && pendingVertex) {
                        pendingVertex.bulge = n;
                    }
                    else if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'spline' &&
                        currentEdge.rational &&
                        currentEdge.controlPoints.length > 0) {
                        currentEdge.controlPoints[currentEdge.controlPoints.length - 1].weight = n;
                    }
                    break;
                case 11:
                    if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'line') {
                        currentEdge.ex = n;
                        edgePhase = 'line-end';
                    }
                    else if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'ellipse') {
                        currentEdge.mx = n;
                    }
                    else if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'spline') {
                        currentEdge.fitPoints.push({ x: n, y: 0 });
                    }
                    break;
                case 21:
                    if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'line') {
                        currentEdge.ey = n;
                    }
                    else if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'ellipse') {
                        currentEdge.my = n;
                    }
                    else if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'spline' &&
                        currentEdge.fitPoints.length > 0) {
                        currentEdge.fitPoints[currentEdge.fitPoints.length - 1].y = n;
                    }
                    break;
                case 40:
                    if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'arc') {
                        currentEdge.radius = n;
                    }
                    else if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'ellipse') {
                        currentEdge.ratio = n;
                    }
                    else if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'spline') {
                        currentEdge.knots.push(n);
                    }
                    break;
                case 50:
                    if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'arc') {
                        currentEdge.startDeg = n;
                    }
                    else if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'ellipse') {
                        currentEdge.startDeg = n;
                    }
                    break;
                case 51:
                    if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'arc') {
                        currentEdge.endDeg = n;
                    }
                    else if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'ellipse') {
                        currentEdge.endDeg = n;
                    }
                    break;
                case 94:
                    if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'spline') {
                        currentEdge.degree = n;
                    }
                    break;
                case 95:
                    if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'spline') {
                        currentEdge.expectedKnots = n;
                    }
                    break;
                case 96:
                    if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'spline') {
                        currentEdge.expectedControls = n;
                    }
                    break;
                case 97:
                    // Spline edges carry their own group 97 (fit-point count), which may
                    // be 0. The next group 97 is the path-level source-boundary count
                    // (dxf-json: SplineEdgeSnippets vs CommonBoundaryPathDataSnippets).
                    if ((currentEdge === null || currentEdge === void 0 ? void 0 : currentEdge.kind) === 'spline' &&
                        !currentEdge.fitCountSeen &&
                        (currentEdge.controlPoints.length > 0 ||
                            currentEdge.knots.length > 0)) {
                        currentEdge.expectedFits = n;
                        currentEdge.fitCountSeen = true;
                        break;
                    }
                    endCurrentPath();
                    break;
                case 75:
                    commitCurrentEdge();
                    flushCurrentLoop();
                    loopsRemaining = 0;
                    afterBoundaries = true;
                    this.hatchStyle = n;
                    break;
                case 210:
                    nx = n;
                    break;
                case 220:
                    ny = n;
                    break;
                case 230:
                    nz = n;
                    break;
                default:
                    break;
            }
        }
        if (currentEdge) {
            edges.push(currentEdge);
            currentEdge = null;
        }
        flushCurrentLoop();
        if (currentPatternLine) {
            this.definitionLines.push(currentPatternLine);
        }
        flushPendingGradientAci();
        this.elevation = elevation;
        var extrusion = new AcGeVector3d(nx, ny, nz);
        if (extrusion.lengthSq() > 0) {
            this.normal = extrusion;
        }
        this.patternAngle = (patternAngleDeg * Math.PI) / 180;
        return this;
    };
    /** The entity type name */
    AcDbHatch.typeName = 'Hatch';
    return AcDbHatch;
}(AcDbEntity));
export { AcDbHatch };
//# sourceMappingURL=AcDbHatch.js.map