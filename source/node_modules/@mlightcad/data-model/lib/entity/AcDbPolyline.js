var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { AcGeArea2d, AcGeBox3d, AcGeCircArc2d, acgeOffsetVertexPath, AcGePoint2d, AcGePoint3d, AcGePolyline2d, AcGeVector3d } from '@mlightcad/geometry-engine';
import { AcDbSystemVariables } from '../database/AcDbSystemVariables';
import { AcDbSysVarManager } from '../database/AcDbSysVarManager';
import { acdbDrawTessellateOptions } from '../misc/AcDbDrawTessellate';
import { AcDbOsnapMode } from '../misc/AcDbOsnapMode';
import { AcDbCurve } from './AcDbCurve';
import { acdbForEachGripIndex, acdbMovePolyline2dVertexAt } from './AcDbGripHelpers';
import { acdbIntersectPrimitivesFromPolyline2d } from './AcDbIntersectHelpers';
import { acdbCollectPolyline2dSegmentOsnapPoints, acdbPickNearestOsnapPoint } from './AcDbOsnapHelpers';
/**
 * Represents a polyline entity in AutoCAD.
 *
 * A polyline is a complex geometric object composed of connected line segments
 * and/or arc segments. Polylines can have:
 * - Straight line segments
 * - Bulge (arc segments) between vertices
 * - Constant and variable width
 * - Thickness
 * - Multiple vertices
 *
 * Polylines are commonly used for creating complex shapes, paths, and boundaries
 * in drawings.
 *
 * @example
 * ```typescript
 * // Create a polyline
 * const polyline = new AcDbPolyline();
 *
 * // Add vertices to create a rectangle
 * polyline.addVertexAt(0, new AcGePoint2d(0, 0));
 * polyline.addVertexAt(1, new AcGePoint2d(10, 0));
 * polyline.addVertexAt(2, new AcGePoint2d(10, 5));
 * polyline.addVertexAt(3, new AcGePoint2d(0, 5));
 * polyline.closed = true; // Close the polyline
 *
 * // Access polyline properties
 * console.log(`Number of vertices: ${polyline.numberOfVertices}`);
 * console.log(`Is closed: ${polyline.closed}`);
 * ```
 */
var AcDbPolyline = /** @class */ (function (_super) {
    __extends(AcDbPolyline, _super);
    /**
     * Creates a new empty polyline entity.
     *
     * This constructor initializes an empty polyline with no vertices.
     * Vertices can be added using the addVertexAt method.
     *
     * @example
     * ```typescript
     * const polyline = new AcDbPolyline();
     * // Add vertices as needed
     * polyline.addVertexAt(0, new AcGePoint2d(0, 0));
     * ```
     */
    function AcDbPolyline() {
        var _this = _super.call(this) || this;
        /** Thickness along the normal (DXF group 39) */
        _this._thickness = 0;
        /** Extrusion / plane normal (DXF group 210) */
        _this._normal = new AcGeVector3d(0, 0, 1);
        _this._elevation = 0;
        _this._geo = new AcGePolyline2d();
        return _this;
    }
    Object.defineProperty(AcDbPolyline.prototype, "dxfTypeName", {
        get: function () {
            return 'LWPOLYLINE';
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbPolyline.prototype, "numberOfVertices", {
        /**
         * Gets the number of vertices in this polyline.
         *
         * @returns The number of vertices
         *
         * @example
         * ```typescript
         * const vertexCount = polyline.numberOfVertices;
         * console.log(`Polyline has ${vertexCount} vertices`);
         * ```
         */
        get: function () {
            return this._geo.numberOfVertices;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbPolyline.prototype, "elevation", {
        /**
         * Gets the elevation of this polyline.
         *
         * The elevation is the distance of the polyline's plane from the WCS origin
         * along the Z-axis.
         *
         * @returns The elevation value
         *
         * @example
         * ```typescript
         * const elevation = polyline.elevation;
         * console.log(`Polyline elevation: ${elevation}`);
         * ```
         */
        get: function () {
            return this._elevation;
        },
        /**
         * Sets the elevation of this polyline.
         *
         * @param value - The new elevation value
         *
         * @example
         * ```typescript
         * polyline.elevation = 10;
         * ```
         */
        set: function (value) {
            this._elevation = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbPolyline.prototype, "thickness", {
        /**
         * Thickness along the entity normal (DXF group 39).
         */
        get: function () {
            return this._thickness;
        },
        set: function (value) {
            this._thickness = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbPolyline.prototype, "normal", {
        /**
         * Extrusion direction / plane normal (DXF group 210).
         */
        get: function () {
            return this._normal;
        },
        set: function (value) {
            this._normal.copy(value);
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbPolyline.prototype, "closed", {
        /**
         * Gets whether this polyline is closed.
         *
         * A closed polyline has a segment drawn from the last vertex to the first vertex,
         * forming a complete loop.
         *
         * @returns True if the polyline is closed, false otherwise
         *
         * @example
         * ```typescript
         * const isClosed = polyline.closed;
         * console.log(`Polyline is closed: ${isClosed}`);
         * ```
         */
        get: function () {
            return this._geo.closed;
        },
        /**
         * Sets whether this polyline is closed.
         *
         * @param value - True to close the polyline, false to open it
         *
         * @example
         * ```typescript
         * polyline.closed = true; // Close the polyline
         * ```
         */
        set: function (value) {
            this._geo.closed = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbPolyline.prototype, "area", {
        /** @inheritdoc */
        get: function () {
            return this._geo.area;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Adds a vertex to this polyline at the specified index.
     *
     * This method inserts a vertex at the specified position. If the index is 0,
     * the vertex becomes the first vertex. If the index equals the number of vertices,
     * the vertex becomes the last vertex. Otherwise, the vertex is inserted before
     * the specified index.
     *
     * @param index - The index (0-based) before which to insert the vertex
     * @param pt - The vertex location point
     * @param bulge - The bulge value for the vertex (0 for straight line, >0 for arc)
     * @param startWidth - The starting width for the vertex (-1 for default)
     * @param endWidth - The ending width for the vertex (-1 for default)
     *
     * @example
     * ```typescript
     * // Add a straight line vertex
     * polyline.addVertexAt(0, new AcGePoint2d(0, 0));
     *
     * // Add a vertex with arc bulge
     * polyline.addVertexAt(1, new AcGePoint2d(5, 0), 0.5);
     *
     * // Add a vertex with custom width
     * polyline.addVertexAt(2, new AcGePoint2d(10, 0), 0, 2, 1);
     * ```
     */
    AcDbPolyline.prototype.addVertexAt = function (index, pt, bulge, startWidth, endWidth) {
        if (bulge === void 0) { bulge = 0; }
        if (startWidth === void 0) { startWidth = -1; }
        if (endWidth === void 0) { endWidth = -1; }
        var newStartWidth = startWidth < 0 ? undefined : startWidth;
        var newEndWidth = endWidth < 0 ? undefined : endWidth;
        var vertex = {
            x: pt.x,
            y: pt.y,
            bulge: bulge,
            startWidth: newStartWidth,
            endWidth: newEndWidth
        };
        this._geo.addVertexAt(index, vertex);
    };
    /**
     * This function removes a vertex from the polyline at the specified index.
     *
     * @param index Input index (0 based) of the vertex to remove
     * @throws Error if the index is out of bounds
     */
    AcDbPolyline.prototype.removeVertexAt = function (index) {
        this._geo.removeVertexAt(index);
    };
    /**
     * This function resets the polyline by optionally retaining some vertices.
     * If reuse is true, the numVerts number of vertices are left intact and all vertices
     * beyond that number are deleted. If reuse is false, numVerts is ignored and all
     * existing vertex information will be deleted.
     *
     * @param reuse Input Boolean indicating whether or not to retain some vertices
     * @param numVerts Input number of vertices to retain (only used when reuse is true)
     */
    AcDbPolyline.prototype.reset = function (reuse, numVerts) {
        this._geo.reset(reuse, numVerts);
    };
    /**
     * Gets the 2D location of a vertex at the specified index.
     *
     * The point is returned in the polyline's own object coordinate system (OCS).
     *
     * @param index - The index (0-based) of the vertex
     * @returns The 2D point location of the vertex
     *
     * @example
     * ```typescript
     * const point2d = polyline.getPoint2dAt(0);
     * console.log(`Vertex 0: ${point2d.x}, ${point2d.y}`);
     * ```
     */
    AcDbPolyline.prototype.getPoint2dAt = function (index) {
        return this._geo.getPointAt(index);
    };
    /**
     * Gets the 3D location of a vertex at the specified index.
     *
     * The point is returned in World Coordinates, with the Z-coordinate
     * set to the polyline's elevation.
     *
     * @param index - The index (0-based) of the vertex
     * @returns The 3D point location of the vertex
     *
     * @example
     * ```typescript
     * const point3d = polyline.getPoint3dAt(0);
     * console.log(`Vertex 0: ${point3d.x}, ${point3d.y}, ${point3d.z}`);
     * ```
     */
    AcDbPolyline.prototype.getPoint3dAt = function (index) {
        var vertex = this.getPoint2dAt(index);
        return new AcGePoint3d(vertex.x, vertex.y, this._elevation);
    };
    Object.defineProperty(AcDbPolyline.prototype, "geometricExtents", {
        /**
         * Gets the geometric extents (bounding box) of this polyline.
         *
         * @returns The bounding box that encompasses the entire polyline
         *
         * @example
         * ```typescript
         * const extents = polyline.geometricExtents;
         * console.log(`Polyline bounds: ${extents.minPoint} to ${extents.maxPoint}`);
         * ```
         */
        get: function () {
            var box = this._geo.box;
            if (box.isEmpty()) {
                return new AcGeBox3d();
            }
            return new AcGeBox3d({ x: box.min.x, y: box.min.y, z: this._elevation }, { x: box.max.x, y: box.max.y, z: this._elevation });
        },
        enumerable: false,
        configurable: true
    });
    /** @inheritdoc */
    AcDbPolyline.prototype.subGetIntersectCurves = function () {
        return acdbIntersectPrimitivesFromPolyline2d(this._geo.vertices, this.closed, this._elevation, this._normal);
    };
    /**
     * Gets the grip points for this polyline.
     *
     * Grip points are control points that can be used to modify the polyline.
     * For a polyline, the grip points are all the vertices. When the **GRIPS**
     * system variable is `2`, midpoint grips are also returned for each segment.
     *
     * @returns Array of grip points (vertices, then optional segment midpoints)
     *
     * @example
     * ```typescript
     * const gripPoints = polyline.subGetGripPoints();
     * // gripPoints contains all vertices of the polyline
     * ```
     */
    AcDbPolyline.prototype.subGetGripPoints = function () {
        var gripPoints = new Array();
        for (var index = 0; index < this.numberOfVertices; ++index) {
            gripPoints.push(this.getPoint3dAt(index));
        }
        if (this.shouldIncludeSegmentMidpointGrips()) {
            this.appendSegmentMidpointGrips(gripPoints);
        }
        return gripPoints;
    };
    /** @inheritdoc */
    AcDbPolyline.prototype.subMoveGripPointsAt = function (indices, offset) {
        var _this = this;
        var vertexCount = this.numberOfVertices;
        var includeMidpoints = this.shouldIncludeSegmentMidpointGrips();
        var segmentCount = includeMidpoints ? this.getSegmentCount() : 0;
        acdbForEachGripIndex(indices, function (index) {
            if (index < vertexCount) {
                acdbMovePolyline2dVertexAt(_this._geo.vertices, index, offset);
                return;
            }
            if (!includeMidpoints || index >= vertexCount + segmentCount) {
                return;
            }
            var segmentIndex = index - vertexCount;
            acdbMovePolyline2dVertexAt(_this._geo.vertices, segmentIndex, offset);
            acdbMovePolyline2dVertexAt(_this._geo.vertices, (segmentIndex + 1) % vertexCount, offset);
        });
        return this;
    };
    /**
     * Returns whether segment midpoint grips should be included.
     *
     * Matches AutoCAD **GRIPS** = `2`.
     */
    AcDbPolyline.prototype.shouldIncludeSegmentMidpointGrips = function () {
        return this.getGripsMode() >= 2;
    };
    /**
     * Reads the current **GRIPS** system variable value.
     */
    AcDbPolyline.prototype.getGripsMode = function () {
        try {
            var value = AcDbSysVarManager.instance().getVar(AcDbSystemVariables.GRIPS, this.database);
            return typeof value === 'number' ? value : 2;
        }
        catch (_a) {
            return 2;
        }
    };
    /**
     * Returns the number of editable polyline segments.
     */
    AcDbPolyline.prototype.getSegmentCount = function () {
        var vertexCount = this.numberOfVertices;
        if (vertexCount < 2) {
            return 0;
        }
        return this.closed ? vertexCount : vertexCount - 1;
    };
    /**
     * Appends one midpoint grip for each polyline segment.
     */
    AcDbPolyline.prototype.appendSegmentMidpointGrips = function (gripPoints) {
        var _a;
        var geo = this._geo;
        var elevation = this._elevation;
        var segmentCount = this.getSegmentCount();
        var vertexCount = geo.numberOfVertices;
        for (var index = 0; index < segmentCount; index++) {
            var segmentSnaps = [];
            acdbCollectPolyline2dSegmentOsnapPoints(geo.getPointAt(index), geo.getPointAt((index + 1) % vertexCount), (_a = geo.vertices[index]) === null || _a === void 0 ? void 0 : _a.bulge, elevation, AcDbOsnapMode.MidPoint, { x: 0, y: 0, z: 0 }, segmentSnaps);
            gripPoints.push.apply(gripPoints, __spreadArray([], __read(segmentSnaps), false));
        }
    };
    /**
     * Gets the object snap points for this polyline.
     *
     * Object snap points are precise points that can be used for positioning
     * when drawing or editing. This method provides snap points based on the
     * specified snap mode.
     *
     * @param osnapMode - The object snap mode
     * @param _pickPoint - The point where the user picked
     * @param _lastPoint - The last point
     * @param snapPoints - Array to populate with snap points
     */
    AcDbPolyline.prototype.subGetOsnapPoints = function (osnapMode, pickPoint, _lastPoint, snapPoints) {
        var _a;
        var geo = this._geo;
        var elevation = this._elevation;
        var vertexCount = geo.numberOfVertices;
        if (vertexCount === 0)
            return;
        switch (osnapMode) {
            case AcDbOsnapMode.EndPoint:
                for (var index = 0; index < vertexCount; index++) {
                    var vertex = geo.getPointAt(index);
                    snapPoints.push(new AcGePoint3d(vertex.x, vertex.y, elevation));
                }
                break;
            case AcDbOsnapMode.MidPoint:
            case AcDbOsnapMode.Nearest:
            case AcDbOsnapMode.Perpendicular:
            case AcDbOsnapMode.Tangent: {
                var segmentCount = this.closed ? vertexCount : vertexCount - 1;
                var candidates = [];
                for (var index = 0; index < segmentCount; index++) {
                    var segmentSnaps = [];
                    acdbCollectPolyline2dSegmentOsnapPoints(geo.getPointAt(index), geo.getPointAt((index + 1) % vertexCount), (_a = geo.vertices[index]) === null || _a === void 0 ? void 0 : _a.bulge, elevation, osnapMode, pickPoint, segmentSnaps);
                    candidates.push.apply(candidates, __spreadArray([], __read(segmentSnaps), false));
                }
                if (osnapMode === AcDbOsnapMode.MidPoint) {
                    snapPoints.push.apply(snapPoints, __spreadArray([], __read(candidates), false));
                }
                else {
                    var nearest = acdbPickNearestOsnapPoint(pickPoint, candidates);
                    if (nearest)
                        snapPoints.push(nearest);
                }
                break;
            }
            default:
                break;
        }
    };
    /**
     * Transforms this polyline by the specified matrix.
     *
     * The lightweight polyline stores 2D vertices plus a shared elevation, so we
     * transform each vertex in 3D and then write the projected XY values back.
     */
    AcDbPolyline.prototype.transformBy = function (matrix) {
        var _this = this;
        var flipBulge = matrix.determinant() < 0;
        var elevation = this._elevation;
        this._geo.vertices.forEach(function (vertex) {
            var transformedPoint = new AcGePoint3d(vertex.x, vertex.y, _this._elevation).applyMatrix4(matrix);
            vertex.x = transformedPoint.x;
            vertex.y = transformedPoint.y;
            elevation = transformedPoint.z;
            if (flipBulge && vertex.bulge != null) {
                vertex.bulge = -vertex.bulge;
            }
        });
        this._elevation = elevation;
        this._geo._boundingBoxNeedsUpdate = true;
        return this;
    };
    Object.defineProperty(AcDbPolyline.prototype, "properties", {
        /**
         * Returns the full property definition for this polyline entity, including
         * general group and geometry group.
         *
         * The geometry group exposes properties via {@link AcDbPropertyAccessor} so
         * the property palette can update the polyline in real-time.
         *
         * Each property is an {@link AcDbEntityRuntimeProperty}.
         */
        get: function () {
            var _this = this;
            return {
                type: this.type,
                groups: [
                    this.getGeneralProperties(),
                    {
                        groupName: 'geometry',
                        properties: [
                            {
                                name: 'vertices',
                                type: 'array',
                                editable: false,
                                itemSchema: {
                                    properties: [
                                        {
                                            name: 'x',
                                            type: 'float',
                                            editable: true
                                        },
                                        {
                                            name: 'y',
                                            type: 'float',
                                            editable: true
                                        }
                                    ]
                                },
                                accessor: {
                                    get: function () { return _this._geo.vertices; }
                                }
                            },
                            {
                                name: 'elevation',
                                type: 'float',
                                editable: true,
                                accessor: {
                                    get: function () { return _this.elevation; },
                                    set: function (v) {
                                        _this.elevation = v;
                                    }
                                }
                            },
                            {
                                name: 'length',
                                type: 'float',
                                editable: false,
                                accessor: {
                                    get: function () { return _this._geo.length; }
                                }
                            }
                        ]
                    },
                    {
                        groupName: 'others',
                        properties: [
                            {
                                name: 'closed',
                                type: 'float',
                                editable: true,
                                accessor: {
                                    get: function () { return _this.closed; },
                                    set: function (v) {
                                        _this.closed = v;
                                    }
                                }
                            }
                        ]
                    }
                ]
            };
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbPolyline.prototype, "directBatchPrimitive", {
        /**
         * Thin centerline → `lineStrip`; any renderable width → solid `area`.
         *
         * @internal
         */
        get: function () {
            return this.hasRenderableWidth() ? 'area' : 'lineStrip';
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Draws this polyline using the specified renderer.
     *
     * This method renders the polyline as a series of connected line segments
     * using the polyline's current style properties.
     *
     * @param renderer - The renderer to use for drawing
     * @returns The rendered polyline entity, or undefined if drawing failed
     */
    AcDbPolyline.prototype.subWorldDraw = function (renderer) {
        var _this = this;
        var tessellateOptions = acdbDrawTessellateOptions(renderer);
        var centerline = this._geo.tessellate(tessellateOptions);
        var widthProfile = this.createWidthProfile(tessellateOptions);
        if (widthProfile != null) {
            var area = createWidePolylineArea(widthProfile, this.closed);
            if (area != null) {
                var traits = renderer.subEntityTraits;
                traits.fillType = {
                    solidFill: true,
                    patternAngle: 0,
                    definitionLines: []
                };
                return renderer.area(area);
            }
        }
        var points = [];
        centerline.forEach(function (point) {
            return points.push(new AcGePoint3d().set(point.x, point.y, _this.elevation));
        });
        return renderer.lines(points);
    };
    /**
     * Writes DXF fields for this object.
     *
     * @param filer - DXF output writer.
     * @returns The instance (for chaining).
     */
    AcDbPolyline.prototype.dxfOutFields = function (filer) {
        var _a;
        _super.prototype.dxfOutFields.call(this, filer);
        filer.writeSubclassMarker('AcDbPolyline');
        filer.writeInt32(90, this.numberOfVertices);
        filer.writeInt16(70, this.closed ? 1 : 0);
        var constantWidth = this.resolveConstantWidth();
        if (constantWidth != null) {
            // Group 43 replaces per-vertex 40/41 when every segment has the same width.
            filer.writeDouble(43, constantWidth);
        }
        filer.writeDouble(38, this.elevation);
        if (this.thickness !== 0) {
            filer.writeDouble(39, this.thickness);
        }
        var vertices = this._geo.vertices;
        for (var i = 0; i < vertices.length; ++i) {
            var vertex = vertices[i];
            filer.writePoint2d(10, this.getPoint2dAt(i));
            if (constantWidth == null) {
                if (vertex.startWidth != null) {
                    filer.writeDouble(40, vertex.startWidth);
                }
                if (vertex.endWidth != null) {
                    filer.writeDouble(41, vertex.endWidth);
                }
            }
            var bulge = (_a = vertex.bulge) !== null && _a !== void 0 ? _a : 0;
            if (bulge !== 0) {
                filer.writeDouble(42, bulge);
            }
            if (vertex.identifier != null) {
                filer.writeInt32(91, vertex.identifier);
            }
        }
        filer.writeVector3d(210, this.normal);
        return this;
    };
    /**
     * Writes this LWPOLYLINE as a legacy R12 POLYLINE + VERTEX + SEQEND sequence.
     *
     * Used when the target DXF version does not support LWPOLYLINE.
     */
    AcDbPolyline.prototype.dxfOutAs2dPolyline = function (filer, _allXdata) {
        var _a, _b, _c, _d;
        if (_allXdata === void 0) { _allXdata = false; }
        filer.writeHandle(5, this.objectId);
        filer.writeObjectId(330, this.ownerId);
        filer.writeObjectId(360, this.extensionDictionary);
        filer.writeSubclassMarker('AcDbEntity');
        filer.writeString(8, this.layer);
        filer.writeString(6, this.lineType);
        filer.writeDouble(48, this.linetypeScale);
        filer.writeInt16(60, this.visibility ? 0 : 1);
        filer.writeCmColor(this.color);
        filer.writeLineWeight(370, this.lineWeight);
        filer.writeTransparency(this.transparency);
        var owner = (_a = this.database) === null || _a === void 0 ? void 0 : _a.tables.blockTable.getIdAt(this.ownerId);
        if (owner === null || owner === void 0 ? void 0 : owner.isPaperSapce) {
            filer.writeInt16(67, 1);
        }
        filer.writeSubclassMarker('AcDb2dPolyline');
        filer.writeInt16(66, this.numberOfVertices > 0 ? 1 : 0);
        filer.writeInt16(70, this.closed ? 1 : 0);
        filer.writeDouble(10, 0);
        filer.writeDouble(20, 0);
        filer.writeDouble(30, this.elevation);
        for (var i = 0; i < this.numberOfVertices; ++i) {
            var vertex = this._geo.vertices[i];
            filer.writeStart('VERTEX');
            filer.writeHandle(5, (_b = this.database) === null || _b === void 0 ? void 0 : _b.generateHandle());
            filer.writeObjectId(330, this.objectId);
            filer.writeSubclassMarker('AcDbEntity');
            filer.writeSubclassMarker('AcDbVertex');
            filer.writeSubclassMarker('AcDb2dVertex');
            filer.writePoint3d(10, {
                x: this.getPoint2dAt(i).x,
                y: this.getPoint2dAt(i).y,
                z: this.elevation
            });
            var bulge = (_c = vertex === null || vertex === void 0 ? void 0 : vertex.bulge) !== null && _c !== void 0 ? _c : 0;
            if (bulge !== 0) {
                filer.writeDouble(42, bulge);
            }
            filer.writeInt16(70, 0);
        }
        filer.writeStart('SEQEND');
        filer.writeHandle(5, (_d = this.database) === null || _d === void 0 ? void 0 : _d.generateHandle());
        filer.writeObjectId(330, this.objectId);
        filer.writeSubclassMarker('AcDbEntity');
        return this;
    };
    AcDbPolyline.prototype.dxfInFields = function (filer) {
        var _this = this;
        _super.prototype.dxfInFields.call(this, filer);
        filer.atSubclassData('AcDbPolyline');
        this.reset(false);
        var closed = false;
        var elevation = this.elevation;
        var thickness = this.thickness;
        var nx = this.normal.x;
        var ny = this.normal.y;
        var nz = this.normal.z;
        var constantWidth = -1;
        var pendingX = 0;
        var pendingY = 0;
        var hasPending = false;
        var bulge = 0;
        var startWidth = -1;
        var endWidth = -1;
        var vertexId;
        var vertexIndex = 0;
        var flushVertex = function () {
            if (!hasPending)
                return;
            var sw = startWidth < 0 ? constantWidth : startWidth;
            var ew = endWidth < 0 ? constantWidth : endWidth;
            _this.addVertexAt(vertexIndex++, new AcGePoint2d(pendingX, pendingY), bulge, sw, ew);
            if (vertexId != null) {
                var vertex = _this._geo.vertices[vertexIndex - 1];
                if (vertex)
                    vertex.identifier = vertexId;
            }
            hasPending = false;
            bulge = 0;
            startWidth = -1;
            endWidth = -1;
            vertexId = undefined;
        };
        while (!filer.atEndOfObject && !filer.atEof && !filer.atExtendedData) {
            var item = filer.readItem();
            if (!item)
                break;
            var code = Number(item.code);
            var n = Number(item.value);
            switch (code) {
                case 90:
                    // Vertex count — informational; ignore.
                    break;
                case 70:
                    closed = (n & 1) !== 0;
                    break;
                case 43:
                    constantWidth = n;
                    break;
                case 38:
                    elevation = n;
                    break;
                case 39:
                    thickness = n;
                    break;
                case 10:
                    flushVertex();
                    pendingX = n;
                    pendingY = 0;
                    hasPending = true;
                    break;
                case 20:
                    pendingY = n;
                    break;
                case 40:
                    startWidth = n;
                    break;
                case 41:
                    endWidth = n;
                    break;
                case 42:
                    bulge = n;
                    break;
                case 91:
                    vertexId = n;
                    break;
                case 210:
                    nx = n;
                    break;
                case 220:
                    ny = n;
                    break;
                case 230:
                    nz = n;
                    break;
                default:
                    break;
            }
        }
        flushVertex();
        this.closed = closed;
        this.elevation = elevation;
        this.thickness = thickness;
        var normal = new AcGeVector3d(nx, ny, nz);
        if (normal.lengthSq() > 0) {
            this.normal.copy(normal.normalize());
        }
        return this;
    };
    /**
     * Returns a single constant width when every vertex uses that same start and
     * end width; otherwise `undefined` so per-vertex 40/41 codes are written.
     */
    AcDbPolyline.prototype.resolveConstantWidth = function () {
        var e_1, _a;
        var vertices = this._geo.vertices;
        if (vertices.length === 0)
            return undefined;
        var common;
        try {
            for (var vertices_1 = __values(vertices), vertices_1_1 = vertices_1.next(); !vertices_1_1.done; vertices_1_1 = vertices_1.next()) {
                var vertex = vertices_1_1.value;
                var startWidth = vertex.startWidth;
                var endWidth = vertex.endWidth;
                if (startWidth == null && endWidth == null) {
                    if (common != null && common !== 0)
                        return undefined;
                    common = common !== null && common !== void 0 ? common : 0;
                    continue;
                }
                var start = startWidth !== null && startWidth !== void 0 ? startWidth : 0;
                var end = endWidth !== null && endWidth !== void 0 ? endWidth : 0;
                if (start !== end)
                    return undefined;
                if (common == null) {
                    common = start;
                }
                else if (common !== start) {
                    return undefined;
                }
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (vertices_1_1 && !vertices_1_1.done && (_a = vertices_1.return)) _a.call(vertices_1);
            }
            finally { if (e_1) throw e_1.error; }
        }
        // Default LWPOLYLINE width is 0; omit group 43 unless there is actual width.
        return common != null && common !== 0 ? common : undefined;
    };
    /**
     * True when any vertex carries a start/end width above the render epsilon.
     * Matches the width gate used by {@link createWidthProfile}.
     */
    AcDbPolyline.prototype.hasRenderableWidth = function () {
        var _a, _b;
        var vertices = this._geo.vertices;
        for (var i = 0; i < vertices.length; i++) {
            var startWidth = Math.max(0, (_a = vertices[i].startWidth) !== null && _a !== void 0 ? _a : 0);
            var endWidth = Math.max(0, (_b = vertices[i].endWidth) !== null && _b !== void 0 ? _b : 0);
            if (startWidth > WIDTH_EPSILON || endWidth > WIDTH_EPSILON) {
                return true;
            }
        }
        return false;
    };
    /**
     * Builds a sampled centerline profile carrying interpolated width values.
     *
     * This method converts polyline vertices (including bulge-based arc segments)
     * into a point list where each sampled point stores:
     * - 2D position on the centerline
     * - Effective width at that position
     *
     * For each segment:
     * - Width starts from the segment start vertex `startWidth`
     * - Width ends at the segment start vertex `endWidth`
     * - Width transitions linearly along sampled points
     *
     * Closed polylines avoid duplicating the seam point; open polylines include
     * full start/end coverage.
     *
     * @returns A width profile suitable for wide-line loop construction, or `null`
     * if the polyline has insufficient geometry or no renderable width.
     */
    AcDbPolyline.prototype.createWidthProfile = function (tessellateOptions) {
        var _a, _b;
        var vertices = this._geo.vertices;
        var count = vertices.length;
        if (count < 2)
            return null;
        var segmentCount = this.closed ? count : count - 1;
        var points = [];
        var hasRenderableWidth = false;
        for (var i = 0; i < segmentCount; i++) {
            var startVertex = vertices[i];
            var endVertex = vertices[(i + 1) % count];
            var startWidth = Math.max(0, (_a = startVertex.startWidth) !== null && _a !== void 0 ? _a : 0);
            var endWidth = Math.max(0, (_b = startVertex.endWidth) !== null && _b !== void 0 ? _b : 0);
            if (startWidth > WIDTH_EPSILON || endWidth > WIDTH_EPSILON) {
                hasRenderableWidth = true;
            }
            var sampled = this.sampleSegment(startVertex, endVertex, tessellateOptions);
            var lastIndex = sampled.length - 1;
            for (var j = 0; j <= lastIndex; j++) {
                if (j === 0 && points.length > 0) {
                    continue;
                }
                if (this.closed && i === segmentCount - 1 && j === lastIndex) {
                    continue;
                }
                var t = lastIndex > 0 ? j / lastIndex : 0;
                points.push({
                    x: sampled[j].x,
                    y: sampled[j].y,
                    width: lerp(startWidth, endWidth, t)
                });
            }
        }
        return hasRenderableWidth && points.length >= 2 ? points : null;
    };
    /**
     * Samples one polyline segment into a point sequence.
     *
     * If the start vertex contains a non-zero bulge, this segment is treated as an
     * arc and sampled via {@link AcGeCircArc2d}. Otherwise, the method returns the
     * two segment endpoints as a straight segment.
     *
     * @param startVertex - Segment start vertex. Its `bulge` controls whether arc
     * sampling is used.
     * @param endVertex - Segment end vertex.
     * @returns Sampled points in segment order (start to end), always at least two
     * points for the straight fallback path.
     */
    AcDbPolyline.prototype.sampleSegment = function (startVertex, endVertex, tessellateOptions) {
        if (startVertex.bulge && Math.abs(startVertex.bulge) > WIDTH_EPSILON) {
            var arc = new AcGeCircArc2d(startVertex, endVertex, startVertex.bulge);
            var sampled = arc.tessellate(tessellateOptions);
            if (sampled.length > 1) {
                return sampled.map(function (point) { return new AcGePoint2d(point.x, point.y); });
            }
        }
        return [
            new AcGePoint2d(startVertex.x, startVertex.y),
            new AcGePoint2d(endVertex.x, endVertex.y)
        ];
    };
    /**
     * Constructs a temporary {@link AcDbPolyline} from a planar vertex list.
     *
     * Other curve types use this factory when they need polyline offset or side-test
     * behavior without persisting an intermediate entity in the database. Vertices are
     * added in list order with zero bulge; arc segments are not represented.
     *
     * @param points - 2D vertices in WCS (XY), at least one point
     * @param closed - Whether the path forms a closed loop for offset and side tests
     * @returns A new lightweight polyline with the given topology
     */
    AcDbPolyline.from2dPoints = function (points, closed) {
        var polyline = new AcDbPolyline();
        points.forEach(function (point, index) { return polyline.addVertexAt(index, point); });
        polyline.closed = closed;
        return polyline;
    };
    /**
     * Creates a polyline entity from {@link AcGePolyline2d} geometry.
     *
     * @param geo - Source polyline geometry
     * @returns Polyline entity with matching vertices and closed flag
     */
    AcDbPolyline.fromGePolyline = function (geo) {
        var polyline = new AcDbPolyline();
        geo.vertices.forEach(function (vertex, index) {
            var _a;
            polyline.addVertexAt(index, new AcGePoint2d(vertex.x, vertex.y), (_a = vertex.bulge) !== null && _a !== void 0 ? _a : 0);
        });
        polyline.closed = geo.closed;
        return polyline;
    };
    AcDbPolyline.prototype.getOffsetCurves = function (offsetDist) {
        var curve = this.createOffsetCurve(offsetDist);
        return curve ? [curve] : [];
    };
    AcDbPolyline.prototype.getOffsetSideAtPoint = function (point) {
        var n = this.numberOfVertices;
        var bestDist = Infinity;
        var bestSide = 1;
        var segCount = this.closed ? n : n - 1;
        for (var i = 0; i < segCount; i++) {
            var a = this.getPoint2dAt(i);
            var b = this.getPoint2dAt((i + 1) % n);
            var dx = b.x - a.x;
            var dy = b.y - a.y;
            var len2 = dx * dx + dy * dy;
            if (len2 === 0)
                continue;
            var t = Math.max(0, Math.min(1, ((point.x - a.x) * dx + (point.y - a.y) * dy) / len2));
            var d = Math.pow((point.x - a.x - t * dx), 2) + Math.pow((point.y - a.y - t * dy), 2);
            if (d < bestDist) {
                bestDist = d;
                bestSide = dx * (point.y - a.y) - dy * (point.x - a.x) >= 0 ? 1 : -1;
            }
        }
        return bestSide;
    };
    AcDbPolyline.prototype.createOffsetCurve = function (offsetDist) {
        var results = this._geo.offset(offsetDist);
        if (results.length === 0)
            return null;
        return AcDbPolyline.fromGePolyline(results[0]);
    };
    /** The entity type name */
    AcDbPolyline.typeName = 'Polyline';
    return AcDbPolyline;
}(AcDbCurve));
export { AcDbPolyline };
/**
 * Offsets a planar vertex path using {@link acgeOffsetVertexPath} and wraps the
 * result as an {@link AcDbPolyline}.
 *
 * @param points - Sampled or vertex-derived 2D path in WCS (XY)
 * @param closed - Whether the path is treated as a closed polygon for offsetting
 * @param offsetDist - Signed offset distance in drawing units (see {@link AcDbCurve#getOffsetCurves})
 * @returns The first offset polyline, or `null` when the path has fewer than two points
 * or offsetting fails
 */
export function acdbOffsetVertexPathAsPolyline(points, closed, offsetDist) {
    var geo = acgeOffsetVertexPath(points, closed, offsetDist);
    return geo ? AcDbPolyline.fromGePolyline(geo) : null;
}
var WIDTH_EPSILON = 1e-6;
var MITER_LIMIT = 4;
var INNER_TO_OUTER_MAX_RATIO = 0.75;
/**
 * Builds a renderable filled area for a wide polyline.
 *
 * Open wide polylines are tessellated into per-segment quads so the stroke band
 * follows the centerline without enclosing interior regions when the path bends
 * back on itself. Closed wide polylines are represented as two loops (outer +
 * inner hole) so the stroke ring does not degenerate into a self-intersecting
 * polygon.
 *
 * @param points - Centerline samples with per-point widths.
 * @param closed - Whether the source polyline is topologically closed.
 * @returns An area ready for fill rendering, or `null` when no valid area can
 * be constructed.
 */
function createWidePolylineArea(points, closed) {
    var e_2, _a, e_3, _b;
    if (points.length < 2)
        return null;
    if (closed) {
        return createWidePolylineAreaForSingleProfile(points, true);
    }
    var subpaths = splitWidePolylineProfileAtRevisits(points);
    if (subpaths.length === 1) {
        return createWidePolylineAreaForSingleProfile(subpaths[0], false);
    }
    var area = new AcGeArea2d();
    var added = false;
    try {
        for (var subpaths_1 = __values(subpaths), subpaths_1_1 = subpaths_1.next(); !subpaths_1_1.done; subpaths_1_1 = subpaths_1.next()) {
            var subpath = subpaths_1_1.value;
            var part = createWidePolylineAreaForSingleProfile(subpath, false);
            if (part == null) {
                continue;
            }
            try {
                for (var _c = (e_3 = void 0, __values(part.loops)), _d = _c.next(); !_d.done; _d = _c.next()) {
                    var loop = _d.value;
                    area.add(loop);
                }
            }
            catch (e_3_1) { e_3 = { error: e_3_1 }; }
            finally {
                try {
                    if (_d && !_d.done && (_b = _c.return)) _b.call(_c);
                }
                finally { if (e_3) throw e_3.error; }
            }
            added = true;
        }
    }
    catch (e_2_1) { e_2 = { error: e_2_1 }; }
    finally {
        try {
            if (subpaths_1_1 && !subpaths_1_1.done && (_a = subpaths_1.return)) _a.call(subpaths_1);
        }
        finally { if (e_2) throw e_2.error; }
    }
    return added ? area : null;
}
function createWidePolylineAreaForSingleProfile(points, closed) {
    var centerline = normalizeCenterline(points, closed);
    if (centerline.length < 2)
        return null;
    var _a = createWidePolylineBoundaries(centerline, closed), left = _a.left, right = _a.right;
    if (countBoundaryPoints(left) < 2 || countBoundaryPoints(right) < 2) {
        return null;
    }
    var area = new AcGeArea2d();
    if (closed) {
        var leftLoop = compactBoundaryLoop(left);
        var rightLoop = compactBoundaryLoop(right);
        var leftOk = isRenderableLoop(leftLoop);
        var rightOk = isRenderableLoop(rightLoop);
        if (!leftOk && !rightOk) {
            return null;
        }
        // When half-width >= centerline radius (e.g. Blowoff circle with
        // width == diameter), the inner offset collapses to a point. Keep the
        // outer loop alone as a solid filled disk instead of aborting to stroke.
        if (!leftOk || !rightOk) {
            area.add(new AcGePolyline2d(leftOk ? leftLoop : rightLoop, true));
            return area;
        }
        var leftArea = Math.abs(calculateSignedArea(leftLoop));
        var rightArea = Math.abs(calculateSignedArea(rightLoop));
        var _b = __read(leftArea >= rightArea ? [leftLoop, rightLoop] : [rightLoop, leftLoop], 2), outer = _b[0], inner = _b[1];
        area.add(new AcGePolyline2d(outer, true));
        area.add(new AcGePolyline2d(inner, true));
        return area;
    }
    if ((isNearlyClosedWidePolyline(centerline) &&
        tryAddClosedOffsetWidePolylineRing(area, centerline)) ||
        addOpenWidePolylineBand(area, left, right)) {
        return area;
    }
    return null;
}
/**
 * Splits a sampled centerline when it revisits the same point so each subpath
 * can be stroked independently without forming one enclosing fill region.
 */
function splitWidePolylineProfileAtRevisits(points) {
    if (points.length < 2) {
        return [points];
    }
    var subpaths = [];
    var segmentStart = 0;
    var firstIndexByKey = new Map();
    for (var i = 0; i < points.length; i++) {
        var key = widePolylinePointKey(points[i]);
        var firstIndex = firstIndexByKey.get(key);
        if (firstIndex != null && i - firstIndex > 1) {
            var subpath = points.slice(segmentStart, i + 1);
            if (subpath.length >= 2) {
                subpaths.push(subpath);
            }
            segmentStart = firstIndex;
            firstIndexByKey.clear();
            for (var j = segmentStart; j <= i; j++) {
                firstIndexByKey.set(widePolylinePointKey(points[j]), j);
            }
            continue;
        }
        firstIndexByKey.set(key, i);
    }
    var tail = points.slice(segmentStart);
    if (tail.length >= 2) {
        subpaths.push(tail);
    }
    return subpaths.length > 0 ? subpaths : [points];
}
function widePolylinePointKey(point) {
    return "".concat(point.x.toFixed(4), ":").concat(point.y.toFixed(4));
}
/**
 * Validates that two offset loops form a thin ring rather than a near-solid slab.
 */
function isValidWidePolylineRing(outerArea, innerArea) {
    var outer = Math.max(outerArea, innerArea);
    var inner = Math.min(outerArea, innerArea);
    if (outer <= WIDTH_EPSILON) {
        return false;
    }
    var ringArea = outer - inner;
    if (ringArea <= WIDTH_EPSILON) {
        return false;
    }
    return inner / outer <= INNER_TO_OUTER_MAX_RATIO;
}
/**
 * Returns true when an open polyline's endpoints are close enough to be treated
 * as a nearly-closed stirrup outline.
 */
function isNearlyClosedWidePolyline(centerline) {
    // A single segment (2 samples) is a stroke, never a stirrup ring — even when
    // length is comparable to width (valve triangles, arrowheads, etc.).
    if (centerline.length < 3) {
        return false;
    }
    var first = centerline[0];
    var last = centerline[centerline.length - 1];
    var gap = Math.hypot(last.x - first.x, last.y - first.y);
    if (gap <= WIDTH_EPSILON) {
        return true;
    }
    var maxWidth = Math.max.apply(Math, __spreadArray([], __read(centerline.map(function (point) { return point.width; })), false));
    if (maxWidth <= WIDTH_EPSILON) {
        return false;
    }
    return gap <= maxWidth * 4;
}
/**
 * Attempts to render an open wide polyline as a closed-offset ring. This matches
 * stirrup-like paths whose endpoints are separated by a small gap.
 */
function tryAddClosedOffsetWidePolylineRing(area, centerline) {
    var _a = createWidePolylineBoundaries(centerline, true), left = _a.left, right = _a.right;
    var leftLoop = compactBoundaryLoop(left);
    var rightLoop = compactBoundaryLoop(right);
    if (!isRenderableLoop(leftLoop) || !isRenderableLoop(rightLoop)) {
        return false;
    }
    var leftArea = Math.abs(calculateSignedArea(leftLoop));
    var rightArea = Math.abs(calculateSignedArea(rightLoop));
    if (!isValidWidePolylineRing(leftArea, rightArea)) {
        return false;
    }
    var _b = __read(leftArea >= rightArea ? [leftLoop, rightLoop] : [rightLoop, leftLoop], 2), outer = _b[0], inner = _b[1];
    area.add(new AcGePolyline2d(outer, true));
    area.add(new AcGePolyline2d(inner, true));
    return true;
}
/**
 * Computes offset boundaries on the left and right sides of a sampled
 * centerline.
 *
 * @param centerline - Normalized centerline samples with local widths.
 * @param closed - Whether the polyline is topologically closed.
 * @returns Left and right boundary vertices.
 */
function createWidePolylineBoundaries(centerline, closed) {
    var left = new Array(centerline.length);
    var right = new Array(centerline.length);
    for (var i = 0; i < centerline.length; i++) {
        var point = centerline[i];
        var halfWidth = Math.max(0, point.width) / 2;
        // Zero width: left/right meet on the centerline (triangle tip / taper end).
        // Returning null here drops the whole open-band segment in
        // addOpenWidePolylineBand (valve symbols, arrowheads, etc.).
        if (halfWidth <= WIDTH_EPSILON) {
            left[i] = { x: point.x, y: point.y };
            right[i] = { x: point.x, y: point.y };
            continue;
        }
        var offset = computeOffsetDirection(centerline, i, closed);
        if (offset == null) {
            left[i] = null;
            right[i] = null;
            continue;
        }
        left[i] = {
            x: point.x + offset.x * halfWidth,
            y: point.y + offset.y * halfWidth
        };
        right[i] = {
            x: point.x - offset.x * halfWidth,
            y: point.y - offset.y * halfWidth
        };
    }
    return { left: left, right: right };
}
/**
 * Adds one segment quad to an open wide-polyline band area.
 */
function addOpenWidePolylineSegmentQuad(area, leftStart, leftEnd, rightEnd, rightStart) {
    var quad = [leftStart, leftEnd, rightEnd, rightStart];
    if (!isRenderableLoop(quad)) {
        return false;
    }
    area.add(new AcGePolyline2d(quad, true));
    return true;
}
/**
 * Tessellates an open wide polyline into segment quads with flat end caps.
 */
function addOpenWidePolylineBand(area, left, right) {
    var added = false;
    for (var i = 0; i < left.length - 1; i++) {
        var leftStart = left[i];
        var leftEnd = left[i + 1];
        var rightStart = right[i];
        var rightEnd = right[i + 1];
        if (leftStart == null || leftEnd == null || rightStart == null || rightEnd == null) {
            continue;
        }
        if (addOpenWidePolylineSegmentQuad(area, leftStart, leftEnd, rightEnd, rightStart)) {
            added = true;
        }
    }
    return added;
}
function countBoundaryPoints(boundary) {
    return boundary.reduce(function (count, point) { return (point == null ? count : count + 1); }, 0);
}
function compactBoundaryLoop(boundary) {
    return boundary.filter(function (point) { return point != null; });
}
/**
 * Removes redundant centerline samples and normalizes closed-end duplication.
 *
 * Adjacent points that are effectively identical (position and width within
 * epsilon) are collapsed. For closed polylines, a duplicated final point equal
 * to the first point is removed to avoid seam artifacts in offset generation.
 *
 * @param points - Raw sampled centerline points.
 * @param closed - Whether the source polyline is closed.
 * @returns A normalized centerline sample array.
 */
function normalizeCenterline(points, closed) {
    var deduped = [];
    points.forEach(function (point) {
        var last = deduped[deduped.length - 1];
        if (!last ||
            Math.abs(last.x - point.x) > WIDTH_EPSILON ||
            Math.abs(last.y - point.y) > WIDTH_EPSILON ||
            Math.abs(last.width - point.width) > WIDTH_EPSILON) {
            deduped.push(point);
        }
    });
    if (closed && deduped.length > 1) {
        var first = deduped[0];
        var last = deduped[deduped.length - 1];
        if (Math.abs(first.x - last.x) <= WIDTH_EPSILON &&
            Math.abs(first.y - last.y) <= WIDTH_EPSILON) {
            deduped.pop();
        }
    }
    return deduped;
}
/**
 * Computes the lateral offset direction at a centerline point.
 *
 * The direction is based on adjacent segment directions:
 * - Endpoints of open polylines reuse the only available segment direction.
 * - Interior points use a mitered join derived from neighboring left normals.
 * - Miter length is clamped by `MITER_LIMIT` to avoid excessive spikes.
 *
 * @param points - Normalized centerline samples.
 * @param index - Current sample index.
 * @param closed - Whether the polyline is closed.
 * @returns Scaled left-side offset direction, or `null` when direction cannot be
 * determined (for example, fully degenerate local geometry).
 */
function computeOffsetDirection(points, index, closed) {
    var count = points.length;
    var point = points[index];
    var prev = points[(index - 1 + count) % count];
    var next = points[(index + 1) % count];
    var prevDir = normalizeDirection(point.x - prev.x, point.y - prev.y);
    var nextDir = normalizeDirection(next.x - point.x, next.y - point.y);
    if (!closed) {
        if (index === 0)
            prevDir = nextDir;
        if (index === count - 1)
            nextDir = prevDir;
    }
    if (prevDir == null && nextDir == null)
        return null;
    if (prevDir == null)
        return leftNormal(nextDir);
    if (nextDir == null)
        return leftNormal(prevDir);
    var prevNormal = leftNormal(prevDir);
    var nextNormal = leftNormal(nextDir);
    var sumX = prevNormal.x + nextNormal.x;
    var sumY = prevNormal.y + nextNormal.y;
    var sumLength = Math.hypot(sumX, sumY);
    if (sumLength <= WIDTH_EPSILON) {
        return nextNormal;
    }
    var miter = { x: sumX / sumLength, y: sumY / sumLength };
    var projection = Math.abs(miter.x * nextNormal.x + miter.y * nextNormal.y);
    var scale = projection <= WIDTH_EPSILON
        ? MITER_LIMIT
        : Math.min(1 / projection, MITER_LIMIT);
    return { x: miter.x * scale, y: miter.y * scale };
}
/**
 * Converts a vector into a unit direction.
 *
 * @param dx - X component of the vector.
 * @param dy - Y component of the vector.
 * @returns A normalized vector, or `null` when the input length is too small.
 */
function normalizeDirection(dx, dy) {
    var length = Math.hypot(dx, dy);
    if (length <= WIDTH_EPSILON)
        return null;
    return { x: dx / length, y: dy / length };
}
/**
 * Computes the left-hand normal of a 2D unit direction.
 *
 * @param direction - Input direction vector.
 * @returns A 90-degree counterclockwise normal.
 */
function leftNormal(direction) {
    return { x: -direction.y, y: direction.x };
}
/**
 * Calculates signed polygon area using the shoelace formula.
 *
 * Positive sign indicates counterclockwise winding, negative indicates
 * clockwise winding.
 *
 * @param points - Polygon vertices in order.
 * @returns Signed area value.
 */
function calculateSignedArea(points) {
    var area = 0;
    var count = points.length;
    for (var i = 0; i < count; i++) {
        var p1 = points[i];
        var p2 = points[(i + 1) % count];
        area += p1.x * p2.y - p2.x * p1.y;
    }
    return area / 2;
}
/**
 * Checks whether a loop can represent a non-degenerate filled polygon.
 *
 * @param points - Loop vertices in order.
 * @returns `true` when the loop encloses non-zero area.
 */
function isRenderableLoop(points) {
    return (points.length >= 3 && Math.abs(calculateSignedArea(points)) > WIDTH_EPSILON);
}
/**
 * Performs linear interpolation between two scalar values.
 *
 * @param start - Start value at `t = 0`.
 * @param end - End value at `t = 1`.
 * @param t - Interpolation factor in `[0, 1]` (not clamped by this function).
 * @returns Interpolated value.
 */
function lerp(start, end, t) {
    return start + (end - start) * t;
}
//# sourceMappingURL=AcDbPolyline.js.map