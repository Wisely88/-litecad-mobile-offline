var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
import { AcGeEllipseArc3d, acgeGetOcsAngle, AcGeMatrix3d, AcGePoint3d, TAU } from '@mlightcad/geometry-engine';
import { AcDbOsnapMode } from '../misc/AcDbOsnapMode';
import { AcDbCurve } from './AcDbCurve';
import { acdbForEachGripIndex } from './AcDbGripHelpers';
import { acdbPickNearestOsnapPoint } from './AcDbOsnapHelpers';
/** Quadrant grip angles in radians: 0?, 90?, 180?, 270?. */
var ELLIPSE_QUADRANT_GRIP_ANGLES = [
    0,
    Math.PI / 2,
    Math.PI,
    (Math.PI / 2) * 3
];
/**
 * Represents an ellipse entity in AutoCAD.
 *
 * An ellipse is a 2D geometric object defined by its center point, major and minor axes,
 * and optional start and end angles. Ellipses are curved shapes that can be used to
 * create elliptical shapes in drawings. The ellipse is always drawn in the plane
 * defined by its normal vector.
 *
 * @example
 * ```typescript
 * // Create a full ellipse
 * const ellipse = new AcDbEllipse(
 *   new AcGePoint3d(0, 0, 0),
 *   AcGeVector3d.Z_AXIS,
 *   AcGeVector3d.X_AXIS,
 *   10, // major axis radius
 *   5,  // minor axis radius
 *   0,  // start angle
 *   2 * Math.PI // end angle (full ellipse)
 * );
 *
 * // Access ellipse properties
 * console.log(`Center: ${ellipse.center}`);
 * console.log(`Major radius: ${ellipse.majorAxisRadius}`);
 * console.log(`Minor radius: ${ellipse.minorAxisRadius}`);
 * ```
 */
var AcDbEllipse = /** @class */ (function (_super) {
    __extends(AcDbEllipse, _super);
    /**
     * Creates a new ellipse entity.
     *
     * This constructor creates an ellipse using the specified center point, normal vector,
     * major axis, and radii. The center point must be in World Coordinate System (WCS) coordinates.
     *
     * @param center - The center point of the ellipse in WCS coordinates
     * @param normal - The normal vector defining the plane of the ellipse
     * @param majorAxis - The major axis vector in WCS coordinates
     * @param majorAxisRadius - The radius of the major axis (must be positive)
     * @param minorAxisRadius - The radius of the minor axis (must be positive)
     * @param startAngle - The starting angle in radians (0 to 2?)
     * @param endAngle - The ending angle in radians (0 to 2?)
     *
     * @example
     * ```typescript
     * // Create a full ellipse in the XY plane
     * const fullEllipse = new AcDbEllipse(
     *   new AcGePoint3d(0, 0, 0),
     *   AcGeVector3d.Z_AXIS,
     *   AcGeVector3d.X_AXIS,
     *   20, // major radius
     *   10, // minor radius
     *   0,
     *   2 * Math.PI
     * );
     *
     * // Create a quarter ellipse
     * const quarterEllipse = new AcDbEllipse(
     *   new AcGePoint3d(10, 20, 0),
     *   AcGeVector3d.Z_AXIS,
     *   AcGeVector3d.X_AXIS,
     *   15,
     *   8,
     *   0,
     *   Math.PI / 2
     * );
     * ```
     */
    function AcDbEllipse(center, normal, majorAxis, majorAxisRadius, minorAxisRadius, startAngle, endAngle) {
        var _this = _super.call(this) || this;
        _this._geo = new AcGeEllipseArc3d(center, normal, majorAxis, majorAxisRadius, minorAxisRadius, startAngle, endAngle);
        return _this;
    }
    Object.defineProperty(AcDbEllipse.prototype, "dxfTypeName", {
        get: function () {
            return 'ELLIPSE';
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbEllipse.prototype, "center", {
        /**
         * Gets the center point of this ellipse.
         *
         * @returns The center point as a 3D point
         *
         * @example
         * ```typescript
         * const centerPoint = ellipse.center;
         * console.log(`Ellipse center: ${centerPoint.x}, ${centerPoint.y}, ${centerPoint.z}`);
         * ```
         */
        get: function () {
            return this._geo.center;
        },
        /**
         * Sets the center point of this ellipse.
         *
         * @param value - The new center point
         *
         * @example
         * ```typescript
         * ellipse.center = new AcGePoint3d(5, 5, 0);
         * ```
         */
        set: function (value) {
            this._geo.center = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbEllipse.prototype, "majorAxis", {
        /**
         * Gets the major axis radius of this ellipse.
         *
         * @returns The major axis radius value
         *
         * @example
         * ```typescript
         * const majorRadius = ellipse.majorAxisRadius;
         * console.log(`Major radius: ${majorRadius}`);
         * ```
         */
        get: function () {
            return this._geo.majorAxis;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbEllipse.prototype, "majorAxisRadius", {
        get: function () {
            return this._geo.majorAxisRadius;
        },
        /**
         * Sets the major axis radius of this ellipse.
         *
         * @param value - The new major axis radius value (must be positive)
         *
         * @example
         * ```typescript
         * ellipse.majorAxisRadius = 25;
         * ```
         */
        set: function (value) {
            this._geo.majorAxisRadius = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbEllipse.prototype, "minorAxisRadius", {
        /**
         * Gets the minor axis radius of this ellipse.
         *
         * @returns The minor axis radius value
         *
         * @example
         * ```typescript
         * const minorRadius = ellipse.minorAxisRadius;
         * console.log(`Minor radius: ${minorRadius}`);
         * ```
         */
        get: function () {
            return this._geo.minorAxisRadius;
        },
        /**
         * Sets the minor axis radius of this ellipse.
         *
         * @param value - The new minor axis radius value (must be positive)
         *
         * @example
         * ```typescript
         * ellipse.minorAxisRadius = 12;
         * ```
         */
        set: function (value) {
            this._geo.minorAxisRadius = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbEllipse.prototype, "startAngle", {
        /**
         * Gets the start angle of this ellipse.
         *
         * @returns The start angle in radians
         *
         * @example
         * ```typescript
         * const startAngle = ellipse.startAngle;
         * console.log(`ellipse start angle: ${startAngle} radians (${startAngle * 180 / Math.PI} degrees)`);
         * ```
         */
        get: function () {
            return this._geo.startAngle;
        },
        /**
         * Sets the start angle of this ellipse.
         *
         * @param value - The new start angle in radians (0 to 2?)
         *
         * @example
         * ```typescript
         * ellipse.startAngle = Math.PI / 4; // 45 degrees
         * ```
         */
        set: function (value) {
            this._geo.startAngle = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbEllipse.prototype, "endAngle", {
        /**
         * Gets the end angle of this ellipse.
         *
         * @returns The end angle in radians
         *
         * @example
         * ```typescript
         * const endAngle = ellipse.endAngle;
         * console.log(`ellipse end angle: ${endAngle} radians (${endAngle * 180 / Math.PI} degrees)`);
         * ```
         */
        get: function () {
            return this._geo.endAngle;
        },
        /**
         * Sets the end angle of this ellipse.
         *
         * @param value - The new end angle in radians (0 to 2?)
         *
         * @example
         * ```typescript
         * ellipse.endAngle = Math.PI; // 180 degrees
         * ```
         */
        set: function (value) {
            this._geo.endAngle = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbEllipse.prototype, "normal", {
        /**
         * Gets the normal vector of this ellipse.
         *
         * The normal vector defines the plane in which the ellipse lies.
         *
         * @returns The unit normal vector in WCS coordinates
         *
         * @example
         * ```typescript
         * const normal = ellipse.normal;
         * console.log(`Ellipse normal: ${normal.x}, ${normal.y}, ${normal.z}`);
         * ```
         */
        get: function () {
            return this._geo.normal;
        },
        /**
         * Sets the normal vector of this ellipse.
         *
         * @param value - The new normal vector
         *
         * @example
         * ```typescript
         * ellipse.normal = AcGeVector3d.Y_AXIS;
         * ```
         */
        set: function (value) {
            this._geo.normal = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbEllipse.prototype, "geometricExtents", {
        /**
         * Gets the geometric extents (bounding box) of this ellipse.
         *
         * @returns The bounding box that encompasses the entire ellipse
         *
         * @example
         * ```typescript
         * const extents = ellipse.geometricExtents;
         * console.log(`Ellipse bounds: ${extents.minPoint} to ${extents.maxPoint}`);
         * ```
         */
        get: function () {
            return this._geo.box;
        },
        enumerable: false,
        configurable: true
    });
    /** @inheritdoc */
    AcDbEllipse.prototype.subGetIntersectCurves = function () {
        return [
            {
                kind: 'ellipseArc',
                arc: this._geo.clone(),
                extendable: !this.closed
            }
        ];
    };
    Object.defineProperty(AcDbEllipse.prototype, "closed", {
        /**
         * Gets whether this ellipse is closed.
         *
         * A full ellipse may be stored either with identical start/end angles (AutoCAD
         * convention) or with a 0??? parameter span. Both represent a closed curve.
         *
         * @returns True if the ellipse is closed (forms a complete ellipse), false otherwise
         */
        get: function () {
            return this._geo.closed || Math.abs(this._geo.deltaAngle - TAU) < 1e-10;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbEllipse.prototype, "area", {
        /** @inheritdoc */
        get: function () {
            return this._geo.area;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Gets the grip points for this ellipse.
     *
     * Closed ellipses return the center and four quadrant points.
     * Open ellipse arcs return the center, start point, and end point.
     *
     * @returns Array of grip points
     */
    AcDbEllipse.prototype.subGetGripPoints = function () {
        var e_1, _a;
        var gripPoints = new Array();
        gripPoints.push(this._geo.center);
        if (this.closed) {
            try {
                for (var ELLIPSE_QUADRANT_GRIP_ANGLES_1 = __values(ELLIPSE_QUADRANT_GRIP_ANGLES), ELLIPSE_QUADRANT_GRIP_ANGLES_1_1 = ELLIPSE_QUADRANT_GRIP_ANGLES_1.next(); !ELLIPSE_QUADRANT_GRIP_ANGLES_1_1.done; ELLIPSE_QUADRANT_GRIP_ANGLES_1_1 = ELLIPSE_QUADRANT_GRIP_ANGLES_1.next()) {
                    var angle = ELLIPSE_QUADRANT_GRIP_ANGLES_1_1.value;
                    gripPoints.push(this._geo.getPointAtAngle(angle));
                }
            }
            catch (e_1_1) { e_1 = { error: e_1_1 }; }
            finally {
                try {
                    if (ELLIPSE_QUADRANT_GRIP_ANGLES_1_1 && !ELLIPSE_QUADRANT_GRIP_ANGLES_1_1.done && (_a = ELLIPSE_QUADRANT_GRIP_ANGLES_1.return)) _a.call(ELLIPSE_QUADRANT_GRIP_ANGLES_1);
                }
                finally { if (e_1) throw e_1.error; }
            }
        }
        else {
            gripPoints.push(this._geo.startPoint);
            gripPoints.push(this._geo.endPoint);
        }
        return gripPoints;
    };
    /** @inheritdoc */
    AcDbEllipse.prototype.subMoveGripPointsAt = function (indices, offset) {
        var _this = this;
        acdbForEachGripIndex(indices, function (index) {
            _this.moveGripAt(index, offset);
        });
        return this;
    };
    /**
     * Gets the object snap points for this ellipse or ellipse arc.
     *
     * Object snap points are precise points that can be used for positioning
     * when drawing or editing. This method provides snap points based on the
     * specified snap mode.
     *
     * @param osnapMode - The object snap mode
     * @param pickPoint - The point where the user picked
     * @param _lastPoint - The last point
     * @param snapPoints - Array to populate with snap points
     */
    AcDbEllipse.prototype.subGetOsnapPoints = function (osnapMode, pickPoint, _lastPoint, snapPoints) {
        var e_2, _a;
        switch (osnapMode) {
            case AcDbOsnapMode.EndPoint:
                if (!this.closed) {
                    snapPoints.push(this._geo.startPoint);
                    snapPoints.push(this._geo.endPoint);
                }
                break;
            case AcDbOsnapMode.MidPoint:
                if (!this.closed) {
                    snapPoints.push(this._geo.midPoint);
                }
                break;
            case AcDbOsnapMode.Center:
            case AcDbOsnapMode.Centroid:
                snapPoints.push(this._geo.center);
                break;
            case AcDbOsnapMode.Quadrant:
                if (this.closed) {
                    try {
                        for (var ELLIPSE_QUADRANT_GRIP_ANGLES_2 = __values(ELLIPSE_QUADRANT_GRIP_ANGLES), ELLIPSE_QUADRANT_GRIP_ANGLES_2_1 = ELLIPSE_QUADRANT_GRIP_ANGLES_2.next(); !ELLIPSE_QUADRANT_GRIP_ANGLES_2_1.done; ELLIPSE_QUADRANT_GRIP_ANGLES_2_1 = ELLIPSE_QUADRANT_GRIP_ANGLES_2.next()) {
                            var angle = ELLIPSE_QUADRANT_GRIP_ANGLES_2_1.value;
                            snapPoints.push(this._geo.getPointAtAngle(angle));
                        }
                    }
                    catch (e_2_1) { e_2 = { error: e_2_1 }; }
                    finally {
                        try {
                            if (ELLIPSE_QUADRANT_GRIP_ANGLES_2_1 && !ELLIPSE_QUADRANT_GRIP_ANGLES_2_1.done && (_a = ELLIPSE_QUADRANT_GRIP_ANGLES_2.return)) _a.call(ELLIPSE_QUADRANT_GRIP_ANGLES_2);
                        }
                        finally { if (e_2) throw e_2.error; }
                    }
                }
                break;
            case AcDbOsnapMode.Nearest:
                snapPoints.push(this._geo.nearestPoint(pickPoint));
                break;
            case AcDbOsnapMode.Tangent:
                snapPoints.push.apply(snapPoints, __spreadArray([], __read(this._geo.tangentPoints(pickPoint)), false));
                break;
            case AcDbOsnapMode.Perpendicular: {
                var candidates = this._geo.perpendicularPoints(pickPoint);
                var nearest = acdbPickNearestOsnapPoint(pickPoint, candidates);
                if (nearest)
                    snapPoints.push(nearest);
                break;
            }
            default:
                break;
        }
    };
    /**
     * Transforms this ellipse by the specified matrix.
     */
    AcDbEllipse.prototype.transformBy = function (matrix) {
        this._geo.transform(matrix);
        return this;
    };
    Object.defineProperty(AcDbEllipse.prototype, "properties", {
        /**
         * Returns the full property definition for this ellipse entity, including
         * general group and geometry group.
         *
         * The geometry group exposes editable properties via {@link AcDbPropertyAccessor}
         * so the property palette can update the ellipse in real-time.
         *
         * Each property is an {@link AcDbEntityRuntimeProperty}.
         */
        get: function () {
            var _this = this;
            return {
                type: this.type,
                groups: [
                    this.getGeneralProperties(),
                    {
                        groupName: 'geometry',
                        properties: [
                            {
                                name: 'centerX',
                                type: 'float',
                                editable: true,
                                accessor: {
                                    get: function () { return _this.center.x; },
                                    set: function (v) {
                                        _this.center.x = v;
                                    }
                                }
                            },
                            {
                                name: 'centerY',
                                type: 'float',
                                editable: true,
                                accessor: {
                                    get: function () { return _this.center.y; },
                                    set: function (v) {
                                        _this.center.y = v;
                                    }
                                }
                            },
                            {
                                name: 'centerZ',
                                type: 'float',
                                editable: true,
                                accessor: {
                                    get: function () { return _this.center.z; },
                                    set: function (v) {
                                        _this.center.z = v;
                                    }
                                }
                            },
                            {
                                name: 'majorAxisRadius',
                                type: 'float',
                                editable: true,
                                accessor: {
                                    get: function () { return _this.majorAxisRadius; },
                                    set: function (v) {
                                        _this.center.x = v;
                                    }
                                }
                            },
                            {
                                name: 'minorAxisRadius',
                                type: 'float',
                                editable: true,
                                accessor: {
                                    get: function () { return _this.minorAxisRadius; },
                                    set: function (v) {
                                        _this.minorAxisRadius = v;
                                    }
                                }
                            },
                            {
                                name: 'startAngle',
                                type: 'float',
                                editable: true,
                                accessor: {
                                    get: function () { return _this.startAngle; },
                                    set: function (v) {
                                        _this.startAngle = v;
                                    }
                                }
                            },
                            {
                                name: 'endAngle',
                                type: 'float',
                                editable: true,
                                accessor: {
                                    get: function () { return _this.endAngle; },
                                    set: function (v) {
                                        _this.endAngle = v;
                                    }
                                }
                            },
                            {
                                name: 'normalX',
                                type: 'float',
                                editable: true,
                                accessor: {
                                    get: function () { return _this.normal.x; },
                                    set: function (v) {
                                        _this.normal.x = v;
                                    }
                                }
                            },
                            {
                                name: 'normalY',
                                type: 'float',
                                editable: true,
                                accessor: {
                                    get: function () { return _this.normal.y; },
                                    set: function (v) {
                                        _this.normal.y = v;
                                    }
                                }
                            },
                            {
                                name: 'normalZ',
                                type: 'float',
                                editable: true,
                                accessor: {
                                    get: function () { return _this.normal.z; },
                                    set: function (v) {
                                        _this.normal.z = v;
                                    }
                                }
                            },
                            {
                                name: 'length',
                                type: 'float',
                                editable: false,
                                accessor: {
                                    get: function () { return _this._geo.length; }
                                }
                            },
                            {
                                name: 'area',
                                type: 'float',
                                editable: false,
                                accessor: {
                                    get: function () { return _this.area; }
                                }
                            }
                        ]
                    }
                ]
            };
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbEllipse.prototype, "directBatchPrimitive", {
        /**
         * This ellipse always draws as a single `lineStrip` primitive.
         *
         * @internal
         */
        get: function () {
            return 'lineStrip';
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Draws this ellipse using the specified renderer.
     *
     * This method renders the ellipse as an elliptical arc using the ellipse's
     * current style properties.
     *
     * @param renderer - The renderer to use for drawing
     * @returns The rendered ellipse entity, or undefined if drawing failed
     */
    AcDbEllipse.prototype.subWorldDraw = function (renderer) {
        return renderer.ellipticalArc(this._geo);
    };
    /**
     * Writes DXF fields for this object.
     *
     * @param filer - DXF output writer.
     * @returns The instance (for chaining).
     */
    AcDbEllipse.prototype.dxfOutFields = function (filer) {
        _super.prototype.dxfOutFields.call(this, filer);
        filer.writeSubclassMarker('AcDbEllipse');
        filer.writePoint3d(10, this.center);
        var u = this._geo.majorAxis;
        var r = this.majorAxisRadius;
        filer.writePoint3d(11, {
            x: u.x * r,
            y: u.y * r,
            z: u.z * r
        });
        filer.writeVector3d(210, this.normal);
        filer.writeDouble(40, this.minorAxisRadius / this.majorAxisRadius);
        filer.writeDouble(41, this.startAngle);
        filer.writeDouble(42, this.endAngle);
        return this;
    };
    AcDbEllipse.prototype.dxfInFields = function (filer) {
        _super.prototype.dxfInFields.call(this, filer);
        filer.atSubclassData('AcDbEllipse');
        var cx = this.center.x;
        var cy = this.center.y;
        var cz = this.center.z;
        var mx = this.majorAxis.x * this.majorAxisRadius;
        var my = this.majorAxis.y * this.majorAxisRadius;
        var mz = this.majorAxis.z * this.majorAxisRadius;
        var nx = this.normal.x;
        var ny = this.normal.y;
        var nz = this.normal.z;
        var axisRatio = this.majorAxisRadius > 0
            ? this.minorAxisRadius / this.majorAxisRadius
            : 1;
        var startAngle = this.startAngle;
        var endAngle = this.endAngle;
        while (!filer.atEndOfObject && !filer.atEof && !filer.atExtendedData) {
            var item = filer.readItem();
            if (!item)
                break;
            var code = Number(item.code);
            var n = Number(item.value);
            switch (code) {
                case 10:
                    cx = n;
                    break;
                case 20:
                    cy = n;
                    break;
                case 30:
                    cz = n;
                    break;
                case 11:
                    mx = n;
                    break;
                case 21:
                    my = n;
                    break;
                case 31:
                    mz = n;
                    break;
                case 210:
                    nx = n;
                    break;
                case 220:
                    ny = n;
                    break;
                case 230:
                    nz = n;
                    break;
                case 40:
                    axisRatio = n;
                    break;
                case 41:
                    startAngle = n;
                    break;
                case 42:
                    endAngle = n;
                    break;
                default:
                    break;
            }
        }
        this.applyDxfInGeometry(cx, cy, cz, mx, my, mz, nx, ny, nz, axisRatio, startAngle, endAngle);
        return this;
    };
    AcDbEllipse.prototype.applyDxfInGeometry = function (cx, cy, cz, mx, my, mz, nx, ny, nz, axisRatio, startAngle, endAngle) {
        var majorRadius = Math.hypot(mx, my, mz) || 1;
        this._geo = new AcGeEllipseArc3d({ x: cx, y: cy, z: cz }, { x: nx, y: ny, z: nz }, { x: mx, y: my, z: mz }, majorRadius, majorRadius * axisRatio, startAngle, endAngle);
    };
    AcDbEllipse.prototype.getOffsetCurves = function (offsetDist) {
        var curve = this.createOffsetCurve(offsetDist);
        return curve ? [curve] : [];
    };
    AcDbEllipse.prototype.getOffsetSideAtPoint = function (point) {
        var c = this.center;
        var dx = point.x - c.x;
        var dy = point.y - c.y;
        var major = this.majorAxis;
        var majorLen = Math.hypot(major.x, major.y) || 1;
        var minorLen = (this.minorAxisRadius / this.majorAxisRadius) * majorLen || 1;
        var ux = major.x / majorLen;
        var uy = major.y / majorLen;
        var vx = -uy;
        var vy = ux;
        var u = dx * ux + dy * uy;
        var v = dx * vx + dy * vy;
        return Math.pow((u / majorLen), 2) + Math.pow((v / minorLen), 2) >= 1 ? 1 : -1;
    };
    AcDbEllipse.prototype.moveGripAt = function (gripIndex, offset) {
        var _a, _b, _c, _d;
        switch (gripIndex) {
            case 0:
                this.transformBy(AcGeMatrix3d.makeTranslation(offset));
                break;
            case 1:
                if (this.closed) {
                    this.moveQuadrantGripAt(ELLIPSE_QUADRANT_GRIP_ANGLES[0], offset);
                }
                else {
                    var point = this._geo.startPoint;
                    this._geo.startAngle = acgeGetOcsAngle(this._geo.center, {
                        x: point.x + offset.x,
                        y: point.y + offset.y,
                        z: ((_a = point.z) !== null && _a !== void 0 ? _a : 0) + ((_b = offset.z) !== null && _b !== void 0 ? _b : 0)
                    }, this._geo.normal);
                }
                break;
            case 2:
                if (this.closed) {
                    this.moveQuadrantGripAt(ELLIPSE_QUADRANT_GRIP_ANGLES[1], offset);
                }
                else {
                    var point = this._geo.endPoint;
                    this._geo.endAngle = acgeGetOcsAngle(this._geo.center, {
                        x: point.x + offset.x,
                        y: point.y + offset.y,
                        z: ((_c = point.z) !== null && _c !== void 0 ? _c : 0) + ((_d = offset.z) !== null && _d !== void 0 ? _d : 0)
                    }, this._geo.normal);
                }
                break;
            case 3:
            case 4:
                if (this.closed) {
                    this.moveQuadrantGripAt(ELLIPSE_QUADRANT_GRIP_ANGLES[gripIndex - 1], offset);
                }
                break;
            default:
                break;
        }
    };
    /**
     * Moves a quadrant grip on a closed ellipse by updating the corresponding axis radius.
     *
     * Grips at 0? and 180? adjust {@link majorAxisRadius}; grips at 90? and 270? adjust
     * {@link minorAxisRadius}. The new radius is the absolute projection of the dragged
     * point onto the relevant axis from the ellipse center.
     *
     * @param angle - Quadrant angle in radians (0, ?/2, ?, or 3?/2).
     * @param offset - Translation applied to the grip point before recomputing the radius.
     * @private
     */
    AcDbEllipse.prototype.moveQuadrantGripAt = function (angle, offset) {
        var _a, _b, _c, _d;
        var point = this._geo.getPointAtAngle(angle);
        var newPoint = new AcGePoint3d(point.x + offset.x, point.y + offset.y, ((_a = point.z) !== null && _a !== void 0 ? _a : 0) + ((_b = offset.z) !== null && _b !== void 0 ? _b : 0));
        var c = this._geo.center;
        var dx = newPoint.x - c.x;
        var dy = newPoint.y - c.y;
        var dz = ((_c = newPoint.z) !== null && _c !== void 0 ? _c : 0) - ((_d = c.z) !== null && _d !== void 0 ? _d : 0);
        var axis = angle === 0 || angle === Math.PI
            ? this._geo.majorAxis
            : this._geo.minorAxis;
        var projected = dx * axis.x + dy * axis.y + dz * axis.z;
        var newRadius = Math.abs(projected);
        if (newRadius <= 0)
            return;
        if (angle === 0 || angle === Math.PI) {
            this.majorAxisRadius = newRadius;
        }
        else {
            this.minorAxisRadius = newRadius;
        }
    };
    AcDbEllipse.prototype.createOffsetCurve = function (offsetDist) {
        var geo = this._geo.offset(offsetDist);
        if (!geo)
            return null;
        return new AcDbEllipse(geo.center, geo.normal, geo.majorAxis, geo.majorAxisRadius, geo.minorAxisRadius, geo.startAngle, geo.endAngle);
    };
    /** The entity type name */
    AcDbEllipse.typeName = 'Ellipse';
    return AcDbEllipse;
}(AcDbCurve));
export { AcDbEllipse };
//# sourceMappingURL=AcDbEllipse.js.map