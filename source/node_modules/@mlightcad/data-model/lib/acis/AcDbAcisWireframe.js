var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
import { acdbDecodeAcisModel } from './AcDbAcisDecode';
import { acdbAcisEdgeParamBounds, acdbAcisParseSurfaceParams, acdbExtractAcisGeometry, acdbSampleAcisEllipseArc, acdbSampleAcisSphereWireframe, } from './AcDbAcisGeometry';
import { acdbAcisBuildNodeTransforms, acdbAcisIdentityTransform, acdbAcisTransformIsIdentity, acdbAcisTransformPoint, } from './AcDbAcisTransform';
/** Default number of samples when tessellating ellipse edges for wireframe output. */
var DEFAULT_ELLIPSE_SAMPLES = 16;
var refOfType = function (node, suffix) { var _a; return (_a = node.refs.find(function (r) { return r !== null && (r.type === suffix || r.type.endsWith(suffix)); })) !== null && _a !== void 0 ? _a : null; };
/** Appends one line segment (`a` → `b`) to a flat coordinate buffer. */
function pushSegment(segments, a, b) {
    segments.push(a[0], a[1], a[2], b[0], b[1], b[2]);
}
/** Appends consecutive point pairs from `points` as line segments. */
function pushPolyline(segments, points) {
    for (var i = 0; i + 1 < points.length; i++) {
        var a = points[i];
        var b = points[i + 1];
        if (a && b)
            pushSegment(segments, a, b);
    }
}
/**
 * Builds wireframe rings for sphere faces when edge-based wireframe is empty.
 *
 * Samples in body space, then maps each face through its owning body transform.
 *
 * @param model - Resolved ACIS model graph.
 * @returns Line-segment endpoint pairs for sphere surface scaffolds.
 */
function acdbAcisWireframeSegmentsFromSphereFaces(model) {
    var e_1, _a;
    var _b;
    var segments = [];
    var transformsByNode = acdbAcisBuildNodeTransforms(model);
    var identity = acdbAcisIdentityTransform();
    var _loop_1 = function (face) {
        var e_2, _e;
        var surfaceNode = refOfType(face, '-surface');
        if ((surfaceNode === null || surfaceNode === void 0 ? void 0 : surfaceNode.type) !== 'sphere-surface')
            return "continue";
        var params = acdbAcisParseSurfaceParams(surfaceNode);
        if (params.kind !== 'sphere')
            return "continue";
        var transform = (_b = transformsByNode.get(face.index)) !== null && _b !== void 0 ? _b : identity;
        try {
            for (var _f = (e_2 = void 0, __values(acdbSampleAcisSphereWireframe(params))), _g = _f.next(); !_g.done; _g = _f.next()) {
                var ring = _g.value;
                if (acdbAcisTransformIsIdentity(transform)) {
                    pushPolyline(segments, ring);
                    continue;
                }
                pushPolyline(segments, ring.map(function (p) { return acdbAcisTransformPoint(p, transform); }));
            }
        }
        catch (e_2_1) { e_2 = { error: e_2_1 }; }
        finally {
            try {
                if (_g && !_g.done && (_e = _f.return)) _e.call(_f);
            }
            finally { if (e_2) throw e_2.error; }
        }
    };
    try {
        for (var _c = __values(model.nodesOfType('face')), _d = _c.next(); !_d.done; _d = _c.next()) {
            var face = _d.value;
            _loop_1(face);
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
        }
        finally { if (e_1) throw e_1.error; }
    }
    return new Float32Array(segments);
}
/**
 * Converts extracted ACIS B-rep geometry into line-segment endpoint pairs
 * suitable for {@link AcGiRenderer.lineSegments}.
 *
 * @param geometry - Geometry extracted from a decoded ACIS model.
 * @param model - Optional source model used to read edge parameter bounds.
 * @returns Flat `Float32Array` of segment endpoint pairs.
 */
export function acdbAcisWireframeSegmentsFromGeometry(geometry, model) {
    var e_3, _a;
    var _b, _c, _d, _e;
    var segments = [];
    try {
        for (var _f = __values(geometry.edges), _g = _f.next(); !_g.done; _g = _f.next()) {
            var edge = _g.value;
            var edgeNode = model === null || model === void 0 ? void 0 : model.nodes[edge.nodeIndex];
            if (((_b = edge.curve) === null || _b === void 0 ? void 0 : _b.kind) === 'ellipse') {
                var bounds = edgeNode !== undefined ? acdbAcisEdgeParamBounds(edgeNode) : null;
                var t0 = (_c = bounds === null || bounds === void 0 ? void 0 : bounds[0]) !== null && _c !== void 0 ? _c : 0;
                var t1 = (_d = bounds === null || bounds === void 0 ? void 0 : bounds[1]) !== null && _d !== void 0 ? _d : Math.PI * 2;
                pushPolyline(segments, acdbSampleAcisEllipseArc(edge.curve, t0, t1, DEFAULT_ELLIPSE_SAMPLES));
                continue;
            }
            if (((_e = edge.curve) === null || _e === void 0 ? void 0 : _e.kind) === 'intcurve' &&
                edge.curve.controlPoints.length > 0) {
                pushPolyline(segments, edge.curve.controlPoints);
                continue;
            }
            if (edge.start && edge.end) {
                pushSegment(segments, edge.start, edge.end);
            }
        }
    }
    catch (e_3_1) { e_3 = { error: e_3_1 }; }
    finally {
        try {
            if (_g && !_g.done && (_a = _f.return)) _a.call(_f);
        }
        finally { if (e_3) throw e_3.error; }
    }
    return new Float32Array(segments);
}
/**
 * Decodes a SAB byte stream and returns wireframe line segments, or `null`
 * when decoding fails or no drawable geometry is present.
 *
 * @param sabBytes - Raw SAB/ASM binary payload.
 * @returns Wireframe segment buffer, or `null` when nothing drawable is found.
 */
export function acdbAcisWireframeSegmentsFromSab(sabBytes) {
    var model = acdbDecodeAcisModel(sabBytes);
    if (model === null)
        return null;
    // Edge wireframe uses geometry already mapped to model space by extract.
    var geometry = acdbExtractAcisGeometry(model);
    var edgeWireframe = acdbAcisWireframeSegmentsFromGeometry(geometry, model);
    if (edgeWireframe.length > 0) {
        return edgeWireframe;
    }
    // Sphere fallback samples raw surfaces and applies per-face body transforms.
    var sphereWireframe = acdbAcisWireframeSegmentsFromSphereFaces(model);
    return sphereWireframe.length > 0 ? sphereWireframe : null;
}
//# sourceMappingURL=AcDbAcisWireframe.js.map