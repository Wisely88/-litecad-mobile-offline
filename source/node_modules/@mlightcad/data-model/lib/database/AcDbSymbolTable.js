var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
import { AcDbObject } from '../base/AcDbObject';
import { AcDbObjectIterator } from '../misc/AcDbObjectIterator';
/**
 * Base class for all symbol tables in AutoCAD.
 *
 * AcDbSymbolTable is the base class for all classes used to manage AutoCAD's
 * built-in symbol tables. Symbol tables organize and store various types of
 * records such as layers, linetypes, text styles, dimension styles, etc.
 *
 * @template RecordType - The type of records this symbol table manages
 *
 * @example
 * ```typescript
 * class MySymbolTable extends AcDbSymbolTable<MySymbolTableRecord> {
 *   constructor(db: AcDbDatabase) {
 *     super(db);
 *   }
 * }
 * ```
 */
var AcDbSymbolTable = /** @class */ (function (_super) {
    __extends(AcDbSymbolTable, _super);
    /**
     * Creates a new AcDbSymbolTable instance.
     *
     * @param db - The database this symbol table belongs to
     *
     * @example
     * ```typescript
     * const symbolTable = new AcDbSymbolTable(database);
     * ```
     */
    function AcDbSymbolTable(db) {
        var _this = _super.call(this) || this;
        _this.database = db;
        _this.objectId = db.generateHandle();
        // Symbol tables are owned by the database object (handle "0").
        _this.ownerId = db.objectId;
        _this._recordsByName = new Map();
        _this._unnamedRecords = [];
        return _this;
    }
    Object.defineProperty(AcDbSymbolTable.prototype, "numEntries", {
        /**
         * Gets the number of entries in the table.
         *
         * @returns The number of entries in the table
         */
        get: function () {
            return this._recordsByName.size;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbSymbolTable.prototype, "totalRecordCount", {
        /**
         * Total record count including unnamed entries.
         *
         * @internal
         */
        get: function () {
            return this._recordsByName.size + this._unnamedRecords.length;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Adds a record to both the database containing the table and the table itself.
     *
     * @param record - The record to add to the table
     *
     * @example
     * ```typescript
     * const record = new AcDbSymbolTableRecord({ name: 'MyRecord' });
     * symbolTable.add(record);
     * ```
     */
    AcDbSymbolTable.prototype.add = function (record) {
        var _this = this;
        var manager = this.database.transactionManager;
        if (manager.strictMode &&
            !manager.isRecording() &&
            !manager.isApplyingUndoRedo()) {
            throw new Error('Cannot add symbol table records outside an active transaction.');
        }
        var tableName = manager.resolveSymbolTableName(this);
        record.database = this.database;
        record.ownerId = this.objectId;
        this.database.commitObjectHandle(record, function (id) { return _this.hasId(id); });
        var normalizedName = this.normalizeName(record.name);
        if (normalizedName) {
            this._recordsByName.set(normalizedName, record);
        }
        else {
            this._unnamedRecords.push(record);
        }
        if (manager.isRecording()) {
            manager.recordAppend({ type: 'symbolTable', tableName: tableName }, record);
        }
    };
    /**
     * Removes the record with the specified name.
     *
     * @param name - The name of the record to remove
     * @returns True if the record was found and removed, false otherwise
     *
     * @example
     * ```typescript
     * const removed = symbolTable.remove('MyRecord');
     * if (removed) {
     *   console.log('Record removed successfully');
     * }
     * ```
     */
    AcDbSymbolTable.prototype.remove = function (name) {
        var normalizedName = this.normalizeName(name);
        var record = this._recordsByName.get(normalizedName);
        if (record) {
            this.removeRecord(record);
            return true;
        }
        return false;
    };
    /**
     * Removes the record with the specified ID.
     *
     * @param id - The object ID of the record to remove
     * @returns True if the record was found and removed, false otherwise
     *
     * @example
     * ```typescript
     * const removed = symbolTable.removeId('some-object-id');
     * if (removed) {
     *   console.log('Record removed successfully');
     * }
     * ```
     */
    AcDbSymbolTable.prototype.removeId = function (id) {
        var record = this.getIdAt(id);
        if (record) {
            this.removeRecord(record);
            return true;
        }
        return false;
    };
    /**
     * Removes one symbol table record from internal maps and records the change when needed.
     *
     * Shared by {@link remove} and {@link removeId}. Enforces strict-mode transaction
     * requirements before mutating table membership.
     *
     * @param record - Symbol table record to remove
     */
    AcDbSymbolTable.prototype.removeRecord = function (record) {
        var manager = this.database.transactionManager;
        if (manager.strictMode &&
            !manager.isRecording() &&
            !manager.isApplyingUndoRedo()) {
            throw new Error('Cannot remove symbol table records outside an active transaction.');
        }
        if (manager.isRecording()) {
            var tableName = manager.resolveSymbolTableName(this);
            manager.recordRemove({ type: 'symbolTable', tableName: tableName }, record);
        }
        this.removeFromMembership(record);
        this.database.releaseObjectHandle(record);
    };
    /**
     * Removes all records from the table.
     *
     * @example
     * ```typescript
     * symbolTable.removeAll();
     * console.log('All records removed');
     * ```
     */
    AcDbSymbolTable.prototype.removeAll = function () {
        var e_1, _a, e_2, _b;
        try {
            for (var _c = __values(this._recordsByName.values()), _d = _c.next(); !_d.done; _d = _c.next()) {
                var record = _d.value;
                this.database.releaseObjectHandle(record);
            }
        }
        catch (e_1_1) { e_1 = { error: e_1_1 }; }
        finally {
            try {
                if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
            }
            finally { if (e_1) throw e_1.error; }
        }
        try {
            for (var _e = __values(this._unnamedRecords), _f = _e.next(); !_f.done; _f = _e.next()) {
                var record = _f.value;
                this.database.releaseObjectHandle(record);
            }
        }
        catch (e_2_1) { e_2 = { error: e_2_1 }; }
        finally {
            try {
                if (_f && !_f.done && (_b = _e.return)) _b.call(_e);
            }
            finally { if (e_2) throw e_2.error; }
        }
        this._recordsByName.clear();
        this._unnamedRecords.length = 0;
    };
    /**
     * Checks if the table contains a record with the specified name.
     *
     * @param name - The name to search for
     * @returns True if a record with the specified name exists, false otherwise
     *
     * @example
     * ```typescript
     * if (symbolTable.has('MyRecord')) {
     *   console.log('Record exists');
     * }
     * ```
     */
    AcDbSymbolTable.prototype.has = function (name) {
        var normalizedName = this.normalizeName(name);
        return this._recordsByName.has(normalizedName);
    };
    /**
     * Checks if the table contains a record with the specified ID.
     *
     * @param id - The ID to search for
     * @returns True if a record with the specified ID exists, false otherwise
     *
     * @example
     * ```typescript
     * if (symbolTable.hasId('some-object-id')) {
     *   console.log('Record exists');
     * }
     * ```
     */
    AcDbSymbolTable.prototype.hasId = function (id) {
        return this.getIdAt(id) !== undefined;
    };
    /**
     * Searches the table for a record with the specified name.
     *
     * @param name - The name to search for
     * @returns The record with the specified name, or undefined if not found
     *
     * @example
     * ```typescript
     * const record = symbolTable.getAt('MyRecord');
     * if (record) {
     *   console.log('Found record:', record.name);
     * }
     * ```
     */
    AcDbSymbolTable.prototype.getAt = function (name) {
        var normalizedName = this.normalizeName(name);
        return this._recordsByName.get(normalizedName);
    };
    /**
     * Searches the table for a record with the specified ID.
     *
     * @param id - The ID to search for
     * @returns The record with the specified ID, or undefined if not found
     *
     * @example
     * ```typescript
     * const record = symbolTable.getIdAt('some-object-id');
     * if (record) {
     *   console.log('Found record:', record.name);
     * }
     * ```
     */
    AcDbSymbolTable.prototype.getIdAt = function (id) {
        var object = this.database.getObjectById(id);
        if (!object) {
            return undefined;
        }
        var ownerId = object.getAttrWithoutException('ownerId');
        if (ownerId !== this.objectId) {
            return undefined;
        }
        return object;
    };
    /**
     * Gets the owner ID of a record with the specified ID.
     *
     * @param id - The ID to search for
     * @returns The record with the specified ID, or undefined if not found
     *
     * @example
     * ```typescript
     * const record = symbolTable.getOwnerIdAt('some-object-id');
     * if (record) {
     *   console.log('Owner ID:', record.ownerId);
     * }
     * ```
     */
    AcDbSymbolTable.prototype.getOwnerIdAt = function (id) {
        return this.getIdAt(id);
    };
    /**
     * Creates an iterator object that can be used to iterate over the records in the table.
     *
     * @param iterateById - When true, iterate all records by object id (includes unnamed entries).
     *   When false (default), iterate named records only.
     * @returns An iterator object that can be used to iterate over the records
     *
     * @example
     * ```typescript
     * const iterator = symbolTable.newIterator();
     * for (const record of iterator) {
     *   console.log('Record:', record.name);
     * }
     * ```
     */
    AcDbSymbolTable.prototype.newIterator = function (iterateById) {
        if (iterateById === void 0) { iterateById = false; }
        if (iterateById) {
            return new AcDbObjectIterator(__spreadArray(__spreadArray([], __read(this._recordsByName.values()), false), __read(this._unnamedRecords), false));
        }
        return new AcDbObjectIterator(this._recordsByName);
    };
    /**
     * Normalizes the name of a symbol table record.
     *
     * Per AutoCAD convention, symbol table record names compare case-insensitively
     * and ignore leading and trailing whitespace. The default implementation trims
     * the name and uppercases it for internal map lookup.
     *
     * Some symbol table records require additional normalization. For example, the
     * model space block table record may appear as either `*Model_Space` or
     * `*MODEL_SPACE`, and should be standardized to a consistent form.
     *
     * Subclasses should override this method to implement record-specific rules
     * and call `super.normalizeName()` when no special handling applies.
     *
     * @param name - The raw name of the symbol table record.
     * @returns The normalized symbol table record name.
     */
    AcDbSymbolTable.prototype.normalizeName = function (name) {
        // LibreDWG / partial DXF can omit symbol names; treat missing as empty
        // so getAt/has/remove return "not found" instead of throwing on `.trim()`.
        return (name !== null && name !== void 0 ? name : '').trim().toUpperCase();
    };
    Object.defineProperty(AcDbSymbolTable.prototype, "dxfEntryCount", {
        /**
         * Entry count written to DXF group code 70 for this symbol table.
         *
         * Defaults to {@link numEntries}. Subclasses may override when unnamed records
         * are stored separately from {@link _recordsByName}.
         */
        get: function () {
            return this.numEntries;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Writes DXF fields for this object.
     *
     * @param filer - DXF output writer.
     * @returns The instance (for chaining).
     */
    AcDbSymbolTable.prototype.dxfOutFields = function (filer) {
        _super.prototype.dxfOutFields.call(this, filer);
        filer.writeSubclassMarker('AcDbSymbolTable');
        // DXF common symbol table group code 70: maximum number of entries.
        // We emit the current count, which matches typical DXF output behavior.
        filer.writeInt16(70, this.dxfEntryCount);
        return this;
    };
    /**
     * Removes a record from internal membership structures only.
     *
     * Named records are removed from {@link _recordsByName}; records without a
     * normalized name are removed from {@link _unnamedRecords}. Does not release
     * object handles or record transaction changes — callers must handle those
     * separately (see {@link removeRecord}).
     *
     * @param record - Symbol table record to remove from membership.
     */
    AcDbSymbolTable.prototype.removeFromMembership = function (record) {
        var normalizedName = this.normalizeName(record.name);
        if (normalizedName) {
            this._recordsByName.delete(normalizedName);
            return;
        }
        var index = this._unnamedRecords.indexOf(record);
        if (index >= 0) {
            this._unnamedRecords.splice(index, 1);
        }
    };
    return AcDbSymbolTable;
}(AcDbObject));
export { AcDbSymbolTable };
//# sourceMappingURL=AcDbSymbolTable.js.map