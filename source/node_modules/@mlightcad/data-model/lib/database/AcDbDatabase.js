var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
var __values = (this && this.__values) || function(o) {
    var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
    if (m) return m.call(o);
    if (o && typeof o.length === "number") return {
        next: function () {
            if (o && i >= o.length) o = void 0;
            return { value: o && o[i++], done: !o };
        }
    };
    throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
/* eslint-disable simple-import-sort/imports */
import { AcCmColor, AcCmColorMethod, AcCmEventManager, AcCmTransparency } from '@mlightcad/common';
import { AcDbDxfFiler } from '../base/AcDbDxfFiler';
import { AcDbObject, TEMP_OBJECT_ID_PREFIX, acdbAssignWorkingDatabase } from '../base/AcDbObject';
import { AcDbOpenMode } from '../base/AcDbOpenMode';
import { AcDbRegenerator } from '../converter/AcDbRegenerator';
import { AcDbDatabaseConverterManager, AcDbFileType } from './AcDbDatabaseConverterManager';
import { AcDbPolyline } from '../entity/AcDbPolyline';
import { ACAD_APPID, ACDB_COMPAREHATCH_DEFAULT, ACDB_COMPAREHATCH_MAX, ACDB_COMPAREHATCH_MIN, ACDB_COMPARERCMARGIN_DEFAULT, ACDB_COMPARERCMARGIN_MAX, ACDB_COMPARERCMARGIN_MIN, ACDB_COMPARETEXT_DEFAULT, ACDB_COMPARETEXT_MAX, ACDB_COMPARETEXT_MIN, ACDB_COMPARETOLERANCE_DEFAULT, ACDB_COMPARETOLERANCE_MAX, ACDB_COMPARETOLERANCE_MIN, acdbCoerceIntegerSysVar, ACTIVE_VPORT_NAME, ByBlock, ByLayer, DEFAULT_MLEADER_STYLE, DEFAULT_MLINE_STYLE, DEFAULT_LINE_TYPE, DEFAULT_TEXT_STYLE, MLIGHTCAD_APPID } from '../misc/AcDbConstants';
import { AcDbAngleUnits } from '../misc/AcDbAngleUnits';
import { AcDbDataGenerator } from '../misc/AcDbDataGenerator';
import { ACDB_DRAW_CIRCLE_SIDES_DRAFT } from '../misc/AcDbDrawTessellate';
import { AcDbFormatter } from '../misc/AcDbFormatter';
import { AcDbLinearUnits } from '../misc/AcDbLinearUnits';
import { AcDbUnitsValue } from '../misc/AcDbUnitsValue';
import { AcDbDictionary } from '../object/AcDbDictionary';
import { AcDbMLeaderStyle } from '../object/AcDbMLeaderStyle';
import { AcDbMlineStyle } from '../object/AcDbMlineStyle';
import { AcDbXrecord } from '../object/AcDbXrecord';
import { AcLyLayerFilterTree } from '../ly/AcLyLayerFilterTree';
import { ACAD_LAYERFILTERS_NAME, ACLY_DICTIONARY_NAME, acdbLayerGroupsToResultBuffer, acdbSerializeLayerFilterTree } from '../ly/AcLyLayerFilterIO';
import { acdbBytesToHexString } from '../misc/proxyGraphic';
import { AcDbBlockTable } from './AcDbBlockTable';
import { AcDbBlockTableRecord } from './AcDbBlockTableRecord';
import { AcDbOpenDatabaseError } from './AcDbOpenDatabaseError';
import { AcDbDimStyleTable } from './AcDbDimStyleTable';
import { AcDbDimStyleTableRecord } from './AcDbDimStyleTableRecord';
import { AcDbLayerTable } from './AcDbLayerTable';
import { AcDbLayerTableRecord } from './AcDbLayerTableRecord';
import { AcDbLinetypeTable } from './AcDbLinetypeTable';
import { AcDbLinetypeTableRecord } from './AcDbLinetypeTableRecord';
import { AcDbTextStyleTable } from './AcDbTextStyleTable';
import { AcDbTextStyleTableRecord } from './AcDbTextStyleTableRecord';
import { AcDbUcsTable } from './AcDbUcsTable';
import { AcDbViewTable } from './AcDbViewTable';
import { AcDbViewportTable } from './AcDbViewportTable';
import { AcDbViewportTableRecord } from './AcDbViewportTableRecord';
import { AcGeBox3d, AcGeCircArc2d } from '@mlightcad/geometry-engine';
import { AcDbDwgVersion } from './AcDbDwgVersion';
import { AcGiLineWeight } from '@mlightcad/graphic-interface';
import { AcDbRegAppTable } from './AcDbRegAppTable';
import { AcDbRegAppTableRecord } from './AcDbRegAppTableRecord';
import { AcDbSysVarManager } from './AcDbSysVarManager';
import { AcDbSystemVariables } from './AcDbSystemVariables';
import { AcDbLayout } from '../object/layout/AcDbLayout';
import { AcDbLayoutDictionary } from '../object/layout/AcDbLayoutDictionary';
import { AcDbDatabaseTransactionManager } from './transaction/AcDbDatabaseTransactionManager';
/**
 * The AcDbDatabase class represents an AutoCAD drawing file.
 *
 * Each AcDbDatabase object contains the various header variables, symbol tables,
 * table records, entities, and objects that make up the drawing. The AcDbDatabase
 * class has member functions to allow access to all the symbol tables, to read
 * and write to DWG files, to get or set database defaults, to execute various
 * database-level operations, and to get or set all header variables.
 *
 * @example
 * ```typescript
 * const database = new AcDbDatabase();
 * await database.read(dxfData, { readOnly: true });
 * const entities = database.tables.blockTable.modelSpace.entities;
 * ```
 */
var AcDbDatabase = /** @class */ (function (_super) {
    __extends(AcDbDatabase, _super);
    /**
     * Creates a new AcDbDatabase instance.
     */
    function AcDbDatabase() {
        var _this = _super.call(this, { objectId: '0' }) || this;
        /** Class definitions from DXF CLASSES / DWG class table (needed for proxy entities). */
        _this._classes = [];
        /** Global registry of committed object handles across all database-resident objects */
        _this._handleRegistry = new Map();
        /**
         * When false, entities on non-plottable layers are not drawn (viewer semantics).
         * Set from {@link AcDbOpenDatabaseOptions.drawNoPlotLayers} when opening a database.
         */
        _this._drawNoPlotLayers = true;
        /**
         * Display tessellation side count for a full circle.
         * Set from {@link AcDbOpenDatabaseOptions.circleSides} when opening a database.
         */
        _this._drawCircleSides = ACDB_DRAW_CIRCLE_SIDES_DRAFT;
        _this._eventBatchDepth = 0;
        _this._pendingEntityAppended = [];
        _this._pendingEntityErased = [];
        _this._pendingDictObjectSet = [];
        _this._pendingDictObjectErased = [];
        _this._lastOpenError = null;
        /**
         * Events that can be triggered by the database.
         *
         * These events allow applications to respond to various database operations
         * such as entity modifications, layer changes, and progress updates.
         */
        _this.events = {
            /** Fired when an object is set to the dictionary */
            dictObjetSet: new AcCmEventManager(),
            /** Fired when an object in the dictionary is removed */
            dictObjectErased: new AcCmEventManager(),
            /** Fired when an entity is appended to the database */
            entityAppended: new AcCmEventManager(),
            /** Fired when an entity is modified in the database */
            entityModified: new AcCmEventManager(),
            /** Fired when an entity is erased from the database */
            entityErased: new AcCmEventManager(),
            /** Fired when a layer is appended to the database */
            layerAppended: new AcCmEventManager(),
            /** Fired when a layer is modified in the database */
            layerModified: new AcCmEventManager(),
            /** Fired when a layer is erased from the database */
            layerErased: new AcCmEventManager(),
            /** Fired during database opening operations to report progress */
            openProgress: new AcCmEventManager(),
            /** Fired when {@link AcDbDatabase.read} or {@link AcDbDatabase.openUri} fails */
            openFailed: new AcCmEventManager()
        };
        AcDbDatabase._unsavedDrawingSequence += 1;
        _this._dwgname = "Drawing".concat(AcDbDatabase._unsavedDrawingSequence, ".dwg");
        _this._version = new AcDbDwgVersion('AC1014');
        _this._angbase = 0;
        _this._angdir = 0;
        _this._aunits = AcDbAngleUnits.DecimalDegrees;
        _this._auprec = 0;
        _this._lunits = AcDbLinearUnits.Decimal;
        _this._luprec = 4;
        _this._celtscale = 1;
        _this._cecolor = new AcCmColor();
        _this._celtype = ByLayer;
        _this._celweight = AcGiLineWeight.ByLayer;
        _this._cetransparency = new AcCmTransparency();
        _this._clayer = '0';
        _this._cmlstyle = DEFAULT_MLINE_STYLE;
        _this._cmlscale = 1;
        _this._cmleaderstyle = DEFAULT_MLEADER_STYLE;
        _this._hpbackgroundcolor = new AcCmColor(AcCmColorMethod.None);
        _this._hpcolor = _this._cecolor.clone();
        _this._hplayer = '.';
        _this._hptransparency = new AcCmTransparency();
        _this._textstyle = DEFAULT_TEXT_STYLE;
        _this._dimstyle = DEFAULT_TEXT_STYLE;
        _this._extents = new AcGeBox3d();
        // TODO: Default value is 1 (imperial) or 4 (metric)
        _this._insunits = AcDbUnitsValue.Millimeters;
        _this._unitmode = 0;
        _this._measurement = 1;
        _this._ltscale = 1;
        _this._lwdisplay = false;
        _this._tilemode = true;
        _this._psltscale = true;
        _this._pdmode = 0;
        _this._pdsize = 0;
        _this._osmode = 0;
        _this._orthomode = 0;
        _this._comparehatch = ACDB_COMPAREHATCH_DEFAULT;
        _this._comparercmargin = ACDB_COMPARERCMARGIN_DEFAULT;
        _this._comparetext = ACDB_COMPARETEXT_DEFAULT;
        _this._comparetolerance = ACDB_COMPARETOLERANCE_DEFAULT;
        _this._maxHandle = 0;
        _this._tables = {
            appIdTable: new AcDbRegAppTable(_this),
            blockTable: new AcDbBlockTable(_this),
            dimStyleTable: new AcDbDimStyleTable(_this),
            linetypeTable: new AcDbLinetypeTable(_this),
            textStyleTable: new AcDbTextStyleTable(_this),
            ucsTable: new AcDbUcsTable(_this),
            viewTable: new AcDbViewTable(_this),
            layerTable: new AcDbLayerTable(_this),
            viewportTable: new AcDbViewportTable(_this)
        };
        _this._objects = {
            dictionary: new AcDbDictionary(_this),
            group: new AcDbDictionary(_this),
            imageDefinition: new AcDbDictionary(_this),
            layerFilter: new AcDbDictionary(_this),
            layerIndex: new AcDbDictionary(_this),
            layout: new AcDbLayoutDictionary(_this),
            mleaderStyle: new AcDbDictionary(_this),
            mlineStyle: new AcDbDictionary(_this),
            sortentsTable: new AcDbDictionary(_this),
            xrecord: new AcDbDictionary(_this)
        };
        _this._layerFilters = new AcLyLayerFilterTree();
        _this.transactionManager = new AcDbDatabaseTransactionManager(_this);
        _this.registerBootstrapHandles();
        return _this;
    }
    Object.defineProperty(AcDbDatabase.prototype, "tables", {
        /**
         * Gets all tables in this drawing database.
         *
         * @returns Object containing all the symbol tables in the database
         *
         * @example
         * ```typescript
         * const tables = database.tables;
         * const layers = tables.layerTable;
         * const blocks = tables.blockTable;
         * ```
         */
        get: function () {
            return this._tables;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "classes", {
        /**
         * Gets or sets class definitions from the drawing (DXF CLASSES / DWG class table).
         *
         * Proxy entities resolve their application class ID (group code **91**)
         * against this list (IDs start at 500 for the first entry).
         */
        get: function () {
            return this._classes;
        },
        set: function (classes) {
            this._classes = classes.map(function (entry) { return (__assign({}, entry)); });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "objects", {
        /**
         * Gets all nongraphical objects in this drawing database.
         *
         * @returns Object containing all nongraphical objects in the database
         *
         * @example
         * ```typescript
         * const objects = database.objects;
         * const layout = objects.layout;
         * ```
         */
        get: function () {
            return this._objects;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "layerFilters", {
        /**
         * Gets the Layer Properties Manager filter tree for this database.
         *
         * @remarks
         * Mirrors AutoCAD .NET `Database.LayerFilters` / ObjectARX `AcLy*` filters.
         * This is the property/group filter **tree**, not the flat
         * {@link objects.layerFilter} dictionary used by `AcDbLayerFilter` /
         * `AcDbLayerIndex`.
         *
         * Like AutoCAD, treat this value as retrieved by value: mutate the tree,
         * then assign it back via the setter if your workflow expects that pattern.
         *
         * @returns The current {@link AcLyLayerFilterTree}.
         *
         * @example
         * ```typescript
         * const tree = db.layerFilters;
         * const filter = new AcLyLayerFilter();
         * filter.name = 'Unlocked Layers';
         * filter.setFilterExpression('LOCKED=="False"');
         * tree.root.addNested(filter);
         * db.layerFilters = tree;
         * ```
         */
        get: function () {
            return this._layerFilters;
        },
        /**
         * Sets the Layer Properties Manager filter tree for this database.
         *
         * @param value - New filter tree.
         */
        set: function (value) {
            this._layerFilters = value !== null && value !== void 0 ? value : new AcLyLayerFilterTree();
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Looks up a database-resident object by its object ID.
     *
     * Uses the global handle registry maintained by {@link registerObjectHandle}.
     *
     * @param id - Object identifier to resolve
     * @param _openErased - Reserved for erased-object support
     * @returns The matching object, or undefined if not found
     */
    AcDbDatabase.prototype.getObjectById = function (id, _openErased) {
        if (_openErased === void 0) { _openErased = false; }
        if (id === this.objectId) {
            return this;
        }
        return this._handleRegistry.get(id);
    };
    /**
     * Returns true when the transaction manager is actively recording changes.
     *
     * Shortcut for {@link AcDbDatabaseTransactionManager.isRecording}.
     */
    AcDbDatabase.prototype.isUndoRecording = function () {
        return this.transactionManager.isRecording();
    };
    /**
     * Opens a database object, preferring the active transaction when present.
     *
     * Editor shortcut: when a transaction is active, objects are opened through it
     * so mutations are tracked for undo. Otherwise falls back to {@link getObjectById}.
     */
    AcDbDatabase.prototype.openObject = function (objectId, mode) {
        var tr = this.transactionManager.currentTransaction();
        if (tr) {
            var opened = tr.getObject(objectId, mode);
            if (opened) {
                return opened;
            }
        }
        return this.getObjectById(objectId);
    };
    /**
     * Opens a database object for read through the active transaction when present.
     *
     * Editor shortcut; see {@link openObject}.
     */
    AcDbDatabase.prototype.openObjectForRead = function (objectId) {
        return this.openObject(objectId, AcDbOpenMode.kForRead);
    };
    /**
     * Opens a database object for write through the active transaction when present.
     *
     * Editor shortcut; see {@link openObject}.
     */
    AcDbDatabase.prototype.openObjectForWrite = function (objectId) {
        return this.openObject(objectId, AcDbOpenMode.kForWrite);
    };
    /**
     * Opens an entity for read through the active transaction when present.
     *
     * Editor shortcut; see {@link openObject}.
     */
    AcDbDatabase.prototype.openEntityForRead = function (entityOrId) {
        var objectId = typeof entityOrId === 'string' ? entityOrId : entityOrId.objectId;
        return this.openObjectForRead(objectId);
    };
    /**
     * Opens an entity for write through the active transaction when present.
     *
     * Editor shortcut; see {@link openObject}.
     */
    AcDbDatabase.prototype.openEntityForWrite = function (entityOrId) {
        var objectId = typeof entityOrId === 'string' ? entityOrId : entityOrId.objectId;
        return this.openObjectForWrite(objectId);
    };
    /**
     * Runs a database mutation as one undoable operation.
     *
     * Editor shortcut: skips creating a new undo mark when the transaction manager
     * is already recording (nested editor operations). Otherwise wraps `fn` in
     * {@link AcDbDatabaseTransactionManager.runUndoable}.
     */
    AcDbDatabase.prototype.runDatabaseEdit = function (label, fn) {
        if (this.isUndoRecording()) {
            fn();
            return;
        }
        this.transactionManager.runUndoable(label, fn);
    };
    /**
     * Returns the top-level named object dictionaries owned by this database.
     */
    AcDbDatabase.prototype.getRootDictionaries = function () {
        return [
            this.objects.dictionary,
            this.objects.group,
            this.objects.imageDefinition,
            this.objects.layerFilter,
            this.objects.layerIndex,
            this.objects.layout,
            this.objects.mleaderStyle,
            this.objects.mlineStyle,
            this.objects.sortentsTable,
            this.objects.xrecord
        ];
    };
    /**
     * Begins suppressing database events until {@link endEventBatch} is called.
     */
    AcDbDatabase.prototype.beginEventBatch = function () {
        this._eventBatchDepth++;
    };
    /**
     * Ends event batching and dispatches accumulated entity and layer events when the outermost batch closes.
     */
    AcDbDatabase.prototype.endEventBatch = function () {
        if (this._eventBatchDepth <= 0) {
            return;
        }
        this._eventBatchDepth--;
        if (this._eventBatchDepth === 0) {
            this.transactionManager.flushPendingEntityModifiedEvents();
            this.transactionManager.flushPendingLayerModifiedEvents();
            this.flushEventBatch();
        }
    };
    /**
     * Ends the outermost event batch, flushing `entityAppended` notifications in
     * chunks so converters can report ENTITY progress while the viewer adds/renders.
     *
     * Nested batches still require matching {@link endEventBatch} /
     * {@link endEventBatchChunked} calls; only the outermost close flushes.
     *
     * @param chunkSize - Max entities per `entityAppended` dispatch
     * @param onChunk - Called after each chunk with `(flushed, total)`
     */
    AcDbDatabase.prototype.endEventBatchChunked = function (chunkSize, onChunk) {
        return __awaiter(this, void 0, void 0, function () {
            var appended, total, size, flushed, i, end, chunk;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        if (this._eventBatchDepth <= 0) {
                            return [2 /*return*/];
                        }
                        this._eventBatchDepth--;
                        if (this._eventBatchDepth !== 0) {
                            return [2 /*return*/];
                        }
                        this.transactionManager.flushPendingEntityModifiedEvents();
                        this.transactionManager.flushPendingLayerModifiedEvents();
                        appended = this._pendingEntityAppended;
                        this._pendingEntityAppended = [];
                        total = appended.length;
                        size = Math.max(1, chunkSize);
                        if (total === 0) {
                            this.flushEventBatchRemainder();
                            return [2 /*return*/];
                        }
                        flushed = 0;
                        _a.label = 1;
                    case 1:
                        _a.trys.push([1, , 6, 7]);
                        i = 0;
                        _a.label = 2;
                    case 2:
                        if (!(i < total)) return [3 /*break*/, 5];
                        end = Math.min(i + size, total);
                        chunk = appended.slice(i, end);
                        this.events.entityAppended.dispatch({
                            database: this,
                            entity: chunk
                        });
                        flushed = end;
                        if (!onChunk) return [3 /*break*/, 4];
                        return [4 /*yield*/, onChunk(end, total)];
                    case 3:
                        _a.sent();
                        _a.label = 4;
                    case 4:
                        i += size;
                        return [3 /*break*/, 2];
                    case 5: return [3 /*break*/, 7];
                    case 6:
                        if (flushed < total) {
                            this.events.entityAppended.dispatch({
                                database: this,
                                entity: appended.slice(flushed)
                            });
                        }
                        this.flushEventBatchRemainder();
                        return [7 /*endfinally*/];
                    case 7: return [2 /*return*/];
                }
            });
        });
    };
    /**
     * Returns true when database events are being batched.
     */
    AcDbDatabase.prototype.isEventBatched = function () {
        return this._eventBatchDepth > 0;
    };
    /**
     * Dispatches or queues an entity-appended notification.
     *
     * @param entity - One entity or batch of entities added to model/paper space
     */
    AcDbDatabase.prototype.notifyEntityAppended = function (entity) {
        var _a;
        if (this.isEventBatched()) {
            var items = Array.isArray(entity) ? entity : [entity];
            (_a = this._pendingEntityAppended).push.apply(_a, __spreadArray([], __read(items), false));
            return;
        }
        this.events.entityAppended.dispatch({
            database: this,
            entity: entity
        });
    };
    /**
     * Dispatches or queues an entity-erased notification.
     *
     * @param entity - One entity or batch of entities removed from model/paper space
     */
    AcDbDatabase.prototype.notifyEntityErased = function (entity) {
        var _a;
        if (this.isEventBatched()) {
            var items = Array.isArray(entity) ? entity : [entity];
            (_a = this._pendingEntityErased).push.apply(_a, __spreadArray([], __read(items), false));
            return;
        }
        this.events.entityErased.dispatch({
            database: this,
            entity: entity
        });
    };
    /**
     * Dispatches or queues a dictionary-object-set notification.
     *
     * @param object - Object inserted or replaced in a named dictionary
     * @param key - Dictionary key under which the object is stored
     */
    AcDbDatabase.prototype.notifyDictObjectSet = function (object, key) {
        if (this.isEventBatched()) {
            this._pendingDictObjectSet.push({ object: object, key: key });
            return;
        }
        this.events.dictObjetSet.dispatch({
            database: this,
            object: object,
            key: key
        });
    };
    /**
     * Dispatches or queues a dictionary-object-erased notification.
     *
     * @param object - Object removed from a named dictionary
     * @param key - Dictionary key that previously referenced the object
     */
    AcDbDatabase.prototype.notifyDictObjectErased = function (object, key) {
        if (this.isEventBatched()) {
            this._pendingDictObjectErased.push({ object: object, key: key });
            return;
        }
        this.events.dictObjectErased.dispatch({
            database: this,
            object: object,
            key: key
        });
    };
    /**
     * Dispatches all notifications accumulated while event batching was active.
     *
     * Appended and erased entities are dispatched in batch arrays where applicable.
     */
    AcDbDatabase.prototype.flushEventBatch = function () {
        if (this._pendingEntityAppended.length > 0) {
            var appended = this._pendingEntityAppended;
            this._pendingEntityAppended = [];
            this.events.entityAppended.dispatch({
                database: this,
                entity: appended
            });
        }
        this.flushEventBatchRemainder();
    };
    /** Flushes non-append batch queues (erase / dictionary) after entity appends. */
    AcDbDatabase.prototype.flushEventBatchRemainder = function () {
        var e_1, _a, e_2, _b;
        if (this._pendingEntityErased.length > 0) {
            var erased = this._pendingEntityErased;
            this._pendingEntityErased = [];
            this.events.entityErased.dispatch({
                database: this,
                entity: erased
            });
        }
        if (this._pendingDictObjectSet.length > 0) {
            var dictSet = this._pendingDictObjectSet;
            this._pendingDictObjectSet = [];
            try {
                for (var dictSet_1 = __values(dictSet), dictSet_1_1 = dictSet_1.next(); !dictSet_1_1.done; dictSet_1_1 = dictSet_1.next()) {
                    var _c = dictSet_1_1.value, object = _c.object, key = _c.key;
                    this.events.dictObjetSet.dispatch({
                        database: this,
                        object: object,
                        key: key
                    });
                }
            }
            catch (e_1_1) { e_1 = { error: e_1_1 }; }
            finally {
                try {
                    if (dictSet_1_1 && !dictSet_1_1.done && (_a = dictSet_1.return)) _a.call(dictSet_1);
                }
                finally { if (e_1) throw e_1.error; }
            }
        }
        if (this._pendingDictObjectErased.length > 0) {
            var dictErased = this._pendingDictObjectErased;
            this._pendingDictObjectErased = [];
            try {
                for (var dictErased_1 = __values(dictErased), dictErased_1_1 = dictErased_1.next(); !dictErased_1_1.done; dictErased_1_1 = dictErased_1.next()) {
                    var _d = dictErased_1_1.value, object = _d.object, key = _d.key;
                    this.events.dictObjectErased.dispatch({
                        database: this,
                        object: object,
                        key: key
                    });
                }
            }
            catch (e_2_1) { e_2 = { error: e_2_1 }; }
            finally {
                try {
                    if (dictErased_1_1 && !dictErased_1_1.done && (_b = dictErased_1.return)) _b.call(dictErased_1);
                }
                finally { if (e_2) throw e_2.error; }
            }
        }
    };
    Object.defineProperty(AcDbDatabase.prototype, "formatter", {
        /**
         * Formatter for linear distances, point coordinates, and angles using this database's
         * **LUNITS**, **AUNITS**, and related system variables.
         *
         * @example
         * ```typescript
         * database.formatter.formatLength(12.3456);
         * database.formatter.formatPoint3d(point);
         * database.formatter.formatAngle(angleRadians, { showUnits: true });
         * ```
         */
        get: function () {
            var _a;
            return ((_a = this._formatter) !== null && _a !== void 0 ? _a : (this._formatter = new AcDbFormatter(this)));
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Generates a new unique object ID (handle) for the database.
     * The handle is a hexadecimal string that increments from the current max handle.
     *
     * @returns A new unique object ID as a hexadecimal string
     *
     * @example
     * ```typescript
     * const newHandle = database.generateHandle();
     * console.log(`New handle: ${newHandle}`);
     * ```
     */
    AcDbDatabase.prototype.generateHandle = function () {
        this._maxHandle++;
        return this._maxHandle.toString(16).toUpperCase();
    };
    /**
     * Generates a handle that is not already registered in this database.
     *
     * @internal
     */
    AcDbDatabase.prototype.generateUniqueHandle = function () {
        var handle = this.generateHandle();
        while (this.isHandleTaken(handle)) {
            handle = this.generateHandle();
        }
        return handle;
    };
    /**
     * Initializes {@link generateHandle} from a DXF/DWG `$HANDSEED` value.
     *
     * `$HANDSEED` is the next handle AutoCAD would assign; internal `_maxHandle`
     * is kept one below that value.
     *
     * @param seed - Hexadecimal handle seed from the drawing header
     */
    AcDbDatabase.prototype.initializeHandleSeed = function (seed) {
        var next = parseInt(seed, 16);
        if (!isNaN(next) && next > 0) {
            var baseline = next - 1;
            if (baseline > this._maxHandle) {
                this._maxHandle = baseline;
            }
        }
    };
    /**
     * Adopts a handle from an external drawing when it is still available.
     *
     * When the preferred handle is already registered to another object, a new
     * unique handle is generated instead.
     *
     * @internal
     */
    AcDbDatabase.prototype.adoptExternalHandle = function (object, preferredId) {
        this.releaseObjectHandle(object);
        if (preferredId &&
            !preferredId.startsWith(TEMP_OBJECT_ID_PREFIX) &&
            !this.isHandleTaken(preferredId)) {
            object.objectId = preferredId;
            this.registerObjectHandle(object);
            this.updateMaxHandle(preferredId);
            return preferredId;
        }
        return this.assignGeneratedHandle(object);
    };
    /**
     * Registers an object's current {@link AcDbObject.objectId} in the global handle map.
     *
     * @internal
     */
    AcDbDatabase.prototype.registerObjectHandle = function (object) {
        var objectId = object.objectId;
        if (!objectId || objectId.startsWith(TEMP_OBJECT_ID_PREFIX)) {
            return;
        }
        this._handleRegistry.set(objectId, object);
    };
    /**
     * Removes an object's handle from the global handle map.
     *
     * @internal
     */
    AcDbDatabase.prototype.releaseObjectHandle = function (object) {
        var objectId = object.objectId;
        if (!objectId || objectId.startsWith(TEMP_OBJECT_ID_PREFIX)) {
            return;
        }
        if (this._handleRegistry.get(objectId) === object) {
            this._handleRegistry.delete(objectId);
        }
    };
    /**
     * Returns true when a handle is already registered to another object.
     *
     * @internal
     */
    AcDbDatabase.prototype.isHandleTaken = function (objectId, except) {
        if (!objectId || objectId.startsWith(TEMP_OBJECT_ID_PREFIX)) {
            return false;
        }
        var owner = this._handleRegistry.get(objectId);
        return owner !== undefined && owner !== except;
    };
    /**
     * Registers bootstrap tables, dictionaries, and the database object itself.
     */
    AcDbDatabase.prototype.registerBootstrapHandles = function () {
        var e_3, _a, e_4, _b;
        this._handleRegistry.clear();
        this.registerObjectHandle(this);
        try {
            for (var _c = __values([
                this._tables.blockTable,
                this._tables.dimStyleTable,
                this._tables.layerTable,
                this._tables.linetypeTable,
                this._tables.appIdTable,
                this._tables.textStyleTable,
                this._tables.ucsTable,
                this._tables.viewTable,
                this._tables.viewportTable
            ]), _d = _c.next(); !_d.done; _d = _c.next()) {
                var table = _d.value;
                this.registerObjectHandle(table);
            }
        }
        catch (e_3_1) { e_3 = { error: e_3_1 }; }
        finally {
            try {
                if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
            }
            finally { if (e_3) throw e_3.error; }
        }
        try {
            for (var _e = __values([
                this._objects.dictionary,
                this._objects.group,
                this._objects.imageDefinition,
                this._objects.layerFilter,
                this._objects.layerIndex,
                this._objects.layout,
                this._objects.mleaderStyle,
                this._objects.mlineStyle,
                this._objects.sortentsTable,
                this._objects.xrecord
            ]), _f = _e.next(); !_f.done; _f = _e.next()) {
                var dictionary = _f.value;
                this.registerObjectHandle(dictionary);
            }
        }
        catch (e_4_1) { e_4 = { error: e_4_1 }; }
        finally {
            try {
                if (_f && !_f.done && (_b = _e.return)) _b.call(_e);
            }
            finally { if (e_4) throw e_4.error; }
        }
    };
    /**
     * Assigns a freshly generated unique handle to an object.
     *
     * @internal
     */
    AcDbDatabase.prototype.assignGeneratedHandle = function (object) {
        var oldId = object.objectId;
        this.releaseObjectHandle(object);
        var handle = this.generateUniqueHandle();
        object.objectId = handle;
        this.registerObjectHandle(object);
        if (oldId && oldId !== handle) {
            this.updateMaxHandle(handle);
        }
        return handle;
    };
    /**
     * Updates the maximum handle value if the provided handle is greater.
     * This is called when setting an object's objectId from external sources (e.g., reading DXF/DWG).
     *
     * @param handle - The handle to check and potentially update maxHandle with
     *
     * @example
     * ```typescript
     * database.updateMaxHandle('1A2B');
     * ```
     */
    AcDbDatabase.prototype.updateMaxHandle = function (handle) {
        var handleValue = parseInt(handle, 16);
        if (!isNaN(handleValue) && handleValue > this._maxHandle) {
            this._maxHandle = handleValue;
        }
    };
    /**
     * Commits an object's handle into the database.
     *
     * Generates a new handle when the object doesn't have one, when it is temporary,
     * or when a duplicate id exists in the target collection.
     *
     * @internal
     */
    AcDbDatabase.prototype.commitObjectHandle = function (object, hasId) {
        var objectId = object.getAttrWithoutException('objectId');
        var tableDuplicate = hasId != null && objectId != null && hasId(objectId);
        var needsGenerated = !objectId || object.isTemp || tableDuplicate;
        if (needsGenerated) {
            this.assignGeneratedHandle(object);
            return;
        }
        var incumbent = this._handleRegistry.get(objectId);
        if (incumbent && incumbent !== object) {
            if (this.shouldPreserveIncomingHandle(object)) {
                this.assignGeneratedHandle(incumbent);
            }
            else {
                this.assignGeneratedHandle(object);
                return;
            }
        }
        this.registerObjectHandle(object);
        this.updateMaxHandle(objectId);
    };
    /**
     * Returns true when an object already carries an explicit handle that should
     * be preserved over auto-generated registry occupants (typically DXF/DWG imports).
     */
    AcDbDatabase.prototype.shouldPreserveIncomingHandle = function (object) {
        return !object.isTemp;
    };
    Object.defineProperty(AcDbDatabase.prototype, "currentSpaceId", {
        /**
         * Gets the object ID of the AcDbBlockTableRecord of the current space.
         *
         * The current space can be either model space or paper space.
         *
         * @returns The object ID of the current space
         *
         * @example
         * ```typescript
         * const currentSpaceId = database.currentSpaceId;
         * ```
         */
        get: function () {
            if (!this._currentSpace) {
                this._currentSpace = this._tables.blockTable.modelSpace;
            }
            return this._currentSpace.objectId;
        },
        /**
         * Sets the current space by object ID.
         *
         * @param value - The object ID of the block table record to set as current space
         * @throws {Error} When the specified block table record ID doesn't exist
         *
         * @example
         * ```typescript
         * database.currentSpaceId = 'some-block-record-id';
         * ```
         */
        set: function (value) {
            var currentSpace = this.tables.blockTable.getIdAt(value);
            if (currentSpace == null) {
                throw new Error("[AcDbDatabase] The specified block table record id '".concat(value, "' doesn't exist in the drawing database!"));
            }
            else {
                this._currentSpace = currentSpace;
            }
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "aunits", {
        /**
         * Angular unit **display and entry** format for the drawing (AutoCAD system variable **AUNITS**).
         *
         * This does not change how angles are stored internally (radians in geometry); it controls how
         * angles are formatted in the UI and how numeric angle input is interpreted, together with
         * {@link angbase} (**ANGBASE**) and {@link angdir} (**ANGDIR**).
         *
         * @returns Integer code matching {@link AcDbAngleUnits}:
         *
         * | Value | Meaning |
         * |------:|---------|
         * | `0` | **Decimal degrees** ??e.g. `45.5` |
         * | `1` | **Degrees/minutes/seconds** ??e.g. `45d30'15"` |
         * | `2` | **Gradians** ??e.g. `50g` (400 grads = full circle) |
         * | `3` | **Radians** ??e.g. `0.785398...` |
         * | `4` | **Surveyor's units** ??quadrant bearing notation (e.g. `N 45d30'15" E`) |
         *
         * @remarks
         * Prefer assigning {@link AcDbAngleUnits} enum members for readability instead of raw integers.
         *
         * @see {@link AcDbAngleUnits} for the canonical enum used by this codebase.
         * @see {@link https://help.autodesk.com/view/ACD/2027/ENU/?caas=caas/documentation/ACD/2014/ENU/files/GUID-C7C0F6A5-7982-43DB-97F9-5B9B0044E9FA-htm.html | AutoCAD Help: AUNITS}
         *
         * @example
         * ```typescript
         * const angleUnits = database.aunits;
         * ```
         */
        get: function () {
            return this._aunits;
        },
        /**
         * Sets **AUNITS** ??the angular unit display format (see {@link aunits} getter for value meanings).
         *
         * @param value - Integer `0`??4` per {@link AcDbAngleUnits}, or `undefined`/`null` coerced to `0` by the setter chain.
         *
         * @example
         * ```typescript
         * database.aunits = AcDbAngleUnits.DecimalDegrees;
         * ```
         */
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.AUNITS, this._aunits, value !== null && value !== void 0 ? value : 0, function (nextValue) {
                _this._aunits = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "auprec", {
        /**
         * Angular display precision for the drawing (**AUPREC**): how many decimal places (or equivalent)
         * are used when showing angles, in conjunction with {@link aunits}.
         *
         * AutoCAD typically uses integers in the range **0??**; behavior for other values is
         * implementation-defined in this library (stored as-is).
         *
         * @see {@link https://help.autodesk.com/view/ACD/2025/ENU/?guid=GUID-EE1ED20C-1096-4299-820F-83F1BC9B96F3 | AutoCAD Help: AUPREC}
         */
        get: function () {
            return this._auprec;
        },
        /**
         * Sets **AUPREC** ??angular display precision (see {@link auprec} getter).
         */
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.AUPREC, this._auprec, value !== null && value !== void 0 ? value : 0, function (nextValue) {
                _this._auprec = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "lunits", {
        /**
         * Linear unit **display and entry** format for coordinates and lengths (**LUNITS**).
         *
         * This does not set real-world drawing units for inserts (see {@link insunits}); it controls how
         * linear distances are shown and parsed (scientific, decimal, engineering, and so on).
         *
         * @returns Integer code matching {@link AcDbLinearUnits}:
         *
         * | Value | Meaning |
         * |------:|---------|
         * | `1` | **Scientific** |
         * | `2` | **Decimal** |
         * | `3` | **Engineering** (feet + decimal inches) |
         * | `4` | **Architectural** (feet + fractional inches) |
         * | `5` | **Fractional** |
         * | `6` | **Windows desktop** (processing / computational format) |
         *
         * @remarks
         * Prefer assigning {@link AcDbLinearUnits} enum members instead of raw integers.
         *
         * @see {@link AcDbLinearUnits}
         * @see {@link https://help.autodesk.com/view/ACD/2025/ENU/?guid=GUID-D7C80D1F-B1C0-44A9-898E-B3100FF391CB | AutoCAD Help: LUNITS}
         */
        get: function () {
            return this._lunits;
        },
        /**
         * Sets **LUNITS** ??linear display format (see {@link lunits} getter).
         *
         * @param value - Integer per {@link AcDbLinearUnits}, or coerced default {@link AcDbLinearUnits.Decimal} when `undefined`/`null`.
         */
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.LUNITS, this._lunits, value !== null && value !== void 0 ? value : AcDbLinearUnits.Decimal, function (nextValue) {
                _this._lunits = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "luprec", {
        /**
         * Linear display precision for the drawing (**LUPREC**): number of decimal places (or equivalent)
         * used when showing linear distances, together with {@link lunits}.
         *
         * AutoCAD typically uses integers in the range **0??**; initial value is commonly **4**.
         * Values outside that range are stored as-is by this library.
         *
         * @see {@link https://help.autodesk.com/view/ACD/2027/ENU/?guid=GUID-5FFF39D6-EFC7-49F5-B56A-6023EB5C0DE7 | AutoCAD Help: LUPREC}
         */
        get: function () {
            return this._luprec;
        },
        /**
         * Sets **LUPREC** ??linear display precision (see {@link luprec} getter).
         */
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.LUPREC, this._luprec, value !== null && value !== void 0 ? value : 4, function (nextValue) {
                _this._luprec = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "version", {
        /**
         * Gets the version of the database.
         *
         * @returns The version of the database
         *
         */
        get: function () {
            return this._version;
        },
        /**
         * Sets the version of the database.
         *
         * @param value - The version value of the database
         */
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.ACADVER, this._version, new AcDbDwgVersion(value), function (nextValue) {
                _this._version = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "insunits", {
        /**
         * Gets the drawing-units value for automatic scaling of blocks, images, or xrefs.
         *
         * This is the current INSUNITS value for the database.
         *
         * @returns The insertion units value
         *
         * @example
         * ```typescript
         * const insertionUnits = database.insunits;
         * ```
         */
        get: function () {
            return this._insunits;
        },
        /**
         * Sets the drawing-units value for automatic scaling.
         *
         * @param value - The new insertion units value
         *
         * @example
         * ```typescript
         * database.insunits = AcDbUnitsValue.Millimeters;
         * ```
         */
        set: function (value) {
            var _this = this;
            // TODO: Default value is 1 (imperial) or 4 (metric)
            this.updateSysVar(AcDbSystemVariables.INSUNITS, this._insunits, value !== null && value !== void 0 ? value : 4, function (nextValue) {
                _this._insunits = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "unitmode", {
        /**
         * Controls how feet-inch and fractional linear values are delimited (**UNITMODE**).
         *
         * - `0`: Report format (for example `1'-3 1/2"`)
         * - `1`: Input format (for example `1'-3-1/2"`, fewer spaces)
         *
         * @see {@link https://help.autodesk.com/view/ACD/2027/ENU/?guid=GUID-C52134E8-10EB-4AE7-A0C0-8F798C68F823 | AutoCAD Help: UNITMODE}
         */
        get: function () {
            return this._unitmode;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.UNITMODE, this._unitmode, value !== null && value !== void 0 ? value : 0, function (nextValue) {
                _this._unitmode = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "measurement", {
        /**
         * Legacy drawing measurement system (**MEASUREMENT**): `0` = English, `1` = metric.
         *
         * When **INSUNITS** is unitless, this selects the default real-world unit family for labels.
         */
        get: function () {
            return this._measurement;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.MEASUREMENT, this._measurement, value !== null && value !== void 0 ? value : 1, function (nextValue) {
                _this._measurement = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "ltscale", {
        /**
         * Gets the line type scale factor.
         *
         * @returns The line type scale factor
         *
         * @example
         * ```typescript
         * const lineTypeScale = database.ltscale;
         * ```
         */
        get: function () {
            return this._ltscale;
        },
        /**
         * Sets the line type scale factor.
         *
         * @param value - The new line type scale factor
         *
         * @example
         * ```typescript
         * database.ltscale = 2.0;
         * ```
         */
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.LTSCALE, this._ltscale, value !== null && value !== void 0 ? value : 1, function (nextValue) {
                _this._ltscale = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "lwdisplay", {
        /**
         * Gets the flag whether to display line weight.
         *
         * @returns The flag whether to display line weight.
         *
         * @example
         * ```typescript
         * const lineTypeScale = database.ltscale;
         * ```
         */
        get: function () {
            return this._lwdisplay;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.LWDISPLAY, this._lwdisplay, value !== null && value !== void 0 ? value : false, function (nextValue) {
                _this._lwdisplay = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "drawNoPlotLayers", {
        /**
         * Sets the flag whether to display line weight.
         *
         * @param value - The flag whether to display line weight.
         *
         * @example
         * ```typescript
         * database.lwdisplay = true;
         * ```
         */
        /**
         * Whether entities on non-plottable layers should be drawn.
         *
         * Configured via {@link AcDbOpenDatabaseOptions.drawNoPlotLayers} when the
         * database is opened. Defaults to `true`.
         */
        get: function () {
            return this._drawNoPlotLayers;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "drawCircleSides", {
        /**
         * Display tessellation side count for a full circle.
         *
         * Configured via {@link AcDbOpenDatabaseOptions.circleSides} when the
         * database is opened. Defaults to {@link ACDB_DRAW_CIRCLE_SIDES_DRAFT}.
         */
        get: function () {
            return this._drawCircleSides;
        },
        set: function (value) {
            this._drawCircleSides = AcGeCircArc2d.resolveCircleSides(value);
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "dwgname", {
        /**
         * Name of the current drawing file (**DWGNAME**), including extension.
         *
         * Read-only through the system-variable API; updated when a drawing is opened
         * or via {@link setDwgName} after save.
         *
         * @see https://help.autodesk.com/view/ACD/2023/ENU/?caas=caas/documentation/ACD/2014/ENU/files/GUID-A89861EF-5F4F-46C6-A1DB-9D985A3858C9-htm.html
         */
        get: function () {
            return this._dwgname;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Updates **DWGNAME** after opening or saving a drawing.
     *
     * @param value - Drawing file name, including extension.
     */
    AcDbDatabase.prototype.setDwgName = function (value) {
        var _this = this;
        var normalized = value.trim();
        if (!normalized) {
            return;
        }
        this.updateSysVar(AcDbSystemVariables.DWGNAME, this._dwgname, normalized, function (nextValue) {
            _this._dwgname = nextValue;
        });
    };
    Object.defineProperty(AcDbDatabase.prototype, "thumbnailImage", {
        /**
         * Gets or sets the drawing thumbnail preview image.
         *
         * Corresponds to AutoCAD .NET `Database.ThumbnailBitmap`, DXF
         * `THUMBNAILIMAGE` section (group codes 90/310), and the DWG file preview.
         * Stored as raw image bytes (typically BMP/DIB or PNG).
         *
         * @returns Thumbnail image bytes, or `undefined` when none exist.
         */
        get: function () {
            return this._thumbnailImage;
        },
        set: function (value) {
            if (!value || value.length === 0) {
                this._thumbnailImage = undefined;
                return;
            }
            this._thumbnailImage = value;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Returns whether entities on the given layer should be drawn under the
     * current {@link drawNoPlotLayers} setting.
     *
     * Layer off/freeze visibility is handled separately by the viewer; this only
     * reflects the no-plot policy.
     */
    AcDbDatabase.prototype.isLayerDrawable = function (layerName) {
        if (this._drawNoPlotLayers) {
            return true;
        }
        var layer = this.tables.layerTable.getAt(layerName);
        return layer == null || layer.isPlottable;
    };
    Object.defineProperty(AcDbDatabase.prototype, "tilemode", {
        /** TILEMODE: true = model space (1), false = paper space (0). */
        get: function () {
            return this._tilemode;
        },
        set: function (value) {
            this._tilemode = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "psltscale", {
        /** PSLTSCALE: paper space linetype scaling. */
        get: function () {
            return this._psltscale;
        },
        set: function (value) {
            this._psltscale = value;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "cecolor", {
        /**
         * Gets the color of new objects as they are created.
         *
         * @returns The current entity color
         *
         * @example
         * ```typescript
         * const currentColor = database.cecolor;
         * ```
         */
        get: function () {
            return this._cecolor;
        },
        /**
         * Sets the color of new objects as they are created.
         *
         * @param value - The new current entity color
         *
         * @example
         * ```typescript
         * database.cecolor = new AcCmColor(0xFF0000);
         * ```
         */
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.CECOLOR, this._cecolor, value || 0, function (nextValue) {
                _this._cecolor = nextValue.clone();
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "celtscale", {
        /**
         * The line type scaling for new objects relative to the ltscale setting. A line created with
         * celtscale = 2 in a drawing with ltscale set to 0.5 would appear the same as a line created
         * with celtscale = 1 in a drawing with ltscale = 1.
         */
        get: function () {
            return this._celtscale;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.CELTSCALE, this._celtscale, value !== null && value !== void 0 ? value : 1, function (nextValue) {
                _this._celtscale = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "celtype", {
        /**
         * The linetype of new objects as they are created.
         */
        get: function () {
            return this._celtype;
        },
        set: function (value) {
            var _this = this;
            var nextValue = this.normalizeLinetypeName(value !== null && value !== void 0 ? value : ByLayer);
            this.updateSysVar(AcDbSystemVariables.CELTYPE, this._celtype, nextValue, function (normalizedValue) {
                _this._celtype = normalizedValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "celweight", {
        /**
         * The layer of new objects as they are created.
         */
        get: function () {
            return this._celweight;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.CELWEIGHT, this._celweight, value !== null && value !== void 0 ? value : AcGiLineWeight.ByLayer, function (nextValue) {
                _this._celweight = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "cetransparency", {
        /**
         * The transparency level of new objects as they are created.
         *
         * Can be ByLayer, ByBlock, or a value from 0 to 90 (percentage).
         */
        get: function () {
            return this._cetransparency;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.CETRANSPARENCY, this._cetransparency, value !== null && value !== void 0 ? value : new AcCmTransparency(), function (nextValue) {
                _this._cetransparency = nextValue.clone();
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "clayer", {
        /**
         * The layer of new objects as they are created.
         */
        get: function () {
            return this._clayer;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.CLAYER, this._clayer, value !== null && value !== void 0 ? value : '0', function (nextValue) {
                _this._clayer = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "cmlstyle", {
        /**
         * The multiline style name used for newly created MLINE entities.
         */
        get: function () {
            return this._cmlstyle;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.CMLSTYLE, this._cmlstyle, value !== null && value !== void 0 ? value : DEFAULT_MLINE_STYLE, function (nextValue) {
                _this._cmlstyle = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "cmlscale", {
        /**
         * The multiline scale used for newly created MLINE entities.
         */
        get: function () {
            return this._cmlscale;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.CMLSCALE, this._cmlscale, value !== null && value !== void 0 ? value : 1, function (nextValue) {
                _this._cmlscale = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "cmleaderstyle", {
        /**
         * The multileader style name used for newly created MLEADER entities.
         */
        get: function () {
            return this._cmleaderstyle;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.CMLEADERSTYLE, this._cmleaderstyle, value !== null && value !== void 0 ? value : DEFAULT_MLEADER_STYLE, function (nextValue) {
                _this._cmleaderstyle = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "hpbackgroundcolor", {
        /**
         * The default hatch background color string.
         */
        get: function () {
            return this._hpbackgroundcolor;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.HPBACKGROUNDCOLOR, this._hpbackgroundcolor, value !== null && value !== void 0 ? value : new AcCmColor(AcCmColorMethod.None), function (nextValue) {
                _this._hpbackgroundcolor = nextValue.clone();
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "hpcolor", {
        /**
         * The default color string used for newly created hatches.
         */
        get: function () {
            return this._hpcolor;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.HPCOLOR, this._hpcolor, value !== null && value !== void 0 ? value : this._cecolor, function (nextValue) {
                _this._hpcolor = nextValue.clone();
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "hplayer", {
        /**
         * The default layer used for newly created hatches and fills.
         */
        get: function () {
            return this._hplayer;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.HPLAYER, this._hplayer, value !== null && value !== void 0 ? value : '.', function (nextValue) {
                _this._hplayer = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "hptransparency", {
        /**
         * The default transparency string used for newly created hatches and fills.
         */
        get: function () {
            return this._hptransparency;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.HPTRANSPARENCY, this._hptransparency, value !== null && value !== void 0 ? value : new AcCmTransparency(), function (nextValue) {
                _this._hptransparency = nextValue.clone();
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "textstyle", {
        /**
         * The text style name for new text objects.
         */
        get: function () {
            return this._textstyle;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.TEXTSTYLE, this._textstyle, value !== null && value !== void 0 ? value : DEFAULT_TEXT_STYLE, function (nextValue) {
                _this._textstyle = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "dimstyle", {
        /**
         * The dimension style name for new dimension objects (`$DIMSTYLE`).
         */
        get: function () {
            return this._dimstyle;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.DIMSTYLE, this._dimstyle, value !== null && value !== void 0 ? value : DEFAULT_TEXT_STYLE, function (nextValue) {
                _this._dimstyle = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "angbase", {
        /**
         * The zero (0) base angle with respect to the current UCS in radians.
         */
        get: function () {
            return this._angbase;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.ANGBASE, this._angbase, value !== null && value !== void 0 ? value : 0, function (nextValue) {
                _this._angbase = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "angdir", {
        /**
         * The direction of positive angles.
         * - 0: Counterclockwise
         * - 1: Clockwise
         */
        get: function () {
            return this._angdir;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.ANGDIR, this._angdir, value !== null && value !== void 0 ? value : 0, function (nextValue) {
                _this._angdir = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "extmax", {
        /**
         * The current Model Space EXTMAX value
         */
        get: function () {
            return this._extents.max;
        },
        set: function (value) {
            if (value) {
                var oldExtMax = this._extents.max.clone();
                this._extents.expandByPoint(value);
                if (!this._extents.max.equals(oldExtMax)) {
                    this.triggerSysVarChangedEvent(AcDbSystemVariables.EXTMAX, oldExtMax, this._extents.max);
                }
            }
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "extmin", {
        /**
         * The current Model Space EXTMIN value
         */
        get: function () {
            return this._extents.min;
        },
        set: function (value) {
            if (value) {
                var oldExtMin = this._extents.min.clone();
                this._extents.expandByPoint(value);
                if (!this._extents.min.equals(oldExtMin)) {
                    this.triggerSysVarChangedEvent(AcDbSystemVariables.EXTMIN, oldExtMin, this._extents.min);
                }
            }
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "extents", {
        /**
         * The extents of current Model Space
         */
        get: function () {
            return this._extents;
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "pdmode", {
        /**
         * Point display mode. Please get more details on value of this property from [this page](https://help.autodesk.com/view/ACDLT/2022/ENU/?guid=GUID-82F9BB52-D026-4D6A-ABA6-BF29641F459B).
         */
        get: function () {
            return this._pdmode;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.PDMODE, this._pdmode, value !== null && value !== void 0 ? value : 0, function (nextValue) {
                _this._pdmode = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "pdsize", {
        /**
         * Point display size.
         * - 0: Creates a point at 5 percent of the drawing area height
         * - > 0: Specifies an absolute size
         * - < 0: Specifies a percentage of the viewport size
         */
        get: function () {
            return this._pdsize;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.PDSIZE, this._pdsize, value !== null && value !== void 0 ? value : 0, function (nextValue) {
                _this._pdsize = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "osmode", {
        /**
         * Running Object Snap (OSNAP) mode bitmask.
         */
        get: function () {
            return this._osmode;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.OSMODE, this._osmode, value !== null && value !== void 0 ? value : 0, function (nextValue) {
                _this._osmode = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "orthomode", {
        /**
         * Orthogonal mode flag (ORTHOMODE). When on, cursor movement is constrained
         * to horizontal or vertical relative to the current UCS.
         */
        get: function () {
            return this._orthomode;
        },
        set: function (value) {
            var _this = this;
            this.updateSysVar(AcDbSystemVariables.ORTHOMODE, this._orthomode, value !== null && value !== void 0 ? value : 0, function (nextValue) {
                _this._orthomode = nextValue;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "comparehatch", {
        /**
         * Whether hatch objects are included in drawing comparison (**COMPAREHATCH**).
         *
         * - `0`: Hatch objects are excluded (AutoCAD default)
         * - `1`: Hatch objects are included
         *
         * @see https://help.autodesk.com/view/ACD/2025/ENU/?guid=GUID-BBB5E4A0-B607-4898-9A6B-A65C51551EE5
         */
        get: function () {
            return this._comparehatch;
        },
        set: function (value) {
            var _this = this;
            var nextValue = acdbCoerceIntegerSysVar('COMPAREHATCH', value !== null && value !== void 0 ? value : ACDB_COMPAREHATCH_DEFAULT, ACDB_COMPAREHATCH_MIN, ACDB_COMPAREHATCH_MAX);
            this.updateSysVar(AcDbSystemVariables.COMPAREHATCH, this._comparehatch, nextValue, function (coerced) {
                _this._comparehatch = coerced;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "comparercmargin", {
        /**
         * Offset between a change-set boundary and the revision cloud
         * (**COMPARERCMARGIN**). AutoCAD range is **1–25**; default is **5**.
         *
         * @see https://help.autodesk.com/view/ACD/2025/ENU/?guid=GUID-7A230058-048B-4EE6-949D-105AF6AC8E73
         */
        get: function () {
            return this._comparercmargin;
        },
        set: function (value) {
            var _this = this;
            var nextValue = acdbCoerceIntegerSysVar('COMPARERCMARGIN', value !== null && value !== void 0 ? value : ACDB_COMPARERCMARGIN_DEFAULT, ACDB_COMPARERCMARGIN_MIN, ACDB_COMPARERCMARGIN_MAX);
            this.updateSysVar(AcDbSystemVariables.COMPARERCMARGIN, this._comparercmargin, nextValue, function (coerced) {
                _this._comparercmargin = coerced;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "comparetext", {
        /**
         * Whether text objects are included in drawing comparison (**COMPARETEXT**).
         *
         * - `0`: Text objects are excluded
         * - `1`: Text objects are included (AutoCAD default)
         *
         * @see https://help.autodesk.com/view/ACD/2025/ENU/?guid=GUID-1BE58261-FA5F-4914-BAC6-C1DF7E3D1E9C
         */
        get: function () {
            return this._comparetext;
        },
        set: function (value) {
            var _this = this;
            var nextValue = acdbCoerceIntegerSysVar('COMPARETEXT', value !== null && value !== void 0 ? value : ACDB_COMPARETEXT_DEFAULT, ACDB_COMPARETEXT_MIN, ACDB_COMPARETEXT_MAX);
            this.updateSysVar(AcDbSystemVariables.COMPARETEXT, this._comparetext, nextValue, function (coerced) {
                _this._comparetext = coerced;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "comparetolerance", {
        /**
         * Decimal-place geometric tolerance used when comparing two drawings
         * (**COMPARETOLERANCE**). AutoCAD range is **0–14**; default is **6**.
         *
         * @see https://help.autodesk.com/view/ACD/2025/ENU/?guid=GUID-3131F7C8-7199-4EC5-9892-88C2D2A86F78
         */
        get: function () {
            return this._comparetolerance;
        },
        set: function (value) {
            var _this = this;
            var nextValue = acdbCoerceIntegerSysVar('COMPARETOLERANCE', value !== null && value !== void 0 ? value : ACDB_COMPARETOLERANCE_DEFAULT, ACDB_COMPARETOLERANCE_MIN, ACDB_COMPARETOLERANCE_MAX);
            this.updateSysVar(AcDbSystemVariables.COMPARETOLERANCE, this._comparetolerance, nextValue, function (coerced) {
                _this._comparetolerance = coerced;
            });
        },
        enumerable: false,
        configurable: true
    });
    Object.defineProperty(AcDbDatabase.prototype, "lastOpenError", {
        /**
         * The most recent failure from {@link read} or {@link openUri}, or `null` after a successful open.
         *
         * Useful when a caller catches no exception (for example a viewer that returns `false`)
         * but still needs to distinguish worker out-of-memory from other parse failures.
         */
        get: function () {
            return this._lastOpenError;
        },
        enumerable: false,
        configurable: true
    });
    /**
     * Reads drawing data from a string or ArrayBuffer.
     *
     * This method parses the provided data and populates the database with
     * the resulting entities, tables, and objects. The method supports
     * both DXF and DWG file formats.
     *
     * @param data - The drawing data as a string or ArrayBuffer
     *   - For DXF files: Pass a string containing the DXF content
     *   - For DWG files: Pass an ArrayBuffer instance containing the binary DWG data
     * @param options - Options for reading the database
     * @param fileType - The type of file being read (defaults to DXF)
     *
     * @example
     * ```typescript
     * // Reading a DXF file (string)
     * const database = new AcDbDatabase();
     * await database.read(dxfString, { readOnly: true }, AcDbFileType.DXF);
     *
     * // Reading a DWG file (ArrayBuffer)
     * const database = new AcDbDatabase();
     * await database.read(dwgArrayBuffer, { readOnly: true }, AcDbFileType.DWG);
     * ```
     */
    AcDbDatabase.prototype.read = function (data_1, options_1) {
        return __awaiter(this, arguments, void 0, function (data, options, fileType) {
            var converter, error_1, openError;
            var _a, _b;
            if (fileType === void 0) { fileType = AcDbFileType.DXF; }
            return __generator(this, function (_c) {
                switch (_c.label) {
                    case 0:
                        converter = AcDbDatabaseConverterManager.instance.get(fileType);
                        if (converter == null)
                            throw new Error("Database converter for file type '".concat(fileType, "' isn't registered and can can't read this file!"));
                        this.clear();
                        this._lastOpenError = null;
                        this._drawNoPlotLayers = (_a = options === null || options === void 0 ? void 0 : options.drawNoPlotLayers) !== null && _a !== void 0 ? _a : true;
                        this.drawCircleSides = (_b = options === null || options === void 0 ? void 0 : options.circleSides) !== null && _b !== void 0 ? _b : ACDB_DRAW_CIRCLE_SIDES_DRAFT;
                        if (options === null || options === void 0 ? void 0 : options.fileName) {
                            this.setDwgName(options.fileName);
                        }
                        // Ensure this database is the host working database for the duration of
                        // conversion. Converters (including peer packages such as dxf-json-converter)
                        // assign real handles on unbound objects; some entity getters still fall
                        // back to the working database before append/add binds them.
                        acdbAssignWorkingDatabase(this);
                        _c.label = 1;
                    case 1:
                        _c.trys.push([1, 3, , 4]);
                        return [4 /*yield*/, converter.read(data, this, {
                                minimumChunkSize: (options && options.minimumChunkSize) || 10,
                                progress: this.createConversionProgressHandler(),
                                timeout: options === null || options === void 0 ? void 0 : options.timeout,
                                encoding: options === null || options === void 0 ? void 0 : options.encoding,
                                sysVars: options === null || options === void 0 ? void 0 : options.sysVars
                            })];
                    case 2:
                        _c.sent();
                        return [3 /*break*/, 4];
                    case 3:
                        error_1 = _c.sent();
                        openError = AcDbOpenDatabaseError.from(error_1);
                        this._lastOpenError = openError;
                        this.events.openFailed.dispatch({ database: this, error: openError });
                        throw openError;
                    case 4:
                        this._lastOpenError = null;
                        this.ensureDatabaseDefaults();
                        return [2 /*return*/];
                }
            });
        });
    };
    AcDbDatabase.prototype.createConversionProgressHandler = function () {
        var _this = this;
        return function (percentage, stage, stageStatus, data, taskError) { return __awaiter(_this, void 0, void 0, function () {
            var progressData, openError;
            var _a;
            return __generator(this, function (_b) {
                progressData = data;
                if (stageStatus === 'ERROR' && taskError) {
                    openError = AcDbOpenDatabaseError.fromTask(taskError);
                    this._lastOpenError = openError;
                    progressData = {
                        code: openError.code,
                        message: openError.message,
                        stage: (_a = openError.stage) !== null && _a !== void 0 ? _a : stage
                    };
                }
                this.events.openProgress.dispatch({
                    database: this,
                    percentage: percentage,
                    stage: 'CONVERSION',
                    subStage: stage,
                    subStageStatus: stageStatus,
                    data: progressData
                });
                return [2 /*return*/];
            });
        }); };
    };
    /**
     * Read AutoCAD DXF or DWG drawing specified by the URL into the database object.
     * The method automatically detects the file type based on the URL extension:
     * - .dxf files are read as text using readAsText()
     * - .dwg files are read as binary data using readAsArrayBuffer()
     * @param url Input the URL linked to one AutoCAD DXF or DWG file
     * @param options Input options to read drawing data
     */
    AcDbDatabase.prototype.openUri = function (url, options) {
        return __awaiter(this, void 0, void 0, function () {
            var response, contentLength, totalBytes, loadedBytes, reader, chunks, _a, done, value, percentage, content, position, chunks_1, chunks_1_1, chunk, fileName, fileExtension;
            var e_5, _b;
            var _c;
            return __generator(this, function (_d) {
                switch (_d.label) {
                    case 0:
                        this.events.openProgress.dispatch({
                            database: this,
                            percentage: 0,
                            stage: 'FETCH_FILE',
                            subStageStatus: 'START'
                        });
                        return [4 /*yield*/, fetch(url)];
                    case 1:
                        response = _d.sent();
                        if (!response.ok) {
                            this.events.openProgress.dispatch({
                                database: this,
                                percentage: 100,
                                stage: 'FETCH_FILE',
                                subStageStatus: 'ERROR'
                            });
                            throw new Error("Failed to fetch file '".concat(url, "' with HTTP status code '").concat(response.status, "'!"));
                        }
                        contentLength = response.headers.get('content-length');
                        totalBytes = contentLength ? parseInt(contentLength, 10) : null;
                        loadedBytes = 0;
                        reader = (_c = response.body) === null || _c === void 0 ? void 0 : _c.getReader();
                        if (!reader) {
                            throw new Error('Failed to get response reader');
                        }
                        chunks = [];
                        _d.label = 2;
                    case 2:
                        if (!true) return [3 /*break*/, 4];
                        return [4 /*yield*/, reader.read()];
                    case 3:
                        _a = _d.sent(), done = _a.done, value = _a.value;
                        if (done) {
                            return [3 /*break*/, 4];
                        }
                        chunks.push(value);
                        loadedBytes += value.length;
                        // Calculate and report progress if we know the total size
                        if (totalBytes !== null) {
                            percentage = Math.round((loadedBytes / totalBytes) * 100);
                            this.events.openProgress.dispatch({
                                database: this,
                                percentage: percentage,
                                stage: 'FETCH_FILE',
                                subStageStatus: 'IN-PROGRESS'
                            });
                        }
                        return [3 /*break*/, 2];
                    case 4:
                        content = new Uint8Array(loadedBytes);
                        position = 0;
                        try {
                            for (chunks_1 = __values(chunks), chunks_1_1 = chunks_1.next(); !chunks_1_1.done; chunks_1_1 = chunks_1.next()) {
                                chunk = chunks_1_1.value;
                                content.set(chunk, position);
                                position += chunk.length;
                            }
                        }
                        catch (e_5_1) { e_5 = { error: e_5_1 }; }
                        finally {
                            try {
                                if (chunks_1_1 && !chunks_1_1.done && (_b = chunks_1.return)) _b.call(chunks_1);
                            }
                            finally { if (e_5) throw e_5.error; }
                        }
                        fileName = this.getFileNameFromUri(url);
                        if (fileName) {
                            this.setDwgName(fileName);
                        }
                        fileExtension = fileName.toLowerCase().split('.').pop();
                        if (!(fileExtension === 'dwg')) return [3 /*break*/, 6];
                        // DWG files are binary, convert to ArrayBuffer
                        return [4 /*yield*/, this.read(content.buffer, options, AcDbFileType.DWG)];
                    case 5:
                        // DWG files are binary, convert to ArrayBuffer
                        _d.sent();
                        return [3 /*break*/, 10];
                    case 6:
                        if (!(fileExtension === 'dxf')) return [3 /*break*/, 8];
                        return [4 /*yield*/, this.read(content.buffer, options, AcDbFileType.DXF)];
                    case 7:
                        _d.sent();
                        return [3 /*break*/, 10];
                    case 8: return [4 /*yield*/, this.read(content.buffer, options, fileExtension)];
                    case 9:
                        _d.sent();
                        _d.label = 10;
                    case 10:
                        this.events.openProgress.dispatch({
                            database: this,
                            percentage: 100,
                            stage: 'FETCH_FILE',
                            subStageStatus: 'END'
                        });
                        return [2 /*return*/];
                }
            });
        });
    };
    /**
     * Exports the current database into DXF (ASCII string or binary bytes).
     *
     * The `fileName` parameter is kept for ObjectARX API parity. In this web
     * implementation the method returns the DXF payload instead of writing the
     * filesystem directly.
     *
     * @param _fileName - Kept for ObjectARX parity. Ignored in this implementation.
     * @param precision - Numeric precision used by the DXF filer.
     * @param version - Target DXF/DWG version name or value.
     * @param optionsOrThumbnail - Legacy boolean thumbnail flag, or options with
     *   `saveThumbnailImage` and `format: 'ascii' | 'binary'`.
     * @returns ASCII DXF string, or `Uint8Array` when format is `'binary'`.
     */
    AcDbDatabase.prototype.dxfOut = function (_fileName, precision, version, optionsOrThumbnail) {
        var _a, _b;
        if (precision === void 0) { precision = 16; }
        if (version === void 0) { version = this.version.name; }
        if (optionsOrThumbnail === void 0) { optionsOrThumbnail = false; }
        this.ensureDatabaseDefaults();
        if (this._layerFilters.root.getNestedFilters().length > 0 &&
            !this.tables.layerTable.extensionDictionary) {
            this.tables.layerTable.extensionDictionary = this.generateHandle();
        }
        var options = typeof optionsOrThumbnail === 'boolean'
            ? {
                saveThumbnailImage: optionsOrThumbnail,
                format: 'ascii'
            }
            : {
                saveThumbnailImage: (_a = optionsOrThumbnail.saveThumbnailImage) !== null && _a !== void 0 ? _a : false,
                format: (_b = optionsOrThumbnail.format) !== null && _b !== void 0 ? _b : 'ascii'
            };
        var outVersion = version instanceof AcDbDwgVersion ? version : new AcDbDwgVersion(version);
        var filer = new AcDbDxfFiler({
            database: this,
            precision: precision,
            version: outVersion,
            outputFormat: options.format
        });
        this.writeDxfHeaderSection(filer);
        this.writeDxfClassesSection(filer);
        this.writeDxfTablesSection(filer, outVersion);
        this.writeDxfBlocksSection(filer);
        this.writeDxfEntitiesSection(filer);
        this.writeDxfObjectsSection(filer);
        this.writeDxfThumbnailImageSection(filer, options.saveThumbnailImage);
        filer.writeStart('EOF');
        return options.format === 'binary' ? filer.toBinary() : filer.toString();
    };
    /**
     * Triggers xxxAppended events with data in the database to redraw the associated viewer.
     */
    AcDbDatabase.prototype.regen = function () {
        return __awaiter(this, void 0, void 0, function () {
            var converter;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        converter = new AcDbRegenerator(this);
                        return [4 /*yield*/, converter.read(null, this, {
                                minimumChunkSize: 500,
                                progress: this.createConversionProgressHandler()
                            })];
                    case 1:
                        _a.sent();
                        return [2 /*return*/];
                }
            });
        });
    };
    /**
     * Create default layer, line type, dimension type, text style and layout.
     * @param - Options to specify data to create
     */
    AcDbDatabase.prototype.createDefaultData = function (options) {
        if (options === void 0) { options = {
            layer: true,
            lineType: true,
            textStyle: true,
            dimStyle: true,
            layout: true
        }; }
        var generator = new AcDbDataGenerator(this);
        // Create default layer
        if (options.layer) {
            generator.createDefaultLayer();
        }
        // Create default line type
        if (options.lineType) {
            generator.createDefaultLineType();
        }
        // Create default text style
        if (options.textStyle) {
            generator.createDefaultTextStyle();
        }
        // Create default dimension style
        if (options.dimStyle) {
            generator.createDefaultDimStyle();
        }
        // Create default layout for model space
        if (options.layout) {
            generator.createDefaultLayout();
        }
    };
    /**
     * Ensures style dictionaries contain defaults required by one entity type.
     *
     * This is primarily used during entity append so newly created entities can
     * immediately resolve their style references.
     *
     * @internal
     */
    AcDbDatabase.prototype.ensureEntityStyleDefaults = function (entity) {
        var _a;
        if (entity.dxfTypeName === 'MLINE') {
            this.ensureMLineStyle(this._cmlstyle || DEFAULT_MLINE_STYLE);
            return;
        }
        if (entity.dxfTypeName === 'MULTILEADER') {
            this.ensureMLeaderStyle(this._cmleaderstyle || DEFAULT_MLEADER_STYLE);
            return;
        }
        if (entity.dxfTypeName === 'HATCH') {
            var hatch = entity;
            (_a = hatch.applyPatternDefaultsFromSysVars) === null || _a === void 0 ? void 0 : _a.call(hatch, this);
        }
    };
    /**
     * Ensures the default text style exists in the text style table.
     *
     * This is invoked while converting STYLE records and again after a drawing
     * is fully opened so TEXT/MTEXT entities can resolve a style during
     * progressive rendering.
     */
    AcDbDatabase.prototype.ensureTextStyleDefaults = function () {
        if (this.hasDefaultTextStyle()) {
            return;
        }
        this.tables.textStyleTable.add(new AcDbTextStyleTableRecord({
            name: DEFAULT_TEXT_STYLE,
            standardFlag: 0,
            fixedTextHeight: 0,
            widthFactor: 1,
            obliqueAngle: 0,
            textGenerationFlag: 0,
            lastHeight: 0.2,
            font: 'SimKai',
            bigFont: '',
            extendedFont: 'SimKai'
        }));
    };
    AcDbDatabase.prototype.hasDefaultTextStyle = function () {
        var e_6, _a, e_7, _b;
        var defaultNames = [DEFAULT_TEXT_STYLE, 'STANDARD'];
        try {
            for (var defaultNames_1 = __values(defaultNames), defaultNames_1_1 = defaultNames_1.next(); !defaultNames_1_1.done; defaultNames_1_1 = defaultNames_1.next()) {
                var name_1 = defaultNames_1_1.value;
                if (this.tables.textStyleTable.has(name_1)) {
                    return true;
                }
            }
        }
        catch (e_6_1) { e_6 = { error: e_6_1 }; }
        finally {
            try {
                if (defaultNames_1_1 && !defaultNames_1_1.done && (_a = defaultNames_1.return)) _a.call(defaultNames_1);
            }
            finally { if (e_6) throw e_6.error; }
        }
        try {
            for (var _c = __values(this.tables.textStyleTable.newIterator()), _d = _c.next(); !_d.done; _d = _c.next()) {
                var record = _d.value;
                if (record.name.toUpperCase() === DEFAULT_TEXT_STYLE.toUpperCase()) {
                    return true;
                }
            }
        }
        catch (e_7_1) { e_7 = { error: e_7_1 }; }
        finally {
            try {
                if (_d && !_d.done && (_b = _c.return)) _b.call(_c);
            }
            finally { if (e_7) throw e_7.error; }
        }
        return false;
    };
    /**
     * Ensures required default database data exists.
     *
     * This is used after opening a file (or before exporting) to fill in any
     * missing defaults such as layers, linetypes, text styles, dim styles,
     * viewports, layouts, and registered application IDs.
     */
    AcDbDatabase.prototype.ensureDatabaseDefaults = function () {
        if (!this.tables.layerTable.has('0')) {
            var defaultColor = new AcCmColor();
            defaultColor.colorIndex = 7;
            this.tables.layerTable.add(new AcDbLayerTableRecord({
                name: '0',
                standardFlags: 0,
                linetype: DEFAULT_LINE_TYPE,
                lineWeight: 0,
                isOff: false,
                color: defaultColor,
                isPlottable: true
            }));
        }
        if (!this.tables.linetypeTable.has(ByBlock)) {
            this.tables.linetypeTable.add(new AcDbLinetypeTableRecord({
                name: ByBlock,
                standardFlag: 0,
                description: '',
                totalPatternLength: 0
            }));
        }
        if (!this.tables.linetypeTable.has(ByLayer)) {
            this.tables.linetypeTable.add(new AcDbLinetypeTableRecord({
                name: ByLayer,
                standardFlag: 0,
                description: '',
                totalPatternLength: 0
            }));
        }
        if (!this.tables.linetypeTable.has(DEFAULT_LINE_TYPE)) {
            this.tables.linetypeTable.add(new AcDbLinetypeTableRecord({
                name: DEFAULT_LINE_TYPE,
                standardFlag: 0,
                description: 'Solid line',
                totalPatternLength: 0
            }));
        }
        this.ensureTextStyleDefaults();
        if (!this.tables.dimStyleTable.has(DEFAULT_TEXT_STYLE)) {
            this.tables.dimStyleTable.add(new AcDbDimStyleTableRecord({
                name: DEFAULT_TEXT_STYLE,
                dimtxsty: DEFAULT_TEXT_STYLE
            }));
        }
        if (!this.tables.viewportTable.has(ACTIVE_VPORT_NAME)) {
            var viewport = new AcDbViewportTableRecord();
            viewport.name = ACTIVE_VPORT_NAME;
            this.tables.viewportTable.add(viewport);
        }
        this.ensureMLineStyle(this._cmlstyle || DEFAULT_MLINE_STYLE);
        this.ensureMLeaderStyle(this._cmleaderstyle || DEFAULT_MLEADER_STYLE);
        var modelSpace = this.tables.blockTable.modelSpace;
        if (!this.objects.layout.getAt('Model')) {
            var layout = new AcDbLayout();
            layout.layoutName = 'Model';
            layout.tabOrder = 0;
            layout.blockTableRecordId = modelSpace.objectId;
            layout.limits.min.copy({ x: 0, y: 0 });
            layout.limits.max.copy({ x: 1000000, y: 1000000 });
            layout.extents.min.copy({ x: 0, y: 0, z: 0 });
            layout.extents.max.copy({ x: 1000000, y: 1000000, z: 0 });
            this.objects.layout.setAt(layout.layoutName, layout);
            modelSpace.layoutId = layout.objectId;
        }
        if (!this.tables.appIdTable.has(ACAD_APPID)) {
            this.tables.appIdTable.add(new AcDbRegAppTableRecord(ACAD_APPID));
        }
        if (!this.tables.appIdTable.has(MLIGHTCAD_APPID)) {
            this.tables.appIdTable.add(new AcDbRegAppTableRecord(MLIGHTCAD_APPID));
        }
    };
    /**
     * Ensures one MLINE style exists for the provided style name.
     */
    AcDbDatabase.prototype.ensureMLineStyle = function (styleName) {
        var e_8, _a;
        var dictionary = this.objects.mlineStyle;
        var normalizedName = styleName.trim();
        if (!normalizedName)
            return;
        if (dictionary.getAt(normalizedName)) {
            return;
        }
        try {
            for (var _b = __values(dictionary.entries()), _c = _b.next(); !_c.done; _c = _b.next()) {
                var _d = __read(_c.value, 2), name_2 = _d[0], style_1 = _d[1];
                if (name_2.toUpperCase() === normalizedName.toUpperCase() ||
                    style_1.styleName.toUpperCase() === normalizedName.toUpperCase()) {
                    return;
                }
            }
        }
        catch (e_8_1) { e_8 = { error: e_8_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_8) throw e_8.error; }
        }
        var style = new AcDbMlineStyle();
        style.styleName = normalizedName;
        style.elements = [
            {
                offset: 0.5,
                color: new AcCmColor().setByLayer(),
                lineType: ByLayer
            },
            {
                offset: -0.5,
                color: new AcCmColor().setByLayer(),
                lineType: ByLayer
            }
        ];
        dictionary.setAt(normalizedName, style);
    };
    /**
     * Ensures one MLEADER style exists for the provided style name.
     */
    AcDbDatabase.prototype.ensureMLeaderStyle = function (styleName) {
        var e_9, _a;
        var dictionary = this.objects.mleaderStyle;
        var normalizedName = styleName.trim();
        if (!normalizedName)
            return;
        if (dictionary.getAt(normalizedName)) {
            return;
        }
        try {
            for (var _b = __values(dictionary.entries()), _c = _b.next(); !_c.done; _c = _b.next()) {
                var _d = __read(_c.value, 1), name_3 = _d[0];
                if (name_3.toUpperCase() === normalizedName.toUpperCase()) {
                    return;
                }
            }
        }
        catch (e_9_1) { e_9 = { error: e_9_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_9) throw e_9.error; }
        }
        var style = new AcDbMLeaderStyle();
        // Match AutoCAD "Standard" defaults observed in exported DXF.
        style.unknown1 = 2;
        style.maxLeaderSegmentsPoints = 2;
        style.leaderLineColor = new AcCmColor().setByBlock();
        style.textColor = new AcCmColor().setByBlock();
        style.blockColor = new AcCmColor().setByBlock();
        style.alignSpace = 4;
        style.breakSize = 3.75;
        style.enableBlockRotation = true;
        style.unknown2 = false;
        var byBlockLinetype = this.tables.linetypeTable.getAt(ByBlock);
        style.leaderLineTypeId = byBlockLinetype === null || byBlockLinetype === void 0 ? void 0 : byBlockLinetype.objectId;
        var standardTextStyle = this.tables.textStyleTable.getAt(DEFAULT_TEXT_STYLE);
        style.textStyleId = standardTextStyle === null || standardTextStyle === void 0 ? void 0 : standardTextStyle.objectId;
        dictionary.setAt(normalizedName, style);
    };
    /**
     * Writes the HEADER section for the DXF export.
     *
     * @param filer - DXF output writer.
     */
    AcDbDatabase.prototype.writeDxfHeaderSection = function (filer) {
        var _a, _b;
        filer.startSection('HEADER');
        filer.writeString(9, '$ACADVER');
        filer.writeString(1, (_b = (_a = filer.version) === null || _a === void 0 ? void 0 : _a.name) !== null && _b !== void 0 ? _b : this.version.name);
        filer.writeString(9, '$HANDSEED');
        filer.writeString(5, filer.nextHandle.toString(16).toUpperCase());
        if (filer.capabilities.supportsUtf8CodePage) {
            filer.writeString(9, '$DWGCODEPAGE');
            filer.writeString(3, 'UTF-8');
        }
        filer.writeString(9, '$INSUNITS');
        filer.writeInt16(70, this.insunits);
        filer.writeString(9, '$LUNITS');
        filer.writeInt16(70, this.lunits);
        filer.writeString(9, '$LUPREC');
        filer.writeInt16(70, this.luprec);
        filer.writeString(9, '$UNITMODE');
        filer.writeInt16(70, this.unitmode);
        filer.writeString(9, '$MEASUREMENT');
        filer.writeInt16(70, this.measurement);
        filer.writeString(9, '$LTSCALE');
        filer.writeDouble(40, this.ltscale);
        if (filer.capabilities.supportsLineWeight) {
            filer.writeString(9, '$LWDISPLAY');
            filer.writeInt16(70, this.lwdisplay ? 1 : 0);
        }
        filer.writeString(9, '$CELTSCALE');
        filer.writeDouble(40, this.celtscale);
        if (filer.capabilities.supportsLineWeight) {
            filer.writeString(9, '$CELWEIGHT');
            filer.writeLineWeight(370, this.celweight);
        }
        filer.writeString(9, '$CECOLOR');
        filer.writeCmColor(this.cecolor);
        filer.writeString(9, '$TILEMODE');
        filer.writeInt16(70, this.tilemode ? 1 : 0);
        filer.writeString(9, '$PSLTSCALE');
        filer.writeInt16(70, this.psltscale ? 1 : 0);
        filer.writeString(9, '$CLAYER');
        filer.writeString(8, this.clayer);
        filer.writeString(9, '$CELTYPE');
        filer.writeString(6, this.celtype);
        if (filer.capabilities.supportsTransparency &&
            !this.cetransparency.isInvalid) {
            filer.writeString(9, '$CETRANSPARENCY');
            filer.writeTransparency(this.cetransparency);
        }
        filer.writeString(9, '$CMLSTYLE');
        filer.writeString(2, this.cmlstyle);
        filer.writeString(9, '$CMLSCALE');
        filer.writeDouble(40, this.cmlscale);
        filer.writeString(9, '$CMLEADERSTYLE');
        filer.writeString(2, this.cmleaderstyle);
        if (this.hpcolor.colorMethod !== AcCmColorMethod.None) {
            filer.writeString(9, '$HPCOLOR');
            filer.writeCmColor(this.hpcolor, 2);
        }
        if (this.hpbackgroundcolor.colorMethod !== AcCmColorMethod.None) {
            filer.writeString(9, '$HPBACKGROUNDCOLOR');
            filer.writeCmColor(this.hpbackgroundcolor, 2);
        }
        filer.writeString(9, '$HPLAYER');
        filer.writeString(8, this.hplayer);
        if (filer.capabilities.supportsTransparency &&
            !this.hptransparency.isInvalid) {
            filer.writeString(9, '$HPTRANSPARENCY');
            filer.writeTransparency(this.hptransparency);
        }
        filer.writeString(9, '$TEXTSTYLE');
        filer.writeString(7, this.textstyle);
        filer.writeString(9, '$DIMSTYLE');
        filer.writeString(2, this.dimstyle);
        filer.writeString(9, '$ANGBASE');
        filer.writeAngle(50, this.angbase);
        filer.writeString(9, '$ANGDIR');
        filer.writeInt16(70, this.angdir);
        filer.writeString(9, '$AUNITS');
        filer.writeInt16(70, this.aunits);
        filer.writeString(9, '$AUPREC');
        filer.writeInt16(70, this.auprec);
        filer.writeString(9, '$EXTMIN');
        filer.writePoint3d(10, this.extmin);
        filer.writeString(9, '$EXTMAX');
        filer.writePoint3d(10, this.extmax);
        filer.writeString(9, '$PDMODE');
        filer.writeInt32(70, this.pdmode);
        filer.writeString(9, '$PDSIZE');
        filer.writeDouble(40, this.pdsize);
        filer.writeString(9, '$OSMODE');
        filer.writeInt32(70, this.osmode);
        filer.writeString(9, '$ORTHOMODE');
        filer.writeInt16(70, this.orthomode);
        filer.endSection();
    };
    /**
     * Writes the CLASSES section for the DXF export.
     *
     * Required for proxy entities whose group code **91** indexes into this
     * section. Skipped when no classes were loaded from the source drawing.
     *
     * @param filer - DXF output writer.
     */
    AcDbDatabase.prototype.writeDxfClassesSection = function (filer) {
        var e_10, _a;
        if (!filer.capabilities.supportsClassesSection ||
            this._classes.length === 0) {
            return;
        }
        filer.startSection('CLASSES');
        try {
            for (var _b = __values(this._classes), _c = _b.next(); !_c.done; _c = _b.next()) {
                var dxfClass = _c.value;
                filer.writeStart('CLASS');
                filer.writeString(1, dxfClass.name);
                filer.writeString(2, dxfClass.cppClassName);
                filer.writeString(3, dxfClass.appName);
                filer.writeInt32(90, dxfClass.proxyFlag);
                filer.writeInt32(91, dxfClass.instanceCount);
                filer.writeInt8(280, dxfClass.wasProxy ? 1 : 0);
                filer.writeInt8(281, dxfClass.isEntity ? 1 : 0);
            }
        }
        catch (e_10_1) { e_10 = { error: e_10_1 }; }
        finally {
            try {
                if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
            }
            finally { if (e_10) throw e_10.error; }
        }
        filer.endSection();
    };
    /**
     * Writes the TABLES section for the DXF export.
     *
     * @param filer - DXF output writer.
     * @param version - Target DXF/DWG version, used for conditional tables.
     */
    AcDbDatabase.prototype.writeDxfTablesSection = function (filer, version) {
        filer.startSection('TABLES');
        this.writeDxfTable(filer, 'VPORT', this.tables.viewportTable, this.tables.viewportTable.newIterator(), 'VPORT');
        this.writeDxfTable(filer, 'VIEW', this.tables.viewTable, this.tables.viewTable.newIterator(), 'VIEW');
        this.writeDxfTable(filer, 'UCS', this.tables.ucsTable, this.tables.ucsTable.newIterator(), 'UCS');
        this.writeDxfTable(filer, 'LTYPE', this.tables.linetypeTable, this.tables.linetypeTable.newIterator(), 'LTYPE');
        this.writeDxfTable(filer, 'LAYER', this.tables.layerTable, this.tables.layerTable.newIterator(), 'LAYER');
        this.writeDxfTable(filer, 'STYLE', this.tables.textStyleTable, this.tables.textStyleTable.newIterator(true), 'STYLE');
        this.writeDxfTable(filer, 'APPID', this.tables.appIdTable, this.tables.appIdTable.newIterator(), 'APPID');
        this.writeDxfTable(filer, 'DIMSTYLE', this.tables.dimStyleTable, this.tables.dimStyleTable.newIterator(), 'DIMSTYLE');
        if (version.capabilities.supportsBlockRecordTable) {
            this.writeDxfTable(filer, 'BLOCK_RECORD', this.tables.blockTable, this.tables.blockTable.newIterator(), 'BLOCK_RECORD');
        }
        filer.endSection();
    };
    /**
     * Writes the BLOCKS section for the DXF export.
     *
     * @param filer - DXF output writer.
     */
    AcDbDatabase.prototype.writeDxfBlocksSection = function (filer) {
        var e_11, _a, e_12, _b;
        filer.startSection('BLOCKS');
        try {
            for (var _c = __values(this.tables.blockTable.newIterator()), _d = _c.next(); !_d.done; _d = _c.next()) {
                var btr = _d.value;
                btr.dxfOutBlockBegin(filer);
                if (!btr.isModelSapce && !btr.isPaperSapce) {
                    try {
                        for (var _e = (e_12 = void 0, __values(btr.newIterator())), _f = _e.next(); !_f.done; _f = _e.next()) {
                            var entity = _f.value;
                            this.writeDxfEntity(filer, entity);
                        }
                    }
                    catch (e_12_1) { e_12 = { error: e_12_1 }; }
                    finally {
                        try {
                            if (_f && !_f.done && (_b = _e.return)) _b.call(_e);
                        }
                        finally { if (e_12) throw e_12.error; }
                    }
                }
                btr.dxfOutBlockEnd(filer);
            }
        }
        catch (e_11_1) { e_11 = { error: e_11_1 }; }
        finally {
            try {
                if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
            }
            finally { if (e_11) throw e_11.error; }
        }
        filer.endSection();
    };
    /**
     * Writes the ENTITIES section for the DXF export.
     *
     * @param filer - DXF output writer.
     */
    AcDbDatabase.prototype.writeDxfEntitiesSection = function (filer) {
        var e_13, _a, e_14, _b;
        filer.startSection('ENTITIES');
        try {
            for (var _c = __values(this.tables.blockTable.newIterator()), _d = _c.next(); !_d.done; _d = _c.next()) {
                var btr = _d.value;
                if (!btr.isModelSapce && !btr.isPaperSapce)
                    continue;
                try {
                    for (var _e = (e_14 = void 0, __values(btr.newIterator())), _f = _e.next(); !_f.done; _f = _e.next()) {
                        var entity = _f.value;
                        this.writeDxfEntity(filer, entity);
                    }
                }
                catch (e_14_1) { e_14 = { error: e_14_1 }; }
                finally {
                    try {
                        if (_f && !_f.done && (_b = _e.return)) _b.call(_e);
                    }
                    finally { if (e_14) throw e_14.error; }
                }
            }
        }
        catch (e_13_1) { e_13 = { error: e_13_1 }; }
        finally {
            try {
                if (_d && !_d.done && (_a = _c.return)) _a.call(_c);
            }
            finally { if (e_13) throw e_13.error; }
        }
        filer.endSection();
    };
    /**
     * Writes the OBJECTS section for the DXF export.
     *
     * @param filer - DXF output writer.
     */
    AcDbDatabase.prototype.writeDxfObjectsSection = function (filer) {
        var e_15, _a, e_16, _b, e_17, _c, e_18, _d, e_19, _e, e_20, _f, e_21, _g, e_22, _h, e_23, _j;
        if (!filer.capabilities.supportsObjectsSection) {
            return;
        }
        filer.startSection('OBJECTS');
        var rootDict = this.objects.dictionary;
        rootDict.ownerId = '0';
        var writeDictionary = function (dict) {
            // Dictionary entries (3/350 pairs) are embedded in the DICTIONARY object.
            filer.writeStart('DICTIONARY');
            dict.dxfOut(filer, true);
        };
        var ensureRootEntry = function (key, dict) {
            if (rootDict.getAt(key) !== dict) {
                rootDict.setAt(key, dict);
            }
        };
        var dropRootEntry = function (key) {
            if (rootDict.getAt(key)) {
                rootDict.remove(key);
            }
        };
        ensureRootEntry('ACAD_LAYOUT', this.objects.layout);
        if (this.objects.group.numEntries > 0) {
            ensureRootEntry('ACAD_GROUP', this.objects.group);
        }
        else {
            dropRootEntry('ACAD_GROUP');
        }
        if (this.objects.sortentsTable.numEntries > 0) {
            ensureRootEntry('ACAD_SORTENTS', this.objects.sortentsTable);
        }
        else {
            dropRootEntry('ACAD_SORTENTS');
        }
        if (this.objects.mleaderStyle.numEntries > 0) {
            ensureRootEntry('ACAD_MLEADERSTYLE', this.objects.mleaderStyle);
        }
        else {
            dropRootEntry('ACAD_MLEADERSTYLE');
        }
        if (this.objects.mlineStyle.numEntries > 0) {
            ensureRootEntry('ACAD_MLINESTYLE', this.objects.mlineStyle);
        }
        else {
            dropRootEntry('ACAD_MLINESTYLE');
        }
        if (this.objects.imageDefinition.numEntries > 0) {
            ensureRootEntry('ISM_RASTER_IMAGE_DICT', this.objects.imageDefinition);
        }
        else {
            dropRootEntry('ISM_RASTER_IMAGE_DICT');
        }
        if (this.objects.xrecord.numEntries > 0) {
            ensureRootEntry('MLIGHT_XRECORD', this.objects.xrecord);
        }
        else {
            dropRootEntry('MLIGHT_XRECORD');
        }
        writeDictionary(rootDict);
        writeDictionary(this.objects.layout);
        if (this.objects.group.numEntries > 0) {
            writeDictionary(this.objects.group);
        }
        if (this.objects.sortentsTable.numEntries > 0) {
            writeDictionary(this.objects.sortentsTable);
        }
        if (this.objects.mleaderStyle.numEntries > 0) {
            writeDictionary(this.objects.mleaderStyle);
        }
        if (this.objects.mlineStyle.numEntries > 0) {
            writeDictionary(this.objects.mlineStyle);
        }
        if (this.objects.imageDefinition.numEntries > 0) {
            writeDictionary(this.objects.imageDefinition);
        }
        if (this.objects.xrecord.numEntries > 0) {
            writeDictionary(this.objects.xrecord);
        }
        try {
            for (var _k = __values(this.objects.layout.entries()), _l = _k.next(); !_l.done; _l = _k.next()) {
                var _m = __read(_l.value, 2), _ = _m[0], layout = _m[1];
                filer.writeStart('LAYOUT');
                layout.dxfOut(filer, true);
            }
        }
        catch (e_15_1) { e_15 = { error: e_15_1 }; }
        finally {
            try {
                if (_l && !_l.done && (_a = _k.return)) _a.call(_k);
            }
            finally { if (e_15) throw e_15.error; }
        }
        try {
            for (var _o = __values(this.objects.group.entries()), _p = _o.next(); !_p.done; _p = _o.next()) {
                var _q = __read(_p.value, 2), key = _q[0], group = _q[1];
                if (!group.name)
                    group.name = key;
                filer.writeStart('GROUP');
                group.dxfOut(filer, true);
            }
        }
        catch (e_16_1) { e_16 = { error: e_16_1 }; }
        finally {
            try {
                if (_p && !_p.done && (_b = _o.return)) _b.call(_o);
            }
            finally { if (e_16) throw e_16.error; }
        }
        try {
            for (var _r = __values(this.objects.sortentsTable.entries()), _s = _r.next(); !_s.done; _s = _r.next()) {
                var _t = __read(_s.value, 2), _ = _t[0], sortents = _t[1];
                filer.writeStart('SORTENTSTABLE');
                sortents.dxfOut(filer, true);
            }
        }
        catch (e_17_1) { e_17 = { error: e_17_1 }; }
        finally {
            try {
                if (_s && !_s.done && (_c = _r.return)) _c.call(_r);
            }
            finally { if (e_17) throw e_17.error; }
        }
        try {
            for (var _u = __values(this.objects.imageDefinition.entries()), _v = _u.next(); !_v.done; _v = _u.next()) {
                var _w = __read(_v.value, 2), _ = _w[0], imageDef = _w[1];
                filer.writeStart('IMAGEDEF');
                imageDef.dxfOut(filer, true);
            }
        }
        catch (e_18_1) { e_18 = { error: e_18_1 }; }
        finally {
            try {
                if (_v && !_v.done && (_d = _u.return)) _d.call(_u);
            }
            finally { if (e_18) throw e_18.error; }
        }
        try {
            for (var _x = __values(this.objects.layerFilter.entries()), _y = _x.next(); !_y.done; _y = _x.next()) {
                var _z = __read(_y.value, 2), _ = _z[0], layerFilter = _z[1];
                filer.writeStart('LAYER_FILTER');
                layerFilter.dxfOut(filer, true);
            }
        }
        catch (e_19_1) { e_19 = { error: e_19_1 }; }
        finally {
            try {
                if (_y && !_y.done && (_e = _x.return)) _e.call(_x);
            }
            finally { if (e_19) throw e_19.error; }
        }
        try {
            for (var _0 = __values(this.objects.layerIndex.entries()), _1 = _0.next(); !_1.done; _1 = _0.next()) {
                var _2 = __read(_1.value, 2), _ = _2[0], layerIndex = _2[1];
                filer.writeStart('LAYER_INDEX');
                layerIndex.dxfOut(filer, true);
            }
        }
        catch (e_20_1) { e_20 = { error: e_20_1 }; }
        finally {
            try {
                if (_1 && !_1.done && (_f = _0.return)) _f.call(_0);
            }
            finally { if (e_20) throw e_20.error; }
        }
        try {
            for (var _3 = __values(this.objects.mleaderStyle.entries()), _4 = _3.next(); !_4.done; _4 = _3.next()) {
                var _5 = __read(_4.value, 2), _ = _5[0], mleaderStyle = _5[1];
                filer.writeStart('MLEADERSTYLE');
                mleaderStyle.dxfOut(filer, true);
            }
        }
        catch (e_21_1) { e_21 = { error: e_21_1 }; }
        finally {
            try {
                if (_4 && !_4.done && (_g = _3.return)) _g.call(_3);
            }
            finally { if (e_21) throw e_21.error; }
        }
        try {
            for (var _6 = __values(this.objects.mlineStyle.entries()), _7 = _6.next(); !_7.done; _7 = _6.next()) {
                var _8 = __read(_7.value, 2), _ = _8[0], mlineStyle = _8[1];
                filer.writeStart('MLINESTYLE');
                mlineStyle.dxfOut(filer, true);
            }
        }
        catch (e_22_1) { e_22 = { error: e_22_1 }; }
        finally {
            try {
                if (_7 && !_7.done && (_h = _6.return)) _h.call(_6);
            }
            finally { if (e_22) throw e_22.error; }
        }
        try {
            for (var _9 = __values(this.objects.xrecord.entries()), _10 = _9.next(); !_10.done; _10 = _9.next()) {
                var _11 = __read(_10.value, 2), _ = _11[0], xrecord = _11[1];
                filer.writeStart('XRECORD');
                xrecord.dxfOut(filer, true);
            }
        }
        catch (e_23_1) { e_23 = { error: e_23_1 }; }
        finally {
            try {
                if (_10 && !_10.done && (_j = _9.return)) _j.call(_9);
            }
            finally { if (e_23) throw e_23.error; }
        }
        this.writeAcLyLayerFilterObjects(filer);
        filer.endSection();
    };
    /**
     * Writes Layer Manager filter tree objects (`ACAD_LAYERFILTERS` /
     * `ACLYDICTIONARY` + XRecords) into the OBJECTS section and attaches an
     * extension dictionary to the Layer Table.
     *
     * @param filer - DXF output writer.
     */
    AcDbDatabase.prototype.writeAcLyLayerFilterObjects = function (filer) {
        var e_24, _a, e_25, _b, e_26, _c, e_27, _d, e_28, _e;
        var _this = this;
        var _f;
        var serialized = acdbSerializeLayerFilterTree(this._layerFilters);
        if (serialized.nodes.length === 0) {
            return;
        }
        var dicts = [];
        var xrecs = [];
        var layerTable = this.tables.layerTable;
        var layerExtHandle = (_f = layerTable.extensionDictionary) !== null && _f !== void 0 ? _f : this.generateHandle();
        layerTable.extensionDictionary = layerExtHandle;
        var aclyHandle = this.generateHandle();
        var acadHandle = this.generateHandle();
        var rootAclyEntries = [];
        var rootAcadEntries = [];
        var emit = function (node, parentAclyHandle, addToLegacy) {
            var e_29, _a;
            var xHandle = _this.generateHandle();
            var extensionDictionaryId;
            if (node.children.length > 0) {
                var extHandle = _this.generateHandle();
                var childAclyHandle = _this.generateHandle();
                var childEntries = [];
                try {
                    for (var _b = __values(node.children), _c = _b.next(); !_c.done; _c = _b.next()) {
                        var child = _c.value;
                        childEntries.push({
                            name: child.key,
                            id: emit(child, childAclyHandle, false)
                        });
                    }
                }
                catch (e_29_1) { e_29 = { error: e_29_1 }; }
                finally {
                    try {
                        if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
                    }
                    finally { if (e_29) throw e_29.error; }
                }
                dicts.push({
                    handle: extHandle,
                    ownerId: xHandle,
                    entries: [{ name: ACLY_DICTIONARY_NAME, id: childAclyHandle }]
                });
                dicts.push({
                    handle: childAclyHandle,
                    ownerId: extHandle,
                    entries: childEntries
                });
                extensionDictionaryId = extHandle;
            }
            xrecs.push({
                handle: xHandle,
                ownerId: parentAclyHandle,
                extensionDictionaryId: extensionDictionaryId,
                data: node.data
            });
            if (addToLegacy) {
                rootAcadEntries.push({ name: node.key, id: xHandle });
            }
            return xHandle;
        };
        try {
            for (var _g = __values(serialized.nodes), _h = _g.next(); !_h.done; _h = _g.next()) {
                var node = _h.value;
                rootAclyEntries.push({
                    name: node.key,
                    id: emit(node, aclyHandle, true)
                });
            }
        }
        catch (e_24_1) { e_24 = { error: e_24_1 }; }
        finally {
            try {
                if (_h && !_h.done && (_a = _g.return)) _a.call(_g);
            }
            finally { if (e_24) throw e_24.error; }
        }
        var layerExtEntries = new Map();
        var existingLayerExt = this.getObjectById(layerExtHandle);
        if (existingLayerExt instanceof AcDbDictionary) {
            try {
                for (var _j = __values(existingLayerExt.entries()), _k = _j.next(); !_k.done; _k = _j.next()) {
                    var _l = __read(_k.value, 2), key = _l[0], value = _l[1];
                    layerExtEntries.set(key, value.objectId);
                }
            }
            catch (e_25_1) { e_25 = { error: e_25_1 }; }
            finally {
                try {
                    if (_k && !_k.done && (_b = _j.return)) _b.call(_j);
                }
                finally { if (e_25) throw e_25.error; }
            }
        }
        layerExtEntries.set(ACLY_DICTIONARY_NAME, aclyHandle);
        layerExtEntries.set(ACAD_LAYERFILTERS_NAME, acadHandle);
        dicts.unshift({
            handle: layerExtHandle,
            ownerId: layerTable.objectId,
            entries: Array.from(layerExtEntries, function (_a) {
                var _b = __read(_a, 2), name = _b[0], id = _b[1];
                return ({ name: name, id: id });
            })
        }, {
            handle: aclyHandle,
            ownerId: layerExtHandle,
            entries: rootAclyEntries
        }, {
            handle: acadHandle,
            ownerId: layerExtHandle,
            entries: rootAcadEntries
        });
        try {
            for (var dicts_1 = __values(dicts), dicts_1_1 = dicts_1.next(); !dicts_1_1.done; dicts_1_1 = dicts_1.next()) {
                var dict = dicts_1_1.value;
                filer.writeStart('DICTIONARY');
                filer.writeHandle(5, dict.handle);
                filer.writeObjectId(330, dict.ownerId);
                filer.writeSubclassMarker('AcDbDictionary');
                filer.writeInt16(280, 1);
                filer.writeInt16(281, 1);
                try {
                    for (var _m = (e_27 = void 0, __values(dict.entries)), _o = _m.next(); !_o.done; _o = _m.next()) {
                        var entry = _o.value;
                        filer.writeString(3, entry.name);
                        filer.writeObjectId(350, entry.id);
                    }
                }
                catch (e_27_1) { e_27 = { error: e_27_1 }; }
                finally {
                    try {
                        if (_o && !_o.done && (_d = _m.return)) _d.call(_m);
                    }
                    finally { if (e_27) throw e_27.error; }
                }
            }
        }
        catch (e_26_1) { e_26 = { error: e_26_1 }; }
        finally {
            try {
                if (dicts_1_1 && !dicts_1_1.done && (_c = dicts_1.return)) _c.call(dicts_1);
            }
            finally { if (e_26) throw e_26.error; }
        }
        try {
            for (var xrecs_1 = __values(xrecs), xrecs_1_1 = xrecs_1.next(); !xrecs_1_1.done; xrecs_1_1 = xrecs_1.next()) {
                var item = xrecs_1_1.value;
                var xrecord = new AcDbXrecord();
                xrecord.objectId = item.handle;
                xrecord.ownerId = item.ownerId;
                if (item.extensionDictionaryId) {
                    xrecord.extensionDictionary = item.extensionDictionaryId;
                }
                xrecord.data = acdbLayerGroupsToResultBuffer(item.data);
                filer.writeStart('XRECORD');
                xrecord.dxfOut(filer);
            }
        }
        catch (e_28_1) { e_28 = { error: e_28_1 }; }
        finally {
            try {
                if (xrecs_1_1 && !xrecs_1_1.done && (_e = xrecs_1.return)) _e.call(xrecs_1);
            }
            finally { if (e_28) throw e_28.error; }
        }
    };
    /**
     * Writes a single TABLE and its records into the TABLES section.
     *
     * @param filer - DXF output writer.
     * @param tableName - DXF table name (e.g. LAYER, LTYPE).
     * @param table - The symbol table instance.
     * @param records - Records to serialize.
     * @param recordType - DXF record type name for each table record.
     */
    AcDbDatabase.prototype.writeDxfTable = function (filer, tableName, table, records, recordType) {
        var e_30, _a;
        var items = __spreadArray([], __read(records), false);
        filer.startTable(tableName);
        table.dxfOut(filer);
        try {
            for (var items_1 = __values(items), items_1_1 = items_1.next(); !items_1_1.done; items_1_1 = items_1.next()) {
                var record = items_1_1.value;
                if (recordType === 'BLOCK_RECORD' &&
                    record instanceof AcDbBlockTableRecord) {
                    record.dxfOutBlockRecord(filer);
                    continue;
                }
                filer.writeStart(recordType);
                record.dxfOut(filer, true);
            }
        }
        catch (e_30_1) { e_30 = { error: e_30_1 }; }
        finally {
            try {
                if (items_1_1 && !items_1_1.done && (_a = items_1.return)) _a.call(items_1);
            }
            finally { if (e_30) throw e_30.error; }
        }
        filer.endTable();
    };
    /**
     * Writes a single entity record into the DXF stream.
     *
     * The entity is responsible for emitting any additional records (such as
     * VERTEX/SEQEND for polylines or ATTRIB/SEQEND for block references) inside
     * its own `dxfOut` override.
     *
     * @param filer - DXF output writer.
     * @param entity - Entity to serialize.
     */
    AcDbDatabase.prototype.writeDxfEntity = function (filer, entity) {
        if (entity instanceof AcDbPolyline &&
            !filer.capabilities.supportsLwPolyline) {
            filer.writeStart('POLYLINE');
            entity.dxfOutAs2dPolyline(filer, true);
            return;
        }
        filer.writeStart(entity.dxfTypeName);
        entity.dxfOut(filer, true);
    };
    /**
     * Writes THUMBNAILIMAGE when preview bytes are present and export requested.
     */
    AcDbDatabase.prototype.writeDxfThumbnailImageSection = function (filer, saveThumbnailImage) {
        if (saveThumbnailImage === void 0) { saveThumbnailImage = false; }
        var bytes = this._thumbnailImage;
        if (!saveThumbnailImage || !bytes || bytes.length === 0) {
            return;
        }
        if (!filer.capabilities.supportsObjectsSection) {
            // Thumbnail section is post-R12; skip for AC1009.
            return;
        }
        var chunkSize = 127;
        filer.startSection('THUMBNAILIMAGE');
        filer.writeInt32(90, bytes.length);
        for (var offset = 0; offset < bytes.length; offset += chunkSize) {
            var chunk = bytes.subarray(offset, offset + chunkSize);
            filer.writeString(310, acdbBytesToHexString(chunk));
        }
        filer.endSection();
    };
    /**
     * Clears all data from the database.
     *
     * This method removes all entities, tables, and objects from the database,
     * effectively resetting it to an empty state.
     *
     * @example
     * ```typescript
     * database.clear();
     * ```
     */
    AcDbDatabase.prototype.clear = function () {
        this.transactionManager.clearUndoStack();
        // Clear all tables and dictionaries
        this._classes = [];
        this._tables.blockTable.removeAll();
        this._tables.dimStyleTable.removeAll();
        this._tables.linetypeTable.removeAll();
        this._tables.textStyleTable.removeAll();
        this._tables.ucsTable.removeAll();
        this._tables.viewTable.removeAll();
        this._tables.layerTable.removeAll();
        this._tables.viewportTable.removeAll();
        this._tables.appIdTable.removeAll();
        this._objects.layout.removeAll();
        this._objects.group.removeAll();
        this._objects.imageDefinition.removeAll();
        this._objects.layerFilter.removeAll();
        this._objects.layerIndex.removeAll();
        this._objects.mleaderStyle.removeAll();
        this._objects.mlineStyle.removeAll();
        this._objects.sortentsTable.removeAll();
        this._objects.xrecord.removeAll();
        this._layerFilters = new AcLyLayerFilterTree();
        this._currentSpace = undefined;
        this._extents.makeEmpty();
        this.registerBootstrapHandles();
    };
    /**
     * Updates a sysvar value and dispatches the change event only when the value changed.
     */
    AcDbDatabase.prototype.updateSysVar = function (sysVarName, currentValue, nextValue, setter) {
        AcDbSysVarManager.instance().applyVarMutation(sysVarName, currentValue, nextValue, this, function () { return setter(nextValue); });
    };
    /**
     * Normalizes special linetype aliases to the internal canonical names.
     */
    AcDbDatabase.prototype.normalizeLinetypeName = function (value) {
        var normalizedValue = value.trim();
        if (normalizedValue.toUpperCase() === 'BYLAYER') {
            return ByLayer;
        }
        if (normalizedValue.toUpperCase() === 'BYBLOCK') {
            return ByBlock;
        }
        return normalizedValue;
    };
    /**
     * Triggers a system variable changed event with old/new values.
     */
    AcDbDatabase.prototype.triggerSysVarChangedEvent = function (sysVarName, oldValue, newValue) {
        var manager = AcDbSysVarManager.instance();
        var name = sysVarName.toLowerCase();
        var descriptor = manager.getDescriptor(name);
        if (descriptor == null) {
            return;
        }
        manager.events.sysVarChanged.dispatch({
            database: this,
            name: name,
            oldVal: oldValue,
            newVal: newValue
        });
    };
    /**
     * Extracts the file name from a URI.
     *
     * @param uri - The URI to extract the file name from
     * @returns The extracted file name, or empty string if extraction fails
     * @private
     */
    AcDbDatabase.prototype.getFileNameFromUri = function (uri) {
        try {
            // Create a new URL object
            var url = new URL(uri);
            // Get the pathname from the URL
            var pathParts = url.pathname.split('/');
            // Return the last part of the pathname as the file name
            return pathParts[pathParts.length - 1] || '';
        }
        catch (error) {
            console.error('Invalid URI:', error);
            return '';
        }
    };
    /** Sequence counter for default unsaved drawing names (Drawing1.dwg, Drawing2.dwg, ...). */
    AcDbDatabase._unsavedDrawingSequence = 0;
    return AcDbDatabase;
}(AcDbObject));
export { AcDbDatabase };
/* eslint-enable simple-import-sort/imports */
//# sourceMappingURL=AcDbDatabase.js.map