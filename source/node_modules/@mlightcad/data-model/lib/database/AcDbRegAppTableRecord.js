var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        if (typeof b !== "function" && b !== null)
            throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import { AcDbSymbolTableRecord } from './AcDbSymbolTableRecord';
/**
 * Represents records in the AcDbRegAppTable (known as the APPID symbol table in AutoCAD and DXF).
 * Each of these records represents an application ID used to identify a group of Extended Entity
 * Data attached to objects in the drawing database.
 */
var AcDbRegAppTableRecord = /** @class */ (function (_super) {
    __extends(AcDbRegAppTableRecord, _super);
    /**
     * Creates a new AcDbRegAppTableRecord instance.
     *
     * @param attrs - Input attribute values, or the application name as a string
     */
    function AcDbRegAppTableRecord(attrs) {
        if (typeof attrs === 'string') {
            attrs = { name: attrs };
        }
        return _super.call(this, attrs) || this;
    }
    /**
     * Writes DXF fields for this object.
     *
     * @param filer - DXF output writer.
     * @returns The instance (for chaining).
     */
    AcDbRegAppTableRecord.prototype.dxfOutFields = function (filer) {
        _super.prototype.dxfOutFields.call(this, filer);
        filer.writeSubclassMarker('AcDbRegAppTableRecord');
        filer.writeString(2, this.name);
        filer.writeInt16(70, 0);
        return this;
    };
    AcDbRegAppTableRecord.prototype.dxfInFields = function (filer) {
        _super.prototype.dxfInFields.call(this, filer);
        filer.atSubclassData('AcDbSymbolTableRecord');
        filer.atSubclassData('AcDbRegAppTableRecord');
        while (!filer.atEndOfObject && !filer.atEof && !filer.atExtendedData) {
            var item = filer.readItem();
            if (!item)
                break;
            var code = Number(item.code);
            if (code === 100) {
                filer.pushBackItem(item);
                break;
            }
            switch (code) {
                case 2:
                    this.name = String(item.value);
                    break;
                case 70:
                    // Standard flags — unused beyond presence.
                    break;
                default:
                    break;
            }
        }
        return this;
    };
    return AcDbRegAppTableRecord;
}(AcDbSymbolTableRecord));
export { AcDbRegAppTableRecord };
//# sourceMappingURL=AcDbRegAppTableRecord.js.map